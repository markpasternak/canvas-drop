/**
 * @canvas-drop/shared — cross-cutting types, schemas, and utilities shared
 * across the server, dashboard, and SDK.
 *
 * Real exports (zod env config, Drizzle schema, shared row types) land in
 * later units (U2, U4). This placeholder exists so workspace resolution and
 * typechecking are proven from U1 onward.
 */
export const VERSION = "0.0.0";
