/**
 * @canvas-drop/sdk — the zero-config browser SDK (global `canvasdrop`).
 *
 * The real SDK surface (kv, files, ai, realtime, me) is built in a later
 * plan (area J). This placeholder keeps the workspace package resolvable.
 */
export const SDK_VERSION = "0.0.0";
