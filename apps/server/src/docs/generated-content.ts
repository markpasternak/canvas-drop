// AUTO-GENERATED by scripts/build-docs.mjs from docs/site/. Do not edit by hand.
// Run `pnpm docs:build` to regenerate. CI asserts this file has no uncommitted drift.

export interface DocHeading { id: string; text: string; level: number; }
export interface DocPage { path: string; section: string; title: string; html: string; headings: DocHeading[]; text: string; }
export interface NavSection { section: string; pages: { path: string; title: string }[]; }
export interface SearchEntry { path: string; title: string; headings: string[]; text: string; }

export const DOC_PAGES: DocPage[] = [
  {
    "path": "",
    "section": "Getting started",
    "title": "Overview",
    "html": "<h1 id=\"canvas-drop\">canvas-drop</h1>\n<p>Deploy and share the small web tools your org builds, behind your sign-in.</p>\n<p>This page is for anyone deciding whether to run canvas-drop, or wondering what\nit does today. By the end you know what a canvas is, what ships, what is still\nopen, and which page to read next for your job.</p>\n<p>canvas-drop is an open-source (MIT), self-hostable platform where signed-in\nmembers of an organization publish and share small web artifacts called\n<strong>canvases</strong>. A canvas is static files (HTML, CSS, JS, images). Drop them in and\nthey are live at a URL your colleagues can open once they sign in. It is\ninspired by Shopify's Quick; canvas-drop is the open way to run the idea on your\nown infrastructure.</p>\n<p>The constraint is the product: a small, fixed set of primitives done well, not a\ngeneral-purpose hosting platform. There is no server-side build step and no\nsecret in the page. A canvas gains backend capability only through six fixed\nprimitives (KV, files, AI, identity, realtime, and admin-granted Connections) served by a zero-config browser\nSDK, and that backend stays off until the owner or an editor turns it on.</p>\n<h2 id=\"try-it\">Try it</h2>\n<p>A signed-in local instance with no external services (<code>path</code> URLs, SQLite, local\ndisk, <code>dev</code> auth). You need Node 24 or newer and pnpm:</p>\n<pre><code class=\"hljs language-sh\">git <span class=\"hljs-built_in\">clone</span> https://github.com/markpasternak/canvas-drop.git\n<span class=\"hljs-built_in\">cd</span> canvas-drop\npnpm install\n<span class=\"hljs-built_in\">cp</span> .env.example .<span class=\"hljs-built_in\">env</span>\npnpm dev\n<span class=\"hljs-comment\"># dashboard http://localhost:5173 · server http://localhost:3000</span></code></pre>\n<p>Open <code>http://localhost:5173</code>, click <strong>Create canvas</strong>, choose <strong>Paste HTML</strong>, and\npaste an <code>index.html</code>. It is created and published in one step and live at\n<code>http://localhost:3000/c/{slug}/</code>. To give it a backend, switch on <strong>Enable\nbackend</strong> in the canvas's <strong>Backend</strong> tab and add one tag to the page:</p>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">src</span>=<span class=\"hljs-string\">\"/sdk/v1.js\"</span>&gt;</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">type</span>=<span class=\"hljs-string\">\"module\"</span>&gt;</span><span class=\"language-javascript\">\n  <span class=\"hljs-keyword\">const</span> me = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-title function_\">me</span>();\n  <span class=\"hljs-keyword\">const</span> views = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">increment</span>(<span class=\"hljs-string\">\"views\"</span>);\n</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span></code></pre>\n<p>The <a href=\"/docs/quickstart\">Quickstart</a> walks the rest of the loop: deploy, edit,\nshare, roll back.</p>\n<p>For the production shape instead, <code>docker compose up --build</code> boots Caddy, an\nidentity-aware proxy (oauth2-proxy) signing in against a bundled demo identity\nprovider (Dex), Postgres, and the app in real <code>proxy</code> auth mode at\n<code>http://localhost:8080</code> (sign in as <code>demo@example.com</code> / <code>canvasdrop</code>). See\n<a href=\"/docs/self-hosting/install\">Self-hosting → Install</a>.</p>\n<h2 id=\"what-ships-today\">What ships today</h2>\n<table>\n<thead>\n<tr>\n<th>Area</th>\n<th>What you get</th>\n<th>Read more</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Publish</td>\n<td>Paste a single <code>index.html</code>, drag files or a folder, upload a <code>.zip</code>, or <code>PUT</code> a ZIP to the Deploy API with a per-canvas <code>cd_...</code> key. A staged upload sends only the files the server does not already have. Limits: 100 MB per canvas, 25 MB per file, 2 000 files.</td>\n<td><a href=\"/docs/authoring/create-and-publish\">Create &amp; publish</a>, <a href=\"/docs/api/deploy-api\">Deploy API</a></td>\n</tr>\n<tr>\n<td>Edit in the browser</td>\n<td>One mutable draft per canvas: file tree, code editor, autosave, inline preview, and on-page text editing when the draft is a single HTML file. <strong>Publish</strong> snapshots the draft into a version.</td>\n<td><a href=\"/docs/authoring/editor\">The editor</a></td>\n</tr>\n<tr>\n<td>Versions</td>\n<td>Every publish or deploy is an immutable version; the last 10 are kept. <strong>Make current</strong> rolls back or forward, <strong>Restore</strong> copies a version into the draft, any version downloads as a ZIP, and non-current versions can be deleted.</td>\n<td><a href=\"/docs/authoring/editor#versions\">Versions</a></td>\n</tr>\n<tr>\n<td>Share</td>\n<td>Name people and teams as viewers or editors — that list always applies — then pick General access: Restricted, Whole org, or Public link. Add a password or an expiry. Access is checked on every request and never cached, so a revoke takes effect on the next one. Public link is static-only: anonymous visitors get no primitives. With an org boundary configured (<code>CANVAS_DROP_ORG_NAME</code>), Whole org means the canvas's home org.</td>\n<td><a href=\"/docs/authoring/sharing\">Sharing &amp; access</a></td>\n</tr>\n<tr>\n<td>Roles</td>\n<td>Each person or team on the people list is a <strong>viewer</strong> or an <strong>editor</strong>. Editors edit, publish, roll back, change settings and sharing, and rotate the deploy key; deleting, transferring ownership, and the guest-AI switch stay with the owner (<code>OWNER_ONLY</code>). Only org members can be editors. A stale save is refused with <code>DRAFT_CONFLICT</code>, so two editors never overwrite each other silently. Ownership transfers to an editor in one step.</td>\n<td><a href=\"/docs/authoring/sharing#roles-viewers-and-editors\">Roles: viewers and editors</a></td>\n</tr>\n<tr>\n<td>Teams</td>\n<td>Any signed-in user can create a personal team; org members can create org-attached teams. Add people by email, then grant the team viewer or editor access to a canvas.</td>\n<td><a href=\"/docs/authoring/teams\">Teams</a></td>\n</tr>\n<tr>\n<td>Backend</td>\n<td>Turn on the backend per canvas, then toggle KV, files, AI, and realtime independently; identity (<code>canvasdrop.me()</code>) is on whenever the backend is. A call to a disabled primitive fails with <code>CAPABILITY_DISABLED</code>. A further opt-in, authoring, lets a page create canvases as the signed-in viewer.</td>\n<td><a href=\"/docs/authoring/capabilities\">Capabilities</a>, <a href=\"/docs/sdk/overview\">Browser SDK</a></td>\n</tr>\n<tr>\n<td>Connections</td>\n<td>An admin can define a reusable exact-origin HTTPS profile with selected methods and write-only protected headers, then grant it to specific canvases. Canvas code gets a bounded <code>fetch</code>-like API; no arbitrary backend code runs.</td>\n<td><a href=\"/docs/sdk/connections\">Connections</a></td>\n</tr>\n<tr>\n<td>Discover</td>\n<td><strong>Shared</strong> lists the canvases already opened to you. The <strong>Gallery</strong> is opt-in: Public-link canvases and Whole-org canvases their owners list, scoped to your org. An owner can also allow a listed canvas to be used as a template, which others clone.</td>\n<td><a href=\"/docs/authoring/sharing#gallery-description-and-tags\">Gallery, description, and tags</a></td>\n</tr>\n<tr>\n<td>Agents</td>\n<td>The MCP server at <code>{base}/mcp</code> (OAuth 2.1, 47 identity-scoped tools) covers everything the dashboard can do, with the same owner/editor/viewer checks. <code>{base}/llms.txt</code> and the agent skill (<code>{base}/skill.zip</code>) describe the whole surface for models.</td>\n<td><a href=\"/docs/agents/mcp\">MCP server</a>, <a href=\"/docs/agents/llms\">llms.txt</a>, <a href=\"/docs/agents/skill\">Agent skill</a></td>\n</tr>\n<tr>\n<td>Admin</td>\n<td>Disable or restore any canvas, reassign owners, feature canvases in the gallery, block users, grant public-link publishing per account, and edit runtime settings (AI key and quotas, screenshots, design skin) without a restart.</td>\n<td><a href=\"/docs/self-hosting/configuration\">Configuration</a></td>\n</tr>\n<tr>\n<td>Operate</td>\n<td>SQLite or Postgres, local disk or S3-compatible storage, <code>path</code> or <code>subdomain</code> URLs, <code>dev</code> / <code>proxy</code> / <code>oidc</code> auth: each is a config swap, never a code change. Docker image and compose stack; <code>pnpm backup</code>, <code>pnpm restore</code>, <code>pnpm purge</code>.</td>\n<td><a href=\"/docs/self-hosting/install\">Install</a>, <a href=\"/docs/self-hosting/deploy\">Deploy</a>, <a href=\"/docs/self-hosting/security-model\">Security model</a></td>\n</tr>\n</tbody></table>\n<h2 id=\"trust-posture\">Trust posture</h2>\n<ul>\n<li>Identity comes from the server-side auth context, never from the client. In\n<code>proxy</code> mode only the trusted proxy may assert it.</li>\n<li>Secrets stay server-side. AI provider keys, deploy keys, and Connection protected headers never reach the\nbrowser.</li>\n<li>The backend is off by default; each primitive is a per-canvas opt-in.</li>\n<li>Every canvas starts Restricted. Only its owner or an editor widens access, and\nPublic link is gated by an instance switch plus a per-account grant on the\nowner.</li>\n<li>Your infrastructure, your data. No telemetry, no phone-home.</li>\n</ul>\n<p>The <a href=\"/docs/self-hosting/security-model\">security model</a> states the threat model\n(a trusted org, not the hostile internet) and the tradeoffs, including path\nversus subdomain isolation.</p>\n<h2 id=\"status\">Status</h2>\n<p>v1 is feature-complete. Milestones M1 through M9 (foundation, hosting and\ndeploy, the dashboard, canvas management, the editor and version model, the five\nprimitives and SDK, admin and hardening, the gallery, AI and realtime) are\nshipped, along with the post-v1 work in the table above: the access ladder,\neditor roles and ownership transfer, teams and auth-delegated invites, Shared\ndiscovery, the MCP server, staged uploads, custom slugs, clone-as-template,\nusage stats, optional screenshots and preview covers, design skins, the\nauthoring capability, and admin-granted outbound Connections.</p>\n<p>Ops and packaging (M10) is the only open milestone. The Docker image and compose\nstack, vendor-neutral deploy docs, backup/restore tooling (with an automated\nround-trip test on both dialects), the security review, a secret scan in CI, and\nthe starter examples have shipped. Still deferred: a single-VPS load test and a\ncolleague pilot behind an identity-aware proxy. canvas-drop has not yet run under\na real org's load; treat it as ready to self-host and evaluate, not as\nbattle-tested.</p>\n<p>Not in v1: a CLI (the Deploy API, MCP server, and agent skill cover programmatic\ndeploys), custom domains, multi-org tenancy, an audit-log viewer in the admin\npanel, KV change subscriptions, and realtime message history.</p>\n<h2 id=\"where-to-go-next\">Where to go next</h2>\n<ul>\n<li>New here? Start with the <a href=\"/docs/quickstart\">Quickstart</a>.</li>\n<li>Building a canvas with a backend? Read the <a href=\"/docs/sdk/overview\">SDK overview</a>.</li>\n<li>Sharing with colleagues or adding editors? See <a href=\"/docs/authoring/sharing\">Sharing &amp; access</a>.</li>\n<li>Running your own instance? See <a href=\"/docs/self-hosting/install\">Self-hosting → Install</a>,\nthen <a href=\"/docs/self-hosting/configuration\">Configuration</a>.</li>\n<li>An AI agent? Read <a href=\"/llms.txt\"><code>/llms.txt</code></a>, then connect over the <a href=\"/docs/agents/mcp\">MCP server</a>.</li>\n</ul>\n<blockquote>\n<p>Examples and URLs in these docs use <code>{base}</code> (your instance's base URL) and\n<code>localhost</code> placeholders. Substitute your own instance's address.</p>\n</blockquote>\n",
    "headings": [
      {
        "id": "try-it",
        "text": "Try it",
        "level": 2
      },
      {
        "id": "what-ships-today",
        "text": "What ships today",
        "level": 2
      },
      {
        "id": "trust-posture",
        "text": "Trust posture",
        "level": 2
      },
      {
        "id": "status",
        "text": "Status",
        "level": 2
      },
      {
        "id": "where-to-go-next",
        "text": "Where to go next",
        "level": 2
      }
    ],
    "text": "canvas-drop Deploy and share the small web tools your org builds, behind your sign-in. This page is for anyone deciding whether to run canvas-drop, or wondering what it does today. By the end you know what a canvas is, what ships, what is still open, and which page to read next for your job. canvas-drop is an open-source (MIT), self-hostable platform where signed-in members of an organization publish and share small web artifacts called canvases . A canvas is static files (HTML, CSS, JS, images). Drop them in and they are live at a URL your colleagues can open once they sign in. It is inspired by Shopify's Quick; canvas-drop is the open way to run the idea on your own infrastructure. The constraint is the product: a small, fixed set of primitives done well, not a general-purpose hosting platform. There is no server-side build step and no secret in the page. A canvas gains backend capability only through six fixed primitives (KV, files, AI, identity, realtime, and admin-granted Connections) served by a zero-config browser SDK, and that backend stays off until the owner or an editor turns it on. Try it A signed-in local instance with no external services ( path URLs, SQLite, local disk, dev auth). You need Node 24 or newer and pnpm: git clone https://github.com/markpasternak/canvas-drop.git cd canvas-drop pnpm install cp .env.example . env pnpm dev # dashboard http://localhost:5173 · server http://localhost:3000 Open http://localhost:5173 , click Create canvas , choose Paste HTML , and paste an index.html . It is created and published in one step and live at http://localhost:3000/c/{slug}/ . To give it a backend, switch on Enable backend in the canvas's Backend tab and add one tag to the page: < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); const views = await canvasdrop. kv . increment ( \"views\" ); </ script > The Quickstart walks the rest of the loop: deploy, edit, share, roll back. For the production shape instead, docker compose up --build boots Caddy, an identity-aware proxy (oauth2-proxy) signing in against a bundled demo identity provider (Dex), Postgres, and the app in real proxy auth mode at http://localhost:8080 (sign in as demo@example.com / canvasdrop ). See Self-hosting → Install . What ships today Area What you get Read more Publish Paste a single index.html , drag files or a folder, upload a .zip , or PUT a ZIP to the Deploy API with a per-canvas cd_... key. A staged upload sends only the files the server does not already have. Limits: 100 MB per canvas, 25 MB per file, 2 000 files. Create & publish , Deploy API Edit in the browser One mutable draft per canvas: file tree, code editor, autosave, inline preview, and on-page text editing when the draft is a single HTML file. Publish snapshots the draft into a version. The editor Versions Every publish or deploy is an immutable version; the last 10 are kept. Make current rolls back or forward, Restore copies a version into the draft, any version downloads as a ZIP, and non-current versions can be deleted. Versions Share Name people and teams as viewers or editors — that list always applies — then pick General access: Restricted, Whole org, or Public link. Add a password or an expiry. Access is checked on every request and never cached, so a revoke takes effect on the next one. Public link is static-only: anonymous visitors get no primitives. With an org boundary configured ( CANVAS_DROP_ORG_NAME ), Whole org means the canvas's home org. Sharing & access Roles Each person or team on the people list is a viewer or an editor . Editors edit, publish, roll back, change settings and sharing, and rotate the deploy key; deleting, transferring ownership, and the guest-AI switch stay with the owner ( OWNER_ONLY ). Only org members can be editors. A stale save is refused with DRAFT_CONFLICT , so two editors never overwrite each other silently. Ownership transfers to an editor in one step. Roles: viewers and editors Teams Any signed-in user can create a personal team; org members can create org-attached teams. Add people by email, then grant the team viewer or editor access to a canvas. Teams Backend Turn on the backend per canvas, then toggle KV, files, AI, and realtime independently; identity ( canvasdrop.me() ) is on whenever the backend is. A call to a disabled primitive fails with CAPABILITY_DISABLED . A further opt-in, authoring, lets a page create canvases as the signed-in viewer. Capabilities , Browser SDK Connections An admin can define a reusable exact-origin HTTPS profile with selected methods and write-only protected headers, then grant it to specific canvases. Canvas code gets a bounded fetch -like API; no arbitrary backend code runs. Connections Discover Shared lists the canvases already opened to you. The Gallery is opt-in: Public-link canvases and Whole-org canvases their owners list, scoped to your org. An owner can also allow a listed canvas to be used as a template, which others clone. Gallery, description, and tags Agents The MCP server at {base}/mcp (OAuth 2.1, 47 identity-scoped tools) covers everything the dashboard can do, with the same owner/editor/viewer checks. {base}/llms.txt and the agent skill ( {base}/skill.zip ) describe the whole surface for models. MCP server , llms.txt , Agent skill Admin Disable or restore any canvas, reassign owners, feature canvases in the gallery, block users, grant public-link publishing per account, and edit runtime settings (AI key and quotas, screenshots, design skin) without a restart. Configuration Operate SQLite or Postgres, local disk or S3-compatible storage, path or subdomain URLs, dev / proxy / oidc auth: each is a config swap, never a code change. Docker image and compose stack; pnpm backup , pnpm restore , pnpm purge . Install , Deploy , Security model Trust posture Identity comes from the server-side auth context, never from the client. In proxy mode only the trusted proxy may assert it. Secrets stay server-side. AI provider keys, deploy keys, and Connection protected headers never reach the browser. The backend is off by default; each primitive is a per-canvas opt-in. Every canvas starts Restricted. Only its owner or an editor widens access, and Public link is gated by an instance switch plus a per-account grant on the owner. Your infrastructure, your data. No telemetry, no phone-home. The security model states the threat model (a trusted org, not the hostile internet) and the tradeoffs, including path versus subdomain isolation. Status v1 is feature-complete. Milestones M1 through M9 (foundation, hosting and deploy, the dashboard, canvas management, the editor and version model, the five primitives and SDK, admin and hardening, the gallery, AI and realtime) are shipped, along with the post-v1 work in the table above: the access ladder, editor roles and ownership transfer, teams and auth-delegated invites, Shared discovery, the MCP server, staged uploads, custom slugs, clone-as-template, usage stats, optional screenshots and preview covers, design skins, the authoring capability, and admin-granted outbound Connections. Ops and packaging (M10) is the only open milestone. The Docker image and compose stack, vendor-neutral deploy docs, backup/restore tooling (with an automated round-trip test on both dialects), the security review, a secret scan in CI, and the starter examples have shipped. Still deferred: a single-VPS load test and a colleague pilot behind an identity-aware proxy. canvas-drop has not yet run under a real org's load; treat it as ready to self-host and evaluate, not as battle-tested. Not in v1: a CLI (the Deploy API, MCP server, and agent skill cover programmatic deploys), custom domains, multi-org tenancy, an audit-log viewer in the admin panel, KV change subscriptions, and realtime message history. Where to go next New here? Start with the Quickstart . Building a canvas with a backend? Read the SDK overview . Sharing with colleagues or adding editors? See Sharing & access . Running your own instance? See Self-hosting → Install , then Configuration . An AI agent? Read /llms.txt , then connect over the MCP server . Examples and URLs in these docs use {base} (your instance's base URL) and localhost placeholders. Substitute your own instance's address."
  },
  {
    "path": "quickstart",
    "section": "Getting started",
    "title": "Quickstart",
    "html": "<h1 id=\"quickstart\">Quickstart</h1>\n<p>For anyone running canvas-drop for the first time: by the end of this page you\nhave an instance on your machine, a canvas at a live URL, and the three ways to\nput content on it (the dashboard, the editor, and the keyed Deploy API). The\ndefault profile needs no external services: SQLite, local disk storage, <code>path</code>\nURL mode, and <code>dev</code> auth that signs you in automatically.</p>\n<h2 id=\"run-an-instance-locally\">Run an instance locally</h2>\n<p>You need Node 24 or newer (<code>.nvmrc</code> says <code>24</code>) and pnpm; the repo pins\n<code>pnpm@11.0.9</code> in <code>package.json</code>.</p>\n<pre><code class=\"hljs language-sh\">git <span class=\"hljs-built_in\">clone</span> https://github.com/markpasternak/canvas-drop.git\n<span class=\"hljs-built_in\">cd</span> canvas-drop\npnpm install\n<span class=\"hljs-built_in\">cp</span> .env.example .<span class=\"hljs-built_in\">env</span>\npnpm dev</code></pre>\n<table>\n<thead>\n<tr>\n<th>URL</th>\n<th>What it serves</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>http://localhost:5173</code></td>\n<td>The dashboard (Vite dev server with HMR). It proxies <code>/api</code>, <code>/auth</code>, <code>/v1</code>, <code>/docs</code>, <code>/llms.txt</code>, <code>/skill.zip</code>, <code>/welcome</code>, and <code>/og.png</code> to the server.</td>\n</tr>\n<tr>\n<td><code>http://localhost:3000</code></td>\n<td>The Hono server: management API, Deploy API, MCP at <code>/mcp</code>, docs, and the canvases themselves at <code>/c/{slug}/</code>. Canvas URLs always point here, not at <code>:5173</code>.</td>\n</tr>\n</tbody></table>\n<pre><code class=\"hljs language-sh\">curl http://localhost:3000/healthz\n<span class=\"hljs-comment\"># 200 {\"status\":\"ok\",\"db\":\"ok\",\"version\":\"...\"}   (503 \"degraded\" until the DB answers)</span></code></pre>\n<p>What <code>pnpm dev</code> does: it loads <code>.env</code> once (Node's <code>--env-file-if-exists</code>, so\nvariables you export in the shell still win), seeds an empty database with\nsample canvases, then runs the server (<code>tsx watch</code>), the dashboard (<code>vite</code>), and\nthe browser SDK build (esbuild <code>--watch</code>) in parallel. Only <code>pnpm dev</code> reads\n<code>.env</code>; a production process takes its config from the environment (see\n<a href=\"/docs/self-hosting/configuration\">Configuration</a>). In production the server\nserves the built dashboard itself on <code>:3000</code>; the Vite server is dev-only.</p>\n<p>The sample data is 100 canvases with no files across seven owners, 70 of them\nyours, so the dashboard, <strong>Shared</strong>, and the gallery are populated on first\nboot. The seed runs only when the database has no canvases.</p>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Effect</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>pnpm dev:stop</code></td>\n<td>Stops the whole tree (Ctrl-C works too). <code>pnpm dev:restart</code> restarts it.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_DEV_SEED=0 pnpm dev</code></td>\n<td>Starts without the sample canvases.</td>\n</tr>\n<tr>\n<td><code>pnpm reset:data</code></td>\n<td>Deletes the local SQLite file and the local storage directory. <code>pnpm dev:fresh</code> resets, reseeds, and starts.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_PORT=3001 pnpm dev</code></td>\n<td>Moves the server if <code>:3000</code> is taken; the dashboard proxy follows. <code>CANVAS_DROP_DASHBOARD_PORT</code> moves the dashboard.</td>\n</tr>\n</tbody></table>\n<p>Data lives at <code>./data/canvasdrop.db</code> (SQLite) and <code>./data/storage</code> (uploaded\nfiles). Both paths are the defaults of <code>CANVAS_DROP_SQLITE_PATH</code> and\n<code>CANVAS_DROP_STORAGE_PATH</code>.</p>\n<h3 id=\"who-you-are-signed-in-as\">Who you are signed in as</h3>\n<p>In <code>dev</code> auth mode every request is <code>dev@example.com</code> (<code>Dev User</code>), and that\naccount is an admin: <code>CANVAS_DROP_ADMIN_EMAILS</code> defaults to the dev user in this\nmode. Override the identity with <code>CANVAS_DROP_DEV_USER_EMAIL</code> and\n<code>CANVAS_DROP_DEV_USER_NAME</code> in <code>.env</code>. <code>dev</code> mode refuses to boot when\n<code>NODE_ENV=production</code>; a real instance uses <code>proxy</code> or <code>oidc</code> auth, and can swap\nin Postgres (<code>CANVAS_DROP_DB=postgres</code>) and S3-compatible storage\n(<code>CANVAS_DROP_STORAGE=s3</code>). Each of those is a config change, not a code change:\nsee <a href=\"/docs/self-hosting/configuration\">Configuration</a> and\n<a href=\"/docs/self-hosting/install\">Install</a>.</p>\n<h3 id=\"prefer-docker\">Prefer Docker?</h3>\n<p><code>docker compose up --build</code> starts a demo stack at <code>http://localhost:8080</code>: the\napp in real <code>proxy</code> auth mode behind oauth2-proxy and a bundled Dex identity\nprovider, plus Postgres. Sign in as <code>demo@example.com</code> / <code>canvasdrop</code>. Only\nCaddy publishes a port; the app itself is unreachable from the host. Details\nand the production shape are in <a href=\"/docs/self-hosting/install\">Install</a>.</p>\n<h2 id=\"create-a-canvas\">Create a canvas</h2>\n<p>Click <strong>Create canvas</strong> in the dashboard (or open <code>/new</code>). Pick a source:</p>\n<table>\n<thead>\n<tr>\n<th>Source</th>\n<th>What happens</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Paste HTML</strong></td>\n<td>A single <code>index.html</code>, created and published in one step.</td>\n</tr>\n<tr>\n<td><strong>Files or folder</strong></td>\n<td>Drag files or a folder; relative paths are kept from the canvas root.</td>\n</tr>\n<tr>\n<td><strong>Upload ZIP</strong></td>\n<td>Extracted server-side; dotfiles are stripped, paths that escape the root are rejected.</td>\n</tr>\n<tr>\n<td><strong>Use the API</strong></td>\n<td>Mints an empty canvas plus a per-canvas key (shown once) and a ready-to-run <code>curl</code> command. The canvas stays unpublished until your first deploy.</td>\n</tr>\n</tbody></table>\n<p>On the same screen you set a title, an optional custom slug (checked live for\navailability), the audience the canvas opens to once it publishes (<strong>Restricted</strong>\nby default), and <strong>Enable backend (optional)</strong>. The first three sources publish\nimmediately.</p>\n<p>Every canvas gets a slug and a URL. The default slug is random\n(<code>quiet-otter-x7k2</code>); a custom slug is lowercase letters, digits, and hyphens,\n1 to 63 characters, no leading or trailing hyphen, with a reserved-word list\n(<code>api</code>, <code>admin</code>, <code>docs</code>, <code>mcp</code>, and similar). Change it later under\n<strong>Settings → URL &amp; routing → Change slug</strong>; leave the field empty for a fresh\nrandom one. The old URL stops working immediately.</p>\n<table>\n<thead>\n<tr>\n<th>URL mode</th>\n<th>Canvas URL</th>\n<th>Locally</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>path</code> (default)</td>\n<td><code>{base}/c/{slug}/</code></td>\n<td><code>http://localhost:3000/c/quiet-otter-x7k2/</code></td>\n</tr>\n<tr>\n<td><code>subdomain</code></td>\n<td><code>{slug}.{baseHost}</code></td>\n<td>not available on <code>localhost</code>; for example <code>quiet-otter-x7k2.canvases.example.com</code></td>\n</tr>\n</tbody></table>\n<h2 id=\"add-content\">Add content</h2>\n<p>A canvas is static files, served as-is. No build step runs on the server. The\npage at the canvas URL is the <code>index.html</code> at the root (a canvas whose only\nHTML file sits elsewhere serves that file instead):</p>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-meta\">&lt;!doctype <span class=\"hljs-keyword\">html</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">html</span>&gt;</span>\n  <span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">body</span>&gt;</span>\n    <span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">h1</span>&gt;</span>Hello from my canvas<span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">h1</span>&gt;</span>\n  <span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">body</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">html</span>&gt;</span></code></pre>\n<p>Limits: 100 MB per canvas, 25 MB per file, 2 000 files. Each publish creates an\nimmutable version; the last 10 are kept, and identical files are stored once.</p>\n<h2 id=\"give-it-a-backend-optional\">Give it a backend (optional)</h2>\n<p>The browser SDK gives a canvas six fixed primitives (KV, files, AI, identity,\nrealtime, and admin-granted Connections) with no keys in the page. Identity comes from the signed-in session,\nand the SDK works out which canvas it is running in from the page URL:</p>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">src</span>=<span class=\"hljs-string\">\"/sdk/v1.js\"</span>&gt;</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">type</span>=<span class=\"hljs-string\">\"module\"</span>&gt;</span><span class=\"language-javascript\">\n  <span class=\"hljs-keyword\">const</span> me = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-title function_\">me</span>();\n  <span class=\"hljs-keyword\">const</span> views = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">increment</span>(<span class=\"hljs-string\">\"views\"</span>); <span class=\"hljs-comment\">// 1, then 2, ...</span>\n</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span></code></pre>\n<p>The backend is off by default. Turn on <strong>Enable backend</strong> in the canvas's\n<strong>Backend</strong> tab, then the primitives you need: Key-value storage, File storage,\nAI, Realtime, and Authoring (a canvas that creates canvases; off by default per\ncanvas and per instance, <code>CANVAS_DROP_AUTHORING</code> defaults to <code>off</code>). Identity\n(<code>me()</code>) is on whenever the backend is on. A primitive is live only when the\nbackend is on, its own toggle is on, and the operator has it available: AI\nneeds <code>CANVAS_DROP_AI_API_KEY</code> set on the server, realtime needs\n<code>CANVAS_DROP_REALTIME=on</code> (the default). A call to a primitive that is not live\nthrows <code>CapabilityDisabledError</code>. See the <a href=\"/docs/sdk/overview\">SDK overview</a> and\n<a href=\"/docs/authoring/capabilities\">Capabilities</a>.</p>\n<p>Connections are granted separately by an administrator. They let a canvas call one\npre-approved HTTPS origin with controlled methods and protected headers—for example, a\nstock-data API that requires a server-side credential or fixed <code>User-Agent</code>. See\n<a href=\"/docs/sdk/connections\">Connections</a>.</p>\n<h2 id=\"publish-and-share\">Publish and share</h2>\n<p>Two words mean different things here. <strong>Publish</strong> turns the editor's draft into\na new immutable version. <strong>Deploy</strong> (folder, ZIP, paste, or the Deploy API)\npublishes a version directly, with no draft step; the <strong>New version</strong> button on\nevery detail tab does this for an existing canvas.</p>\n<p>The <strong>Editor</strong> tab works on a mutable draft with autosave. <strong>Publish</strong> (⌘↵ or\nCtrl+Enter) snapshots it into a new version and points the canvas URL at it.\nTwo people editing the same file cannot silently overwrite each other; a stale\nsave shows a conflict banner instead. The <strong>Versions</strong> tab lists every kept\nversion: <strong>Make current</strong> re-points the live URL at it (in either direction),\n<strong>Restore</strong> copies one back into the draft, <strong>Download ZIP</strong> exports it.</p>\n<p>The <strong>Sharing and permissions</strong> page sets who can open the canvas, in two parts. <strong>People and\nteams with direct access</strong> names people (by email) and teams, each as a <strong>viewer</strong> or an <strong>editor</strong>; that\nlist always applies, and a brand-new email becomes live only after that person's first\nverified sign-in. <strong>General access</strong> says who <em>else</em> can open the URL (opening it to the\norg or the public needs a published version):</p>\n<table>\n<thead>\n<tr>\n<th>General access</th>\n<th>Who else can open the URL</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Restricted</strong> (default)</td>\n<td>Nobody beyond you, your editors, and the people and teams on the list. A non-owner admin is treated like any member and gets a 404.</td>\n</tr>\n<tr>\n<td><strong>Whole org</strong></td>\n<td>Any signed-in member with the link.</td>\n</tr>\n<tr>\n<td><strong>Public link</strong></td>\n<td>Anyone with the link, static files only: the primitives are refused for anonymous visitors. Needs the instance's public-links switch (on by default) and the owner account's public-link permission (granted by default, revocable by an admin).</td>\n</tr>\n</tbody></table>\n<p>On top of any choice you can add a password gate or a share expiry. People and teams\non the list see the canvas in <strong>Shared</strong> at once; a Whole-org share is link-only until\nyou turn on <strong>List for your org</strong>. The gallery is\nnarrower: it lists only Public-link canvases and Whole-org canvases whose\nowners turn on <strong>List in the gallery</strong>. Revoking access takes effect on the next\nrequest. Details in <a href=\"/docs/authoring/sharing\">Sharing &amp; access</a>.</p>\n<h2 id=\"deploy-as-an-agent\">Deploy as an agent</h2>\n<p>Scripts and agents deploy over HTTP with the per-canvas key and no dashboard\nsession. The key is shown once at creation; an owner or editor can mint a new\none under <strong>Settings → Regenerate key</strong>.</p>\n<pre><code class=\"hljs language-sh\">curl -fsS -X PUT <span class=\"hljs-string\">\"http://localhost:3000/v1/canvases/{id}/deploy\"</span> \\\n  -H <span class=\"hljs-string\">\"Authorization: Bearer <span class=\"hljs-variable\">$CANVAS_KEY</span>\"</span> \\\n  --data-binary @site.zip\n<span class=\"hljs-comment\"># 200 {\"url\":\"http://localhost:3000/c/quiet-otter-x7k2/\",\"version\":1,\"fileCount\":3,\"totalBytes\":4096,\"warnings\":[]}</span></code></pre>\n<p>The key operates only on its own canvas and never belongs in canvas files (a\ndeploy that embeds one returns a warning). Deploys are limited to 10 per minute\nper canvas. The same surface offers <code>GET /v1/canvases/{id}/files</code> to read the\nlive version back, <code>POST …/rollback</code>, <code>POST …/unpublish</code>, and a staged upload\n(begin, send only the blobs the server is missing, finalize) for large or\nfrequently redeployed canvases. See the <a href=\"/docs/api/deploy-api\">Deploy API</a>.</p>\n<p>Agents can also connect once to the instance's <a href=\"/docs/agents/mcp\">MCP server</a>\nat <code>{base}/mcp</code> (on by default; OAuth 2.1 through your own sign-in) for the\nfull owner and editor surface, install the <a href=\"/docs/agents/skill\">agent skill</a>,\nor read the whole contract at <a href=\"/llms.txt\"><code>/llms.txt</code></a>.</p>\n<h2 id=\"where-next\">Where next</h2>\n<ul>\n<li>Authors: <a href=\"/docs/authoring/create-and-publish\">Create &amp; publish</a>, <a href=\"/docs/authoring/editor\">The editor</a>, <a href=\"/docs/authoring/teams\">Teams</a>.</li>\n<li>Operators: <a href=\"/docs/self-hosting/install\">Install</a>, <a href=\"/docs/self-hosting/configuration\">Configuration</a>, <a href=\"/docs/self-hosting/security-model\">Security model</a>, <a href=\"/docs/self-hosting/deploy\">Deploy</a>.</li>\n</ul>\n",
    "headings": [
      {
        "id": "run-an-instance-locally",
        "text": "Run an instance locally",
        "level": 2
      },
      {
        "id": "who-you-are-signed-in-as",
        "text": "Who you are signed in as",
        "level": 3
      },
      {
        "id": "prefer-docker",
        "text": "Prefer Docker?",
        "level": 3
      },
      {
        "id": "create-a-canvas",
        "text": "Create a canvas",
        "level": 2
      },
      {
        "id": "add-content",
        "text": "Add content",
        "level": 2
      },
      {
        "id": "give-it-a-backend-optional",
        "text": "Give it a backend (optional)",
        "level": 2
      },
      {
        "id": "publish-and-share",
        "text": "Publish and share",
        "level": 2
      },
      {
        "id": "deploy-as-an-agent",
        "text": "Deploy as an agent",
        "level": 2
      },
      {
        "id": "where-next",
        "text": "Where next",
        "level": 2
      }
    ],
    "text": "Quickstart For anyone running canvas-drop for the first time: by the end of this page you have an instance on your machine, a canvas at a live URL, and the three ways to put content on it (the dashboard, the editor, and the keyed Deploy API). The default profile needs no external services: SQLite, local disk storage, path URL mode, and dev auth that signs you in automatically. Run an instance locally You need Node 24 or newer ( .nvmrc says 24 ) and pnpm; the repo pins pnpm@11.0.9 in package.json . git clone https://github.com/markpasternak/canvas-drop.git cd canvas-drop pnpm install cp .env.example . env pnpm dev URL What it serves http://localhost:5173 The dashboard (Vite dev server with HMR). It proxies /api , /auth , /v1 , /docs , /llms.txt , /skill.zip , /welcome , and /og.png to the server. http://localhost:3000 The Hono server: management API, Deploy API, MCP at /mcp , docs, and the canvases themselves at /c/{slug}/ . Canvas URLs always point here, not at :5173 . curl http://localhost:3000/healthz # 200 {\"status\":\"ok\",\"db\":\"ok\",\"version\":\"...\"} (503 \"degraded\" until the DB answers) What pnpm dev does: it loads .env once (Node's --env-file-if-exists , so variables you export in the shell still win), seeds an empty database with sample canvases, then runs the server ( tsx watch ), the dashboard ( vite ), and the browser SDK build (esbuild --watch ) in parallel. Only pnpm dev reads .env ; a production process takes its config from the environment (see Configuration ). In production the server serves the built dashboard itself on :3000 ; the Vite server is dev-only. The sample data is 100 canvases with no files across seven owners, 70 of them yours, so the dashboard, Shared , and the gallery are populated on first boot. The seed runs only when the database has no canvases. Command Effect pnpm dev:stop Stops the whole tree (Ctrl-C works too). pnpm dev:restart restarts it. CANVAS_DROP_DEV_SEED=0 pnpm dev Starts without the sample canvases. pnpm reset:data Deletes the local SQLite file and the local storage directory. pnpm dev:fresh resets, reseeds, and starts. CANVAS_DROP_PORT=3001 pnpm dev Moves the server if :3000 is taken; the dashboard proxy follows. CANVAS_DROP_DASHBOARD_PORT moves the dashboard. Data lives at ./data/canvasdrop.db (SQLite) and ./data/storage (uploaded files). Both paths are the defaults of CANVAS_DROP_SQLITE_PATH and CANVAS_DROP_STORAGE_PATH . Who you are signed in as In dev auth mode every request is dev@example.com ( Dev User ), and that account is an admin: CANVAS_DROP_ADMIN_EMAILS defaults to the dev user in this mode. Override the identity with CANVAS_DROP_DEV_USER_EMAIL and CANVAS_DROP_DEV_USER_NAME in .env . dev mode refuses to boot when NODE_ENV=production ; a real instance uses proxy or oidc auth, and can swap in Postgres ( CANVAS_DROP_DB=postgres ) and S3-compatible storage ( CANVAS_DROP_STORAGE=s3 ). Each of those is a config change, not a code change: see Configuration and Install . Prefer Docker? docker compose up --build starts a demo stack at http://localhost:8080 : the app in real proxy auth mode behind oauth2-proxy and a bundled Dex identity provider, plus Postgres. Sign in as demo@example.com / canvasdrop . Only Caddy publishes a port; the app itself is unreachable from the host. Details and the production shape are in Install . Create a canvas Click Create canvas in the dashboard (or open /new ). Pick a source: Source What happens Paste HTML A single index.html , created and published in one step. Files or folder Drag files or a folder; relative paths are kept from the canvas root. Upload ZIP Extracted server-side; dotfiles are stripped, paths that escape the root are rejected. Use the API Mints an empty canvas plus a per-canvas key (shown once) and a ready-to-run curl command. The canvas stays unpublished until your first deploy. On the same screen you set a title, an optional custom slug (checked live for availability), the audience the canvas opens to once it publishes ( Restricted by default), and Enable backend (optional) . The first three sources publish immediately. Every canvas gets a slug and a URL. The default slug is random ( quiet-otter-x7k2 ); a custom slug is lowercase letters, digits, and hyphens, 1 to 63 characters, no leading or trailing hyphen, with a reserved-word list ( api , admin , docs , mcp , and similar). Change it later under Settings → URL & routing → Change slug ; leave the field empty for a fresh random one. The old URL stops working immediately. URL mode Canvas URL Locally path (default) {base}/c/{slug}/ http://localhost:3000/c/quiet-otter-x7k2/ subdomain {slug}.{baseHost} not available on localhost ; for example quiet-otter-x7k2.canvases.example.com Add content A canvas is static files, served as-is. No build step runs on the server. The page at the canvas URL is the index.html at the root (a canvas whose only HTML file sits elsewhere serves that file instead): <!doctype html > < html > < body > < h1 > Hello from my canvas </ h1 > </ body > </ html > Limits: 100 MB per canvas, 25 MB per file, 2 000 files. Each publish creates an immutable version; the last 10 are kept, and identical files are stored once. Give it a backend (optional) The browser SDK gives a canvas six fixed primitives (KV, files, AI, identity, realtime, and admin-granted Connections) with no keys in the page. Identity comes from the signed-in session, and the SDK works out which canvas it is running in from the page URL: < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); const views = await canvasdrop. kv . increment ( \"views\" ); // 1, then 2, ... </ script > The backend is off by default. Turn on Enable backend in the canvas's Backend tab, then the primitives you need: Key-value storage, File storage, AI, Realtime, and Authoring (a canvas that creates canvases; off by default per canvas and per instance, CANVAS_DROP_AUTHORING defaults to off ). Identity ( me() ) is on whenever the backend is on. A primitive is live only when the backend is on, its own toggle is on, and the operator has it available: AI needs CANVAS_DROP_AI_API_KEY set on the server, realtime needs CANVAS_DROP_REALTIME=on (the default). A call to a primitive that is not live throws CapabilityDisabledError . See the SDK overview and Capabilities . Connections are granted separately by an administrator. They let a canvas call one pre-approved HTTPS origin with controlled methods and protected headers—for example, a stock-data API that requires a server-side credential or fixed User-Agent . See Connections . Publish and share Two words mean different things here. Publish turns the editor's draft into a new immutable version. Deploy (folder, ZIP, paste, or the Deploy API) publishes a version directly, with no draft step; the New version button on every detail tab does this for an existing canvas. The Editor tab works on a mutable draft with autosave. Publish (⌘↵ or Ctrl+Enter) snapshots it into a new version and points the canvas URL at it. Two people editing the same file cannot silently overwrite each other; a stale save shows a conflict banner instead. The Versions tab lists every kept version: Make current re-points the live URL at it (in either direction), Restore copies one back into the draft, Download ZIP exports it. The Sharing and permissions page sets who can open the canvas, in two parts. People and teams with direct access names people (by email) and teams, each as a viewer or an editor ; that list always applies, and a brand-new email becomes live only after that person's first verified sign-in. General access says who else can open the URL (opening it to the org or the public needs a published version): General access Who else can open the URL Restricted (default) Nobody beyond you, your editors, and the people and teams on the list. A non-owner admin is treated like any member and gets a 404. Whole org Any signed-in member with the link. Public link Anyone with the link, static files only: the primitives are refused for anonymous visitors. Needs the instance's public-links switch (on by default) and the owner account's public-link permission (granted by default, revocable by an admin). On top of any choice you can add a password gate or a share expiry. People and teams on the list see the canvas in Shared at once; a Whole-org share is link-only until you turn on List for your org . The gallery is narrower: it lists only Public-link canvases and Whole-org canvases whose owners turn on List in the gallery . Revoking access takes effect on the next request. Details in Sharing & access . Deploy as an agent Scripts and agents deploy over HTTP with the per-canvas key and no dashboard session. The key is shown once at creation; an owner or editor can mint a new one under Settings → Regenerate key . curl -fsS -X PUT \"http://localhost:3000/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip # 200 {\"url\":\"http://localhost:3000/c/quiet-otter-x7k2/\",\"version\":1,\"fileCount\":3,\"totalBytes\":4096,\"warnings\":[]} The key operates only on its own canvas and never belongs in canvas files (a deploy that embeds one returns a warning). Deploys are limited to 10 per minute per canvas. The same surface offers GET /v1/canvases/{id}/files to read the live version back, POST …/rollback , POST …/unpublish , and a staged upload (begin, send only the blobs the server is missing, finalize) for large or frequently redeployed canvases. See the Deploy API . Agents can also connect once to the instance's MCP server at {base}/mcp (on by default; OAuth 2.1 through your own sign-in) for the full owner and editor surface, install the agent skill , or read the whole contract at /llms.txt . Where next Authors: Create & publish , The editor , Teams . Operators: Install , Configuration , Security model , Deploy ."
  },
  {
    "path": "authoring/create-and-publish",
    "section": "Authoring canvases",
    "title": "Create & publish",
    "html": "<h1 id=\"create-amp-publish\">Create &amp; publish</h1>\n<p>You have HTML, a folder of static files, or a build directory, and you want a live URL that people in your org can open. By the end of this page you will have published a canvas, know what a publish does, and be able to control its URL.</p>\n<p>The shortest path, from a signed-in dashboard:</p>\n<ol>\n<li>Open <code>/new</code> and pick <strong>Paste HTML</strong>.</li>\n<li>Paste your markup, optionally give it a title, and click <strong>Create and publish</strong>.</li>\n<li>Save the key from the dialog (it is shown once), then <strong>Go to canvas</strong>. The <strong>Overview</strong> tab has the URL: <code>{base}/c/{slug}/</code> in <code>path</code> mode, <code>https://{slug}.canvases.example.com/</code> in <code>subdomain</code> mode.</li>\n</ol>\n<p>The same from a terminal, with the canvas id and key shown when you create a canvas:</p>\n<pre><code class=\"hljs language-bash\">curl -fsS -X PUT <span class=\"hljs-string\">\"{base}/v1/canvases/{id}/deploy\"</span> \\\n  -H <span class=\"hljs-string\">\"Authorization: Bearer <span class=\"hljs-variable\">$CANVAS_KEY</span>\"</span> \\\n  --data-binary @site.zip\n<span class=\"hljs-comment\"># 200 {\"url\":\"…\",\"version\":1,\"fileCount\":12,\"totalBytes\":48213,\"warnings\":[]}</span></code></pre>\n<h2 id=\"ways-in\">Ways in</h2>\n<table>\n<thead>\n<tr>\n<th>Source</th>\n<th>Where</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Paste HTML</strong></td>\n<td><code>/new</code>, or <strong>New version</strong> on a canvas</td>\n<td>Stores your markup as <code>index.html</code> and publishes it in one step</td>\n</tr>\n<tr>\n<td><strong>Files or folder</strong></td>\n<td><code>/new</code>, or <strong>New version</strong></td>\n<td>Uploads with relative paths preserved and publishes</td>\n</tr>\n<tr>\n<td><strong>Upload ZIP</strong></td>\n<td><code>/new</code>, or <strong>New version</strong></td>\n<td>Extracts the archive server-side and publishes</td>\n</tr>\n<tr>\n<td><strong>Use the API</strong></td>\n<td><code>/new</code></td>\n<td>Mints an empty canvas plus its key; the first <code>PUT …/deploy</code> publishes</td>\n</tr>\n<tr>\n<td><strong>Editor</strong></td>\n<td>The <strong>Editor</strong> tab</td>\n<td>Autosaves a <strong>draft</strong>; you click <strong>Publish</strong> when ready</td>\n</tr>\n<tr>\n<td><strong>Deploy API / MCP</strong></td>\n<td>Scripts and agents</td>\n<td>Publishes directly to live with a key or over MCP</td>\n</tr>\n</tbody></table>\n<pre class=\"mermaid\">flowchart TD\n    subgraph Sources[\"Ways in\"]\n      DnD[Files or folder]\n      Zip[Upload ZIP]\n      Paste[Paste HTML]\n      API[Deploy API / MCP]\n      Ed[Editor]\n    end\n    DnD --&gt; Pub{Publish}\n    Zip --&gt; Pub\n    Paste --&gt; Pub\n    API --&gt; Pub\n    Ed --&gt; Draft[(Draft, autosaved)]\n    Draft --&gt;|keep editing| Draft\n    Draft --&gt;|Publish when ready| Pub\n    Pub --&gt; V[Immutable version N, content-addressed]\n    V --&gt; Live([Served at the canvas URL])\n    Live -.-&gt;|Make current| Prev[Re-point to an earlier version]\n    Prev -.-&gt; Live</pre>\n<p>Every publish writes an <strong>immutable version</strong>: files are stored by content hash, and the live URL is a pointer at one version. Rolling back re-points; nothing is rewritten. The last 10 versions are kept.</p>\n<p>The editor is the one source with a draft step. Everything else publishes the moment it succeeds. Two words for the same event: the dashboard says <strong>publish</strong>, the API and code say <strong>deploy</strong>.</p>\n<p>Owners and editors can do everything on this page. A viewer opens the canvas but cannot change it; only the owner can delete it. See <a href=\"/docs/authoring/sharing\">Sharing &amp; access</a>.</p>\n<h2 id=\"options-at-create-time\">Options at create time</h2>\n<p>After you pick a source, <code>/new</code> asks for:</p>\n<ul>\n<li><strong>Title</strong> (optional, up to 200 characters). Rename later from the <strong>Overview</strong> tab.</li>\n<li><strong>Slug</strong> (optional). Leave it empty for a readable random slug, or choose your own; availability is checked as you type. See <a href=\"#custom-slug\">Custom slug</a>.</li>\n<li><strong>Workspace</strong>, shown only when your account belongs to an org. <strong>Personal</strong> means only you and the people you specifically add. Picking your org homes the canvas there so it can later open to the <strong>Whole org</strong> rung. This choice is fixed once the canvas exists: a Personal canvas cannot be opened to Whole org later. Guests never see this choice.</li>\n<li><strong>Audience</strong> (not shown for <strong>Use the API</strong>). <strong>Restricted</strong> is the default: only you and people or teams you add can open it. A workspace canvas offers <strong>Everyone in {workspace}</strong> with a <strong>List in Shared</strong> toggle (off by default) so members can find it. A Personal canvas offers <strong>Public link</strong>, when the instance and your account allow public links, with an optional <strong>Require password</strong>. Changing the Workspace resets the audience to Restricted. The audience is applied after the first publish; if that fails, the canvas stays Restricted and the key dialog says so (<strong>Save key and open Share</strong>). The full ladder, named people, teams, and expiry live on the <strong>Share</strong> tab.</li>\n<li><strong>Enable backend</strong> (off by default). Turns on identity and, subject to what your instance allows, KV, files, AI, and realtime. Change it any time on the <strong>Backend</strong> tab. See <a href=\"/docs/authoring/capabilities\">Capabilities</a>.</li>\n</ul>\n<p>For <strong>Files or folder</strong> and <strong>Upload ZIP</strong>, the create and the publish start as soon as you drop the files. If the publish fails, the empty canvas is removed and the error is shown so you can retry. On success the key is shown once; copy it if you plan to use the API.</p>\n<h2 id=\"files-or-folder\">Files or folder</h2>\n<p>Drop individual files or a whole folder, or use the file and folder pickers. Relative paths are preserved at the canvas root, so an <code>index.html</code> at the folder root is served at the canvas URL. Upload, version, and publish happen in one step, with progress shown while files transfer.</p>\n<h2 id=\"upload-a-zip\">Upload a ZIP</h2>\n<p>Upload a <code>.zip</code> (not a tar) and the server extracts it. Extraction is path-safe: entries that escape the canvas root are rejected (<code>ZIP_SLIP_REJECTED</code>, <code>INVALID_PATH</code>), an entry that declares more than 25 MB, or inflates past it, is rejected before it can fill memory (<code>ZIP_BOMB_REJECTED</code>), and an unreadable archive is <code>INVALID_ZIP</code>. As with a folder, an <code>index.html</code> at the archive root is the home page.</p>\n<h2 id=\"paste-html\">Paste HTML</h2>\n<p>For a one-file canvas, paste HTML directly. canvas-drop stores it as a single <code>index.html</code> and publishes it. From <code>/new</code> this creates and publishes in one request; on an existing canvas it publishes the next version.</p>\n<h2 id=\"new-version-on-an-existing-canvas\">New version on an existing canvas</h2>\n<p>The <strong>New version</strong> button sits on every tab of a canvas you own or edit. It opens the same three sources (<strong>Paste HTML</strong>, <strong>Files or folder</strong>, <strong>ZIP</strong>) and publishes the next version to everyone as soon as it succeeds. Use it when your files are built elsewhere. An archived canvas refuses new versions (<code>NOT_ACTIVE</code>) until you unarchive it.</p>\n<h2 id=\"editor\">Editor</h2>\n<p>Create and edit files in the browser with syntax highlighting. Changes autosave to a <strong>draft</strong> that only you and other editors see; you choose when to <strong>Publish</strong>. See <a href=\"/docs/authoring/editor\">The editor</a>.</p>\n<p>If a direct publish (folder, ZIP, paste, or API) lands while the draft has unpublished edits, the draft is marked <strong>stale</strong> rather than overwritten, so nothing you typed is lost. A clean draft simply follows the new version.</p>\n<h2 id=\"what-every-publish-enforces\">What every publish enforces</h2>\n<table>\n<thead>\n<tr>\n<th>Rule</th>\n<th>Detail</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Size</td>\n<td>100 MB per canvas, 25 MB per file, 2000 files. Over the cap fails with <code>CANVAS_TOO_LARGE</code>, <code>FILE_TOO_LARGE</code>, or <code>TOO_MANY_FILES</code>.</td>\n</tr>\n<tr>\n<td>Dotfiles</td>\n<td>Files and directories whose name starts with <code>.</code> (<code>.git</code>, <code>.env</code>, <code>.DS_Store</code>) are dropped from every publish.</td>\n</tr>\n<tr>\n<td>Home page</td>\n<td><code>index.html</code> at the root is the home page. If there is no <code>index.html</code> but exactly one HTML file, that file is served at the root and the Overview tab reports an inferred home page. No <code>index.html</code> and zero or several HTML files means the root returns 404 and the Overview tab shows <strong>Root page missing</strong>.</td>\n</tr>\n<tr>\n<td>Single-page app mode</td>\n<td>Off by default. Turn it on under <strong>Settings</strong> → URL &amp; routing to serve the home page for any path that does not match a file (or a directory's own <code>index.html</code>), for client-side routing.</td>\n</tr>\n<tr>\n<td>Key lint</td>\n<td>A text file that contains something shaped like a canvas key (<code>cd_...</code>) produces a warning. Remove the key and publish again.</td>\n</tr>\n</tbody></table>\n<p>Warnings never block a publish. They come back in the <code>warnings[]</code> array of the deploy response, in three forms: a file that will be served as <code>text/plain</code>, a file that may contain a canvas key, and a missing <code>index.html</code>.</p>\n<h2 id=\"custom-slug\">Custom slug</h2>\n<p>The slug identifies the canvas in its URL: <code>{base}/c/{slug}/</code> in <code>path</code> mode, <code>https://{slug}.canvases.example.com/</code> in <code>subdomain</code> mode. A new canvas gets a readable random slug such as <code>quiet-otter-x7k2m9p3q1r5t</code>: an adjective, a noun, and a 13-character random suffix that carries the unguessability. Choose your own at create time, or change it later from <strong>Settings</strong> → URL &amp; routing → <strong>Change slug</strong> (enter a new slug, or leave the field empty for a fresh random one). Over MCP the tool is <code>set_canvas_slug</code>.</p>\n<p>The grammar is one DNS label: lowercase <code>a-z</code>, digits, and hyphen, 1 to 63 characters, no leading or trailing hyphen. Reserved words (<code>api</code>, <code>v1</code>, <code>sdk</code>, <code>auth</code>, <code>mcp</code>, <code>healthz</code>, <code>welcome</code>, <code>docs</code>, <code>gallery</code>, <code>privacy</code>, <code>terms</code>, <code>skill</code>, <code>www</code>, <code>app</code>, <code>admin</code>, <code>mail</code>, <code>static</code>, <code>assets</code>) are refused with <code>400 invalid_slug</code>, and a slug someone else holds returns <code>409 slug_taken</code>.</p>\n<p>A slug change takes effect on the next request: the old URL stops resolving and live realtime connections are dropped so clients reconnect under the new slug. A custom slug is guessable, so rely on the access rung, not on the URL being secret; the Share tab reminds you of this on a link-reachable canvas. See <a href=\"/docs/authoring/sharing\">Sharing &amp; access</a>.</p>\n<h2 id=\"deploy-api\">Deploy API</h2>\n<p>Ship from CI or an agent with a per-canvas secret key over HTTP; no dashboard session is involved. The body is a ZIP archive and the response is machine-readable. This path publishes <strong>directly to live</strong> with no draft step.</p>\n<pre><code class=\"hljs language-bash\">curl -fsS -X PUT <span class=\"hljs-string\">\"{base}/v1/canvases/{id}/deploy\"</span> \\\n  -H <span class=\"hljs-string\">\"Authorization: Bearer <span class=\"hljs-variable\">$CANVAS_KEY</span>\"</span> \\\n  --data-binary @site.zip</code></pre>\n<p><strong>The key.</strong> Every canvas has one key, format <code>cd_...</code>. It is shown once when the canvas is created (the <strong>Use the API</strong> source shows the id, the key, and a ready-to-run <code>curl</code> with the exact URL for your instance), and you can regenerate it any time from <strong>Settings</strong> → Deploy API → <strong>Regenerate key</strong>; the old key stops working on the next request, and when an editor rotates it the owner is emailed. The key is a Bearer secret, not a session cookie, and works only on its own canvas: a key for a different canvas returns <code>403</code>, and a missing, unknown, or revoked key returns <code>401</code>. A key stops working the moment its canvas is archived, disabled, or deleted. Deploys made with the key are attributed to the canvas owner in the Versions tab.</p>\n<p><strong>Success</strong> returns the new version's details:</p>\n<pre><code class=\"hljs language-json\"><span class=\"hljs-punctuation\">{</span> <span class=\"hljs-attr\">\"url\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"…\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"version\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">7</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"fileCount\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">12</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"totalBytes\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">48213</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"warnings\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-punctuation\">[</span><span class=\"hljs-punctuation\">]</span> <span class=\"hljs-punctuation\">}</span></code></pre>\n<p><strong>Failure</strong> returns a stable <code>{ code, message, path? }</code> body so an agent can repair and retry:</p>\n<pre><code class=\"hljs language-json\"><span class=\"hljs-punctuation\">{</span> <span class=\"hljs-attr\">\"code\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"FILE_TOO_LARGE\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"message\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"…\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"path\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"media/intro.mp4\"</span> <span class=\"hljs-punctuation\">}</span></code></pre>\n<p>Validation errors (<code>EMPTY_DEPLOY</code>, <code>INVALID_ZIP</code>, <code>INVALID_PATH</code>, <code>ZIP_SLIP_REJECTED</code>, and the rest) come back as <code>400</code>. A body over the size cap is refused with <code>413 CANVAS_TOO_LARGE</code> before it is parsed. Once the key is verified, the endpoint is rate-limited per canvas: 10 deploys per minute by default (the operator sets <code>CANVAS_DROP_RATELIMIT_DEPLOY_PER_MIN</code>); over the limit you get <code>429 {\"error\":\"rate_limited\"}</code> with a <code>Retry-After</code> header.</p>\n<p><strong>Staged upload</strong> for large or repeat deploys sends only the files that changed. <code>POST {base}/v1/canvases/{id}/uploads</code> with a manifest of <code>{ path, hash, size }</code> entries returns an <code>uploadId</code> plus the hashes the server does not already hold; <code>PUT .../uploads/{uploadId}/blobs/{hash}</code> each missing blob (<code>204</code> per blob); <code>POST .../uploads/{uploadId}/finalize</code> publishes the version. An upload session lasts 15 minutes.</p>\n<p><strong>Read-back and lifecycle</strong> on the same key: <code>GET {base}/v1/canvases/{id}</code> (status JSON, including <code>slug</code> and <code>url</code>), <code>GET .../versions</code>, <code>GET .../files</code> (the live manifest; <code>?path=app.js</code> returns that file's bytes), <code>POST .../unpublish</code>, and <code>POST .../rollback</code> with body <code>{ \"version\": 3 }</code>. Full contract in the <a href=\"/docs/api/deploy-api\">Deploy API reference</a>; error codes in <a href=\"/docs/api/errors\">Error codes</a>.</p>\n<p><strong>Over MCP</strong>, <code>deploy_canvas</code> takes either <code>zipBase64</code> or a <code>files[]</code> array (text as <code>utf8</code>, binary as <code>base64</code>), and <code>begin_deploy</code> / <code>add_files</code> / <code>finalize_deploy</code> mirror the staged flow. See <a href=\"/docs/agents/mcp\">MCP server</a>.</p>\n<h2 id=\"versions-and-rollback\">Versions and rollback</h2>\n<p>Every publish creates a new immutable version and the canvas always serves its <strong>current</strong> one. The <strong>Versions</strong> tab lists them newest first with source, who published, file count, and size. For any version you can:</p>\n<ul>\n<li><strong>Make current</strong>: roll back (or forward) by re-pointing the live URL. Instant and non-destructive; no new version is written. With the key: <code>POST {base}/v1/canvases/{id}/rollback</code>.</li>\n<li><strong>Download ZIP</strong>: the exact files of that version.</li>\n<li><strong>Restore</strong>: copy its files into the editor draft (confirms first if the draft has unpublished changes).</li>\n<li><strong>Delete</strong>: permanently remove a historical version. The current version cannot be deleted.</li>\n</ul>\n<p>Because files are content-addressed, only changed files cost storage and republishing identical files is cheap. The last 10 versions are kept; older ones are pruned on publish.</p>\n<p>To take a canvas offline without deleting it, use <strong>Settings</strong> → Lifecycle → <strong>Unpublish</strong> (or <code>POST .../unpublish</code> with the key). The canvas returns to draft state, the URL stops serving, sharing and gallery listing are cleared, and live realtime connections are dropped; publish again to bring it back.</p>\n",
    "headings": [
      {
        "id": "ways-in",
        "text": "Ways in",
        "level": 2
      },
      {
        "id": "options-at-create-time",
        "text": "Options at create time",
        "level": 2
      },
      {
        "id": "files-or-folder",
        "text": "Files or folder",
        "level": 2
      },
      {
        "id": "upload-a-zip",
        "text": "Upload a ZIP",
        "level": 2
      },
      {
        "id": "paste-html",
        "text": "Paste HTML",
        "level": 2
      },
      {
        "id": "new-version-on-an-existing-canvas",
        "text": "New version on an existing canvas",
        "level": 2
      },
      {
        "id": "editor",
        "text": "Editor",
        "level": 2
      },
      {
        "id": "what-every-publish-enforces",
        "text": "What every publish enforces",
        "level": 2
      },
      {
        "id": "custom-slug",
        "text": "Custom slug",
        "level": 2
      },
      {
        "id": "deploy-api",
        "text": "Deploy API",
        "level": 2
      },
      {
        "id": "versions-and-rollback",
        "text": "Versions and rollback",
        "level": 2
      }
    ],
    "text": "Create & publish You have HTML, a folder of static files, or a build directory, and you want a live URL that people in your org can open. By the end of this page you will have published a canvas, know what a publish does, and be able to control its URL. The shortest path, from a signed-in dashboard: Open /new and pick Paste HTML . Paste your markup, optionally give it a title, and click Create and publish . Save the key from the dialog (it is shown once), then Go to canvas . The Overview tab has the URL: {base}/c/{slug}/ in path mode, https://{slug}.canvases.example.com/ in subdomain mode. The same from a terminal, with the canvas id and key shown when you create a canvas: curl -fsS -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip # 200 {\"url\":\"…\",\"version\":1,\"fileCount\":12,\"totalBytes\":48213,\"warnings\":[]} Ways in Source Where What it does Paste HTML /new , or New version on a canvas Stores your markup as index.html and publishes it in one step Files or folder /new , or New version Uploads with relative paths preserved and publishes Upload ZIP /new , or New version Extracts the archive server-side and publishes Use the API /new Mints an empty canvas plus its key; the first PUT …/deploy publishes Editor The Editor tab Autosaves a draft ; you click Publish when ready Deploy API / MCP Scripts and agents Publishes directly to live with a key or over MCP flowchart TD subgraph Sources[\"Ways in\"] DnD[Files or folder] Zip[Upload ZIP] Paste[Paste HTML] API[Deploy API / MCP] Ed[Editor] end DnD --> Pub{Publish} Zip --> Pub Paste --> Pub API --> Pub Ed --> Draft[(Draft, autosaved)] Draft -->|keep editing| Draft Draft -->|Publish when ready| Pub Pub --> V[Immutable version N, content-addressed] V --> Live([Served at the canvas URL]) Live -.->|Make current| Prev[Re-point to an earlier version] Prev -.-> Live Every publish writes an immutable version : files are stored by content hash, and the live URL is a pointer at one version. Rolling back re-points; nothing is rewritten. The last 10 versions are kept. The editor is the one source with a draft step. Everything else publishes the moment it succeeds. Two words for the same event: the dashboard says publish , the API and code say deploy . Owners and editors can do everything on this page. A viewer opens the canvas but cannot change it; only the owner can delete it. See Sharing & access . Options at create time After you pick a source, /new asks for: Title (optional, up to 200 characters). Rename later from the Overview tab. Slug (optional). Leave it empty for a readable random slug, or choose your own; availability is checked as you type. See Custom slug . Workspace , shown only when your account belongs to an org. Personal means only you and the people you specifically add. Picking your org homes the canvas there so it can later open to the Whole org rung. This choice is fixed once the canvas exists: a Personal canvas cannot be opened to Whole org later. Guests never see this choice. Audience (not shown for Use the API ). Restricted is the default: only you and people or teams you add can open it. A workspace canvas offers Everyone in {workspace} with a List in Shared toggle (off by default) so members can find it. A Personal canvas offers Public link , when the instance and your account allow public links, with an optional Require password . Changing the Workspace resets the audience to Restricted. The audience is applied after the first publish; if that fails, the canvas stays Restricted and the key dialog says so ( Save key and open Share ). The full ladder, named people, teams, and expiry live on the Share tab. Enable backend (off by default). Turns on identity and, subject to what your instance allows, KV, files, AI, and realtime. Change it any time on the Backend tab. See Capabilities . For Files or folder and Upload ZIP , the create and the publish start as soon as you drop the files. If the publish fails, the empty canvas is removed and the error is shown so you can retry. On success the key is shown once; copy it if you plan to use the API. Files or folder Drop individual files or a whole folder, or use the file and folder pickers. Relative paths are preserved at the canvas root, so an index.html at the folder root is served at the canvas URL. Upload, version, and publish happen in one step, with progress shown while files transfer. Upload a ZIP Upload a .zip (not a tar) and the server extracts it. Extraction is path-safe: entries that escape the canvas root are rejected ( ZIP_SLIP_REJECTED , INVALID_PATH ), an entry that declares more than 25 MB, or inflates past it, is rejected before it can fill memory ( ZIP_BOMB_REJECTED ), and an unreadable archive is INVALID_ZIP . As with a folder, an index.html at the archive root is the home page. Paste HTML For a one-file canvas, paste HTML directly. canvas-drop stores it as a single index.html and publishes it. From /new this creates and publishes in one request; on an existing canvas it publishes the next version. New version on an existing canvas The New version button sits on every tab of a canvas you own or edit. It opens the same three sources ( Paste HTML , Files or folder , ZIP ) and publishes the next version to everyone as soon as it succeeds. Use it when your files are built elsewhere. An archived canvas refuses new versions ( NOT_ACTIVE ) until you unarchive it. Editor Create and edit files in the browser with syntax highlighting. Changes autosave to a draft that only you and other editors see; you choose when to Publish . See The editor . If a direct publish (folder, ZIP, paste, or API) lands while the draft has unpublished edits, the draft is marked stale rather than overwritten, so nothing you typed is lost. A clean draft simply follows the new version. What every publish enforces Rule Detail Size 100 MB per canvas, 25 MB per file, 2000 files. Over the cap fails with CANVAS_TOO_LARGE , FILE_TOO_LARGE , or TOO_MANY_FILES . Dotfiles Files and directories whose name starts with . ( .git , .env , .DS_Store ) are dropped from every publish. Home page index.html at the root is the home page. If there is no index.html but exactly one HTML file, that file is served at the root and the Overview tab reports an inferred home page. No index.html and zero or several HTML files means the root returns 404 and the Overview tab shows Root page missing . Single-page app mode Off by default. Turn it on under Settings → URL & routing to serve the home page for any path that does not match a file (or a directory's own index.html ), for client-side routing. Key lint A text file that contains something shaped like a canvas key ( cd_... ) produces a warning. Remove the key and publish again. Warnings never block a publish. They come back in the warnings[] array of the deploy response, in three forms: a file that will be served as text/plain , a file that may contain a canvas key, and a missing index.html . Custom slug The slug identifies the canvas in its URL: {base}/c/{slug}/ in path mode, https://{slug}.canvases.example.com/ in subdomain mode. A new canvas gets a readable random slug such as quiet-otter-x7k2m9p3q1r5t : an adjective, a noun, and a 13-character random suffix that carries the unguessability. Choose your own at create time, or change it later from Settings → URL & routing → Change slug (enter a new slug, or leave the field empty for a fresh random one). Over MCP the tool is set_canvas_slug . The grammar is one DNS label: lowercase a-z , digits, and hyphen, 1 to 63 characters, no leading or trailing hyphen. Reserved words ( api , v1 , sdk , auth , mcp , healthz , welcome , docs , gallery , privacy , terms , skill , www , app , admin , mail , static , assets ) are refused with 400 invalid_slug , and a slug someone else holds returns 409 slug_taken . A slug change takes effect on the next request: the old URL stops resolving and live realtime connections are dropped so clients reconnect under the new slug. A custom slug is guessable, so rely on the access rung, not on the URL being secret; the Share tab reminds you of this on a link-reachable canvas. See Sharing & access . Deploy API Ship from CI or an agent with a per-canvas secret key over HTTP; no dashboard session is involved. The body is a ZIP archive and the response is machine-readable. This path publishes directly to live with no draft step. curl -fsS -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip The key. Every canvas has one key, format cd_... . It is shown once when the canvas is created (the Use the API source shows the id, the key, and a ready-to-run curl with the exact URL for your instance), and you can regenerate it any time from Settings → Deploy API → Regenerate key ; the old key stops working on the next request, and when an editor rotates it the owner is emailed. The key is a Bearer secret, not a session cookie, and works only on its own canvas: a key for a different canvas returns 403 , and a missing, unknown, or revoked key returns 401 . A key stops working the moment its canvas is archived, disabled, or deleted. Deploys made with the key are attributed to the canvas owner in the Versions tab. Success returns the new version's details: { \"url\" : \"…\" , \"version\" : 7 , \"fileCount\" : 12 , \"totalBytes\" : 48213 , \"warnings\" : [ ] } Failure returns a stable { code, message, path? } body so an agent can repair and retry: { \"code\" : \"FILE_TOO_LARGE\" , \"message\" : \"…\" , \"path\" : \"media/intro.mp4\" } Validation errors ( EMPTY_DEPLOY , INVALID_ZIP , INVALID_PATH , ZIP_SLIP_REJECTED , and the rest) come back as 400 . A body over the size cap is refused with 413 CANVAS_TOO_LARGE before it is parsed. Once the key is verified, the endpoint is rate-limited per canvas: 10 deploys per minute by default (the operator sets CANVAS_DROP_RATELIMIT_DEPLOY_PER_MIN ); over the limit you get 429 {\"error\":\"rate_limited\"} with a Retry-After header. Staged upload for large or repeat deploys sends only the files that changed. POST {base}/v1/canvases/{id}/uploads with a manifest of { path, hash, size } entries returns an uploadId plus the hashes the server does not already hold; PUT .../uploads/{uploadId}/blobs/{hash} each missing blob ( 204 per blob); POST .../uploads/{uploadId}/finalize publishes the version. An upload session lasts 15 minutes. Read-back and lifecycle on the same key: GET {base}/v1/canvases/{id} (status JSON, including slug and url ), GET .../versions , GET .../files (the live manifest; ?path=app.js returns that file's bytes), POST .../unpublish , and POST .../rollback with body { \"version\": 3 } . Full contract in the Deploy API reference ; error codes in Error codes . Over MCP , deploy_canvas takes either zipBase64 or a files[] array (text as utf8 , binary as base64 ), and begin_deploy / add_files / finalize_deploy mirror the staged flow. See MCP server . Versions and rollback Every publish creates a new immutable version and the canvas always serves its current one. The Versions tab lists them newest first with source, who published, file count, and size. For any version you can: Make current : roll back (or forward) by re-pointing the live URL. Instant and non-destructive; no new version is written. With the key: POST {base}/v1/canvases/{id}/rollback . Download ZIP : the exact files of that version. Restore : copy its files into the editor draft (confirms first if the draft has unpublished changes). Delete : permanently remove a historical version. The current version cannot be deleted. Because files are content-addressed, only changed files cost storage and republishing identical files is cheap. The last 10 versions are kept; older ones are pruned on publish. To take a canvas offline without deleting it, use Settings → Lifecycle → Unpublish (or POST .../unpublish with the key). The canvas returns to draft state, the URL stops serving, sharing and gallery listing are cleared, and live realtime connections are dropped; publish again to bring it back."
  },
  {
    "path": "authoring/sharing",
    "section": "Authoring canvases",
    "title": "Sharing & access",
    "html": "<h1 id=\"sharing-amp-access\">Sharing &amp; access</h1>\n<p>You own or edit a canvas and want to decide who can open it, who edits it with you, and where it shows up. Every control on this page lives on the canvas's <strong>Share</strong> tab. Agents reach the same controls over <a href=\"#over-mcp\">MCP</a>, through the same service and the same role checks.</p>\n<p>Every canvas starts <strong>Restricted</strong>: only you, the owner, can open it. Sharing is two controls plus optional layers, and they read the way Google Docs does:</p>\n<ol>\n<li><strong>People and teams with direct access</strong> names who can open the canvas, each as a <strong>viewer</strong> or an <strong>editor</strong>. This list <strong>always applies</strong>, whatever the next control says.</li>\n<li><strong>General access</strong> says who <em>else</em> can open it: <strong>Restricted</strong> (nobody beyond the list), <strong>Whole org</strong>, or <strong>Public link</strong>.</li>\n<li>Optionally add a <strong>password</strong> or a <strong>share expiry</strong> under <strong>Protection</strong>.</li>\n<li>Optionally opt into discovery: <strong>List for your org</strong> (the Shared page, Whole org only) or <strong>List in the gallery</strong>.</li>\n</ol>\n<h2 id=\"share-with-one-colleague\">Share with one colleague</h2>\n<ol>\n<li>Publish first. Until the canvas has a live version, the Share tab shows one locked panel with a <strong>Publish</strong> button.</li>\n<li>Under <strong>People and teams with direct access</strong>, keep <strong>People</strong> selected, type their email, pick <strong>Viewer</strong> or <strong>Editor</strong>, and choose <strong>Add</strong>.</li>\n</ol>\n<p>That is all: they can open the canvas on their next request, with General access still on <strong>Restricted</strong>. Over MCP it is one call:</p>\n<pre><code class=\"hljs language-text\">grant_access   { \"id\": \"&lt;canvas id&gt;\", \"email\": \"someone@example.com\", \"role\": \"viewer\" }</code></pre>\n<p>Opening General access to <strong>Whole org</strong> or <strong>Public link</strong> on an unpublished or archived canvas is refused with <code>SHARE_REQUIRES_PUBLISH</code> (HTTP 409); the people-and-teams list can be filled in before publishing.</p>\n<blockquote>\n<p>Admins have no back door into your content. For a canvas they do not own, an admin is an ordinary member: a Restricted canvas returns 404, a password prompts them too, and they cannot open the editor or change settings. Cross-owner admin power is moderation and offboarding only: the all-canvases list, disable / re-enable / restore, and reassigning the owner when someone leaves.</p>\n</blockquote>\n<h2 id=\"general-access\">General access</h2>\n<p>One choice per canvas, stored as <code>access</code> (default <code>private</code>). The people and teams on the list are admitted <strong>at every choice</strong>; General access only ever widens beyond them.</p>\n<table>\n<thead>\n<tr>\n<th>Choice</th>\n<th>Who else can open it</th>\n<th>Backend primitives</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Restricted</strong> (<code>private</code>)</td>\n<td>Nobody beyond the owner, the editors, and the people and teams on the list. With nobody added, that is just you.</td>\n<td>Full (whatever the Backend tab enables), for everyone admitted.</td>\n</tr>\n<tr>\n<td><strong>Whole org</strong> (<code>whole_org</code>)</td>\n<td>Any signed-in member with the link.</td>\n<td>Full, for members.</td>\n</tr>\n<tr>\n<td><strong>Public link</strong> (<code>public_link</code>)</td>\n<td>Anyone with the URL, no sign-in, while the instance switch is on and the owner's account may publish publicly.</td>\n<td><strong>None</strong> for the public: static files only. People on the list keep full access.</td>\n</tr>\n</tbody></table>\n<p>Two legacy values, <code>specific_people</code> and <code>team</code>, are still accepted and stored by the API for compatibility. They are aliases of <code>private</code>: the dashboard shows them as <strong>Restricted</strong>, and the server treats all three identically. Nothing you set through the dashboard writes them.</p>\n<p>Editors are admitted at every choice, never see the password prompt, and are unaffected by expiry. Everyone else is evaluated per request, with nothing cached: a change to General access, the list, or the locks applies on the very next request. Anyone with no route in gets an opaque <strong>404</strong>, never a \"forbidden\" that confirms the canvas exists.</p>\n<p><strong>When an org boundary is configured</strong> (the operator set <code>CANVAS_DROP_ORG_NAME</code>; off by default), <strong>Whole org</strong> means <em>members of this canvas's home org</em>, not \"anyone signed in\". Members pick <strong>Personal</strong> or the workspace when they create a canvas (a <a href=\"/docs/authoring/create-and-publish#personal-vs-workspace\">fixed choice</a>). A Personal canvas cannot be shared org-wide: the choice is shown disabled, and the server refuses with <code>ORG_REQUIRED</code> (HTTP 409). A <strong>guest</strong> (a signed-in user in no org) never sees Whole-org canvases, only the ones they are added to. With no org configured, Whole org is \"any signed-in user\".</p>\n<p>Which choices you see depends on your account: <strong>Whole org</strong> is hidden for guests, and <strong>Public link</strong> appears only while your own account may publish publicly. A choice the canvas is already on stays visible either way.</p>\n<h3 id=\"slugs-are-not-access-control\">Slugs are not access control</h3>\n<p>The random slug in a canvas URL (<code>quiet-otter-x7k2</code>) is defense in depth, not access control. A <strong>custom slug</strong> (<code>team-dashboard</code>) is guessable, so the list and General access do all the work; the Share tab shows a heads-up when a canvas is link-reachable (Whole org or Public link) and uses a custom slug.</p>\n<h2 id=\"access-vs-discovery\">Access vs discovery</h2>\n<p>Access and discovery are separate settings:</p>\n<ul>\n<li>The <strong>people-and-teams list</strong> and <strong>General access</strong> decide who can open the URL.</li>\n<li><strong>List for your org</strong> (<code>discoverability</code>: <code>link_only</code> or <code>listed</code>, Whole org only) decides whether a <strong>Whole org</strong> share appears on the <strong>Shared</strong> page for every member of the home org. It never widens URL access.</li>\n</ul>\n<p>People and teams on the list always see the canvas in <strong>Shared</strong> (a team's members through the team), at every General-access choice and without any switch. Whole-org shares default to <strong>link-only</strong>: members can open the URL, but the canvas is not listed for them until you turn <strong>List for your org</strong> on, which also makes it eligible for the gallery. Listing a Whole-org canvas in the gallery turns this switch on as part of the same action; turning it off again also removes the canvas from the gallery. <strong>Public link</strong> canvases never appear in Shared; use the gallery for deliberate discovery.</p>\n<h2 id=\"roles-viewers-and-editors\">Roles: viewers and editors</h2>\n<p>Every entry under <strong>People and teams with direct access</strong>, whether a person, a pending email, or a team, is a <strong>viewer</strong> or an <strong>editor</strong>. The owner is pinned first and has no role control. Use the <strong>People</strong> and <strong>Teams</strong> tabs to change what the single add form accepts; the access list stays visible below.</p>\n<p><img src=\"/docs/assets/tour-people.webp\" alt=\"Sharing and permissions: People and Teams add tabs, the owner pinned first, and direct-access rows with role and action controls\" /></p>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Viewer</th>\n<th>Editor</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Open the canvas</td>\n<td>Always, at every General-access choice (the password and expiry still apply).</td>\n<td>Always, with no password prompt and no expiry.</td>\n</tr>\n<tr>\n<td>Edit the draft, publish, roll back, restore</td>\n<td>No</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td>Change settings and sharing, add or remove people (other editors included)</td>\n<td>No</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td>Deploy new versions, regenerate the deploy key, use the authoring routes</td>\n<td>No</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td>Clone an eligible published canvas</td>\n<td>Yes, for a direct or team grant</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td>Delete, transfer ownership, guest-AI opt-in</td>\n<td>No</td>\n<td>No: <strong>owner only</strong> (<code>OWNER_ONLY</code>, HTTP 403)</td>\n</tr>\n</tbody></table>\n<p>Editors see the canvas in <strong>Your canvases</strong>, marked <code>editor · &lt;owner&gt;</code> on a card and <code>owned by &lt;owner&gt; · editor</code> on a row, and can filter with <strong>Owned by me</strong> / <strong>Editing</strong>. Edited canvases do not appear in Shared.</p>\n<ul>\n<li><strong>Only org members and teams can be editors.</strong> A guest (someone outside your org domains) is always a viewer (<code>GUEST_VIEWER_ONLY</code>, HTTP 400), and a pending email can carry the editor role only when it belongs to an org domain. When an org boundary is configured, an editor grant holds only while the person is <em>currently</em> a member, checked on every request: someone who leaves the org loses edit access on their next request, and their editor session and realtime sockets are dropped. Demoting an editor keeps them on the list as a viewer, so the canvas moves to <strong>Shared</strong> as view-only; removing them ends their access on the next request.</li>\n<li><strong>Entitlements follow the owner.</strong> An editor can switch on <strong>Public link</strong> only when the <em>owner's</em> account may publish publicly (<code>PUBLIC_LINK_OWNER_GATED</code>, HTTP 403, otherwise). Usage and AI spend stay attributed to the canvas and its owner.</li>\n</ul>\n<p>Pick the role when you add someone (<strong>Viewer</strong> is the default) and change it any time from the row. A person promoted to editor gets a courtesy email when outbound email is on; when an editor makes someone else an editor, the owner is notified too. After you remove or demote an editor, the dashboard offers to <strong>regenerate the deploy key</strong>: a key they copied keeps working until you do.</p>\n<h3 id=\"two-editors-one-file\">Two editors, one file</h3>\n<p>There is no real-time co-editing. Each draft save carries the hash of the file the editor loaded; if someone else saved that file first, the save is refused with <code>DRAFT_CONFLICT</code> (HTTP 409) carrying <code>path</code>, <code>currentHash</code>, <code>updatedBy</code>, <code>updatedByName</code>, and <code>updatedAt</code>. The editor keeps your unsaved text next to the other version so you can use theirs or overwrite. Edits to <em>different</em> files never conflict. Each publish records who created the version, so the <strong>Versions</strong> tab shows authorship.</p>\n<h3 id=\"transfer-ownership\">Transfer ownership</h3>\n<p>Under owner-only <strong>Advanced</strong>, the owner can hand the canvas to any current <strong>editor who is an org member</strong>, including a member of an editor team; never to a team itself, and never by email (the picker lists eligible people; anyone else is refused with <code>NOT_ELIGIBLE</code>, HTTP 409). Editors do not see this section. The transfer applies at once and is audited:</p>\n<ul>\n<li>The recipient becomes the owner; you stay on as an editor while your account is active and still passes the org check.</li>\n<li>If the canvas was on <strong>Public link</strong> and the new owner's account cannot publish publicly, the rung is turned off (<code>publicLinkReverted</code>).</li>\n<li>The deploy key keeps working; the new owner can rotate it.</li>\n</ul>\n<p>When an owner leaves, an admin can <strong>reassign</strong> any of their canvases to another member from <strong>Admin → Canvases</strong> with a recorded reason. The previous owner stays an editor when their account is still active, both parties are emailed, and the deploy key is rotated in the same step (the new owner issues a fresh one).</p>\n<h2 id=\"adding-specific-people\">Adding specific people</h2>\n<p>Under <strong>People and teams with direct access</strong>, leave the <strong>People</strong> tab selected and add by email as a <strong>viewer</strong> or an <strong>editor</strong>. The outcome is deterministic:</p>\n<table>\n<thead>\n<tr>\n<th>Outcome</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>granted</code></td>\n<td>An existing signed-in user; they can open the canvas now.</td>\n</tr>\n<tr>\n<td><code>pending</code></td>\n<td>A new email your auth setup can admit. It has no login power by itself; access materializes after that exact email signs in through the configured auth (<code>oidc</code>, <code>proxy</code>, or <code>dev</code>).</td>\n</tr>\n<tr>\n<td><code>already_added</code> / <code>already_pending</code></td>\n<td>Nothing changed.</td>\n</tr>\n<tr>\n<td><code>role_changed</code></td>\n<td>You passed a role for someone already listed. An omitted role never changes an existing entry.</td>\n</tr>\n<tr>\n<td><code>NOT_PERMITTED</code> (403)</td>\n<td>A brand-new external email that policy does not let you admit.</td>\n</tr>\n<tr>\n<td><code>AUTH_ADMISSION_REQUIRED</code> (403)</td>\n<td>The identity provider must admit that email before canvas-drop can grant it.</td>\n</tr>\n<tr>\n<td><code>GUEST_VIEWER_ONLY</code> (400)</td>\n<td>Editor requested for a guest email.</td>\n</tr>\n<tr>\n<td><code>BLOCKED</code> (403)</td>\n<td>That account is blocked.</td>\n</tr>\n<tr>\n<td><code>RATE_LIMITED</code> (429)</td>\n<td>Too many adds in an hour, or the instance's pending-invite cap is reached. Try again later.</td>\n</tr>\n</tbody></table>\n<p>Who may admit a <strong>brand-new external email</strong>: admins always can, from the Share tab or <strong>Admin → People</strong>. A non-admin member can only when the operator turns on <strong>Let members add brand-new emails</strong> (<code>invites.allowMemberNewEmails</code>, off by default), or when the email can already authenticate through an allowed domain or an existing sign-in permit.</p>\n<p>Pending people show in the list with the role you gave them and can be re-roled or removed before they ever sign in. Removing an active or pending person takes effect on the next request. When outbound email is enabled (<code>CANVAS_DROP_EMAIL_DRIVER</code>), the person receives a courtesy sign-in or access email. There is no app-owned password or magic link; everyone authenticates the same way.</p>\n<blockquote>\n<p>In <code>proxy</code> mode the upstream identity-aware proxy owns admission. canvas-drop can record grants for people who already exist or are already admitted, but it cannot make a brand-new external email reachable on its own. Add that person to the upstream access policy first.</p>\n</blockquote>\n<h2 id=\"sharing-with-a-team\">Sharing with a team</h2>\n<p>A team can be granted two ways from the <strong>Teams</strong> tab under <strong>People and teams with direct access</strong>, and either way the grant applies at every General-access choice:</p>\n<ul>\n<li>As <strong>viewers</strong>: every current member can open and use the canvas (full backend).</li>\n<li>As <strong>editors</strong>: every current member edits with you. People who join the team later are editors too; a member who leaves the team stops being one.</li>\n</ul>\n<p>You can grant only teams you belong to, and an org team must match the canvas's org (<code>TEAM_FORBIDDEN</code>, HTTP 403, otherwise). The grant is independent of your own membership afterward: if you leave the team, the canvas stays shared with it until you change the list. For an <strong>org</strong> team, membership is re-checked against live org membership on every request, so someone removed from the org loses access immediately even if a stale team row lingers. For a <strong>personal</strong> team, direct membership is the boundary.</p>\n<p>Members find a team-shared canvas under <strong>Shared</strong> right away (or through <code>list_shared_canvases</code> over MCP); a canvas shared only with people and teams never appears in the gallery. Changing General access never touches team grants: only removing the team from the list does.</p>\n<blockquote>\n<p>Unlike <strong>Whole org</strong>, sharing with a team does not need an org workspace: any signed-in user can create a personal team and share even a <a href=\"/docs/authoring/create-and-publish#personal-vs-workspace\">Personal</a> canvas with it. Manage teams on the <strong>Teams</strong> page.</p>\n</blockquote>\n<h2 id=\"protection-password-amp-expiry\">Protection: password &amp; expiry</h2>\n<p>Both live under <strong>Protection</strong> and apply on top of any General-access choice:</p>\n<ul>\n<li><strong>Password</strong>: non-owners who can open the canvas are prompted before it loads, the people and teams on the list included. The password is stored as an argon2id hash and cannot be shown again; <strong>Generate</strong> produces one you can copy. Owners and editors are never prompted; a non-owner admin is.</li>\n<li><strong>Share expiry</strong>: set a timestamp and access auto-revokes when it passes. The tab shows a countdown, then an expired notice; non-owners get a 404 until you clear or extend it.</li>\n</ul>\n<p>A canvas with a password cannot be listed in the gallery. Setting a password always un-lists the canvas, turns off its template setting, and clears its tags, whether or not it was listed at the time.</p>\n<h2 id=\"public-links\">Public links</h2>\n<p><strong>Public link</strong> lets anyone with the URL view the canvas with no sign-in, <strong>static-only</strong>: the page and its files serve, while every backend primitive (KV, files, AI, identity, realtime, Connections) is refused for public visitors, so the open internet never touches your org's spend, stored data, or granted outbound authority. Signed-in members hitting a public-link canvas are static-only too; only the owner and editors get full content.</p>\n<p>Two switches govern it:</p>\n<ul>\n<li>The instance-wide <strong>Public links enabled</strong> setting (<code>access.publicLinksEnabled</code>, on by default). Turning it off makes every existing public link stop working immediately, and new attempts fail with <code>PUBLIC_LINKS_DISABLED</code> (HTTP 403).</li>\n<li>The per-account <strong>publish public</strong> entitlement an admin can grant or revoke. Without it the owner gets <code>PUBLIC_NOT_ALLOWED</code>, and an editor acting on that owner's canvas gets <code>PUBLIC_LINK_OWNER_GATED</code> (both HTTP 403).</li>\n</ul>\n<h2 id=\"ai-for-added-people\">AI for added people</h2>\n<p>Shown when a legacy guest session is on the people-and-teams list (at any General-access choice), and owner-only: <strong>Allow added people to use AI</strong> (<code>guestAiEnabled</code>, off by default) plus a USD <strong>spend cap</strong> (<code>guestAiCap</code>). Added people can already use KV, files, and realtime when those capabilities are on; AI is metered and billed to the owner, so it is opt-in per canvas. An editor who touches these fields gets <code>OWNER_ONLY</code>.</p>\n<h2 id=\"gallery-description-and-tags\">Gallery, description, and tags</h2>\n<p><strong>Gallery &amp; templates</strong> on the Share tab is an opt-in listing. A canvas can be listed only when it is published, has <strong>no password</strong>, and is either <strong>Public link</strong> or <strong>Whole org</strong> (listing a Whole-org canvas also turns on <strong>List for your org</strong>). A Restricted canvas never appears there, however many people and teams are on its list. When an org boundary is configured, Whole-org entries are returned only to signed-in members of the canvas's home org; Public-link entries are visible to any signed-in gallery viewer. <strong>Allow others to use as a template</strong> lets colleagues clone the canvas as a starting point; your canvas is untouched.</p>\n<p>A signed-in viewer with direct access, or through a team, can also clone the canvas while it is published, active, unexpired, and password-free, at any General-access choice. General access by itself does not grant cloning, and pending invitations and retained legacy guest sessions cannot clone. The copy is durable: it remains after the source grant is revoked, because it is a new canvas owned by the viewer. It starts unpublished and Restricted, with an empty direct-access list, backend off, and a fresh slug and deploy key.</p>\n<p>The <strong>description</strong> is one field (max 2000 characters) used everywhere the canvas is shown: Overview, Shared, grid cards, and the gallery. There is no separate gallery summary.</p>\n<p><strong>Tags</strong> are one set per canvas, edited on the Overview tab (Enter or a comma confirms each; values are trimmed and lowercased; max <strong>20 tags of up to 50 characters</strong>). They power the tag filter in <strong>Your canvases</strong>, and once the canvas is gallery-listed the same tags show there and drive its tag shortcuts. Agents set both with <code>update_canvas</code> (<code>description</code>, <code>tags</code>).</p>\n<h2 id=\"finding-canvases\">Finding canvases</h2>\n<p><strong>Your canvases</strong>, <strong>Shared</strong>, and the <strong>gallery</strong> share one forgiving search over a canvas's <strong>title, description, tags, and slug</strong>; Shared also matches the owner's name and the access label (a team name, for example). Matching is case-, accent-, and whitespace-insensitive: <code>café</code>, <code>Cafe</code>, and <code>caf</code> all find \"Café Menu\". A multi-word query is AND-matched: every word must appear somewhere in those fields, in any combination. The same search backs the MCP <code>list_canvases</code> and <code>list_shared_canvases</code> <code>query</code> filters; <code>list_canvases</code> also takes a <code>tags</code> filter that matches canvases carrying any of the given tags.</p>\n<h2 id=\"when-an-admin-disables-a-canvas\">When an admin disables a canvas</h2>\n<p>An admin can take a canvas down for moderation. A disabled canvas becomes <strong>read-only to its owner and editors</strong>: every mutation (settings, sharing, tags, capabilities, slug, preview, deploy / publish / rollback, archive / unpublish, draft edits) is refused with <code>DISABLED</code> (HTTP 409 over the management API, a <code>DISABLED: …</code> failure over MCP). Reads still work, so you can open the canvas, see its versions and usage, and read the takedown reason. You cannot delete a disabled canvas; an admin must re-enable or restore it first.</p>\n<h2 id=\"revoking\">Revoking</h2>\n<p>Access is always revocable and never cached. Lowering the rung, removing a person, pending email, or team, hitting an expiry, regenerating the slug, or unpublishing takes effect on the <strong>next request</strong> and drops live realtime sockets. Legacy guest rows kept from older deployments (<code>guest:&lt;id&gt;</code> entries) are revocation-only migration data; re-publishing a canvas does not restore old grants.</p>\n<h2 id=\"over-mcp\">Over MCP</h2>\n<p>Every control above has an MCP tool that wraps the same service and role gate (owner or editor; a canvas you hold no role on reads as not found):</p>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>list_access</code></td>\n<td>The people list with a <code>role</code> per entry and stable ids (<code>owner</code>, <code>member:&lt;id&gt;</code>, <code>guest:&lt;id&gt;</code>, <code>pending:&lt;id&gt;</code>, <code>team:&lt;teamId&gt;</code>); owners also get <code>transferCandidates</code>.</td>\n</tr>\n<tr>\n<td><code>grant_access</code></td>\n<td>Add a person (<code>email</code>) or team (<code>teamId</code>) with <code>role</code> <code>viewer</code> or <code>editor</code>; the silent add.</td>\n</tr>\n<tr>\n<td><code>invite_to_canvas</code></td>\n<td>The same add with a courtesy email.</td>\n</tr>\n<tr>\n<td><code>set_access_role</code></td>\n<td>Change one entry's role.</td>\n</tr>\n<tr>\n<td><code>revoke_access</code></td>\n<td>Remove an entry (the <code>owner</code> entry cannot be removed).</td>\n</tr>\n<tr>\n<td><code>search_people</code></td>\n<td>Eligible-people suggestions for a canvas you own or edit, or a team you can see.</td>\n</tr>\n<tr>\n<td><code>update_canvas</code></td>\n<td><code>access</code> (<code>private</code>, <code>whole_org</code>, <code>public_link</code>; the legacy <code>specific_people</code> / <code>team</code> are accepted as aliases of <code>private</code>), <code>discoverability</code>, <code>password</code>, <code>sharedExpiresAt</code>, <code>guestAiEnabled</code>, <code>guestAiCap</code>, <code>galleryListed</code>, <code>galleryTemplatable</code>, <code>description</code>, <code>tags</code>. The legacy <code>teamIds</code> replaces the viewer-team grants on the list.</td>\n</tr>\n<tr>\n<td><code>transfer_canvas</code></td>\n<td>Owner-only: hand the canvas to an existing editor by user id.</td>\n</tr>\n<tr>\n<td><code>list_shared_canvases</code></td>\n<td>The Shared view: canvases opened to you that you do not own or edit.</td>\n</tr>\n</tbody></table>\n",
    "headings": [
      {
        "id": "share-with-one-colleague",
        "text": "Share with one colleague",
        "level": 2
      },
      {
        "id": "general-access",
        "text": "General access",
        "level": 2
      },
      {
        "id": "slugs-are-not-access-control",
        "text": "Slugs are not access control",
        "level": 3
      },
      {
        "id": "access-vs-discovery",
        "text": "Access vs discovery",
        "level": 2
      },
      {
        "id": "roles-viewers-and-editors",
        "text": "Roles: viewers and editors",
        "level": 2
      },
      {
        "id": "two-editors-one-file",
        "text": "Two editors, one file",
        "level": 3
      },
      {
        "id": "transfer-ownership",
        "text": "Transfer ownership",
        "level": 3
      },
      {
        "id": "adding-specific-people",
        "text": "Adding specific people",
        "level": 2
      },
      {
        "id": "sharing-with-a-team",
        "text": "Sharing with a team",
        "level": 2
      },
      {
        "id": "protection-password-amp-expiry",
        "text": "Protection: password & expiry",
        "level": 2
      },
      {
        "id": "public-links",
        "text": "Public links",
        "level": 2
      },
      {
        "id": "ai-for-added-people",
        "text": "AI for added people",
        "level": 2
      },
      {
        "id": "gallery-description-and-tags",
        "text": "Gallery, description, and tags",
        "level": 2
      },
      {
        "id": "finding-canvases",
        "text": "Finding canvases",
        "level": 2
      },
      {
        "id": "when-an-admin-disables-a-canvas",
        "text": "When an admin disables a canvas",
        "level": 2
      },
      {
        "id": "revoking",
        "text": "Revoking",
        "level": 2
      },
      {
        "id": "over-mcp",
        "text": "Over MCP",
        "level": 2
      }
    ],
    "text": "Sharing & access You own or edit a canvas and want to decide who can open it, who edits it with you, and where it shows up. Every control on this page lives on the canvas's Share tab. Agents reach the same controls over MCP , through the same service and the same role checks. Every canvas starts Restricted : only you, the owner, can open it. Sharing is two controls plus optional layers, and they read the way Google Docs does: People and teams with direct access names who can open the canvas, each as a viewer or an editor . This list always applies , whatever the next control says. General access says who else can open it: Restricted (nobody beyond the list), Whole org , or Public link . Optionally add a password or a share expiry under Protection . Optionally opt into discovery: List for your org (the Shared page, Whole org only) or List in the gallery . Share with one colleague Publish first. Until the canvas has a live version, the Share tab shows one locked panel with a Publish button. Under People and teams with direct access , keep People selected, type their email, pick Viewer or Editor , and choose Add . That is all: they can open the canvas on their next request, with General access still on Restricted . Over MCP it is one call: grant_access { \"id\": \"<canvas id>\", \"email\": \"someone@example.com\", \"role\": \"viewer\" } Opening General access to Whole org or Public link on an unpublished or archived canvas is refused with SHARE_REQUIRES_PUBLISH (HTTP 409); the people-and-teams list can be filled in before publishing. Admins have no back door into your content. For a canvas they do not own, an admin is an ordinary member: a Restricted canvas returns 404, a password prompts them too, and they cannot open the editor or change settings. Cross-owner admin power is moderation and offboarding only: the all-canvases list, disable / re-enable / restore, and reassigning the owner when someone leaves. General access One choice per canvas, stored as access (default private ). The people and teams on the list are admitted at every choice ; General access only ever widens beyond them. Choice Who else can open it Backend primitives Restricted ( private ) Nobody beyond the owner, the editors, and the people and teams on the list. With nobody added, that is just you. Full (whatever the Backend tab enables), for everyone admitted. Whole org ( whole_org ) Any signed-in member with the link. Full, for members. Public link ( public_link ) Anyone with the URL, no sign-in, while the instance switch is on and the owner's account may publish publicly. None for the public: static files only. People on the list keep full access. Two legacy values, specific_people and team , are still accepted and stored by the API for compatibility. They are aliases of private : the dashboard shows them as Restricted , and the server treats all three identically. Nothing you set through the dashboard writes them. Editors are admitted at every choice, never see the password prompt, and are unaffected by expiry. Everyone else is evaluated per request, with nothing cached: a change to General access, the list, or the locks applies on the very next request. Anyone with no route in gets an opaque 404 , never a \"forbidden\" that confirms the canvas exists. When an org boundary is configured (the operator set CANVAS_DROP_ORG_NAME ; off by default), Whole org means members of this canvas's home org , not \"anyone signed in\". Members pick Personal or the workspace when they create a canvas (a fixed choice ). A Personal canvas cannot be shared org-wide: the choice is shown disabled, and the server refuses with ORG_REQUIRED (HTTP 409). A guest (a signed-in user in no org) never sees Whole-org canvases, only the ones they are added to. With no org configured, Whole org is \"any signed-in user\". Which choices you see depends on your account: Whole org is hidden for guests, and Public link appears only while your own account may publish publicly. A choice the canvas is already on stays visible either way. Slugs are not access control The random slug in a canvas URL ( quiet-otter-x7k2 ) is defense in depth, not access control. A custom slug ( team-dashboard ) is guessable, so the list and General access do all the work; the Share tab shows a heads-up when a canvas is link-reachable (Whole org or Public link) and uses a custom slug. Access vs discovery Access and discovery are separate settings: The people-and-teams list and General access decide who can open the URL. List for your org ( discoverability : link_only or listed , Whole org only) decides whether a Whole org share appears on the Shared page for every member of the home org. It never widens URL access. People and teams on the list always see the canvas in Shared (a team's members through the team), at every General-access choice and without any switch. Whole-org shares default to link-only : members can open the URL, but the canvas is not listed for them until you turn List for your org on, which also makes it eligible for the gallery. Listing a Whole-org canvas in the gallery turns this switch on as part of the same action; turning it off again also removes the canvas from the gallery. Public link canvases never appear in Shared; use the gallery for deliberate discovery. Roles: viewers and editors Every entry under People and teams with direct access , whether a person, a pending email, or a team, is a viewer or an editor . The owner is pinned first and has no role control. Use the People and Teams tabs to change what the single add form accepts; the access list stays visible below. Viewer Editor Open the canvas Always, at every General-access choice (the password and expiry still apply). Always, with no password prompt and no expiry. Edit the draft, publish, roll back, restore No Yes Change settings and sharing, add or remove people (other editors included) No Yes Deploy new versions, regenerate the deploy key, use the authoring routes No Yes Clone an eligible published canvas Yes, for a direct or team grant Yes Delete, transfer ownership, guest-AI opt-in No No: owner only ( OWNER_ONLY , HTTP 403) Editors see the canvas in Your canvases , marked editor · <owner> on a card and owned by <owner> · editor on a row, and can filter with Owned by me / Editing . Edited canvases do not appear in Shared. Only org members and teams can be editors. A guest (someone outside your org domains) is always a viewer ( GUEST_VIEWER_ONLY , HTTP 400), and a pending email can carry the editor role only when it belongs to an org domain. When an org boundary is configured, an editor grant holds only while the person is currently a member, checked on every request: someone who leaves the org loses edit access on their next request, and their editor session and realtime sockets are dropped. Demoting an editor keeps them on the list as a viewer, so the canvas moves to Shared as view-only; removing them ends their access on the next request. Entitlements follow the owner. An editor can switch on Public link only when the owner's account may publish publicly ( PUBLIC_LINK_OWNER_GATED , HTTP 403, otherwise). Usage and AI spend stay attributed to the canvas and its owner. Pick the role when you add someone ( Viewer is the default) and change it any time from the row. A person promoted to editor gets a courtesy email when outbound email is on; when an editor makes someone else an editor, the owner is notified too. After you remove or demote an editor, the dashboard offers to regenerate the deploy key : a key they copied keeps working until you do. Two editors, one file There is no real-time co-editing. Each draft save carries the hash of the file the editor loaded; if someone else saved that file first, the save is refused with DRAFT_CONFLICT (HTTP 409) carrying path , currentHash , updatedBy , updatedByName , and updatedAt . The editor keeps your unsaved text next to the other version so you can use theirs or overwrite. Edits to different files never conflict. Each publish records who created the version, so the Versions tab shows authorship. Transfer ownership Under owner-only Advanced , the owner can hand the canvas to any current editor who is an org member , including a member of an editor team; never to a team itself, and never by email (the picker lists eligible people; anyone else is refused with NOT_ELIGIBLE , HTTP 409). Editors do not see this section. The transfer applies at once and is audited: The recipient becomes the owner; you stay on as an editor while your account is active and still passes the org check. If the canvas was on Public link and the new owner's account cannot publish publicly, the rung is turned off ( publicLinkReverted ). The deploy key keeps working; the new owner can rotate it. When an owner leaves, an admin can reassign any of their canvases to another member from Admin → Canvases with a recorded reason. The previous owner stays an editor when their account is still active, both parties are emailed, and the deploy key is rotated in the same step (the new owner issues a fresh one). Adding specific people Under People and teams with direct access , leave the People tab selected and add by email as a viewer or an editor . The outcome is deterministic: Outcome Meaning granted An existing signed-in user; they can open the canvas now. pending A new email your auth setup can admit. It has no login power by itself; access materializes after that exact email signs in through the configured auth ( oidc , proxy , or dev ). already_added / already_pending Nothing changed. role_changed You passed a role for someone already listed. An omitted role never changes an existing entry. NOT_PERMITTED (403) A brand-new external email that policy does not let you admit. AUTH_ADMISSION_REQUIRED (403) The identity provider must admit that email before canvas-drop can grant it. GUEST_VIEWER_ONLY (400) Editor requested for a guest email. BLOCKED (403) That account is blocked. RATE_LIMITED (429) Too many adds in an hour, or the instance's pending-invite cap is reached. Try again later. Who may admit a brand-new external email : admins always can, from the Share tab or Admin → People . A non-admin member can only when the operator turns on Let members add brand-new emails ( invites.allowMemberNewEmails , off by default), or when the email can already authenticate through an allowed domain or an existing sign-in permit. Pending people show in the list with the role you gave them and can be re-roled or removed before they ever sign in. Removing an active or pending person takes effect on the next request. When outbound email is enabled ( CANVAS_DROP_EMAIL_DRIVER ), the person receives a courtesy sign-in or access email. There is no app-owned password or magic link; everyone authenticates the same way. In proxy mode the upstream identity-aware proxy owns admission. canvas-drop can record grants for people who already exist or are already admitted, but it cannot make a brand-new external email reachable on its own. Add that person to the upstream access policy first. Sharing with a team A team can be granted two ways from the Teams tab under People and teams with direct access , and either way the grant applies at every General-access choice: As viewers : every current member can open and use the canvas (full backend). As editors : every current member edits with you. People who join the team later are editors too; a member who leaves the team stops being one. You can grant only teams you belong to, and an org team must match the canvas's org ( TEAM_FORBIDDEN , HTTP 403, otherwise). The grant is independent of your own membership afterward: if you leave the team, the canvas stays shared with it until you change the list. For an org team, membership is re-checked against live org membership on every request, so someone removed from the org loses access immediately even if a stale team row lingers. For a personal team, direct membership is the boundary. Members find a team-shared canvas under Shared right away (or through list_shared_canvases over MCP); a canvas shared only with people and teams never appears in the gallery. Changing General access never touches team grants: only removing the team from the list does. Unlike Whole org , sharing with a team does not need an org workspace: any signed-in user can create a personal team and share even a Personal canvas with it. Manage teams on the Teams page. Protection: password & expiry Both live under Protection and apply on top of any General-access choice: Password : non-owners who can open the canvas are prompted before it loads, the people and teams on the list included. The password is stored as an argon2id hash and cannot be shown again; Generate produces one you can copy. Owners and editors are never prompted; a non-owner admin is. Share expiry : set a timestamp and access auto-revokes when it passes. The tab shows a countdown, then an expired notice; non-owners get a 404 until you clear or extend it. A canvas with a password cannot be listed in the gallery. Setting a password always un-lists the canvas, turns off its template setting, and clears its tags, whether or not it was listed at the time. Public links Public link lets anyone with the URL view the canvas with no sign-in, static-only : the page and its files serve, while every backend primitive (KV, files, AI, identity, realtime, Connections) is refused for public visitors, so the open internet never touches your org's spend, stored data, or granted outbound authority. Signed-in members hitting a public-link canvas are static-only too; only the owner and editors get full content. Two switches govern it: The instance-wide Public links enabled setting ( access.publicLinksEnabled , on by default). Turning it off makes every existing public link stop working immediately, and new attempts fail with PUBLIC_LINKS_DISABLED (HTTP 403). The per-account publish public entitlement an admin can grant or revoke. Without it the owner gets PUBLIC_NOT_ALLOWED , and an editor acting on that owner's canvas gets PUBLIC_LINK_OWNER_GATED (both HTTP 403). AI for added people Shown when a legacy guest session is on the people-and-teams list (at any General-access choice), and owner-only: Allow added people to use AI ( guestAiEnabled , off by default) plus a USD spend cap ( guestAiCap ). Added people can already use KV, files, and realtime when those capabilities are on; AI is metered and billed to the owner, so it is opt-in per canvas. An editor who touches these fields gets OWNER_ONLY . Gallery, description, and tags Gallery & templates on the Share tab is an opt-in listing. A canvas can be listed only when it is published, has no password , and is either Public link or Whole org (listing a Whole-org canvas also turns on List for your org ). A Restricted canvas never appears there, however many people and teams are on its list. When an org boundary is configured, Whole-org entries are returned only to signed-in members of the canvas's home org; Public-link entries are visible to any signed-in gallery viewer. Allow others to use as a template lets colleagues clone the canvas as a starting point; your canvas is untouched. A signed-in viewer with direct access, or through a team, can also clone the canvas while it is published, active, unexpired, and password-free, at any General-access choice. General access by itself does not grant cloning, and pending invitations and retained legacy guest sessions cannot clone. The copy is durable: it remains after the source grant is revoked, because it is a new canvas owned by the viewer. It starts unpublished and Restricted, with an empty direct-access list, backend off, and a fresh slug and deploy key. The description is one field (max 2000 characters) used everywhere the canvas is shown: Overview, Shared, grid cards, and the gallery. There is no separate gallery summary. Tags are one set per canvas, edited on the Overview tab (Enter or a comma confirms each; values are trimmed and lowercased; max 20 tags of up to 50 characters ). They power the tag filter in Your canvases , and once the canvas is gallery-listed the same tags show there and drive its tag shortcuts. Agents set both with update_canvas ( description , tags ). Finding canvases Your canvases , Shared , and the gallery share one forgiving search over a canvas's title, description, tags, and slug ; Shared also matches the owner's name and the access label (a team name, for example). Matching is case-, accent-, and whitespace-insensitive: café , Cafe , and caf all find \"Café Menu\". A multi-word query is AND-matched: every word must appear somewhere in those fields, in any combination. The same search backs the MCP list_canvases and list_shared_canvases query filters; list_canvases also takes a tags filter that matches canvases carrying any of the given tags. When an admin disables a canvas An admin can take a canvas down for moderation. A disabled canvas becomes read-only to its owner and editors : every mutation (settings, sharing, tags, capabilities, slug, preview, deploy / publish / rollback, archive / unpublish, draft edits) is refused with DISABLED (HTTP 409 over the management API, a DISABLED: … failure over MCP). Reads still work, so you can open the canvas, see its versions and usage, and read the takedown reason. You cannot delete a disabled canvas; an admin must re-enable or restore it first. Revoking Access is always revocable and never cached. Lowering the rung, removing a person, pending email, or team, hitting an expiry, regenerating the slug, or unpublishing takes effect on the next request and drops live realtime sockets. Legacy guest rows kept from older deployments ( guest:<id> entries) are revocation-only migration data; re-publishing a canvas does not restore old grants. Over MCP Every control above has an MCP tool that wraps the same service and role gate (owner or editor; a canvas you hold no role on reads as not found): Tool What it does list_access The people list with a role per entry and stable ids ( owner , member:<id> , guest:<id> , pending:<id> , team:<teamId> ); owners also get transferCandidates . grant_access Add a person ( email ) or team ( teamId ) with role viewer or editor ; the silent add. invite_to_canvas The same add with a courtesy email. set_access_role Change one entry's role. revoke_access Remove an entry (the owner entry cannot be removed). search_people Eligible-people suggestions for a canvas you own or edit, or a team you can see. update_canvas access ( private , whole_org , public_link ; the legacy specific_people / team are accepted as aliases of private ), discoverability , password , sharedExpiresAt , guestAiEnabled , guestAiCap , galleryListed , galleryTemplatable , description , tags . The legacy teamIds replaces the viewer-team grants on the list. transfer_canvas Owner-only: hand the canvas to an existing editor by user id. list_shared_canvases The Shared view: canvases opened to you that you do not own or edit."
  },
  {
    "path": "authoring/teams",
    "section": "Authoring canvases",
    "title": "Teams",
    "html": "<h1 id=\"teams\">Teams</h1>\n<p>A <strong>team</strong> is a named group you share canvases with — the slice between \"just me\"\nand \"everyone.\" Share a canvas with a team from its <strong>Share</strong> tab, and every member\ncan open and use it (full backend, like a member). Manage teams on the <strong>Teams</strong>\npage in the dashboard, or over <a href=\"/docs/agents/mcp\">MCP</a>.</p>\n<p>A team is one kind of entry under <strong>People and teams with direct access</strong>, next to\nindividual people. Whatever is on that list can open the canvas at every <strong>General access</strong>\nchoice:</p>\n<table>\n<thead>\n<tr>\n<th>General access</th>\n<th>Who else can open it</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Restricted</strong></td>\n<td>Nobody beyond the owner, the editors, and the people and teams on the list.</td>\n</tr>\n<tr>\n<td><strong>Whole org</strong></td>\n<td>Anyone signed in to your organization.</td>\n</tr>\n<tr>\n<td><strong>Public link</strong></td>\n<td>Anyone with the URL — static files only while the instance switch is on and the owner account has not been revoked.</td>\n</tr>\n</tbody></table>\n<p>See <a href=\"/docs/authoring/sharing\">Sharing &amp; access</a> for the full model and the\npassword / expiry modifiers.</p>\n<h2 id=\"personal-vs-org-teams\">Personal vs org teams</h2>\n<p>The kind of team is a <strong>fixed choice at creation</strong>:</p>\n<ul>\n<li><strong>Personal</strong> — friends &amp; family. <em>Any</em> signed-in user can create one (no org\nrequired) and add people by email when policy allows it. Direct membership is the access boundary.</li>\n<li><strong>Org-attached</strong> — a subset of your org. Only org members can create one, and\nmembers must belong to that org. Access is re-checked against your <strong>live</strong> org\nmembership on every request, so someone removed from the org loses access at once\n— even if a stale membership row lingers.</li>\n</ul>\n<p>Team names are <strong>per-creator</strong>: you can't make two teams with the same name, but\ndifferent people can each have a team named \"Design.\"</p>\n<h2 id=\"creating-a-team-and-adding-people\">Creating a team and adding people</h2>\n<p>On the <strong>Teams</strong> page, pick <strong>Personal</strong> (or your org), name the team, and create\nit — you're its first member and its manager. Then add people by email:</p>\n<ul>\n<li>An <strong>existing user</strong> joins immediately.</li>\n<li>A <strong>brand-new person</strong> (no account yet) becomes <strong>Pending sign-in</strong>. They\nturn into a full member the <strong>first time they sign in</strong> through your instance's\nconfigured auth — there's <strong>no app-owned password and no magic-link account</strong> to\nmanage. Until they sign in, they show as <em>Pending</em> on the roster and simply can't\nopen anything yet.</li>\n</ul>\n<p>This is <strong>auth-delegated</strong>: the identity provider is the only authority, so there's\nnothing to take over. See the\n<a href=\"/docs/self-hosting/security-model#adds-are-auth-delegated-no-app-owned-credentials\">security model</a>\nfor the full picture.</p>\n<blockquote>\n<p><strong>Who may add a brand-new external email is gated.</strong> A self-serve member can\nalways add existing users and people on your org's domains, but adding a\nbrand-new <em>external</em> email (one that can't already sign in) is <strong>admin-only</strong> unless\nthe operator turns on <code>invites.allowMemberNewEmails</code>. Adds are also\nrate-limited per person. See\n<a href=\"/docs/self-hosting/configuration#sign-in-permits--access-emails\">Sign-in permits &amp; access emails</a>.</p>\n</blockquote>\n<h2 id=\"the-roster\">The roster</h2>\n<p>Expanding a team shows its <strong>members</strong> plus any <strong>pending sign-ins</strong> (email-only\nrows for people who haven't signed in yet). Remove a member at any time — including\na <strong>pending</strong> invite, so a typo'd email can be taken back without an admin — or\n<strong>leave</strong> a team yourself (leaving asks you to confirm: you can't re-add yourself).\nCanceling a pending invite removes the team access it would have granted; if the\nadd also newly admitted that address to sign in to the instance, an admin can\nrevoke the sign-in permit from the <strong>People</strong> directory. The team's creator (or an\nadmin) can rename or delete it — deleting a team removes its memberships and\nunshares every canvas shared with it, but the canvases themselves are untouched.</p>\n<h2 id=\"sharing-a-canvas-with-a-team\">Sharing a canvas with a team</h2>\n<p>On a canvas's <strong>Share</strong> tab, open the <strong>Teams</strong> tab under <strong>People and teams with direct access</strong>, pick a team you belong\nto, and add it as <strong>Viewer</strong> (or <strong>Editor</strong>). The list labels each team <strong>Personal</strong> or by\nits <strong>org</strong>, so you can see how far the share reaches. A team grant is independent of your\nown membership afterward: if you later leave the team, the canvas stays shared with it\nuntil you remove it from the list. Changing <strong>General access</strong> never touches it.</p>\n<p>Members can open the canvas at once and find it under <strong>Shared</strong> in the dashboard (or\nthrough <code>list_shared_canvases</code> over MCP) — there is no listing switch to turn on. A canvas\nshared only with people and teams never appears in the org-wide gallery.</p>\n<h2 id=\"editor-teams\">Editor teams</h2>\n<p>A team can also be granted the <strong>editor</strong> role on a canvas, from the <strong>Teams</strong> tab under\n<strong>People and teams with direct access</strong> (choose the team, pick <strong>Editor</strong>). Every current member\nthen manages that canvas like the owner — content, publishing, settings, sharing —\nexcept deleting or transferring it; membership changes apply on the next request, and\nfor an org team the live org membership is re-checked as well. A team added as\n<strong>viewers</strong> opens the canvas for its members at every General-access choice. See\n<a href=\"/docs/authoring/sharing#roles-viewers-and-editors\">Roles: viewers and editors</a>.</p>\n<h2 id=\"over-mcp-agents\">Over MCP (agents)</h2>\n<p>Everything here is available to an agent over <a href=\"/docs/agents/mcp\">MCP</a>, wrapping the\nsame service the dashboard uses: <code>create_team</code> (omit <code>orgId</code> for a personal team),\n<code>add_team_member</code> (returns <code>granted</code> or <code>pending</code>), <code>cancel_team_invite</code> (take back\na pending invite by its roster <code>id</code>), <code>list_team_members</code> (members +\npending), <code>grant_access</code> (with <code>teamId</code> and a <code>role</code> of <code>viewer</code> or <code>editor</code>), <code>set_access_role</code>,\n<code>revoke_access</code>, and <code>list_shared_canvases</code>. The legacy <code>update_canvas</code> fields <code>access: \"team\"</code>\n(an alias of <code>private</code>) and <code>teamIds</code> (replace the viewer-team grants) are still accepted;\nteam members see a shared canvas in Shared without any <code>discoverability</code> setting.</p>\n",
    "headings": [
      {
        "id": "personal-vs-org-teams",
        "text": "Personal vs org teams",
        "level": 2
      },
      {
        "id": "creating-a-team-and-adding-people",
        "text": "Creating a team and adding people",
        "level": 2
      },
      {
        "id": "the-roster",
        "text": "The roster",
        "level": 2
      },
      {
        "id": "sharing-a-canvas-with-a-team",
        "text": "Sharing a canvas with a team",
        "level": 2
      },
      {
        "id": "editor-teams",
        "text": "Editor teams",
        "level": 2
      },
      {
        "id": "over-mcp-agents",
        "text": "Over MCP (agents)",
        "level": 2
      }
    ],
    "text": "Teams A team is a named group you share canvases with — the slice between \"just me\" and \"everyone.\" Share a canvas with a team from its Share tab, and every member can open and use it (full backend, like a member). Manage teams on the Teams page in the dashboard, or over MCP . A team is one kind of entry under People and teams with direct access , next to individual people. Whatever is on that list can open the canvas at every General access choice: General access Who else can open it Restricted Nobody beyond the owner, the editors, and the people and teams on the list. Whole org Anyone signed in to your organization. Public link Anyone with the URL — static files only while the instance switch is on and the owner account has not been revoked. See Sharing & access for the full model and the password / expiry modifiers. Personal vs org teams The kind of team is a fixed choice at creation : Personal — friends & family. Any signed-in user can create one (no org required) and add people by email when policy allows it. Direct membership is the access boundary. Org-attached — a subset of your org. Only org members can create one, and members must belong to that org. Access is re-checked against your live org membership on every request, so someone removed from the org loses access at once — even if a stale membership row lingers. Team names are per-creator : you can't make two teams with the same name, but different people can each have a team named \"Design.\" Creating a team and adding people On the Teams page, pick Personal (or your org), name the team, and create it — you're its first member and its manager. Then add people by email: An existing user joins immediately. A brand-new person (no account yet) becomes Pending sign-in . They turn into a full member the first time they sign in through your instance's configured auth — there's no app-owned password and no magic-link account to manage. Until they sign in, they show as Pending on the roster and simply can't open anything yet. This is auth-delegated : the identity provider is the only authority, so there's nothing to take over. See the security model for the full picture. Who may add a brand-new external email is gated. A self-serve member can always add existing users and people on your org's domains, but adding a brand-new external email (one that can't already sign in) is admin-only unless the operator turns on invites.allowMemberNewEmails . Adds are also rate-limited per person. See Sign-in permits & access emails . The roster Expanding a team shows its members plus any pending sign-ins (email-only rows for people who haven't signed in yet). Remove a member at any time — including a pending invite, so a typo'd email can be taken back without an admin — or leave a team yourself (leaving asks you to confirm: you can't re-add yourself). Canceling a pending invite removes the team access it would have granted; if the add also newly admitted that address to sign in to the instance, an admin can revoke the sign-in permit from the People directory. The team's creator (or an admin) can rename or delete it — deleting a team removes its memberships and unshares every canvas shared with it, but the canvases themselves are untouched. Sharing a canvas with a team On a canvas's Share tab, open the Teams tab under People and teams with direct access , pick a team you belong to, and add it as Viewer (or Editor ). The list labels each team Personal or by its org , so you can see how far the share reaches. A team grant is independent of your own membership afterward: if you later leave the team, the canvas stays shared with it until you remove it from the list. Changing General access never touches it. Members can open the canvas at once and find it under Shared in the dashboard (or through list_shared_canvases over MCP) — there is no listing switch to turn on. A canvas shared only with people and teams never appears in the org-wide gallery. Editor teams A team can also be granted the editor role on a canvas, from the Teams tab under People and teams with direct access (choose the team, pick Editor ). Every current member then manages that canvas like the owner — content, publishing, settings, sharing — except deleting or transferring it; membership changes apply on the next request, and for an org team the live org membership is re-checked as well. A team added as viewers opens the canvas for its members at every General-access choice. See Roles: viewers and editors . Over MCP (agents) Everything here is available to an agent over MCP , wrapping the same service the dashboard uses: create_team (omit orgId for a personal team), add_team_member (returns granted or pending ), cancel_team_invite (take back a pending invite by its roster id ), list_team_members (members + pending), grant_access (with teamId and a role of viewer or editor ), set_access_role , revoke_access , and list_shared_canvases . The legacy update_canvas fields access: \"team\" (an alias of private ) and teamIds (replace the viewer-team grants) are still accepted; team members see a shared canvas in Shared without any discoverability setting."
  },
  {
    "path": "authoring/capabilities",
    "section": "Authoring canvases",
    "title": "Capabilities",
    "html": "<h1 id=\"capabilities\">Capabilities</h1>\n<p>Give a canvas a backend, one feature at a time, and know exactly when a call\nfrom the page will work. This page is for a canvas's owner or editor. A canvas\nis static until you switch <strong>Enable backend</strong> on in its <strong>Backend</strong> tab. With\nthe backend on, five features toggle independently: KV, files, AI, realtime, and\nauthoring. Identity (<code>me()</code>) has no toggle; it is on whenever the backend is on.\nOutbound Connections also has no owner-controlled feature toggle: each reusable\nprofile is granted to the canvas by an instance administrator and remains gated\nby the same Backend master switch.</p>\n<h2 id=\"turn-on-the-backend\">Turn on the backend</h2>\n<ol>\n<li>Open the canvas and go to the <strong>Backend</strong> tab.</li>\n<li>Switch <strong>Enable backend</strong> on. It is off by default. You can also set it when\nyou create the canvas: <strong>Enable backend (optional)</strong> on the create page, or\n<code>\"backendEnabled\": true</code> on <code>POST /api/canvases</code> and <code>POST /api/canvases/paste</code>.</li>\n<li>Check the feature toggles. <strong>Key-value storage</strong>, <strong>File storage</strong>, <strong>AI</strong>,\nand <strong>Realtime</strong> are on by default, so they go live as soon as the backend is\non (where the instance supports them). <strong>Authoring</strong> starts off and stays off\nuntil you turn it on.</li>\n</ol>\n<p>Then call the features from the page through <code>window.canvasdrop</code>. No keys, no\nsetup:</p>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">src</span>=<span class=\"hljs-string\">\"/sdk/v1.js\"</span>&gt;</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">type</span>=<span class=\"hljs-string\">\"module\"</span>&gt;</span><span class=\"language-javascript\">\n  <span class=\"hljs-keyword\">const</span> me = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-title function_\">me</span>();                 <span class=\"hljs-comment\">// on whenever the backend is on</span>\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">set</span>(<span class=\"hljs-string\">\"last-viewer\"</span>, me.<span class=\"hljs-property\">name</span>);  <span class=\"hljs-comment\">// needs Key-value storage on</span>\n</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span></code></pre>\n<p>Agents and scripts flip the same switches. The MCP tool <code>set_capabilities</code> and\n<code>PATCH {base}/api/canvases/{id}/capabilities</code> take the same body: any subset of\n<code>backendEnabled</code>, <code>kv</code>, <code>files</code>, <code>ai</code>, <code>realtime</code>, <code>authoring</code> as booleans.\nOmitted fields are unchanged.</p>\n<pre><code class=\"hljs language-json\"><span class=\"hljs-punctuation\">{</span> <span class=\"hljs-attr\">\"backendEnabled\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-literal\"><span class=\"hljs-keyword\">true</span></span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"realtime\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-literal\"><span class=\"hljs-keyword\">false</span></span> <span class=\"hljs-punctuation\">}</span></code></pre>\n<p>The response is the canvas view. Every canvas view (the management API and the\nMCP <code>get_canvas</code> tool included) carries two objects: <code>capabilities</code> (what is\nstored) and <code>effective</code> (what runs right now, after the instance switches\nbelow are applied).</p>\n<p>Owners and editors can change capabilities; viewers cannot. Every change is\naudited (<code>capabilities_update</code>, with the list of changed fields) and applies on\nthe next request. Turning the backend or realtime off also drops the canvas's\nlive realtime sockets. A canvas an admin has disabled refuses the change with\n<code>409 DISABLED</code>.</p>\n<h2 id=\"the-toggles\">The toggles</h2>\n<table>\n<thead>\n<tr>\n<th>Backend tab row</th>\n<th>Key</th>\n<th>Stored default</th>\n<th>What it gives the canvas</th>\n<th>SDK</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Enable backend</td>\n<td><code>backendEnabled</code></td>\n<td>off</td>\n<td>The master switch; nothing below runs without it</td>\n<td></td>\n</tr>\n<tr>\n<td>Identity (no toggle)</td>\n<td><code>identity</code></td>\n<td>follows the backend</td>\n<td>The signed-in viewer: id, email, name, avatar</td>\n<td><a href=\"/docs/sdk/identity\"><code>me()</code></a></td>\n</tr>\n<tr>\n<td>Key-value storage</td>\n<td><code>kv</code></td>\n<td>on</td>\n<td>Shared and per-viewer JSON storage, atomic increment</td>\n<td><a href=\"/docs/sdk/kv\"><code>kv</code></a></td>\n</tr>\n<tr>\n<td>File storage</td>\n<td><code>files</code></td>\n<td>on</td>\n<td>Upload, list, delete, and serve files</td>\n<td><a href=\"/docs/sdk/files\"><code>files</code></a></td>\n</tr>\n<tr>\n<td>AI</td>\n<td><code>ai</code></td>\n<td>on</td>\n<td>Server-side model calls; no provider key in the page</td>\n<td><a href=\"/docs/sdk/ai\"><code>ai</code></a></td>\n</tr>\n<tr>\n<td>Realtime</td>\n<td><code>realtime</code></td>\n<td>on</td>\n<td>Ephemeral pub/sub and presence over WebSockets</td>\n<td><a href=\"/docs/sdk/realtime\"><code>realtime</code></a></td>\n</tr>\n<tr>\n<td>Connections</td>\n<td>no feature flag</td>\n<td>no grants</td>\n<td>Bounded requests to exact HTTPS origins an admin attached to this canvas</td>\n<td><a href=\"/docs/sdk/connections\"><code>connections</code></a></td>\n</tr>\n<tr>\n<td>Authoring</td>\n<td><code>authoring</code></td>\n<td>off</td>\n<td>A signed-in viewer creates and manages canvases from the page, as themselves</td>\n<td><a href=\"/docs/sdk/authoring\"><code>canvases</code></a></td>\n</tr>\n</tbody></table>\n<p>The feature toggles are disabled in the UI while the backend is off. Their\nstored values are kept, so switching the backend back on restores the same set.\nThe Identity row reads <strong>Always on</strong> when the backend is on and <strong>Off</strong> when it\nis not.</p>\n<h2 id=\"when-a-feature-is-effective\">When a feature is effective</h2>\n<p>A feature runs only when every gate in its row is open: the backend, its own\ntoggle, and (for AI, realtime, and authoring) an instance switch the operator\ncontrols. The server applies this rule on each request; the Backend tab shows\nthe outcome, and <code>effective</code> in the API is the same answer.</p>\n<table>\n<thead>\n<tr>\n<th>Feature</th>\n<th>Backend on</th>\n<th>Its toggle on</th>\n<th>Instance switch</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Identity (<code>me()</code>)</td>\n<td>yes</td>\n<td>none</td>\n<td>none</td>\n</tr>\n<tr>\n<td>KV</td>\n<td>yes</td>\n<td>yes</td>\n<td>none</td>\n</tr>\n<tr>\n<td>Files</td>\n<td>yes</td>\n<td>yes</td>\n<td>none</td>\n</tr>\n<tr>\n<td>AI</td>\n<td>yes</td>\n<td>yes</td>\n<td>An AI provider key is configured: <code>CANVAS_DROP_AI_API_KEY</code>, or the <strong>Provider API key</strong> an admin sets in Admin → Settings</td>\n</tr>\n<tr>\n<td>Realtime</td>\n<td>yes</td>\n<td>yes</td>\n<td><code>CANVAS_DROP_REALTIME=on</code> (the default)</td>\n</tr>\n<tr>\n<td>Connections</td>\n<td>yes</td>\n<td>an enabled profile is attached</td>\n<td><code>CANVAS_DROP_CONNECTIONS_ENCRYPTION_KEY</code> is available when protected headers are configured</td>\n</tr>\n<tr>\n<td>Authoring</td>\n<td>yes</td>\n<td>yes</td>\n<td><code>CANVAS_DROP_AUTHORING=on</code> (default <code>off</code>), or <strong>Authoring enabled</strong> set by an admin in Admin → Settings</td>\n</tr>\n</tbody></table>\n<p>KV and files have no instance switch: your two toggles are the whole story.\nWhen your toggle is on but the instance switch is off, the toggle stays on and\nthe row is labelled <strong>Disabled by your administrator for this instance.</strong> The\nAI key and the authoring switch are read per request, so an admin's change\napplies immediately. Realtime follows <code>CANVAS_DROP_REALTIME</code> as set when the\nserver started; the admin panel shows it but cannot change it.</p>\n<h2 id=\"limits\">Limits</h2>\n<p>Each feature has fixed ceilings. Exceeding one returns the error named in the\nlast column, not <code>CAPABILITY_DISABLED</code>; see <a href=\"/docs/api/errors\">error codes</a>.</p>\n<table>\n<thead>\n<tr>\n<th>Feature</th>\n<th>Limit</th>\n<th>Admin-adjustable</th>\n<th>Error</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>KV</td>\n<td>64 KB per value, 512 bytes per key; 10 000 shared keys and 1 000 per-viewer keys per canvas</td>\n<td>the key counts</td>\n<td><code>VALUE_TOO_LARGE</code>, <code>KEY_TOO_LARGE</code>, <code>KEY_LIMIT</code></td>\n</tr>\n<tr>\n<td>Files</td>\n<td>25 MB per file, 1 GB per canvas</td>\n<td>both</td>\n<td><code>FILE_TOO_LARGE</code>, <code>QUOTA_EXCEEDED</code></td>\n</tr>\n<tr>\n<td>AI</td>\n<td>Models on the allowlist (<code>CANVAS_DROP_AI_MODELS</code>); spend caps of <code>CANVAS_DROP_AI_USER_DAILY_USD</code> (default <code>5</code>) per viewer per day and <code>CANVAS_DROP_AI_CANVAS_MONTHLY_USD</code> (default <code>50</code>) per canvas per month</td>\n<td>allowlist and both caps</td>\n<td><code>MODEL_NOT_ALLOWED</code>, <code>QUOTA_EXCEEDED</code></td>\n</tr>\n<tr>\n<td>Realtime</td>\n<td>30 concurrent connections per canvas, 16 KB per message</td>\n<td>no</td>\n<td><code>CONNECTION_LIMIT</code> (socket close <code>4429</code>)</td>\n</tr>\n<tr>\n<td>Connections</td>\n<td>8 KiB URL; 32/16 KiB caller headers; 256 KiB request; 2 MiB response; 10 s; 3 redirects; 60/min actor+canvas+profile; 600/min profile; 5 concurrent/canvas; 50/process</td>\n<td>env-only</td>\n<td><code>REQUEST_TOO_LARGE</code>, <code>RESPONSE_TOO_LARGE</code>, <code>UPSTREAM_TIMEOUT</code>, <code>CONNECTION_RATE_LIMIT</code>, <code>CONNECTION_LIMIT</code></td>\n</tr>\n</tbody></table>\n<h2 id=\"when-a-feature-is-off\">When a feature is off</h2>\n<p>A call to a feature that is off fails with a <code>403</code> whose body names the gate\nthat failed:</p>\n<pre><code class=\"hljs language-json\"><span class=\"hljs-punctuation\">{</span>\n  <span class=\"hljs-attr\">\"code\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"CAPABILITY_DISABLED\"</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"capability\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"kv\"</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"backendEnabled\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-literal\"><span class=\"hljs-keyword\">false</span></span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"reason\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"backend_off\"</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"hint\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"This canvas's backend is off (the master switch, off by default). Turn it on in the dashboard Backend tab, the set_capabilities MCP tool, or PATCH /api/canvases/:id/capabilities {\\\"backendEnabled\\\": true}.\"</span>\n<span class=\"hljs-punctuation\">}</span></code></pre>\n<p><code>reason</code> is <code>backend_off</code>, <code>feature_off</code>, or <code>operator_disabled</code>; <code>hint</code> says\nwhat to turn on. The SDK throws a <code>CapabilityDisabledError</code>\n(<code>err.code === \"CAPABILITY_DISABLED\"</code>, <code>err.status === 403</code>) and exposes the\nserver hint as <code>err.hint</code>, which is also the error message:</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">try</span> {\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">set</span>(<span class=\"hljs-string\">\"count\"</span>, <span class=\"hljs-number\">1</span>);\n} <span class=\"hljs-keyword\">catch</span> (err) {\n  <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"CAPABILITY_DISABLED\"</span>) <span class=\"hljs-variable language_\">console</span>.<span class=\"hljs-title function_\">log</span>(err.<span class=\"hljs-property\">hint</span>);\n  <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">throw</span> err;\n}</code></pre>\n<p>Realtime reports the same condition over the socket: a connection opened while\nrealtime is off receives one\n<code>{ \"type\": \"error\", \"code\": \"CAPABILITY_DISABLED\", \"capability\": \"realtime\" }</code>\nframe and is closed with code <code>4403</code>. A socket that is already open when\nrealtime is turned off is closed with <code>4403</code> too. The SDK turns both into the\nsame <code>CapabilityDisabledError</code> and does not reconnect. See\n<a href=\"/docs/api/errors\">error codes</a> for the full list.</p>\n<h2 id=\"public-links-are-static-only\">Public links are static-only</h2>\n<p>On the <strong>Public link</strong> rung (<code>public_link</code>) the server serves the canvas's files\nto anyone with the URL and refuses every primitive with <code>403 STATIC_ONLY</code> for\neveryone except the canvas's owner and editors. Signed-in org members are\nrefused too. The Backend tab shows a warning when a public-link canvas has its\nbackend on. If the canvas needs a backend for its audience, share it on a more\nrestricted rung; see <a href=\"/docs/authoring/sharing\">Sharing &amp; access</a>.</p>\n<h2 id=\"authoring\">Authoring</h2>\n<p>Authoring lets a signed-in org member viewing your canvas create a new canvas\nfrom the page, as themselves, through <code>canvasdrop.canvases.publish(...)</code>. The\nnew canvas is created under the viewer's own account and appears in their\ndashboard; they can <code>update</code>, <code>list</code>, and <code>revoke</code> it later. Legacy guest\nsessions and public-link visitors cannot use it.</p>\n<p>Because it mints canvases, authoring is the one feature whose stored flag starts\noff, and its instance switch (<code>CANVAS_DROP_AUTHORING</code>) is off by default as\nwell. The operator also sets the policy a publish is checked against:</p>\n<table>\n<thead>\n<tr>\n<th>Policy</th>\n<th>Env var</th>\n<th>Default</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Canvases one viewer may publish per day</td>\n<td><code>CANVAS_DROP_AUTHORING_USER_DAILY_MAX</code></td>\n<td><code>20</code></td>\n</tr>\n<tr>\n<td>Canvases one viewer may publish in total</td>\n<td><code>CANVAS_DROP_AUTHORING_USER_TOTAL_MAX</code></td>\n<td><code>200</code></td>\n</tr>\n<tr>\n<td>Access rungs a publish may request</td>\n<td><code>CANVAS_DROP_AUTHORING_ALLOWED_RUNGS</code></td>\n<td><code>private,specific_people,whole_org,public_link</code></td>\n</tr>\n<tr>\n<td>Longest allowed share expiry, in days</td>\n<td><code>CANVAS_DROP_AUTHORING_MAX_EXPIRY_DAYS</code></td>\n<td><code>0</code> (no cap)</td>\n</tr>\n<tr>\n<td>Whether a share expiry is required</td>\n<td><code>CANVAS_DROP_AUTHORING_REQUIRE_EXPIRY</code></td>\n<td><code>false</code></td>\n</tr>\n</tbody></table>\n<p>An admin can change the two quotas at runtime in Admin → Settings; the rung and\nexpiry policy is env-only (<code>specific_people</code> is a legacy alias of <code>private</code>). Requiring\nan expiry applies only to <strong>Whole org</strong> and <strong>Public link</strong> publishes. Restricted\npublishes (<code>private</code>, <code>specific_people</code>, or <code>team</code>) do not require one. See the\n<a href=\"/docs/sdk/authoring\">authoring SDK reference</a>.</p>\n<h2 id=\"clones-start-static\">Clones start static</h2>\n<p>Duplicating a canvas, or using a gallery template, creates a new canvas with the\nbackend off and the feature flags at their defaults (KV, files, AI, and realtime\non; authoring off). KV data, files, and usage are not copied. The new owner\nturns the backend on when they need it.</p>\n<h2 id=\"why-off-by-default\">Why off by default</h2>\n<p>Capabilities are enforced by the server, per request, from the signed-in\nsession: the canvas can ask, the server decides. Canvas files never carry a\nsecret, and a static canvas has no backend surface at all. Turning a capability\non is normally a per-canvas choice its owner or an editor makes. Connections is\nthe deliberate exception: only an admin defines profiles and attaches or revokes\ntheir grants, while owners and editors inspect the non-secret authority in the\nBackend tab.</p>\n",
    "headings": [
      {
        "id": "turn-on-the-backend",
        "text": "Turn on the backend",
        "level": 2
      },
      {
        "id": "the-toggles",
        "text": "The toggles",
        "level": 2
      },
      {
        "id": "when-a-feature-is-effective",
        "text": "When a feature is effective",
        "level": 2
      },
      {
        "id": "limits",
        "text": "Limits",
        "level": 2
      },
      {
        "id": "when-a-feature-is-off",
        "text": "When a feature is off",
        "level": 2
      },
      {
        "id": "public-links-are-static-only",
        "text": "Public links are static-only",
        "level": 2
      },
      {
        "id": "authoring",
        "text": "Authoring",
        "level": 2
      },
      {
        "id": "clones-start-static",
        "text": "Clones start static",
        "level": 2
      },
      {
        "id": "why-off-by-default",
        "text": "Why off by default",
        "level": 2
      }
    ],
    "text": "Capabilities Give a canvas a backend, one feature at a time, and know exactly when a call from the page will work. This page is for a canvas's owner or editor. A canvas is static until you switch Enable backend on in its Backend tab. With the backend on, five features toggle independently: KV, files, AI, realtime, and authoring. Identity ( me() ) has no toggle; it is on whenever the backend is on. Outbound Connections also has no owner-controlled feature toggle: each reusable profile is granted to the canvas by an instance administrator and remains gated by the same Backend master switch. Turn on the backend Open the canvas and go to the Backend tab. Switch Enable backend on. It is off by default. You can also set it when you create the canvas: Enable backend (optional) on the create page, or \"backendEnabled\": true on POST /api/canvases and POST /api/canvases/paste . Check the feature toggles. Key-value storage , File storage , AI , and Realtime are on by default, so they go live as soon as the backend is on (where the instance supports them). Authoring starts off and stays off until you turn it on. Then call the features from the page through window.canvasdrop . No keys, no setup: < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); // on whenever the backend is on await canvasdrop. kv . set ( \"last-viewer\" , me. name ); // needs Key-value storage on </ script > Agents and scripts flip the same switches. The MCP tool set_capabilities and PATCH {base}/api/canvases/{id}/capabilities take the same body: any subset of backendEnabled , kv , files , ai , realtime , authoring as booleans. Omitted fields are unchanged. { \"backendEnabled\" : true , \"realtime\" : false } The response is the canvas view. Every canvas view (the management API and the MCP get_canvas tool included) carries two objects: capabilities (what is stored) and effective (what runs right now, after the instance switches below are applied). Owners and editors can change capabilities; viewers cannot. Every change is audited ( capabilities_update , with the list of changed fields) and applies on the next request. Turning the backend or realtime off also drops the canvas's live realtime sockets. A canvas an admin has disabled refuses the change with 409 DISABLED . The toggles Backend tab row Key Stored default What it gives the canvas SDK Enable backend backendEnabled off The master switch; nothing below runs without it Identity (no toggle) identity follows the backend The signed-in viewer: id, email, name, avatar me() Key-value storage kv on Shared and per-viewer JSON storage, atomic increment kv File storage files on Upload, list, delete, and serve files files AI ai on Server-side model calls; no provider key in the page ai Realtime realtime on Ephemeral pub/sub and presence over WebSockets realtime Connections no feature flag no grants Bounded requests to exact HTTPS origins an admin attached to this canvas connections Authoring authoring off A signed-in viewer creates and manages canvases from the page, as themselves canvases The feature toggles are disabled in the UI while the backend is off. Their stored values are kept, so switching the backend back on restores the same set. The Identity row reads Always on when the backend is on and Off when it is not. When a feature is effective A feature runs only when every gate in its row is open: the backend, its own toggle, and (for AI, realtime, and authoring) an instance switch the operator controls. The server applies this rule on each request; the Backend tab shows the outcome, and effective in the API is the same answer. Feature Backend on Its toggle on Instance switch Identity ( me() ) yes none none KV yes yes none Files yes yes none AI yes yes An AI provider key is configured: CANVAS_DROP_AI_API_KEY , or the Provider API key an admin sets in Admin → Settings Realtime yes yes CANVAS_DROP_REALTIME=on (the default) Connections yes an enabled profile is attached CANVAS_DROP_CONNECTIONS_ENCRYPTION_KEY is available when protected headers are configured Authoring yes yes CANVAS_DROP_AUTHORING=on (default off ), or Authoring enabled set by an admin in Admin → Settings KV and files have no instance switch: your two toggles are the whole story. When your toggle is on but the instance switch is off, the toggle stays on and the row is labelled Disabled by your administrator for this instance. The AI key and the authoring switch are read per request, so an admin's change applies immediately. Realtime follows CANVAS_DROP_REALTIME as set when the server started; the admin panel shows it but cannot change it. Limits Each feature has fixed ceilings. Exceeding one returns the error named in the last column, not CAPABILITY_DISABLED ; see error codes . Feature Limit Admin-adjustable Error KV 64 KB per value, 512 bytes per key; 10 000 shared keys and 1 000 per-viewer keys per canvas the key counts VALUE_TOO_LARGE , KEY_TOO_LARGE , KEY_LIMIT Files 25 MB per file, 1 GB per canvas both FILE_TOO_LARGE , QUOTA_EXCEEDED AI Models on the allowlist ( CANVAS_DROP_AI_MODELS ); spend caps of CANVAS_DROP_AI_USER_DAILY_USD (default 5 ) per viewer per day and CANVAS_DROP_AI_CANVAS_MONTHLY_USD (default 50 ) per canvas per month allowlist and both caps MODEL_NOT_ALLOWED , QUOTA_EXCEEDED Realtime 30 concurrent connections per canvas, 16 KB per message no CONNECTION_LIMIT (socket close 4429 ) Connections 8 KiB URL; 32/16 KiB caller headers; 256 KiB request; 2 MiB response; 10 s; 3 redirects; 60/min actor+canvas+profile; 600/min profile; 5 concurrent/canvas; 50/process env-only REQUEST_TOO_LARGE , RESPONSE_TOO_LARGE , UPSTREAM_TIMEOUT , CONNECTION_RATE_LIMIT , CONNECTION_LIMIT When a feature is off A call to a feature that is off fails with a 403 whose body names the gate that failed: { \"code\" : \"CAPABILITY_DISABLED\" , \"capability\" : \"kv\" , \"backendEnabled\" : false , \"reason\" : \"backend_off\" , \"hint\" : \"This canvas's backend is off (the master switch, off by default). Turn it on in the dashboard Backend tab, the set_capabilities MCP tool, or PATCH /api/canvases/:id/capabilities {\\\"backendEnabled\\\": true}.\" } reason is backend_off , feature_off , or operator_disabled ; hint says what to turn on. The SDK throws a CapabilityDisabledError ( err.code === \"CAPABILITY_DISABLED\" , err.status === 403 ) and exposes the server hint as err.hint , which is also the error message: try { await canvasdrop. kv . set ( \"count\" , 1 ); } catch (err) { if (err. code === \"CAPABILITY_DISABLED\" ) console . log (err. hint ); else throw err; } Realtime reports the same condition over the socket: a connection opened while realtime is off receives one { \"type\": \"error\", \"code\": \"CAPABILITY_DISABLED\", \"capability\": \"realtime\" } frame and is closed with code 4403 . A socket that is already open when realtime is turned off is closed with 4403 too. The SDK turns both into the same CapabilityDisabledError and does not reconnect. See error codes for the full list. Public links are static-only On the Public link rung ( public_link ) the server serves the canvas's files to anyone with the URL and refuses every primitive with 403 STATIC_ONLY for everyone except the canvas's owner and editors. Signed-in org members are refused too. The Backend tab shows a warning when a public-link canvas has its backend on. If the canvas needs a backend for its audience, share it on a more restricted rung; see Sharing & access . Authoring Authoring lets a signed-in org member viewing your canvas create a new canvas from the page, as themselves, through canvasdrop.canvases.publish(...) . The new canvas is created under the viewer's own account and appears in their dashboard; they can update , list , and revoke it later. Legacy guest sessions and public-link visitors cannot use it. Because it mints canvases, authoring is the one feature whose stored flag starts off, and its instance switch ( CANVAS_DROP_AUTHORING ) is off by default as well. The operator also sets the policy a publish is checked against: Policy Env var Default Canvases one viewer may publish per day CANVAS_DROP_AUTHORING_USER_DAILY_MAX 20 Canvases one viewer may publish in total CANVAS_DROP_AUTHORING_USER_TOTAL_MAX 200 Access rungs a publish may request CANVAS_DROP_AUTHORING_ALLOWED_RUNGS private,specific_people,whole_org,public_link Longest allowed share expiry, in days CANVAS_DROP_AUTHORING_MAX_EXPIRY_DAYS 0 (no cap) Whether a share expiry is required CANVAS_DROP_AUTHORING_REQUIRE_EXPIRY false An admin can change the two quotas at runtime in Admin → Settings; the rung and expiry policy is env-only ( specific_people is a legacy alias of private ). Requiring an expiry applies only to Whole org and Public link publishes. Restricted publishes ( private , specific_people , or team ) do not require one. See the authoring SDK reference . Clones start static Duplicating a canvas, or using a gallery template, creates a new canvas with the backend off and the feature flags at their defaults (KV, files, AI, and realtime on; authoring off). KV data, files, and usage are not copied. The new owner turns the backend on when they need it. Why off by default Capabilities are enforced by the server, per request, from the signed-in session: the canvas can ask, the server decides. Canvas files never carry a secret, and a static canvas has no backend surface at all. Turning a capability on is normally a per-canvas choice its owner or an editor makes. Connections is the deliberate exception: only an admin defines profiles and attaches or revokes their grants, while owners and editors inspect the non-secret authority in the Backend tab."
  },
  {
    "path": "authoring/editor",
    "section": "Authoring canvases",
    "title": "The editor",
    "html": "<h1 id=\"the-editor\">The editor</h1>\n<p>Change a canvas's files in the browser, check the result, and publish when you are\nready. Nothing viewers see changes until you publish. The editor is the <strong>Editor</strong>\ntab of any canvas you own or edit; there is no local setup and no build step.</p>\n<p>The loop:</p>\n<ol>\n<li>Open the canvas and pick <strong>Editor</strong>. On first open the draft is seeded from the\ncurrent published version, or starts empty for a canvas that has never published.</li>\n<li>Edit files in the tree. Changes autosave to the draft 700 ms after you stop\ntyping; <code>⌘S</code> / <code>Ctrl+S</code> saves at once.</li>\n<li>Check the <strong>Preview</strong> pane, or <strong>Open full preview</strong> in a new tab.</li>\n<li>Press <strong>Publish</strong> (or <code>⌘↵</code> / <code>Ctrl+Enter</code>). The draft becomes a new immutable\nversion and the canvas URL serves it immediately. The toast names the version:\n<code>Published version 7</code>.</li>\n</ol>\n<p>Editors get the same tab and the same controls as the owner (see\n<a href=\"/docs/authoring/sharing#roles-viewers-and-editors\">Roles: viewers and editors</a>).\nAgents run the same loop over MCP with <code>get_draft</code>, <code>read_draft_file</code>,\n<code>write_draft_file</code>, <code>rename_draft_file</code>, <code>delete_draft_file</code>, <code>publish_draft</code>, and\n<code>restore_draft</code> (see <a href=\"/docs/agents/mcp\">MCP server</a>).</p>\n<h2 id=\"draft-and-published-version\">Draft and published version</h2>\n<p>Every canvas has exactly one <strong>draft</strong>: a working copy separate from the version\nviewers see. Saving changes the draft only; the canvas URL keeps serving the current\nversion until you publish.</p>\n<p>Autosave runs 700 ms after you stop typing, with a final flush when you leave the\neditor or switch files. <code>⌘S</code> / <code>Ctrl+S</code> in the code editor saves at once. The status\nline in the bar above the editor tells you where your edit is, in this order of\nprecedence:</p>\n<table>\n<thead>\n<tr>\n<th>Status</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Saving...</strong></td>\n<td>A save is in flight.</td>\n</tr>\n<tr>\n<td><strong>Save conflict</strong></td>\n<td>Someone else saved this file first. See <a href=\"#two-people-one-file\">Two people, one file</a>.</td>\n</tr>\n<tr>\n<td><strong>Save failed</strong></td>\n<td>The save errored. Your text stays in the editor; edit again or publish to retry (publishing saves first).</td>\n</tr>\n<tr>\n<td><strong>Unsaved changes</strong></td>\n<td>The edit is still in the autosave window.</td>\n</tr>\n<tr>\n<td><strong>Unpublished changes</strong></td>\n<td>The draft is saved and differs from the published version.</td>\n</tr>\n<tr>\n<td><strong>Behind the published version</strong></td>\n<td>The draft has no edits of its own, but a direct deploy moved the live version under it. See <a href=\"#when-a-direct-deploy-lands\">When a direct deploy lands</a>.</td>\n</tr>\n<tr>\n<td><strong>All changes published</strong></td>\n<td>Draft and published version match.</td>\n</tr>\n</tbody></table>\n<p>Whenever the draft is behind, a separate <strong>A newer version was published</strong> notice sits\nnext to the status line, whatever else the status says.</p>\n<p><strong>Publish</strong> is enabled when the draft has at least one file and something to ship:\nunpublished changes, an unsaved edit, or a draft that is behind. When it is disabled,\nits tooltip says why: <strong>The published version already matches this draft</strong>, or <strong>Add\na file to the draft before publishing</strong>. The <code>⌘↵</code> shortcut uses the same gate and is\nignored while a dialog (Add file, Rename, Delete) is open.</p>\n<p>Publishing snapshots the draft's file list into a new immutable version, points the\ncanvas at it, and prunes history beyond the last <strong>10</strong> versions. Files are already\nstored as you edit, so publishing writes no bytes, only the manifest and the pointer.\nA save that lands while a publish is running is kept as new unpublished work, not\nerased. When the canvas's preview mode is <strong>auto</strong> and the instance has\n<a href=\"/docs/self-hosting/screenshots\">screenshots</a> on, a fresh cover is captured after each\npublish.</p>\n<p>The <strong>Publish</strong> / <strong>New version</strong> button in the canvas header is a different thing: it\nuploads files (paste, folder, or ZIP) as a direct deploy. Only the Editor tab's\n<strong>Publish</strong> ships the draft.</p>\n<h3 id=\"when-a-direct-deploy-lands\">When a direct deploy lands</h3>\n<p>Direct deploys (folder, ZIP, paste, the Deploy API, MCP <code>deploy_canvas</code>) publish a\nversion without going through the draft. Afterwards the server reconciles the draft\nagainst what was just deployed:</p>\n<table>\n<thead>\n<tr>\n<th>Draft before the deploy</th>\n<th>After the deploy</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>No draft yet</td>\n<td>A draft is created from the new version.</td>\n</tr>\n<tr>\n<td>Draft matches the version it was seeded from (no real edits)</td>\n<td>The draft is replaced with the new version; nothing to lose.</td>\n</tr>\n<tr>\n<td>Draft has unpublished edits</td>\n<td>The edits are kept and the draft is flagged: <strong>A newer version was published</strong>.</td>\n</tr>\n</tbody></table>\n<p>In the flagged case, Publish stays enabled and would replace that newer version with\nyour draft's content (the newer version stays in history). To build on the newer\nversion instead, open <strong>Versions</strong> and <strong>Restore</strong> it into the draft, then reapply\nyour edits.</p>\n<h3 id=\"archived-and-disabled-canvases\">Archived and disabled canvases</h3>\n<p>The Editor tab shows <strong>Editing is paused</strong> while a canvas is archived or disabled by an\nadmin: no edits, no publish. <strong>Unarchive</strong> from the canvas header to resume. On an\narchived canvas the Versions tab still lets you <strong>Download ZIP</strong>, <strong>Restore</strong> a version\ninto the draft, and <strong>Delete</strong> a non-current version; <strong>Make current</strong> is hidden and\npublishing returns <code>409 NOT_ACTIVE</code> until the canvas is active again. A disabled canvas\nis read-only throughout: every draft write, restore, rollback, and version deletion\nreturns <code>409 DISABLED</code>, and the Versions tab shows <strong>Read-only while disabled</strong>.</p>\n<h2 id=\"two-people-one-file\">Two people, one file</h2>\n<p>The owner and every editor can work on the same draft at once. Saves are checked per\nfile: each save carries the content hash the editor last loaded, and the server\nrefuses a save whose file changed underneath it (<code>409 DRAFT_CONFLICT</code>, naming who\nsaved and when). Edits to different files merge without any of this.</p>\n<p>On a conflict the editor keeps your text and shows the other person's saved version\nbeside it under <strong>Someone else saved <code>path</code> first</strong>, with three choices:</p>\n<ul>\n<li><strong>Use their version</strong>: your buffer becomes theirs.</li>\n<li><strong>Overwrite with mine</strong>: re-save your buffer over theirs. The save is pinned to the\nversion you just saw, so a third change in between is still caught.</li>\n<li><strong>Copy my version</strong>: copy your buffer to the clipboard and decide later.</li>\n</ul>\n<p>The conflicted file does not autosave until you choose. Uploading a file over a path\nsomeone else changed is refused the same way; the draft is refreshed so a second\nupload carries the current hash.</p>\n<p>If your access changes mid-session (you were removed, demoted to viewer, or left the\norg), the server stops accepting saves from that session and the editor shows <strong>You no\nlonger have edit access to this canvas</strong>. Your unsaved edits stay on screen with\n<strong>Copy my edits</strong> and <strong>Download my edits</strong> until you leave.</p>\n<h2 id=\"files\">Files</h2>\n<p>The <strong>Files</strong> pane covers <strong>Add file</strong>, <strong>Upload files</strong> (or drag files onto the pane),\nrename, delete, replace, and download. Paths are relative to the canvas root and stored\nas written, so <code>css/site.css</code> and <code>&lt;link href=\"css/site.css\"&gt;</code> line up. Folders start\ncollapsed; expand the branch you need. Adding or renaming onto an existing path is\nrefused rather than overwriting it (<code>PATH_EXISTS</code>); dotfiles and absolute paths are\nrejected (<code>INVALID_PATH</code>), and a <code>..</code> segment is rejected (<code>ZIP_SLIP_REJECTED</code>).</p>\n<p>Text files on the editable allowlist (HTML, CSS, JS/TS, JSON, Markdown, SVG, XML, CSV,\nYAML, TOML, plain text, and similar) up to <strong>2 MB</strong> open in the code editor\n(CodeMirror) with syntax highlighting. Everything else, including images, fonts,\nspreadsheets, and larger text files, is a non-editable asset: images preview inline,\nand any such file offers <strong>Download</strong> and <strong>Replace</strong>.</p>\n<p>Limits per canvas: <strong>25 MB per file</strong> (<code>FILE_TOO_LARGE</code>), <strong>100 MB total</strong>\n(<code>CANVAS_TOO_LARGE</code>), <strong>2,000 files</strong> (<code>TOO_MANY_FILES</code>). A save that would cross a\nlimit is rejected with the reason. A text file that looks like it embeds a canvas API\nkey is saved but logged as a warning on the server; keys belong server-side only.</p>\n<h3 id=\"the-root-page\">The root page</h3>\n<p>Name your home page <code>index.html</code> at the canvas root. Resolution at the canvas URL:</p>\n<ul>\n<li><code>index.html</code> exists: it is the root page. A folder request such as <code>docs/</code> serves\n<code>docs/index.html</code>.</li>\n<li>No <code>index.html</code> but exactly one HTML file: that file is served at the root,\nwhatever its name.</li>\n<li>Several HTML files and no <code>index.html</code>: the root returns 404, because there is no\nway to pick the home page.</li>\n</ul>\n<p>With <strong>Single-page app mode</strong> on (Settings, URL &amp; routing), any unmatched path serves\nthe same root page, so client-side routes survive a reload.</p>\n<p>The editor flags each state above the tree: <strong>No HTML page in this draft</strong> (with\n<strong>Add index.html</strong>), <strong>Home page is inferred</strong> (with <strong>Rename to index.html</strong>), and\n<strong>Choose the root page</strong> (select the intended page, then <strong>Rename selected</strong>). Publish\nis not blocked by these, but a canvas with no root page returns 404 at its URL.</p>\n<h2 id=\"preview\">Preview</h2>\n<p>The <strong>Preview</strong> pane renders the whole draft site from its root page, not the selected\nfile, so what you see is what publishing would ship. Refresh, full screen (Escape\nexits), hide, and <strong>Open full preview</strong> in a new tab sit in the pane header.</p>\n<p>The inline pane runs the draft in a sandboxed frame (<code>allow-scripts allow-forms</code>, no\n<code>allow-same-origin</code>), which keeps draft code away from your dashboard session. Static\nHTML, CSS, and images render faithfully there. ES modules, signed-in SDK calls\n(<code>canvasdrop.me()</code>, <code>kv</code>, <code>files</code>, <code>ai</code>, <code>realtime</code>), and self-hosted fonts do not work\ninside the sandbox, so a draft that contains JavaScript files starts on the notice\n<strong>This canvas runs JavaScript</strong> with two options: <strong>Run preview</strong> (load it in the\nsandbox anyway; plain inline scripts run) or <strong>Open full preview</strong> (a top-level tab\nwhere everything runs as it ships).</p>\n<p>The preview is served from the dashboard origin at <code>/api/canvases/{id}/preview/</code> and\nonly to the owner and editors; anyone else, admins included, gets 404. The public\ncanvas URL never serves draft content.</p>\n<h2 id=\"page-text-mode\">Page text mode</h2>\n<p>For a single static page you can edit copy directly on the rendered page instead of\nin the source. Switch the mode control from <strong>Code</strong> to <strong>Page text</strong>, then click any\ntext to edit it. A small toolbar on selection covers bold, italic, underline,\nstrikethrough, heading levels (H1, H2, Body, Quote), bulleted and numbered lists,\nlink, unlink, and clear formatting. Edits save back to that HTML file through the same\nautosave and conflict checks as the code editor.</p>\n<p><strong>Page text</strong> is offered only when the draft has exactly one HTML file and no\nJavaScript files. Other assets (CSS, images) are fine. Drafts with zero or several\nHTML pages, or with script files, use <strong>Code</strong>; the mode control's tooltip names the\ncondition that is not met. If a draft stops qualifying while you are in Page text,\nthe editor returns to Code.</p>\n<h2 id=\"versions\">Versions</h2>\n<p>Each publish, and each direct deploy, is an immutable version. canvas-drop keeps the\nlast <strong>10</strong> per canvas.</p>\n<p>The <strong>Versions</strong> tab lists them newest first with the source (<code>editor</code>, folder upload,\nZIP, paste, the API, or <code>upload</code> for the staged upload API), who created it, when, file\ncount, and total size. Per version:</p>\n<ul>\n<li><strong>Download ZIP</strong>: every file in that version as one archive, named\n<code>{slug}-v{version}.zip</code>.</li>\n<li><strong>Restore</strong>: load that version's files into the draft. If the draft has unpublished\nchanges, canvas-drop asks first (<strong>Load and discard changes</strong>). The published\nversion is untouched until you publish.</li>\n<li><strong>Make current</strong>: point the canvas URL at that version, older or newer, for all\nvisitors immediately, after a confirmation. This is the roll back (and roll\nforward) control.</li>\n<li><strong>Delete</strong>: remove a non-current version from history after confirmation. The\ncurrent version cannot be deleted (<code>409 CURRENT_VERSION</code>); files still used by\nanother version or the draft are kept.</li>\n</ul>\n<p>Restore changes only the draft; Make current changes only what visitors see. To\ninspect a single file from an older version, restore it into the draft and use the\ntree, or download the ZIP.</p>\n<p>Storage is content-addressed: each file is stored once per canvas by content hash, so\na version that changes one file writes one new blob, and blobs no version or the\ndraft references are swept after publishes and deletions.</p>\n<h2 id=\"the-draft-over-http\">The draft over HTTP</h2>\n<p>The editor talks to these same-origin routes with your dashboard session. Every one\nneeds the owner or editor role; a canvas you hold no role on reads 404.</p>\n<table>\n<thead>\n<tr>\n<th>Route</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>GET /api/canvases/{id}/draft</code></td>\n<td>Draft view: <code>files[]</code> (path, size, mime, hash, last writer), <code>dirty</code>, <code>stale</code>, <code>baseVersionId</code>. Creates the draft on first call.</td>\n</tr>\n<tr>\n<td><code>GET /api/canvases/{id}/draft/file?path=</code></td>\n<td>Raw bytes of one draft file.</td>\n</tr>\n<tr>\n<td><code>PUT /api/canvases/{id}/draft/file?path=</code></td>\n<td>Write or replace a file (raw body). <code>?mode=create</code> refuses an existing path. Send <code>If-Draft-File-Hash</code> with the hash you loaded (<code>none</code> for a new path) to get <code>409 DRAFT_CONFLICT</code> instead of overwriting.</td>\n</tr>\n<tr>\n<td><code>DELETE /api/canvases/{id}/draft/file?path=</code></td>\n<td>Remove a file. Same precondition header.</td>\n</tr>\n<tr>\n<td><code>POST /api/canvases/{id}/draft/rename</code></td>\n<td><code>{from, to}</code>. Refuses an existing target (<code>PATH_EXISTS</code>).</td>\n</tr>\n<tr>\n<td><code>POST /api/canvases/{id}/publish</code></td>\n<td>New version from the draft: <code>{version, versionId, fileCount, totalBytes}</code>. <code>EMPTY_DEPLOY</code> on an empty draft; <code>409 NOT_ACTIVE</code> when archived.</td>\n</tr>\n<tr>\n<td><code>POST /api/canvases/{id}/restore</code></td>\n<td><code>{version}</code>. Copies that version's files into the draft.</td>\n</tr>\n<tr>\n<td><code>GET /api/canvases/{id}/preview/{path}</code></td>\n<td>The draft as a site (used by the Preview pane).</td>\n</tr>\n</tbody></table>\n<p>See also <a href=\"/docs/authoring/create-and-publish\">Create &amp; publish</a> for the direct deploy\npaths (folder, ZIP, paste, Deploy API) and <a href=\"/docs/authoring/sharing\">Sharing &amp; access</a>\nfor adding editors.</p>\n",
    "headings": [
      {
        "id": "draft-and-published-version",
        "text": "Draft and published version",
        "level": 2
      },
      {
        "id": "when-a-direct-deploy-lands",
        "text": "When a direct deploy lands",
        "level": 3
      },
      {
        "id": "archived-and-disabled-canvases",
        "text": "Archived and disabled canvases",
        "level": 3
      },
      {
        "id": "two-people-one-file",
        "text": "Two people, one file",
        "level": 2
      },
      {
        "id": "files",
        "text": "Files",
        "level": 2
      },
      {
        "id": "the-root-page",
        "text": "The root page",
        "level": 3
      },
      {
        "id": "preview",
        "text": "Preview",
        "level": 2
      },
      {
        "id": "page-text-mode",
        "text": "Page text mode",
        "level": 2
      },
      {
        "id": "versions",
        "text": "Versions",
        "level": 2
      },
      {
        "id": "the-draft-over-http",
        "text": "The draft over HTTP",
        "level": 2
      }
    ],
    "text": "The editor Change a canvas's files in the browser, check the result, and publish when you are ready. Nothing viewers see changes until you publish. The editor is the Editor tab of any canvas you own or edit; there is no local setup and no build step. The loop: Open the canvas and pick Editor . On first open the draft is seeded from the current published version, or starts empty for a canvas that has never published. Edit files in the tree. Changes autosave to the draft 700 ms after you stop typing; ⌘S / Ctrl+S saves at once. Check the Preview pane, or Open full preview in a new tab. Press Publish (or ⌘↵ / Ctrl+Enter ). The draft becomes a new immutable version and the canvas URL serves it immediately. The toast names the version: Published version 7 . Editors get the same tab and the same controls as the owner (see Roles: viewers and editors ). Agents run the same loop over MCP with get_draft , read_draft_file , write_draft_file , rename_draft_file , delete_draft_file , publish_draft , and restore_draft (see MCP server ). Draft and published version Every canvas has exactly one draft : a working copy separate from the version viewers see. Saving changes the draft only; the canvas URL keeps serving the current version until you publish. Autosave runs 700 ms after you stop typing, with a final flush when you leave the editor or switch files. ⌘S / Ctrl+S in the code editor saves at once. The status line in the bar above the editor tells you where your edit is, in this order of precedence: Status Meaning Saving... A save is in flight. Save conflict Someone else saved this file first. See Two people, one file . Save failed The save errored. Your text stays in the editor; edit again or publish to retry (publishing saves first). Unsaved changes The edit is still in the autosave window. Unpublished changes The draft is saved and differs from the published version. Behind the published version The draft has no edits of its own, but a direct deploy moved the live version under it. See When a direct deploy lands . All changes published Draft and published version match. Whenever the draft is behind, a separate A newer version was published notice sits next to the status line, whatever else the status says. Publish is enabled when the draft has at least one file and something to ship: unpublished changes, an unsaved edit, or a draft that is behind. When it is disabled, its tooltip says why: The published version already matches this draft , or Add a file to the draft before publishing . The ⌘↵ shortcut uses the same gate and is ignored while a dialog (Add file, Rename, Delete) is open. Publishing snapshots the draft's file list into a new immutable version, points the canvas at it, and prunes history beyond the last 10 versions. Files are already stored as you edit, so publishing writes no bytes, only the manifest and the pointer. A save that lands while a publish is running is kept as new unpublished work, not erased. When the canvas's preview mode is auto and the instance has screenshots on, a fresh cover is captured after each publish. The Publish / New version button in the canvas header is a different thing: it uploads files (paste, folder, or ZIP) as a direct deploy. Only the Editor tab's Publish ships the draft. When a direct deploy lands Direct deploys (folder, ZIP, paste, the Deploy API, MCP deploy_canvas ) publish a version without going through the draft. Afterwards the server reconciles the draft against what was just deployed: Draft before the deploy After the deploy No draft yet A draft is created from the new version. Draft matches the version it was seeded from (no real edits) The draft is replaced with the new version; nothing to lose. Draft has unpublished edits The edits are kept and the draft is flagged: A newer version was published . In the flagged case, Publish stays enabled and would replace that newer version with your draft's content (the newer version stays in history). To build on the newer version instead, open Versions and Restore it into the draft, then reapply your edits. Archived and disabled canvases The Editor tab shows Editing is paused while a canvas is archived or disabled by an admin: no edits, no publish. Unarchive from the canvas header to resume. On an archived canvas the Versions tab still lets you Download ZIP , Restore a version into the draft, and Delete a non-current version; Make current is hidden and publishing returns 409 NOT_ACTIVE until the canvas is active again. A disabled canvas is read-only throughout: every draft write, restore, rollback, and version deletion returns 409 DISABLED , and the Versions tab shows Read-only while disabled . Two people, one file The owner and every editor can work on the same draft at once. Saves are checked per file: each save carries the content hash the editor last loaded, and the server refuses a save whose file changed underneath it ( 409 DRAFT_CONFLICT , naming who saved and when). Edits to different files merge without any of this. On a conflict the editor keeps your text and shows the other person's saved version beside it under Someone else saved path first , with three choices: Use their version : your buffer becomes theirs. Overwrite with mine : re-save your buffer over theirs. The save is pinned to the version you just saw, so a third change in between is still caught. Copy my version : copy your buffer to the clipboard and decide later. The conflicted file does not autosave until you choose. Uploading a file over a path someone else changed is refused the same way; the draft is refreshed so a second upload carries the current hash. If your access changes mid-session (you were removed, demoted to viewer, or left the org), the server stops accepting saves from that session and the editor shows You no longer have edit access to this canvas . Your unsaved edits stay on screen with Copy my edits and Download my edits until you leave. Files The Files pane covers Add file , Upload files (or drag files onto the pane), rename, delete, replace, and download. Paths are relative to the canvas root and stored as written, so css/site.css and <link href=\"css/site.css\"> line up. Folders start collapsed; expand the branch you need. Adding or renaming onto an existing path is refused rather than overwriting it ( PATH_EXISTS ); dotfiles and absolute paths are rejected ( INVALID_PATH ), and a .. segment is rejected ( ZIP_SLIP_REJECTED ). Text files on the editable allowlist (HTML, CSS, JS/TS, JSON, Markdown, SVG, XML, CSV, YAML, TOML, plain text, and similar) up to 2 MB open in the code editor (CodeMirror) with syntax highlighting. Everything else, including images, fonts, spreadsheets, and larger text files, is a non-editable asset: images preview inline, and any such file offers Download and Replace . Limits per canvas: 25 MB per file ( FILE_TOO_LARGE ), 100 MB total ( CANVAS_TOO_LARGE ), 2,000 files ( TOO_MANY_FILES ). A save that would cross a limit is rejected with the reason. A text file that looks like it embeds a canvas API key is saved but logged as a warning on the server; keys belong server-side only. The root page Name your home page index.html at the canvas root. Resolution at the canvas URL: index.html exists: it is the root page. A folder request such as docs/ serves docs/index.html . No index.html but exactly one HTML file: that file is served at the root, whatever its name. Several HTML files and no index.html : the root returns 404, because there is no way to pick the home page. With Single-page app mode on (Settings, URL & routing), any unmatched path serves the same root page, so client-side routes survive a reload. The editor flags each state above the tree: No HTML page in this draft (with Add index.html ), Home page is inferred (with Rename to index.html ), and Choose the root page (select the intended page, then Rename selected ). Publish is not blocked by these, but a canvas with no root page returns 404 at its URL. Preview The Preview pane renders the whole draft site from its root page, not the selected file, so what you see is what publishing would ship. Refresh, full screen (Escape exits), hide, and Open full preview in a new tab sit in the pane header. The inline pane runs the draft in a sandboxed frame ( allow-scripts allow-forms , no allow-same-origin ), which keeps draft code away from your dashboard session. Static HTML, CSS, and images render faithfully there. ES modules, signed-in SDK calls ( canvasdrop.me() , kv , files , ai , realtime ), and self-hosted fonts do not work inside the sandbox, so a draft that contains JavaScript files starts on the notice This canvas runs JavaScript with two options: Run preview (load it in the sandbox anyway; plain inline scripts run) or Open full preview (a top-level tab where everything runs as it ships). The preview is served from the dashboard origin at /api/canvases/{id}/preview/ and only to the owner and editors; anyone else, admins included, gets 404. The public canvas URL never serves draft content. Page text mode For a single static page you can edit copy directly on the rendered page instead of in the source. Switch the mode control from Code to Page text , then click any text to edit it. A small toolbar on selection covers bold, italic, underline, strikethrough, heading levels (H1, H2, Body, Quote), bulleted and numbered lists, link, unlink, and clear formatting. Edits save back to that HTML file through the same autosave and conflict checks as the code editor. Page text is offered only when the draft has exactly one HTML file and no JavaScript files. Other assets (CSS, images) are fine. Drafts with zero or several HTML pages, or with script files, use Code ; the mode control's tooltip names the condition that is not met. If a draft stops qualifying while you are in Page text, the editor returns to Code. Versions Each publish, and each direct deploy, is an immutable version. canvas-drop keeps the last 10 per canvas. The Versions tab lists them newest first with the source ( editor , folder upload, ZIP, paste, the API, or upload for the staged upload API), who created it, when, file count, and total size. Per version: Download ZIP : every file in that version as one archive, named {slug}-v{version}.zip . Restore : load that version's files into the draft. If the draft has unpublished changes, canvas-drop asks first ( Load and discard changes ). The published version is untouched until you publish. Make current : point the canvas URL at that version, older or newer, for all visitors immediately, after a confirmation. This is the roll back (and roll forward) control. Delete : remove a non-current version from history after confirmation. The current version cannot be deleted ( 409 CURRENT_VERSION ); files still used by another version or the draft are kept. Restore changes only the draft; Make current changes only what visitors see. To inspect a single file from an older version, restore it into the draft and use the tree, or download the ZIP. Storage is content-addressed: each file is stored once per canvas by content hash, so a version that changes one file writes one new blob, and blobs no version or the draft references are swept after publishes and deletions. The draft over HTTP The editor talks to these same-origin routes with your dashboard session. Every one needs the owner or editor role; a canvas you hold no role on reads 404. Route What it does GET /api/canvases/{id}/draft Draft view: files[] (path, size, mime, hash, last writer), dirty , stale , baseVersionId . Creates the draft on first call. GET /api/canvases/{id}/draft/file?path= Raw bytes of one draft file. PUT /api/canvases/{id}/draft/file?path= Write or replace a file (raw body). ?mode=create refuses an existing path. Send If-Draft-File-Hash with the hash you loaded ( none for a new path) to get 409 DRAFT_CONFLICT instead of overwriting. DELETE /api/canvases/{id}/draft/file?path= Remove a file. Same precondition header. POST /api/canvases/{id}/draft/rename {from, to} . Refuses an existing target ( PATH_EXISTS ). POST /api/canvases/{id}/publish New version from the draft: {version, versionId, fileCount, totalBytes} . EMPTY_DEPLOY on an empty draft; 409 NOT_ACTIVE when archived. POST /api/canvases/{id}/restore {version} . Copies that version's files into the draft. GET /api/canvases/{id}/preview/{path} The draft as a site (used by the Preview pane). See also Create & publish for the direct deploy paths (folder, ZIP, paste, Deploy API) and Sharing & access for adding editors."
  },
  {
    "path": "sdk/overview",
    "section": "Browser SDK",
    "title": "Overview",
    "html": "<h1 id=\"browser-sdk\">Browser SDK</h1>\n<p>You have a static canvas and want it to keep data, accept files, call a model,\nknow who is looking, or sync between viewers. One <code>&lt;script&gt;</code> tag gives the page a\nglobal, <code>canvasdrop</code>, carrying the six fixed primitives (KV, files, AI, identity,\nrealtime, and admin-granted outbound Connections) plus authoring. There is no build step and nothing to configure in the\npage: identity comes from the viewer's session, the canvas is identified from its\nown URL, and no key ever reaches the browser.</p>\n<h2 id=\"add-it-to-a-canvas\">Add it to a canvas</h2>\n<p>One script tag, then call the global:</p>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">src</span>=<span class=\"hljs-string\">\"/sdk/v1.js\"</span>&gt;</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">type</span>=<span class=\"hljs-string\">\"module\"</span>&gt;</span><span class=\"language-javascript\">\n  <span class=\"hljs-keyword\">const</span> me = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-title function_\">me</span>();                     <span class=\"hljs-comment\">// { id, email, name, avatarUrl, kind }</span>\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">set</span>(<span class=\"hljs-string\">\"last-viewer\"</span>, me.<span class=\"hljs-property\">name</span>);\n  <span class=\"hljs-keyword\">const</span> views = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">increment</span>(<span class=\"hljs-string\">\"views\"</span>); <span class=\"hljs-comment\">// 1 on the first call, then 2, 3, ...</span>\n</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span></code></pre>\n<p>The SDK tag is a plain <code>&lt;script&gt;</code>, so it runs before your module; <code>type=\"module\"</code>\ngives you top-level <code>await</code>. The root-relative <code>src</code> resolves against the\ncanvas's own origin, and the server serves the script there in both URL modes,\nbehind the same sign-in as the canvas. A fresh local instance (<code>pnpm dev</code>, <code>dev</code>\nauth) is already signed in, so the snippet runs as soon as the canvas has its\nbackend on (see <a href=\"#turn-the-backend-on-first\">Turn the backend on first</a>).</p>\n<p><code>{base}/sdk/v1.js</code> is a stable path for the life of v1, cached for an hour\n(<code>cache-control: public, max-age=3600</code>), so deployed canvases pick up SDK fixes\nwithout a redeploy. On a local checkout, a <code>503</code> from that path means the bundle\nis not built yet: run <code>pnpm build</code> (or <code>pnpm --filter @canvas-drop/sdk build</code>).</p>\n<h2 id=\"one-global-canvasdrop\">One global: <code>canvasdrop</code></h2>\n<p>The script defines exactly one global, <code>window.canvasdrop</code>. There is no <code>cd</code>\nalias, no second name, and no version property on the object. The client has\n<code>me()</code>, <code>kv</code>, <code>files</code>, <code>ai</code>, <code>realtime</code>, <code>connections</code>, and <code>canvases</code>, and nothing else.</p>\n<p>The same client lives in the workspace package <code>@canvas-drop/sdk</code> (private, not\npublished). Its module entry exports <code>createClient</code>, <code>detectContext</code>,\n<code>SDK_VERSION</code> (<code>\"1\"</code>), <code>ERROR_CODES</code>, <code>errorFromResponse</code>, the error classes, and\nthe types. The served script is exactly\n<code>createClient({ context: detectContext(window.location) })</code> assigned to\n<code>window.canvasdrop</code>.</p>\n<h2 id=\"how-the-sdk-finds-your-canvas\">How the SDK finds your canvas</h2>\n<p><code>detectContext(window.location)</code> reads the slug and the API base from the page\nURL. You never pass either yourself, and there is no override.</p>\n<table>\n<thead>\n<tr>\n<th>URL mode</th>\n<th>Page URL</th>\n<th>Slug</th>\n<th>API base</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Path</td>\n<td><code>https://canvases.example.com/c/quiet-otter-x7k2/</code></td>\n<td>the segment after <code>/c/</code></td>\n<td>the page's own origin</td>\n</tr>\n<tr>\n<td>Subdomain</td>\n<td><code>https://quiet-otter-x7k2.canvases.example.com/</code></td>\n<td>the first hostname label</td>\n<td>the protocol plus the remaining labels (port kept, if any)</td>\n</tr>\n</tbody></table>\n<p>Path mode is checked first (<code>/c/{slug}</code>); any other URL is treated as a\nsubdomain. Every HTTP request goes to <code>{apiBase}/v1/c/{slug}/…</code> with\n<code>credentials: \"include\"</code>, so the viewer's session cookie rides along; realtime\nopens one WebSocket per page at <code>{apiBase}/v1/c/{slug}/realtime</code> (<code>ws</code> or <code>wss</code>\nto match the page). On a local path-mode instance the page is\n<code>http://localhost:3000/c/quiet-otter-x7k2/</code> and the API base is\n<code>http://localhost:3000</code>.</p>\n<h2 id=\"turn-the-backend-on-first\">Turn the backend on first</h2>\n<p>A canvas is static until its owner or an editor switches <strong>Enable backend</strong> on\nin the canvas's <strong>Backend</strong> tab. That master switch is off by default and gates\nevery primitive, including <code>me()</code>. With it on, each feature has its own toggle,\nand three of them also depend on an operator setting for the instance:</p>\n<table>\n<thead>\n<tr>\n<th>Feature</th>\n<th>Toggle in the Backend tab</th>\n<th>Toggle default</th>\n<th>Operator setting</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Identity, <code>me()</code></td>\n<td>none; on whenever the backend is on</td>\n<td>on</td>\n<td>none</td>\n</tr>\n<tr>\n<td>KV</td>\n<td><strong>Key-value storage</strong></td>\n<td>on</td>\n<td>none</td>\n</tr>\n<tr>\n<td>Files</td>\n<td><strong>File storage</strong></td>\n<td>on</td>\n<td>none</td>\n</tr>\n<tr>\n<td>AI</td>\n<td><strong>AI</strong></td>\n<td>on</td>\n<td>an AI provider key (<code>CANVAS_DROP_AI_API_KEY</code>, or the admin override)</td>\n</tr>\n<tr>\n<td>Realtime</td>\n<td><strong>Realtime</strong></td>\n<td>on</td>\n<td><code>CANVAS_DROP_REALTIME</code> (default <code>on</code>)</td>\n</tr>\n<tr>\n<td>Connections</td>\n<td>no canvas-owned toggle; each admin grant is the switch</td>\n<td>no grants</td>\n<td>an enabled admin profile attached to this canvas</td>\n</tr>\n<tr>\n<td>Authoring</td>\n<td><strong>Authoring</strong></td>\n<td>off</td>\n<td><code>CANVAS_DROP_AUTHORING</code> (default <code>off</code>), or the admin override in Admin → Configuration</td>\n</tr>\n</tbody></table>\n<p>A call to anything that is off throws <code>CapabilityDisabledError</code> (code\n<code>CAPABILITY_DISABLED</code>, status 403). Its <code>.hint</code> (also its <code>.message</code>) names the\ngate that failed, the master switch, the feature toggle, or the operator\nsetting, and how to open it. Two share states close the whole API no matter how\nthe switches are set; both skip the owner and editors:</p>\n<ul>\n<li><strong>Public link</strong>: every viewer who is not the owner or an editor, signed in or\nnot, gets <code>STATIC_ONLY</code> (403) from every primitive; the owner and editors keep\nthe full API.</li>\n<li><strong>Password</strong>: on a password-protected canvas the API answers\n<code>PASSWORD_REQUIRED</code> (403) until the viewer has passed the password gate on the\ncanvas page itself; the owner and editors are never prompted.</li>\n</ul>\n<p>The <a href=\"/docs/authoring/capabilities\">Capabilities</a> page covers the tab.</p>\n<h2 id=\"the-surface\">The surface</h2>\n<table>\n<thead>\n<tr>\n<th>Namespace</th>\n<th>What it does</th>\n<th>Reference</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>canvasdrop.me()</code></td>\n<td>The signed-in viewer: <code>{ id, email, name, avatarUrl, kind }</code>. <code>kind</code> is <code>\"member\"</code>, or <code>\"guest\"</code> for a retained legacy guest session.</td>\n<td><a href=\"/docs/sdk/identity\">Identity</a></td>\n</tr>\n<tr>\n<td><code>canvasdrop.kv</code></td>\n<td><code>get</code>, <code>set</code>, <code>delete</code>, <code>list</code>, <code>increment</code>. The same five on <code>canvasdrop.kv.user</code> store per viewer. <code>get</code> resolves <code>null</code> for a missing key.</td>\n<td><a href=\"/docs/sdk/kv\">KV</a></td>\n</tr>\n<tr>\n<td><code>canvasdrop.files</code></td>\n<td><code>upload(file)</code>, <code>list()</code>, <code>delete(id)</code>, and the synchronous <code>url(id)</code>.</td>\n<td><a href=\"/docs/sdk/files\">Files</a></td>\n</tr>\n<tr>\n<td><code>canvasdrop.ai</code></td>\n<td><code>chat(messages, { model })</code> resolves <code>{ text, usage, cost }</code>; <code>stream(messages, { model })</code> yields text chunks. <code>model</code> is required; <code>system</code> and <code>maxTokens</code> are optional.</td>\n<td><a href=\"/docs/sdk/ai\">AI</a></td>\n</tr>\n<tr>\n<td><code>canvasdrop.realtime</code></td>\n<td><code>channel(name)</code> returns a channel with <code>publish</code>, <code>subscribe</code>, <code>unsubscribe</code>, <code>presence</code>, <code>onPresence</code>, <code>onJoin</code>, <code>onLeave</code>, <code>close</code>, over one shared WebSocket that reconnects on its own.</td>\n<td><a href=\"/docs/sdk/realtime\">Realtime</a></td>\n</tr>\n<tr>\n<td><code>canvasdrop.connections</code></td>\n<td><code>fetch(profile, relativePath, init?)</code> returns a native upstream <code>Response</code> through one exact origin an admin granted to the canvas. Protected headers stay server-side.</td>\n<td><a href=\"/docs/sdk/connections\">Connections</a></td>\n</tr>\n<tr>\n<td><code>canvasdrop.canvases</code></td>\n<td><code>publish</code>, <code>update</code>, <code>list</code>, <code>revoke</code>: a signed-in viewer creates and manages canvases from the page, as themselves (the authoring capability).</td>\n<td><a href=\"/docs/sdk/authoring\">Authoring</a></td>\n</tr>\n</tbody></table>\n<h2 id=\"errors\">Errors</h2>\n<p>Every failure throws an error extending <code>CanvasdropError</code>: a string <code>.code</code>, a\nnumeric <code>.status</code>, and, when the server sent one, a <code>.hint</code> with the fix. Six\nsubclasses cover the cases you most often branch on:</p>\n<table>\n<thead>\n<tr>\n<th>Class</th>\n<th><code>.code</code></th>\n<th><code>.status</code></th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NotAuthenticatedError</code></td>\n<td><code>NOT_AUTHENTICATED</code></td>\n<td>401</td>\n</tr>\n<tr>\n<td><code>NotFoundError</code></td>\n<td><code>NOT_FOUND</code></td>\n<td>404</td>\n</tr>\n<tr>\n<td><code>CapabilityDisabledError</code></td>\n<td><code>CAPABILITY_DISABLED</code></td>\n<td>403</td>\n</tr>\n<tr>\n<td><code>QuotaExceededError</code></td>\n<td><code>QUOTA_EXCEEDED</code>, <code>GUEST_AI_CAP</code>, <code>KEY_LIMIT</code>, <code>CONNECTION_LIMIT</code>, or a size code (<code>KEY_TOO_LARGE</code>, <code>VALUE_TOO_LARGE</code>, <code>FILE_TOO_LARGE</code>)</td>\n<td>429, 409, or 413 (the wire status)</td>\n</tr>\n<tr>\n<td><code>PublishFailedError</code></td>\n<td><code>PUBLISH_FAILED</code>, with the new canvas's <code>.id</code></td>\n<td>502</td>\n</tr>\n<tr>\n<td><code>UpdatePartialError</code></td>\n<td><code>UPDATE_PARTIAL</code>, with <code>.stage</code> and the saved <code>.current</code> record</td>\n<td>502</td>\n</tr>\n</tbody></table>\n<p>Everything else (<code>PASSWORD_REQUIRED</code>, <code>STATIC_ONLY</code>, <code>DISABLED</code>,\n<code>MODEL_NOT_ALLOWED</code>, <code>NOT_NUMERIC</code>, <code>INVALID_BODY</code>, and the rest) arrives as the\nbase <code>CanvasdropError</code>; branch on <code>.code</code>. The full list is on the\n<a href=\"/docs/api/errors\">error codes</a> page.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">try</span> {\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">increment</span>(<span class=\"hljs-string\">\"votes\"</span>);\n} <span class=\"hljs-keyword\">catch</span> (err) {\n  <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"CAPABILITY_DISABLED\"</span>) {\n    <span class=\"hljs-comment\">// ask the owner to enable KV in the Backend tab; err.hint says what is off</span>\n  } <span class=\"hljs-keyword\">else</span> {\n    <span class=\"hljs-keyword\">throw</span> err;\n  }\n}</code></pre>\n<p>Three cases do not throw the way the rest do:</p>\n<ul>\n<li><code>kv.get</code> on a missing key resolves <code>null</code> rather than throwing <code>NotFoundError</code>.</li>\n<li>Realtime reports sign-in, capability, and connection-limit failures by closing\nthe socket; the next <code>publish</code> or <code>presence()</code> call on any channel throws that\nerror. A <code>presence()</code> call still in flight when the connection drops rejects\nwith <code>DISCONNECTED</code> (status 0); retry it once the socket is back. A channel\nyou have <code>close()</code>d throws <code>CHANNEL_CLOSED</code> (status 0).</li>\n<li>With the module import, the error classes are exported for <code>instanceof</code>\nchecks; with the global script, branch on <code>.code</code> or <code>.name</code>.</li>\n</ul>\n",
    "headings": [
      {
        "id": "add-it-to-a-canvas",
        "text": "Add it to a canvas",
        "level": 2
      },
      {
        "id": "one-global-canvasdrop",
        "text": "One global: canvasdrop",
        "level": 2
      },
      {
        "id": "how-the-sdk-finds-your-canvas",
        "text": "How the SDK finds your canvas",
        "level": 2
      },
      {
        "id": "turn-the-backend-on-first",
        "text": "Turn the backend on first",
        "level": 2
      },
      {
        "id": "the-surface",
        "text": "The surface",
        "level": 2
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      }
    ],
    "text": "Browser SDK You have a static canvas and want it to keep data, accept files, call a model, know who is looking, or sync between viewers. One <script> tag gives the page a global, canvasdrop , carrying the six fixed primitives (KV, files, AI, identity, realtime, and admin-granted outbound Connections) plus authoring. There is no build step and nothing to configure in the page: identity comes from the viewer's session, the canvas is identified from its own URL, and no key ever reaches the browser. Add it to a canvas One script tag, then call the global: < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); // { id, email, name, avatarUrl, kind } await canvasdrop. kv . set ( \"last-viewer\" , me. name ); const views = await canvasdrop. kv . increment ( \"views\" ); // 1 on the first call, then 2, 3, ... </ script > The SDK tag is a plain <script> , so it runs before your module; type=\"module\" gives you top-level await . The root-relative src resolves against the canvas's own origin, and the server serves the script there in both URL modes, behind the same sign-in as the canvas. A fresh local instance ( pnpm dev , dev auth) is already signed in, so the snippet runs as soon as the canvas has its backend on (see Turn the backend on first ). {base}/sdk/v1.js is a stable path for the life of v1, cached for an hour ( cache-control: public, max-age=3600 ), so deployed canvases pick up SDK fixes without a redeploy. On a local checkout, a 503 from that path means the bundle is not built yet: run pnpm build (or pnpm --filter @canvas-drop/sdk build ). One global: canvasdrop The script defines exactly one global, window.canvasdrop . There is no cd alias, no second name, and no version property on the object. The client has me() , kv , files , ai , realtime , connections , and canvases , and nothing else. The same client lives in the workspace package @canvas-drop/sdk (private, not published). Its module entry exports createClient , detectContext , SDK_VERSION ( \"1\" ), ERROR_CODES , errorFromResponse , the error classes, and the types. The served script is exactly createClient({ context: detectContext(window.location) }) assigned to window.canvasdrop . How the SDK finds your canvas detectContext(window.location) reads the slug and the API base from the page URL. You never pass either yourself, and there is no override. URL mode Page URL Slug API base Path https://canvases.example.com/c/quiet-otter-x7k2/ the segment after /c/ the page's own origin Subdomain https://quiet-otter-x7k2.canvases.example.com/ the first hostname label the protocol plus the remaining labels (port kept, if any) Path mode is checked first ( /c/{slug} ); any other URL is treated as a subdomain. Every HTTP request goes to {apiBase}/v1/c/{slug}/… with credentials: \"include\" , so the viewer's session cookie rides along; realtime opens one WebSocket per page at {apiBase}/v1/c/{slug}/realtime ( ws or wss to match the page). On a local path-mode instance the page is http://localhost:3000/c/quiet-otter-x7k2/ and the API base is http://localhost:3000 . Turn the backend on first A canvas is static until its owner or an editor switches Enable backend on in the canvas's Backend tab. That master switch is off by default and gates every primitive, including me() . With it on, each feature has its own toggle, and three of them also depend on an operator setting for the instance: Feature Toggle in the Backend tab Toggle default Operator setting Identity, me() none; on whenever the backend is on on none KV Key-value storage on none Files File storage on none AI AI on an AI provider key ( CANVAS_DROP_AI_API_KEY , or the admin override) Realtime Realtime on CANVAS_DROP_REALTIME (default on ) Connections no canvas-owned toggle; each admin grant is the switch no grants an enabled admin profile attached to this canvas Authoring Authoring off CANVAS_DROP_AUTHORING (default off ), or the admin override in Admin → Configuration A call to anything that is off throws CapabilityDisabledError (code CAPABILITY_DISABLED , status 403). Its .hint (also its .message ) names the gate that failed, the master switch, the feature toggle, or the operator setting, and how to open it. Two share states close the whole API no matter how the switches are set; both skip the owner and editors: Public link : every viewer who is not the owner or an editor, signed in or not, gets STATIC_ONLY (403) from every primitive; the owner and editors keep the full API. Password : on a password-protected canvas the API answers PASSWORD_REQUIRED (403) until the viewer has passed the password gate on the canvas page itself; the owner and editors are never prompted. The Capabilities page covers the tab. The surface Namespace What it does Reference canvasdrop.me() The signed-in viewer: { id, email, name, avatarUrl, kind } . kind is \"member\" , or \"guest\" for a retained legacy guest session. Identity canvasdrop.kv get , set , delete , list , increment . The same five on canvasdrop.kv.user store per viewer. get resolves null for a missing key. KV canvasdrop.files upload(file) , list() , delete(id) , and the synchronous url(id) . Files canvasdrop.ai chat(messages, { model }) resolves { text, usage, cost } ; stream(messages, { model }) yields text chunks. model is required; system and maxTokens are optional. AI canvasdrop.realtime channel(name) returns a channel with publish , subscribe , unsubscribe , presence , onPresence , onJoin , onLeave , close , over one shared WebSocket that reconnects on its own. Realtime canvasdrop.connections fetch(profile, relativePath, init?) returns a native upstream Response through one exact origin an admin granted to the canvas. Protected headers stay server-side. Connections canvasdrop.canvases publish , update , list , revoke : a signed-in viewer creates and manages canvases from the page, as themselves (the authoring capability). Authoring Errors Every failure throws an error extending CanvasdropError : a string .code , a numeric .status , and, when the server sent one, a .hint with the fix. Six subclasses cover the cases you most often branch on: Class .code .status NotAuthenticatedError NOT_AUTHENTICATED 401 NotFoundError NOT_FOUND 404 CapabilityDisabledError CAPABILITY_DISABLED 403 QuotaExceededError QUOTA_EXCEEDED , GUEST_AI_CAP , KEY_LIMIT , CONNECTION_LIMIT , or a size code ( KEY_TOO_LARGE , VALUE_TOO_LARGE , FILE_TOO_LARGE ) 429, 409, or 413 (the wire status) PublishFailedError PUBLISH_FAILED , with the new canvas's .id 502 UpdatePartialError UPDATE_PARTIAL , with .stage and the saved .current record 502 Everything else ( PASSWORD_REQUIRED , STATIC_ONLY , DISABLED , MODEL_NOT_ALLOWED , NOT_NUMERIC , INVALID_BODY , and the rest) arrives as the base CanvasdropError ; branch on .code . The full list is on the error codes page. try { await canvasdrop. kv . increment ( \"votes\" ); } catch (err) { if (err. code === \"CAPABILITY_DISABLED\" ) { // ask the owner to enable KV in the Backend tab; err.hint says what is off } else { throw err; } } Three cases do not throw the way the rest do: kv.get on a missing key resolves null rather than throwing NotFoundError . Realtime reports sign-in, capability, and connection-limit failures by closing the socket; the next publish or presence() call on any channel throws that error. A presence() call still in flight when the connection drops rejects with DISCONNECTED (status 0); retry it once the socket is back. A channel you have close() d throws CHANNEL_CLOSED (status 0). With the module import, the error classes are exported for instanceof checks; with the global script, branch on .code or .name ."
  },
  {
    "path": "sdk/kv",
    "section": "Browser SDK",
    "title": "Key–value storage",
    "html": "<h1 id=\"key-value-storage\">Key-value storage</h1>\n<p>Store JSON that outlives a reload, from a static canvas, with no server code:\ncounters, settings, form submissions, small documents. This page is the\nreference for <code>canvasdrop.kv</code>, the KV primitive on the <code>canvasdrop</code> global that\n<code>&lt;script src=\"/sdk/v1.js\"&gt;</code> defines in every canvas. By the end you can read,\nwrite, count, and page through keys in both scopes and handle every error KV\nreturns.</p>\n<p>The canvas needs <strong>Enable backend</strong> on and the <strong>Key-value storage</strong> toggle on\n(it is pre-enabled) in its <strong>Backend</strong> tab; see\n<a href=\"/docs/authoring/capabilities\">Capabilities</a>.</p>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">src</span>=<span class=\"hljs-string\">\"/sdk/v1.js\"</span>&gt;</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">type</span>=<span class=\"hljs-string\">\"module\"</span>&gt;</span><span class=\"language-javascript\">\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">set</span>(<span class=\"hljs-string\">\"votes\"</span>, <span class=\"hljs-number\">0</span>);                        <span class=\"hljs-comment\">// any JSON value except null</span>\n  <span class=\"hljs-keyword\">const</span> n = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">get</span>(<span class=\"hljs-string\">\"votes\"</span>);                 <span class=\"hljs-comment\">// 0 (null if the key is absent)</span>\n  <span class=\"hljs-keyword\">const</span> total = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">increment</span>(<span class=\"hljs-string\">\"votes\"</span>);       <span class=\"hljs-comment\">// 1, atomic +1</span>\n  <span class=\"hljs-keyword\">const</span> stepped = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">increment</span>(<span class=\"hljs-string\">\"votes\"</span>, <span class=\"hljs-number\">5</span>);  <span class=\"hljs-comment\">// 6, atomic +5</span>\n  <span class=\"hljs-keyword\">const</span> page = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">list</span>({ <span class=\"hljs-attr\">prefix</span>: <span class=\"hljs-string\">\"p:\"</span>, <span class=\"hljs-attr\">limit</span>: <span class=\"hljs-number\">100</span> }); <span class=\"hljs-comment\">// { entries, nextCursor }</span>\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">delete</span>(<span class=\"hljs-string\">\"votes\"</span>);                        <span class=\"hljs-comment\">// idempotent</span>\n</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span></code></pre>\n<h2 id=\"two-scopes-one-interface\">Two scopes, one interface</h2>\n<table>\n<thead>\n<tr>\n<th>Scope</th>\n<th>Namespace</th>\n<th>Who shares the keys</th>\n<th>HTTP base</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Shared</td>\n<td><code>canvasdrop.kv</code></td>\n<td>Every viewer of the canvas reads and writes the same values</td>\n<td><code>{base}/v1/c/{slug}/kv</code></td>\n</tr>\n<tr>\n<td>Per-viewer</td>\n<td><code>canvasdrop.kv.user</code></td>\n<td>Only the signed-in viewer; each person sees their own values</td>\n<td><code>{base}/v1/c/{slug}/kv/user</code></td>\n</tr>\n</tbody></table>\n<p>Both namespaces have the same five methods (<code>KvNamespace</code>). The server derives\nthe per-viewer scope from the viewer's session identity, never from anything\nyour code sends, so one viewer cannot read or write another viewer's keys, and\na canvas has no way to enumerate other viewers' per-viewer data.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-property\">user</span>.<span class=\"hljs-title function_\">set</span>(<span class=\"hljs-string\">\"theme\"</span>, <span class=\"hljs-string\">\"dark\"</span>);\n<span class=\"hljs-keyword\">const</span> theme = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-property\">user</span>.<span class=\"hljs-title function_\">get</span>(<span class=\"hljs-string\">\"theme\"</span>); <span class=\"hljs-comment\">// this viewer's value only</span></code></pre>\n<h2 id=\"methods\">Methods</h2>\n<p>Signatures as declared in the SDK:</p>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Signature</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>get</code></td>\n<td><code>get&lt;T = unknown&gt;(key: string): Promise&lt;T | null&gt;</code></td>\n</tr>\n<tr>\n<td><code>set</code></td>\n<td><code>set(key: string, value: unknown): Promise&lt;void&gt;</code></td>\n</tr>\n<tr>\n<td><code>delete</code></td>\n<td><code>delete(key: string): Promise&lt;void&gt;</code></td>\n</tr>\n<tr>\n<td><code>list</code></td>\n<td><code>list(opts?: { prefix?: string; cursor?: string; limit?: number }): Promise&lt;KvList&gt;</code></td>\n</tr>\n<tr>\n<td><code>increment</code></td>\n<td><code>increment(key: string, by?: number): Promise&lt;number&gt;</code></td>\n</tr>\n</tbody></table>\n<pre><code class=\"hljs language-ts\"><span class=\"hljs-keyword\">interface</span> <span class=\"hljs-title class_\">KvList</span> {\n  <span class=\"hljs-attr\">entries</span>: <span class=\"hljs-title class_\">Array</span>&lt;{ <span class=\"hljs-attr\">key</span>: <span class=\"hljs-built_in\">string</span>; <span class=\"hljs-attr\">value</span>: <span class=\"hljs-built_in\">unknown</span> }&gt;;\n  <span class=\"hljs-attr\">nextCursor</span>: <span class=\"hljs-built_in\">string</span> | <span class=\"hljs-literal\">null</span>;\n}</code></pre>\n<p>Keys are any string; the SDK URL-encodes them, so <code>/</code>, spaces, and Unicode are\nfine. A key is at most 512 bytes of UTF-8 (see <a href=\"#limits\">Limits</a>).</p>\n<h3 id=\"get\">get</h3>\n<p><code>get(key)</code> resolves to the stored value, or <code>null</code> when the key does not exist.\nThe <code>404</code> never surfaces as an error; the SDK folds <code>NotFoundError</code> into <code>null</code>.\nThe type parameter is a convenience for your own code; nothing is validated at\nruntime.</p>\n<h3 id=\"set\">set</h3>\n<p><code>set(key, value)</code> writes any JSON value except <code>null</code> (string, number, boolean,\nobject, array) and overwrites what was there; the last write wins. Because\n<code>null</code> means \"absent\" on read, the server refuses to store a JSON <code>null</code>\n(<code>INVALID_BODY</code>, 400, <code>value must not be null</code>); <code>delete</code> the key instead.</p>\n<h3 id=\"delete\">delete</h3>\n<p><code>delete(key)</code> removes the key. Deleting a key that does not exist succeeds and\ndoes nothing.</p>\n<h3 id=\"increment\">increment</h3>\n<p><code>increment(key, by = 1)</code> adds <code>by</code> to a numeric value in one atomic upsert on\nthe server, so concurrent increments never lose an update. A missing key starts\nat <code>0</code>. <code>by</code> may be negative or fractional but must be a finite number\n(<code>INVALID_BODY</code>, 400, <code>by must be a finite number</code> otherwise). It resolves to the\nnew total and rejects with <code>NOT_NUMERIC</code> (409) when the stored value is not a\nnumber. <code>increment</code> is the only read-modify-write the server performs\natomically; there is no batch, TTL, compare-and-set, or transaction API. For\nanything else, design keys so each writer owns its own.</p>\n<h3 id=\"list\">list</h3>\n<p><code>list(opts)</code> returns entries in ascending key order. <code>prefix</code> is a literal\nstring match. <code>limit</code> defaults to <code>100</code>; the server clamps it to <code>1..1000</code>.\n<code>nextCursor</code> is the last key of the page when more entries remain and <code>null</code> on\nthe last page; pass it back as <code>cursor</code> to continue.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">let</span> cursor;\n<span class=\"hljs-keyword\">do</span> {\n  <span class=\"hljs-keyword\">const</span> page = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">list</span>({ <span class=\"hljs-attr\">prefix</span>: <span class=\"hljs-string\">\"entry:\"</span>, cursor, <span class=\"hljs-attr\">limit</span>: <span class=\"hljs-number\">500</span> });\n  <span class=\"hljs-keyword\">for</span> (<span class=\"hljs-keyword\">const</span> { key, value } <span class=\"hljs-keyword\">of</span> page.<span class=\"hljs-property\">entries</span>) <span class=\"hljs-title function_\">render</span>(key, value);\n  cursor = page.<span class=\"hljs-property\">nextCursor</span>;\n} <span class=\"hljs-keyword\">while</span> (cursor);</code></pre>\n<h2 id=\"limits\">Limits</h2>\n<table>\n<thead>\n<tr>\n<th>Limit</th>\n<th>Value</th>\n<th>Error when exceeded</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Key size</td>\n<td>512 bytes (UTF-8)</td>\n<td><code>KEY_TOO_LARGE</code> (413)</td>\n</tr>\n<tr>\n<td>Value size, serialized JSON</td>\n<td>64 KiB</td>\n<td><code>VALUE_TOO_LARGE</code> (413)</td>\n</tr>\n<tr>\n<td>Keys per canvas, shared scope</td>\n<td>10,000</td>\n<td><code>KEY_LIMIT</code> (409)</td>\n</tr>\n<tr>\n<td>Keys per viewer per canvas, per-viewer scope</td>\n<td>1,000</td>\n<td><code>KEY_LIMIT</code> (409)</td>\n</tr>\n</tbody></table>\n<p>The key-count caps are admin-tunable per instance; the key and value sizes are\nfixed. <code>KEY_LIMIT</code> applies only when a <code>set</code> or <code>increment</code> would create a new\nkey; updating an existing key always succeeds. The server checks a <code>set</code> in\nthis order: key size, body parses as JSON, not <code>null</code>, value size, key count.\nAn <code>increment</code> checks key size, <code>by</code>, key count, then the stored type.</p>\n<p>Runtime API calls are rate limited per viewer per canvas: 120 per minute by\ndefault (<code>CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN</code>), shared by every Runtime API\ncall that viewer makes on that canvas except AI, which has its own bucket. Past\nthe limit, calls reject with <code>RATE_LIMITED</code> (429) and a <code>Retry-After</code> header;\ndebounce hot counters rather than writing on every keystroke. Every call counts\ntoward the canvas's usage stats, and <code>set</code>, <code>delete</code>, and <code>increment</code> are\nrecorded in the instance audit log.</p>\n<h2 id=\"errors\">Errors</h2>\n<p>Every method rejects with a <code>CanvasdropError</code> subclass; branch on <code>err.code</code>\n(the wire code) and <code>err.status</code>, or catch the subclass you care about.</p>\n<table>\n<thead>\n<tr>\n<th>What happened</th>\n<th><code>err.code</code></th>\n<th>Status</th>\n<th>Class</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Key, value, or key count over a limit</td>\n<td><code>KEY_TOO_LARGE</code>, <code>VALUE_TOO_LARGE</code>, <code>KEY_LIMIT</code></td>\n<td>413, 413, 409</td>\n<td><code>QuotaExceededError</code></td>\n</tr>\n<tr>\n<td><code>increment</code> on a non-number</td>\n<td><code>NOT_NUMERIC</code></td>\n<td>409</td>\n<td><code>CanvasdropError</code></td>\n</tr>\n<tr>\n<td><code>set(key, null)</code> or a non-finite <code>by</code></td>\n<td><code>INVALID_BODY</code></td>\n<td>400</td>\n<td><code>CanvasdropError</code></td>\n</tr>\n<tr>\n<td>Too many calls this minute</td>\n<td><code>RATE_LIMITED</code></td>\n<td>429</td>\n<td><code>CanvasdropError</code></td>\n</tr>\n<tr>\n<td>KV toggle off, or the canvas backend off</td>\n<td><code>CAPABILITY_DISABLED</code></td>\n<td>403</td>\n<td><code>CapabilityDisabledError</code>; <code>err.message</code> carries the server's hint</td>\n</tr>\n<tr>\n<td>Public link canvas, caller is not the owner or an editor</td>\n<td><code>STATIC_ONLY</code></td>\n<td>403</td>\n<td><code>CanvasdropError</code>; public canvases are static-only and every primitive is refused</td>\n</tr>\n<tr>\n<td>Password-protected canvas, gate not passed</td>\n<td><code>PASSWORD_REQUIRED</code></td>\n<td>403</td>\n<td><code>CanvasdropError</code>; the owner and editors never see it</td>\n</tr>\n<tr>\n<td>Viewer's session has ended</td>\n<td><code>NOT_AUTHENTICATED</code></td>\n<td>401</td>\n<td><code>NotAuthenticatedError</code> in <code>dev</code> and <code>proxy</code> auth modes; in <code>oidc</code> mode the gateway redirects to sign-in instead. Either way, reload the page</td>\n</tr>\n</tbody></table>\n<p><code>get</code> never throws <code>NotFoundError</code>; the 404 is folded into <code>null</code>.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">try</span> {\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">set</span>(<span class=\"hljs-string\">\"doc\"</span>, bigObject);\n} <span class=\"hljs-keyword\">catch</span> (err) {\n  <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"VALUE_TOO_LARGE\"</span>) <span class=\"hljs-title function_\">showToast</span>(<span class=\"hljs-string\">\"Keep it under 64 KiB\"</span>);\n  <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">throw</span> err;\n}</code></pre>\n<h2 id=\"http-calls-behind-each-method\">HTTP calls behind each method</h2>\n<p>Useful when you are reading the network tab or calling the\n<a href=\"/docs/api/runtime-api\">Runtime API</a> directly. <code>{kv}</code> is <code>/v1/c/{slug}/kv</code> for\nthe shared scope and <code>/v1/c/{slug}/kv/user</code> for the per-viewer scope; the key\nis one URL-encoded path segment.</p>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Request</th>\n<th>Success response</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>get(key)</code></td>\n<td><code>GET {kv}/{key}</code></td>\n<td><code>200 {\"value\": ...}</code>; <code>404 {\"code\":\"NOT_FOUND\"}</code> becomes <code>null</code></td>\n</tr>\n<tr>\n<td><code>set(key, value)</code></td>\n<td><code>PUT {kv}/{key}</code>, body is the raw JSON value</td>\n<td><code>200 {\"ok\":true}</code></td>\n</tr>\n<tr>\n<td><code>delete(key)</code></td>\n<td><code>DELETE {kv}/{key}</code></td>\n<td><code>200 {\"ok\":true}</code></td>\n</tr>\n<tr>\n<td><code>increment(key, by)</code></td>\n<td><code>POST {kv}/{key}/increment</code>, body <code>{\"by\": 1}</code></td>\n<td><code>200 {\"value\": 1}</code></td>\n</tr>\n<tr>\n<td><code>list(opts)</code></td>\n<td><code>GET {kv}?prefix=&amp;cursor=&amp;limit=</code></td>\n<td><code>200 {\"entries\":[{\"key\",\"value\"}],\"nextCursor\":null}</code></td>\n</tr>\n</tbody></table>\n<p>See <a href=\"/docs/api/errors\">error codes</a> for the full list across every primitive.</p>\n",
    "headings": [
      {
        "id": "two-scopes-one-interface",
        "text": "Two scopes, one interface",
        "level": 2
      },
      {
        "id": "methods",
        "text": "Methods",
        "level": 2
      },
      {
        "id": "get",
        "text": "get",
        "level": 3
      },
      {
        "id": "set",
        "text": "set",
        "level": 3
      },
      {
        "id": "delete",
        "text": "delete",
        "level": 3
      },
      {
        "id": "increment",
        "text": "increment",
        "level": 3
      },
      {
        "id": "list",
        "text": "list",
        "level": 3
      },
      {
        "id": "limits",
        "text": "Limits",
        "level": 2
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      },
      {
        "id": "http-calls-behind-each-method",
        "text": "HTTP calls behind each method",
        "level": 2
      }
    ],
    "text": "Key-value storage Store JSON that outlives a reload, from a static canvas, with no server code: counters, settings, form submissions, small documents. This page is the reference for canvasdrop.kv , the KV primitive on the canvasdrop global that <script src=\"/sdk/v1.js\"> defines in every canvas. By the end you can read, write, count, and page through keys in both scopes and handle every error KV returns. The canvas needs Enable backend on and the Key-value storage toggle on (it is pre-enabled) in its Backend tab; see Capabilities . < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > await canvasdrop. kv . set ( \"votes\" , 0 ); // any JSON value except null const n = await canvasdrop. kv . get ( \"votes\" ); // 0 (null if the key is absent) const total = await canvasdrop. kv . increment ( \"votes\" ); // 1, atomic +1 const stepped = await canvasdrop. kv . increment ( \"votes\" , 5 ); // 6, atomic +5 const page = await canvasdrop. kv . list ({ prefix : \"p:\" , limit : 100 }); // { entries, nextCursor } await canvasdrop. kv . delete ( \"votes\" ); // idempotent </ script > Two scopes, one interface Scope Namespace Who shares the keys HTTP base Shared canvasdrop.kv Every viewer of the canvas reads and writes the same values {base}/v1/c/{slug}/kv Per-viewer canvasdrop.kv.user Only the signed-in viewer; each person sees their own values {base}/v1/c/{slug}/kv/user Both namespaces have the same five methods ( KvNamespace ). The server derives the per-viewer scope from the viewer's session identity, never from anything your code sends, so one viewer cannot read or write another viewer's keys, and a canvas has no way to enumerate other viewers' per-viewer data. await canvasdrop. kv . user . set ( \"theme\" , \"dark\" ); const theme = await canvasdrop. kv . user . get ( \"theme\" ); // this viewer's value only Methods Signatures as declared in the SDK: Method Signature get get<T = unknown>(key: string): Promise<T | null> set set(key: string, value: unknown): Promise<void> delete delete(key: string): Promise<void> list list(opts?: { prefix?: string; cursor?: string; limit?: number }): Promise<KvList> increment increment(key: string, by?: number): Promise<number> interface KvList { entries : Array <{ key : string ; value : unknown }>; nextCursor : string | null ; } Keys are any string; the SDK URL-encodes them, so / , spaces, and Unicode are fine. A key is at most 512 bytes of UTF-8 (see Limits ). get get(key) resolves to the stored value, or null when the key does not exist. The 404 never surfaces as an error; the SDK folds NotFoundError into null . The type parameter is a convenience for your own code; nothing is validated at runtime. set set(key, value) writes any JSON value except null (string, number, boolean, object, array) and overwrites what was there; the last write wins. Because null means \"absent\" on read, the server refuses to store a JSON null ( INVALID_BODY , 400, value must not be null ); delete the key instead. delete delete(key) removes the key. Deleting a key that does not exist succeeds and does nothing. increment increment(key, by = 1) adds by to a numeric value in one atomic upsert on the server, so concurrent increments never lose an update. A missing key starts at 0 . by may be negative or fractional but must be a finite number ( INVALID_BODY , 400, by must be a finite number otherwise). It resolves to the new total and rejects with NOT_NUMERIC (409) when the stored value is not a number. increment is the only read-modify-write the server performs atomically; there is no batch, TTL, compare-and-set, or transaction API. For anything else, design keys so each writer owns its own. list list(opts) returns entries in ascending key order. prefix is a literal string match. limit defaults to 100 ; the server clamps it to 1..1000 . nextCursor is the last key of the page when more entries remain and null on the last page; pass it back as cursor to continue. let cursor; do { const page = await canvasdrop. kv . list ({ prefix : \"entry:\" , cursor, limit : 500 }); for ( const { key, value } of page. entries ) render (key, value); cursor = page. nextCursor ; } while (cursor); Limits Limit Value Error when exceeded Key size 512 bytes (UTF-8) KEY_TOO_LARGE (413) Value size, serialized JSON 64 KiB VALUE_TOO_LARGE (413) Keys per canvas, shared scope 10,000 KEY_LIMIT (409) Keys per viewer per canvas, per-viewer scope 1,000 KEY_LIMIT (409) The key-count caps are admin-tunable per instance; the key and value sizes are fixed. KEY_LIMIT applies only when a set or increment would create a new key; updating an existing key always succeeds. The server checks a set in this order: key size, body parses as JSON, not null , value size, key count. An increment checks key size, by , key count, then the stored type. Runtime API calls are rate limited per viewer per canvas: 120 per minute by default ( CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN ), shared by every Runtime API call that viewer makes on that canvas except AI, which has its own bucket. Past the limit, calls reject with RATE_LIMITED (429) and a Retry-After header; debounce hot counters rather than writing on every keystroke. Every call counts toward the canvas's usage stats, and set , delete , and increment are recorded in the instance audit log. Errors Every method rejects with a CanvasdropError subclass; branch on err.code (the wire code) and err.status , or catch the subclass you care about. What happened err.code Status Class Key, value, or key count over a limit KEY_TOO_LARGE , VALUE_TOO_LARGE , KEY_LIMIT 413, 413, 409 QuotaExceededError increment on a non-number NOT_NUMERIC 409 CanvasdropError set(key, null) or a non-finite by INVALID_BODY 400 CanvasdropError Too many calls this minute RATE_LIMITED 429 CanvasdropError KV toggle off, or the canvas backend off CAPABILITY_DISABLED 403 CapabilityDisabledError ; err.message carries the server's hint Public link canvas, caller is not the owner or an editor STATIC_ONLY 403 CanvasdropError ; public canvases are static-only and every primitive is refused Password-protected canvas, gate not passed PASSWORD_REQUIRED 403 CanvasdropError ; the owner and editors never see it Viewer's session has ended NOT_AUTHENTICATED 401 NotAuthenticatedError in dev and proxy auth modes; in oidc mode the gateway redirects to sign-in instead. Either way, reload the page get never throws NotFoundError ; the 404 is folded into null . try { await canvasdrop. kv . set ( \"doc\" , bigObject); } catch (err) { if (err. code === \"VALUE_TOO_LARGE\" ) showToast ( \"Keep it under 64 KiB\" ); else throw err; } HTTP calls behind each method Useful when you are reading the network tab or calling the Runtime API directly. {kv} is /v1/c/{slug}/kv for the shared scope and /v1/c/{slug}/kv/user for the per-viewer scope; the key is one URL-encoded path segment. Method Request Success response get(key) GET {kv}/{key} 200 {\"value\": ...} ; 404 {\"code\":\"NOT_FOUND\"} becomes null set(key, value) PUT {kv}/{key} , body is the raw JSON value 200 {\"ok\":true} delete(key) DELETE {kv}/{key} 200 {\"ok\":true} increment(key, by) POST {kv}/{key}/increment , body {\"by\": 1} 200 {\"value\": 1} list(opts) GET {kv}?prefix=&cursor=&limit= 200 {\"entries\":[{\"key\",\"value\"}],\"nextCursor\":null} See error codes for the full list across every primitive."
  },
  {
    "path": "sdk/files",
    "section": "Browser SDK",
    "title": "File storage",
    "html": "<h1 id=\"file-storage\">File storage</h1>\n<p>Let viewers upload files to a canvas and get back a URL you can put in an\n<code>&lt;img&gt;</code>, a link, or a <code>fetch()</code>. This page is the reference for\n<code>canvasdrop.files</code>, the files primitive on the <code>canvasdrop</code> global that\n<code>&lt;script src=\"/sdk/v1.js\"&gt;</code> defines in every canvas. By the end you can upload,\nlist, serve, and delete files, and handle every error the primitive returns.</p>\n<p>The canvas needs <strong>Enable backend</strong> on and the <strong>File storage</strong> toggle on (it is\npre-enabled) in its <strong>Backend</strong> tab; see\n<a href=\"/docs/authoring/capabilities\">Capabilities</a>. There is nothing to configure in\nthe page and no key to hold.</p>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">src</span>=<span class=\"hljs-string\">\"/sdk/v1.js\"</span>&gt;</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">type</span>=<span class=\"hljs-string\">\"module\"</span>&gt;</span><span class=\"language-javascript\">\n  <span class=\"hljs-comment\">// &lt;input type=\"file\" id=\"picker\"&gt; and &lt;img id=\"img\"&gt; are on the page</span>\n  <span class=\"hljs-keyword\">const</span> f = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">files</span>.<span class=\"hljs-title function_\">upload</span>(picker.<span class=\"hljs-property\">files</span>[<span class=\"hljs-number\">0</span>]); <span class=\"hljs-comment\">// { id, name, size, url }</span>\n  img.<span class=\"hljs-property\">src</span> = f.<span class=\"hljs-property\">url</span>;                                           <span class=\"hljs-comment\">// absolute content URL</span>\n\n  <span class=\"hljs-keyword\">const</span> all = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">files</span>.<span class=\"hljs-title function_\">list</span>();                 <span class=\"hljs-comment\">// FileMeta[]</span>\n  <span class=\"hljs-keyword\">const</span> href = canvasdrop.<span class=\"hljs-property\">files</span>.<span class=\"hljs-title function_\">url</span>(f.<span class=\"hljs-property\">id</span>);                   <span class=\"hljs-comment\">// same URL as f.url, no request</span>\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">files</span>.<span class=\"hljs-title function_\">delete</span>(f.<span class=\"hljs-property\">id</span>);                       <span class=\"hljs-comment\">// the URL now returns 404</span>\n</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span></code></pre>\n<p>Files belong to the canvas, not to the viewer who uploaded them: every viewer\nwho can open the canvas can list, read, and delete every file in it. There is no\nper-viewer scope for files (KV has <code>kv.user</code>; files has no equivalent). The\nserver records who uploaded each file for its audit trail, but the API does not\nexpose or filter by uploader.</p>\n<h2 id=\"methods\">Methods</h2>\n<p>Signatures as declared in the SDK (<code>CanvasdropClient.files</code>), with the runtime\nAPI call each one makes:</p>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Signature</th>\n<th>HTTP call</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>upload</code></td>\n<td><code>upload(file: File): Promise&lt;{ id: string; name: string; size: number; url: string }&gt;</code></td>\n<td><code>POST {base}/v1/c/{slug}/files</code></td>\n</tr>\n<tr>\n<td><code>list</code></td>\n<td><code>list(): Promise&lt;FileMeta[]&gt;</code></td>\n<td><code>GET {base}/v1/c/{slug}/files</code></td>\n</tr>\n<tr>\n<td><code>delete</code></td>\n<td><code>delete(id: string): Promise&lt;void&gt;</code></td>\n<td><code>DELETE {base}/v1/c/{slug}/files/{id}</code></td>\n</tr>\n<tr>\n<td><code>url</code></td>\n<td><code>url(id: string): string</code></td>\n<td>none (builds <code>{base}/v1/c/{slug}/files/{id}/content</code>)</td>\n</tr>\n</tbody></table>\n<pre><code class=\"hljs language-ts\"><span class=\"hljs-keyword\">interface</span> <span class=\"hljs-title class_\">FileMeta</span> {\n  <span class=\"hljs-attr\">id</span>: <span class=\"hljs-built_in\">string</span>;\n  <span class=\"hljs-attr\">name</span>: <span class=\"hljs-built_in\">string</span>;\n  <span class=\"hljs-attr\">size</span>: <span class=\"hljs-built_in\">number</span>;      <span class=\"hljs-comment\">// bytes</span>\n  <span class=\"hljs-attr\">mime</span>?: <span class=\"hljs-built_in\">string</span>;     <span class=\"hljs-comment\">// always sent by the server</span>\n  <span class=\"hljs-attr\">createdAt</span>?: <span class=\"hljs-built_in\">number</span>; <span class=\"hljs-comment\">// Unix ms; always sent by the server</span>\n}</code></pre>\n<h3 id=\"upload\">upload</h3>\n<p><code>upload(file)</code> posts the <code>File</code> as <code>multipart/form-data</code> under the field name\n<code>file</code>. The stored name is <code>file.name</code> (or <code>upload</code> when the <code>File</code> has none)\nand the stored MIME type is <code>file.type</code> (or <code>application/octet-stream</code>). The\nserver answers <code>201 { id, name, size, url }</code> with a root-relative <code>url</code>; the SDK\nreplaces it with the absolute content URL before resolving, so <code>f.url</code> is\ncorrect in both URL modes. The result carries no <code>mime</code> or <code>createdAt</code>; call\n<code>list()</code> when you need them.</p>\n<p>There is no progress callback and no upload option. For a large file, show your\nown pending state around the <code>await</code>. Ids are server-assigned UUIDs.</p>\n<h3 id=\"list\">list</h3>\n<p><code>list()</code> resolves to every file in the canvas, with its metadata, in one array.\nThere is no paging and no filter.</p>\n<h3 id=\"delete\">delete</h3>\n<p><code>delete(id)</code> removes the file row and its bytes. It rejects with <code>NotFoundError</code>\nwhen <code>id</code> is not a file of this canvas, so deleting the same id twice rejects\nthe second call. After a delete, the file's content URL returns <code>404</code>.</p>\n<h3 id=\"url\">url</h3>\n<p><code>url(id)</code> builds the content URL synchronously from the id, without a request.\nIt is the same value <code>upload</code> returns in <code>url</code>. It does not check that the id\nexists.</p>\n<p>Every call (upload, list, download, delete) counts in the canvas's usage stats;\nuploads and deletes are also written to the audit log.</p>\n<h2 id=\"content-urls\">Content URLs</h2>\n<p><code>f.url</code> and <code>canvasdrop.files.url(id)</code> both resolve to\n<code>{base}/v1/c/{slug}/files/{id}/content</code>, absolute and correct for the URL mode\nthe instance runs in. The SDK detects the mode from <code>location</code>, so you never\nbuild this URL by hand.</p>\n<table>\n<thead>\n<tr>\n<th>URL mode</th>\n<th>Content URL origin</th>\n<th>Cross-origin?</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>path</code></td>\n<td>the canvas's own origin</td>\n<td>no</td>\n</tr>\n<tr>\n<td><code>subdomain</code></td>\n<td>the base host (<code>{base}</code>), not the canvas subdomain</td>\n<td>yes; the response carries the credentialed CORS headers your canvas origin needs</td>\n</tr>\n</tbody></table>\n<p>Content is served behind the same sign-in as the canvas and through the same\ncross-canvas isolation as every other runtime API call, so use a file's URL from\nthe canvas that owns it. An <code>&lt;img src&gt;</code> works directly. When you <code>fetch()</code> a\ncontent URL yourself, send credentials so the request succeeds in <code>subdomain</code>\nmode too:</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">const</span> res = <span class=\"hljs-keyword\">await</span> <span class=\"hljs-title function_\">fetch</span>(canvasdrop.<span class=\"hljs-property\">files</span>.<span class=\"hljs-title function_\">url</span>(id), { <span class=\"hljs-attr\">credentials</span>: <span class=\"hljs-string\">\"include\"</span> });\n<span class=\"hljs-keyword\">const</span> blob = <span class=\"hljs-keyword\">await</span> res.<span class=\"hljs-title function_\">blob</span>();</code></pre>\n<p>A content URL for an id that does not exist returns <code>404</code> with\n<code>{ \"code\": \"NOT_FOUND\" }</code>.</p>\n<p>Content requests count toward the same per-viewer runtime rate limit as every\nother SDK call: 120 requests per minute per viewer per canvas by default, set by\nthe operator with <code>CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN</code>. A page that loads\nhundreds of images at once can hit <code>RATE_LIMITED</code> (429); lazy-load or paginate\nlarge galleries.</p>\n<h2 id=\"how-content-is-served\">How content is served</h2>\n<p>Uploaded bytes come from other people, so the content endpoint treats them as\nuntrusted:</p>\n<ul>\n<li><code>Content-Type</code> is the MIME type stored at upload, with\n<code>X-Content-Type-Options: nosniff</code> on every response.</li>\n<li><code>Content-Disposition: inline</code> only for <code>image/png</code>, <code>image/jpeg</code>, <code>image/gif</code>,\n<code>image/webp</code>, and <code>image/avif</code>. Everything else, SVG and HTML included, is\nserved as an <code>attachment</code> and downloads instead of rendering.</li>\n<li>The filename in <code>Content-Disposition</code> is sanitized (ASCII fallback) and\nRFC 5987 encoded (<code>filename*</code>).</li>\n</ul>\n<p>This keeps an uploaded active document (HTML, or a scriptable SVG) from running\nagainst another viewer's session on the canvas origin. If your canvas needs to\ndisplay an SVG a viewer uploaded, <code>fetch()</code> it and render it yourself in a way\nthat does not execute its scripts.</p>\n<h2 id=\"limits\">Limits</h2>\n<table>\n<thead>\n<tr>\n<th>Limit</th>\n<th>Default</th>\n<th>Error when exceeded</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Bytes per file</td>\n<td>25 MiB</td>\n<td><code>FILE_TOO_LARGE</code> (413)</td>\n</tr>\n<tr>\n<td>Bytes per canvas, all files</td>\n<td>1 GiB</td>\n<td><code>QUOTA_EXCEEDED</code> (409)</td>\n</tr>\n</tbody></table>\n<p>Both defaults are admin-tunable per instance (Admin settings, <strong>Limits</strong> group:\n<strong>Max file bytes</strong> and <strong>Max canvas bytes</strong>). The request body itself is capped\nat 25 MiB plus 1 MiB of multipart framing, and that transport cap is a fixed\nconstant: an admin can lower the per-file limit, but raising it above 25 MiB has\nno effect on uploads through the SDK. A body over the transport cap is refused\nwith the same <code>FILE_TOO_LARGE</code> (413) before the file is read.</p>\n<p>The per-canvas quota is a check before the write, not a reservation. Two\nuploads racing at the boundary can both succeed; this is acceptable on the\ntrusted-org model the platform is built for.</p>\n<h2 id=\"errors\">Errors</h2>\n<p>Every method rejects with a <code>CanvasdropError</code> subclass; branch on <code>err.code</code>, or\ncatch the subclass you care about.</p>\n<table>\n<thead>\n<tr>\n<th>Wire code</th>\n<th>Status</th>\n<th>Thrown as</th>\n<th>When</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>FILE_TOO_LARGE</code></td>\n<td>413</td>\n<td><code>QuotaExceededError</code></td>\n<td>the file, or the request body, is over the per-file limit</td>\n</tr>\n<tr>\n<td><code>QUOTA_EXCEEDED</code></td>\n<td>409</td>\n<td><code>QuotaExceededError</code></td>\n<td>the upload would push the canvas over its byte quota</td>\n</tr>\n<tr>\n<td><code>INVALID_BODY</code></td>\n<td>400</td>\n<td><code>CanvasdropError</code></td>\n<td>the request was not multipart, or the <code>file</code> field was missing or not a <code>File</code></td>\n</tr>\n<tr>\n<td><code>NOT_FOUND</code></td>\n<td>404</td>\n<td><code>NotFoundError</code></td>\n<td><code>delete</code> with an id that is not a file of this canvas</td>\n</tr>\n<tr>\n<td><code>CAPABILITY_DISABLED</code></td>\n<td>403</td>\n<td><code>CapabilityDisabledError</code></td>\n<td>the <strong>File storage</strong> toggle or the canvas backend is off</td>\n</tr>\n<tr>\n<td><code>STATIC_ONLY</code></td>\n<td>403</td>\n<td><code>CanvasdropError</code></td>\n<td>the canvas is on the <strong>Public link</strong> rung and the caller is not the owner or an editor</td>\n</tr>\n<tr>\n<td><code>RATE_LIMITED</code></td>\n<td>429</td>\n<td><code>CanvasdropError</code></td>\n<td>the per-viewer runtime rate limit was hit; the response carries <code>Retry-After</code></td>\n</tr>\n</tbody></table>\n<p>Notes:</p>\n<ul>\n<li><code>QuotaExceededError</code> keeps the wire code in <code>err.code</code> and the status in\n<code>err.status</code>. Note the 409: on this route the per-canvas quota is a storage\nconflict, not the 429 that <code>QUOTA_EXCEEDED</code> means for AI spend.</li>\n<li><code>CapabilityDisabledError</code> exposes the server's repair hint as <code>err.hint</code>, and\n<code>err.message</code> is that hint when the server sends one.</li>\n<li>On a <strong>Public link</strong> canvas every primitive is refused for non-owners; only\nthe owner and the canvas's editors reach <code>canvasdrop.files</code> there.</li>\n</ul>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">try</span> {\n  <span class=\"hljs-keyword\">const</span> f = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">files</span>.<span class=\"hljs-title function_\">upload</span>(file);\n  img.<span class=\"hljs-property\">src</span> = f.<span class=\"hljs-property\">url</span>;\n} <span class=\"hljs-keyword\">catch</span> (err) {\n  <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"FILE_TOO_LARGE\"</span>) <span class=\"hljs-title function_\">showToast</span>(<span class=\"hljs-string\">\"Keep files under 25 MiB\"</span>);\n  <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"QUOTA_EXCEEDED\"</span>) <span class=\"hljs-title function_\">showToast</span>(<span class=\"hljs-string\">\"This canvas is out of file storage\"</span>);\n  <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"CAPABILITY_DISABLED\"</span>) <span class=\"hljs-title function_\">showToast</span>(<span class=\"hljs-string\">\"File storage is off for this canvas\"</span>);\n  <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">throw</span> err;\n}</code></pre>\n<p>See <a href=\"/docs/api/errors\">error codes</a> for the full list, and the\n<a href=\"/docs/api/runtime-api\">Runtime API</a> for the HTTP endpoints under\n<code>/v1/c/{slug}/files</code> that these methods call.</p>\n",
    "headings": [
      {
        "id": "methods",
        "text": "Methods",
        "level": 2
      },
      {
        "id": "upload",
        "text": "upload",
        "level": 3
      },
      {
        "id": "list",
        "text": "list",
        "level": 3
      },
      {
        "id": "delete",
        "text": "delete",
        "level": 3
      },
      {
        "id": "url",
        "text": "url",
        "level": 3
      },
      {
        "id": "content-urls",
        "text": "Content URLs",
        "level": 2
      },
      {
        "id": "how-content-is-served",
        "text": "How content is served",
        "level": 2
      },
      {
        "id": "limits",
        "text": "Limits",
        "level": 2
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      }
    ],
    "text": "File storage Let viewers upload files to a canvas and get back a URL you can put in an <img> , a link, or a fetch() . This page is the reference for canvasdrop.files , the files primitive on the canvasdrop global that <script src=\"/sdk/v1.js\"> defines in every canvas. By the end you can upload, list, serve, and delete files, and handle every error the primitive returns. The canvas needs Enable backend on and the File storage toggle on (it is pre-enabled) in its Backend tab; see Capabilities . There is nothing to configure in the page and no key to hold. < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > // <input type=\"file\" id=\"picker\"> and <img id=\"img\"> are on the page const f = await canvasdrop. files . upload (picker. files [ 0 ]); // { id, name, size, url } img. src = f. url ; // absolute content URL const all = await canvasdrop. files . list (); // FileMeta[] const href = canvasdrop. files . url (f. id ); // same URL as f.url, no request await canvasdrop. files . delete (f. id ); // the URL now returns 404 </ script > Files belong to the canvas, not to the viewer who uploaded them: every viewer who can open the canvas can list, read, and delete every file in it. There is no per-viewer scope for files (KV has kv.user ; files has no equivalent). The server records who uploaded each file for its audit trail, but the API does not expose or filter by uploader. Methods Signatures as declared in the SDK ( CanvasdropClient.files ), with the runtime API call each one makes: Method Signature HTTP call upload upload(file: File): Promise<{ id: string; name: string; size: number; url: string }> POST {base}/v1/c/{slug}/files list list(): Promise<FileMeta[]> GET {base}/v1/c/{slug}/files delete delete(id: string): Promise<void> DELETE {base}/v1/c/{slug}/files/{id} url url(id: string): string none (builds {base}/v1/c/{slug}/files/{id}/content ) interface FileMeta { id : string ; name : string ; size : number ; // bytes mime ?: string ; // always sent by the server createdAt ?: number ; // Unix ms; always sent by the server } upload upload(file) posts the File as multipart/form-data under the field name file . The stored name is file.name (or upload when the File has none) and the stored MIME type is file.type (or application/octet-stream ). The server answers 201 { id, name, size, url } with a root-relative url ; the SDK replaces it with the absolute content URL before resolving, so f.url is correct in both URL modes. The result carries no mime or createdAt ; call list() when you need them. There is no progress callback and no upload option. For a large file, show your own pending state around the await . Ids are server-assigned UUIDs. list list() resolves to every file in the canvas, with its metadata, in one array. There is no paging and no filter. delete delete(id) removes the file row and its bytes. It rejects with NotFoundError when id is not a file of this canvas, so deleting the same id twice rejects the second call. After a delete, the file's content URL returns 404 . url url(id) builds the content URL synchronously from the id, without a request. It is the same value upload returns in url . It does not check that the id exists. Every call (upload, list, download, delete) counts in the canvas's usage stats; uploads and deletes are also written to the audit log. Content URLs f.url and canvasdrop.files.url(id) both resolve to {base}/v1/c/{slug}/files/{id}/content , absolute and correct for the URL mode the instance runs in. The SDK detects the mode from location , so you never build this URL by hand. URL mode Content URL origin Cross-origin? path the canvas's own origin no subdomain the base host ( {base} ), not the canvas subdomain yes; the response carries the credentialed CORS headers your canvas origin needs Content is served behind the same sign-in as the canvas and through the same cross-canvas isolation as every other runtime API call, so use a file's URL from the canvas that owns it. An <img src> works directly. When you fetch() a content URL yourself, send credentials so the request succeeds in subdomain mode too: const res = await fetch (canvasdrop. files . url (id), { credentials : \"include\" }); const blob = await res. blob (); A content URL for an id that does not exist returns 404 with { \"code\": \"NOT_FOUND\" } . Content requests count toward the same per-viewer runtime rate limit as every other SDK call: 120 requests per minute per viewer per canvas by default, set by the operator with CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN . A page that loads hundreds of images at once can hit RATE_LIMITED (429); lazy-load or paginate large galleries. How content is served Uploaded bytes come from other people, so the content endpoint treats them as untrusted: Content-Type is the MIME type stored at upload, with X-Content-Type-Options: nosniff on every response. Content-Disposition: inline only for image/png , image/jpeg , image/gif , image/webp , and image/avif . Everything else, SVG and HTML included, is served as an attachment and downloads instead of rendering. The filename in Content-Disposition is sanitized (ASCII fallback) and RFC 5987 encoded ( filename* ). This keeps an uploaded active document (HTML, or a scriptable SVG) from running against another viewer's session on the canvas origin. If your canvas needs to display an SVG a viewer uploaded, fetch() it and render it yourself in a way that does not execute its scripts. Limits Limit Default Error when exceeded Bytes per file 25 MiB FILE_TOO_LARGE (413) Bytes per canvas, all files 1 GiB QUOTA_EXCEEDED (409) Both defaults are admin-tunable per instance (Admin settings, Limits group: Max file bytes and Max canvas bytes ). The request body itself is capped at 25 MiB plus 1 MiB of multipart framing, and that transport cap is a fixed constant: an admin can lower the per-file limit, but raising it above 25 MiB has no effect on uploads through the SDK. A body over the transport cap is refused with the same FILE_TOO_LARGE (413) before the file is read. The per-canvas quota is a check before the write, not a reservation. Two uploads racing at the boundary can both succeed; this is acceptable on the trusted-org model the platform is built for. Errors Every method rejects with a CanvasdropError subclass; branch on err.code , or catch the subclass you care about. Wire code Status Thrown as When FILE_TOO_LARGE 413 QuotaExceededError the file, or the request body, is over the per-file limit QUOTA_EXCEEDED 409 QuotaExceededError the upload would push the canvas over its byte quota INVALID_BODY 400 CanvasdropError the request was not multipart, or the file field was missing or not a File NOT_FOUND 404 NotFoundError delete with an id that is not a file of this canvas CAPABILITY_DISABLED 403 CapabilityDisabledError the File storage toggle or the canvas backend is off STATIC_ONLY 403 CanvasdropError the canvas is on the Public link rung and the caller is not the owner or an editor RATE_LIMITED 429 CanvasdropError the per-viewer runtime rate limit was hit; the response carries Retry-After Notes: QuotaExceededError keeps the wire code in err.code and the status in err.status . Note the 409: on this route the per-canvas quota is a storage conflict, not the 429 that QUOTA_EXCEEDED means for AI spend. CapabilityDisabledError exposes the server's repair hint as err.hint , and err.message is that hint when the server sends one. On a Public link canvas every primitive is refused for non-owners; only the owner and the canvas's editors reach canvasdrop.files there. try { const f = await canvasdrop. files . upload (file); img. src = f. url ; } catch (err) { if (err. code === \"FILE_TOO_LARGE\" ) showToast ( \"Keep files under 25 MiB\" ); else if (err. code === \"QUOTA_EXCEEDED\" ) showToast ( \"This canvas is out of file storage\" ); else if (err. code === \"CAPABILITY_DISABLED\" ) showToast ( \"File storage is off for this canvas\" ); else throw err; } See error codes for the full list, and the Runtime API for the HTTP endpoints under /v1/c/{slug}/files that these methods call."
  },
  {
    "path": "sdk/identity",
    "section": "Browser SDK",
    "title": "Identity",
    "html": "<h1 id=\"identity\">Identity</h1>\n<p>Know who is looking at your canvas. This page is the reference for\n<code>canvasdrop.me()</code>, the identity primitive on the <code>canvasdrop</code> global that\n<code>&lt;script src=\"/sdk/v1.js\"&gt;</code> defines in every canvas. <code>me()</code> returns the\nsigned-in viewer, resolved from the server-side session: the page never handles\na token, and a viewer cannot claim to be someone else. By the end you can greet\nthe viewer by name, key shared data per person, and handle every error <code>me()</code>\nreturns.</p>\n<p>The canvas needs <strong>Enable backend</strong> on in its <strong>Backend</strong> tab. Identity has no\ntoggle of its own: it is available whenever the backend is on (see\n<a href=\"/docs/authoring/capabilities\">Capabilities</a>).</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">const</span> me = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-title function_\">me</span>();\n<span class=\"hljs-comment\">// {</span>\n<span class=\"hljs-comment\">//   id: \"0190a3f2-7c4e-7a1b-9d2f-3c5e6a7b8c9d\",</span>\n<span class=\"hljs-comment\">//   email: \"someone@example.com\",</span>\n<span class=\"hljs-comment\">//   name: \"Alex Rivera\",</span>\n<span class=\"hljs-comment\">//   avatarUrl: null,</span>\n<span class=\"hljs-comment\">//   kind: \"member\"</span>\n<span class=\"hljs-comment\">// }</span>\n<span class=\"hljs-variable language_\">document</span>.<span class=\"hljs-title function_\">querySelector</span>(<span class=\"hljs-string\">\"#greeting\"</span>).<span class=\"hljs-property\">textContent</span> = <span class=\"hljs-string\">`Hi, <span class=\"hljs-subst\">${me.name}</span>`</span>;</code></pre>\n<p>Call it once per page load and keep the result. The runtime API is rate-limited\nper viewer per canvas (120 requests a minute by default, one budget shared by\n<code>me()</code>, <code>kv</code>, and <code>files</code>; <code>ai</code> has its own), so a <code>me()</code> on every render spends\nbudget for nothing.</p>\n<h2 id=\"signature\">Signature</h2>\n<pre><code class=\"hljs language-ts\"><span class=\"hljs-title function_\">me</span>(): <span class=\"hljs-title class_\">Promise</span>&lt;<span class=\"hljs-title class_\">Me</span>&gt;\n\n<span class=\"hljs-keyword\">interface</span> <span class=\"hljs-title class_\">Me</span> {\n  <span class=\"hljs-attr\">id</span>: <span class=\"hljs-built_in\">string</span>;\n  <span class=\"hljs-attr\">email</span>: <span class=\"hljs-built_in\">string</span>;\n  <span class=\"hljs-attr\">name</span>: <span class=\"hljs-built_in\">string</span>;\n  <span class=\"hljs-attr\">avatarUrl</span>: <span class=\"hljs-built_in\">string</span> | <span class=\"hljs-literal\">null</span>;\n  <span class=\"hljs-attr\">kind</span>: <span class=\"hljs-string\">\"member\"</span> | <span class=\"hljs-string\">\"guest\"</span>;\n}</code></pre>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>id</code></td>\n<td><code>string</code></td>\n<td>Stable per-user id. Key per-user data on this, never on <code>email</code>.</td>\n</tr>\n<tr>\n<td><code>email</code></td>\n<td><code>string</code></td>\n<td>The viewer's email.</td>\n</tr>\n<tr>\n<td><code>name</code></td>\n<td><code>string</code></td>\n<td>Display name.</td>\n</tr>\n<tr>\n<td><code>avatarUrl</code></td>\n<td><code>string | null</code></td>\n<td>Avatar URL, or <code>null</code> when the identity provider gives none.</td>\n</tr>\n<tr>\n<td><code>kind</code></td>\n<td><code>\"member\" | \"guest\"</code></td>\n<td><code>\"member\"</code> for a signed-in org user. <code>\"guest\"</code> is retained only for legacy guest sessions; see below. There is no <code>\"anonymous\"</code>: a signed-out visitor never reaches the runtime API.</td>\n</tr>\n</tbody></table>\n<p>The projection is deliberately minimal. It carries no admin flag and no org\nmembership, so canvas code cannot tell an admin from any other member. That\ninformation stays on the dashboard side.</p>\n<h2 id=\"where-the-identity-comes-from\">Where the identity comes from</h2>\n<p><code>me()</code> calls <code>GET {base}/v1/c/{slug}/me</code> with the viewer's session cookie\n(<code>credentials: \"include\"</code>). The SDK works out the slug and the API origin from\n<code>window.location</code>; there is nothing to configure.</p>\n<table>\n<thead>\n<tr>\n<th>URL mode</th>\n<th>Canvas page</th>\n<th><code>me()</code> request</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>path</code></td>\n<td><code>{base}/c/{slug}/</code></td>\n<td><code>{base}/v1/c/{slug}/me</code> (same origin)</td>\n</tr>\n<tr>\n<td><code>subdomain</code></td>\n<td><code>https://{slug}.canvases.example.com/</code></td>\n<td><code>https://canvases.example.com/v1/c/{slug}/me</code> (the base host, with credentialed CORS)</td>\n</tr>\n</tbody></table>\n<p>The server resolves the user from the session and returns the projection above.\nNothing in the page identifies the viewer, so nothing in the page can be edited\nto impersonate someone.</p>\n<p>The same server-side <code>id</code> is what scopes the <a href=\"/docs/sdk/kv\">per-viewer KV\nnamespace</a> (<code>canvasdrop.kv.user</code>) and what appears as <code>from.id</code>\non <a href=\"/docs/sdk/realtime\">realtime</a> messages and in presence lists. You never pass\nit; the server already knows it.</p>\n<h3 id=\"legacy-guest-sessions\">Legacy guest sessions</h3>\n<p>People you add under <strong>People and teams with direct access</strong> sign in through the org's normal\nlogin and arrive as <code>\"member\"</code>, so on a current instance that is the only\n<code>kind</code> you will see. <code>\"guest\"</code> remains for guest sessions retained from older\ninstances. A guest's <code>id</code> is namespaced <code>guest:&lt;inviteId&gt;</code> so it never collides\nwith an org user id; its <code>name</code> is its email and its <code>avatarUrl</code> is <code>null</code>.\nGuests hold no org capabilities, and AI for guests has its own per-canvas switch\n(see <a href=\"/docs/sdk/ai\">AI</a>).</p>\n<h2 id=\"errors\">Errors</h2>\n<p><code>me()</code> rejects with a <code>CanvasdropError</code> (or a subclass); branch on <code>err.code</code>.\nThe codes you will actually meet:</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Status</th>\n<th>When</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CAPABILITY_DISABLED</code></td>\n<td>403</td>\n<td>The canvas's backend is off. <code>err.hint</code> names the switch: the dashboard Backend tab, the <code>set_capabilities</code> MCP tool, or <code>PATCH /api/canvases/:id/capabilities</code>. Thrown as <code>CapabilityDisabledError</code>.</td>\n</tr>\n<tr>\n<td><code>STATIC_ONLY</code></td>\n<td>403</td>\n<td>The canvas is at the Public link rung. Every backend primitive, <code>me()</code> included, is refused for anyone who is not the owner or an editor, signed in or not.</td>\n</tr>\n<tr>\n<td><code>RATE_LIMITED</code></td>\n<td>429</td>\n<td>Too many runtime-API calls from this viewer on this canvas within the last minute. Back off and retry. A plain <code>CanvasdropError</code>, not a <code>QuotaExceededError</code>.</td>\n</tr>\n<tr>\n<td><code>NOT_AUTHENTICATED</code></td>\n<td>401</td>\n<td>No session. Rare in practice: a viewer who reached the canvas has already signed in. In <code>proxy</code> and <code>dev</code> auth modes the gateway answers 401; in <code>oidc</code> mode it redirects to login instead. Thrown as <code>NotAuthenticatedError</code>.</td>\n</tr>\n</tbody></table>\n<p>Access is re-checked on every call, so <code>me()</code> can also fail later in a session\nwhen the viewer's access changed after the page loaded: <code>PASSWORD_REQUIRED</code>\n(403) when the password gate must be passed again, <code>DISABLED</code> (403) when an\nadmin disabled the canvas, and a <code>NotFoundError</code> (404) when the share expired or\nwas revoked or the canvas was archived. A page reload sends the viewer back\nthrough the normal entry flow.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">try</span> {\n  <span class=\"hljs-keyword\">const</span> me = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-title function_\">me</span>();\n  <span class=\"hljs-title function_\">greet</span>(me.<span class=\"hljs-property\">name</span>);\n} <span class=\"hljs-keyword\">catch</span> (err) {\n  <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"CAPABILITY_DISABLED\"</span>) {\n    <span class=\"hljs-comment\">// backend is off for this canvas; err.hint says what to switch on</span>\n  } <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"STATIC_ONLY\"</span>) {\n    <span class=\"hljs-comment\">// public-link canvas: static for everyone but the owner and editors</span>\n  } <span class=\"hljs-keyword\">else</span> {\n    <span class=\"hljs-keyword\">throw</span> err;\n  }\n}</code></pre>\n<p>The error classes are also exported by <code>@canvas-drop/sdk</code> for <code>instanceof</code>\nchecks; the <a href=\"/docs/api/errors\">error codes reference</a> lists every code.</p>\n<h2 id=\"per-user-data-without-plumbing\">Per-user data without plumbing</h2>\n<p>Most canvases do not need <code>me().id</code> at all. <code>canvasdrop.kv.user</code> scopes every\nkey to the current viewer on the server, so per-person state is\n<code>await canvasdrop.kv.user.set(\"draft\", text)</code> with no id in sight. Reach for\n<code>me()</code> when you want to show the viewer's name or avatar, or when the shared\nnamespace needs a per-user key (<code>votes:${me.id}</code>) that other viewers can read.</p>\n",
    "headings": [
      {
        "id": "signature",
        "text": "Signature",
        "level": 2
      },
      {
        "id": "where-the-identity-comes-from",
        "text": "Where the identity comes from",
        "level": 2
      },
      {
        "id": "legacy-guest-sessions",
        "text": "Legacy guest sessions",
        "level": 3
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      },
      {
        "id": "per-user-data-without-plumbing",
        "text": "Per-user data without plumbing",
        "level": 2
      }
    ],
    "text": "Identity Know who is looking at your canvas. This page is the reference for canvasdrop.me() , the identity primitive on the canvasdrop global that <script src=\"/sdk/v1.js\"> defines in every canvas. me() returns the signed-in viewer, resolved from the server-side session: the page never handles a token, and a viewer cannot claim to be someone else. By the end you can greet the viewer by name, key shared data per person, and handle every error me() returns. The canvas needs Enable backend on in its Backend tab. Identity has no toggle of its own: it is available whenever the backend is on (see Capabilities ). const me = await canvasdrop. me (); // { // id: \"0190a3f2-7c4e-7a1b-9d2f-3c5e6a7b8c9d\", // email: \"someone@example.com\", // name: \"Alex Rivera\", // avatarUrl: null, // kind: \"member\" // } document . querySelector ( \"#greeting\" ). textContent = `Hi, ${me.name} ` ; Call it once per page load and keep the result. The runtime API is rate-limited per viewer per canvas (120 requests a minute by default, one budget shared by me() , kv , and files ; ai has its own), so a me() on every render spends budget for nothing. Signature me (): Promise < Me > interface Me { id : string ; email : string ; name : string ; avatarUrl : string | null ; kind : \"member\" | \"guest\" ; } Field Type Notes id string Stable per-user id. Key per-user data on this, never on email . email string The viewer's email. name string Display name. avatarUrl string | null Avatar URL, or null when the identity provider gives none. kind \"member\" | \"guest\" \"member\" for a signed-in org user. \"guest\" is retained only for legacy guest sessions; see below. There is no \"anonymous\" : a signed-out visitor never reaches the runtime API. The projection is deliberately minimal. It carries no admin flag and no org membership, so canvas code cannot tell an admin from any other member. That information stays on the dashboard side. Where the identity comes from me() calls GET {base}/v1/c/{slug}/me with the viewer's session cookie ( credentials: \"include\" ). The SDK works out the slug and the API origin from window.location ; there is nothing to configure. URL mode Canvas page me() request path {base}/c/{slug}/ {base}/v1/c/{slug}/me (same origin) subdomain https://{slug}.canvases.example.com/ https://canvases.example.com/v1/c/{slug}/me (the base host, with credentialed CORS) The server resolves the user from the session and returns the projection above. Nothing in the page identifies the viewer, so nothing in the page can be edited to impersonate someone. The same server-side id is what scopes the per-viewer KV namespace ( canvasdrop.kv.user ) and what appears as from.id on realtime messages and in presence lists. You never pass it; the server already knows it. Legacy guest sessions People you add under People and teams with direct access sign in through the org's normal login and arrive as \"member\" , so on a current instance that is the only kind you will see. \"guest\" remains for guest sessions retained from older instances. A guest's id is namespaced guest:<inviteId> so it never collides with an org user id; its name is its email and its avatarUrl is null . Guests hold no org capabilities, and AI for guests has its own per-canvas switch (see AI ). Errors me() rejects with a CanvasdropError (or a subclass); branch on err.code . The codes you will actually meet: Code Status When CAPABILITY_DISABLED 403 The canvas's backend is off. err.hint names the switch: the dashboard Backend tab, the set_capabilities MCP tool, or PATCH /api/canvases/:id/capabilities . Thrown as CapabilityDisabledError . STATIC_ONLY 403 The canvas is at the Public link rung. Every backend primitive, me() included, is refused for anyone who is not the owner or an editor, signed in or not. RATE_LIMITED 429 Too many runtime-API calls from this viewer on this canvas within the last minute. Back off and retry. A plain CanvasdropError , not a QuotaExceededError . NOT_AUTHENTICATED 401 No session. Rare in practice: a viewer who reached the canvas has already signed in. In proxy and dev auth modes the gateway answers 401; in oidc mode it redirects to login instead. Thrown as NotAuthenticatedError . Access is re-checked on every call, so me() can also fail later in a session when the viewer's access changed after the page loaded: PASSWORD_REQUIRED (403) when the password gate must be passed again, DISABLED (403) when an admin disabled the canvas, and a NotFoundError (404) when the share expired or was revoked or the canvas was archived. A page reload sends the viewer back through the normal entry flow. try { const me = await canvasdrop. me (); greet (me. name ); } catch (err) { if (err. code === \"CAPABILITY_DISABLED\" ) { // backend is off for this canvas; err.hint says what to switch on } else if (err. code === \"STATIC_ONLY\" ) { // public-link canvas: static for everyone but the owner and editors } else { throw err; } } The error classes are also exported by @canvas-drop/sdk for instanceof checks; the error codes reference lists every code. Per-user data without plumbing Most canvases do not need me().id at all. canvasdrop.kv.user scopes every key to the current viewer on the server, so per-person state is await canvasdrop.kv.user.set(\"draft\", text) with no id in sight. Reach for me() when you want to show the viewer's name or avatar, or when the shared namespace needs a per-user key ( votes:${me.id} ) that other viewers can read."
  },
  {
    "path": "sdk/ai",
    "section": "Browser SDK",
    "title": "AI",
    "html": "<h1 id=\"ai\">AI</h1>\n<p>Call a language model from a static canvas without holding a provider key. This\npage is the reference for <code>canvasdrop.ai</code>, the AI primitive on the <code>canvasdrop</code>\nglobal that <code>&lt;script src=\"/sdk/v1.js\"&gt;</code> defines in every canvas. The page posts\nyour messages to the server, the server runs the call with the instance's key\nand streams the reply back, and the key never reaches the browser. By the end\nyou can await a reply, render it as it streams, read usage and cost, stay inside\nthe model allowlist and spend caps, and handle every error AI returns.</p>\n<p>The canvas needs <strong>Enable backend</strong> on and the <strong>AI</strong> toggle on (it is\npre-enabled) in its <strong>Backend</strong> tab; see\n<a href=\"/docs/authoring/capabilities\">Capabilities</a>. The operator must also have\nconfigured a provider key for the instance (<code>CANVAS_DROP_AI_API_KEY</code>, or the\nkey an admin sets in Admin → Settings); without one the capability reads as off.</p>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">src</span>=<span class=\"hljs-string\">\"/sdk/v1.js\"</span>&gt;</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">type</span>=<span class=\"hljs-string\">\"module\"</span>&gt;</span><span class=\"language-javascript\">\n  <span class=\"hljs-keyword\">const</span> { text, usage, cost } = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">ai</span>.<span class=\"hljs-title function_\">chat</span>(\n    [{ <span class=\"hljs-attr\">role</span>: <span class=\"hljs-string\">\"user\"</span>, <span class=\"hljs-attr\">content</span>: <span class=\"hljs-string\">\"Summarise this poll result in one line.\"</span> }],\n    { <span class=\"hljs-attr\">model</span>: <span class=\"hljs-string\">\"claude-haiku-4-5\"</span> },\n  );\n  <span class=\"hljs-comment\">// text:  the model's reply (string)</span>\n  <span class=\"hljs-comment\">// usage: { inputTokens, outputTokens, cacheCreationInputTokens, cacheReadInputTokens }</span>\n  <span class=\"hljs-comment\">// cost:  number (USD)</span>\n</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span></code></pre>\n<p>Two entry points, both backed by <code>POST /v1/c/{slug}/ai/chat</code>:</p>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Returns</th>\n<th>Use when</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>chat(messages, options)</code></td>\n<td><code>Promise&lt;{ text, usage, cost }&gt;</code></td>\n<td>You want the whole reply, plus usage and cost</td>\n</tr>\n<tr>\n<td><code>stream(messages, options)</code></td>\n<td><code>AsyncIterable&lt;string&gt;</code> of text deltas</td>\n<td>You want to render the reply as it is generated</td>\n</tr>\n</tbody></table>\n<p><code>messages</code> must hold at least one message. <code>options.model</code> is <strong>required</strong> and\nmust be on the instance's <a href=\"#models\">model allowlist</a>. Calls go to Anthropic;\nthere is no per-call provider choice.</p>\n<h2 id=\"chat\">Chat</h2>\n<p><code>chat(messages, options)</code> collects every text delta and resolves once with\n<code>{ text, usage, cost }</code>. It rejects if the model is not allowlisted, a quota or\nrate limit is hit, the provider fails, or the stream ends early; see\n<a href=\"#errors\">Errors</a>.</p>\n<p><code>cost</code> is the call's price in USD, computed on the server from its per-model\nprice table and the token counts the provider reported. It is the same number\nthe spend quotas sum. If the provider's usage report does not arrive within 5 s\nof the stream ending, the call is recorded, and reported, with <code>0</code> tokens and\n<code>0</code> cost.</p>\n<h2 id=\"stream\">Stream</h2>\n<p>Render the reply as it is generated:</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">const</span> output = <span class=\"hljs-variable language_\">document</span>.<span class=\"hljs-title function_\">querySelector</span>(<span class=\"hljs-string\">\"#answer\"</span>);\n<span class=\"hljs-keyword\">for</span> <span class=\"hljs-title function_\">await</span> (<span class=\"hljs-keyword\">const</span> delta <span class=\"hljs-keyword\">of</span> canvasdrop.<span class=\"hljs-property\">ai</span>.<span class=\"hljs-title function_\">stream</span>(messages, { <span class=\"hljs-attr\">model</span>: <span class=\"hljs-string\">\"claude-haiku-4-5\"</span> })) {\n  output.<span class=\"hljs-property\">textContent</span> += delta;\n}</code></pre>\n<p><code>stream()</code> yields only text; usage and cost are not available from it. Use\n<code>chat()</code> when you need them.</p>\n<p>There is no abort option. Breaking out of the <code>for await</code> loop stops delivery\nto your code but does not cancel the request: the server keeps the call running\nto completion. Only a dropped connection (the viewer closes the tab or navigates\naway) reaches the server as an abort, which cancels the upstream call. Either\nway every call is metered exactly once, including one abandoned mid-stream, so\nit still counts against the quotas.</p>\n<h2 id=\"messages-and-options\">Messages and options</h2>\n<pre><code class=\"hljs language-ts\"><span class=\"hljs-keyword\">type</span> <span class=\"hljs-title class_\">AiMessage</span> = { <span class=\"hljs-attr\">role</span>: <span class=\"hljs-string\">\"user\"</span> | <span class=\"hljs-string\">\"assistant\"</span>; <span class=\"hljs-attr\">content</span>: <span class=\"hljs-built_in\">string</span> };\n\n<span class=\"hljs-keyword\">type</span> <span class=\"hljs-title class_\">AiChatOptions</span> = {\n  <span class=\"hljs-attr\">model</span>: <span class=\"hljs-built_in\">string</span>;       <span class=\"hljs-comment\">// required; must be allowlisted</span>\n  <span class=\"hljs-attr\">system</span>?: <span class=\"hljs-built_in\">string</span>;     <span class=\"hljs-comment\">// system prompt (not a message role)</span>\n  <span class=\"hljs-attr\">maxTokens</span>?: <span class=\"hljs-built_in\">number</span>;  <span class=\"hljs-comment\">// positive integer; default 1024, clamped to 8192</span>\n};\n\n<span class=\"hljs-keyword\">type</span> <span class=\"hljs-title class_\">AiUsage</span> = {\n  <span class=\"hljs-attr\">inputTokens</span>: <span class=\"hljs-built_in\">number</span>;                <span class=\"hljs-comment\">// total prompt tokens, cached or not</span>\n  <span class=\"hljs-attr\">outputTokens</span>: <span class=\"hljs-built_in\">number</span>;\n  <span class=\"hljs-attr\">cacheCreationInputTokens</span>: <span class=\"hljs-built_in\">number</span>;   <span class=\"hljs-comment\">// prompt-cache writes, else 0</span>\n  <span class=\"hljs-attr\">cacheReadInputTokens</span>: <span class=\"hljs-built_in\">number</span>;       <span class=\"hljs-comment\">// prompt-cache reads, else 0</span>\n};\n\n<span class=\"hljs-keyword\">type</span> <span class=\"hljs-title class_\">AiResult</span> = { <span class=\"hljs-attr\">text</span>: <span class=\"hljs-built_in\">string</span>; <span class=\"hljs-attr\">usage</span>: <span class=\"hljs-title class_\">AiUsage</span>; <span class=\"hljs-attr\">cost</span>: <span class=\"hljs-built_in\">number</span> };</code></pre>\n<p>There is no <code>\"system\"</code> message role. Pass the system prompt via\n<code>options.system</code>:</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">ai</span>.<span class=\"hljs-title function_\">chat</span>(messages, {\n  <span class=\"hljs-attr\">model</span>: <span class=\"hljs-string\">\"claude-sonnet-4-6\"</span>,\n  <span class=\"hljs-attr\">system</span>: <span class=\"hljs-string\">\"You are a terse assistant. Answer in one sentence.\"</span>,\n  <span class=\"hljs-attr\">maxTokens</span>: <span class=\"hljs-number\">256</span>,\n});</code></pre>\n<p><code>maxTokens</code> caps the model's output. Omit it for <code>1024</code>; a larger value is\nclamped to <code>8192</code>; a zero, negative, or fractional value rejects with\n<code>INVALID_BODY</code> (400). There are no options for temperature, tools, images, JSON\nmode, or stop sequences.</p>\n<p>The request body (messages plus system prompt, as JSON) must stay under\n256 KiB; beyond that the server rejects with <code>BODY_TOO_LARGE</code> (413). Trim long\nconversations client-side rather than sending the whole history every turn.</p>\n<h2 id=\"models\">Models</h2>\n<p>The operator sets the allowlist with <code>CANVAS_DROP_AI_MODELS</code> (a CSV; default\n<code>claude-haiku-4-5,claude-sonnet-4-6,claude-opus-4-8</code>), and an admin can override\nit at runtime in Admin → Settings. Use the exact model id as configured. Asking\nfor a model that is not on the list, or one that is listed but has no entry in\nthe server's price table, rejects with <code>MODEL_NOT_ALLOWED</code> (403). The second\ncase is deliberate: an unpriced model would cost <code>0</code> and the spend quotas could\nnever see it. The three default models are priced. Ask your administrator to\nadd a model if you need it.</p>\n<h2 id=\"prompt-caching-and-cost\">Prompt caching and cost</h2>\n<p>Prompt caching is automatic and best-effort. The server marks two stable prefix\npoints for Anthropic's ephemeral prompt cache: the system prompt, when it is\nnon-empty, and the last non-empty message before the newest <code>user</code> turn.\nRepeating the same system prompt and conversation history across turns is what\nmakes the cache hit.</p>\n<p><code>usage.inputTokens</code> is the total prompt size; <code>cacheCreationInputTokens</code> and\n<code>cacheReadInputTokens</code> say how many of those tokens wrote to or were read from\nthe cache. Both are <code>0</code> when the provider reported no cache activity for the\ncall. The remainder (<code>inputTokens</code> minus both cache counts) is billed at the\nmodel's input rate; cache writes at 1.25x that rate and cache reads at 0.1x, so\na cached follow-up turn costs less and adds less to the quota windows.</p>\n<h2 id=\"limits\">Limits</h2>\n<table>\n<thead>\n<tr>\n<th>Limit</th>\n<th>Value</th>\n<th>Error when exceeded</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>AI calls per viewer per minute, across all canvases</td>\n<td>10 (<code>CANVAS_DROP_RATELIMIT_AI_PER_MIN</code>)</td>\n<td><code>RATE_LIMITED</code> (429, with <code>Retry-After</code>)</td>\n</tr>\n<tr>\n<td>Spend per viewer per UTC day</td>\n<td>5 USD (<code>CANVAS_DROP_AI_USER_DAILY_USD</code>)</td>\n<td><code>QUOTA_EXCEEDED</code> (429)</td>\n</tr>\n<tr>\n<td>Spend per canvas per UTC month</td>\n<td>50 USD (<code>CANVAS_DROP_AI_CANVAS_MONTHLY_USD</code>)</td>\n<td><code>QUOTA_EXCEEDED</code> (429)</td>\n</tr>\n<tr>\n<td>Guest AI spend per canvas per UTC month</td>\n<td>owner-set; <code>0</code> (the default) means no guest-specific cap</td>\n<td><code>GUEST_AI_CAP</code> (429)</td>\n</tr>\n<tr>\n<td>Output tokens per call</td>\n<td>1024 default, 8192 maximum</td>\n<td>clamped, never an error</td>\n</tr>\n<tr>\n<td>Request body</td>\n<td>256 KiB</td>\n<td><code>BODY_TOO_LARGE</code> (413)</td>\n</tr>\n</tbody></table>\n<p>The two USD spend caps are instance defaults an admin can change at runtime. A\ncall is refused when spend in the window already meets the cap; a call that\nstarts under the cap runs to completion even if it crosses it. The viewer's\ndaily window is checked first. The HTTP response says which window closed\n(<code>scope: \"user_daily\"</code> or <code>\"canvas_monthly\"</code>); the SDK error carries only the\ncode and status. Windows reset at the UTC day and month boundaries.</p>\n<p>Retained legacy guest sessions have their own gates. AI is off for them unless\nthe owner opted the canvas in (<code>GUEST_AI_DISABLED</code>, 403). The owner can also set\na per-canvas monthly USD ceiling for guest calls; once the canvas's total spend\nthis month reaches it, guest calls are refused with <code>GUEST_AI_CAP</code> (429). Both\ncontrols live in the canvas's <strong>Share</strong> tab under <strong>AI for added people</strong> (shown\nwhen a legacy guest is on the people-and-teams list). Only the owner can change them; an editor sees\nthem read-only. People added through <strong>Add person</strong> sign in as members and are\nnot affected by either gate.</p>\n<h2 id=\"errors\">Errors</h2>\n<p>Every method rejects with a <code>CanvasdropError</code> subclass; branch on <code>err.code</code>, or\ncatch the subclass you care about.</p>\n<ul>\n<li><code>QUOTA_EXCEEDED</code> and <code>GUEST_AI_CAP</code> throw <code>QuotaExceededError</code> with the wire\ncode in <code>err.code</code> and <code>429</code> in <code>err.status</code>. <code>BODY_TOO_LARGE</code> also arrives as\na <code>QuotaExceededError</code>, with <code>413</code> in <code>err.status</code>.</li>\n<li><code>MODEL_NOT_ALLOWED</code> (403), <code>GUEST_AI_DISABLED</code> (403), <code>INVALID_BODY</code> (400),\nand <code>RATE_LIMITED</code> (429) throw a plain <code>CanvasdropError</code> with that <code>code</code>.</li>\n<li>AI switched off for the canvas, the canvas backend off, or no provider key on\nthe instance throws <code>CapabilityDisabledError</code> (<code>code: \"CAPABILITY_DISABLED\"</code>,\n403); <code>err.hint</code> names the gate that failed.</li>\n<li>On a Public link canvas, viewers other than the owner and editors get\n<code>STATIC_ONLY</code> (403): public canvases are static-only and every primitive is\nrefused.</li>\n<li>A provider failure throws <code>AI_UPSTREAM_ERROR</code> (502). The server retries a\ntransient provider error up to twice before the first byte streams; a failure\nafter that arrives mid-stream. If the stream ends before its terminal frame,\nboth <code>chat</code> and <code>stream</code> throw <code>AI_STREAM_TRUNCATED</code> (502); an unparseable\nframe throws <code>MALFORMED_FRAME</code> (502). Text already yielded by <code>stream()</code>\nbefore the error stays with you.</li>\n</ul>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">try</span> {\n  <span class=\"hljs-keyword\">const</span> { text } = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">ai</span>.<span class=\"hljs-title function_\">chat</span>(messages, { <span class=\"hljs-attr\">model</span>: <span class=\"hljs-string\">\"claude-haiku-4-5\"</span> });\n  <span class=\"hljs-title function_\">show</span>(text);\n} <span class=\"hljs-keyword\">catch</span> (err) {\n  <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"QUOTA_EXCEEDED\"</span>) <span class=\"hljs-title function_\">showBanner</span>(<span class=\"hljs-string\">\"AI budget used up for now\"</span>);\n  <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"RATE_LIMITED\"</span>) <span class=\"hljs-title function_\">showBanner</span>(<span class=\"hljs-string\">\"Slow down a little\"</span>);\n  <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">throw</span> err;\n}</code></pre>\n<p>See <a href=\"/docs/api/errors\">error codes</a> for the full list, and the\n<a href=\"/docs/api/runtime-api#ai\">Runtime API</a> for the <code>POST /v1/c/{slug}/ai/chat</code>\nendpoint and its SSE wire format.</p>\n",
    "headings": [
      {
        "id": "chat",
        "text": "Chat",
        "level": 2
      },
      {
        "id": "stream",
        "text": "Stream",
        "level": 2
      },
      {
        "id": "messages-and-options",
        "text": "Messages and options",
        "level": 2
      },
      {
        "id": "models",
        "text": "Models",
        "level": 2
      },
      {
        "id": "prompt-caching-and-cost",
        "text": "Prompt caching and cost",
        "level": 2
      },
      {
        "id": "limits",
        "text": "Limits",
        "level": 2
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      }
    ],
    "text": "AI Call a language model from a static canvas without holding a provider key. This page is the reference for canvasdrop.ai , the AI primitive on the canvasdrop global that <script src=\"/sdk/v1.js\"> defines in every canvas. The page posts your messages to the server, the server runs the call with the instance's key and streams the reply back, and the key never reaches the browser. By the end you can await a reply, render it as it streams, read usage and cost, stay inside the model allowlist and spend caps, and handle every error AI returns. The canvas needs Enable backend on and the AI toggle on (it is pre-enabled) in its Backend tab; see Capabilities . The operator must also have configured a provider key for the instance ( CANVAS_DROP_AI_API_KEY , or the key an admin sets in Admin → Settings); without one the capability reads as off. < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const { text, usage, cost } = await canvasdrop. ai . chat ( [{ role : \"user\" , content : \"Summarise this poll result in one line.\" }], { model : \"claude-haiku-4-5\" }, ); // text: the model's reply (string) // usage: { inputTokens, outputTokens, cacheCreationInputTokens, cacheReadInputTokens } // cost: number (USD) </ script > Two entry points, both backed by POST /v1/c/{slug}/ai/chat : Method Returns Use when chat(messages, options) Promise<{ text, usage, cost }> You want the whole reply, plus usage and cost stream(messages, options) AsyncIterable<string> of text deltas You want to render the reply as it is generated messages must hold at least one message. options.model is required and must be on the instance's model allowlist . Calls go to Anthropic; there is no per-call provider choice. Chat chat(messages, options) collects every text delta and resolves once with { text, usage, cost } . It rejects if the model is not allowlisted, a quota or rate limit is hit, the provider fails, or the stream ends early; see Errors . cost is the call's price in USD, computed on the server from its per-model price table and the token counts the provider reported. It is the same number the spend quotas sum. If the provider's usage report does not arrive within 5 s of the stream ending, the call is recorded, and reported, with 0 tokens and 0 cost. Stream Render the reply as it is generated: const output = document . querySelector ( \"#answer\" ); for await ( const delta of canvasdrop. ai . stream (messages, { model : \"claude-haiku-4-5\" })) { output. textContent += delta; } stream() yields only text; usage and cost are not available from it. Use chat() when you need them. There is no abort option. Breaking out of the for await loop stops delivery to your code but does not cancel the request: the server keeps the call running to completion. Only a dropped connection (the viewer closes the tab or navigates away) reaches the server as an abort, which cancels the upstream call. Either way every call is metered exactly once, including one abandoned mid-stream, so it still counts against the quotas. Messages and options type AiMessage = { role : \"user\" | \"assistant\" ; content : string }; type AiChatOptions = { model : string ; // required; must be allowlisted system ?: string ; // system prompt (not a message role) maxTokens ?: number ; // positive integer; default 1024, clamped to 8192 }; type AiUsage = { inputTokens : number ; // total prompt tokens, cached or not outputTokens : number ; cacheCreationInputTokens : number ; // prompt-cache writes, else 0 cacheReadInputTokens : number ; // prompt-cache reads, else 0 }; type AiResult = { text : string ; usage : AiUsage ; cost : number }; There is no \"system\" message role. Pass the system prompt via options.system : await canvasdrop. ai . chat (messages, { model : \"claude-sonnet-4-6\" , system : \"You are a terse assistant. Answer in one sentence.\" , maxTokens : 256 , }); maxTokens caps the model's output. Omit it for 1024 ; a larger value is clamped to 8192 ; a zero, negative, or fractional value rejects with INVALID_BODY (400). There are no options for temperature, tools, images, JSON mode, or stop sequences. The request body (messages plus system prompt, as JSON) must stay under 256 KiB; beyond that the server rejects with BODY_TOO_LARGE (413). Trim long conversations client-side rather than sending the whole history every turn. Models The operator sets the allowlist with CANVAS_DROP_AI_MODELS (a CSV; default claude-haiku-4-5,claude-sonnet-4-6,claude-opus-4-8 ), and an admin can override it at runtime in Admin → Settings. Use the exact model id as configured. Asking for a model that is not on the list, or one that is listed but has no entry in the server's price table, rejects with MODEL_NOT_ALLOWED (403). The second case is deliberate: an unpriced model would cost 0 and the spend quotas could never see it. The three default models are priced. Ask your administrator to add a model if you need it. Prompt caching and cost Prompt caching is automatic and best-effort. The server marks two stable prefix points for Anthropic's ephemeral prompt cache: the system prompt, when it is non-empty, and the last non-empty message before the newest user turn. Repeating the same system prompt and conversation history across turns is what makes the cache hit. usage.inputTokens is the total prompt size; cacheCreationInputTokens and cacheReadInputTokens say how many of those tokens wrote to or were read from the cache. Both are 0 when the provider reported no cache activity for the call. The remainder ( inputTokens minus both cache counts) is billed at the model's input rate; cache writes at 1.25x that rate and cache reads at 0.1x, so a cached follow-up turn costs less and adds less to the quota windows. Limits Limit Value Error when exceeded AI calls per viewer per minute, across all canvases 10 ( CANVAS_DROP_RATELIMIT_AI_PER_MIN ) RATE_LIMITED (429, with Retry-After ) Spend per viewer per UTC day 5 USD ( CANVAS_DROP_AI_USER_DAILY_USD ) QUOTA_EXCEEDED (429) Spend per canvas per UTC month 50 USD ( CANVAS_DROP_AI_CANVAS_MONTHLY_USD ) QUOTA_EXCEEDED (429) Guest AI spend per canvas per UTC month owner-set; 0 (the default) means no guest-specific cap GUEST_AI_CAP (429) Output tokens per call 1024 default, 8192 maximum clamped, never an error Request body 256 KiB BODY_TOO_LARGE (413) The two USD spend caps are instance defaults an admin can change at runtime. A call is refused when spend in the window already meets the cap; a call that starts under the cap runs to completion even if it crosses it. The viewer's daily window is checked first. The HTTP response says which window closed ( scope: \"user_daily\" or \"canvas_monthly\" ); the SDK error carries only the code and status. Windows reset at the UTC day and month boundaries. Retained legacy guest sessions have their own gates. AI is off for them unless the owner opted the canvas in ( GUEST_AI_DISABLED , 403). The owner can also set a per-canvas monthly USD ceiling for guest calls; once the canvas's total spend this month reaches it, guest calls are refused with GUEST_AI_CAP (429). Both controls live in the canvas's Share tab under AI for added people (shown when a legacy guest is on the people-and-teams list). Only the owner can change them; an editor sees them read-only. People added through Add person sign in as members and are not affected by either gate. Errors Every method rejects with a CanvasdropError subclass; branch on err.code , or catch the subclass you care about. QUOTA_EXCEEDED and GUEST_AI_CAP throw QuotaExceededError with the wire code in err.code and 429 in err.status . BODY_TOO_LARGE also arrives as a QuotaExceededError , with 413 in err.status . MODEL_NOT_ALLOWED (403), GUEST_AI_DISABLED (403), INVALID_BODY (400), and RATE_LIMITED (429) throw a plain CanvasdropError with that code . AI switched off for the canvas, the canvas backend off, or no provider key on the instance throws CapabilityDisabledError ( code: \"CAPABILITY_DISABLED\" , 403); err.hint names the gate that failed. On a Public link canvas, viewers other than the owner and editors get STATIC_ONLY (403): public canvases are static-only and every primitive is refused. A provider failure throws AI_UPSTREAM_ERROR (502). The server retries a transient provider error up to twice before the first byte streams; a failure after that arrives mid-stream. If the stream ends before its terminal frame, both chat and stream throw AI_STREAM_TRUNCATED (502); an unparseable frame throws MALFORMED_FRAME (502). Text already yielded by stream() before the error stays with you. try { const { text } = await canvasdrop. ai . chat (messages, { model : \"claude-haiku-4-5\" }); show (text); } catch (err) { if (err. code === \"QUOTA_EXCEEDED\" ) showBanner ( \"AI budget used up for now\" ); else if (err. code === \"RATE_LIMITED\" ) showBanner ( \"Slow down a little\" ); else throw err; } See error codes for the full list, and the Runtime API for the POST /v1/c/{slug}/ai/chat endpoint and its SSE wire format."
  },
  {
    "path": "sdk/realtime",
    "section": "Browser SDK",
    "title": "Realtime",
    "html": "<h1 id=\"realtime\">Realtime</h1>\n<p>Add live cursors, reactions, or a shared room to a canvas with\n<code>canvasdrop.realtime</code>: ephemeral pub/sub plus presence over named channels. By\nthe end of this page you can publish to a channel, receive what others publish,\nread who is present, and handle the three ways a socket ends for good. Messages\nfan out to whoever is subscribed to the channel at that moment and are never\nstored; put anything that must survive a reload in <a href=\"/docs/sdk/kv\">KV</a>.</p>\n<p><code>canvasdrop</code> is already on <code>window</code> in any served canvas (see the\n<a href=\"/docs/sdk/overview\">SDK overview</a>) and connects to the right canvas from its\nURL. The <code>from</code> on every message and every entry in the presence list come from\nthe viewer's server-side session, never from the page, so names are trustworthy.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">const</span> room = canvasdrop.<span class=\"hljs-property\">realtime</span>.<span class=\"hljs-title function_\">channel</span>(<span class=\"hljs-string\">\"room-1\"</span>);\n\n<span class=\"hljs-comment\">// Receive what anyone on the channel publishes, including yourself.</span>\nroom.<span class=\"hljs-title function_\">subscribe</span>(<span class=\"hljs-function\">(<span class=\"hljs-params\">msg</span>) =&gt;</span> {\n  <span class=\"hljs-comment\">// msg = { event, data, from: { id, name } }</span>\n  <span class=\"hljs-variable language_\">console</span>.<span class=\"hljs-title function_\">log</span>(msg.<span class=\"hljs-property\">from</span>.<span class=\"hljs-property\">name</span>, msg.<span class=\"hljs-property\">event</span>, msg.<span class=\"hljs-property\">data</span>);\n});\n\n<span class=\"hljs-comment\">// Broadcast to everyone subscribed right now. Fire-and-forget.</span>\nroom.<span class=\"hljs-title function_\">publish</span>(<span class=\"hljs-string\">\"cursor\"</span>, { <span class=\"hljs-attr\">x</span>: <span class=\"hljs-number\">120</span>, <span class=\"hljs-attr\">y</span>: <span class=\"hljs-number\">48</span> });\n\n<span class=\"hljs-comment\">// Who is in the channel right now.</span>\n<span class=\"hljs-keyword\">const</span> users = <span class=\"hljs-keyword\">await</span> room.<span class=\"hljs-title function_\">presence</span>(); <span class=\"hljs-comment\">// [{ id, name }, ...]</span>\n\nroom.<span class=\"hljs-title function_\">onJoin</span>(<span class=\"hljs-function\">(<span class=\"hljs-params\">user</span>) =&gt;</span> <span class=\"hljs-variable language_\">console</span>.<span class=\"hljs-title function_\">log</span>(user.<span class=\"hljs-property\">name</span>, <span class=\"hljs-string\">\"joined\"</span>));\nroom.<span class=\"hljs-title function_\">onLeave</span>(<span class=\"hljs-function\">(<span class=\"hljs-params\">user</span>) =&gt;</span> <span class=\"hljs-variable language_\">console</span>.<span class=\"hljs-title function_\">log</span>(user.<span class=\"hljs-property\">name</span>, <span class=\"hljs-string\">\"left\"</span>));\n\nroom.<span class=\"hljs-title function_\">unsubscribe</span>(); <span class=\"hljs-comment\">// stop receiving messages; the handle stays usable</span>\nroom.<span class=\"hljs-title function_\">close</span>();       <span class=\"hljs-comment\">// done with this channel; the socket closes when none remain</span></code></pre>\n<h2 id=\"when-realtime-is-on\">When realtime is on</h2>\n<p>Realtime works when all three switches are on. A new canvas ships with the\nbackend off, so the first step is usually turning that on.</p>\n<table>\n<thead>\n<tr>\n<th>Switch</th>\n<th>Default</th>\n<th>Who sets it</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>The canvas's backend (master switch)</td>\n<td>off</td>\n<td>The owner or an editor, in the canvas's Backend tab or with the <code>set_capabilities</code> MCP tool</td>\n</tr>\n<tr>\n<td>The canvas's <code>realtime</code> flag</td>\n<td>on</td>\n<td>Same place</td>\n</tr>\n<tr>\n<td>The instance switch <code>CANVAS_DROP_REALTIME</code></td>\n<td><code>on</code></td>\n<td>The operator, in the server environment. The admin panel shows it read-only.</td>\n</tr>\n</tbody></table>\n<p>When any of the three is off, the server closes the socket with code <code>4403</code>\nand the SDK raises <code>CapabilityDisabledError</code> (see\n<a href=\"#limits-and-errors\">Limits and errors</a>). The rest of the canvas keeps working.\nDetails in <a href=\"/docs/authoring/capabilities\">Capabilities</a>.</p>\n<h2 id=\"channel-methods\"><code>Channel</code> methods</h2>\n<p><code>canvasdrop.realtime.channel(name)</code> returns a <code>Channel</code>. Signatures as declared\nin the SDK:</p>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Signature</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>publish</code></td>\n<td><code>publish(event: string, data: unknown): void</code></td>\n<td>Send <code>{ event, data }</code> to every current subscriber. Returns nothing; there is no delivery receipt. You do not need to be subscribed to publish, but see <a href=\"#how-the-connection-works\">How the connection works</a> for when the socket opens.</td>\n</tr>\n<tr>\n<td><code>subscribe</code></td>\n<td><code>subscribe(handler: (msg: RealtimeMessage) =&gt; void): void</code></td>\n<td>Join the channel and run <code>handler</code> for each message. Calling it again adds another handler; it does not return an unsubscribe function.</td>\n</tr>\n<tr>\n<td><code>unsubscribe</code></td>\n<td><code>unsubscribe(): void</code></td>\n<td>Leave the channel and drop every message handler. <code>onPresence</code>, <code>onJoin</code>, and <code>onLeave</code> handlers stay registered, but no join or leave reaches you until you subscribe again.</td>\n</tr>\n<tr>\n<td><code>presence</code></td>\n<td><code>presence(): Promise&lt;RealtimeUser[]&gt;</code></td>\n<td>Resolve with the users subscribed to the channel right now. Works whether or not you are subscribed.</td>\n</tr>\n<tr>\n<td><code>onPresence</code></td>\n<td><code>onPresence(handler: (users: RealtimeUser[]) =&gt; void): void</code></td>\n<td>Run <code>handler</code> each time the server sends a full roster (see below).</td>\n</tr>\n<tr>\n<td><code>onJoin</code></td>\n<td><code>onJoin(handler: (user: RealtimeUser) =&gt; void): void</code></td>\n<td>Run <code>handler</code> when another user's first connection joins the channel.</td>\n</tr>\n<tr>\n<td><code>onLeave</code></td>\n<td><code>onLeave(handler: (user: RealtimeUser) =&gt; void): void</code></td>\n<td>Run <code>handler</code> when another user's last connection leaves the channel.</td>\n</tr>\n<tr>\n<td><code>close</code></td>\n<td><code>close(): void</code></td>\n<td>Retire this handle: it leaves the channel, and the shared socket closes once no channels remain. <code>publish</code> and <code>presence</code> on a closed handle fail with a <code>CanvasdropError</code> (<code>code: \"CHANNEL_CLOSED\"</code>, <code>status: 0</code>).</td>\n</tr>\n</tbody></table>\n<p>There is no generic <code>on(event, handler)</code>, <code>off</code>, or <code>once</code>; the four listener\nhooks above are the whole surface.</p>\n<pre><code class=\"hljs language-ts\"><span class=\"hljs-keyword\">type</span> <span class=\"hljs-title class_\">RealtimeUser</span> = { <span class=\"hljs-attr\">id</span>: <span class=\"hljs-built_in\">string</span>; <span class=\"hljs-attr\">name</span>: <span class=\"hljs-built_in\">string</span> };\n<span class=\"hljs-keyword\">type</span> <span class=\"hljs-title class_\">RealtimeMessage</span> = { <span class=\"hljs-attr\">event</span>: <span class=\"hljs-built_in\">string</span>; <span class=\"hljs-attr\">data</span>: <span class=\"hljs-built_in\">unknown</span>; <span class=\"hljs-attr\">from</span>: <span class=\"hljs-title class_\">RealtimeUser</span> };</code></pre>\n<h2 id=\"what-presence-and-messages-mean\">What presence and messages mean</h2>\n<ul>\n<li>A message reaches every connection subscribed to the channel, the sender's\nincluded. If you subscribe and publish from the same page, your own handler\nruns for your own messages.</li>\n<li>Presence is the list of distinct users subscribed to the channel. A person\nwith two tabs open appears once.</li>\n<li><code>onJoin</code> fires for the other users when a person's first connection\nsubscribes; <code>onLeave</code> fires when their last connection unsubscribes, closes,\nor drops. You never receive your own join or leave.</li>\n<li><code>onPresence</code> fires when the server sends a roster: right after your\n<code>subscribe</code> (including the re-subscribe after a reconnect) and after every\n<code>presence()</code> call. It does not fire when someone else joins or leaves. To keep\na live roster, apply <code>onJoin</code> and <code>onLeave</code> to the list you got from\n<code>presence()</code>, or call <code>presence()</code> again.</li>\n<li>Channels live in the server process's memory. Nothing is persisted, and a\nchannel is only reachable within its own canvas: a channel name is a key\ninside the canvas the socket was opened for, never across canvases.</li>\n</ul>\n<h2 id=\"how-the-connection-works\">How the connection works</h2>\n<ul>\n<li>All channels on a canvas share one WebSocket, at\n<code>{base}/v1/c/{slug}/realtime</code> over <code>wss://</code> (<code>ws://</code> when the instance runs on\nplain HTTP). It is the same host the SDK uses for every other primitive, so\nyou never construct the URL yourself. Each accepted connection counts once in\nthe canvas's usage stats (realtime connects).</li>\n<li>The socket opens on the first <code>subscribe()</code> or <code>presence()</code> on any channel.\n<code>publish()</code> on its own does not open it on a page where no channel has been\nsubscribed, asked for presence, or given a listener; those frames wait in the\nbuffer and nothing connects. Subscribe once (or call <code>presence()</code>) before you\npublish.</li>\n<li>Frames sent while the socket is not open (<code>publish</code>, <code>presence</code>,\n<code>unsubscribe</code>) are buffered and flushed once it connects; the buffer keeps the\nmost recent 256 frames and drops the oldest beyond that. Subscriptions are\nnot buffered: every subscribed channel is re-sent from the client's state\neach time the socket opens.</li>\n<li>After a transient drop the SDK reconnects with exponential backoff, starting\nat 500 ms, doubling, and capped at 10 s. On open it re-subscribes every\nsubscribed channel, then flushes the buffer. Any <code>presence()</code> in flight at\nthe moment of the drop rejects with a <code>CanvasdropError</code> (<code>code: \"DISCONNECTED\"</code>, <code>status: 0</code>) rather than hanging; call it again.</li>\n<li>A refused upgrade never becomes a socket: the viewer is signed out, has not\npassed the password gate, cannot see the canvas at its current rung, or is on\na Public link canvas (static-only, every primitive refused). The browser\nreports each attempt as an ordinary close, so the SDK keeps retrying, backing\noff to the 10 s cap, and no typed error reaches your code.</li>\n<li><code>close()</code> on the last open channel tears the socket down with a normal close.\nSigning out of the dashboard does not by itself close a socket that is\nalready open; the next reconnect attempt is what gets refused.</li>\n</ul>\n<h2 id=\"limits-and-errors\">Limits and errors</h2>\n<p>Limits enforced by the server:</p>\n<table>\n<thead>\n<tr>\n<th>Limit</th>\n<th>Value</th>\n<th>When exceeded</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Connections per canvas</td>\n<td>30</td>\n<td>Close <code>4429</code> (terminal)</td>\n</tr>\n<tr>\n<td>Publishes per connection</td>\n<td>100 per rolling minute</td>\n<td>Error frame <code>RATE_LIMITED</code>; the publish is dropped</td>\n</tr>\n<tr>\n<td>Frame size (whole JSON frame)</td>\n<td>16 KiB</td>\n<td>Error frame <code>MESSAGE_TOO_LARGE</code>; the frame is dropped</td>\n</tr>\n<tr>\n<td>Channel name</td>\n<td>128 bytes</td>\n<td>Error frame <code>CHANNEL_NAME_TOO_LARGE</code>; the frame is dropped</td>\n</tr>\n<tr>\n<td>Channels per connection</td>\n<td>64</td>\n<td>Error frame <code>CHANNEL_LIMIT</code>; the subscribe is dropped</td>\n</tr>\n</tbody></table>\n<p>The error frames (<code>RATE_LIMITED</code>, <code>MESSAGE_TOO_LARGE</code>, <code>CHANNEL_NAME_TOO_LARGE</code>,\n<code>CHANNEL_LIMIT</code>, <code>INVALID_FRAME</code>, <code>UNKNOWN_FRAME</code>) keep the socket open, and the\nSDK does not surface them to your code: a rejected publish is dropped silently.\nKeep high-frequency publishers such as cursors under 100 sends a minute per\ntab, and keep payloads small.</p>\n<p>Three failures are terminal. The SDK stops reconnecting, rejects any pending\n<code>presence()</code>, and makes every later <code>publish()</code> throw and <code>presence()</code> reject\nwith the same typed error:</p>\n<table>\n<thead>\n<tr>\n<th>Condition</th>\n<th>Close code</th>\n<th>Error</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Realtime is off for the canvas (any of the three switches above). When you connect while it is already off, the server sends one <code>{ type: \"error\", code: \"CAPABILITY_DISABLED\", capability: \"realtime\" }</code> frame before closing. When it is switched off while you are connected, the socket is closed on the next re-check.</td>\n<td><code>4403</code></td>\n<td><code>CapabilityDisabledError</code> (<code>code: \"CAPABILITY_DISABLED\"</code>, <code>status: 403</code>)</td>\n</tr>\n<tr>\n<td>The connection lost access: the canvas was deleted, archived, disabled, or unpublished; it moved to a rung the viewer is not on; the viewer was removed from the people list, a team, or the editor role; the share expired; a password was set (owner and editors are exempt); or the account was blocked or deleted. The server re-checks live sockets on every access-changing change and on a heartbeat every 60 s.</td>\n<td><code>4401</code></td>\n<td><code>NotAuthenticatedError</code> (<code>code: \"NOT_AUTHENTICATED\"</code>, <code>status: 401</code>)</td>\n</tr>\n<tr>\n<td>The canvas already has 30 open connections.</td>\n<td><code>4429</code></td>\n<td><code>QuotaExceededError</code> (<code>code: \"CONNECTION_LIMIT\"</code>, <code>status: 429</code>)</td>\n</tr>\n</tbody></table>\n<p>Wrap <code>presence()</code> in a <code>try</code>/<code>catch</code> and branch on <code>err.code</code> to show a \"live\nfeatures are off\" state instead of failing the whole page; the rest of the\ncanvas keeps working without the socket.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">try</span> {\n  <span class=\"hljs-keyword\">await</span> room.<span class=\"hljs-title function_\">presence</span>();\n} <span class=\"hljs-keyword\">catch</span> (err) {\n  <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"CAPABILITY_DISABLED\"</span>) <span class=\"hljs-title function_\">showBanner</span>(<span class=\"hljs-string\">\"Live features are off for this canvas.\"</span>);\n  <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"CONNECTION_LIMIT\"</span>) <span class=\"hljs-title function_\">showBanner</span>(<span class=\"hljs-string\">\"This room is full right now.\"</span>);\n  <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"DISCONNECTED\"</span>) <span class=\"hljs-title function_\">retryLater</span>();\n  <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">throw</span> err;\n}</code></pre>\n<p>See the <a href=\"/docs/api/errors\">error codes reference</a> for the full code table and\nthe <a href=\"/docs/api/runtime-api\">Runtime API</a> for the wire protocol behind the SDK.</p>\n",
    "headings": [
      {
        "id": "when-realtime-is-on",
        "text": "When realtime is on",
        "level": 2
      },
      {
        "id": "channel-methods",
        "text": "Channel methods",
        "level": 2
      },
      {
        "id": "what-presence-and-messages-mean",
        "text": "What presence and messages mean",
        "level": 2
      },
      {
        "id": "how-the-connection-works",
        "text": "How the connection works",
        "level": 2
      },
      {
        "id": "limits-and-errors",
        "text": "Limits and errors",
        "level": 2
      }
    ],
    "text": "Realtime Add live cursors, reactions, or a shared room to a canvas with canvasdrop.realtime : ephemeral pub/sub plus presence over named channels. By the end of this page you can publish to a channel, receive what others publish, read who is present, and handle the three ways a socket ends for good. Messages fan out to whoever is subscribed to the channel at that moment and are never stored; put anything that must survive a reload in KV . canvasdrop is already on window in any served canvas (see the SDK overview ) and connects to the right canvas from its URL. The from on every message and every entry in the presence list come from the viewer's server-side session, never from the page, so names are trustworthy. const room = canvasdrop. realtime . channel ( \"room-1\" ); // Receive what anyone on the channel publishes, including yourself. room. subscribe ( ( msg ) => { // msg = { event, data, from: { id, name } } console . log (msg. from . name , msg. event , msg. data ); }); // Broadcast to everyone subscribed right now. Fire-and-forget. room. publish ( \"cursor\" , { x : 120 , y : 48 }); // Who is in the channel right now. const users = await room. presence (); // [{ id, name }, ...] room. onJoin ( ( user ) => console . log (user. name , \"joined\" )); room. onLeave ( ( user ) => console . log (user. name , \"left\" )); room. unsubscribe (); // stop receiving messages; the handle stays usable room. close (); // done with this channel; the socket closes when none remain When realtime is on Realtime works when all three switches are on. A new canvas ships with the backend off, so the first step is usually turning that on. Switch Default Who sets it The canvas's backend (master switch) off The owner or an editor, in the canvas's Backend tab or with the set_capabilities MCP tool The canvas's realtime flag on Same place The instance switch CANVAS_DROP_REALTIME on The operator, in the server environment. The admin panel shows it read-only. When any of the three is off, the server closes the socket with code 4403 and the SDK raises CapabilityDisabledError (see Limits and errors ). The rest of the canvas keeps working. Details in Capabilities . Channel methods canvasdrop.realtime.channel(name) returns a Channel . Signatures as declared in the SDK: Method Signature What it does publish publish(event: string, data: unknown): void Send { event, data } to every current subscriber. Returns nothing; there is no delivery receipt. You do not need to be subscribed to publish, but see How the connection works for when the socket opens. subscribe subscribe(handler: (msg: RealtimeMessage) => void): void Join the channel and run handler for each message. Calling it again adds another handler; it does not return an unsubscribe function. unsubscribe unsubscribe(): void Leave the channel and drop every message handler. onPresence , onJoin , and onLeave handlers stay registered, but no join or leave reaches you until you subscribe again. presence presence(): Promise<RealtimeUser[]> Resolve with the users subscribed to the channel right now. Works whether or not you are subscribed. onPresence onPresence(handler: (users: RealtimeUser[]) => void): void Run handler each time the server sends a full roster (see below). onJoin onJoin(handler: (user: RealtimeUser) => void): void Run handler when another user's first connection joins the channel. onLeave onLeave(handler: (user: RealtimeUser) => void): void Run handler when another user's last connection leaves the channel. close close(): void Retire this handle: it leaves the channel, and the shared socket closes once no channels remain. publish and presence on a closed handle fail with a CanvasdropError ( code: \"CHANNEL_CLOSED\" , status: 0 ). There is no generic on(event, handler) , off , or once ; the four listener hooks above are the whole surface. type RealtimeUser = { id : string ; name : string }; type RealtimeMessage = { event : string ; data : unknown ; from : RealtimeUser }; What presence and messages mean A message reaches every connection subscribed to the channel, the sender's included. If you subscribe and publish from the same page, your own handler runs for your own messages. Presence is the list of distinct users subscribed to the channel. A person with two tabs open appears once. onJoin fires for the other users when a person's first connection subscribes; onLeave fires when their last connection unsubscribes, closes, or drops. You never receive your own join or leave. onPresence fires when the server sends a roster: right after your subscribe (including the re-subscribe after a reconnect) and after every presence() call. It does not fire when someone else joins or leaves. To keep a live roster, apply onJoin and onLeave to the list you got from presence() , or call presence() again. Channels live in the server process's memory. Nothing is persisted, and a channel is only reachable within its own canvas: a channel name is a key inside the canvas the socket was opened for, never across canvases. How the connection works All channels on a canvas share one WebSocket, at {base}/v1/c/{slug}/realtime over wss:// ( ws:// when the instance runs on plain HTTP). It is the same host the SDK uses for every other primitive, so you never construct the URL yourself. Each accepted connection counts once in the canvas's usage stats (realtime connects). The socket opens on the first subscribe() or presence() on any channel. publish() on its own does not open it on a page where no channel has been subscribed, asked for presence, or given a listener; those frames wait in the buffer and nothing connects. Subscribe once (or call presence() ) before you publish. Frames sent while the socket is not open ( publish , presence , unsubscribe ) are buffered and flushed once it connects; the buffer keeps the most recent 256 frames and drops the oldest beyond that. Subscriptions are not buffered: every subscribed channel is re-sent from the client's state each time the socket opens. After a transient drop the SDK reconnects with exponential backoff, starting at 500 ms, doubling, and capped at 10 s. On open it re-subscribes every subscribed channel, then flushes the buffer. Any presence() in flight at the moment of the drop rejects with a CanvasdropError ( code: \"DISCONNECTED\" , status: 0 ) rather than hanging; call it again. A refused upgrade never becomes a socket: the viewer is signed out, has not passed the password gate, cannot see the canvas at its current rung, or is on a Public link canvas (static-only, every primitive refused). The browser reports each attempt as an ordinary close, so the SDK keeps retrying, backing off to the 10 s cap, and no typed error reaches your code. close() on the last open channel tears the socket down with a normal close. Signing out of the dashboard does not by itself close a socket that is already open; the next reconnect attempt is what gets refused. Limits and errors Limits enforced by the server: Limit Value When exceeded Connections per canvas 30 Close 4429 (terminal) Publishes per connection 100 per rolling minute Error frame RATE_LIMITED ; the publish is dropped Frame size (whole JSON frame) 16 KiB Error frame MESSAGE_TOO_LARGE ; the frame is dropped Channel name 128 bytes Error frame CHANNEL_NAME_TOO_LARGE ; the frame is dropped Channels per connection 64 Error frame CHANNEL_LIMIT ; the subscribe is dropped The error frames ( RATE_LIMITED , MESSAGE_TOO_LARGE , CHANNEL_NAME_TOO_LARGE , CHANNEL_LIMIT , INVALID_FRAME , UNKNOWN_FRAME ) keep the socket open, and the SDK does not surface them to your code: a rejected publish is dropped silently. Keep high-frequency publishers such as cursors under 100 sends a minute per tab, and keep payloads small. Three failures are terminal. The SDK stops reconnecting, rejects any pending presence() , and makes every later publish() throw and presence() reject with the same typed error: Condition Close code Error Realtime is off for the canvas (any of the three switches above). When you connect while it is already off, the server sends one { type: \"error\", code: \"CAPABILITY_DISABLED\", capability: \"realtime\" } frame before closing. When it is switched off while you are connected, the socket is closed on the next re-check. 4403 CapabilityDisabledError ( code: \"CAPABILITY_DISABLED\" , status: 403 ) The connection lost access: the canvas was deleted, archived, disabled, or unpublished; it moved to a rung the viewer is not on; the viewer was removed from the people list, a team, or the editor role; the share expired; a password was set (owner and editors are exempt); or the account was blocked or deleted. The server re-checks live sockets on every access-changing change and on a heartbeat every 60 s. 4401 NotAuthenticatedError ( code: \"NOT_AUTHENTICATED\" , status: 401 ) The canvas already has 30 open connections. 4429 QuotaExceededError ( code: \"CONNECTION_LIMIT\" , status: 429 ) Wrap presence() in a try / catch and branch on err.code to show a \"live features are off\" state instead of failing the whole page; the rest of the canvas keeps working without the socket. try { await room. presence (); } catch (err) { if (err. code === \"CAPABILITY_DISABLED\" ) showBanner ( \"Live features are off for this canvas.\" ); else if (err. code === \"CONNECTION_LIMIT\" ) showBanner ( \"This room is full right now.\" ); else if (err. code === \"DISCONNECTED\" ) retryLater (); else throw err; } See the error codes reference for the full code table and the Runtime API for the wire protocol behind the SDK."
  },
  {
    "path": "sdk/connections",
    "section": "Browser SDK",
    "title": "Connections",
    "html": "<h1 id=\"outbound-connections\">Outbound Connections</h1>\n<p>Use <code>canvasdrop.connections.fetch</code> when a canvas needs data from a third-party\nHTTP API that cannot be called directly from the browser—for example, an API\nthat does not allow browser CORS or requires a fixed <code>User-Agent</code> or API key.</p>\n<p>Connections is a controlled request forwarder, not a Lambda runtime. Canvas\nDrop never runs canvas-supplied server code. An instance administrator creates\na reusable profile for one exact HTTPS origin, chooses the allowed standard\nmethods, stores any protected headers, and grants the profile to individual\ncanvases. The protected values remain encrypted on the server.</p>\n<h2 id=\"fetch-through-a-profile\">Fetch through a profile</h2>\n<p>Suppose an administrator created the profile key <code>stocks</code>, approved\n<code>https://api.example.com</code>, allowed <code>GET</code>, fixed its <code>User-Agent</code>, and granted it\nto your canvas:</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">const</span> symbol = <span class=\"hljs-string\">\"ACME\"</span>;\n<span class=\"hljs-keyword\">const</span> response = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">connections</span>.<span class=\"hljs-title function_\">fetch</span>(\n  <span class=\"hljs-string\">\"stocks\"</span>,\n  <span class=\"hljs-string\">`/v2/quote?symbol=<span class=\"hljs-subst\">${<span class=\"hljs-built_in\">encodeURIComponent</span>(symbol)}</span>`</span>,\n  { <span class=\"hljs-attr\">headers</span>: { <span class=\"hljs-attr\">accept</span>: <span class=\"hljs-string\">\"application/json\"</span> } },\n);\n\n<span class=\"hljs-keyword\">if</span> (!response.<span class=\"hljs-property\">ok</span>) {\n  <span class=\"hljs-comment\">// This is the upstream's status and bounded response body, not a platform error.</span>\n  <span class=\"hljs-keyword\">throw</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title class_\">Error</span>(<span class=\"hljs-string\">`Stock provider returned <span class=\"hljs-subst\">${response.status}</span>`</span>);\n}\n<span class=\"hljs-keyword\">const</span> quote = <span class=\"hljs-keyword\">await</span> response.<span class=\"hljs-title function_\">json</span>();</code></pre>\n<p>The signature is:</p>\n<pre><code class=\"hljs language-ts\">connections.<span class=\"hljs-title function_\">fetch</span>(\n  <span class=\"hljs-attr\">profile</span>: <span class=\"hljs-built_in\">string</span>,\n  <span class=\"hljs-attr\">path</span>: <span class=\"hljs-built_in\">string</span>,\n  <span class=\"hljs-attr\">init</span>?: {\n    <span class=\"hljs-attr\">method</span>?: <span class=\"hljs-string\">\"GET\"</span> | <span class=\"hljs-string\">\"HEAD\"</span> | <span class=\"hljs-string\">\"POST\"</span> | <span class=\"hljs-string\">\"PUT\"</span> | <span class=\"hljs-string\">\"PATCH\"</span> | <span class=\"hljs-string\">\"DELETE\"</span>;\n    <span class=\"hljs-attr\">headers</span>?: <span class=\"hljs-title class_\">RequestInit</span>[<span class=\"hljs-string\">\"headers\"</span>];\n    <span class=\"hljs-attr\">body</span>?: <span class=\"hljs-title class_\">RequestInit</span>[<span class=\"hljs-string\">\"body\"</span>];\n    <span class=\"hljs-attr\">signal</span>?: <span class=\"hljs-title class_\">RequestInit</span>[<span class=\"hljs-string\">\"signal\"</span>];\n  },\n): <span class=\"hljs-title class_\">Promise</span>&lt;<span class=\"hljs-title class_\">Response</span>&gt;</code></pre>\n<p><code>path</code> must start with one <code>/</code>. Absolute URLs and protocol-relative paths are\nrejected in the SDK before a request is sent. The path and query are appended to\nthe profile's exact origin. Canvas code cannot change its scheme, hostname, or\nport.</p>\n<p>Only a method the administrator selected for that profile is accepted. Request\nbodies work for <code>POST</code>, <code>PUT</code>, <code>PATCH</code>, and <code>DELETE</code>; <code>GET</code> and <code>HEAD</code> do not send\none. <code>headers</code> accepts the normal browser <code>HeadersInit</code> shapes. Canvas cookies,\nauthorization, forwarding, hop-by-hop, compression, and host headers are never\npassed through. A protected profile header wins over a caller header with the\nsame name.</p>\n<h2 id=\"responses-and-errors\">Responses and errors</h2>\n<p>The promise resolves to a native <code>Response</code> for every response the approved\nupstream returned, including <code>4xx</code> and <code>5xx</code>. Check <code>response.ok</code> or\n<code>response.status</code> just as you would with <code>fetch</code>.</p>\n<p>Canvas Drop policy and transport failures throw <code>CanvasdropError</code> instead. The\nstable codes include:</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Status</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CONNECTION_NOT_GRANTED</code></td>\n<td>404</td>\n<td>The profile is not attached to this canvas.</td>\n</tr>\n<tr>\n<td><code>CONNECTION_DISABLED</code></td>\n<td>503</td>\n<td>An administrator disabled the profile.</td>\n</tr>\n<tr>\n<td><code>CONNECTION_KEY_UNAVAILABLE</code></td>\n<td>503</td>\n<td>The external encryption key needed for protected headers is unavailable.</td>\n</tr>\n<tr>\n<td><code>METHOD_NOT_ALLOWED</code></td>\n<td>405</td>\n<td>The method is unsupported or not approved for this profile.</td>\n</tr>\n<tr>\n<td><code>DESTINATION_BLOCKED</code></td>\n<td>403</td>\n<td>Origin, DNS answer, redirect, or content encoding crossed the connection boundary.</td>\n</tr>\n<tr>\n<td><code>REQUEST_TOO_LARGE</code></td>\n<td>413</td>\n<td>URL, headers, or body exceeded a request limit.</td>\n</tr>\n<tr>\n<td><code>RESPONSE_TOO_LARGE</code></td>\n<td>502</td>\n<td>The upstream response exceeded the buffer limit.</td>\n</tr>\n<tr>\n<td><code>CONNECTION_RATE_LIMIT</code></td>\n<td>429</td>\n<td>A connection-specific request bucket is spent.</td>\n</tr>\n<tr>\n<td><code>CONNECTION_LIMIT</code></td>\n<td>429</td>\n<td>A per-canvas or process-wide in-flight limit is full.</td>\n</tr>\n<tr>\n<td><code>UPSTREAM_TIMEOUT</code></td>\n<td>504</td>\n<td>The total DNS, request, redirect, and response deadline expired.</td>\n</tr>\n<tr>\n<td><code>UPSTREAM_UNAVAILABLE</code></td>\n<td>502</td>\n<td>DNS or the approved upstream failed.</td>\n</tr>\n</tbody></table>\n<p>The normal canvas gates still run first: identity, canvas access, password,\nlifecycle, Public-link static-only, the broad runtime rate limit, and the\nBackend master switch. Detaching a grant, disabling its profile or Backend,\nrevoking access, adding a password, expiring/archiving/disabling/deleting the\ncanvas blocks the next request.</p>\n<h2 id=\"fixed-safety-bounds\">Fixed safety bounds</h2>\n<p>The first release is deliberately small and buffered:</p>\n<table>\n<thead>\n<tr>\n<th>Boundary</th>\n<th>Default</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Relative URL</td>\n<td>8 KiB</td>\n</tr>\n<tr>\n<td>Caller headers</td>\n<td>32 headers, 16 KiB total</td>\n</tr>\n<tr>\n<td>Request body</td>\n<td>256 KiB</td>\n</tr>\n<tr>\n<td>Response body</td>\n<td>2 MiB</td>\n</tr>\n<tr>\n<td>Total deadline</td>\n<td>10 seconds</td>\n</tr>\n<tr>\n<td>Redirects</td>\n<td>3, exact-origin only; DNS is revalidated at every hop</td>\n</tr>\n<tr>\n<td>Rate</td>\n<td>60/min per actor + canvas + profile; 600/min per profile</td>\n</tr>\n<tr>\n<td>In flight</td>\n<td>5 per canvas; 50 in the server process</td>\n</tr>\n</tbody></table>\n<p>The operator can set these read-only deployment values through the\n<code>CANVAS_DROP_CONNECTIONS_*</code> environment variables. Multi-process coordination\nis not part of this first release.</p>\n<h2 id=\"security-boundary\">Security boundary</h2>\n<p>Canvas Drop accepts only HTTPS DNS origins—no IP literals, credentials, path,\nquery, or fragment in the profile. Every DNS answer must be public. Mixed\npublic/private answers are rejected, and the chosen public address is pinned to\nthe TLS socket while the approved hostname remains the certificate, SNI, and\n<code>Host</code> name. Redirects must retain the exact origin and are re-resolved before\nthe next socket is opened. Responses are identity-encoded, bounded, <code>no-store</code>,\nand expose only safe headers; cookies, redirects, CORS, server, and hop-by-hop\nheaders are removed.</p>\n<p>Protected header names are visible only to administrators as “configured”; the\nvalues are never returned by an API or loaded into the dashboard. Owners,\neditors, and <code>list_canvas_connections</code> over MCP see only the profile key, label,\norigin, allowed methods, and availability.</p>\n<p>One residual trust boundary remains: the approved upstream receives the\nprotected value and can deliberately reflect it in its own response body. Use a\nleast-privilege credential scoped to that upstream and profile. Operators should\nalso restrict the Canvas Drop server's network egress as defense in depth.</p>\n<p>Public-link visitors can never use Connections. If an audience needs this\nbackend, use Restricted or Whole org access and grant people or teams as needed.</p>\n",
    "headings": [
      {
        "id": "fetch-through-a-profile",
        "text": "Fetch through a profile",
        "level": 2
      },
      {
        "id": "responses-and-errors",
        "text": "Responses and errors",
        "level": 2
      },
      {
        "id": "fixed-safety-bounds",
        "text": "Fixed safety bounds",
        "level": 2
      },
      {
        "id": "security-boundary",
        "text": "Security boundary",
        "level": 2
      }
    ],
    "text": "Outbound Connections Use canvasdrop.connections.fetch when a canvas needs data from a third-party HTTP API that cannot be called directly from the browser—for example, an API that does not allow browser CORS or requires a fixed User-Agent or API key. Connections is a controlled request forwarder, not a Lambda runtime. Canvas Drop never runs canvas-supplied server code. An instance administrator creates a reusable profile for one exact HTTPS origin, chooses the allowed standard methods, stores any protected headers, and grants the profile to individual canvases. The protected values remain encrypted on the server. Fetch through a profile Suppose an administrator created the profile key stocks , approved https://api.example.com , allowed GET , fixed its User-Agent , and granted it to your canvas: const symbol = \"ACME\" ; const response = await canvasdrop. connections . fetch ( \"stocks\" , `/v2/quote?symbol= ${ encodeURIComponent (symbol)} ` , { headers : { accept : \"application/json\" } }, ); if (!response. ok ) { // This is the upstream's status and bounded response body, not a platform error. throw new Error ( `Stock provider returned ${response.status} ` ); } const quote = await response. json (); The signature is: connections. fetch ( profile : string , path : string , init ?: { method ?: \"GET\" | \"HEAD\" | \"POST\" | \"PUT\" | \"PATCH\" | \"DELETE\" ; headers ?: RequestInit [ \"headers\" ]; body ?: RequestInit [ \"body\" ]; signal ?: RequestInit [ \"signal\" ]; }, ): Promise < Response > path must start with one / . Absolute URLs and protocol-relative paths are rejected in the SDK before a request is sent. The path and query are appended to the profile's exact origin. Canvas code cannot change its scheme, hostname, or port. Only a method the administrator selected for that profile is accepted. Request bodies work for POST , PUT , PATCH , and DELETE ; GET and HEAD do not send one. headers accepts the normal browser HeadersInit shapes. Canvas cookies, authorization, forwarding, hop-by-hop, compression, and host headers are never passed through. A protected profile header wins over a caller header with the same name. Responses and errors The promise resolves to a native Response for every response the approved upstream returned, including 4xx and 5xx . Check response.ok or response.status just as you would with fetch . Canvas Drop policy and transport failures throw CanvasdropError instead. The stable codes include: Code Status Meaning CONNECTION_NOT_GRANTED 404 The profile is not attached to this canvas. CONNECTION_DISABLED 503 An administrator disabled the profile. CONNECTION_KEY_UNAVAILABLE 503 The external encryption key needed for protected headers is unavailable. METHOD_NOT_ALLOWED 405 The method is unsupported or not approved for this profile. DESTINATION_BLOCKED 403 Origin, DNS answer, redirect, or content encoding crossed the connection boundary. REQUEST_TOO_LARGE 413 URL, headers, or body exceeded a request limit. RESPONSE_TOO_LARGE 502 The upstream response exceeded the buffer limit. CONNECTION_RATE_LIMIT 429 A connection-specific request bucket is spent. CONNECTION_LIMIT 429 A per-canvas or process-wide in-flight limit is full. UPSTREAM_TIMEOUT 504 The total DNS, request, redirect, and response deadline expired. UPSTREAM_UNAVAILABLE 502 DNS or the approved upstream failed. The normal canvas gates still run first: identity, canvas access, password, lifecycle, Public-link static-only, the broad runtime rate limit, and the Backend master switch. Detaching a grant, disabling its profile or Backend, revoking access, adding a password, expiring/archiving/disabling/deleting the canvas blocks the next request. Fixed safety bounds The first release is deliberately small and buffered: Boundary Default Relative URL 8 KiB Caller headers 32 headers, 16 KiB total Request body 256 KiB Response body 2 MiB Total deadline 10 seconds Redirects 3, exact-origin only; DNS is revalidated at every hop Rate 60/min per actor + canvas + profile; 600/min per profile In flight 5 per canvas; 50 in the server process The operator can set these read-only deployment values through the CANVAS_DROP_CONNECTIONS_* environment variables. Multi-process coordination is not part of this first release. Security boundary Canvas Drop accepts only HTTPS DNS origins—no IP literals, credentials, path, query, or fragment in the profile. Every DNS answer must be public. Mixed public/private answers are rejected, and the chosen public address is pinned to the TLS socket while the approved hostname remains the certificate, SNI, and Host name. Redirects must retain the exact origin and are re-resolved before the next socket is opened. Responses are identity-encoded, bounded, no-store , and expose only safe headers; cookies, redirects, CORS, server, and hop-by-hop headers are removed. Protected header names are visible only to administrators as “configured”; the values are never returned by an API or loaded into the dashboard. Owners, editors, and list_canvas_connections over MCP see only the profile key, label, origin, allowed methods, and availability. One residual trust boundary remains: the approved upstream receives the protected value and can deliberately reflect it in its own response body. Use a least-privilege credential scoped to that upstream and profile. Operators should also restrict the Canvas Drop server's network egress as defense in depth. Public-link visitors can never use Connections. If an audience needs this backend, use Restricted or Whole org access and grant people or teams as needed."
  },
  {
    "path": "sdk/authoring",
    "section": "Browser SDK",
    "title": "Authoring",
    "html": "<h1 id=\"authoring\">Authoring</h1>\n<p>Let a signed-in viewer of your canvas create and <strong>manage</strong> canvases from the page —\nas themselves, with real ownership and no secret in the browser. A share created here\nis a <strong>managed artifact</strong>: you <code>publish</code> it once, <code>update</code> it in place (same URL) as its\ncontent changes, <code>list</code> your shares with their live status, and <code>revoke</code> one when it's\ndone. The <code>canvasdrop.canvases</code> namespace wraps <code>publish</code>, <code>update</code>, <code>list</code>, and <code>revoke</code>.</p>\n<p>This is the <strong>authoring</strong> capability. It is off by default and higher-privilege\nthan the other primitives: the owner turns it on in the <strong>Backend</strong> tab <em>and</em> the\noperator must enable it for the instance. Guests and public-link visitors can't\nuse it — creation needs a signed-in org member.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-comment\">// Create a durable share once.</span>\n<span class=\"hljs-keyword\">const</span> share = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">canvases</span>.<span class=\"hljs-title function_\">publish</span>({\n  <span class=\"hljs-attr\">title</span>: <span class=\"hljs-string\">\"Team roadmap — Q3 snapshot\"</span>,\n  <span class=\"hljs-attr\">access</span>: <span class=\"hljs-string\">\"public_link\"</span>,             <span class=\"hljs-comment\">// audience; defaults to Restricted (`private`)</span>\n  <span class=\"hljs-attr\">password</span>: <span class=\"hljs-string\">\"optional-extra-lock\"</span>,   <span class=\"hljs-comment\">// independent of the audience</span>\n  <span class=\"hljs-attr\">tags</span>: [<span class=\"hljs-string\">\"roadmap\"</span>],\n  <span class=\"hljs-attr\">metadata</span>: { <span class=\"hljs-attr\">sourceApp</span>: <span class=\"hljs-string\">\"product-roadmap\"</span>, <span class=\"hljs-attr\">sourceKind</span>: <span class=\"hljs-string\">\"roadmap-share\"</span>, <span class=\"hljs-attr\">theme</span>: <span class=\"hljs-string\">\"light\"</span> },\n  <span class=\"hljs-attr\">expiresAt</span>: <span class=\"hljs-title class_\">Date</span>.<span class=\"hljs-title function_\">now</span>() + <span class=\"hljs-number\">7</span> * <span class=\"hljs-number\">864e5</span>, <span class=\"hljs-comment\">// optional; the operator may require/limit it</span>\n  <span class=\"hljs-attr\">bundle</span>: zipBlob,                   <span class=\"hljs-comment\">// a static-site zip (Blob or ArrayBuffer)</span>\n});\n<span class=\"hljs-comment\">// share.url is the live canvas; it now appears in the viewer's own dashboard.</span>\n\n<span class=\"hljs-comment\">// Later — update IN PLACE. The URL never changes; a new version is deployed.</span>\n<span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">canvases</span>.<span class=\"hljs-title function_\">update</span>(share.<span class=\"hljs-property\">id</span>, {\n  <span class=\"hljs-attr\">bundle</span>: newZipBlob,                <span class=\"hljs-comment\">// omit to change only settings/metadata</span>\n  <span class=\"hljs-attr\">metadata</span>: { ...share.<span class=\"hljs-property\">metadata</span>, <span class=\"hljs-attr\">itemCount</span>: <span class=\"hljs-number\">42</span>, <span class=\"hljs-attr\">generatedAt</span>: <span class=\"hljs-title class_\">Date</span>.<span class=\"hljs-title function_\">now</span>() },\n});\n\n<span class=\"hljs-comment\">// The management view — filter to this app's shares.</span>\n<span class=\"hljs-keyword\">const</span> mine = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">canvases</span>.<span class=\"hljs-title function_\">list</span>({ <span class=\"hljs-attr\">sourceApp</span>: <span class=\"hljs-string\">\"product-roadmap\"</span> });\n<span class=\"hljs-comment\">//    → [{ id, url, title, status, access, expiresAt, updatedAt, metadata, ... }]</span>\n\n<span class=\"hljs-comment\">// Retire a share. Its URL goes dead, but it STAYS in list() as status \"revoked\".</span>\n<span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">canvases</span>.<span class=\"hljs-title function_\">revoke</span>(share.<span class=\"hljs-property\">id</span>);</code></pre>\n<h2 id=\"methods\">Methods</h2>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Returns</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>publish(options)</code></td>\n<td><code>Promise&lt;AuthoredCanvas&gt;</code> — creates, deploys, and configures a new share</td>\n</tr>\n<tr>\n<td><code>update(id, options)</code></td>\n<td><code>Promise&lt;AuthoredCanvas&gt;</code> — replaces the bundle and/or settings <strong>at the same URL</strong></td>\n</tr>\n<tr>\n<td><code>list(filter?)</code></td>\n<td><code>Promise&lt;AuthoredCanvas[]&gt;</code> — the viewer's shares (incl. revoked/expired), optionally filtered</td>\n</tr>\n<tr>\n<td><code>revoke(id)</code></td>\n<td><code>Promise&lt;void&gt;</code> — make a share's public URL unavailable; it stays listed as <code>revoked</code></td>\n</tr>\n</tbody></table>\n<h3 id=\"authoredcanvas\"><code>AuthoredCanvas</code></h3>\n<p>What <code>publish</code>, <code>update</code>, and each <code>list</code> entry return — a share plus its management state:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>id</code> / <code>url</code></td>\n<td><code>string</code></td>\n<td>stable id and public URL (the URL never changes across updates)</td>\n</tr>\n<tr>\n<td><code>title</code> / <code>tags</code></td>\n<td><code>string</code> / <code>string[]</code></td>\n<td></td>\n</tr>\n<tr>\n<td><code>access</code></td>\n<td><code>string</code></td>\n<td>the persisted General-access value (<code>private</code>, <code>whole_org</code>, <code>public_link</code>, or a legacy alias of <code>private</code>: <code>specific_people</code> / <code>team</code>)</td>\n</tr>\n<tr>\n<td><code>accessMode</code></td>\n<td><code>\"restricted\" | \"whole_org\" | \"public_link\"</code></td>\n<td><strong>who else</strong> can open it beyond the people-and-teams list, which applies at every value; <code>private</code> and its aliases read <code>restricted</code>. Branch on this, not on <code>access</code></td>\n</tr>\n<tr>\n<td><code>publicationStatus</code></td>\n<td><code>\"draft\" | \"published\" | \"expired\" | \"unpublished\" | \"archived\" | \"disabled\"</code></td>\n<td><strong>whether</strong> it is published, independent of the audience; precedence <code>disabled</code> › <code>archived</code> › <code>unpublished</code> (revoked) › <code>draft</code> (no version) › <code>expired</code> › <code>published</code>. Deleted canvases are omitted from every authoring response</td>\n</tr>\n<tr>\n<td><code>hasPassword</code></td>\n<td><code>boolean</code></td>\n<td>whether an additional password lock is currently set</td>\n</tr>\n<tr>\n<td><code>discoverability</code></td>\n<td><code>\"link_only\" | \"listed\"</code></td>\n<td>whether eligible org/team shares appear in discovery surfaces</td>\n</tr>\n<tr>\n<td><code>galleryTemplatable</code></td>\n<td><code>boolean</code></td>\n<td>whether gallery viewers may use the canvas as a template</td>\n</tr>\n<tr>\n<td><code>viewerRole</code></td>\n<td><code>\"owner\" | \"editor\" | \"admin\"</code></td>\n<td>why the current viewer may manage this record</td>\n</tr>\n<tr>\n<td><code>audienceSummary</code></td>\n<td><code>{ count: number | null; names: string[] }</code></td>\n<td>safe summary of the viewer people and teams on the list (<code>count</code> = both; <code>names</code> = the teams)</td>\n</tr>\n<tr>\n<td><code>status</code></td>\n<td><code>\"live\" | \"expired\" | \"revoked\" | \"private\"</code></td>\n<td><strong>deprecated</strong>; frozen for older clients. <code>private</code> means the persisted value is literally <code>private</code> (the people on the list can still open it), and the legacy aliases read <code>live</code>. Read <code>accessMode</code> + <code>publicationStatus</code> instead</td>\n</tr>\n<tr>\n<td><code>createdAt</code> / <code>updatedAt</code></td>\n<td><code>number</code></td>\n<td>unix ms</td>\n</tr>\n<tr>\n<td><code>expiresAt</code> / <code>revokedAt</code></td>\n<td><code>number | null</code></td>\n<td>share expiry; when it was revoked</td>\n</tr>\n<tr>\n<td><code>galleryListed</code></td>\n<td><code>boolean</code></td>\n<td>whether the canvas is explicitly listed in the gallery</td>\n</tr>\n<tr>\n<td><code>createdBy</code></td>\n<td><code>string</code></td>\n<td>the creator (owner) id</td>\n</tr>\n<tr>\n<td><code>version</code></td>\n<td><code>string | null</code></td>\n<td>the current version id — advances on every bundle deploy (a change signal)</td>\n</tr>\n<tr>\n<td><code>bundleUpdatedAt</code></td>\n<td><code>number</code></td>\n<td>legacy last-change stamp; use <code>version</code> to detect bundle changes</td>\n</tr>\n<tr>\n<td><code>sourceApp</code> / <code>sourceKind</code></td>\n<td><code>string | null</code></td>\n<td>pulled from <code>metadata</code> (the filterable dimensions)</td>\n</tr>\n<tr>\n<td><code>metadata</code></td>\n<td><code>Record&lt;string, unknown&gt;</code></td>\n<td>your free-form blob, round-tripped verbatim</td>\n</tr>\n</tbody></table>\n<h3 id=\"publish-options\"><code>publish</code> options</h3>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>title</code></td>\n<td><code>string</code></td>\n<td>required</td>\n</tr>\n<tr>\n<td><code>bundle</code></td>\n<td><code>Blob | ArrayBuffer</code></td>\n<td>required — the static-site <strong>zip</strong></td>\n</tr>\n<tr>\n<td><code>slug</code></td>\n<td><code>string?</code></td>\n<td>omit for a readable-random slug</td>\n</tr>\n<tr>\n<td><code>tags</code></td>\n<td><code>string[]?</code></td>\n<td></td>\n</tr>\n<tr>\n<td><code>access</code></td>\n<td><code>\"private\" | \"specific_people\" | \"whole_org\" | \"public_link\" | \"password\"?</code></td>\n<td>General access; defaults to <code>\"private\"</code> (Restricted: the people-and-teams list only). <code>\"specific_people\"</code> is a legacy alias of <code>\"private\"</code>; <code>\"password\"</code> remains a compatibility shorthand for public link + password</td>\n</tr>\n<tr>\n<td><code>password</code></td>\n<td><code>string?</code></td>\n<td>optional extra lock on any audience; required with the <code>\"password\"</code> shorthand</td>\n</tr>\n<tr>\n<td><code>expiresAt</code></td>\n<td><code>number?</code></td>\n<td>unix ms; the operator may require an expiry and cap how far out it may be</td>\n</tr>\n<tr>\n<td><code>metadata</code></td>\n<td><code>Record&lt;string, unknown&gt;?</code></td>\n<td>free-form structured state (<code>sourceApp</code>/<code>sourceKind</code>/<code>theme</code>/…), bounded in size</td>\n</tr>\n</tbody></table>\n<h3 id=\"update-options\"><code>update</code> options</h3>\n<p>Every field is optional — omitted fields leave the share unchanged. <code>password</code> and\n<code>expiresAt</code> accept <code>null</code> to explicitly clear them. Omit <code>bundle</code> to change only\nsettings/metadata (the URL and current version stay put).</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Type</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>bundle</code></td>\n<td><code>Blob | ArrayBuffer?</code></td>\n<td>a new static-site <strong>zip</strong> → a new immutable version at the <strong>same URL</strong></td>\n</tr>\n<tr>\n<td><code>title</code></td>\n<td><code>string?</code></td>\n<td></td>\n</tr>\n<tr>\n<td><code>tags</code></td>\n<td><code>string[]?</code></td>\n<td></td>\n</tr>\n<tr>\n<td><code>access</code></td>\n<td><code>\"private\" | \"specific_people\" | \"whole_org\" | \"public_link\" | \"password\"?</code></td>\n<td>re-checks the same operator gates as publish</td>\n</tr>\n<tr>\n<td><code>password</code></td>\n<td><code>string | null?</code></td>\n<td>set, or <code>null</code> to clear</td>\n</tr>\n<tr>\n<td><code>expiresAt</code></td>\n<td><code>number | null?</code></td>\n<td>set, or <code>null</code> to clear</td>\n</tr>\n<tr>\n<td><code>metadata</code></td>\n<td><code>Record&lt;string, unknown&gt;?</code></td>\n<td>replaces the stored blob</td>\n</tr>\n<tr>\n<td><code>expectedUpdatedAt</code></td>\n<td><code>number?</code></td>\n<td>compare-and-swap token from the last read; stale updates reject with <code>SHARE_CONFLICT</code></td>\n</tr>\n</tbody></table>\n<p><code>publish</code> and <code>update</code> each send one <code>multipart/form-data</code> request\n(<code>credentials: \"include\"</code>): a JSON <code>metadata</code> part plus (optionally, for <code>update</code>) the\nzip <code>bundle</code> part.</p>\n<h3 id=\"list-filter\"><code>list</code> filter</h3>\n<p><code>list()</code> returns every canvas record the viewer can currently manage as owner or editor,\nincluding draft, unpublished, expired, archived, and admin-disabled rows. Deleted\ncanvases are omitted. <code>publicationStatus</code> makes those lifecycle states explicit. Pass a\nfilter to narrow server-side:</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">canvases</span>.<span class=\"hljs-title function_\">list</span>({ <span class=\"hljs-attr\">sourceApp</span>: <span class=\"hljs-string\">\"product-roadmap\"</span> });\n<span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">canvases</span>.<span class=\"hljs-title function_\">list</span>({ <span class=\"hljs-attr\">sourceKind</span>: <span class=\"hljs-string\">\"roadmap-share\"</span>, <span class=\"hljs-attr\">tags</span>: [<span class=\"hljs-string\">\"q3\"</span>] });</code></pre>\n<p><code>{ sourceApp?, sourceKind?, tags? }</code> — <code>sourceApp</code>/<code>sourceKind</code> match the values inside\n<code>metadata</code>; <code>tags</code> matches shares carrying <strong>every</strong> listed tag.</p>\n<h2 id=\"update-revoke-and-status\">Update, revoke, and status</h2>\n<ul>\n<li><strong>Update is in place.</strong> <code>update</code> deploys a new immutable version to the <strong>same</strong>\ncanvas — the public URL never changes, and version history + one-click rollback are\npreserved. Use it instead of <code>publish</code> when re-sharing the same artifact.</li>\n<li><strong>Unpublish keeps the record.</strong> <code>revoke</code> makes the public URL unavailable (readers get\nnot-found) and stamps <code>revokedAt</code>, but the share <strong>stays in <code>list()</code></strong> as\n<code>status: \"revoked\"</code>. Calling <code>update</code> with a bundle publishes it again at the same\nURL and clears <code>revokedAt</code>. A settings-only update while unpublished rejects with\n<code>SHARE_REVOKED</code>.</li>\n<li><strong>Two independent facts, not one status.</strong> <code>accessMode</code> says who else can open the share\n(<code>restricted</code>, <code>whole_org</code>, <code>public_link</code>); <code>publicationStatus</code> says whether it is\npublished (<code>draft</code>, <code>published</code>, <code>expired</code>, <code>unpublished</code>, <code>archived</code>, <code>disabled</code>). A\n<code>restricted</code> share that is <code>published</code> is open to everyone on its people-and-teams list.\nThe older <code>status</code> field (<code>revoked</code> › <code>expired</code> › <code>private</code> › <code>live</code>) conflated the two\nand is kept only for existing clients: its <code>private</code> does <strong>not</strong> mean owner-only.</li>\n<li><strong>Reader isolation.</strong> A public/permitted reader receives only the shared static\ncontent — never <code>metadata</code>, <code>createdBy</code>, <code>status</code>, or any management field. Those are\nassembled only on the authenticated management API.</li>\n</ul>\n<h2 id=\"ownership-quotas-and-authorization\">Ownership, quotas, and authorization</h2>\n<ul>\n<li>The new share is owned by the <strong>viewer</strong> who called <code>publish</code>. It counts against\ntheir normal canvas ownership <strong>and</strong> a per-viewer authoring quota (a daily and an\nall-time cap the operator sets).</li>\n<li>Quota is consumed the moment the canvas is <strong>created</strong> by <code>publish</code> — a\n<code>PublishFailedError</code> still counts, since the canvas exists. <strong><code>update</code> does not consume\nquota</strong> (it edits an existing share). <code>revoke</code> does not refund quota.</li>\n<li><code>list</code> is scoped to shares the viewer currently manages as owner or editor. An\nadministrator may still update or revoke a known share id through the explicit admin\nallowance; an unauthorized id reads as <strong>not-found</strong>.</li>\n<li><code>expiresAt</code> uses the same share-expiry mechanism as the dashboard, so it only means\nsomething for people other than the owner and editors.</li>\n</ul>\n<h2 id=\"errors\">Errors</h2>\n<p>Every method rejects with a <code>CanvasdropError</code> subclass — catch the one you care about,\nor read <code>err.code</code> / <code>err.status</code>.</p>\n<ul>\n<li>Not a signed-in member (a guest / public visitor) → <code>NotAuthenticatedError</code>\n(<code>status: 401</code>, <code>code: \"NOT_AUTHENTICATED\"</code>).</li>\n<li>Authoring off (backend off, the per-canvas toggle off, or the operator switch off) →\n<code>CapabilityDisabledError</code> (<code>status: 403</code>, <code>code: \"CAPABILITY_DISABLED\"</code>).</li>\n<li>Per-viewer daily or all-time cap hit → <code>QuotaExceededError</code> (<code>status: 429</code>,\n<code>code: \"QUOTA_EXCEEDED\"</code>, with a <code>scope</code> of <code>user_daily</code> or <code>user_total</code>).</li>\n<li>Invalid request (bundle or metadata too large, a disallowed access rung, a\nmissing/over-max expiry) → <code>CanvasdropError</code> (<code>status: 400</code>/<code>413</code>, <code>code: \"INVALID_BODY\"</code>).</li>\n<li>settings-only <code>update</code> on a share that's unpublished → <code>CanvasdropError</code>\n(<code>status: 409</code>, <code>code: \"SHARE_REVOKED\"</code>) — include a bundle to publish it again.</li>\n<li>An <code>expectedUpdatedAt</code> token is stale → <code>CanvasdropError</code>\n(<code>status: 409</code>, <code>code: \"SHARE_CONFLICT\"</code>) with the current share state; refresh before retrying.</li>\n<li>The canvas was created but its deploy or share-config failed →\n<code>PublishFailedError</code> (<code>status: 502</code>, <code>code: \"PUBLISH_FAILED\"</code>). Its <code>.id</code> is the new\ncanvas's id, so you can retry the publish or <code>canvasdrop.canvases.revoke(id)</code>.</li>\n<li><code>update</code> saved the settings but the bundle deploy then failed →\n<code>UpdatePartialError</code> (<code>status: 502</code>, <code>code: \"UPDATE_PARTIAL\"</code>). <code>.stage</code> names the\nfailed stage and <code>.current</code> is the saved share state, so nothing you changed is lost;\nretry the <code>update</code> with the bundle.</li>\n</ul>\n<p>See <a href=\"/docs/api/errors\">error codes</a>. The underlying HTTP endpoints live under\n<code>/v1/c/&lt;slug&gt;/authoring</code> — see the <a href=\"/docs/api/runtime-api\">Runtime API</a>.</p>\n",
    "headings": [
      {
        "id": "methods",
        "text": "Methods",
        "level": 2
      },
      {
        "id": "authoredcanvas",
        "text": "AuthoredCanvas",
        "level": 3
      },
      {
        "id": "publish-options",
        "text": "publish options",
        "level": 3
      },
      {
        "id": "update-options",
        "text": "update options",
        "level": 3
      },
      {
        "id": "list-filter",
        "text": "list filter",
        "level": 3
      },
      {
        "id": "update-revoke-and-status",
        "text": "Update, revoke, and status",
        "level": 2
      },
      {
        "id": "ownership-quotas-and-authorization",
        "text": "Ownership, quotas, and authorization",
        "level": 2
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      }
    ],
    "text": "Authoring Let a signed-in viewer of your canvas create and manage canvases from the page — as themselves, with real ownership and no secret in the browser. A share created here is a managed artifact : you publish it once, update it in place (same URL) as its content changes, list your shares with their live status, and revoke one when it's done. The canvasdrop.canvases namespace wraps publish , update , list , and revoke . This is the authoring capability. It is off by default and higher-privilege than the other primitives: the owner turns it on in the Backend tab and the operator must enable it for the instance. Guests and public-link visitors can't use it — creation needs a signed-in org member. // Create a durable share once. const share = await canvasdrop. canvases . publish ({ title : \"Team roadmap — Q3 snapshot\" , access : \"public_link\" , // audience; defaults to Restricted (`private`) password : \"optional-extra-lock\" , // independent of the audience tags : [ \"roadmap\" ], metadata : { sourceApp : \"product-roadmap\" , sourceKind : \"roadmap-share\" , theme : \"light\" }, expiresAt : Date . now () + 7 * 864e5 , // optional; the operator may require/limit it bundle : zipBlob, // a static-site zip (Blob or ArrayBuffer) }); // share.url is the live canvas; it now appears in the viewer's own dashboard. // Later — update IN PLACE. The URL never changes; a new version is deployed. await canvasdrop. canvases . update (share. id , { bundle : newZipBlob, // omit to change only settings/metadata metadata : { ...share. metadata , itemCount : 42 , generatedAt : Date . now () }, }); // The management view — filter to this app's shares. const mine = await canvasdrop. canvases . list ({ sourceApp : \"product-roadmap\" }); // → [{ id, url, title, status, access, expiresAt, updatedAt, metadata, ... }] // Retire a share. Its URL goes dead, but it STAYS in list() as status \"revoked\". await canvasdrop. canvases . revoke (share. id ); Methods Method Returns publish(options) Promise<AuthoredCanvas> — creates, deploys, and configures a new share update(id, options) Promise<AuthoredCanvas> — replaces the bundle and/or settings at the same URL list(filter?) Promise<AuthoredCanvas[]> — the viewer's shares (incl. revoked/expired), optionally filtered revoke(id) Promise<void> — make a share's public URL unavailable; it stays listed as revoked AuthoredCanvas What publish , update , and each list entry return — a share plus its management state: Field Type Notes id / url string stable id and public URL (the URL never changes across updates) title / tags string / string[] access string the persisted General-access value ( private , whole_org , public_link , or a legacy alias of private : specific_people / team ) accessMode \"restricted\" | \"whole_org\" | \"public_link\" who else can open it beyond the people-and-teams list, which applies at every value; private and its aliases read restricted . Branch on this, not on access publicationStatus \"draft\" | \"published\" | \"expired\" | \"unpublished\" | \"archived\" | \"disabled\" whether it is published, independent of the audience; precedence disabled › archived › unpublished (revoked) › draft (no version) › expired › published . Deleted canvases are omitted from every authoring response hasPassword boolean whether an additional password lock is currently set discoverability \"link_only\" | \"listed\" whether eligible org/team shares appear in discovery surfaces galleryTemplatable boolean whether gallery viewers may use the canvas as a template viewerRole \"owner\" | \"editor\" | \"admin\" why the current viewer may manage this record audienceSummary { count: number | null; names: string[] } safe summary of the viewer people and teams on the list ( count = both; names = the teams) status \"live\" | \"expired\" | \"revoked\" | \"private\" deprecated ; frozen for older clients. private means the persisted value is literally private (the people on the list can still open it), and the legacy aliases read live . Read accessMode + publicationStatus instead createdAt / updatedAt number unix ms expiresAt / revokedAt number | null share expiry; when it was revoked galleryListed boolean whether the canvas is explicitly listed in the gallery createdBy string the creator (owner) id version string | null the current version id — advances on every bundle deploy (a change signal) bundleUpdatedAt number legacy last-change stamp; use version to detect bundle changes sourceApp / sourceKind string | null pulled from metadata (the filterable dimensions) metadata Record<string, unknown> your free-form blob, round-tripped verbatim publish options Field Type Notes title string required bundle Blob | ArrayBuffer required — the static-site zip slug string? omit for a readable-random slug tags string[]? access \"private\" | \"specific_people\" | \"whole_org\" | \"public_link\" | \"password\"? General access; defaults to \"private\" (Restricted: the people-and-teams list only). \"specific_people\" is a legacy alias of \"private\" ; \"password\" remains a compatibility shorthand for public link + password password string? optional extra lock on any audience; required with the \"password\" shorthand expiresAt number? unix ms; the operator may require an expiry and cap how far out it may be metadata Record<string, unknown>? free-form structured state ( sourceApp / sourceKind / theme /…), bounded in size update options Every field is optional — omitted fields leave the share unchanged. password and expiresAt accept null to explicitly clear them. Omit bundle to change only settings/metadata (the URL and current version stay put). Field Type Notes bundle Blob | ArrayBuffer? a new static-site zip → a new immutable version at the same URL title string? tags string[]? access \"private\" | \"specific_people\" | \"whole_org\" | \"public_link\" | \"password\"? re-checks the same operator gates as publish password string | null? set, or null to clear expiresAt number | null? set, or null to clear metadata Record<string, unknown>? replaces the stored blob expectedUpdatedAt number? compare-and-swap token from the last read; stale updates reject with SHARE_CONFLICT publish and update each send one multipart/form-data request ( credentials: \"include\" ): a JSON metadata part plus (optionally, for update ) the zip bundle part. list filter list() returns every canvas record the viewer can currently manage as owner or editor, including draft, unpublished, expired, archived, and admin-disabled rows. Deleted canvases are omitted. publicationStatus makes those lifecycle states explicit. Pass a filter to narrow server-side: await canvasdrop. canvases . list ({ sourceApp : \"product-roadmap\" }); await canvasdrop. canvases . list ({ sourceKind : \"roadmap-share\" , tags : [ \"q3\" ] }); { sourceApp?, sourceKind?, tags? } — sourceApp / sourceKind match the values inside metadata ; tags matches shares carrying every listed tag. Update, revoke, and status Update is in place. update deploys a new immutable version to the same canvas — the public URL never changes, and version history + one-click rollback are preserved. Use it instead of publish when re-sharing the same artifact. Unpublish keeps the record. revoke makes the public URL unavailable (readers get not-found) and stamps revokedAt , but the share stays in list() as status: \"revoked\" . Calling update with a bundle publishes it again at the same URL and clears revokedAt . A settings-only update while unpublished rejects with SHARE_REVOKED . Two independent facts, not one status. accessMode says who else can open the share ( restricted , whole_org , public_link ); publicationStatus says whether it is published ( draft , published , expired , unpublished , archived , disabled ). A restricted share that is published is open to everyone on its people-and-teams list. The older status field ( revoked › expired › private › live ) conflated the two and is kept only for existing clients: its private does not mean owner-only. Reader isolation. A public/permitted reader receives only the shared static content — never metadata , createdBy , status , or any management field. Those are assembled only on the authenticated management API. Ownership, quotas, and authorization The new share is owned by the viewer who called publish . It counts against their normal canvas ownership and a per-viewer authoring quota (a daily and an all-time cap the operator sets). Quota is consumed the moment the canvas is created by publish — a PublishFailedError still counts, since the canvas exists. update does not consume quota (it edits an existing share). revoke does not refund quota. list is scoped to shares the viewer currently manages as owner or editor. An administrator may still update or revoke a known share id through the explicit admin allowance; an unauthorized id reads as not-found . expiresAt uses the same share-expiry mechanism as the dashboard, so it only means something for people other than the owner and editors. Errors Every method rejects with a CanvasdropError subclass — catch the one you care about, or read err.code / err.status . Not a signed-in member (a guest / public visitor) → NotAuthenticatedError ( status: 401 , code: \"NOT_AUTHENTICATED\" ). Authoring off (backend off, the per-canvas toggle off, or the operator switch off) → CapabilityDisabledError ( status: 403 , code: \"CAPABILITY_DISABLED\" ). Per-viewer daily or all-time cap hit → QuotaExceededError ( status: 429 , code: \"QUOTA_EXCEEDED\" , with a scope of user_daily or user_total ). Invalid request (bundle or metadata too large, a disallowed access rung, a missing/over-max expiry) → CanvasdropError ( status: 400 / 413 , code: \"INVALID_BODY\" ). settings-only update on a share that's unpublished → CanvasdropError ( status: 409 , code: \"SHARE_REVOKED\" ) — include a bundle to publish it again. An expectedUpdatedAt token is stale → CanvasdropError ( status: 409 , code: \"SHARE_CONFLICT\" ) with the current share state; refresh before retrying. The canvas was created but its deploy or share-config failed → PublishFailedError ( status: 502 , code: \"PUBLISH_FAILED\" ). Its .id is the new canvas's id, so you can retry the publish or canvasdrop.canvases.revoke(id) . update saved the settings but the bundle deploy then failed → UpdatePartialError ( status: 502 , code: \"UPDATE_PARTIAL\" ). .stage names the failed stage and .current is the saved share state, so nothing you changed is lost; retry the update with the bundle. See error codes . The underlying HTTP endpoints live under /v1/c/<slug>/authoring — see the Runtime API ."
  },
  {
    "path": "api/deploy-api",
    "section": "API reference",
    "title": "Deploy API",
    "html": "<h1 id=\"deploy-api\">Deploy API</h1>\n<p>Put a canvas live over HTTP from a script, CI, or an AI agent, with no dashboard\nsession. You authenticate with the canvas's <strong>secret key</strong> as a Bearer token. A key\noperates only on its own canvas, and every response has a stable, machine-readable\nshape so a client can repair and retry without a human. This page covers the whole\nkeyed loop: publish (one ZIP or a staged set of files), verify what shipped through\nthe server, roll back, and unpublish.</p>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-comment\"># Publish a ZIP.</span>\ncurl -fsS -X PUT <span class=\"hljs-string\">\"{base}/v1/canvases/{id}/deploy\"</span> \\\n  -H <span class=\"hljs-string\">\"Authorization: Bearer <span class=\"hljs-variable\">$CANVAS_KEY</span>\"</span> \\\n  --data-binary @site.zip\n<span class=\"hljs-comment\"># 200 {\"url\":\"&lt;canvas URL&gt;\",\"version\":7,\"fileCount\":12,\"totalBytes\":348201,\"warnings\":[]}</span>\n\n<span class=\"hljs-comment\"># Read back the live manifest to confirm what is being served.</span>\ncurl -fsS <span class=\"hljs-string\">\"{base}/v1/canvases/{id}/files\"</span> \\\n  -H <span class=\"hljs-string\">\"Authorization: Bearer <span class=\"hljs-variable\">$CANVAS_KEY</span>\"</span>\n<span class=\"hljs-comment\"># 200 {\"version\":7,\"fileCount\":12,\"files\":[{\"path\":\"index.html\",\"size\":1280,\"mime\":\"text/html; charset=utf-8\",\"hash\":\"9f86d0...\"}, ...]}</span></code></pre>\n<p>Base path: <code>{base}/v1/canvases/{id}</code>. <code>{id}</code> is the canvas id, not the slug.</p>\n<blockquote>\n<p><strong>Auth:</strong> <code>Authorization: Bearer cd_...</code>, the canvas secret key. It is shown once:\nwhen the canvas is created (<code>POST /api/canvases</code> answers <code>201</code> with an <code>apiKey</code>\nfield; the MCP <code>create_canvas</code> tool returns it too) or regenerated\n(<code>POST /api/canvases/{id}/regenerate-key</code>, MCP <code>regenerate_deploy_key</code>). The server\nstores only its SHA-256 hash, and a clone never inherits the source canvas's key.\nThis path takes no cookies and no CORS; it is separate from the session auth the\n<a href=\"/docs/api/runtime-api\">Runtime API</a> and browser SDK use.</p>\n</blockquote>\n<blockquote>\n<p><strong>What is <code>{base}</code>?</strong> The host serving the Deploy API: <code>CANVAS_DROP_API_BASE_URL</code>,\nwhich defaults to the instance base URL. Operators set it only when the API is\nrouted on a different host than the canvases (for example canvases at\n<code>{slug}.canvases.example.com</code> and the API at <code>api.canvases.example.com</code> in\n<code>subdomain</code> mode), so do not assume it equals the dashboard host. You never have to\nguess: over <a href=\"/docs/agents/mcp\">MCP</a>, <code>create_canvas</code> and <code>regenerate_deploy_key</code>\n(with the real key filled in) and <code>get_canvas</code> (with <code>$CANVAS_KEY</code>) return a\n<code>deploy</code> block holding the canvas's exact <code>apiBase</code>, the ZIP, staged-upload and\nread-back endpoints, and a copy-paste <code>curl</code> command.</p>\n</blockquote>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Path</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>PUT</code></td>\n<td><code>/v1/canvases/{id}/deploy</code></td>\n<td>Publish a live version from a ZIP body</td>\n</tr>\n<tr>\n<td><code>POST</code></td>\n<td><code>/v1/canvases/{id}/uploads</code></td>\n<td>Open a staged upload from a manifest</td>\n</tr>\n<tr>\n<td><code>PUT</code></td>\n<td><code>/v1/canvases/{id}/uploads/{uploadId}/blobs/{hash}</code></td>\n<td>Stage one file's bytes</td>\n</tr>\n<tr>\n<td><code>POST</code></td>\n<td><code>/v1/canvases/{id}/uploads/{uploadId}/finalize</code></td>\n<td>Publish from the staged upload</td>\n</tr>\n<tr>\n<td><code>GET</code></td>\n<td><code>/v1/canvases/{id}</code></td>\n<td>Canvas metadata and publication state</td>\n</tr>\n<tr>\n<td><code>GET</code></td>\n<td><code>/v1/canvases/{id}/versions</code></td>\n<td>List versions, newest first</td>\n</tr>\n<tr>\n<td><code>GET</code></td>\n<td><code>/v1/canvases/{id}/files</code></td>\n<td>Read back the live version (verify a deploy)</td>\n</tr>\n<tr>\n<td><code>POST</code></td>\n<td><code>/v1/canvases/{id}/rollback</code></td>\n<td>Make a prior ready version current</td>\n</tr>\n<tr>\n<td><code>POST</code></td>\n<td><code>/v1/canvases/{id}/unpublish</code></td>\n<td>Take the canvas back to Draft</td>\n</tr>\n</tbody></table>\n<p><strong>Deploy means live.</strong> A deploy creates a new version and points the canvas at it in\none step; there is no draft to publish afterwards. If the canvas has a browser draft,\nthe draft is synced to the new version, or flagged stale when it holds unpublished\nedits; a deploy never overwrites those edits. Deploys carry static files only: the\nkey cannot switch on capabilities. An owner or editor enables Backend and the\nprimitives the canvas needs (KV, files, AI, realtime) on the canvas's Backend tab or\nwith the <code>set_capabilities</code> MCP tool.</p>\n<p><strong>Attribution.</strong> There is no user on this path, so every write (deploy, rollback,\nunpublish) is audited as the canvas <strong>owner</strong>, and a version's <code>createdBy</code> is the\nowner's id, whoever holds the key.</p>\n<h2 id=\"deploy-a-version\">Deploy a version</h2>\n<pre><code class=\"hljs\">PUT {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}/deploy\nAuthorization: Bearer cd_...\nContent-Type: application/zip\n\n&lt;ZIP bytes&gt;</code></pre>\n<p>The body is read as a <strong>ZIP</strong> regardless of <code>Content-Type</code> (a tar archive fails with\n<code>INVALID_ZIP</code>). Put <code>index.html</code> at the archive root. Dotfiles and dot-directories\n(<code>.git/</code>, <code>.env</code>) and directory entries are dropped. The ZIP reader rejects an entry\nname with <code>..</code> segments or a leading <code>/</code> (<code>ZIP_SLIP_REJECTED</code>), and an entry that\ndeclares, or inflates to, more than 25 MB is rejected before it can exhaust memory\n(<code>ZIP_BOMB_REJECTED</code>). The version's <code>source</code> is <code>\"api\"</code>.</p>\n<p><strong>Success, <code>200</code></strong> (<code>DeployResult</code>, the same shape on every deploy path):</p>\n<pre><code class=\"hljs language-json\"><span class=\"hljs-punctuation\">{</span>\n  <span class=\"hljs-attr\">\"url\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"&lt;canvas URL&gt;\"</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"version\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">7</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"fileCount\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">12</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"totalBytes\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">348201</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"warnings\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-punctuation\">[</span><span class=\"hljs-string\">\"legacy.php will be served as text/plain\"</span><span class=\"hljs-punctuation\">]</span>\n<span class=\"hljs-punctuation\">}</span></code></pre>\n<ul>\n<li><code>url</code>: the canvas URL (<code>{scheme}//{slug}.{host}/</code> in <code>subdomain</code> mode,\n<code>{base}/c/{slug}/</code> in <code>path</code> mode).</li>\n<li><code>version</code>: the new version number; it only ever increases for a canvas.</li>\n<li><code>fileCount</code> / <code>totalBytes</code>: files and bytes in the version, after stripping.</li>\n<li><code>warnings[]</code>: non-fatal notices. Three exist: <code>&lt;path&gt; will be served as text/plain</code>\n(the extension is unknown, or is a server-side script type such as <code>.php</code>, so the\ntype is downgraded); <code>&lt;path&gt; may contain a canvas API key</code> (a text file matches the\n<code>cd_...</code> key shape; keys are server-side only, so remove it and redeploy); and a\nno-<code>index.html</code> notice, because the canvas root will 404 (a deploy whose only HTML\nfile has another name is served at the root and is not flagged). Warnings never\nfail the deploy.</li>\n</ul>\n<p><strong>Limits:</strong> 100 MB per canvas, 25 MB per file, 2000 files, enforced while the archive\nstreams. The request body is also capped before buffering at 110 MB (canvas cap plus\n10 MB of archive headroom); an over-cap body returns <code>413 { \"code\": \"CANVAS_TOO_LARGE\" }</code>.\nAn empty body returns <code>400 { \"code\": \"EMPTY_DEPLOY\" }</code>. Every other validation failure\non this route is a <code>400</code> with the <a href=\"#errors\">error shape</a>. A failed deploy never touches\nthe live version.</p>\n<p><strong>Retention:</strong> the 10 most recent ready versions are kept per canvas; older ones are\npruned and can no longer be rolled back to.</p>\n<p><strong>Rate limit:</strong> when rate limiting is on (<code>CANVAS_DROP_RATELIMIT_ENABLED</code>, default\n<code>true</code>), the deploy class throttles <code>PUT .../deploy</code>, <code>POST .../uploads</code> (begin),\n<code>POST .../uploads/{uploadId}/finalize</code>, and <code>POST .../rollback</code> per canvas, keyed\nafter the key is verified. The default is 10 per minute\n(<code>CANVAS_DROP_RATELIMIT_DEPLOY_PER_MIN</code>). Over the limit returns\n<code>429 { \"error\": \"rate_limited\" }</code> with a <code>Retry-After</code> header. Blob staging, the\n<code>GET</code> routes, and unpublish are not throttled.</p>\n<h2 id=\"staged-upload-large-or-incremental\">Staged upload (large or incremental)</h2>\n<p><code>PUT .../deploy</code> sends the whole archive in one request. For large canvases, or when\nyou redeploy often and want to send only what changed, use the three-step staged\nflow. Bytes go straight to the server (never base64'd through an agent's context),\nand blobs are content-addressed, so a file the canvas already stores is not uploaded\nagain.</p>\n<p><strong>1. Begin.</strong> Send the full manifest: each file's canvas-relative <code>path</code>, the <code>hash</code>\n(lowercase sha256 hex of its bytes, 64 characters), and its <code>size</code> in bytes (<code>0</code> is\nallowed).</p>\n<pre><code class=\"hljs\">POST {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}/uploads\nAuthorization: Bearer cd_...\nContent-Type: application/json\n\n{ <span class=\"hljs-string\">\"manifest\"</span>: [ { <span class=\"hljs-string\">\"path\"</span>: <span class=\"hljs-string\">\"index.html\"</span>, <span class=\"hljs-string\">\"hash\"</span>: <span class=\"hljs-string\">\"&lt;sha256&gt;\"</span>, <span class=\"hljs-string\">\"size\"</span>: 1234 } ] }</code></pre>\n<p>Returns the handle and the subset of hashes the canvas does not already store:</p>\n<pre><code class=\"hljs language-json\"><span class=\"hljs-punctuation\">{</span> <span class=\"hljs-attr\">\"uploadId\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"up_...\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"missingHashes\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-punctuation\">[</span><span class=\"hljs-string\">\"&lt;sha256&gt;\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-string\">\"...\"</span><span class=\"hljs-punctuation\">]</span> <span class=\"hljs-punctuation\">}</span></code></pre>\n<p>Dotfile and directory entries are skipped; a leading <code>./</code> or <code>/</code> on a path is\nstripped. A body that is not JSON, a <code>manifest</code> that is not an array, an empty\nmanifest, a manifest with no deployable entry left after skipping, a malformed\n<code>hash</code> or <code>size</code>, or two entries that share a hash but declare different sizes\nreturns <code>400 INVALID_MANIFEST</code>. A path with a <code>..</code> segment returns\n<code>400 ZIP_SLIP_REJECTED</code>. The declared sizes are checked against the caps up front:\n<code>413 FILE_TOO_LARGE</code>, <code>413 TOO_MANY_FILES</code>, or <code>413 CANVAS_TOO_LARGE</code>.</p>\n<p><strong>2. Stage each missing blob.</strong> Raw bytes; the path is irrelevant here because blobs\nare addressed by <code>{hash}</code>:</p>\n<pre><code class=\"hljs\">PUT {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}/uploads/{uploadId}/blobs/{<span class=\"hljs-built_in\">hash</span>}\nAuthorization: Bearer cd_...\n\n&lt;raw file bytes&gt;</code></pre>\n<p><code>204</code> on success. A blob is capped at 25 MB (<code>413 FILE_TOO_LARGE</code>), the bytes must\nhash to <code>{hash}</code> (else <code>400 BLOB_HASH_MISMATCH</code>), the hash must appear in the begin\nmanifest (else <code>400 UPLOAD_UNEXPECTED_BLOB</code>), and the byte count must match the size\ndeclared for that hash (else <code>400 BLOB_HASH_MISMATCH</code>). A handle that is unknown, was\nminted for a different canvas, or was begun by a different actor returns\n<code>404 UPLOAD_HANDLE_INVALID</code> (one code, no existence leak). Staging a blob whose text\nmatches the <code>cd_...</code> key shape is accepted; the API-key lint is logged on the server\nrather than returned on this channel.</p>\n<p><strong>3. Finalize</strong> to publish a version from the staged manifest:</p>\n<pre><code class=\"hljs\">POST {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}/uploads/{uploadId}/finalize\nAuthorization: Bearer cd_...</code></pre>\n<p>Returns the same <code>DeployResult</code> as <code>PUT .../deploy</code>; the version's <code>source</code> is\n<code>\"upload\"</code>. Finalize re-checks that the actor who began the upload is still active\nand still an owner or editor of the canvas (else <code>404 UPLOAD_HANDLE_INVALID</code>). The\nhandle is <strong>single-use</strong> and lives 15 minutes from begin (then <code>400 UPLOAD_EXPIRED</code>\non stage or finalize). A finalize before every blob is staged returns\n<code>400 UPLOAD_MISSING_BLOB</code>; stage the rest and call finalize again. The handle is\nconsumed when finalize commits a version, after which any further stage or finalize\nreturns <code>409 UPLOAD_ALREADY_FINALIZED</code> and a fresh begin is required. Two concurrent\nfinalizes of one handle: the second gets <code>409 UPLOAD_IN_PROGRESS</code> while the first\nholds a 60-second lease. The <code>warnings</code> on this path cover the <code>text/plain</code>\ndowngrade and the missing <code>index.html</code>.</p>\n<h2 id=\"get-a-canvas\">Get a canvas</h2>\n<pre><code class=\"hljs\">GET {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}\nAuthorization: Bearer cd_...</code></pre>\n<p>Returns <code>{ id, slug, url, title, status, publicationState, accessMode, currentVersionId }</code>.\nA key resolves only an active canvas (an archived, disabled, or deleted canvas's key fails\nauth with <code>401</code>), so <code>status</code> is always <code>\"active\"</code> here and <code>publicationState</code> is\n<code>\"published\"</code> when a live version exists, otherwise <code>\"draft\"</code>. To confirm a canvas is\nlive, check <code>publicationState === \"published\"</code>; you do not need to interpret\n<code>currentVersionId</code> yourself. <code>accessMode</code> is the audience — <code>restricted</code> (only the\npeople-and-teams list), <code>whole_org</code>, or <code>public_link</code> — the same derived value the\nmanagement API and MCP return.</p>\n<h2 id=\"list-versions\">List versions</h2>\n<pre><code class=\"hljs\">GET {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}/versions\nAuthorization: Bearer cd_...</code></pre>\n<p>Returns <code>{ versions: [...] }</code>, newest first. Each entry is\n<code>{ number, source, status, createdBy, createdAt, fileCount, totalBytes, current }</code>.\n<code>current</code> marks the live version. <code>source</code> records how the version was made:\n<code>\"api\"</code> for <code>PUT .../deploy</code>, <code>\"upload\"</code> for the staged flow, <code>\"editor\"</code> for a publish\nfrom the browser editor, and <code>\"zip\"</code>, <code>\"folder\"</code>, or <code>\"paste\"</code> for dashboard deploys.\n<code>status</code> is <code>\"pending\"</code> while a version is being written and <code>\"ready\"</code> once\ncommitted; only a <code>ready</code> version can be a rollback target.</p>\n<h2 id=\"verify-a-deploy-read-back-the-live-version\">Verify a deploy (read back the live version)</h2>\n<p>The canvas URL is access-controlled, so a keyed client cannot fetch it to check what\nshipped: an unauthenticated request gets the login page. Read the live version\nthrough the key instead.</p>\n<pre><code class=\"hljs\">GET {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}/files\nAuthorization: Bearer cd_...</code></pre>\n<p>With no query, returns the live version's manifest, sorted by path:</p>\n<pre><code class=\"hljs language-json\"><span class=\"hljs-punctuation\">{</span> <span class=\"hljs-attr\">\"version\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">7</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"fileCount\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">3</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"files\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-punctuation\">[</span>\n  <span class=\"hljs-punctuation\">{</span> <span class=\"hljs-attr\">\"path\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"index.html\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"size\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">1280</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"mime\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"text/html; charset=utf-8\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"hash\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"9f86d0...\"</span> <span class=\"hljs-punctuation\">}</span>\n<span class=\"hljs-punctuation\">]</span> <span class=\"hljs-punctuation\">}</span></code></pre>\n<p>Add <code>?path=</code> (the path exactly as it appears in the listing) to get one file's\n<strong>raw bytes</strong>: the body is the file itself, with its <code>Content-Type</code>, an <code>ETag</code> equal\nto the quoted sha256 of the content, and <code>Cache-Control: no-store</code>. Pipe it to a\nchecksum to confirm the bytes match what you deployed:</p>\n<pre><code class=\"hljs language-bash\">curl -fsS <span class=\"hljs-string\">\"{base}/v1/canvases/{id}/files?path=index.html\"</span> \\\n  -H <span class=\"hljs-string\">\"Authorization: Bearer <span class=\"hljs-variable\">$CANVAS_KEY</span>\"</span> | <span class=\"hljs-built_in\">sha256sum</span></code></pre>\n<p><code>404 NOT_PUBLISHED</code> when the canvas has no live version; <code>404 NOT_FOUND</code> when the\npath is not in the live manifest. There is no size cap on this read: you stream the\nbody. (The MCP <a href=\"/docs/agents/mcp\"><code>get_canvas_file</code></a> tool is the identity-scoped\nequivalent; it inlines content up to 256 KiB and returns hash-only metadata above\nthat.)</p>\n<h2 id=\"roll-back\">Roll back</h2>\n<pre><code class=\"hljs\">POST {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}/rollback\nAuthorization: Bearer cd_...\nContent-Type: application/json\n\n{ <span class=\"hljs-string\">\"version\"</span>: 5 }</code></pre>\n<p>Makes a prior ready version current and returns\n<code>200 { \"url\": \"&lt;canvas URL&gt;\", \"version\": 5 }</code>. <code>version</code> must be a positive integer;\na missing or invalid body returns <code>400 { \"error\": \"invalid_body\" }</code>. A number with no\nready version behind it (never existed, or pruned out of the 10-version window)\nreturns <code>404 { \"code\": \"INVALID_PATH\" }</code>. If the target is pruned between selection\nand the pointer swap you get <code>409 { \"code\": \"VERSION_UNAVAILABLE\" }</code>: list versions\nagain and retry. Rollback moves only the live pointer; it does not change sharing\nsettings. It shares the deploy-class rate limit.</p>\n<h2 id=\"unpublish\">Unpublish</h2>\n<pre><code class=\"hljs\">POST {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}/unpublish\nAuthorization: Bearer cd_...</code></pre>\n<p>Takes the canvas back to <strong>Draft</strong>. The live version pointer is cleared, so the canvas\nURL stops serving, and any live realtime sockets are dropped. Publication settings\nare reset at the same time: access returns to <strong>Restricted</strong>, <strong>List for people with\naccess</strong> is turned off, any share expiry is cleared, and the canvas is unlisted from\nthe gallery. The draft and the version history are kept. Publish again with a new\ndeploy, or by rolling back to a kept version; either way the canvas comes back\nRestricted, so re-share it from the dashboard or with the <code>update_canvas</code> MCP tool.</p>\n<p><strong>Success, <code>200</code>:</strong> <code>{ \"url\": \"&lt;canvas URL&gt;\", \"publicationState\": \"draft\", \"currentVersionId\": null }</code>.</p>\n<p>Unpublishing a canvas that is not currently published returns\n<code>409 { \"code\": \"CANNOT_UNPUBLISH\" }</code>.</p>\n<h2 id=\"errors\">Errors</h2>\n<p>Branch on <code>code</code> (or <code>error</code>), never on message text. Two body shapes appear on these\nroutes.</p>\n<p><strong>Auth, body validation, and throttling</strong> use <code>{ \"error\": \"...\" }</code>:</p>\n<table>\n<thead>\n<tr>\n<th>Status</th>\n<th>Body</th>\n<th>Cause</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>401</code></td>\n<td><code>{ \"error\": \"unauthorized\" }</code></td>\n<td>Missing or unknown Bearer key, or a key whose canvas is archived, disabled, or deleted</td>\n</tr>\n<tr>\n<td><code>403</code></td>\n<td><code>{ \"error\": \"unauthorized\" }</code></td>\n<td>The key belongs to a different canvas than <code>{id}</code></td>\n</tr>\n<tr>\n<td><code>400</code></td>\n<td><code>{ \"error\": \"invalid_body\" }</code></td>\n<td>Rollback body without a positive-integer <code>version</code></td>\n</tr>\n<tr>\n<td><code>429</code></td>\n<td><code>{ \"error\": \"rate_limited\" }</code></td>\n<td>Over the deploy-class limit for this canvas; honor <code>Retry-After</code></td>\n</tr>\n</tbody></table>\n<p><strong>Deploy and lifecycle failures</strong> use a stable code plus a human message, and name\nthe offending file when there is one:</p>\n<pre><code class=\"hljs language-json\"><span class=\"hljs-punctuation\">{</span> <span class=\"hljs-attr\">\"code\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"&lt;code&gt;\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"message\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"...\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"path\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"&lt;offending path, optional&gt;\"</span> <span class=\"hljs-punctuation\">}</span></code></pre>\n<p>On <code>PUT .../deploy</code> every validation code is a <code>400</code>, except <code>CANVAS_TOO_LARGE</code>, which\nis a <code>413</code> when the body is rejected before buffering:</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Cause</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>EMPTY_DEPLOY</code></td>\n<td>Empty body, or an archive with no deployable files once dotfiles and directory entries are stripped</td>\n</tr>\n<tr>\n<td><code>INVALID_ZIP</code></td>\n<td>The body is not a readable ZIP (a tar archive lands here), or an entry name contains a backslash</td>\n</tr>\n<tr>\n<td><code>ZIP_SLIP_REJECTED</code></td>\n<td>An entry name with <code>..</code> segments or a leading <code>/</code></td>\n</tr>\n<tr>\n<td><code>ZIP_BOMB_REJECTED</code></td>\n<td>An entry declares, or inflates to, more than 25 MB (the per-file cap; inside a ZIP it never surfaces as <code>FILE_TOO_LARGE</code>)</td>\n</tr>\n<tr>\n<td><code>TOO_MANY_FILES</code></td>\n<td>More than 2000 files</td>\n</tr>\n<tr>\n<td><code>CANVAS_TOO_LARGE</code></td>\n<td>More than 100 MB in total (<code>413</code> when caught at the body cap)</td>\n</tr>\n</tbody></table>\n<p>The staged-upload routes map the same shape to richer statuses:</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Status</th>\n<th>Cause</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>INVALID_MANIFEST</code></td>\n<td><code>400</code></td>\n<td>Body not JSON; <code>manifest</code> not an array, or empty, or with no deployable entry left after dotfiles are skipped; a <code>hash</code> that is not 64 lowercase hex characters; a <code>size</code> that is not a non-negative number; or one hash declared with two sizes</td>\n</tr>\n<tr>\n<td><code>ZIP_SLIP_REJECTED</code></td>\n<td><code>400</code></td>\n<td>A manifest path with a <code>..</code> segment</td>\n</tr>\n<tr>\n<td><code>INVALID_PATH</code></td>\n<td><code>400</code></td>\n<td>A manifest path still absolute after one leading <code>/</code> or <code>./</code> is stripped (<code>//a.js</code>); make paths relative</td>\n</tr>\n<tr>\n<td><code>FILE_TOO_LARGE</code>, <code>TOO_MANY_FILES</code>, <code>CANVAS_TOO_LARGE</code></td>\n<td><code>413</code></td>\n<td>A cap exceeded: by declared size at begin, by actual bytes at stage, or by the manifest sum at finalize</td>\n</tr>\n<tr>\n<td><code>UPLOAD_HANDLE_INVALID</code></td>\n<td><code>404</code></td>\n<td>Unknown handle, a different canvas, a different actor, or an actor no longer active or no longer owner/editor at finalize</td>\n</tr>\n<tr>\n<td><code>UPLOAD_EXPIRED</code></td>\n<td><code>400</code></td>\n<td>More than 15 minutes since begin</td>\n</tr>\n<tr>\n<td><code>UPLOAD_UNEXPECTED_BLOB</code></td>\n<td><code>400</code></td>\n<td>The staged hash is not in the begin manifest</td>\n</tr>\n<tr>\n<td><code>BLOB_HASH_MISMATCH</code></td>\n<td><code>400</code></td>\n<td>The bytes do not hash to <code>{hash}</code>, or their size differs from the declared size</td>\n</tr>\n<tr>\n<td><code>UPLOAD_MISSING_BLOB</code></td>\n<td><code>400</code></td>\n<td>Finalize before every manifest hash is staged; stage the rest and retry</td>\n</tr>\n<tr>\n<td><code>UPLOAD_IN_PROGRESS</code></td>\n<td><code>409</code></td>\n<td>Another finalize holds the lease; retry shortly</td>\n</tr>\n<tr>\n<td><code>UPLOAD_ALREADY_FINALIZED</code></td>\n<td><code>409</code></td>\n<td>The handle was consumed; begin again</td>\n</tr>\n</tbody></table>\n<p>The remaining routes use the same shape:</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Status</th>\n<th>Route and cause</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>INVALID_PATH</code></td>\n<td><code>404</code></td>\n<td>Rollback: no ready version with that number</td>\n</tr>\n<tr>\n<td><code>VERSION_UNAVAILABLE</code></td>\n<td><code>409</code></td>\n<td>Rollback: the target was pruned during the swap; refresh and retry</td>\n</tr>\n<tr>\n<td><code>CANNOT_UNPUBLISH</code></td>\n<td><code>409</code></td>\n<td>Unpublish: the canvas is not currently published</td>\n</tr>\n<tr>\n<td><code>NOT_PUBLISHED</code></td>\n<td><code>404</code></td>\n<td>Read-back: no live version</td>\n</tr>\n<tr>\n<td><code>NOT_FOUND</code></td>\n<td><code>404</code></td>\n<td>Read-back: <code>?path=</code> is not in the live manifest</td>\n</tr>\n</tbody></table>\n<p>The MCP deploy tools (<code>deploy_canvas</code>, <code>begin_deploy</code>, <code>add_files</code>,\n<code>finalize_deploy</code>, <code>rollback_canvas</code>, <code>unpublish_canvas</code>) wrap the same service layer\nand surface the same codes; <code>INVALID_ENCODING</code> belongs to that channel only (a\n<code>files[]</code> entry with an encoding other than <code>utf8</code> or <code>base64</code>) and never occurs on\nthese HTTP routes. Codes returned to canvases at runtime are a separate set; see\n<a href=\"/docs/api/errors\">Error codes</a>.</p>\n",
    "headings": [
      {
        "id": "deploy-a-version",
        "text": "Deploy a version",
        "level": 2
      },
      {
        "id": "staged-upload-large-or-incremental",
        "text": "Staged upload (large or incremental)",
        "level": 2
      },
      {
        "id": "get-a-canvas",
        "text": "Get a canvas",
        "level": 2
      },
      {
        "id": "list-versions",
        "text": "List versions",
        "level": 2
      },
      {
        "id": "verify-a-deploy-read-back-the-live-version",
        "text": "Verify a deploy (read back the live version)",
        "level": 2
      },
      {
        "id": "roll-back",
        "text": "Roll back",
        "level": 2
      },
      {
        "id": "unpublish",
        "text": "Unpublish",
        "level": 2
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      }
    ],
    "text": "Deploy API Put a canvas live over HTTP from a script, CI, or an AI agent, with no dashboard session. You authenticate with the canvas's secret key as a Bearer token. A key operates only on its own canvas, and every response has a stable, machine-readable shape so a client can repair and retry without a human. This page covers the whole keyed loop: publish (one ZIP or a staged set of files), verify what shipped through the server, roll back, and unpublish. # Publish a ZIP. curl -fsS -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip # 200 {\"url\":\"<canvas URL>\",\"version\":7,\"fileCount\":12,\"totalBytes\":348201,\"warnings\":[]} # Read back the live manifest to confirm what is being served. curl -fsS \"{base}/v1/canvases/{id}/files\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" # 200 {\"version\":7,\"fileCount\":12,\"files\":[{\"path\":\"index.html\",\"size\":1280,\"mime\":\"text/html; charset=utf-8\",\"hash\":\"9f86d0...\"}, ...]} Base path: {base}/v1/canvases/{id} . {id} is the canvas id, not the slug. Auth: Authorization: Bearer cd_... , the canvas secret key. It is shown once: when the canvas is created ( POST /api/canvases answers 201 with an apiKey field; the MCP create_canvas tool returns it too) or regenerated ( POST /api/canvases/{id}/regenerate-key , MCP regenerate_deploy_key ). The server stores only its SHA-256 hash, and a clone never inherits the source canvas's key. This path takes no cookies and no CORS; it is separate from the session auth the Runtime API and browser SDK use. What is {base} ? The host serving the Deploy API: CANVAS_DROP_API_BASE_URL , which defaults to the instance base URL. Operators set it only when the API is routed on a different host than the canvases (for example canvases at {slug}.canvases.example.com and the API at api.canvases.example.com in subdomain mode), so do not assume it equals the dashboard host. You never have to guess: over MCP , create_canvas and regenerate_deploy_key (with the real key filled in) and get_canvas (with $CANVAS_KEY ) return a deploy block holding the canvas's exact apiBase , the ZIP, staged-upload and read-back endpoints, and a copy-paste curl command. Method Path Purpose PUT /v1/canvases/{id}/deploy Publish a live version from a ZIP body POST /v1/canvases/{id}/uploads Open a staged upload from a manifest PUT /v1/canvases/{id}/uploads/{uploadId}/blobs/{hash} Stage one file's bytes POST /v1/canvases/{id}/uploads/{uploadId}/finalize Publish from the staged upload GET /v1/canvases/{id} Canvas metadata and publication state GET /v1/canvases/{id}/versions List versions, newest first GET /v1/canvases/{id}/files Read back the live version (verify a deploy) POST /v1/canvases/{id}/rollback Make a prior ready version current POST /v1/canvases/{id}/unpublish Take the canvas back to Draft Deploy means live. A deploy creates a new version and points the canvas at it in one step; there is no draft to publish afterwards. If the canvas has a browser draft, the draft is synced to the new version, or flagged stale when it holds unpublished edits; a deploy never overwrites those edits. Deploys carry static files only: the key cannot switch on capabilities. An owner or editor enables Backend and the primitives the canvas needs (KV, files, AI, realtime) on the canvas's Backend tab or with the set_capabilities MCP tool. Attribution. There is no user on this path, so every write (deploy, rollback, unpublish) is audited as the canvas owner , and a version's createdBy is the owner's id, whoever holds the key. Deploy a version PUT {base}/v1/canvases/{ id }/deploy Authorization: Bearer cd_... Content-Type: application/zip <ZIP bytes> The body is read as a ZIP regardless of Content-Type (a tar archive fails with INVALID_ZIP ). Put index.html at the archive root. Dotfiles and dot-directories ( .git/ , .env ) and directory entries are dropped. The ZIP reader rejects an entry name with .. segments or a leading / ( ZIP_SLIP_REJECTED ), and an entry that declares, or inflates to, more than 25 MB is rejected before it can exhaust memory ( ZIP_BOMB_REJECTED ). The version's source is \"api\" . Success, 200 ( DeployResult , the same shape on every deploy path): { \"url\" : \"<canvas URL>\" , \"version\" : 7 , \"fileCount\" : 12 , \"totalBytes\" : 348201 , \"warnings\" : [ \"legacy.php will be served as text/plain\" ] } url : the canvas URL ( {scheme}//{slug}.{host}/ in subdomain mode, {base}/c/{slug}/ in path mode). version : the new version number; it only ever increases for a canvas. fileCount / totalBytes : files and bytes in the version, after stripping. warnings[] : non-fatal notices. Three exist: <path> will be served as text/plain (the extension is unknown, or is a server-side script type such as .php , so the type is downgraded); <path> may contain a canvas API key (a text file matches the cd_... key shape; keys are server-side only, so remove it and redeploy); and a no- index.html notice, because the canvas root will 404 (a deploy whose only HTML file has another name is served at the root and is not flagged). Warnings never fail the deploy. Limits: 100 MB per canvas, 25 MB per file, 2000 files, enforced while the archive streams. The request body is also capped before buffering at 110 MB (canvas cap plus 10 MB of archive headroom); an over-cap body returns 413 { \"code\": \"CANVAS_TOO_LARGE\" } . An empty body returns 400 { \"code\": \"EMPTY_DEPLOY\" } . Every other validation failure on this route is a 400 with the error shape . A failed deploy never touches the live version. Retention: the 10 most recent ready versions are kept per canvas; older ones are pruned and can no longer be rolled back to. Rate limit: when rate limiting is on ( CANVAS_DROP_RATELIMIT_ENABLED , default true ), the deploy class throttles PUT .../deploy , POST .../uploads (begin), POST .../uploads/{uploadId}/finalize , and POST .../rollback per canvas, keyed after the key is verified. The default is 10 per minute ( CANVAS_DROP_RATELIMIT_DEPLOY_PER_MIN ). Over the limit returns 429 { \"error\": \"rate_limited\" } with a Retry-After header. Blob staging, the GET routes, and unpublish are not throttled. Staged upload (large or incremental) PUT .../deploy sends the whole archive in one request. For large canvases, or when you redeploy often and want to send only what changed, use the three-step staged flow. Bytes go straight to the server (never base64'd through an agent's context), and blobs are content-addressed, so a file the canvas already stores is not uploaded again. 1. Begin. Send the full manifest: each file's canvas-relative path , the hash (lowercase sha256 hex of its bytes, 64 characters), and its size in bytes ( 0 is allowed). POST {base}/v1/canvases/{ id }/uploads Authorization: Bearer cd_... Content-Type: application/json { \"manifest\" : [ { \"path\" : \"index.html\" , \"hash\" : \"<sha256>\" , \"size\" : 1234 } ] } Returns the handle and the subset of hashes the canvas does not already store: { \"uploadId\" : \"up_...\" , \"missingHashes\" : [ \"<sha256>\" , \"...\" ] } Dotfile and directory entries are skipped; a leading ./ or / on a path is stripped. A body that is not JSON, a manifest that is not an array, an empty manifest, a manifest with no deployable entry left after skipping, a malformed hash or size , or two entries that share a hash but declare different sizes returns 400 INVALID_MANIFEST . A path with a .. segment returns 400 ZIP_SLIP_REJECTED . The declared sizes are checked against the caps up front: 413 FILE_TOO_LARGE , 413 TOO_MANY_FILES , or 413 CANVAS_TOO_LARGE . 2. Stage each missing blob. Raw bytes; the path is irrelevant here because blobs are addressed by {hash} : PUT {base}/v1/canvases/{ id }/uploads/{uploadId}/blobs/{ hash } Authorization: Bearer cd_... <raw file bytes> 204 on success. A blob is capped at 25 MB ( 413 FILE_TOO_LARGE ), the bytes must hash to {hash} (else 400 BLOB_HASH_MISMATCH ), the hash must appear in the begin manifest (else 400 UPLOAD_UNEXPECTED_BLOB ), and the byte count must match the size declared for that hash (else 400 BLOB_HASH_MISMATCH ). A handle that is unknown, was minted for a different canvas, or was begun by a different actor returns 404 UPLOAD_HANDLE_INVALID (one code, no existence leak). Staging a blob whose text matches the cd_... key shape is accepted; the API-key lint is logged on the server rather than returned on this channel. 3. Finalize to publish a version from the staged manifest: POST {base}/v1/canvases/{ id }/uploads/{uploadId}/finalize Authorization: Bearer cd_... Returns the same DeployResult as PUT .../deploy ; the version's source is \"upload\" . Finalize re-checks that the actor who began the upload is still active and still an owner or editor of the canvas (else 404 UPLOAD_HANDLE_INVALID ). The handle is single-use and lives 15 minutes from begin (then 400 UPLOAD_EXPIRED on stage or finalize). A finalize before every blob is staged returns 400 UPLOAD_MISSING_BLOB ; stage the rest and call finalize again. The handle is consumed when finalize commits a version, after which any further stage or finalize returns 409 UPLOAD_ALREADY_FINALIZED and a fresh begin is required. Two concurrent finalizes of one handle: the second gets 409 UPLOAD_IN_PROGRESS while the first holds a 60-second lease. The warnings on this path cover the text/plain downgrade and the missing index.html . Get a canvas GET {base}/v1/canvases/{ id } Authorization: Bearer cd_... Returns { id, slug, url, title, status, publicationState, accessMode, currentVersionId } . A key resolves only an active canvas (an archived, disabled, or deleted canvas's key fails auth with 401 ), so status is always \"active\" here and publicationState is \"published\" when a live version exists, otherwise \"draft\" . To confirm a canvas is live, check publicationState === \"published\" ; you do not need to interpret currentVersionId yourself. accessMode is the audience — restricted (only the people-and-teams list), whole_org , or public_link — the same derived value the management API and MCP return. List versions GET {base}/v1/canvases/{ id }/versions Authorization: Bearer cd_... Returns { versions: [...] } , newest first. Each entry is { number, source, status, createdBy, createdAt, fileCount, totalBytes, current } . current marks the live version. source records how the version was made: \"api\" for PUT .../deploy , \"upload\" for the staged flow, \"editor\" for a publish from the browser editor, and \"zip\" , \"folder\" , or \"paste\" for dashboard deploys. status is \"pending\" while a version is being written and \"ready\" once committed; only a ready version can be a rollback target. Verify a deploy (read back the live version) The canvas URL is access-controlled, so a keyed client cannot fetch it to check what shipped: an unauthenticated request gets the login page. Read the live version through the key instead. GET {base}/v1/canvases/{ id }/files Authorization: Bearer cd_... With no query, returns the live version's manifest, sorted by path: { \"version\" : 7 , \"fileCount\" : 3 , \"files\" : [ { \"path\" : \"index.html\" , \"size\" : 1280 , \"mime\" : \"text/html; charset=utf-8\" , \"hash\" : \"9f86d0...\" } ] } Add ?path= (the path exactly as it appears in the listing) to get one file's raw bytes : the body is the file itself, with its Content-Type , an ETag equal to the quoted sha256 of the content, and Cache-Control: no-store . Pipe it to a checksum to confirm the bytes match what you deployed: curl -fsS \"{base}/v1/canvases/{id}/files?path=index.html\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" | sha256sum 404 NOT_PUBLISHED when the canvas has no live version; 404 NOT_FOUND when the path is not in the live manifest. There is no size cap on this read: you stream the body. (The MCP get_canvas_file tool is the identity-scoped equivalent; it inlines content up to 256 KiB and returns hash-only metadata above that.) Roll back POST {base}/v1/canvases/{ id }/rollback Authorization: Bearer cd_... Content-Type: application/json { \"version\" : 5 } Makes a prior ready version current and returns 200 { \"url\": \"<canvas URL>\", \"version\": 5 } . version must be a positive integer; a missing or invalid body returns 400 { \"error\": \"invalid_body\" } . A number with no ready version behind it (never existed, or pruned out of the 10-version window) returns 404 { \"code\": \"INVALID_PATH\" } . If the target is pruned between selection and the pointer swap you get 409 { \"code\": \"VERSION_UNAVAILABLE\" } : list versions again and retry. Rollback moves only the live pointer; it does not change sharing settings. It shares the deploy-class rate limit. Unpublish POST {base}/v1/canvases/{ id }/unpublish Authorization: Bearer cd_... Takes the canvas back to Draft . The live version pointer is cleared, so the canvas URL stops serving, and any live realtime sockets are dropped. Publication settings are reset at the same time: access returns to Restricted , List for people with access is turned off, any share expiry is cleared, and the canvas is unlisted from the gallery. The draft and the version history are kept. Publish again with a new deploy, or by rolling back to a kept version; either way the canvas comes back Restricted, so re-share it from the dashboard or with the update_canvas MCP tool. Success, 200 : { \"url\": \"<canvas URL>\", \"publicationState\": \"draft\", \"currentVersionId\": null } . Unpublishing a canvas that is not currently published returns 409 { \"code\": \"CANNOT_UNPUBLISH\" } . Errors Branch on code (or error ), never on message text. Two body shapes appear on these routes. Auth, body validation, and throttling use { \"error\": \"...\" } : Status Body Cause 401 { \"error\": \"unauthorized\" } Missing or unknown Bearer key, or a key whose canvas is archived, disabled, or deleted 403 { \"error\": \"unauthorized\" } The key belongs to a different canvas than {id} 400 { \"error\": \"invalid_body\" } Rollback body without a positive-integer version 429 { \"error\": \"rate_limited\" } Over the deploy-class limit for this canvas; honor Retry-After Deploy and lifecycle failures use a stable code plus a human message, and name the offending file when there is one: { \"code\" : \"<code>\" , \"message\" : \"...\" , \"path\" : \"<offending path, optional>\" } On PUT .../deploy every validation code is a 400 , except CANVAS_TOO_LARGE , which is a 413 when the body is rejected before buffering: Code Cause EMPTY_DEPLOY Empty body, or an archive with no deployable files once dotfiles and directory entries are stripped INVALID_ZIP The body is not a readable ZIP (a tar archive lands here), or an entry name contains a backslash ZIP_SLIP_REJECTED An entry name with .. segments or a leading / ZIP_BOMB_REJECTED An entry declares, or inflates to, more than 25 MB (the per-file cap; inside a ZIP it never surfaces as FILE_TOO_LARGE ) TOO_MANY_FILES More than 2000 files CANVAS_TOO_LARGE More than 100 MB in total ( 413 when caught at the body cap) The staged-upload routes map the same shape to richer statuses: Code Status Cause INVALID_MANIFEST 400 Body not JSON; manifest not an array, or empty, or with no deployable entry left after dotfiles are skipped; a hash that is not 64 lowercase hex characters; a size that is not a non-negative number; or one hash declared with two sizes ZIP_SLIP_REJECTED 400 A manifest path with a .. segment INVALID_PATH 400 A manifest path still absolute after one leading / or ./ is stripped ( //a.js ); make paths relative FILE_TOO_LARGE , TOO_MANY_FILES , CANVAS_TOO_LARGE 413 A cap exceeded: by declared size at begin, by actual bytes at stage, or by the manifest sum at finalize UPLOAD_HANDLE_INVALID 404 Unknown handle, a different canvas, a different actor, or an actor no longer active or no longer owner/editor at finalize UPLOAD_EXPIRED 400 More than 15 minutes since begin UPLOAD_UNEXPECTED_BLOB 400 The staged hash is not in the begin manifest BLOB_HASH_MISMATCH 400 The bytes do not hash to {hash} , or their size differs from the declared size UPLOAD_MISSING_BLOB 400 Finalize before every manifest hash is staged; stage the rest and retry UPLOAD_IN_PROGRESS 409 Another finalize holds the lease; retry shortly UPLOAD_ALREADY_FINALIZED 409 The handle was consumed; begin again The remaining routes use the same shape: Code Status Route and cause INVALID_PATH 404 Rollback: no ready version with that number VERSION_UNAVAILABLE 409 Rollback: the target was pruned during the swap; refresh and retry CANNOT_UNPUBLISH 409 Unpublish: the canvas is not currently published NOT_PUBLISHED 404 Read-back: no live version NOT_FOUND 404 Read-back: ?path= is not in the live manifest The MCP deploy tools ( deploy_canvas , begin_deploy , add_files , finalize_deploy , rollback_canvas , unpublish_canvas ) wrap the same service layer and surface the same codes; INVALID_ENCODING belongs to that channel only (a files[] entry with an encoding other than utf8 or base64 ) and never occurs on these HTTP routes. Codes returned to canvases at runtime are a separate set; see Error codes ."
  },
  {
    "path": "api/runtime-api",
    "section": "API reference",
    "title": "Runtime API",
    "html": "<h1 id=\"runtime-api\">Runtime API</h1>\n<p>The runtime API is the HTTP surface a canvas calls from the browser: the six fixed primitives\n(KV, files, AI, identity, realtime, and admin-granted Connections) plus authoring, each under <code>{base}/v1/c/{slug}</code>. The\n<a href=\"/docs/sdk/overview\">browser SDK</a> (<code>window.canvasdrop</code>, served at <code>{base}/sdk/v1.js</code>)\nbuilds every one of these requests, speaks the SSE and WebSocket wire formats, and maps\nerrors to typed exceptions, so reach for it first. Use this page when you need the raw\nroutes: reading the network tab, a non-JS client, or the exact shape a primitive returns.</p>\n<p>Requests are credentialed with the session cookie the browser already holds. Identity is\nresolved server-side from that session; the canvas never asserts who the viewer is.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-comment\">// Inside a canvas served in path mode at http://localhost:3000/c/quiet-otter-x7k2/</span>\n<span class=\"hljs-keyword\">const</span> api = <span class=\"hljs-string\">\"/v1/c/quiet-otter-x7k2\"</span>;\n\n<span class=\"hljs-keyword\">await</span> <span class=\"hljs-title function_\">fetch</span>(<span class=\"hljs-string\">`<span class=\"hljs-subst\">${api}</span>/kv/greeting`</span>, {\n  <span class=\"hljs-attr\">method</span>: <span class=\"hljs-string\">\"PUT\"</span>,\n  <span class=\"hljs-attr\">credentials</span>: <span class=\"hljs-string\">\"include\"</span>,\n  <span class=\"hljs-attr\">headers</span>: { <span class=\"hljs-string\">\"content-type\"</span>: <span class=\"hljs-string\">\"application/json\"</span> },\n  <span class=\"hljs-attr\">body</span>: <span class=\"hljs-title class_\">JSON</span>.<span class=\"hljs-title function_\">stringify</span>(<span class=\"hljs-string\">\"hi\"</span>),                  <span class=\"hljs-comment\">// the body is the value itself</span>\n});\n<span class=\"hljs-comment\">// 200 {\"ok\":true}</span>\n<span class=\"hljs-keyword\">const</span> { value } = <span class=\"hljs-title function_\">await</span> (<span class=\"hljs-keyword\">await</span> <span class=\"hljs-title function_\">fetch</span>(<span class=\"hljs-string\">`<span class=\"hljs-subst\">${api}</span>/kv/greeting`</span>, { <span class=\"hljs-attr\">credentials</span>: <span class=\"hljs-string\">\"include\"</span> })).<span class=\"hljs-title function_\">json</span>();\n<span class=\"hljs-comment\">// 200 {\"value\":\"hi\"}</span>\n\n<span class=\"hljs-comment\">// The same two calls through the SDK:</span>\n<span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">set</span>(<span class=\"hljs-string\">\"greeting\"</span>, <span class=\"hljs-string\">\"hi\"</span>);\n<span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">get</span>(<span class=\"hljs-string\">\"greeting\"</span>);            <span class=\"hljs-comment\">// \"hi\"</span></code></pre>\n<h2 id=\"where-the-api-lives\">Where the API lives</h2>\n<table>\n<thead>\n<tr>\n<th>URL mode</th>\n<th>Canvas content</th>\n<th>Runtime API</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>path</code></td>\n<td><code>{base}/c/{slug}/</code></td>\n<td>Same origin: <code>{base}/v1/c/{slug}/...</code></td>\n</tr>\n<tr>\n<td><code>subdomain</code></td>\n<td><code>https://{slug}.canvases.example.com/</code></td>\n<td>The base host: <code>https://canvases.example.com/v1/c/{slug}/...</code></td>\n</tr>\n</tbody></table>\n<p>The route path is identical in both modes; only the origin changes. The SDK derives it\nfrom <code>location</code>: a pathname starting <code>/c/{slug}</code> means <code>path</code> mode and <code>location.origin</code>;\notherwise it strips the first hostname label and keeps the port. Every request goes out\nwith <code>credentials: \"include\"</code>. <code>CANVAS_DROP_API_BASE_URL</code> shapes the Bearer-key\n<a href=\"/docs/api/deploy-api\">Deploy API</a> URLs (<code>/v1/canvases/...</code>), not this surface.</p>\n<h2 id=\"before-a-handler-runs\">Before a handler runs</h2>\n<p>Every <code>/v1/c/{slug}/*</code> request passes the same pipeline, in this order. Any route on this\npage can return these.</p>\n<p><strong>1. Auth gateway.</strong> No session: <code>401 {\"error\":\"unauthorized\"}</code> in <code>proxy</code> and <code>dev</code>\nmode, or a <code>302</code> to <code>/auth/login</code> in <code>oidc</code> mode. One carve-out: an anonymous visitor to\nan active, unexpired Public link canvas (public links on for the instance and for the\nowner) is let through as an anonymous principal, then refused with <code>STATIC_ONLY</code> in\nstep 3.</p>\n<p><strong>2. Rate limit.</strong> <code>429 {\"code\":\"RATE_LIMITED\"}</code> with <code>Retry-After</code>, <code>X-RateLimit-Limit</code>,\n<code>X-RateLimit-Remaining</code>, <code>X-RateLimit-Reset</code>. Two classes:</p>\n<table>\n<thead>\n<tr>\n<th>Class</th>\n<th>Paths</th>\n<th>Keyed</th>\n<th>Default</th>\n<th>Env</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>ai</code></td>\n<td><code>/v1/c/{slug}/ai*</code></td>\n<td>per user</td>\n<td>10/min</td>\n<td><code>CANVAS_DROP_RATELIMIT_AI_PER_MIN</code></td>\n</tr>\n<tr>\n<td><code>canvas</code></td>\n<td>everything else under <code>/v1/c/{slug}</code></td>\n<td>per user per canvas</td>\n<td>120/min</td>\n<td><code>CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN</code></td>\n</tr>\n</tbody></table>\n<p><code>CANVAS_DROP_RATELIMIT_ENABLED=false</code> skips this step.</p>\n<p><strong>3. Resolve and authorize.</strong> The canvas is looked up by slug and the viewer is checked\nagainst its sharing rung, then the password gate, then the static-only rule. The owner\nand effective editors pass the rung and the password gate. A non-owner admin has no\nbypass here: they face the rung and the gate like any other member.</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>HTTP</th>\n<th>When</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NOT_FOUND</code></td>\n<td>404</td>\n<td>Unknown slug, or the canvas is deleted.</td>\n</tr>\n<tr>\n<td><code>ARCHIVED</code></td>\n<td>404</td>\n<td>The canvas is archived.</td>\n</tr>\n<tr>\n<td><code>DISABLED</code></td>\n<td>403</td>\n<td>An admin disabled the canvas. The owner is not exempt.</td>\n</tr>\n<tr>\n<td><code>NOT_INVITED</code></td>\n<td>404</td>\n<td>A guest principal scoped to a different canvas.</td>\n</tr>\n<tr>\n<td><code>OWNER_ONLY</code></td>\n<td>404</td>\n<td>The viewer has no route in: not on the people-and-teams list (directly or through a team) and not admitted by General access — Restricted, Whole org as a non-member (or, under tenancy, a member of another org), or Public link while public links are off for the instance or for the owner.</td>\n</tr>\n<tr>\n<td><code>SHARE_EXPIRED</code></td>\n<td>404</td>\n<td>The share's expiry has passed (for anyone but the owner and editors).</td>\n</tr>\n<tr>\n<td><code>PASSWORD_REQUIRED</code></td>\n<td>403</td>\n<td>A shared canvas with a password set and no valid <code>__canvasdrop_gate</code> cookie. The gate that sets the cookie lives on the canvas page, not under <code>/v1/c/</code>; this API only checks it. Guests skip the gate.</td>\n</tr>\n<tr>\n<td><code>STATIC_ONLY</code></td>\n<td>403</td>\n<td>Public link canvas and the viewer is not the owner or an editor. Applies to signed-in members as well as anonymous visitors: the whole runtime API is closed. Body: <code>{\"code\":\"STATIC_ONLY\",\"message\":\"This canvas is public and static-only.\"}</code>.</td>\n</tr>\n</tbody></table>\n<p>A guest principal reaches the handlers as a synthetic user whose <code>id</code> is\n<code>guest:&lt;inviteId&gt;</code>, never as an admin and never able to publish public links.</p>\n<p><strong>4. Cross-canvas isolation.</strong></p>\n<table>\n<thead>\n<tr>\n<th>Mode</th>\n<th>Rule</th>\n<th>Response</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>subdomain</code></td>\n<td><code>Origin</code> present and not exactly <code>https://{slug}.{baseHost}</code></td>\n<td><code>403 CROSS_CANVAS_FORBIDDEN</code></td>\n</tr>\n<tr>\n<td><code>path</code></td>\n<td><code>Sec-Fetch-Site</code> present and not <code>same-origin</code> or <code>none</code></td>\n<td><code>403 CROSS_SITE_FORBIDDEN</code></td>\n</tr>\n<tr>\n<td><code>path</code></td>\n<td><code>Referer</code> path not <code>/c/{slug}</code> or beneath it</td>\n<td><code>403 CROSS_CANVAS_FORBIDDEN</code></td>\n</tr>\n</tbody></table>\n<p>In <code>subdomain</code> mode a request with no <code>Origin</code> passes (a programmatic caller), and a\nmatching <code>Origin</code> gets credentialed CORS headers: <code>Access-Control-Allow-Origin: &lt;origin&gt;</code>,\n<code>Access-Control-Allow-Credentials: true</code>, <code>Vary: Origin</code>. Those headers are applied before\nthe step 3 refusals, so <code>STATIC_ONLY</code> and <code>PASSWORD_REQUIRED</code> reach the SDK as readable\nJSON rather than a CORS failure. In <code>path</code> mode the canvas is same-origin and no CORS\nheaders are sent.</p>\n<p>Preflight <code>OPTIONS /v1/c/{slug}/*</code> is answered before the auth gateway: always <code>204</code>,\n<code>Access-Control-Allow-Methods: GET,HEAD,POST,PUT,PATCH,DELETE,OPTIONS</code>, and either\n<code>Access-Control-Allow-Headers: Content-Type</code> or the canvas origin's requested header names,\nplus the credentialed headers above in\n<code>subdomain</code> mode when <code>Origin</code> matches. It never returns <code>401</code>.</p>\n<p><strong>5. Capability gate.</strong> Each primitive checks its capability and returns <code>403</code> when it is\noff:</p>\n<pre><code class=\"hljs language-json\"><span class=\"hljs-punctuation\">{</span> <span class=\"hljs-attr\">\"code\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"CAPABILITY_DISABLED\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"capability\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"kv\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"backendEnabled\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-literal\"><span class=\"hljs-keyword\">true</span></span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"reason\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"feature_off\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"hint\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"...\"</span> <span class=\"hljs-punctuation\">}</span></code></pre>\n<p><code>reason</code> is <code>backend_off</code>, <code>feature_off</code>, or <code>operator_disabled</code>; <code>hint</code> is a short\nremediation string.</p>\n<table>\n<thead>\n<tr>\n<th>Capability</th>\n<th>Effective when</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>identity</code></td>\n<td>the canvas backend is on</td>\n</tr>\n<tr>\n<td><code>kv</code>, <code>files</code></td>\n<td>backend on, plus the capability's own switch</td>\n</tr>\n<tr>\n<td><code>ai</code></td>\n<td>backend on, switch on, and the instance has a provider key</td>\n</tr>\n<tr>\n<td><code>realtime</code></td>\n<td>backend on, switch on, and <code>CANVAS_DROP_REALTIME=on</code> (the default)</td>\n</tr>\n<tr>\n<td><code>connections</code></td>\n<td>backend on, plus a live enabled profile and per-canvas admin grant; the external encryption key must be available when the profile has protected headers</td>\n</tr>\n<tr>\n<td><code>authoring</code></td>\n<td>backend on, switch on, and <code>CANVAS_DROP_AUTHORING=on</code> (default <code>off</code>)</td>\n</tr>\n</tbody></table>\n<h2 id=\"identity\">Identity</h2>\n<p>Capability: <code>identity</code> (on whenever the canvas backend is on).</p>\n<pre><code class=\"hljs\">GET {base}/v1/c/{slug}/me   → 200 { <span class=\"hljs-built_in\">id</span>, email, name, avatarUrl, kind }</code></pre>\n<p><code>avatarUrl</code> may be <code>null</code>. <code>kind</code> is <code>\"member\"</code> for a signed-in org member; <code>\"guest\"</code> is\nretained for legacy guest sessions. The response deliberately omits <code>isAdmin</code>; that lives\non the dashboard's <code>/api/me</code>, covered under Adjacent endpoints below. An anonymous\nvisitor never reaches this handler: a Public link canvas answers <code>STATIC_ONLY</code> first.</p>\n<h2 id=\"key-value\">Key-value</h2>\n<p>Capability: <code>kv</code>. Two scopes with the same five routes:</p>\n<ul>\n<li>Shared at <code>/kv</code>: one namespace for every viewer of the canvas.</li>\n<li>Per-viewer at <code>/kv/user</code>: scoped to the caller's server-resolved user id. The client\nnever names the scope, and <code>user</code> is never read as a key.</li>\n</ul>\n<pre><code class=\"hljs\">GET    {base}/v1/c/{slug}/kv?prefix=&amp;cursor=&amp;<span class=\"hljs-built_in\">limit</span>=   list       → 200 { entries: [{ key, value }], nextCursor }\nGET    {base}/v1/c/{slug}/kv/{key}                    <span class=\"hljs-built_in\">read</span>       → 200 { value }        404 NOT_FOUND <span class=\"hljs-keyword\">if</span> absent\nPUT    {base}/v1/c/{slug}/kv/{key}                    write      → 200 { ok: <span class=\"hljs-literal\">true</span> }     body = the JSON value\nDELETE {base}/v1/c/{slug}/kv/{key}                    delete     → 200 { ok: <span class=\"hljs-literal\">true</span> }     idempotent, never 404\nPOST   {base}/v1/c/{slug}/kv/{key}/increment          atomic add → 200 { value }        body { by?: number }</code></pre>\n<p>Replace <code>/kv</code> with <code>/kv/user</code> for the per-viewer scope. <code>{key}</code> is one path segment;\nURL-encode it.</p>\n<ul>\n<li><code>list</code>: <code>limit</code> is clamped to 1..1000 (default 100, non-numeric ignored); entries come\nback in key order; <code>nextCursor</code> is the last key on the page and <code>null</code> on the last page.</li>\n<li><code>read</code>: a stored JSON <code>null</code> reads as <code>{ \"value\": null }</code>.</li>\n<li><code>PUT</code>: the body is the value itself (<code>content-type: application/json</code>), any JSON except\n<code>null</code>. Unparseable JSON and <code>null</code> are <code>400 INVALID_BODY</code> (delete the key instead of\nstoring <code>null</code>).</li>\n<li><code>increment</code>: <code>by</code> defaults to <code>1</code> and must be a finite number (<code>400 INVALID_BODY</code>); a\nmissing or malformed body counts as <code>{}</code>. A missing key starts at <code>0</code>. An existing\nnon-numeric value (a stored <code>null</code> included) is <code>409 NOT_NUMERIC</code>.</li>\n</ul>\n<p>Limits: key ≤ 512 bytes (<code>413 KEY_TOO_LARGE</code>), serialized value ≤ 64 KiB\n(<code>413 VALUE_TOO_LARGE</code>), 10 000 shared keys and 1 000 per-viewer keys per canvas\n(<code>409 KEY_LIMIT</code>; admin-tunable quota keys <code>kv.keys.shared</code> and <code>kv.keys.user</code>). The key\ncap applies to new keys; updating an existing key always succeeds. Checks run in the\norder key size, body, value size, key count.</p>\n<h2 id=\"files\">Files</h2>\n<p>Capability: <code>files</code>.</p>\n<pre><code class=\"hljs\">POST   {base}/v1/c/{slug}/files              upload   → 201 { <span class=\"hljs-built_in\">id</span>, name, size, url }\nGET    {base}/v1/c/{slug}/files              list     → 200 { files: [{ <span class=\"hljs-built_in\">id</span>, name, size, mime, createdAt }] }\nGET    {base}/v1/c/{slug}/files/{<span class=\"hljs-built_in\">id</span>}/content download → 200 raw bytes          404 NOT_FOUND\nDELETE {base}/v1/c/{slug}/files/{<span class=\"hljs-built_in\">id</span>}         delete   → 200 { ok: <span class=\"hljs-literal\">true</span> }       404 NOT_FOUND</code></pre>\n<p>Upload is <code>multipart/form-data</code> with the file in a field named <code>file</code>; the name defaults\nto <code>upload</code> and the type to <code>application/octet-stream</code> when the part carries none. The\nreturned <code>url</code> is root-relative (<code>/v1/c/{slug}/files/{id}/content</code>); the SDK rewrites it\nto an absolute URL on the API base, which matters in <code>subdomain</code> mode.</p>\n<p>Content is served with <code>Content-Type</code> set to the stored mime,\n<code>X-Content-Type-Options: nosniff</code>, and a sanitized filename (<code>filename</code> plus RFC 5987\n<code>filename*</code>). Only <code>image/png</code>, <code>image/jpeg</code>, <code>image/gif</code>, <code>image/webp</code>, and <code>image/avif</code>\nrender inline; everything else, SVG and HTML included, is sent as <code>attachment</code>.</p>\n<p>Limits: 25 MiB per file (<code>413 FILE_TOO_LARGE</code>, from either the transport body cap or the\nper-file quota), 1 GiB per canvas (<code>409 QUOTA_EXCEEDED</code>); both admin-tunable\n(<code>files.bytes.file</code>, <code>files.bytes.canvas</code>). A missing or non-multipart body is\n<code>400 INVALID_BODY</code>. <code>DELETE</code> and <code>content</code> answer <code>404 NOT_FOUND</code> for a file that belongs\nto another canvas.</p>\n<h2 id=\"ai\">AI</h2>\n<p>Capability: <code>ai</code>. Effective only when the canvas has <code>ai</code> on and the instance has a\nprovider key configured; the key stays server-side and never appears in a response. This\nroute uses the stricter <code>ai</code> rate-limit class (default 10/min per user).</p>\n<pre><code class=\"hljs\">POST   {base}/v1/c/{slug}/ai/chat            chat completion → 200 SSE stream</code></pre>\n<p>Request body (JSON, ≤ 256 KiB):</p>\n<pre><code class=\"hljs language-json\"><span class=\"hljs-punctuation\">{</span>\n  <span class=\"hljs-attr\">\"model\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"&lt;provider model id&gt;\"</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"messages\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-punctuation\">[</span><span class=\"hljs-punctuation\">{</span> <span class=\"hljs-attr\">\"role\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"user\"</span><span class=\"hljs-punctuation\">,</span> <span class=\"hljs-attr\">\"content\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"Hello\"</span> <span class=\"hljs-punctuation\">}</span><span class=\"hljs-punctuation\">]</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"system\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"optional system prompt\"</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"maxTokens\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-number\">1024</span>\n<span class=\"hljs-punctuation\">}</span></code></pre>\n<p><code>model</code> is required and must be on the instance allowlist. <code>messages</code> needs at least one\nentry, each with role <code>user</code> or <code>assistant</code>; the system prompt goes in <code>system</code>, not in a\nmessage. <code>maxTokens</code> is a positive integer, defaults to 1024, and is capped at 8192.</p>\n<p>The response is <code>text/event-stream</code>. Each event is one JSON <code>data:</code> line:</p>\n<pre><code class=\"hljs\"><span class=\"hljs-attribute\">data</span>: {<span class=\"hljs-string\">\"type\"</span>:<span class=\"hljs-string\">\"delta\"</span>,<span class=\"hljs-string\">\"text\"</span>:<span class=\"hljs-string\">\"Hel\"</span>}\n<span class=\"hljs-attribute\">data</span>: {<span class=\"hljs-string\">\"type\"</span>:<span class=\"hljs-string\">\"delta\"</span>,<span class=\"hljs-string\">\"text\"</span>:<span class=\"hljs-string\">\"lo\"</span>}\n<span class=\"hljs-attribute\">data</span>: {<span class=\"hljs-string\">\"type\"</span>:<span class=\"hljs-string\">\"done\"</span>,<span class=\"hljs-string\">\"usage\"</span>:{<span class=\"hljs-string\">\"inputTokens\"</span>:<span class=\"hljs-number\">12</span>,<span class=\"hljs-string\">\"outputTokens\"</span>:<span class=\"hljs-number\">2</span>,<span class=\"hljs-string\">\"cacheCreationInputTokens\"</span>:<span class=\"hljs-number\">0</span>,<span class=\"hljs-string\">\"cacheReadInputTokens\"</span>:<span class=\"hljs-number\">0</span>},<span class=\"hljs-string\">\"cost\"</span>:<span class=\"hljs-number\">0</span>.<span class=\"hljs-number\">000123</span>}</code></pre>\n<p><code>cost</code> is in USD. The cache fields report prompt-cache writes and reads when the provider\nsupplies them, <code>0</code> otherwise. With the Anthropic provider the server sets ephemeral cache\nbreakpoints on the <code>system</code> prompt and on the last message before the newest user turn,\nso a stable conversation prefix is cached across calls.</p>\n<p>Errors, by when they happen:</p>\n<ul>\n<li>Before the stream (JSON body, normal status), in this order: <code>413 BODY_TOO_LARGE</code>,\n<code>400 INVALID_BODY</code>, <code>403 MODEL_NOT_ALLOWED</code> (not allowlisted, or allowlisted but\nunpriced), <code>403 GUEST_AI_DISABLED</code> (legacy guest session on a canvas with guest AI off),\n<code>429 GUEST_AI_CAP</code> (<code>scope: \"guest\"</code>), <code>429 QUOTA_EXCEEDED</code> (<code>scope: \"user_daily\"</code> or\n<code>\"canvas_monthly\"</code>, the USD spend caps), <code>403 CAPABILITY_DISABLED</code> (no effective\nprovider key after the gate).</li>\n<li>During the stream (HTTP is already <code>200</code>): a final\n<code>{\"type\":\"error\",\"code\":\"AI_UPSTREAM_ERROR\",\"message\":\"the AI provider returned an error\"}</code>\nevent. The SDK raises <code>AI_STREAM_TRUNCATED</code> (502) itself when the stream ends with\nneither <code>done</code> nor <code>error</code>.</li>\n</ul>\n<p>Usage and spend are recorded exactly once per request: on success, on upstream error, and\nwhen the client aborts mid-stream.</p>\n<h2 id=\"outbound-connections\">Outbound Connections</h2>\n<p>Capability: <code>connections</code>. A grant, not a canvas-owned feature flag, is the\nswitch. An instance administrator defines one exact HTTPS origin, the allowed\nstandard methods, and optional protected headers, then attaches that profile to\nindividual canvases. Backend must also be on.</p>\n<pre><code class=\"hljs\">GET|<span class=\"hljs-type\">HEAD</span>|<span class=\"hljs-type\">POST</span>|<span class=\"hljs-type\">PUT</span>|<span class=\"hljs-type\">PATCH</span>|<span class=\"hljs-type\">DELETE</span>\n  {base}/v1/c/{slug}/connections/{profile}/{relative-path}?query</code></pre>\n<p><code>{relative-path}</code> stays under the approved origin. An empty suffix means <code>/</code>.\nOnly methods selected for the profile are accepted. <code>GET</code> and <code>HEAD</code> have no\nrequest body; the other supported methods accept at most 256 KiB. Canvas caller\nheaders are limited to 32 and 16 KiB total. Cookies, authorization, forwarding,\nhost, compression, and hop-by-hop headers are stripped; fixed protected headers\nare applied last.</p>\n<p>The upstream response status and bounded body are returned unchanged, including\n<code>4xx</code> and <code>5xx</code>, with only safe response headers. Canvas Drop adds\n<code>X-Canvas-Drop-Connection-Response: upstream</code>, <code>Cache-Control: no-store</code>,\n<code>X-Content-Type-Options: nosniff</code>, and <code>Content-Security-Policy: sandbox</code>.\nUpstream cookies, redirects, CORS policy, server identification, and hop-by-hop\nheaders are removed. Non-identity content encoding is refused.</p>\n<p>Platform failures are JSON and carry one of the stable connection codes on the\n<a href=\"/docs/api/errors\">Error codes</a> page. Default bounds are an 8 KiB relative URL,\n2 MiB response, 10-second total DNS/request/body deadline, three exact-origin\nredirects, 60 requests/minute per actor+canvas+profile, 600/minute per profile,\nfive concurrent requests per canvas, and 50 per server process.</p>\n<p>Security is enforced before every socket: IP literals and non-HTTPS origins are\ninvalid; every A/AAAA answer must be public; mixed public/private answers reject;\nthe validated address is pinned to the socket while TLS verifies the approved\nhostname; every redirect retains the exact origin and re-runs DNS validation.\nSee <a href=\"/docs/sdk/connections\">Outbound Connections</a> for the SDK contract and the\nremaining upstream-reflection trust boundary.</p>\n<h2 id=\"realtime\">Realtime</h2>\n<p>Capability: <code>realtime</code>. Effective only with <code>CANVAS_DROP_REALTIME=on</code> (the default) plus\nthe canvas switch. The route exists only where the server wires a WebSocket adaptor (the\nNode server does).</p>\n<pre><code class=\"hljs\">GET    {base}<span class=\"hljs-regexp\">/v1/</span>c<span class=\"hljs-regexp\">/{slug}/</span>realtime           WebSocket upgrade (ws:<span class=\"hljs-regexp\">//</span> or wss:<span class=\"hljs-regexp\">//</span> on the API base)</code></pre>\n<p>The full pipeline above runs before the upgrade: a failed login, authorization, password\ngate, or isolation check refuses the <code>101</code> with the usual HTTP error. After the socket is\nopen, the server closes it with one of these codes:</p>\n<table>\n<thead>\n<tr>\n<th>Close code</th>\n<th>When</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>4403</code></td>\n<td><code>realtime</code> is off. The server first sends <code>{\"type\":\"error\",\"code\":\"CAPABILITY_DISABLED\",\"capability\":\"realtime\"}</code>, then closes. Also used when realtime is switched off mid-session.</td>\n</tr>\n<tr>\n<td><code>4429</code></td>\n<td>The canvas already has 30 open connections (<code>\"connection limit\"</code>).</td>\n</tr>\n<tr>\n<td><code>4401</code></td>\n<td>Access was lost on revalidation: canvas gone, access removed, the canvas became a Public link (static-only), a password gate was set, or the user was deactivated.</td>\n</tr>\n<tr>\n<td><code>1001</code></td>\n<td>Server shutdown.</td>\n</tr>\n</tbody></table>\n<p>Limits: 30 connections per canvas, 100 publishes per minute per connection, 16 KiB per\nframe. The SDK treats <code>4401</code>, <code>4403</code>, and <code>4429</code> as terminal and does not reconnect.</p>\n<p>Frames are JSON text, ≤ 16 KiB each, all scoped to the canvas from the handshake. Client\nto server:</p>\n<table>\n<thead>\n<tr>\n<th>Frame</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>{\"type\":\"subscribe\",\"channel\"}</code></td>\n<td>Join a channel; the server answers <code>subscribed</code> and a <code>presence</code> snapshot, and tells other subscribers <code>join</code>.</td>\n</tr>\n<tr>\n<td><code>{\"type\":\"unsubscribe\",\"channel\"}</code></td>\n<td>Leave it; others get <code>leave</code> once the user's last connection is gone.</td>\n</tr>\n<tr>\n<td><code>{\"type\":\"publish\",\"channel\",\"event\",\"data\"}</code></td>\n<td>Fan a message out to the channel's subscribers. A missing <code>event</code> is sent as <code>\"\"</code>.</td>\n</tr>\n<tr>\n<td><code>{\"type\":\"presence\",\"channel\"}</code></td>\n<td>Ask who is on the channel.</td>\n</tr>\n</tbody></table>\n<p>Server to client:</p>\n<table>\n<thead>\n<tr>\n<th>Frame</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>{\"type\":\"subscribed\",\"channel\"}</code></td>\n<td>Subscription confirmed.</td>\n</tr>\n<tr>\n<td><code>{\"type\":\"message\",\"channel\",\"event\",\"data\",\"from\":{id,name}}</code></td>\n<td>A published message. <code>from</code> is resolved server-side; a client cannot spoof it.</td>\n</tr>\n<tr>\n<td><code>{\"type\":\"presence\",\"channel\",\"users\":[{id,name}]}</code></td>\n<td>Current members.</td>\n</tr>\n<tr>\n<td><code>{\"type\":\"join\"|\"leave\",\"channel\",\"user\":{id,name}}</code></td>\n<td>Membership change.</td>\n</tr>\n<tr>\n<td><code>{\"type\":\"error\",\"code\",\"message\"}</code></td>\n<td><code>MESSAGE_TOO_LARGE</code> (&gt; 16 KiB), <code>INVALID_FRAME</code> (not JSON), <code>CHANNEL_NAME_TOO_LARGE</code> (&gt; 128 bytes), <code>CHANNEL_LIMIT</code> (&gt; 64 channels on one connection), <code>RATE_LIMITED</code> (&gt; 100 publishes per minute on one connection), <code>UNKNOWN_FRAME</code>.</td>\n</tr>\n</tbody></table>\n<p>The SDK's <code>canvasdrop.realtime.channel(name)</code> handles framing, reconnection, and presence;\nsee <a href=\"/docs/sdk/realtime\">Realtime</a>.</p>\n<h2 id=\"authoring\">Authoring</h2>\n<p>Capability: <code>authoring</code>. Off by default; needs the canvas switch and the instance switch\n<code>CANVAS_DROP_AUTHORING=on</code>. Lets a signed-in org member create and manage further\ncanvases (managed shares) from inside a canvas. A legacy guest principal gets\n<code>401 NOT_AUTHENTICATED</code>; an anonymous visitor never gets this far (<code>STATIC_ONLY</code>).</p>\n<pre><code class=\"hljs\">POST   {base}/v1/c/{slug}/authoring          publish a new canvas → 200 AuthoredCanvas\nPUT    {base}/v1/c/{slug}/authoring/{<span class=\"hljs-built_in\">id</span>}     update <span class=\"hljs-keyword\">in</span> place      → 200 AuthoredCanvas\nGET    {base}/v1/c/{slug}/authoring          list your shares     → 200 { canvases: [AuthoredCanvas] }\nDELETE {base}/v1/c/{slug}/authoring/{<span class=\"hljs-built_in\">id</span>}     revoke               → 204</code></pre>\n<p><code>POST</code> and <code>PUT</code> are <code>multipart/form-data</code> with two parts: <code>metadata</code>, a JSON string, and\n<code>bundle</code>, the static-site zip (required on <code>POST</code>, optional on <code>PUT</code>, never empty). The\npassword is an optional lock independent of the audience; <code>access: \"password\"</code> is the\ncompatibility shorthand for a Public link plus a password.</p>\n<table>\n<thead>\n<tr>\n<th><code>metadata</code> field</th>\n<th><code>POST</code></th>\n<th><code>PUT</code></th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>title</code></td>\n<td>required, 1-200 chars</td>\n<td>optional</td>\n<td></td>\n</tr>\n<tr>\n<td><code>slug</code></td>\n<td>optional</td>\n<td>not accepted</td>\n<td>Custom slug. Invalid: <code>400 INVALID_BODY</code> with <code>reason: \"invalid_slug\"</code>; in use: <code>409 SLUG_TAKEN</code>.</td>\n</tr>\n<tr>\n<td><code>tags</code></td>\n<td>optional</td>\n<td>optional</td>\n<td>Up to 20, each 1-64 chars.</td>\n</tr>\n<tr>\n<td><code>access</code></td>\n<td>optional, default <code>private</code></td>\n<td>optional</td>\n<td><code>private</code> (Restricted: the people-and-teams list only), <code>whole_org</code>, <code>public_link</code>, or <code>password</code> (a Public link with a password); <code>specific_people</code> is accepted as a legacy alias of <code>private</code>. Must be in <code>CANVAS_DROP_AUTHORING_ALLOWED_RUNGS</code> (default <code>private,specific_people,whole_org,public_link</code>; <code>public_link</code> covers <code>password</code>), else <code>400 INVALID_BODY</code>.</td>\n</tr>\n<tr>\n<td><code>password</code></td>\n<td>optional, 1-200 chars</td>\n<td>optional; <code>null</code> clears</td>\n<td>Required when <code>access</code> is <code>password</code>.</td>\n</tr>\n<tr>\n<td><code>expiresAt</code></td>\n<td>optional, epoch ms</td>\n<td>optional; <code>null</code> clears</td>\n<td>Must be in the future and within <code>CANVAS_DROP_AUTHORING_MAX_EXPIRY_DAYS</code> (default <code>0</code>, no cap); required when <code>CANVAS_DROP_AUTHORING_REQUIRE_EXPIRY</code> is on (default off).</td>\n</tr>\n<tr>\n<td><code>metadata</code></td>\n<td>optional object</td>\n<td>optional</td>\n<td>Free-form, ≤ 16 KiB.</td>\n</tr>\n<tr>\n<td><code>expectedUpdatedAt</code></td>\n<td>not accepted</td>\n<td>optional, epoch ms</td>\n<td>Compare-and-swap token: the <code>updatedAt</code> you last read. A stale value is refused with <code>409 SHARE_CONFLICT</code> and the current record.</td>\n</tr>\n</tbody></table>\n<p><strong><code>POST</code></strong> creates the canvas under your account, deploys the bundle, and applies the\nshare settings in one call. It counts against the authoring quota (<code>429 QUOTA_EXCEEDED</code>\nwith <code>scope: \"user_daily\"</code> or <code>\"user_total\"</code>; defaults 20 per day and 200 total, set by\n<code>CANVAS_DROP_AUTHORING_USER_DAILY_MAX</code> and <code>CANVAS_DROP_AUTHORING_USER_TOTAL_MAX</code>). A\ndeploy or share-config failure after the row exists still counts.</p>\n<p><strong><code>PUT</code></strong> updates an existing share in place: a new immutable version at the same URL when\n<code>bundle</code> is included, settings and metadata only when it is omitted. You must be the\nowner or an editor of <code>{id}</code> (an instance admin acting on a known id also passes);\nanything else reads as <code>404 NOT_FOUND</code>. It does not consume quota. Gates are checked\nagainst the share's resulting state, so clearing a password or dropping an expiry on a\nPublic link faces the same checks a fresh publish would. Settings are saved before the\nbundle deploys: if the deploy then fails, the response is <code>502 UPDATE_PARTIAL</code> with\n<code>stage</code> and the saved <code>current</code> record, so nothing you changed is lost.</p>\n<p><strong><code>GET</code></strong> returns the shares you authored and still manage as owner or editor, including\ndraft, revoked, expired, archived, and admin-disabled rows. Deleted canvases are omitted.\nFilters: <code>?sourceApp=&amp;sourceKind=&amp;tags=a,b</code> (every listed tag must match). The response\nis <code>Cache-Control: private, no-store</code>.</p>\n<p><strong><code>DELETE</code></strong> revokes: the URL stops serving and <code>revokedAt</code> is set, but the record stays\nlisted with <code>status: \"revoked\"</code>. A later <code>PUT</code> with a bundle publishes it again; a\nsettings-only <code>PUT</code> on a revoked share is <code>409 SHARE_REVOKED</code>.</p>\n<p><code>AuthoredCanvas</code>:</p>\n<pre><code class=\"hljs\">{ id, url, title, tags, access, accessMode, publicationStatus, hasPassword, status,\n<span class=\"hljs-built_in\">  createdAt,</span> updatedAt, expiresAt, galleryListed, galleryTemplatable, discoverability,\n<span class=\"hljs-built_in\">  revokedAt,</span> createdBy, viewerRole, audienceSummary, version, bundleUpdatedAt, sourceApp,\n<span class=\"hljs-built_in\">  sourceKind,</span> metadata }</code></pre>\n<p><code>accessMode</code> is the audience: <code>restricted</code> (<code>access</code> is <code>private</code> or a legacy alias —\nonly the people and teams on the list, who are admitted at every value), <code>whole_org</code>, or\n<code>public_link</code>. <code>publicationStatus</code> is the lifecycle, independent of the audience, first\nmatch wins: <code>disabled</code>, <code>archived</code>, <code>unpublished</code> (<code>revokedAt</code> set), <code>draft</code> (no version),\n<code>expired</code> (<code>expiresAt</code> passed), else <code>published</code>; deleted canvases never appear. The older\n<code>status</code> (<code>revoked</code> › <code>expired</code> › <code>private</code> › <code>live</code>) is deprecated and frozen: its <code>private</code>\nmeans the persisted value is literally <code>private</code>, not that nobody can open the share. <code>version</code> is the id of the\ncurrent published version (<code>null</code> when none) and advances on every deploy;\n<code>bundleUpdatedAt</code> is the row's last write (deploy or settings), the same value as\n<code>updatedAt</code>, so watch <code>version</code> to detect a bundle change. <code>sourceApp</code> and <code>sourceKind</code>\nare read from the free-form <code>metadata</code> when they are strings. <code>viewerRole</code> (<code>owner</code>,\n<code>editor</code>, or <code>admin</code>) says why you may manage the record; <code>audienceSummary</code>\n(<code>{ count, names }</code>) is a safe summary of the people and teams on its list.</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>HTTP</th>\n<th>When</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NOT_AUTHENTICATED</code></td>\n<td>401</td>\n<td>Legacy guest caller.</td>\n</tr>\n<tr>\n<td><code>INVALID_BODY</code></td>\n<td>400</td>\n<td>Not multipart, <code>metadata</code> missing or not JSON, <code>bundle</code> missing (<code>POST</code>) or empty, schema failure, rung not allowed, missing password for <code>password</code> access, expiry missing, past, or over the maximum, <code>metadata</code> over 16 KiB. <code>reason</code> may be <code>org_forbidden</code> or <code>invalid_slug</code>.</td>\n</tr>\n<tr>\n<td><code>INVALID_BODY</code></td>\n<td>413</td>\n<td>Bundle over 50 MiB (<code>message: \"bundle too large\"</code>).</td>\n</tr>\n<tr>\n<td><code>ORG_REQUIRED</code></td>\n<td>409</td>\n<td><code>access: whole_org</code> requested while the caller (or, on <code>PUT</code>, the canvas) has no home org under tenancy.</td>\n</tr>\n<tr>\n<td><code>PUBLIC_LINKS_DISABLED</code></td>\n<td>403</td>\n<td>A Public link requested while the instance has public links off.</td>\n</tr>\n<tr>\n<td><code>PUBLIC_NOT_ALLOWED</code></td>\n<td>403</td>\n<td>A Public link requested by an owner whose account may not publish public links.</td>\n</tr>\n<tr>\n<td><code>PUBLIC_LINK_OWNER_GATED</code></td>\n<td>403</td>\n<td><code>PUT</code> by an editor requesting a Public link on a canvas whose owner may not publish them.</td>\n</tr>\n<tr>\n<td><code>QUOTA_EXCEEDED</code></td>\n<td>429</td>\n<td><code>POST</code> only; <code>scope</code> is <code>user_daily</code> or <code>user_total</code>.</td>\n</tr>\n<tr>\n<td><code>SLUG_TAKEN</code></td>\n<td>409</td>\n<td><code>POST</code> with a custom slug already in use.</td>\n</tr>\n<tr>\n<td><code>NOT_FOUND</code></td>\n<td>404</td>\n<td><code>PUT</code> or <code>DELETE</code> on a canvas you hold no role on, or that is missing, deleted, or (for <code>DELETE</code>) not active.</td>\n</tr>\n<tr>\n<td><code>DISABLED</code></td>\n<td>409</td>\n<td><code>PUT</code> or <code>DELETE</code> on an admin-disabled canvas. Note the status: <code>409</code> here, <code>403</code> from the shared pipeline.</td>\n</tr>\n<tr>\n<td><code>SHARE_REVOKED</code></td>\n<td>409</td>\n<td>Settings-only <code>PUT</code> on a revoked share.</td>\n</tr>\n<tr>\n<td><code>SHARE_CONFLICT</code></td>\n<td>409</td>\n<td><code>PUT</code> with a stale <code>expectedUpdatedAt</code>; the body carries the current record.</td>\n</tr>\n<tr>\n<td><code>PUBLISH_FAILED</code></td>\n<td>502</td>\n<td>Create, deploy, or share config failed. <code>id</code> is included once the record exists so you can retry or revoke.</td>\n</tr>\n<tr>\n<td><code>UPDATE_PARTIAL</code></td>\n<td>502</td>\n<td><code>PUT</code> saved the settings but the bundle deploy failed; <code>stage</code> and <code>current</code> are included.</td>\n</tr>\n</tbody></table>\n<p>The SDK wraps these four routes as <a href=\"/docs/sdk/authoring\"><code>canvasdrop.canvases</code></a>.</p>\n<h2 id=\"adjacent-endpoints\">Adjacent endpoints</h2>\n<p>Not under <code>/v1/c/{slug}</code>, but part of the browser-facing surface:</p>\n<pre><code class=\"hljs\">GET {<span class=\"hljs-keyword\">base}/sdk/v1.js </span>  the <span class=\"hljs-keyword\">browser </span>SDK <span class=\"hljs-keyword\">bundle\n</span>GET {<span class=\"hljs-keyword\">base}/api/me </span>     dashboard identity <span class=\"hljs-keyword\">and </span><span class=\"hljs-keyword\">instance </span><span class=\"hljs-built_in\">config</span></code></pre>\n<p><code>/sdk/v1.js</code> sits behind the auth gateway, is served as\n<code>application/javascript; charset=utf-8</code> with <code>cache-control: public, max-age=3600</code>, is not\nrate-limited, and answers <code>503</code> (plain text) when the bundle has not been built\n(<code>pnpm build</code>).</p>\n<p><code>/api/me</code> is the dashboard's identity call:\n<code>{ id, email, name, avatarUrl, isAdmin, orgs: [{ id, name }], isGuest, canPublishPublic, authMode, urlMode, baseUrl, designSkin }</code>.\n<code>authMode</code> is <code>proxy</code>, <code>oidc</code>, or <code>dev</code>; <code>urlMode</code> is <code>path</code> or <code>subdomain</code>. It is not\ncapability-gated and uses the <code>management</code> rate-limit class\n(<code>429 {\"error\":\"rate_limited\"}</code>). Canvas code should call <code>/v1/c/{slug}/me</code> instead.</p>\n<h2 id=\"errors\">Errors</h2>\n<p>Every <code>code</code> is stable; branch on it, never on message text. The full list, with the\nSDK's exception classes, is on <a href=\"/docs/api/errors\">Error codes</a>.</p>\n",
    "headings": [
      {
        "id": "where-the-api-lives",
        "text": "Where the API lives",
        "level": 2
      },
      {
        "id": "before-a-handler-runs",
        "text": "Before a handler runs",
        "level": 2
      },
      {
        "id": "identity",
        "text": "Identity",
        "level": 2
      },
      {
        "id": "key-value",
        "text": "Key-value",
        "level": 2
      },
      {
        "id": "files",
        "text": "Files",
        "level": 2
      },
      {
        "id": "ai",
        "text": "AI",
        "level": 2
      },
      {
        "id": "outbound-connections",
        "text": "Outbound Connections",
        "level": 2
      },
      {
        "id": "realtime",
        "text": "Realtime",
        "level": 2
      },
      {
        "id": "authoring",
        "text": "Authoring",
        "level": 2
      },
      {
        "id": "adjacent-endpoints",
        "text": "Adjacent endpoints",
        "level": 2
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      }
    ],
    "text": "Runtime API The runtime API is the HTTP surface a canvas calls from the browser: the six fixed primitives (KV, files, AI, identity, realtime, and admin-granted Connections) plus authoring, each under {base}/v1/c/{slug} . The browser SDK ( window.canvasdrop , served at {base}/sdk/v1.js ) builds every one of these requests, speaks the SSE and WebSocket wire formats, and maps errors to typed exceptions, so reach for it first. Use this page when you need the raw routes: reading the network tab, a non-JS client, or the exact shape a primitive returns. Requests are credentialed with the session cookie the browser already holds. Identity is resolved server-side from that session; the canvas never asserts who the viewer is. // Inside a canvas served in path mode at http://localhost:3000/c/quiet-otter-x7k2/ const api = \"/v1/c/quiet-otter-x7k2\" ; await fetch ( ` ${api} /kv/greeting` , { method : \"PUT\" , credentials : \"include\" , headers : { \"content-type\" : \"application/json\" }, body : JSON . stringify ( \"hi\" ), // the body is the value itself }); // 200 {\"ok\":true} const { value } = await ( await fetch ( ` ${api} /kv/greeting` , { credentials : \"include\" })). json (); // 200 {\"value\":\"hi\"} // The same two calls through the SDK: await canvasdrop. kv . set ( \"greeting\" , \"hi\" ); await canvasdrop. kv . get ( \"greeting\" ); // \"hi\" Where the API lives URL mode Canvas content Runtime API path {base}/c/{slug}/ Same origin: {base}/v1/c/{slug}/... subdomain https://{slug}.canvases.example.com/ The base host: https://canvases.example.com/v1/c/{slug}/... The route path is identical in both modes; only the origin changes. The SDK derives it from location : a pathname starting /c/{slug} means path mode and location.origin ; otherwise it strips the first hostname label and keeps the port. Every request goes out with credentials: \"include\" . CANVAS_DROP_API_BASE_URL shapes the Bearer-key Deploy API URLs ( /v1/canvases/... ), not this surface. Before a handler runs Every /v1/c/{slug}/* request passes the same pipeline, in this order. Any route on this page can return these. 1. Auth gateway. No session: 401 {\"error\":\"unauthorized\"} in proxy and dev mode, or a 302 to /auth/login in oidc mode. One carve-out: an anonymous visitor to an active, unexpired Public link canvas (public links on for the instance and for the owner) is let through as an anonymous principal, then refused with STATIC_ONLY in step 3. 2. Rate limit. 429 {\"code\":\"RATE_LIMITED\"} with Retry-After , X-RateLimit-Limit , X-RateLimit-Remaining , X-RateLimit-Reset . Two classes: Class Paths Keyed Default Env ai /v1/c/{slug}/ai* per user 10/min CANVAS_DROP_RATELIMIT_AI_PER_MIN canvas everything else under /v1/c/{slug} per user per canvas 120/min CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN CANVAS_DROP_RATELIMIT_ENABLED=false skips this step. 3. Resolve and authorize. The canvas is looked up by slug and the viewer is checked against its sharing rung, then the password gate, then the static-only rule. The owner and effective editors pass the rung and the password gate. A non-owner admin has no bypass here: they face the rung and the gate like any other member. Code HTTP When NOT_FOUND 404 Unknown slug, or the canvas is deleted. ARCHIVED 404 The canvas is archived. DISABLED 403 An admin disabled the canvas. The owner is not exempt. NOT_INVITED 404 A guest principal scoped to a different canvas. OWNER_ONLY 404 The viewer has no route in: not on the people-and-teams list (directly or through a team) and not admitted by General access — Restricted, Whole org as a non-member (or, under tenancy, a member of another org), or Public link while public links are off for the instance or for the owner. SHARE_EXPIRED 404 The share's expiry has passed (for anyone but the owner and editors). PASSWORD_REQUIRED 403 A shared canvas with a password set and no valid __canvasdrop_gate cookie. The gate that sets the cookie lives on the canvas page, not under /v1/c/ ; this API only checks it. Guests skip the gate. STATIC_ONLY 403 Public link canvas and the viewer is not the owner or an editor. Applies to signed-in members as well as anonymous visitors: the whole runtime API is closed. Body: {\"code\":\"STATIC_ONLY\",\"message\":\"This canvas is public and static-only.\"} . A guest principal reaches the handlers as a synthetic user whose id is guest:<inviteId> , never as an admin and never able to publish public links. 4. Cross-canvas isolation. Mode Rule Response subdomain Origin present and not exactly https://{slug}.{baseHost} 403 CROSS_CANVAS_FORBIDDEN path Sec-Fetch-Site present and not same-origin or none 403 CROSS_SITE_FORBIDDEN path Referer path not /c/{slug} or beneath it 403 CROSS_CANVAS_FORBIDDEN In subdomain mode a request with no Origin passes (a programmatic caller), and a matching Origin gets credentialed CORS headers: Access-Control-Allow-Origin: <origin> , Access-Control-Allow-Credentials: true , Vary: Origin . Those headers are applied before the step 3 refusals, so STATIC_ONLY and PASSWORD_REQUIRED reach the SDK as readable JSON rather than a CORS failure. In path mode the canvas is same-origin and no CORS headers are sent. Preflight OPTIONS /v1/c/{slug}/* is answered before the auth gateway: always 204 , Access-Control-Allow-Methods: GET,HEAD,POST,PUT,PATCH,DELETE,OPTIONS , and either Access-Control-Allow-Headers: Content-Type or the canvas origin's requested header names, plus the credentialed headers above in subdomain mode when Origin matches. It never returns 401 . 5. Capability gate. Each primitive checks its capability and returns 403 when it is off: { \"code\" : \"CAPABILITY_DISABLED\" , \"capability\" : \"kv\" , \"backendEnabled\" : true , \"reason\" : \"feature_off\" , \"hint\" : \"...\" } reason is backend_off , feature_off , or operator_disabled ; hint is a short remediation string. Capability Effective when identity the canvas backend is on kv , files backend on, plus the capability's own switch ai backend on, switch on, and the instance has a provider key realtime backend on, switch on, and CANVAS_DROP_REALTIME=on (the default) connections backend on, plus a live enabled profile and per-canvas admin grant; the external encryption key must be available when the profile has protected headers authoring backend on, switch on, and CANVAS_DROP_AUTHORING=on (default off ) Identity Capability: identity (on whenever the canvas backend is on). GET {base}/v1/c/{slug}/me → 200 { id , email, name, avatarUrl, kind } avatarUrl may be null . kind is \"member\" for a signed-in org member; \"guest\" is retained for legacy guest sessions. The response deliberately omits isAdmin ; that lives on the dashboard's /api/me , covered under Adjacent endpoints below. An anonymous visitor never reaches this handler: a Public link canvas answers STATIC_ONLY first. Key-value Capability: kv . Two scopes with the same five routes: Shared at /kv : one namespace for every viewer of the canvas. Per-viewer at /kv/user : scoped to the caller's server-resolved user id. The client never names the scope, and user is never read as a key. GET {base}/v1/c/{slug}/kv?prefix=&cursor=& limit = list → 200 { entries: [{ key, value }], nextCursor } GET {base}/v1/c/{slug}/kv/{key} read → 200 { value } 404 NOT_FOUND if absent PUT {base}/v1/c/{slug}/kv/{key} write → 200 { ok: true } body = the JSON value DELETE {base}/v1/c/{slug}/kv/{key} delete → 200 { ok: true } idempotent, never 404 POST {base}/v1/c/{slug}/kv/{key}/increment atomic add → 200 { value } body { by?: number } Replace /kv with /kv/user for the per-viewer scope. {key} is one path segment; URL-encode it. list : limit is clamped to 1..1000 (default 100, non-numeric ignored); entries come back in key order; nextCursor is the last key on the page and null on the last page. read : a stored JSON null reads as { \"value\": null } . PUT : the body is the value itself ( content-type: application/json ), any JSON except null . Unparseable JSON and null are 400 INVALID_BODY (delete the key instead of storing null ). increment : by defaults to 1 and must be a finite number ( 400 INVALID_BODY ); a missing or malformed body counts as {} . A missing key starts at 0 . An existing non-numeric value (a stored null included) is 409 NOT_NUMERIC . Limits: key ≤ 512 bytes ( 413 KEY_TOO_LARGE ), serialized value ≤ 64 KiB ( 413 VALUE_TOO_LARGE ), 10 000 shared keys and 1 000 per-viewer keys per canvas ( 409 KEY_LIMIT ; admin-tunable quota keys kv.keys.shared and kv.keys.user ). The key cap applies to new keys; updating an existing key always succeeds. Checks run in the order key size, body, value size, key count. Files Capability: files . POST {base}/v1/c/{slug}/files upload → 201 { id , name, size, url } GET {base}/v1/c/{slug}/files list → 200 { files: [{ id , name, size, mime, createdAt }] } GET {base}/v1/c/{slug}/files/{ id }/content download → 200 raw bytes 404 NOT_FOUND DELETE {base}/v1/c/{slug}/files/{ id } delete → 200 { ok: true } 404 NOT_FOUND Upload is multipart/form-data with the file in a field named file ; the name defaults to upload and the type to application/octet-stream when the part carries none. The returned url is root-relative ( /v1/c/{slug}/files/{id}/content ); the SDK rewrites it to an absolute URL on the API base, which matters in subdomain mode. Content is served with Content-Type set to the stored mime, X-Content-Type-Options: nosniff , and a sanitized filename ( filename plus RFC 5987 filename* ). Only image/png , image/jpeg , image/gif , image/webp , and image/avif render inline; everything else, SVG and HTML included, is sent as attachment . Limits: 25 MiB per file ( 413 FILE_TOO_LARGE , from either the transport body cap or the per-file quota), 1 GiB per canvas ( 409 QUOTA_EXCEEDED ); both admin-tunable ( files.bytes.file , files.bytes.canvas ). A missing or non-multipart body is 400 INVALID_BODY . DELETE and content answer 404 NOT_FOUND for a file that belongs to another canvas. AI Capability: ai . Effective only when the canvas has ai on and the instance has a provider key configured; the key stays server-side and never appears in a response. This route uses the stricter ai rate-limit class (default 10/min per user). POST {base}/v1/c/{slug}/ai/chat chat completion → 200 SSE stream Request body (JSON, ≤ 256 KiB): { \"model\" : \"<provider model id>\" , \"messages\" : [ { \"role\" : \"user\" , \"content\" : \"Hello\" } ] , \"system\" : \"optional system prompt\" , \"maxTokens\" : 1024 } model is required and must be on the instance allowlist. messages needs at least one entry, each with role user or assistant ; the system prompt goes in system , not in a message. maxTokens is a positive integer, defaults to 1024, and is capped at 8192. The response is text/event-stream . Each event is one JSON data: line: data : { \"type\" : \"delta\" , \"text\" : \"Hel\" } data : { \"type\" : \"delta\" , \"text\" : \"lo\" } data : { \"type\" : \"done\" , \"usage\" :{ \"inputTokens\" : 12 , \"outputTokens\" : 2 , \"cacheCreationInputTokens\" : 0 , \"cacheReadInputTokens\" : 0 }, \"cost\" : 0 . 000123 } cost is in USD. The cache fields report prompt-cache writes and reads when the provider supplies them, 0 otherwise. With the Anthropic provider the server sets ephemeral cache breakpoints on the system prompt and on the last message before the newest user turn, so a stable conversation prefix is cached across calls. Errors, by when they happen: Before the stream (JSON body, normal status), in this order: 413 BODY_TOO_LARGE , 400 INVALID_BODY , 403 MODEL_NOT_ALLOWED (not allowlisted, or allowlisted but unpriced), 403 GUEST_AI_DISABLED (legacy guest session on a canvas with guest AI off), 429 GUEST_AI_CAP ( scope: \"guest\" ), 429 QUOTA_EXCEEDED ( scope: \"user_daily\" or \"canvas_monthly\" , the USD spend caps), 403 CAPABILITY_DISABLED (no effective provider key after the gate). During the stream (HTTP is already 200 ): a final {\"type\":\"error\",\"code\":\"AI_UPSTREAM_ERROR\",\"message\":\"the AI provider returned an error\"} event. The SDK raises AI_STREAM_TRUNCATED (502) itself when the stream ends with neither done nor error . Usage and spend are recorded exactly once per request: on success, on upstream error, and when the client aborts mid-stream. Outbound Connections Capability: connections . A grant, not a canvas-owned feature flag, is the switch. An instance administrator defines one exact HTTPS origin, the allowed standard methods, and optional protected headers, then attaches that profile to individual canvases. Backend must also be on. GET| HEAD | POST | PUT | PATCH | DELETE {base}/v1/c/{slug}/connections/{profile}/{relative-path}?query {relative-path} stays under the approved origin. An empty suffix means / . Only methods selected for the profile are accepted. GET and HEAD have no request body; the other supported methods accept at most 256 KiB. Canvas caller headers are limited to 32 and 16 KiB total. Cookies, authorization, forwarding, host, compression, and hop-by-hop headers are stripped; fixed protected headers are applied last. The upstream response status and bounded body are returned unchanged, including 4xx and 5xx , with only safe response headers. Canvas Drop adds X-Canvas-Drop-Connection-Response: upstream , Cache-Control: no-store , X-Content-Type-Options: nosniff , and Content-Security-Policy: sandbox . Upstream cookies, redirects, CORS policy, server identification, and hop-by-hop headers are removed. Non-identity content encoding is refused. Platform failures are JSON and carry one of the stable connection codes on the Error codes page. Default bounds are an 8 KiB relative URL, 2 MiB response, 10-second total DNS/request/body deadline, three exact-origin redirects, 60 requests/minute per actor+canvas+profile, 600/minute per profile, five concurrent requests per canvas, and 50 per server process. Security is enforced before every socket: IP literals and non-HTTPS origins are invalid; every A/AAAA answer must be public; mixed public/private answers reject; the validated address is pinned to the socket while TLS verifies the approved hostname; every redirect retains the exact origin and re-runs DNS validation. See Outbound Connections for the SDK contract and the remaining upstream-reflection trust boundary. Realtime Capability: realtime . Effective only with CANVAS_DROP_REALTIME=on (the default) plus the canvas switch. The route exists only where the server wires a WebSocket adaptor (the Node server does). GET {base} /v1/ c /{slug}/ realtime WebSocket upgrade (ws: // or wss: // on the API base) The full pipeline above runs before the upgrade: a failed login, authorization, password gate, or isolation check refuses the 101 with the usual HTTP error. After the socket is open, the server closes it with one of these codes: Close code When 4403 realtime is off. The server first sends {\"type\":\"error\",\"code\":\"CAPABILITY_DISABLED\",\"capability\":\"realtime\"} , then closes. Also used when realtime is switched off mid-session. 4429 The canvas already has 30 open connections ( \"connection limit\" ). 4401 Access was lost on revalidation: canvas gone, access removed, the canvas became a Public link (static-only), a password gate was set, or the user was deactivated. 1001 Server shutdown. Limits: 30 connections per canvas, 100 publishes per minute per connection, 16 KiB per frame. The SDK treats 4401 , 4403 , and 4429 as terminal and does not reconnect. Frames are JSON text, ≤ 16 KiB each, all scoped to the canvas from the handshake. Client to server: Frame Purpose {\"type\":\"subscribe\",\"channel\"} Join a channel; the server answers subscribed and a presence snapshot, and tells other subscribers join . {\"type\":\"unsubscribe\",\"channel\"} Leave it; others get leave once the user's last connection is gone. {\"type\":\"publish\",\"channel\",\"event\",\"data\"} Fan a message out to the channel's subscribers. A missing event is sent as \"\" . {\"type\":\"presence\",\"channel\"} Ask who is on the channel. Server to client: Frame Meaning {\"type\":\"subscribed\",\"channel\"} Subscription confirmed. {\"type\":\"message\",\"channel\",\"event\",\"data\",\"from\":{id,name}} A published message. from is resolved server-side; a client cannot spoof it. {\"type\":\"presence\",\"channel\",\"users\":[{id,name}]} Current members. {\"type\":\"join\"|\"leave\",\"channel\",\"user\":{id,name}} Membership change. {\"type\":\"error\",\"code\",\"message\"} MESSAGE_TOO_LARGE (> 16 KiB), INVALID_FRAME (not JSON), CHANNEL_NAME_TOO_LARGE (> 128 bytes), CHANNEL_LIMIT (> 64 channels on one connection), RATE_LIMITED (> 100 publishes per minute on one connection), UNKNOWN_FRAME . The SDK's canvasdrop.realtime.channel(name) handles framing, reconnection, and presence; see Realtime . Authoring Capability: authoring . Off by default; needs the canvas switch and the instance switch CANVAS_DROP_AUTHORING=on . Lets a signed-in org member create and manage further canvases (managed shares) from inside a canvas. A legacy guest principal gets 401 NOT_AUTHENTICATED ; an anonymous visitor never gets this far ( STATIC_ONLY ). POST {base}/v1/c/{slug}/authoring publish a new canvas → 200 AuthoredCanvas PUT {base}/v1/c/{slug}/authoring/{ id } update in place → 200 AuthoredCanvas GET {base}/v1/c/{slug}/authoring list your shares → 200 { canvases: [AuthoredCanvas] } DELETE {base}/v1/c/{slug}/authoring/{ id } revoke → 204 POST and PUT are multipart/form-data with two parts: metadata , a JSON string, and bundle , the static-site zip (required on POST , optional on PUT , never empty). The password is an optional lock independent of the audience; access: \"password\" is the compatibility shorthand for a Public link plus a password. metadata field POST PUT Notes title required, 1-200 chars optional slug optional not accepted Custom slug. Invalid: 400 INVALID_BODY with reason: \"invalid_slug\" ; in use: 409 SLUG_TAKEN . tags optional optional Up to 20, each 1-64 chars. access optional, default private optional private (Restricted: the people-and-teams list only), whole_org , public_link , or password (a Public link with a password); specific_people is accepted as a legacy alias of private . Must be in CANVAS_DROP_AUTHORING_ALLOWED_RUNGS (default private,specific_people,whole_org,public_link ; public_link covers password ), else 400 INVALID_BODY . password optional, 1-200 chars optional; null clears Required when access is password . expiresAt optional, epoch ms optional; null clears Must be in the future and within CANVAS_DROP_AUTHORING_MAX_EXPIRY_DAYS (default 0 , no cap); required when CANVAS_DROP_AUTHORING_REQUIRE_EXPIRY is on (default off). metadata optional object optional Free-form, ≤ 16 KiB. expectedUpdatedAt not accepted optional, epoch ms Compare-and-swap token: the updatedAt you last read. A stale value is refused with 409 SHARE_CONFLICT and the current record. POST creates the canvas under your account, deploys the bundle, and applies the share settings in one call. It counts against the authoring quota ( 429 QUOTA_EXCEEDED with scope: \"user_daily\" or \"user_total\" ; defaults 20 per day and 200 total, set by CANVAS_DROP_AUTHORING_USER_DAILY_MAX and CANVAS_DROP_AUTHORING_USER_TOTAL_MAX ). A deploy or share-config failure after the row exists still counts. PUT updates an existing share in place: a new immutable version at the same URL when bundle is included, settings and metadata only when it is omitted. You must be the owner or an editor of {id} (an instance admin acting on a known id also passes); anything else reads as 404 NOT_FOUND . It does not consume quota. Gates are checked against the share's resulting state, so clearing a password or dropping an expiry on a Public link faces the same checks a fresh publish would. Settings are saved before the bundle deploys: if the deploy then fails, the response is 502 UPDATE_PARTIAL with stage and the saved current record, so nothing you changed is lost. GET returns the shares you authored and still manage as owner or editor, including draft, revoked, expired, archived, and admin-disabled rows. Deleted canvases are omitted. Filters: ?sourceApp=&sourceKind=&tags=a,b (every listed tag must match). The response is Cache-Control: private, no-store . DELETE revokes: the URL stops serving and revokedAt is set, but the record stays listed with status: \"revoked\" . A later PUT with a bundle publishes it again; a settings-only PUT on a revoked share is 409 SHARE_REVOKED . AuthoredCanvas : { id, url, title, tags, access, accessMode, publicationStatus, hasPassword, status, createdAt, updatedAt, expiresAt, galleryListed, galleryTemplatable, discoverability, revokedAt, createdBy, viewerRole, audienceSummary, version, bundleUpdatedAt, sourceApp, sourceKind, metadata } accessMode is the audience: restricted ( access is private or a legacy alias — only the people and teams on the list, who are admitted at every value), whole_org , or public_link . publicationStatus is the lifecycle, independent of the audience, first match wins: disabled , archived , unpublished ( revokedAt set), draft (no version), expired ( expiresAt passed), else published ; deleted canvases never appear. The older status ( revoked › expired › private › live ) is deprecated and frozen: its private means the persisted value is literally private , not that nobody can open the share. version is the id of the current published version ( null when none) and advances on every deploy; bundleUpdatedAt is the row's last write (deploy or settings), the same value as updatedAt , so watch version to detect a bundle change. sourceApp and sourceKind are read from the free-form metadata when they are strings. viewerRole ( owner , editor , or admin ) says why you may manage the record; audienceSummary ( { count, names } ) is a safe summary of the people and teams on its list. Code HTTP When NOT_AUTHENTICATED 401 Legacy guest caller. INVALID_BODY 400 Not multipart, metadata missing or not JSON, bundle missing ( POST ) or empty, schema failure, rung not allowed, missing password for password access, expiry missing, past, or over the maximum, metadata over 16 KiB. reason may be org_forbidden or invalid_slug . INVALID_BODY 413 Bundle over 50 MiB ( message: \"bundle too large\" ). ORG_REQUIRED 409 access: whole_org requested while the caller (or, on PUT , the canvas) has no home org under tenancy. PUBLIC_LINKS_DISABLED 403 A Public link requested while the instance has public links off. PUBLIC_NOT_ALLOWED 403 A Public link requested by an owner whose account may not publish public links. PUBLIC_LINK_OWNER_GATED 403 PUT by an editor requesting a Public link on a canvas whose owner may not publish them. QUOTA_EXCEEDED 429 POST only; scope is user_daily or user_total . SLUG_TAKEN 409 POST with a custom slug already in use. NOT_FOUND 404 PUT or DELETE on a canvas you hold no role on, or that is missing, deleted, or (for DELETE ) not active. DISABLED 409 PUT or DELETE on an admin-disabled canvas. Note the status: 409 here, 403 from the shared pipeline. SHARE_REVOKED 409 Settings-only PUT on a revoked share. SHARE_CONFLICT 409 PUT with a stale expectedUpdatedAt ; the body carries the current record. PUBLISH_FAILED 502 Create, deploy, or share config failed. id is included once the record exists so you can retry or revoke. UPDATE_PARTIAL 502 PUT saved the settings but the bundle deploy failed; stage and current are included. The SDK wraps these four routes as canvasdrop.canvases . Adjacent endpoints Not under /v1/c/{slug} , but part of the browser-facing surface: GET { base}/sdk/v1.js the browser SDK bundle GET { base}/api/me dashboard identity and instance config /sdk/v1.js sits behind the auth gateway, is served as application/javascript; charset=utf-8 with cache-control: public, max-age=3600 , is not rate-limited, and answers 503 (plain text) when the bundle has not been built ( pnpm build ). /api/me is the dashboard's identity call: { id, email, name, avatarUrl, isAdmin, orgs: [{ id, name }], isGuest, canPublishPublic, authMode, urlMode, baseUrl, designSkin } . authMode is proxy , oidc , or dev ; urlMode is path or subdomain . It is not capability-gated and uses the management rate-limit class ( 429 {\"error\":\"rate_limited\"} ). Canvas code should call /v1/c/{slug}/me instead. Errors Every code is stable; branch on it, never on message text. The full list, with the SDK's exception classes, is on Error codes ."
  },
  {
    "path": "api/errors",
    "section": "API reference",
    "title": "Error codes",
    "html": "<h1 id=\"error-codes\">Error codes</h1>\n<p>If you call the Runtime API or the browser SDK from a canvas, or you are an agent\nhandling its failures, this page is the contract. Branch on the error's <code>code</code>,\nnever on its message text. Every refusal from <code>/v1/c/{slug}/*</code> is JSON with a\nstable <code>code</code> and an HTTP status, and the browser SDK throws typed errors that\nextend <code>CanvasdropError</code>, each with a readonly <code>.code</code> and <code>.status</code> (plus <code>.hint</code>\nwhen the server sent a remediation hint).</p>\n<p>The browser global is <code>window.canvasdrop</code>, loaded from <code>{base}/sdk/v1.js</code>; there\nis no <code>cd</code> alias. The error classes, <code>ERROR_CODES</code>, and <code>errorFromResponse</code> are\nnamed exports of <code>@canvas-drop/sdk</code> for code that imports the package.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">try</span> {\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">set</span>(<span class=\"hljs-string\">\"prefs\"</span>, { <span class=\"hljs-attr\">theme</span>: <span class=\"hljs-string\">\"dark\"</span> });\n} <span class=\"hljs-keyword\">catch</span> (err) {\n  <span class=\"hljs-keyword\">switch</span> (err.<span class=\"hljs-property\">code</span>) {\n    <span class=\"hljs-keyword\">case</span> <span class=\"hljs-string\">\"CAPABILITY_DISABLED\"</span>: <span class=\"hljs-comment\">// KV is off for this canvas; the hint says how to turn it on</span>\n      <span class=\"hljs-title function_\">showBanner</span>(err.<span class=\"hljs-property\">hint</span>);\n      <span class=\"hljs-keyword\">break</span>;\n    <span class=\"hljs-keyword\">case</span> <span class=\"hljs-string\">\"VALUE_TOO_LARGE\"</span>:\n    <span class=\"hljs-keyword\">case</span> <span class=\"hljs-string\">\"KEY_LIMIT\"</span>:\n      <span class=\"hljs-title function_\">showBanner</span>(<span class=\"hljs-string\">\"Storage limit reached.\"</span>);\n      <span class=\"hljs-keyword\">break</span>;\n    <span class=\"hljs-attr\">default</span>:\n      <span class=\"hljs-keyword\">throw</span> err;\n  }\n}</code></pre>\n<p><code>kv.get</code> returns <code>null</code> for a missing key instead of throwing, so most reads need\nno <code>try/catch</code>.</p>\n<h2 id=\"the-codes\">The codes</h2>\n<p>Codes, statuses, and meanings below are the SDK's exported <code>ERROR_CODES</code>, in the\nsame order (each entry is <code>{ status, summary }</code>; <code>ErrorCode</code> is its key type).</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Status</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NOT_AUTHENTICATED</code></td>\n<td>401</td>\n<td>The viewer is not signed in.</td>\n</tr>\n<tr>\n<td><code>PASSWORD_REQUIRED</code></td>\n<td>403</td>\n<td>The canvas is password-protected.</td>\n</tr>\n<tr>\n<td><code>CAPABILITY_DISABLED</code></td>\n<td>403</td>\n<td>Backend or the specific feature is off for this canvas.</td>\n</tr>\n<tr>\n<td><code>CROSS_CANVAS_FORBIDDEN</code></td>\n<td>403</td>\n<td>A request targeted another canvas's resources.</td>\n</tr>\n<tr>\n<td><code>MODEL_NOT_ALLOWED</code></td>\n<td>403</td>\n<td>The requested AI model is not in the allow-list.</td>\n</tr>\n<tr>\n<td><code>DISABLED</code></td>\n<td>403</td>\n<td>The canvas has been disabled by an administrator.</td>\n</tr>\n<tr>\n<td><code>STATIC_ONLY</code></td>\n<td>403</td>\n<td>The canvas is a public link (<code>public_link</code>): every backend primitive is refused for non-owners.</td>\n</tr>\n<tr>\n<td><code>GUEST_AI_DISABLED</code></td>\n<td>403</td>\n<td>AI is not enabled for a retained legacy guest-session viewer.</td>\n</tr>\n<tr>\n<td><code>GUEST_AI_CAP</code></td>\n<td>429</td>\n<td>The canvas reached its retained legacy guest-session AI spend cap.</td>\n</tr>\n<tr>\n<td><code>NOT_FOUND</code></td>\n<td>404</td>\n<td>The key, file, or canvas does not exist.</td>\n</tr>\n<tr>\n<td><code>INVALID_BODY</code></td>\n<td>400</td>\n<td>The request body failed validation.</td>\n</tr>\n<tr>\n<td><code>KEY_TOO_LARGE</code></td>\n<td>413</td>\n<td>The KV key exceeds the size limit.</td>\n</tr>\n<tr>\n<td><code>VALUE_TOO_LARGE</code></td>\n<td>413</td>\n<td>The KV value exceeds the size limit.</td>\n</tr>\n<tr>\n<td><code>FILE_TOO_LARGE</code></td>\n<td>413</td>\n<td>An uploaded file exceeds the per-file size limit.</td>\n</tr>\n<tr>\n<td><code>KEY_LIMIT</code></td>\n<td>409</td>\n<td>The canvas hit its key-count limit.</td>\n</tr>\n<tr>\n<td><code>NOT_NUMERIC</code></td>\n<td>409</td>\n<td><code>increment</code> was called on a non-numeric value.</td>\n</tr>\n<tr>\n<td><code>QUOTA_EXCEEDED</code></td>\n<td>429</td>\n<td>A spend or rate quota was exceeded.</td>\n</tr>\n<tr>\n<td><code>CONNECTION_LIMIT</code></td>\n<td>429</td>\n<td>A realtime or outbound connection concurrency limit was reached.</td>\n</tr>\n<tr>\n<td><code>CONNECTION_RATE_LIMIT</code></td>\n<td>429</td>\n<td>The outbound connection rate limit was reached.</td>\n</tr>\n<tr>\n<td><code>CONNECTION_NOT_GRANTED</code></td>\n<td>404</td>\n<td>The outbound connection is not granted to this canvas.</td>\n</tr>\n<tr>\n<td><code>CONNECTION_DISABLED</code></td>\n<td>503</td>\n<td>The outbound connection profile is disabled.</td>\n</tr>\n<tr>\n<td><code>CONNECTION_KEY_UNAVAILABLE</code></td>\n<td>503</td>\n<td>The protected-header encryption key is unavailable.</td>\n</tr>\n<tr>\n<td><code>METHOD_NOT_ALLOWED</code></td>\n<td>405</td>\n<td>The outbound method is unsupported or not allowed by the profile.</td>\n</tr>\n<tr>\n<td><code>DESTINATION_BLOCKED</code></td>\n<td>403</td>\n<td>The outbound origin, DNS answer, redirect, or encoding crossed the approved boundary.</td>\n</tr>\n<tr>\n<td><code>UPSTREAM_TIMEOUT</code></td>\n<td>504</td>\n<td>The total outbound deadline expired.</td>\n</tr>\n<tr>\n<td><code>UPSTREAM_UNAVAILABLE</code></td>\n<td>502</td>\n<td>DNS or the approved upstream failed.</td>\n</tr>\n<tr>\n<td><code>RESPONSE_TOO_LARGE</code></td>\n<td>502</td>\n<td>The upstream response exceeded its limit.</td>\n</tr>\n<tr>\n<td><code>REQUEST_TOO_LARGE</code></td>\n<td>413</td>\n<td>The connection URL, headers, or request body exceeded its limit.</td>\n</tr>\n<tr>\n<td><code>AI_STREAM_TRUNCATED</code></td>\n<td>502</td>\n<td>An AI stream ended before completion.</td>\n</tr>\n<tr>\n<td><code>AI_UPSTREAM_ERROR</code></td>\n<td>502</td>\n<td>The AI provider returned an error.</td>\n</tr>\n<tr>\n<td><code>PUBLISH_FAILED</code></td>\n<td>502</td>\n<td><code>canvasdrop.canvases.publish</code> created the canvas but its deploy or share-config failed; the new canvas's id is returned so the caller can retry or revoke.</td>\n</tr>\n<tr>\n<td><code>SHARE_REVOKED</code></td>\n<td>409</td>\n<td><code>canvasdrop.canvases.update</code> was called on an unpublished share without a bundle; include a bundle to publish it again.</td>\n</tr>\n<tr>\n<td><code>SHARE_CONFLICT</code></td>\n<td>409</td>\n<td><code>canvasdrop.canvases.update</code> sent a stale <code>expectedUpdatedAt</code>: the share changed after it was read. The current state is returned; refresh before retrying.</td>\n</tr>\n<tr>\n<td><code>UPDATE_PARTIAL</code></td>\n<td>502</td>\n<td><code>canvasdrop.canvases.update</code> saved the share settings, but a later stage (the bundle deploy) failed; <code>stage</code> names it and <code>current</code> carries the saved state.</td>\n</tr>\n<tr>\n<td><code>REQUEST_FAILED</code></td>\n<td>0</td>\n<td>A request failed without a more specific code.</td>\n</tr>\n</tbody></table>\n<p>Five rows need a note:</p>\n<ul>\n<li><code>NOT_AUTHENTICATED</code> is what the SDK throws for <strong>any</strong> 401. The auth gateway's\nown 401 (in <code>dev</code> and <code>proxy</code> modes) has the shape <code>{ \"error\": \"unauthorized\" }</code>\nwith no <code>code</code>; the SDK normalizes it. In <code>oidc</code> mode the gateway does not\nreturn 401 at all: a signed-out request is redirected (302) to the login page.\nThe authoring routes return <code>401 { \"code\": \"NOT_AUTHENTICATED\" }</code> to a retained\nlegacy guest session, since only org members can author.</li>\n<li><code>STATIC_ONLY</code> applies to every caller except the canvas's owner and its\neditors. A signed-in member gets it on a Public link canvas too, not only an\nanonymous visitor.</li>\n<li><code>QUOTA_EXCEEDED</code> is 429 from AI (the body adds <code>scope</code>: <code>user_daily</code> or\n<code>canvas_monthly</code>) and from authoring (<code>scope</code>: <code>user_daily</code> or <code>user_total</code>),\nbut <strong>409</strong> when a file upload would exceed the per-canvas byte quota.\n<code>err.code</code> is the same in all three cases; <code>err.status</code> and <code>scope</code> tell them\napart.</li>\n<li><code>INVALID_BODY</code> is 400 everywhere except one case: an authoring bundle over\n50 MiB returns <code>413 { \"code\": \"INVALID_BODY\", \"message\": \"bundle too large\" }</code>,\nand the SDK's any-413 rule puts it on <code>QuotaExceededError</code> (see below).</li>\n<li><code>REQUEST_FAILED</code> is the SDK's fallback when a response carries no <code>code</code>. The\n<code>0</code> is nominal; <code>err.status</code> holds the real HTTP status.</li>\n</ul>\n<blockquote>\n<p><strong><code>CAPABILITY_DISABLED</code> is self-repairing.</strong> Its 403 body carries extra fields\nbeyond <code>code</code>, so a caller (or an agent) can fix it without guessing:\n<code>capability</code> (which one), <code>backendEnabled</code> (the master switch, <code>false</code> on a new\ncanvas), <code>reason</code> (<code>backend_off</code>, <code>feature_off</code>, or <code>operator_disabled</code>), and a\nhuman-readable <code>hint</code>. The SDK puts the hint on the thrown\n<code>CapabilityDisabledError</code> as both its message and <code>.hint</code>. For <code>backend_off</code> and\n<code>feature_off</code> the fix is the owner's or an editor's: the canvas's <strong>Backend</strong> tab,\nthe <code>set_capabilities</code> MCP tool, or\n<code>PATCH /api/canvases/{id}/capabilities {\"backendEnabled\": true, \"kv\": true}</code>.\n<code>operator_disabled</code> (AI, realtime, and authoring only) is a deployment-level\nsetting: no AI provider key, <code>CANVAS_DROP_REALTIME=off</code>, or\n<code>CANVAS_DROP_AUTHORING=off</code> (the default). See\n<a href=\"/docs/authoring/capabilities\">Capabilities</a>.</p>\n</blockquote>\n<blockquote>\n<p><strong><code>DISABLED</code> has two surfaces.</strong> On the Runtime API (viewers, the browser SDK) a\ncanvas an admin has taken down returns <code>DISABLED</code> with status <strong>403</strong>, the row\nabove. On the owner management API (<code>/api/canvases/{id}/...</code>), the authoring\nroutes' <code>PUT</code>/<code>DELETE</code>, and over MCP, the same takedown makes the canvas\nread-only to its owner and editors: reads succeed, but every mutation (settings,\nsharing, tags, capabilities, slug, preview, deploy, publish, rollback, archive,\nunpublish, draft edits) is refused with HTTP <strong>409</strong>\n<code>{ \"code\": \"DISABLED\", \"message\": \"This canvas has been disabled by an administrator.\" }</code>,\nwith <code> Reason: &lt;text&gt;</code> appended when the admin set one. The MCP tool result is\n<code>DISABLED: &lt;that message&gt;</code>. The same management surfaces refuse an editor's\nowner-only act (delete, transfer, the guest-AI opt-in) with <strong>403</strong>\n<code>{ \"code\": \"OWNER_ONLY\", \"message\": \"Only the canvas owner can do this.\" }</code>; do\nnot confuse it with the Runtime API's <code>OWNER_ONLY</code>, which is a 404 (next section).</p>\n</blockquote>\n<h2 id=\"typed-sdk-errors\">Typed SDK errors</h2>\n<p>The SDK exports the base class and six subclasses. Any code without a dedicated\nsubclass is thrown as the base <code>CanvasdropError</code> with <code>.code</code> set from the\nresponse.</p>\n<table>\n<thead>\n<tr>\n<th>Class</th>\n<th><code>.code</code></th>\n<th><code>.status</code></th>\n<th>Extra</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NotAuthenticatedError</code></td>\n<td><code>NOT_AUTHENTICATED</code></td>\n<td>401</td>\n<td></td>\n</tr>\n<tr>\n<td><code>CapabilityDisabledError</code></td>\n<td><code>CAPABILITY_DISABLED</code></td>\n<td>403</td>\n<td>message and <code>.hint</code> from the server's <code>hint</code>; no <code>.capability</code> property</td>\n</tr>\n<tr>\n<td><code>NotFoundError</code></td>\n<td><code>NOT_FOUND</code></td>\n<td>404</td>\n<td>Every 404 except <code>CONNECTION_NOT_GRANTED</code>.</td>\n</tr>\n<tr>\n<td><code>QuotaExceededError</code></td>\n<td><code>QUOTA_EXCEEDED</code> by default; see below</td>\n<td>429 by default; see below</td>\n<td>message is always <code>quota exceeded</code></td>\n</tr>\n<tr>\n<td><code>PublishFailedError</code></td>\n<td><code>PUBLISH_FAILED</code></td>\n<td>502</td>\n<td><code>.id</code>: the created canvas's id, when the failure happened after creation</td>\n</tr>\n<tr>\n<td><code>UpdatePartialError</code></td>\n<td><code>UPDATE_PARTIAL</code></td>\n<td>502</td>\n<td><code>.stage</code>: the stage that failed after the settings were saved; <code>.current</code>: the saved share state, when the server returned it</td>\n</tr>\n<tr>\n<td><code>CanvasdropError</code> (base)</td>\n<td>any</td>\n<td>any</td>\n<td><code>.hint</code> when the server sent one</td>\n</tr>\n</tbody></table>\n<p>The SDK picks the class from the HTTP response in this order (<code>errorFromResponse</code>):</p>\n<ol>\n<li>Status 401 → <code>NotAuthenticatedError</code> (any body <code>code</code> is ignored).</li>\n<li>Status 403 with <code>code: \"CAPABILITY_DISABLED\"</code> → <code>CapabilityDisabledError</code>.</li>\n<li>Status 404, except <code>code: \"CONNECTION_NOT_GRANTED\"</code>, → <code>NotFoundError</code> (the other body codes are ignored; see the next section).</li>\n<li><code>code: \"PUBLISH_FAILED\"</code> → <code>PublishFailedError</code> with <code>.id</code> from the body.</li>\n<li><code>code: \"UPDATE_PARTIAL\"</code> → <code>UpdatePartialError</code> with <code>.stage</code> and <code>.current</code> from the body.</li>\n<li><code>code</code> of <code>QUOTA_EXCEEDED</code>, <code>GUEST_AI_CAP</code>, <code>KEY_LIMIT</code>, <code>CONNECTION_LIMIT</code>, or <code>CONNECTION_RATE_LIMIT</code>, or <strong>any 413</strong> →\n<code>QuotaExceededError</code>, keeping the body's <code>code</code> and the response's status.</li>\n<li>Anything else—including <code>CONNECTION_NOT_GRANTED</code> at 404—→ <code>CanvasdropError</code> with the body's <code>code</code> (or <code>REQUEST_FAILED</code>),\nthe response status, and the body's <code>hint</code> or <code>message</code> as the message.</li>\n</ol>\n<p>So <code>QuotaExceededError</code> is the one limit-shaped class and it is reused: expect\n<code>.code</code> values of <code>QUOTA_EXCEEDED</code>, <code>GUEST_AI_CAP</code>, <code>KEY_LIMIT</code> (409),\n<code>KEY_TOO_LARGE</code>, <code>VALUE_TOO_LARGE</code>, <code>FILE_TOO_LARGE</code>, <code>BODY_TOO_LARGE</code>,\n<code>INVALID_BODY</code> (413), <code>CONNECTION_LIMIT</code> (429, realtime or outbound admission), and\n<code>CONNECTION_RATE_LIMIT</code> (429). <code>NOT_NUMERIC</code>\nis a 409 but not a limit, so it stays on the base class. Never assume a\n<code>QuotaExceededError</code> is literally <code>QUOTA_EXCEEDED</code>; read <code>err.code</code>.</p>\n<p>Classes such as <code>PasswordRequiredError</code>, <code>ModelNotAllowedError</code>, or\n<code>CrossCanvasForbiddenError</code> do not exist. <code>PASSWORD_REQUIRED</code>, <code>STATIC_ONLY</code>,\n<code>DISABLED</code>, <code>MODEL_NOT_ALLOWED</code>, <code>CROSS_CANVAS_FORBIDDEN</code>, <code>GUEST_AI_DISABLED</code>,\n<code>INVALID_BODY</code> (at 400), <code>NOT_NUMERIC</code>, <code>SHARE_REVOKED</code>, <code>SHARE_CONFLICT</code>, and\n<code>AI_UPSTREAM_ERROR</code> all arrive on the base <code>CanvasdropError</code>. Branching on <code>err.code</code> is the only\nreliable check.</p>\n<h2 id=\"server-codes-outside-the-enum\">Server codes outside the enum</h2>\n<p>The Runtime API can return a few codes that are not in <code>ERROR_CODES</code>. They are\nstable; the table shows how each reaches SDK callers.</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Status</th>\n<th>When</th>\n<th>In the SDK</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>RATE_LIMITED</code></td>\n<td>429</td>\n<td>A request bucket is spent. Primitives: per user per canvas, default 120/min. <code>.../ai/*</code>: per user across all canvases, default 10/min. Headers: <code>Retry-After</code>, <code>X-RateLimit-Limit</code>, <code>X-RateLimit-Remaining</code>, <code>X-RateLimit-Reset</code>.</td>\n<td><code>CanvasdropError</code>, <code>code: \"RATE_LIMITED\"</code></td>\n</tr>\n<tr>\n<td><code>ARCHIVED</code>, <code>NOT_INVITED</code>, <code>OWNER_ONLY</code>, <code>SHARE_EXPIRED</code></td>\n<td>404</td>\n<td>The canvas resolved but access was denied (archived; guest scoped to another canvas; not on the sharing rung, including a non-owner admin; share expired).</td>\n<td><code>NotFoundError</code>, <code>code: \"NOT_FOUND\"</code>; the body's code is not preserved</td>\n</tr>\n<tr>\n<td><code>CROSS_SITE_FORBIDDEN</code></td>\n<td>403</td>\n<td>Path mode only: <code>Sec-Fetch-Site</code> is present and not <code>same-origin</code> or <code>none</code>.</td>\n<td><code>CanvasdropError</code></td>\n</tr>\n<tr>\n<td><code>BODY_TOO_LARGE</code></td>\n<td>413</td>\n<td><code>POST .../ai/chat</code> body over 256 KiB.</td>\n<td><code>QuotaExceededError</code>, <code>code: \"BODY_TOO_LARGE\"</code> (every 413 maps there)</td>\n</tr>\n<tr>\n<td><code>ORG_REQUIRED</code>, <code>SLUG_TAKEN</code></td>\n<td>409</td>\n<td>Authoring publish/update.</td>\n<td><code>CanvasdropError</code></td>\n</tr>\n<tr>\n<td><code>PUBLIC_LINKS_DISABLED</code>, <code>PUBLIC_NOT_ALLOWED</code>, <code>PUBLIC_LINK_OWNER_GATED</code></td>\n<td>403</td>\n<td>Authoring publish/update asked for a Public link the instance, the actor, or the actor's role (editor) cannot grant.</td>\n<td><code>CanvasdropError</code></td>\n</tr>\n</tbody></table>\n<p>The full per-route list, including which routes emit <code>INVALID_BODY</code> with a\n<code>reason</code> field, is on the <a href=\"/docs/api/runtime-api\">Runtime API</a> page.</p>\n<h2 id=\"client-side-codes\">Client-side codes</h2>\n<p>The SDK also throws a handful of codes that never come from the server. They are\nbase <code>CanvasdropError</code>s and are not in <code>ERROR_CODES</code>, so <code>err.code</code> is typed\n<code>string</code>, not <code>ErrorCode</code>.</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Status</th>\n<th>When</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NO_STREAM</code></td>\n<td>the HTTP status</td>\n<td>The AI response had no body.</td>\n</tr>\n<tr>\n<td><code>MALFORMED_FRAME</code></td>\n<td>502</td>\n<td>An SSE <code>data:</code> line was not valid JSON (proxy teardown, partial flush).</td>\n</tr>\n<tr>\n<td><code>AI_ERROR</code></td>\n<td>502</td>\n<td>An in-stream <code>error</code> frame arrived without a string <code>code</code>.</td>\n</tr>\n<tr>\n<td><code>DISCONNECTED</code></td>\n<td>0</td>\n<td>The realtime socket dropped while a <code>presence()</code> call was in flight; the SDK is reconnecting, retry the call.</td>\n</tr>\n<tr>\n<td><code>CHANNEL_CLOSED</code></td>\n<td>0</td>\n<td><code>publish()</code> or <code>presence()</code> was called on a channel after <code>close()</code>; pending <code>presence()</code> calls also reject with it when <code>close()</code> runs.</td>\n</tr>\n</tbody></table>\n<h2 id=\"ai-stream-errors\">AI stream errors</h2>\n<p><code>ai.chat</code> and <code>ai.stream</code> consume a server-sent event stream. Failures surface in\ntwo ways:</p>\n<ul>\n<li><strong>Before the stream starts</strong>, as an HTTP error mapped per the tables above, in\nthe server's check order: <code>BODY_TOO_LARGE</code> (413), <code>INVALID_BODY</code> (400),\n<code>MODEL_NOT_ALLOWED</code> (403), <code>GUEST_AI_DISABLED</code> (403), <code>GUEST_AI_CAP</code> (429),\n<code>QUOTA_EXCEEDED</code> (429), or <code>CAPABILITY_DISABLED</code> (403). <code>RATE_LIMITED</code> (429)\nfires before any of them.</li>\n<li><strong>Mid-stream</strong>, as an <code>error</code> frame. <code>CAPABILITY_DISABLED</code> becomes\n<code>CapabilityDisabledError</code>; <code>QUOTA_EXCEEDED</code> and <code>GUEST_AI_CAP</code> become\n<code>QuotaExceededError</code> with status 429; any other code becomes a base\n<code>CanvasdropError</code> with that code and status 502. The server's mid-stream\nprovider failure is <code>AI_UPSTREAM_ERROR</code>; a frame with no code defaults to\n<code>AI_ERROR</code>.</li>\n</ul>\n<p>If the stream ends without a terminal <code>done</code> or <code>error</code> frame, both methods\nthrow <code>AI_STREAM_TRUNCATED</code> (502). <code>ai.stream</code> yields text only; usage and cost\nare available from <code>ai.chat</code>.</p>\n<h2 id=\"realtime-close-codes\">Realtime close codes</h2>\n<p>A terminal WebSocket close maps to a typed error and stops reconnecting. Any\npending <code>presence()</code> call rejects with it, and later <code>publish()</code> or <code>presence()</code>\ncalls on the same client throw it.</p>\n<table>\n<thead>\n<tr>\n<th>Close code</th>\n<th>Error</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>4403</code></td>\n<td><code>CapabilityDisabledError</code> (<code>realtime</code> is off)</td>\n</tr>\n<tr>\n<td><code>4401</code></td>\n<td><code>NotAuthenticatedError</code> (access lost, canvas gone, canvas turned Public link, password gate newly set, or user inactive)</td>\n</tr>\n<tr>\n<td><code>4429</code></td>\n<td><code>QuotaExceededError</code> (<code>code: \"CONNECTION_LIMIT\"</code>, status 429; 30 connections per canvas)</td>\n</tr>\n</tbody></table>\n<p>An in-band <code>{ \"type\": \"error\", \"code\": \"CAPABILITY_DISABLED\" }</code> frame is terminal\nin the same way. The hub's other error frames (<code>MESSAGE_TOO_LARGE</code>,\n<code>INVALID_FRAME</code>, <code>CHANNEL_NAME_TOO_LARGE</code>, <code>CHANNEL_LIMIT</code>, <code>RATE_LIMITED</code>,\n<code>UNKNOWN_FRAME</code>) are dropped by the SDK; there is no hook to observe them, so a\nrejected publish is silent. Stay inside the limits on the\n<a href=\"/docs/api/runtime-api#realtime\">Runtime API</a> page.</p>\n<p>Any other close is transient: the SDK reconnects with exponential backoff (500 ms\ndoubling to a 10 s cap), re-subscribes every channel, and flushes up to 256\nbuffered frames. An in-flight <code>presence()</code> call rejects with <code>DISCONNECTED</code>\n(status 0) so the caller can retry.</p>\n",
    "headings": [
      {
        "id": "the-codes",
        "text": "The codes",
        "level": 2
      },
      {
        "id": "typed-sdk-errors",
        "text": "Typed SDK errors",
        "level": 2
      },
      {
        "id": "server-codes-outside-the-enum",
        "text": "Server codes outside the enum",
        "level": 2
      },
      {
        "id": "client-side-codes",
        "text": "Client-side codes",
        "level": 2
      },
      {
        "id": "ai-stream-errors",
        "text": "AI stream errors",
        "level": 2
      },
      {
        "id": "realtime-close-codes",
        "text": "Realtime close codes",
        "level": 2
      }
    ],
    "text": "Error codes If you call the Runtime API or the browser SDK from a canvas, or you are an agent handling its failures, this page is the contract. Branch on the error's code , never on its message text. Every refusal from /v1/c/{slug}/* is JSON with a stable code and an HTTP status, and the browser SDK throws typed errors that extend CanvasdropError , each with a readonly .code and .status (plus .hint when the server sent a remediation hint). The browser global is window.canvasdrop , loaded from {base}/sdk/v1.js ; there is no cd alias. The error classes, ERROR_CODES , and errorFromResponse are named exports of @canvas-drop/sdk for code that imports the package. try { await canvasdrop. kv . set ( \"prefs\" , { theme : \"dark\" }); } catch (err) { switch (err. code ) { case \"CAPABILITY_DISABLED\" : // KV is off for this canvas; the hint says how to turn it on showBanner (err. hint ); break ; case \"VALUE_TOO_LARGE\" : case \"KEY_LIMIT\" : showBanner ( \"Storage limit reached.\" ); break ; default : throw err; } } kv.get returns null for a missing key instead of throwing, so most reads need no try/catch . The codes Codes, statuses, and meanings below are the SDK's exported ERROR_CODES , in the same order (each entry is { status, summary } ; ErrorCode is its key type). Code Status Meaning NOT_AUTHENTICATED 401 The viewer is not signed in. PASSWORD_REQUIRED 403 The canvas is password-protected. CAPABILITY_DISABLED 403 Backend or the specific feature is off for this canvas. CROSS_CANVAS_FORBIDDEN 403 A request targeted another canvas's resources. MODEL_NOT_ALLOWED 403 The requested AI model is not in the allow-list. DISABLED 403 The canvas has been disabled by an administrator. STATIC_ONLY 403 The canvas is a public link ( public_link ): every backend primitive is refused for non-owners. GUEST_AI_DISABLED 403 AI is not enabled for a retained legacy guest-session viewer. GUEST_AI_CAP 429 The canvas reached its retained legacy guest-session AI spend cap. NOT_FOUND 404 The key, file, or canvas does not exist. INVALID_BODY 400 The request body failed validation. KEY_TOO_LARGE 413 The KV key exceeds the size limit. VALUE_TOO_LARGE 413 The KV value exceeds the size limit. FILE_TOO_LARGE 413 An uploaded file exceeds the per-file size limit. KEY_LIMIT 409 The canvas hit its key-count limit. NOT_NUMERIC 409 increment was called on a non-numeric value. QUOTA_EXCEEDED 429 A spend or rate quota was exceeded. CONNECTION_LIMIT 429 A realtime or outbound connection concurrency limit was reached. CONNECTION_RATE_LIMIT 429 The outbound connection rate limit was reached. CONNECTION_NOT_GRANTED 404 The outbound connection is not granted to this canvas. CONNECTION_DISABLED 503 The outbound connection profile is disabled. CONNECTION_KEY_UNAVAILABLE 503 The protected-header encryption key is unavailable. METHOD_NOT_ALLOWED 405 The outbound method is unsupported or not allowed by the profile. DESTINATION_BLOCKED 403 The outbound origin, DNS answer, redirect, or encoding crossed the approved boundary. UPSTREAM_TIMEOUT 504 The total outbound deadline expired. UPSTREAM_UNAVAILABLE 502 DNS or the approved upstream failed. RESPONSE_TOO_LARGE 502 The upstream response exceeded its limit. REQUEST_TOO_LARGE 413 The connection URL, headers, or request body exceeded its limit. AI_STREAM_TRUNCATED 502 An AI stream ended before completion. AI_UPSTREAM_ERROR 502 The AI provider returned an error. PUBLISH_FAILED 502 canvasdrop.canvases.publish created the canvas but its deploy or share-config failed; the new canvas's id is returned so the caller can retry or revoke. SHARE_REVOKED 409 canvasdrop.canvases.update was called on an unpublished share without a bundle; include a bundle to publish it again. SHARE_CONFLICT 409 canvasdrop.canvases.update sent a stale expectedUpdatedAt : the share changed after it was read. The current state is returned; refresh before retrying. UPDATE_PARTIAL 502 canvasdrop.canvases.update saved the share settings, but a later stage (the bundle deploy) failed; stage names it and current carries the saved state. REQUEST_FAILED 0 A request failed without a more specific code. Five rows need a note: NOT_AUTHENTICATED is what the SDK throws for any 401. The auth gateway's own 401 (in dev and proxy modes) has the shape { \"error\": \"unauthorized\" } with no code ; the SDK normalizes it. In oidc mode the gateway does not return 401 at all: a signed-out request is redirected (302) to the login page. The authoring routes return 401 { \"code\": \"NOT_AUTHENTICATED\" } to a retained legacy guest session, since only org members can author. STATIC_ONLY applies to every caller except the canvas's owner and its editors. A signed-in member gets it on a Public link canvas too, not only an anonymous visitor. QUOTA_EXCEEDED is 429 from AI (the body adds scope : user_daily or canvas_monthly ) and from authoring ( scope : user_daily or user_total ), but 409 when a file upload would exceed the per-canvas byte quota. err.code is the same in all three cases; err.status and scope tell them apart. INVALID_BODY is 400 everywhere except one case: an authoring bundle over 50 MiB returns 413 { \"code\": \"INVALID_BODY\", \"message\": \"bundle too large\" } , and the SDK's any-413 rule puts it on QuotaExceededError (see below). REQUEST_FAILED is the SDK's fallback when a response carries no code . The 0 is nominal; err.status holds the real HTTP status. CAPABILITY_DISABLED is self-repairing. Its 403 body carries extra fields beyond code , so a caller (or an agent) can fix it without guessing: capability (which one), backendEnabled (the master switch, false on a new canvas), reason ( backend_off , feature_off , or operator_disabled ), and a human-readable hint . The SDK puts the hint on the thrown CapabilityDisabledError as both its message and .hint . For backend_off and feature_off the fix is the owner's or an editor's: the canvas's Backend tab, the set_capabilities MCP tool, or PATCH /api/canvases/{id}/capabilities {\"backendEnabled\": true, \"kv\": true} . operator_disabled (AI, realtime, and authoring only) is a deployment-level setting: no AI provider key, CANVAS_DROP_REALTIME=off , or CANVAS_DROP_AUTHORING=off (the default). See Capabilities . DISABLED has two surfaces. On the Runtime API (viewers, the browser SDK) a canvas an admin has taken down returns DISABLED with status 403 , the row above. On the owner management API ( /api/canvases/{id}/... ), the authoring routes' PUT / DELETE , and over MCP, the same takedown makes the canvas read-only to its owner and editors: reads succeed, but every mutation (settings, sharing, tags, capabilities, slug, preview, deploy, publish, rollback, archive, unpublish, draft edits) is refused with HTTP 409 { \"code\": \"DISABLED\", \"message\": \"This canvas has been disabled by an administrator.\" } , with Reason: <text> appended when the admin set one. The MCP tool result is DISABLED: <that message> . The same management surfaces refuse an editor's owner-only act (delete, transfer, the guest-AI opt-in) with 403 { \"code\": \"OWNER_ONLY\", \"message\": \"Only the canvas owner can do this.\" } ; do not confuse it with the Runtime API's OWNER_ONLY , which is a 404 (next section). Typed SDK errors The SDK exports the base class and six subclasses. Any code without a dedicated subclass is thrown as the base CanvasdropError with .code set from the response. Class .code .status Extra NotAuthenticatedError NOT_AUTHENTICATED 401 CapabilityDisabledError CAPABILITY_DISABLED 403 message and .hint from the server's hint ; no .capability property NotFoundError NOT_FOUND 404 Every 404 except CONNECTION_NOT_GRANTED . QuotaExceededError QUOTA_EXCEEDED by default; see below 429 by default; see below message is always quota exceeded PublishFailedError PUBLISH_FAILED 502 .id : the created canvas's id, when the failure happened after creation UpdatePartialError UPDATE_PARTIAL 502 .stage : the stage that failed after the settings were saved; .current : the saved share state, when the server returned it CanvasdropError (base) any any .hint when the server sent one The SDK picks the class from the HTTP response in this order ( errorFromResponse ): Status 401 → NotAuthenticatedError (any body code is ignored). Status 403 with code: \"CAPABILITY_DISABLED\" → CapabilityDisabledError . Status 404, except code: \"CONNECTION_NOT_GRANTED\" , → NotFoundError (the other body codes are ignored; see the next section). code: \"PUBLISH_FAILED\" → PublishFailedError with .id from the body. code: \"UPDATE_PARTIAL\" → UpdatePartialError with .stage and .current from the body. code of QUOTA_EXCEEDED , GUEST_AI_CAP , KEY_LIMIT , CONNECTION_LIMIT , or CONNECTION_RATE_LIMIT , or any 413 → QuotaExceededError , keeping the body's code and the response's status. Anything else—including CONNECTION_NOT_GRANTED at 404—→ CanvasdropError with the body's code (or REQUEST_FAILED ), the response status, and the body's hint or message as the message. So QuotaExceededError is the one limit-shaped class and it is reused: expect .code values of QUOTA_EXCEEDED , GUEST_AI_CAP , KEY_LIMIT (409), KEY_TOO_LARGE , VALUE_TOO_LARGE , FILE_TOO_LARGE , BODY_TOO_LARGE , INVALID_BODY (413), CONNECTION_LIMIT (429, realtime or outbound admission), and CONNECTION_RATE_LIMIT (429). NOT_NUMERIC is a 409 but not a limit, so it stays on the base class. Never assume a QuotaExceededError is literally QUOTA_EXCEEDED ; read err.code . Classes such as PasswordRequiredError , ModelNotAllowedError , or CrossCanvasForbiddenError do not exist. PASSWORD_REQUIRED , STATIC_ONLY , DISABLED , MODEL_NOT_ALLOWED , CROSS_CANVAS_FORBIDDEN , GUEST_AI_DISABLED , INVALID_BODY (at 400), NOT_NUMERIC , SHARE_REVOKED , SHARE_CONFLICT , and AI_UPSTREAM_ERROR all arrive on the base CanvasdropError . Branching on err.code is the only reliable check. Server codes outside the enum The Runtime API can return a few codes that are not in ERROR_CODES . They are stable; the table shows how each reaches SDK callers. Code Status When In the SDK RATE_LIMITED 429 A request bucket is spent. Primitives: per user per canvas, default 120/min. .../ai/* : per user across all canvases, default 10/min. Headers: Retry-After , X-RateLimit-Limit , X-RateLimit-Remaining , X-RateLimit-Reset . CanvasdropError , code: \"RATE_LIMITED\" ARCHIVED , NOT_INVITED , OWNER_ONLY , SHARE_EXPIRED 404 The canvas resolved but access was denied (archived; guest scoped to another canvas; not on the sharing rung, including a non-owner admin; share expired). NotFoundError , code: \"NOT_FOUND\" ; the body's code is not preserved CROSS_SITE_FORBIDDEN 403 Path mode only: Sec-Fetch-Site is present and not same-origin or none . CanvasdropError BODY_TOO_LARGE 413 POST .../ai/chat body over 256 KiB. QuotaExceededError , code: \"BODY_TOO_LARGE\" (every 413 maps there) ORG_REQUIRED , SLUG_TAKEN 409 Authoring publish/update. CanvasdropError PUBLIC_LINKS_DISABLED , PUBLIC_NOT_ALLOWED , PUBLIC_LINK_OWNER_GATED 403 Authoring publish/update asked for a Public link the instance, the actor, or the actor's role (editor) cannot grant. CanvasdropError The full per-route list, including which routes emit INVALID_BODY with a reason field, is on the Runtime API page. Client-side codes The SDK also throws a handful of codes that never come from the server. They are base CanvasdropError s and are not in ERROR_CODES , so err.code is typed string , not ErrorCode . Code Status When NO_STREAM the HTTP status The AI response had no body. MALFORMED_FRAME 502 An SSE data: line was not valid JSON (proxy teardown, partial flush). AI_ERROR 502 An in-stream error frame arrived without a string code . DISCONNECTED 0 The realtime socket dropped while a presence() call was in flight; the SDK is reconnecting, retry the call. CHANNEL_CLOSED 0 publish() or presence() was called on a channel after close() ; pending presence() calls also reject with it when close() runs. AI stream errors ai.chat and ai.stream consume a server-sent event stream. Failures surface in two ways: Before the stream starts , as an HTTP error mapped per the tables above, in the server's check order: BODY_TOO_LARGE (413), INVALID_BODY (400), MODEL_NOT_ALLOWED (403), GUEST_AI_DISABLED (403), GUEST_AI_CAP (429), QUOTA_EXCEEDED (429), or CAPABILITY_DISABLED (403). RATE_LIMITED (429) fires before any of them. Mid-stream , as an error frame. CAPABILITY_DISABLED becomes CapabilityDisabledError ; QUOTA_EXCEEDED and GUEST_AI_CAP become QuotaExceededError with status 429; any other code becomes a base CanvasdropError with that code and status 502. The server's mid-stream provider failure is AI_UPSTREAM_ERROR ; a frame with no code defaults to AI_ERROR . If the stream ends without a terminal done or error frame, both methods throw AI_STREAM_TRUNCATED (502). ai.stream yields text only; usage and cost are available from ai.chat . Realtime close codes A terminal WebSocket close maps to a typed error and stops reconnecting. Any pending presence() call rejects with it, and later publish() or presence() calls on the same client throw it. Close code Error 4403 CapabilityDisabledError ( realtime is off) 4401 NotAuthenticatedError (access lost, canvas gone, canvas turned Public link, password gate newly set, or user inactive) 4429 QuotaExceededError ( code: \"CONNECTION_LIMIT\" , status 429; 30 connections per canvas) An in-band { \"type\": \"error\", \"code\": \"CAPABILITY_DISABLED\" } frame is terminal in the same way. The hub's other error frames ( MESSAGE_TOO_LARGE , INVALID_FRAME , CHANNEL_NAME_TOO_LARGE , CHANNEL_LIMIT , RATE_LIMITED , UNKNOWN_FRAME ) are dropped by the SDK; there is no hook to observe them, so a rejected publish is silent. Stay inside the limits on the Runtime API page. Any other close is transient: the SDK reconnects with exponential backoff (500 ms doubling to a 10 s cap), re-subscribes every channel, and flushes up to 256 buffered frames. An in-flight presence() call rejects with DISCONNECTED (status 0) so the caller can retry."
  },
  {
    "path": "self-hosting/install",
    "section": "Self-hosting",
    "title": "Install",
    "html": "<h1 id=\"install\">Install</h1>\n<p>You are standing up canvas-drop for the first time. By the end of this page you\nhave an instance you can sign in to, and you know which database, storage, URL\nmode, and auth driver it is running. Two starting points, same repository:</p>\n<pre><code class=\"hljs language-bash\">git <span class=\"hljs-built_in\">clone</span> https://github.com/markpasternak/canvas-drop.git\n<span class=\"hljs-built_in\">cd</span> canvas-drop\n\n<span class=\"hljs-comment\"># Evaluate or self-host: the production shape behind an identity-aware proxy</span>\ndocker compose up --build\n<span class=\"hljs-comment\"># open http://localhost:8080  and sign in as  demo@example.com / canvasdrop</span>\n\n<span class=\"hljs-comment\"># Develop canvas-drop itself: SQLite, local storage, automatic sign-in</span>\npnpm install &amp;&amp; <span class=\"hljs-built_in\">cp</span> .env.example .<span class=\"hljs-built_in\">env</span> &amp;&amp; pnpm dev\n<span class=\"hljs-comment\"># open http://localhost:5173</span></code></pre>\n<table>\n<thead>\n<tr>\n<th>Path</th>\n<th>Needs</th>\n<th>What you get</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><a href=\"#docker-compose\">Docker Compose</a></td>\n<td>Docker with Compose v2 (<code>docker compose</code>, not the legacy <code>docker-compose</code>)</td>\n<td>An identity-aware proxy in front, canvas-drop in real <code>proxy</code> auth mode, Postgres, and a bundled demo identity provider on <code>http://localhost:8080</code>. Evaluate here; graduating the same files to a real IdP is configuration.</td>\n</tr>\n<tr>\n<td><a href=\"#run-from-source\">From source</a></td>\n<td>Node 24 or newer, pnpm 11</td>\n<td><code>pnpm dev</code> on <code>http://localhost:5173</code> with SQLite, local storage, and <code>dev</code> auth that signs every request in. Develop canvas-drop itself, or try Bearer deploys and MCP with no proxy in the way.</td>\n</tr>\n</tbody></table>\n<p>Both run the same code. Database, storage, URL mode, and auth are drivers behind\none switch variable each; swapping one later is a config change, never a code\nchange. The switches are listed in <a href=\"#what-you-choose-at-install-time\">What you choose at install\ntime</a>.</p>\n<h2 id=\"docker-compose\">Docker Compose</h2>\n<pre><code class=\"hljs language-bash\">docker compose up --build\ndocker compose ps app          <span class=\"hljs-comment\"># STATUS ends in \"(healthy)\" when the stack is ready</span></code></pre>\n<p>The first <code>--build</code> compiles the workspace inside the image. The server does not\nlisten until Postgres is reachable and migrations have run, and <code>GET /healthz</code>\nreturns 503 while the database is unreachable, so the container's health check\nallows a 60-second start period before it counts failures. Sign in at\n<code>http://localhost:8080</code> as <code>demo@example.com</code> / <code>canvasdrop</code>.</p>\n<p>What comes up:</p>\n<table>\n<thead>\n<tr>\n<th>Service</th>\n<th>Image</th>\n<th>Role</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>caddy</code></td>\n<td><code>caddy:2-alpine</code></td>\n<td>Edge proxy on host port <code>8080</code>, the only published port. Routes <code>/dex/*</code> to Dex and everything else to oauth2-proxy, and strips client-supplied identity and <code>Authorization</code> headers before they reach the auth layer. Plain HTTP for the demo; in production it would terminate TLS.</td>\n</tr>\n<tr>\n<td><code>oauth2-proxy</code></td>\n<td><code>quay.io/oauth2-proxy/oauth2-proxy:v7.6.0</code></td>\n<td>Identity-aware proxy. Signs users in against Dex and forwards the Dex-signed access token to the app in <code>X-Forwarded-Access-Token</code>.</td>\n</tr>\n<tr>\n<td><code>dex</code></td>\n<td><code>dexidp/dex:v2.41.1</code></td>\n<td>Bundled demo identity provider with one static user, <code>demo@example.com</code> / <code>canvasdrop</code>.</td>\n</tr>\n<tr>\n<td><code>app</code></td>\n<td>built from the repo <code>Dockerfile</code> as <code>canvas-drop:dev</code></td>\n<td>canvas-drop in <code>proxy</code> auth mode, verifying the JWT against Dex's JWKS (<code>CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL=http://dex:5556/dex/keys</code>). Path URL mode, Postgres, local storage on the <code>app-data</code> volume, a separate <code>backups</code> volume. No published port.</td>\n</tr>\n<tr>\n<td><code>postgres</code></td>\n<td><code>postgres:16-alpine</code></td>\n<td>Database, on the <code>pg-data</code> volume, with a <code>pg_isready</code> health check the app waits on.</td>\n</tr>\n<tr>\n<td><code>minio</code></td>\n<td><code>minio/minio:RELEASE.2024-11-07T00-52-20Z</code></td>\n<td>Optional S3-compatible storage. Starts only with <code>--profile minio</code> (below).</td>\n</tr>\n</tbody></table>\n<p>What to know about the demo:</p>\n<ul>\n<li>The demo user is also the instance admin (<code>CANVAS_DROP_ADMIN_EMAILS: demo@example.com</code>), so the Admin area is available on first sign-in.</li>\n<li>It runs path URL mode (<code>{base}/c/{slug}/</code>) because subdomain mode refuses to\nboot on a localhost <code>CANVAS_DROP_BASE_URL</code>; <code>CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE</code>\nis set to <code>true</code> for that reason. Path mode shares one browser origin across\nall canvases, so isolation is weaker than subdomain mode; production should use\nsubdomain mode (see the <a href=\"/docs/self-hosting/security-model\">Security model</a>).</li>\n<li>The Deploy API (<code>/v1/canvases/*</code>) and MCP (<code>/mcp</code>) are not reachable through\nthis edge: oauth2-proxy gates every route and Caddy strips <code>Authorization</code>.\nDeploy from the dashboard, or use the from-source profile to try Bearer\ndeploys.</li>\n<li>The app's config is an inline <code>environment:</code> block on the <code>app</code> service in\n<code>docker-compose.yml</code> (no <code>env_file</code>). Change it by editing that file and running\n<code>docker compose up -d</code> again.</li>\n</ul>\n<p>Pause the stack with <code>docker compose stop</code>. Tear it down and delete every volume\n(<code>app-data</code>, <code>backups</code>, <code>pg-data</code>, <code>minio-data</code>) with <code>docker compose down -v</code>.</p>\n<blockquote>\n<p>The Dex and oauth2-proxy secrets in <code>docker/</code>, the session secret in\n<code>docker-compose.yml</code>, and the <code>demo@example.com</code> login are public, demo-only\nplaceholders, and the stack serves plain HTTP in path mode. Do not expose it to a\nnetwork as-is. Rotate every secret and work through \"Graduating to a real IdP\" on\nthe <a href=\"/docs/self-hosting/deploy\">Deploy</a> page before any real use.</p>\n</blockquote>\n<h3 id=\"verify-the-launch-invariants\">Verify the launch invariants</h3>\n<p><code>scripts/compose-smoke.sh</code> boots the stack (<code>docker compose up -d --build</code>), waits\nup to two minutes for <code>app</code> to report healthy, and asserts the load-bearing\ninvariants: the app publishes no host port, an unauthenticated request is\nredirected, a forged <code>X-Forwarded-Access-Token</code> / <code>X-Auth-Request-Email</code> pair is\nstill redirected, a real Dex sign-in resolves <code>/api/me</code> as <code>demo@example.com</code>\nwith <code>authMode: proxy</code>, and the same user id survives\n<code>docker compose restart app postgres</code>. It needs <code>curl</code>.</p>\n<pre><code class=\"hljs language-bash\">./scripts/compose-smoke.sh             <span class=\"hljs-comment\"># boots, verifies, leaves the stack up</span>\nKEEP_UP=0 ./scripts/compose-smoke.sh   <span class=\"hljs-comment\"># same, then `docker compose down -v`</span></code></pre>\n<h3 id=\"switch-the-demo-to-s3-compatible-storage-minio\">Switch the demo to S3-compatible storage (MinIO)</h3>\n<p><code>--profile minio</code> adds a MinIO container (<code>minioadmin</code> / <code>minioadmin</code>, data on the\n<code>minio-data</code> volume). It does not switch the app over by itself: the <code>app</code> service\nkeeps <code>CANVAS_DROP_STORAGE: local</code> until you change it. Do this on a fresh\ninstance; blobs already written to local storage are not moved when the driver\nchanges (to move an existing instance, use <code>backup</code> and <code>restore</code>, below).</p>\n<ol>\n<li><p>Start MinIO alongside the stack. Pass <code>--profile minio</code> on every later\n<code>docker compose</code> command too, or Compose leaves the MinIO container out.</p>\n<pre><code class=\"hljs language-bash\">docker compose --profile minio up -d --build</code></pre>\n</li>\n<li><p>Create the bucket. Neither MinIO nor the storage driver creates it on first\nwrite. The compose file publishes no MinIO port, so either run your S3 client\ninside the compose network, or add <code>ports: [\"9000:9000\"]</code> to the <code>minio</code>\nservice and create it from the host (the AWS CLI works, as CI does for its\nown test bucket):</p>\n<pre><code class=\"hljs language-bash\">AWS_ACCESS_KEY_ID=minioadmin AWS_SECRET_ACCESS_KEY=minioadmin AWS_DEFAULT_REGION=us-east-1 \\\n  aws --endpoint-url http://localhost:9000 s3 mb s3://canvas-drop</code></pre>\n</li>\n<li><p>Point the app at it. In <code>docker-compose.yml</code>, replace <code>CANVAS_DROP_STORAGE: local</code>\non the <code>app</code> service with:</p>\n<pre><code class=\"hljs language-yaml\">    <span class=\"hljs-attr\">environment:</span>\n      <span class=\"hljs-comment\"># …</span>\n      <span class=\"hljs-attr\">CANVAS_DROP_STORAGE:</span> <span class=\"hljs-string\">s3</span>\n      <span class=\"hljs-attr\">CANVAS_DROP_S3_ENDPOINT:</span> <span class=\"hljs-string\">http://minio:9000</span>\n      <span class=\"hljs-attr\">CANVAS_DROP_S3_BUCKET:</span> <span class=\"hljs-string\">canvas-drop</span>\n      <span class=\"hljs-attr\">CANVAS_DROP_S3_REGION:</span> <span class=\"hljs-string\">us-east-1</span>\n      <span class=\"hljs-attr\">CANVAS_DROP_S3_ACCESS_KEY:</span> <span class=\"hljs-string\">minioadmin</span>\n      <span class=\"hljs-attr\">CANVAS_DROP_S3_SECRET_KEY:</span> <span class=\"hljs-string\">minioadmin</span>\n      <span class=\"hljs-attr\">CANVAS_DROP_S3_FORCE_PATH_STYLE:</span> <span class=\"hljs-string\">\"true\"</span></code></pre>\n<p><code>CANVAS_DROP_S3_ENDPOINT</code> is MinIO's in-network address.\n<code>CANVAS_DROP_S3_FORCE_PATH_STYLE</code> already defaults to <code>true</code>; it is shown for\nclarity. Boot refuses <code>CANVAS_DROP_STORAGE=s3</code> without bucket, region, access\nkey, and secret key, naming each missing variable.</p>\n</li>\n<li><p>Apply. Compose recreates <code>app</code> with the new environment:</p>\n<pre><code class=\"hljs language-bash\">docker compose --profile minio up -d</code></pre>\n</li>\n</ol>\n<h3 id=\"add-chromium-for-canvas-screenshots\">Add Chromium for canvas screenshots</h3>\n<p>The preview/screenshot pipeline ships off, and the default image contains no\nbrowser. Build with the <code>SCREENSHOTS</code> build arg (about 300 MB more, via\n<code>playwright install --with-deps chromium</code>), then set <code>CANVAS_DROP_SCREENSHOTS=on</code>\nand turn the feature on in Admin. Both are required; the env var only makes the\npipeline available.</p>\n<pre><code class=\"hljs language-bash\">docker build --build-arg SCREENSHOTS=1 -t canvas-drop:screenshots .</code></pre>\n<p>In the compose stack, give the <code>app</code> service the build arg and the env var:</p>\n<pre><code class=\"hljs language-yaml\">  <span class=\"hljs-attr\">app:</span>\n    <span class=\"hljs-attr\">build:</span>\n      <span class=\"hljs-attr\">context:</span> <span class=\"hljs-string\">.</span>\n      <span class=\"hljs-attr\">args:</span>\n        <span class=\"hljs-attr\">SCREENSHOTS:</span> <span class=\"hljs-string\">\"1\"</span>\n    <span class=\"hljs-attr\">environment:</span>\n      <span class=\"hljs-comment\"># …</span>\n      <span class=\"hljs-attr\">CANVAS_DROP_SCREENSHOTS:</span> <span class=\"hljs-string\">\"on\"</span></code></pre>\n<p>Details, tuning variables, and the memory cost are on the\n<a href=\"/docs/self-hosting/screenshots\">Screenshots</a> page.</p>\n<h3 id=\"run-the-image-with-your-own-proxy-database-and-idp\">Run the image with your own proxy, database, and IdP</h3>\n<p>There is no published image; build it from the repo. If you bring your own\nreverse proxy, database, and identity provider, run the application image\ndirectly.</p>\n<pre><code class=\"hljs language-bash\">docker build -t canvas-drop .\n<span class=\"hljs-built_in\">cp</span> .env.production.example canvas-drop.env      <span class=\"hljs-comment\"># replace every CHANGE_ME / REPLACE_ME</span>\ndocker run -d --name canvas-drop \\\n  --env-file canvas-drop.env \\\n  -p 127.0.0.1:3000:3000 \\\n  -v canvas-drop-data:/data \\\n  canvas-drop\ncurl -fsS http://127.0.0.1:3000/healthz\n<span class=\"hljs-comment\"># 200 {\"status\":\"ok\",\"db\":\"ok\",\"version\":\"...\"}   (503 \"degraded\" until the DB answers)</span></code></pre>\n<p><code>.env.production.example</code> is the annotated subdomain + <code>proxy</code> (JWKS) + Postgres +\nS3 profile; <code>docker run --env-file</code> reads its <code>KEY=VALUE</code> lines and ignores the\ncomments. What the image fixes:</p>\n<table>\n<thead>\n<tr>\n<th>Preset</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NODE_ENV</code></td>\n<td><code>production</code> (<code>dev</code> auth is refused; configure <code>proxy</code> or <code>oidc</code>)</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_PORT</code></td>\n<td><code>3000</code> (<code>EXPOSE 3000</code>)</td>\n</tr>\n<tr>\n<td>Runtime user</td>\n<td><code>canvasdrop</code>, uid/gid 1001, non-root</td>\n</tr>\n<tr>\n<td><code>VOLUME /data</code></td>\n<td>writable, owned by the app user; <code>CANVAS_DROP_SQLITE_PATH=/data/canvasdrop.db</code>, <code>CANVAS_DROP_STORAGE_PATH=/data/storage</code></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_DASHBOARD_DIST</code></td>\n<td><code>/app/apps/dashboard/dist</code></td>\n</tr>\n<tr>\n<td><code>HEALTHCHECK</code></td>\n<td><code>http://127.0.0.1:3000/healthz</code> every 15 s, 5 s timeout, 60 s start period, 5 retries</td>\n</tr>\n</tbody></table>\n<p>Mount <code>/data</code> whenever you use SQLite or local storage; on Postgres + S3 the\ncontainer holds no state. The image reads no <code>.env</code>; pass <code>CANVAS_DROP_*</code> as\ncontainer environment. Boot validates the whole config and exits 1 listing every\nproblem, for example a <code>CANVAS_DROP_SESSION_SECRET</code> shorter than 32 characters.\nBind the port to loopback as above and put a TLS-terminating proxy in front; the\n<a href=\"/docs/self-hosting/deploy\">Deploy</a> page covers what that proxy must do.</p>\n<h3 id=\"upgrade-the-compose-stack\">Upgrade the compose stack</h3>\n<p>Pull, rebuild, restart. Pending migrations run at boot, so the new version applies\nits own schema changes. Take a backup first: the app binary doubles as the backup\ntool, and the compose stack mounts a dedicated <code>backups</code> volume for it.</p>\n<pre><code class=\"hljs language-bash\">docker compose <span class=\"hljs-built_in\">exec</span> -T app sh -lc <span class=\"hljs-string\">'node --conditions=node-dist apps/server/dist/index.js backup /backups/$(date -u +%Y%m%dT%H%M%SZ)'</span>\ngit pull\ndocker compose up -d --build</code></pre>\n<p><code>restore &lt;backup-dir&gt;</code> is the inverse; it refuses a non-empty database without\n<code>--force</code>. A backup is a cleartext export that includes credential hashes, so keep\nit off the data volume and encrypt it before it leaves the host. For scheduled\nbackups, <code>docker-compose.yml</code> carries a commented-out <code>maintenance</code> sidecar\n(<code>docker compose --profile maintenance up -d</code>) that runs the nightly <code>backup</code> and\nweekly <code>purge</code> from <code>docker/maintenance.cron</code>. The full runbook is <code>docs/ops.md</code>\nin the repo; the <a href=\"/docs/self-hosting/deploy\">Deploy</a> page summarises it under\n\"Backups and maintenance\".</p>\n<h2 id=\"run-from-source\">Run from source</h2>\n<p>For developing canvas-drop, or for a zero-config local instance. Requires Node 24\nor newer and pnpm 11 (<code>package.json</code> pins <code>pnpm@11.0.9</code>; <code>corepack enable</code> selects\nit).</p>\n<pre><code class=\"hljs language-bash\">git <span class=\"hljs-built_in\">clone</span> https://github.com/markpasternak/canvas-drop.git\n<span class=\"hljs-built_in\">cd</span> canvas-drop\npnpm install\n<span class=\"hljs-built_in\">cp</span> .env.example .<span class=\"hljs-built_in\">env</span>\npnpm dev\ncurl -fsS http://localhost:3000/healthz\n<span class=\"hljs-comment\"># 200 {\"status\":\"ok\",\"db\":\"ok\",\"version\":\"...\"}</span></code></pre>\n<p><code>pnpm install</code> builds <code>better-sqlite3</code> natively; the build is pre-approved in\n<code>pnpm-workspace.yaml</code>, so there is no prompt. (The Docker builder installs\n<code>python3 make g++</code> for the same reason.)</p>\n<table>\n<thead>\n<tr>\n<th>URL</th>\n<th>What it serves</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>http://localhost:5173</code></td>\n<td>The dashboard (Vite dev server with HMR). It proxies <code>/api</code>, <code>/auth</code>, <code>/v1</code>, <code>/docs</code>, <code>/llms.txt</code>, <code>/skill.zip</code>, <code>/welcome</code>, and <code>/og.png</code> to the server.</td>\n</tr>\n<tr>\n<td><code>http://localhost:3000</code></td>\n<td>The Hono server: management API, Deploy API, MCP at <code>/mcp</code>, docs, and the canvases at <code>/c/{slug}/</code>.</td>\n</tr>\n</tbody></table>\n<p>This is the zero-config profile: path URL mode, SQLite at <code>./data/canvasdrop.db</code>,\nlocal storage at <code>./data/storage</code>, and <code>dev</code> auth, which signs every request in as\n<code>dev@example.com</code> and makes that user the admin. The <code>data/</code> directory is created\non first boot. <code>dev</code> auth is refused when <code>NODE_ENV=production</code>; it is for local\nuse only.</p>\n<p>What <code>pnpm dev</code> does:</p>\n<ul>\n<li>Loads <code>.env</code> once with <code>node --env-file-if-exists=.env</code>. The file is optional\n(the defaults are the dev profile), and variables already in your environment\nwin, so <code>CANVAS_DROP_PORT=3001 pnpm dev</code> works without editing anything. Nothing\nelse reads <code>.env</code>; production takes config from the process environment.</li>\n<li>Seeds 100 sample canvases the first time the database has none. Skip with\n<code>CANVAS_DROP_DEV_SEED=0</code>; wipe the local database and storage with\n<code>pnpm reset:data</code> (stop the dev server first).</li>\n<li>Runs the server (<code>tsx watch</code>), the dashboard (<code>vite</code>), and the browser SDK build\n(esbuild watch) in parallel. Ctrl-C or <code>pnpm dev:stop</code> stops all three.</li>\n</ul>\n<p>Ports: <code>CANVAS_DROP_PORT</code> moves the server and the Vite proxy target together;\n<code>CANVAS_DROP_DASHBOARD_PORT</code> moves the Vite server. Neither hops to a free port:\nVite fails when <code>5173</code> is taken, and the server exits 1 with a message naming the\nbound port. Either usually means a stale dev server is still running\n(<code>pnpm dev:stop</code>).</p>\n<p>The server serves the built dashboard from <code>apps/dashboard/dist</code> whenever it\nexists, so after <code>pnpm build</code> the dashboard also answers on\n<code>http://localhost:3000</code> (without HMR); before a build, that route returns 503\n<code>dashboard_not_built</code>. That is the production layout: one process serves the\ndashboard, the API, and the canvases. To run it that way without Docker:</p>\n<pre><code class=\"hljs language-bash\">pnpm install --frozen-lockfile\npnpm build\nnode --conditions=node-dist apps/server/dist/index.js</code></pre>\n<p>Supply configuration through the process manager (for example systemd\n<code>EnvironmentFile=</code>), not a <code>.env</code> file; only <code>pnpm dev</code> reads <code>.env</code>. \"Running the\nbare process\" on the <a href=\"/docs/self-hosting/deploy\">Deploy</a> page covers the proxy in\nfront.</p>\n<p>Next, publish your first canvas: <a href=\"/docs/quickstart\">Quickstart</a>.</p>\n<h2 id=\"what-you-choose-at-install-time\">What you choose at install time</h2>\n<p>Four interfaces, one switch variable each. Every value is validated at boot; a bad\ncombination fails at startup naming the variable to fix.</p>\n<table>\n<thead>\n<tr>\n<th>Interface</th>\n<th>Switch</th>\n<th>Options (default first)</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Database</td>\n<td><code>CANVAS_DROP_DB</code></td>\n<td><code>sqlite</code> / <code>postgres</code></td>\n<td><code>postgres</code> needs <code>CANVAS_DROP_DATABASE_URL</code>.</td>\n</tr>\n<tr>\n<td>Storage</td>\n<td><code>CANVAS_DROP_STORAGE</code></td>\n<td><code>local</code> / <code>s3</code></td>\n<td><code>s3</code> needs <code>CANVAS_DROP_S3_BUCKET</code>, <code>CANVAS_DROP_S3_REGION</code>, <code>CANVAS_DROP_S3_ACCESS_KEY</code>, <code>CANVAS_DROP_S3_SECRET_KEY</code>, plus <code>CANVAS_DROP_S3_ENDPOINT</code> for MinIO or R2.</td>\n</tr>\n<tr>\n<td>URL mode</td>\n<td><code>CANVAS_DROP_URL_MODE</code></td>\n<td><code>path</code> / <code>subdomain</code></td>\n<td><code>path</code> serves <code>{base}/c/{slug}/</code>; <code>subdomain</code> serves <code>{slug}.{host}</code> and needs a non-localhost <code>CANVAS_DROP_BASE_URL</code>, wildcard DNS, and a wildcard certificate. Set <code>CANVAS_DROP_API_BASE_URL</code> only when the Deploy API is fronted on its own host.</td>\n</tr>\n<tr>\n<td>Auth</td>\n<td><code>CANVAS_DROP_AUTH_MODE</code></td>\n<td><code>dev</code> / <code>proxy</code> / <code>oidc</code></td>\n<td><code>dev</code> is local-only. <code>proxy</code> and <code>oidc</code> need <code>CANVAS_DROP_ALLOWED_EMAIL_DOMAINS</code> and a <code>CANVAS_DROP_SESSION_SECRET</code> of 32 or more characters; <code>proxy</code> also needs a JWKS URL (or <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code>). Real auth in <code>path</code> mode needs <code>CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true</code>.</td>\n</tr>\n</tbody></table>\n<p>The recommended production profile is subdomain URLs, <code>proxy</code> auth verified\nagainst a JWKS, Postgres, and S3-compatible storage; <code>.env.production.example</code> is\nthat profile, annotated. SQLite, local storage, and path mode stay first-class for\ndevelopment and small trusted instances.</p>\n<p>See <a href=\"/docs/self-hosting/configuration\">Configuration</a> for every variable, the\n<a href=\"/docs/self-hosting/security-model\">Security model</a> for the trade-offs behind the\nauth and URL modes, and <a href=\"/docs/self-hosting/deploy\">Deploy</a> for the production\nwalkthrough.</p>\n",
    "headings": [
      {
        "id": "docker-compose",
        "text": "Docker Compose",
        "level": 2
      },
      {
        "id": "verify-the-launch-invariants",
        "text": "Verify the launch invariants",
        "level": 3
      },
      {
        "id": "switch-the-demo-to-s3-compatible-storage-minio",
        "text": "Switch the demo to S3-compatible storage (MinIO)",
        "level": 3
      },
      {
        "id": "add-chromium-for-canvas-screenshots",
        "text": "Add Chromium for canvas screenshots",
        "level": 3
      },
      {
        "id": "run-the-image-with-your-own-proxy-database-and-idp",
        "text": "Run the image with your own proxy, database, and IdP",
        "level": 3
      },
      {
        "id": "upgrade-the-compose-stack",
        "text": "Upgrade the compose stack",
        "level": 3
      },
      {
        "id": "run-from-source",
        "text": "Run from source",
        "level": 2
      },
      {
        "id": "what-you-choose-at-install-time",
        "text": "What you choose at install time",
        "level": 2
      }
    ],
    "text": "Install You are standing up canvas-drop for the first time. By the end of this page you have an instance you can sign in to, and you know which database, storage, URL mode, and auth driver it is running. Two starting points, same repository: git clone https://github.com/markpasternak/canvas-drop.git cd canvas-drop # Evaluate or self-host: the production shape behind an identity-aware proxy docker compose up --build # open http://localhost:8080 and sign in as demo@example.com / canvasdrop # Develop canvas-drop itself: SQLite, local storage, automatic sign-in pnpm install && cp .env.example . env && pnpm dev # open http://localhost:5173 Path Needs What you get Docker Compose Docker with Compose v2 ( docker compose , not the legacy docker-compose ) An identity-aware proxy in front, canvas-drop in real proxy auth mode, Postgres, and a bundled demo identity provider on http://localhost:8080 . Evaluate here; graduating the same files to a real IdP is configuration. From source Node 24 or newer, pnpm 11 pnpm dev on http://localhost:5173 with SQLite, local storage, and dev auth that signs every request in. Develop canvas-drop itself, or try Bearer deploys and MCP with no proxy in the way. Both run the same code. Database, storage, URL mode, and auth are drivers behind one switch variable each; swapping one later is a config change, never a code change. The switches are listed in What you choose at install time . Docker Compose docker compose up --build docker compose ps app # STATUS ends in \"(healthy)\" when the stack is ready The first --build compiles the workspace inside the image. The server does not listen until Postgres is reachable and migrations have run, and GET /healthz returns 503 while the database is unreachable, so the container's health check allows a 60-second start period before it counts failures. Sign in at http://localhost:8080 as demo@example.com / canvasdrop . What comes up: Service Image Role caddy caddy:2-alpine Edge proxy on host port 8080 , the only published port. Routes /dex/* to Dex and everything else to oauth2-proxy, and strips client-supplied identity and Authorization headers before they reach the auth layer. Plain HTTP for the demo; in production it would terminate TLS. oauth2-proxy quay.io/oauth2-proxy/oauth2-proxy:v7.6.0 Identity-aware proxy. Signs users in against Dex and forwards the Dex-signed access token to the app in X-Forwarded-Access-Token . dex dexidp/dex:v2.41.1 Bundled demo identity provider with one static user, demo@example.com / canvasdrop . app built from the repo Dockerfile as canvas-drop:dev canvas-drop in proxy auth mode, verifying the JWT against Dex's JWKS ( CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL=http://dex:5556/dex/keys ). Path URL mode, Postgres, local storage on the app-data volume, a separate backups volume. No published port. postgres postgres:16-alpine Database, on the pg-data volume, with a pg_isready health check the app waits on. minio minio/minio:RELEASE.2024-11-07T00-52-20Z Optional S3-compatible storage. Starts only with --profile minio (below). What to know about the demo: The demo user is also the instance admin ( CANVAS_DROP_ADMIN_EMAILS: demo@example.com ), so the Admin area is available on first sign-in. It runs path URL mode ( {base}/c/{slug}/ ) because subdomain mode refuses to boot on a localhost CANVAS_DROP_BASE_URL ; CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE is set to true for that reason. Path mode shares one browser origin across all canvases, so isolation is weaker than subdomain mode; production should use subdomain mode (see the Security model ). The Deploy API ( /v1/canvases/* ) and MCP ( /mcp ) are not reachable through this edge: oauth2-proxy gates every route and Caddy strips Authorization . Deploy from the dashboard, or use the from-source profile to try Bearer deploys. The app's config is an inline environment: block on the app service in docker-compose.yml (no env_file ). Change it by editing that file and running docker compose up -d again. Pause the stack with docker compose stop . Tear it down and delete every volume ( app-data , backups , pg-data , minio-data ) with docker compose down -v . The Dex and oauth2-proxy secrets in docker/ , the session secret in docker-compose.yml , and the demo@example.com login are public, demo-only placeholders, and the stack serves plain HTTP in path mode. Do not expose it to a network as-is. Rotate every secret and work through \"Graduating to a real IdP\" on the Deploy page before any real use. Verify the launch invariants scripts/compose-smoke.sh boots the stack ( docker compose up -d --build ), waits up to two minutes for app to report healthy, and asserts the load-bearing invariants: the app publishes no host port, an unauthenticated request is redirected, a forged X-Forwarded-Access-Token / X-Auth-Request-Email pair is still redirected, a real Dex sign-in resolves /api/me as demo@example.com with authMode: proxy , and the same user id survives docker compose restart app postgres . It needs curl . ./scripts/compose-smoke.sh # boots, verifies, leaves the stack up KEEP_UP=0 ./scripts/compose-smoke.sh # same, then `docker compose down -v` Switch the demo to S3-compatible storage (MinIO) --profile minio adds a MinIO container ( minioadmin / minioadmin , data on the minio-data volume). It does not switch the app over by itself: the app service keeps CANVAS_DROP_STORAGE: local until you change it. Do this on a fresh instance; blobs already written to local storage are not moved when the driver changes (to move an existing instance, use backup and restore , below). Start MinIO alongside the stack. Pass --profile minio on every later docker compose command too, or Compose leaves the MinIO container out. docker compose --profile minio up -d --build Create the bucket. Neither MinIO nor the storage driver creates it on first write. The compose file publishes no MinIO port, so either run your S3 client inside the compose network, or add ports: [\"9000:9000\"] to the minio service and create it from the host (the AWS CLI works, as CI does for its own test bucket): AWS_ACCESS_KEY_ID=minioadmin AWS_SECRET_ACCESS_KEY=minioadmin AWS_DEFAULT_REGION=us-east-1 \\ aws --endpoint-url http://localhost:9000 s3 mb s3://canvas-drop Point the app at it. In docker-compose.yml , replace CANVAS_DROP_STORAGE: local on the app service with: environment: # … CANVAS_DROP_STORAGE: s3 CANVAS_DROP_S3_ENDPOINT: http://minio:9000 CANVAS_DROP_S3_BUCKET: canvas-drop CANVAS_DROP_S3_REGION: us-east-1 CANVAS_DROP_S3_ACCESS_KEY: minioadmin CANVAS_DROP_S3_SECRET_KEY: minioadmin CANVAS_DROP_S3_FORCE_PATH_STYLE: \"true\" CANVAS_DROP_S3_ENDPOINT is MinIO's in-network address. CANVAS_DROP_S3_FORCE_PATH_STYLE already defaults to true ; it is shown for clarity. Boot refuses CANVAS_DROP_STORAGE=s3 without bucket, region, access key, and secret key, naming each missing variable. Apply. Compose recreates app with the new environment: docker compose --profile minio up -d Add Chromium for canvas screenshots The preview/screenshot pipeline ships off, and the default image contains no browser. Build with the SCREENSHOTS build arg (about 300 MB more, via playwright install --with-deps chromium ), then set CANVAS_DROP_SCREENSHOTS=on and turn the feature on in Admin. Both are required; the env var only makes the pipeline available. docker build --build-arg SCREENSHOTS=1 -t canvas-drop:screenshots . In the compose stack, give the app service the build arg and the env var: app: build: context: . args: SCREENSHOTS: \"1\" environment: # … CANVAS_DROP_SCREENSHOTS: \"on\" Details, tuning variables, and the memory cost are on the Screenshots page. Run the image with your own proxy, database, and IdP There is no published image; build it from the repo. If you bring your own reverse proxy, database, and identity provider, run the application image directly. docker build -t canvas-drop . cp .env.production.example canvas-drop.env # replace every CHANGE_ME / REPLACE_ME docker run -d --name canvas-drop \\ --env-file canvas-drop.env \\ -p 127.0.0.1:3000:3000 \\ -v canvas-drop-data:/data \\ canvas-drop curl -fsS http://127.0.0.1:3000/healthz # 200 {\"status\":\"ok\",\"db\":\"ok\",\"version\":\"...\"} (503 \"degraded\" until the DB answers) .env.production.example is the annotated subdomain + proxy (JWKS) + Postgres + S3 profile; docker run --env-file reads its KEY=VALUE lines and ignores the comments. What the image fixes: Preset Value NODE_ENV production ( dev auth is refused; configure proxy or oidc ) CANVAS_DROP_PORT 3000 ( EXPOSE 3000 ) Runtime user canvasdrop , uid/gid 1001, non-root VOLUME /data writable, owned by the app user; CANVAS_DROP_SQLITE_PATH=/data/canvasdrop.db , CANVAS_DROP_STORAGE_PATH=/data/storage CANVAS_DROP_DASHBOARD_DIST /app/apps/dashboard/dist HEALTHCHECK http://127.0.0.1:3000/healthz every 15 s, 5 s timeout, 60 s start period, 5 retries Mount /data whenever you use SQLite or local storage; on Postgres + S3 the container holds no state. The image reads no .env ; pass CANVAS_DROP_* as container environment. Boot validates the whole config and exits 1 listing every problem, for example a CANVAS_DROP_SESSION_SECRET shorter than 32 characters. Bind the port to loopback as above and put a TLS-terminating proxy in front; the Deploy page covers what that proxy must do. Upgrade the compose stack Pull, rebuild, restart. Pending migrations run at boot, so the new version applies its own schema changes. Take a backup first: the app binary doubles as the backup tool, and the compose stack mounts a dedicated backups volume for it. docker compose exec -T app sh -lc 'node --conditions=node-dist apps/server/dist/index.js backup /backups/$(date -u +%Y%m%dT%H%M%SZ)' git pull docker compose up -d --build restore <backup-dir> is the inverse; it refuses a non-empty database without --force . A backup is a cleartext export that includes credential hashes, so keep it off the data volume and encrypt it before it leaves the host. For scheduled backups, docker-compose.yml carries a commented-out maintenance sidecar ( docker compose --profile maintenance up -d ) that runs the nightly backup and weekly purge from docker/maintenance.cron . The full runbook is docs/ops.md in the repo; the Deploy page summarises it under \"Backups and maintenance\". Run from source For developing canvas-drop, or for a zero-config local instance. Requires Node 24 or newer and pnpm 11 ( package.json pins pnpm@11.0.9 ; corepack enable selects it). git clone https://github.com/markpasternak/canvas-drop.git cd canvas-drop pnpm install cp .env.example . env pnpm dev curl -fsS http://localhost:3000/healthz # 200 {\"status\":\"ok\",\"db\":\"ok\",\"version\":\"...\"} pnpm install builds better-sqlite3 natively; the build is pre-approved in pnpm-workspace.yaml , so there is no prompt. (The Docker builder installs python3 make g++ for the same reason.) URL What it serves http://localhost:5173 The dashboard (Vite dev server with HMR). It proxies /api , /auth , /v1 , /docs , /llms.txt , /skill.zip , /welcome , and /og.png to the server. http://localhost:3000 The Hono server: management API, Deploy API, MCP at /mcp , docs, and the canvases at /c/{slug}/ . This is the zero-config profile: path URL mode, SQLite at ./data/canvasdrop.db , local storage at ./data/storage , and dev auth, which signs every request in as dev@example.com and makes that user the admin. The data/ directory is created on first boot. dev auth is refused when NODE_ENV=production ; it is for local use only. What pnpm dev does: Loads .env once with node --env-file-if-exists=.env . The file is optional (the defaults are the dev profile), and variables already in your environment win, so CANVAS_DROP_PORT=3001 pnpm dev works without editing anything. Nothing else reads .env ; production takes config from the process environment. Seeds 100 sample canvases the first time the database has none. Skip with CANVAS_DROP_DEV_SEED=0 ; wipe the local database and storage with pnpm reset:data (stop the dev server first). Runs the server ( tsx watch ), the dashboard ( vite ), and the browser SDK build (esbuild watch) in parallel. Ctrl-C or pnpm dev:stop stops all three. Ports: CANVAS_DROP_PORT moves the server and the Vite proxy target together; CANVAS_DROP_DASHBOARD_PORT moves the Vite server. Neither hops to a free port: Vite fails when 5173 is taken, and the server exits 1 with a message naming the bound port. Either usually means a stale dev server is still running ( pnpm dev:stop ). The server serves the built dashboard from apps/dashboard/dist whenever it exists, so after pnpm build the dashboard also answers on http://localhost:3000 (without HMR); before a build, that route returns 503 dashboard_not_built . That is the production layout: one process serves the dashboard, the API, and the canvases. To run it that way without Docker: pnpm install --frozen-lockfile pnpm build node --conditions=node-dist apps/server/dist/index.js Supply configuration through the process manager (for example systemd EnvironmentFile= ), not a .env file; only pnpm dev reads .env . \"Running the bare process\" on the Deploy page covers the proxy in front. Next, publish your first canvas: Quickstart . What you choose at install time Four interfaces, one switch variable each. Every value is validated at boot; a bad combination fails at startup naming the variable to fix. Interface Switch Options (default first) Notes Database CANVAS_DROP_DB sqlite / postgres postgres needs CANVAS_DROP_DATABASE_URL . Storage CANVAS_DROP_STORAGE local / s3 s3 needs CANVAS_DROP_S3_BUCKET , CANVAS_DROP_S3_REGION , CANVAS_DROP_S3_ACCESS_KEY , CANVAS_DROP_S3_SECRET_KEY , plus CANVAS_DROP_S3_ENDPOINT for MinIO or R2. URL mode CANVAS_DROP_URL_MODE path / subdomain path serves {base}/c/{slug}/ ; subdomain serves {slug}.{host} and needs a non-localhost CANVAS_DROP_BASE_URL , wildcard DNS, and a wildcard certificate. Set CANVAS_DROP_API_BASE_URL only when the Deploy API is fronted on its own host. Auth CANVAS_DROP_AUTH_MODE dev / proxy / oidc dev is local-only. proxy and oidc need CANVAS_DROP_ALLOWED_EMAIL_DOMAINS and a CANVAS_DROP_SESSION_SECRET of 32 or more characters; proxy also needs a JWKS URL (or CANVAS_DROP_TRUSTED_PROXY_IPS ). Real auth in path mode needs CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true . The recommended production profile is subdomain URLs, proxy auth verified against a JWKS, Postgres, and S3-compatible storage; .env.production.example is that profile, annotated. SQLite, local storage, and path mode stay first-class for development and small trusted instances. See Configuration for every variable, the Security model for the trade-offs behind the auth and URL modes, and Deploy for the production walkthrough."
  },
  {
    "path": "self-hosting/configuration",
    "section": "Self-hosting",
    "title": "Configuration",
    "html": "<h1 id=\"configuration\">Configuration</h1>\n<p>If you run a canvas-drop instance, this is the environment-variable reference: every\nvariable the server reads, its default, and what refuses to boot when it is wrong. Every\nsetting is an environment variable, validated once at startup. One module\n(<code>packages/shared/src/config/env.ts</code>) is the only reader of <code>process.env</code>; everything else\ntakes typed config. An invalid value or combination fails at boot with a message naming\nevery variable to fix, never at request time. Nothing is reported anywhere: no telemetry,\nno phone-home.</p>\n<p>The defaults below are the schema defaults. <code>.env.example</code> mirrors them with comments;\n<code>.env.production.example</code> is the annotated production profile.</p>\n<h2 id=\"how-config-gets-loaded\">How config gets loaded</h2>\n<p><strong>Local dev.</strong> <code>pnpm dev</code> reads the root <code>.env</code> file once (<code>node --env-file-if-exists=.env</code>).\nCopy the template and start:</p>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">cp</span> .env.example .<span class=\"hljs-built_in\">env</span> &amp;&amp; pnpm dev\n<span class=\"hljs-comment\"># server on http://localhost:3000, dashboard on http://localhost:5173, signed in as dev@example.com</span></code></pre>\n<p>The defaults give you a logged-in instance: <code>path</code> URLs, <code>sqlite</code>, <code>local</code> storage, <code>dev</code> auth.</p>\n<p><strong>Production.</strong> The <code>.env</code> file is not read. Set variables in the process environment: a\nsystemd <code>EnvironmentFile</code>, the container <code>environment:</code> block, or a secrets manager. The\nrecommended profile (subdomain URLs, <code>proxy</code> auth verified against a JWKS, Postgres, S3)\nneeds this set; everything else keeps its default:</p>\n<pre><code class=\"hljs language-bash\">NODE_ENV=production\nCANVAS_DROP_URL_MODE=subdomain\nCANVAS_DROP_BASE_URL=https://canvases.example.com\nCANVAS_DROP_SESSION_SECRET=&lt;output of: openssl rand -hex 32&gt;\n\nCANVAS_DROP_AUTH_MODE=proxy\nCANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL=https://idp.example.com/.well-known/jwks.json\nCANVAS_DROP_AUTH_PROXY_JWT_ISSUER=https://idp.example.com\nCANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE=canvas-drop\nCANVAS_DROP_ALLOWED_EMAIL_DOMAINS=example.com\nCANVAS_DROP_ADMIN_EMAILS=admin@example.com\n\nCANVAS_DROP_DB=postgres\nCANVAS_DROP_DATABASE_URL=postgres://canvasdrop:&lt;password&gt;@db.internal:5432/canvasdrop\n\nCANVAS_DROP_STORAGE=s3\nCANVAS_DROP_S3_BUCKET=canvas-drop\nCANVAS_DROP_S3_REGION=us-east-1\nCANVAS_DROP_S3_ACCESS_KEY=&lt;access key&gt;\nCANVAS_DROP_S3_SECRET_KEY=&lt;secret key&gt;</code></pre>\n<p>A misconfigured start prints every problem at once and exits with status 1:</p>\n<pre><code class=\"hljs language-text\">Invalid canvas-drop configuration:\n  - CANVAS_DROP_SESSION_SECRET: a session secret of at least 32 characters is required outside dev mode (and always in production)\n  - CANVAS_DROP_DATABASE_URL: CANVAS_DROP_DATABASE_URL is required when CANVAS_DROP_DB=postgres\n\nFix the variables above (see .env.example) and restart.</code></pre>\n<p><strong>Docker image.</strong> The image presets <code>NODE_ENV=production</code>, <code>CANVAS_DROP_PORT=3000</code>,\n<code>CANVAS_DROP_SQLITE_PATH=/data/canvasdrop.db</code>, <code>CANVAS_DROP_STORAGE_PATH=/data/storage</code>, and\n<code>CANVAS_DROP_DASHBOARD_DIST=/app/apps/dashboard/dist</code>, with <code>/data</code> as the volume. Everything\nelse comes from the environment you pass in. See <a href=\"/docs/self-hosting/deploy\">Deploy</a>.</p>\n<p><strong>Admin view.</strong> Admin → Configuration lists the main settings with their source: Environment,\nDefault, or a DB override. Secrets render as set or unset plus the last four characters. A\nfew settings can be edited there at runtime (a DB override layered over env); the tables below\nmark them <strong>runtime-editable</strong>. Everything else is env-only by design: a web panel must not be\nable to flip the auth mode, the database driver, or the session secret. A handful of env-only\nknobs are not shown in the panel at all (<code>CANVAS_DROP_MCP</code>, <code>CANVAS_DROP_DASHBOARD_DIST</code>, the\nscreenshot tuning values, the proxy header names, <code>CANVAS_DROP_ORG_*</code>,\n<code>CANVAS_DROP_S3_FORCE_PATH_STYLE</code>, <code>CANVAS_DROP_DEV_USER_*</code>, and the authoring share policy).</p>\n<h2 id=\"value-formats\">Value formats</h2>\n<table>\n<thead>\n<tr>\n<th>Format</th>\n<th>Accepted values</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>enum</td>\n<td>exactly one of the listed tokens, e.g. <code>on</code> or <code>off</code></td>\n</tr>\n<tr>\n<td>CSV</td>\n<td>comma-separated; entries trimmed, empty entries dropped</td>\n</tr>\n<tr>\n<td>boolean</td>\n<td><code>1</code>, <code>true</code>, <code>on</code>, <code>yes</code> or <code>0</code>, <code>false</code>, <code>off</code>, <code>no</code> (case-insensitive); anything else fails boot</td>\n</tr>\n<tr>\n<td>number</td>\n<td>any finite number</td>\n</tr>\n<tr>\n<td>integer ≥ 1</td>\n<td>rate limits and screenshot tuning; <code>0</code> or a negative value fails boot (use the <code>_ENABLED</code> switch to turn a class off)</td>\n</tr>\n<tr>\n<td>integer ≥ 0</td>\n<td><code>CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL</code></td>\n</tr>\n<tr>\n<td>URL</td>\n<td>an absolute URL with a scheme (<code>CANVAS_DROP_BASE_URL</code>, JWKS, OIDC issuer, AI and Mailgun base URLs)</td>\n</tr>\n</tbody></table>\n<p>An empty value counts as unset and takes the default.</p>\n<h2 id=\"boot-time-checks\">Boot-time checks</h2>\n<p>These combinations are enforced at startup, in addition to each variable's format:</p>\n<ul>\n<li><code>CANVAS_DROP_AUTH_MODE=dev</code> with <code>NODE_ENV=production</code> refuses to start.</li>\n<li><code>CANVAS_DROP_URL_MODE=subdomain</code> needs a non-localhost <code>CANVAS_DROP_BASE_URL</code> (<code>localhost</code>, <code>127.0.0.1</code>, <code>::1</code>, and <code>*.localhost</code> are rejected).</li>\n<li><code>CANVAS_DROP_SESSION_SECRET</code> must be at least 32 characters whenever the auth mode is not <code>dev</code>, and always when <code>NODE_ENV=production</code>.</li>\n<li><code>CANVAS_DROP_DB=postgres</code> requires <code>CANVAS_DROP_DATABASE_URL</code>.</li>\n<li><code>CANVAS_DROP_STORAGE=s3</code> requires <code>CANVAS_DROP_S3_BUCKET</code>, <code>CANVAS_DROP_S3_REGION</code>, <code>CANVAS_DROP_S3_ACCESS_KEY</code>, and <code>CANVAS_DROP_S3_SECRET_KEY</code>.</li>\n<li>Every <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code> entry is validated, in every auth mode.</li>\n<li><code>proxy</code> auth requires <code>CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL</code> or a non-empty <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code>; with a JWKS URL, <code>CANVAS_DROP_AUTH_PROXY_JWT_ISSUER</code> and <code>CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE</code> are required too.</li>\n<li><code>oidc</code> auth requires <code>CANVAS_DROP_OIDC_ISSUER</code>, <code>CANVAS_DROP_OIDC_CLIENT_ID</code>, and <code>CANVAS_DROP_OIDC_CLIENT_SECRET</code>.</li>\n<li>Any auth mode other than <code>dev</code> requires at least one <code>CANVAS_DROP_ALLOWED_EMAIL_DOMAINS</code> entry.</li>\n<li><code>path</code> URL mode with <code>proxy</code> or <code>oidc</code> auth requires <code>CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true</code>.</li>\n</ul>\n<p>Tenancy adds three more once <code>CANVAS_DROP_ORG_NAME</code> is set; see\n<a href=\"#tenancy-the-org-boundary-optional\">Tenancy</a>.</p>\n<h2 id=\"the-four-pluggable-interfaces\">The four pluggable interfaces</h2>\n<p>Each interface is selected by one switch variable. Swapping a driver is a config change, never\na code change. Migrations run at boot on whichever database is selected, and a backup taken on\none driver restores onto another (see\n<a href=\"/docs/self-hosting/deploy#backups-and-maintenance\">Backups and maintenance</a>).</p>\n<table>\n<thead>\n<tr>\n<th>Interface</th>\n<th>Switch</th>\n<th>Options (default first)</th>\n<th>Driver-specific vars</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Database</td>\n<td><code>CANVAS_DROP_DB</code></td>\n<td><code>sqlite</code> / <code>postgres</code></td>\n<td>sqlite: <code>CANVAS_DROP_SQLITE_PATH</code>; postgres: <code>CANVAS_DROP_DATABASE_URL</code></td>\n</tr>\n<tr>\n<td>Storage</td>\n<td><code>CANVAS_DROP_STORAGE</code></td>\n<td><code>local</code> / <code>s3</code></td>\n<td>local: <code>CANVAS_DROP_STORAGE_PATH</code>; s3: <code>CANVAS_DROP_S3_*</code> (any S3-compatible store via a custom endpoint)</td>\n</tr>\n<tr>\n<td>URL mode</td>\n<td><code>CANVAS_DROP_URL_MODE</code></td>\n<td><code>path</code> / <code>subdomain</code></td>\n<td><code>CANVAS_DROP_BASE_URL</code> (non-localhost for subdomain); optional <code>CANVAS_DROP_API_BASE_URL</code>; <code>CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE</code> for path mode with real auth</td>\n</tr>\n<tr>\n<td>Auth</td>\n<td><code>CANVAS_DROP_AUTH_MODE</code></td>\n<td><code>dev</code> / <code>proxy</code> / <code>oidc</code></td>\n<td>dev: <code>CANVAS_DROP_DEV_USER_*</code>; proxy: JWT vars or trusted-header vars; oidc: <code>CANVAS_DROP_OIDC_*</code>; both real modes: <code>CANVAS_DROP_ALLOWED_EMAIL_DOMAINS</code>, <code>CANVAS_DROP_SESSION_SECRET</code></td>\n</tr>\n</tbody></table>\n<p>Email transport (<code>CANVAS_DROP_EMAIL_DRIVER</code>) is a fifth, smaller driver seam; see\n<a href=\"#email-transport\">Email transport</a>.</p>\n<h2 id=\"core\">Core</h2>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NODE_ENV</code></td>\n<td><code>development</code></td>\n<td><code>development</code> | <code>production</code> | <code>test</code>. <code>production</code> forbids <code>dev</code> auth, makes the session secret mandatory, and switches the <code>LOG_FORMAT</code> default to <code>json</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_URL_MODE</code></td>\n<td><code>path</code></td>\n<td><code>path</code> serves canvases at <code>{base}/c/{slug}/</code>; <code>subdomain</code> serves <code>{slug}.{baseHost}</code> and gives each canvas its own origin.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_BASE_URL</code></td>\n<td><code>http://localhost:3000</code></td>\n<td>Public origin of the instance. Must be non-localhost in <code>subdomain</code> mode. The app never reads <code>X-Forwarded-Proto</code>; behind TLS this must be the <code>https://</code> origin.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_API_BASE_URL</code></td>\n<td>(= <code>CANVAS_DROP_BASE_URL</code>)</td>\n<td>Where the Deploy API (<code>/v1/canvases/*</code>) is reachable. Set it only when the API is fronted on its own host, e.g. canvases at <code>{slug}.canvases.example.com</code> and the API at <code>https://api.canvases.example.com</code>. MCP tools advertise endpoints built from this value, so agents never probe for the host.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_PORT</code></td>\n<td><code>3000</code></td>\n<td>Listen port.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SESSION_SECRET</code></td>\n<td>(insecure dev fallback)</td>\n<td><strong>Required, at least 32 characters, outside <code>dev</code> auth and always in production.</strong> HMAC key for password-gate cookies and screenshot capture tokens. Session cookies (<code>oidc</code>, <code>dev</code>) are random tokens stored hashed in the database, not derived from it. Generate with <code>openssl rand -hex 32</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_ADMIN_EMAILS</code></td>\n<td>(empty; in <code>dev</code> auth, the dev user)</td>\n<td>CSV, lowercased. Bootstrap admins: these accounts get the admin surface.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_REALTIME</code></td>\n<td><code>on</code></td>\n<td><code>on</code> | <code>off</code>. Master switch for the realtime primitive.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_MCP</code></td>\n<td><code>on</code></td>\n<td><code>on</code> | <code>off</code>. When <code>off</code>, the <code>/mcp</code> and OAuth routes are not mounted at all.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE</code></td>\n<td><code>false</code></td>\n<td>boolean. Must be <code>true</code> to boot <code>path</code> mode with <code>proxy</code> or <code>oidc</code> auth: several users then share one origin, so browser isolation is reduced. See <a href=\"/docs/self-hosting/security-model#path-mode-vs-subdomain-mode-invariant-4\">Path mode vs subdomain mode</a>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_DASHBOARD_DIST</code></td>\n<td>(resolved from the server module)</td>\n<td>Path to the built dashboard SPA. The Docker image sets <code>/app/apps/dashboard/dist</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL</code></td>\n<td><code>300</code></td>\n<td>integer ≥ 0, seconds. How long a shared cache (a CDN in front) may hold a <strong>public</strong> canvas's HTML, emitted as <code>s-maxage</code>; the browser still revalidates every load. Only the Public link rung without a password is ever shared-cacheable; auth-gated canvases stay <code>private</code>. It is also how long a canvas can stay visible at the edge after access is restricted, and the dashboard warns owners with this figure. <code>0</code> disables shared caching. See <a href=\"/docs/self-hosting/cdn\">Behind a CDN</a>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_DESIGN_SKIN</code></td>\n<td><code>editorial</code></td>\n<td><code>editorial</code> | <code>studio</code> | <code>workshop</code> | <code>canvas</code>. Instance-wide visual language. <strong>Runtime-editable</strong>; a DB override wins over this value. See <a href=\"#design-skins\">Design skins</a>.</td>\n</tr>\n</tbody></table>\n<p>Admin → Configuration also carries <code>core.instanceName</code>, a DB-only display name (default: the\nhost of <code>CANVAS_DROP_BASE_URL</code>) for custom email templates that still use <code>{{instanceName}}</code>.\nThe seeded templates name the recipient email, the inviter, and the specific canvas, team, or\nsign-in action instead of instance or org branding.</p>\n<h2 id=\"design-skins\">Design skins</h2>\n<p>A design skin is an instance-wide visual language: the dashboard, the editor, the signed-out\nlanding page, the docs, and the legal pages all change together. It is a token-only layer over\nthe brand tokens: a skin overrides the accent colour family, the display-type bundle (font,\nweight, tracking), and the corner-radius scale. No skin forks layout or structure. Error pages\nkeep the default look.</p>\n<table>\n<thead>\n<tr>\n<th>Skin</th>\n<th>Accent</th>\n<th>Display type</th>\n<th>Corners</th>\n<th>Feel</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>editorial</code> (default)</td>\n<td>deep teal</td>\n<td>Newsreader serif, weight 500</td>\n<td>1×</td>\n<td>Calm publishing OS; the base look.</td>\n</tr>\n<tr>\n<td><code>studio</code></td>\n<td>terracotta</td>\n<td>Newsreader serif, weight 500</td>\n<td>1×</td>\n<td>Warm editorial.</td>\n</tr>\n<tr>\n<td><code>workshop</code></td>\n<td>green</td>\n<td>Geist Mono, weight 500</td>\n<td>0.62× (tighter)</td>\n<td>Developer / IDE.</td>\n</tr>\n<tr>\n<td><code>canvas</code></td>\n<td>violet</td>\n<td>Geist sans, weight 800</td>\n<td>1.3× (rounder)</td>\n<td>Playful, bold.</td>\n</tr>\n</tbody></table>\n<p>Accent lightness is kept conservative in every skin so the accent foreground clears WCAG AA on\nthe fill in light and dark mode.</p>\n<p>Two ways to set it, with the same effect for everyone on the instance:</p>\n<ul>\n<li><strong>Admin console.</strong> Admin → Configuration → Design skin. Changing the dropdown previews the\nskin live in your own session; <strong>Save</strong> writes the override to the database and applies it to\nall users at runtime, with no restart. Leaving the page without saving reverts your preview.</li>\n<li><strong><code>CANVAS_DROP_DESIGN_SKIN</code>.</strong> The boot default. A DB override set in the console always\nwins over this value; <strong>Clear</strong> in the console removes the override and falls back to it.</li>\n</ul>\n<p>The skin is global and admin-only. It is not a per-user preference; light or dark theme is the\nper-user setting.</p>\n<h2 id=\"auth\">Auth</h2>\n<p><code>CANVAS_DROP_AUTH_MODE</code> selects how identity is established. Identity always comes from the\nserver-side auth strategy, never from anything the client sends. In every mode the same checks\nrun on every request: the email must pass the domain allowlist (or a sign-in permit), the user\nmust not be blocked, and org membership is derived from the verified email domain. See\n<a href=\"/docs/self-hosting/security-model\">Security model</a>.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_AUTH_MODE</code></td>\n<td><code>dev</code></td>\n<td><code>dev</code> | <code>proxy</code> | <code>oidc</code>. <code>dev</code> is <strong>rejected when <code>NODE_ENV=production</code></strong>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_ALLOWED_EMAIL_DOMAINS</code></td>\n<td>(empty; in <code>dev</code> auth, the dev user's domain)</td>\n<td>CSV, lowercased. <strong>Required (at least one) in <code>proxy</code> and <code>oidc</code>.</strong> Enforced on every request.</td>\n</tr>\n</tbody></table>\n<blockquote>\n<p><strong>Sign-in permits.</strong> Beyond the domain list, an admin can permit specific outside emails to\nsign in (a contractor, a test account) under Admin → People → Sign-in permits. It is an\nadditive, DB-managed layer: the env domain list is unchanged, and an email passes if its\ndomain is allowed <strong>or</strong> it is on this list. There is no app-owned password; adding an email\nsends a sign-in invitation (when access emails are on), and removing an entry revokes that\nemail's access on its next sign-in. In <code>proxy</code> mode this list cannot widen the upstream IAP\nby itself, and no email is sent: admit a brand-new external email in the proxy first, then\nadd it here. See <a href=\"#sign-in-permits-and-access-emails\">Sign-in permits and access emails</a>.</p>\n</blockquote>\n<h3 id=\"dev-mode\">dev mode</h3>\n<p>Auto-logs-in a fixed local user with zero setup; no credential is examined. A session cookie\nstill exists so <code>/auth/logout</code> works. Localhost only; production refuses to boot in this mode.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_DEV_USER_EMAIL</code></td>\n<td><code>dev@example.com</code></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_DEV_USER_NAME</code></td>\n<td><code>Dev User</code></td>\n</tr>\n</tbody></table>\n<h3 id=\"oidc-mode\">oidc mode</h3>\n<p>The app owns login: Authorization Code with PKCE against your provider, then an <code>HttpOnly</code>\nsession cookie (<code>__canvasdrop_session</code>, 14-day rolling expiry, <code>SameSite=Lax</code>, <code>Secure</code> in\nproduction, <code>Domain=.{baseHost}</code> in subdomain mode, host-only in path mode). An\nunauthenticated request is redirected to <code>/auth/login</code>. Use this when no identity-aware proxy\nfronts the app.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_OIDC_ISSUER</code></td>\n<td>(unset)</td>\n<td><strong>Required.</strong> Provider issuer URL (discovery runs against it).</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_OIDC_CLIENT_ID</code></td>\n<td>(unset)</td>\n<td><strong>Required.</strong></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_OIDC_CLIENT_SECRET</code></td>\n<td>(unset)</td>\n<td><strong>Required.</strong></td>\n</tr>\n</tbody></table>\n<h3 id=\"proxy-mode\">proxy mode</h3>\n<p>Front the app with an identity-aware proxy that asserts identity on every request. The app\nissues no session cookie; an unauthenticated request gets <code>401</code>. Exactly <strong>one</strong> trust path is\nactive, selected by whether <code>CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL</code> is set. They do not compose:\nwhen the JWKS URL is set, identity headers are never honored, even if they arrive (their\narrival is logged as a downgrade probe). Boot fails if neither path is configured.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_AUTH_PROXY_JWT_HEADER</code></td>\n<td><code>Cf-Access-Jwt-Assertion</code></td>\n<td>Header carrying the proxy's signed JWT (JWKS path). The default is the header name Cloudflare Access sends; the bundled compose demo sets <code>X-Forwarded-Access-Token</code> for oauth2-proxy.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL</code></td>\n<td>(unset)</td>\n<td>Enables the JWKS path: the JWT's signature, <code>iss</code>, <code>aud</code>, and expiry are verified against this key set on every request. The <code>email</code> claim is required.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AUTH_PROXY_JWT_ISSUER</code></td>\n<td>(unset)</td>\n<td><strong>Required when the JWKS URL is set.</strong></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE</code></td>\n<td>(unset)</td>\n<td><strong>Required when the JWKS URL is set.</strong></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AUTH_PROXY_EMAIL_HEADER</code></td>\n<td><code>X-Auth-Request-Email</code></td>\n<td>Identity header (trusted-header path). The default is the header oauth2-proxy sends.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AUTH_PROXY_NAME_HEADER</code></td>\n<td><code>X-Auth-Request-Preferred-Username</code></td>\n<td>Display-name header (trusted-header path).</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_TRUSTED_PROXY_IPS</code></td>\n<td>(empty)</td>\n<td>CSV of IPv4 addresses or CIDRs (<code>/1</code> to <code>/32</code>). Enables the trusted-header path when no JWKS URL is set: identity headers are honored only when the TCP socket peer is in this list (never based on <code>X-Forwarded-For</code>). Each entry is validated at boot in every mode: <code>/0</code>, malformed IPv4, and IPv6 are rejected (use the JWKS path for IPv6 proxies). Set it in <code>oidc</code> mode too, to your proxy's egress, so login throttling and audit rows are keyed per client rather than per proxy. See <a href=\"/docs/self-hosting/cdn\">Behind a CDN</a>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CLIENT_IP_HEADER</code></td>\n<td>(unset)</td>\n<td>Header carrying the real client IP behind a CDN (for example <code>True-Client-IP</code>; name whatever your CDN sends). Read <strong>only</strong> when the socket peer is a trusted proxy above, so it cannot be spoofed. When unset, the rightmost untrusted <code>X-Forwarded-For</code> hop is used. This IP keys rate limits and audit rows; it is never an auth input.</td>\n</tr>\n</tbody></table>\n<p>The proxy must be the only way to reach the app, and it must overwrite the identity headers it\nforwards. See <a href=\"/docs/self-hosting/deploy#auth-at-the-edge\">Auth at the edge</a>.</p>\n<h2 id=\"tenancy-the-org-boundary-optional\">Tenancy: the org boundary (optional)</h2>\n<p>Names an org so that guests (people who sign in on a non-org domain: a contractor, a personal\naddress, an admin on another domain) cannot see canvases shared with the Whole org rung.\nMembership is derived server-side from the user's verified email domain; a member can home a\ncanvas in the org, a guest only ever gets Personal.</p>\n<p>Off by default. With no <code>CANVAS_DROP_ORG_NAME</code> the feature is inert: Whole org keeps its legacy\n\"any signed-in user\" meaning and the home org is ignored everywhere. Setting the name turns it\non, so you can deploy first and migrate later.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_ORG_NAME</code></td>\n<td>(unset)</td>\n<td>The org's display name, trimmed. <strong>Setting this turns tenancy on.</strong> Unset or blank = inert.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_ORG_DOMAINS</code></td>\n<td>(= <code>CANVAS_DROP_ALLOWED_EMAIL_DOMAINS</code>)</td>\n<td>CSV of member domains, lowercased ASCII (list IDNs in punycode form). Defaults to the sign-in domain allowlist, the common single-org case.</td>\n</tr>\n</tbody></table>\n<p>At boot the instance materializes the org and its domains idempotently. The configured domain\nset is authoritative: a domain you remove is pruned at the next boot, and its users drop to\nguest on their next request. Boot fails on a bad config: a domain mapped to two orgs, more than\none org (multi-org is a later phase), or an org with no domains. Migrating an existing instance\n(homing current canvases, clamping guest-owned Whole org rows) is a one-time cutover with a\nread-only dry run: <code>pnpm tenancy:plan</code>, then <code>pnpm tenancy:plan --apply</code>. After narrowing\n<code>CANVAS_DROP_ORG_DOMAINS</code>, <code>pnpm tenancy:reconcile --apply</code> revokes the stale domain-sourced\nmemberships. The runbook is <code>docs/tenancy.md</code> in the repo.</p>\n<h2 id=\"database\">Database</h2>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_DB</code></td>\n<td><code>sqlite</code></td>\n<td><code>sqlite</code> | <code>postgres</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SQLITE_PATH</code></td>\n<td><code>./data/canvasdrop.db</code></td>\n<td>SQLite file path (WAL mode). The Docker image sets <code>/data/canvasdrop.db</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_DATABASE_URL</code></td>\n<td>(unset)</td>\n<td><strong>Required when <code>CANVAS_DROP_DB=postgres</code>.</strong> Standard <code>postgres://</code> connection string.</td>\n</tr>\n</tbody></table>\n<p>Migrations run at boot on the selected database; there is no separate migrate command.</p>\n<h2 id=\"storage\">Storage</h2>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_STORAGE</code></td>\n<td><code>local</code></td>\n<td><code>local</code> | <code>s3</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_STORAGE_PATH</code></td>\n<td><code>./data/storage</code></td>\n<td>Local storage root. The Docker image sets <code>/data/storage</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_S3_ENDPOINT</code></td>\n<td>(unset; the provider's default)</td>\n<td>Custom endpoint for a self-hosted or non-AWS S3-compatible store.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_S3_BUCKET</code></td>\n<td>(unset)</td>\n<td><strong>Required when <code>CANVAS_DROP_STORAGE=s3</code>.</strong> The bucket must already exist.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_S3_REGION</code></td>\n<td>(unset)</td>\n<td><strong>Required when <code>s3</code>.</strong></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_S3_ACCESS_KEY</code></td>\n<td>(unset)</td>\n<td><strong>Required when <code>s3</code>.</strong></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_S3_SECRET_KEY</code></td>\n<td>(unset)</td>\n<td><strong>Required when <code>s3</code>.</strong></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_S3_FORCE_PATH_STYLE</code></td>\n<td><code>true</code></td>\n<td>boolean. Keep <code>true</code> for path-style stores such as MinIO.</td>\n</tr>\n</tbody></table>\n<h2 id=\"screenshots-optional\">Screenshots (optional)</h2>\n<p>Generates canvas preview thumbnails with headless Chromium. The pipeline is off by default and\nneeds two things: Chromium in the image (build with <code>--build-arg SCREENSHOTS=1</code>) and\n<code>CANVAS_DROP_SCREENSHOTS=on</code>. The env var only sets availability; an admin still turns capture\non under Admin → Configuration (<code>screenshots.enabled</code>, <strong>runtime-editable</strong>, default off).\nEffective = available and enabled. Each tuning var is an integer ≥ 1. See\n<a href=\"/docs/self-hosting/screenshots\">Screenshots</a>.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_SCREENSHOTS</code></td>\n<td><code>off</code></td>\n<td><code>on</code> | <code>off</code>. Availability switch.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_CONCURRENCY</code></td>\n<td><code>1</code></td>\n<td>Concurrent render workers.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_TIMEOUT_MS</code></td>\n<td><code>20000</code></td>\n<td>Per-render timeout.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_RECYCLE_EVERY</code></td>\n<td><code>50</code></td>\n<td>Renders before a worker is recycled.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_LEASE_MS</code></td>\n<td><code>120000</code></td>\n<td>Job lease duration.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_MAX_ATTEMPTS</code></td>\n<td><code>3</code></td>\n<td>Attempts before a job is marked failed.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_FAILED_TTL_MS</code></td>\n<td><code>86400000</code></td>\n<td>How long a failed job is retained (24 h).</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_TOKEN_TTL_MS</code></td>\n<td><code>60000</code></td>\n<td>Render-token lifetime.</td>\n</tr>\n</tbody></table>\n<h2 id=\"rate-limiting\">Rate limiting</h2>\n<p>Per-minute request budgets, applied per class with in-process counters over a fixed 60-second\nwindow. Env-only: they are read on the hot path and are shown read-only in Admin →\nConfiguration. Each limit is an integer ≥ 1; boot fails on <code>0</code> or a negative value, so use the\nmaster switch to disable limiting.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_RATELIMIT_ENABLED</code></td>\n<td><code>true</code></td>\n<td>boolean. Master switch.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN</code></td>\n<td><code>120</code></td>\n<td>Canvas Runtime API.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_RATELIMIT_AI_PER_MIN</code></td>\n<td><code>10</code></td>\n<td>AI proxy calls.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_RATELIMIT_DEPLOY_PER_MIN</code></td>\n<td><code>10</code></td>\n<td>Deploy API, keyed per canvas, applied after key verification.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_RATELIMIT_MANAGEMENT_PER_MIN</code></td>\n<td><code>120</code></td>\n<td>Dashboard management routes.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_RATELIMIT_LOGIN_PER_MIN</code></td>\n<td><code>10</code></td>\n<td>Keyed on the resolved client IP.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_RATELIMIT_PASSWORD_GATE_PER_MIN</code></td>\n<td><code>5</code></td>\n<td>Password attempts on gated canvases.</td>\n</tr>\n</tbody></table>\n<h2 id=\"ai-optional\">AI (optional)</h2>\n<p>The AI primitive is off until a provider key is present, from <code>CANVAS_DROP_AI_API_KEY</code> or a\nruntime override. The key is server-side only and never reaches the browser. An empty or\nwhitespace value counts as unset.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_AI_PROVIDER</code></td>\n<td><code>anthropic</code></td>\n<td></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AI_API_KEY</code></td>\n<td>(unset)</td>\n<td><strong>Runtime-editable</strong> (write-only in the admin view). Unset or blank disables AI.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AI_BASE_URL</code></td>\n<td>(unset)</td>\n<td>Override the provider base URL.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AI_MODELS</code></td>\n<td><code>claude-haiku-4-5,claude-sonnet-4-6,claude-opus-4-8</code></td>\n<td>CSV allowlist of model ids canvases may call. <strong>Runtime-editable.</strong></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AI_USER_DAILY_USD</code></td>\n<td><code>5</code></td>\n<td>Per-user daily spend cap. <strong>Runtime-editable.</strong></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AI_CANVAS_MONTHLY_USD</code></td>\n<td><code>50</code></td>\n<td>Per-canvas monthly spend cap. <strong>Runtime-editable.</strong></td>\n</tr>\n</tbody></table>\n<h2 id=\"outbound-connections-optional\">Outbound Connections (optional)</h2>\n<p>Connections stays inert until an admin creates a profile and grants it to a\ncanvas. The root key is optional on a clean instance and for profiles with no\nprotected headers. Once protected values exist, store the same key with the\ndeployment secrets on every restart and restore. It must be base64 for exactly\n32 decoded bytes (<code>openssl rand -base64 32</code>). Missing or malformed key material\nfails protected-header create/use closed without preventing an otherwise unused\ninstance from booting.</p>\n<p>All rows are env-only and shown read-only in Admin → Configuration. Limits are\nper server process; the supported first-release deployment is one app process.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_CONNECTIONS_ENCRYPTION_KEY</code></td>\n<td>unset</td>\n<td>Write-only external AES-256-GCM root key. Not stored in canvas-drop backups.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CONNECTIONS_MAX_URL_BYTES</code></td>\n<td><code>8192</code></td>\n<td>Relative path plus query.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CONNECTIONS_MAX_BODY_BYTES</code></td>\n<td><code>262144</code></td>\n<td>Request body (256 KiB).</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CONNECTIONS_MAX_RESPONSE_BYTES</code></td>\n<td><code>2097152</code></td>\n<td>Buffered upstream body (2 MiB).</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CONNECTIONS_TIMEOUT_MS</code></td>\n<td><code>10000</code></td>\n<td>Total DNS, redirects, request, and response deadline.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CONNECTIONS_MAX_REDIRECTS</code></td>\n<td><code>3</code></td>\n<td>Exact-origin redirects; <code>0</code> disables following.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CONNECTIONS_CANVAS_CONCURRENCY</code></td>\n<td><code>5</code></td>\n<td>In-flight calls per canvas.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CONNECTIONS_INSTANCE_CONCURRENCY</code></td>\n<td><code>50</code></td>\n<td>In-flight calls in one app process.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CONNECTIONS_ACTOR_PER_MIN</code></td>\n<td><code>60</code></td>\n<td>Per server-resolved actor + canvas + profile.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CONNECTIONS_PROFILE_PER_MIN</code></td>\n<td><code>600</code></td>\n<td>Instance-wide bucket for one profile.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CONNECTIONS_MAX_CALLER_HEADERS</code></td>\n<td><code>32</code></td>\n<td>Canvas-supplied header count.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_CONNECTIONS_MAX_CALLER_HEADER_BYTES</code></td>\n<td><code>16384</code></td>\n<td>Combined canvas-supplied header names and values.</td>\n</tr>\n</tbody></table>\n<p>The <a href=\"/docs/sdk/connections\">Connections SDK reference</a> explains the grant,\nnetwork, header, and response boundaries.</p>\n<h2 id=\"authoring-optional\">Authoring (optional)</h2>\n<p>The authoring capability lets a signed-in viewer create a new canvas from a backend-enabled\ncanvas's page, as themselves. It is higher-privilege than the other primitives, so it is off\ninstance-wide by default and the per-canvas Backend toggle also defaults off; both must be on.\nThe share policy (the last three rows) is env-only.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_AUTHORING</code></td>\n<td><code>off</code></td>\n<td><code>on</code> | <code>off</code>. Instance master switch. <strong>Runtime-editable.</strong></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AUTHORING_USER_DAILY_MAX</code></td>\n<td><code>20</code></td>\n<td>Max canvases a viewer may author per UTC day. <strong>Runtime-editable.</strong></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AUTHORING_USER_TOTAL_MAX</code></td>\n<td><code>200</code></td>\n<td>All-time cap per viewer. <strong>Runtime-editable.</strong></td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AUTHORING_ALLOWED_RUNGS</code></td>\n<td><code>private,specific_people,whole_org,public_link</code></td>\n<td>CSV of <code>access</code> values a publish may request (<code>specific_people</code> is a legacy alias of <code>private</code>). <code>public_link</code> also covers the SDK's <code>access: \"password\"</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AUTHORING_MAX_EXPIRY_DAYS</code></td>\n<td><code>0</code></td>\n<td>Max share expiry in days; <code>0</code> = no cap.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AUTHORING_REQUIRE_EXPIRY</code></td>\n<td><code>false</code></td>\n<td>boolean. Require an expiry for <strong>Whole org</strong> and <strong>Public link</strong> authoring publishes. Restricted values (<code>private</code>, <code>specific_people</code>, <code>team</code>) never require one.</td>\n</tr>\n</tbody></table>\n<h2 id=\"email-transport\">Email transport</h2>\n<p>Sends the auth-delegated sign-in and access emails for account, canvas, and team access. It is\na driver behind an interface, like the database and storage. Provider secrets are env-only,\nnever DB-overridable, and never logged.</p>\n<p><strong><code>proxy</code> mode sends no email.</strong> The mailer is not built in that mode (the IAP owns sign-in),\nso every sign-in or access email is skipped; grants still happen. The variables below apply to\n<code>dev</code> and <code>oidc</code>.</p>\n<p>A driver that cannot send skips the email rather than failing the grant: <code>smtp</code> needs a host,\n<code>mailgun</code> needs an API key and a domain, <code>noop</code> never sends, <code>log</code> always \"sends\" (to the log).</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_EMAIL_DRIVER</code></td>\n<td><code>log</code></td>\n<td><code>log</code> (writes the message to the server log, nothing delivered; zero-setup dev) | <code>smtp</code> | <code>mailgun</code> | <code>noop</code> (discards).</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_EMAIL_FROM</code></td>\n<td><code>no-reply@&lt;CANVAS_DROP_MAILGUN_DOMAIN&gt;</code> when that is set, else <code>no-reply@localhost</code></td>\n<td>Sender address.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SMTP_HOST</code></td>\n<td>(unset)</td>\n<td>SMTP server host (driver <code>smtp</code>). Not checked at boot; without it every email is skipped.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SMTP_PORT</code></td>\n<td><code>587</code></td>\n<td><code>587</code> = STARTTLS, <code>465</code> = implicit TLS.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SMTP_USER</code> / <code>CANVAS_DROP_SMTP_PASS</code></td>\n<td>(unset)</td>\n<td>Omit both for an IP-allowlisted relay; auth is sent only when both are present.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SMTP_SECURE</code></td>\n<td><code>false</code></td>\n<td>boolean. <code>true</code> for implicit TLS (port 465).</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_MAILGUN_API_KEY</code></td>\n<td>(unset)</td>\n<td>Mailgun HTTP API key (driver <code>mailgun</code>).</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_MAILGUN_DOMAIN</code></td>\n<td>(unset)</td>\n<td>e.g. <code>mg.example.com</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_MAILGUN_BASE_URL</code></td>\n<td><code>https://api.mailgun.net</code></td>\n<td>Use <code>https://api.eu.mailgun.net</code> for EU accounts.</td>\n</tr>\n</tbody></table>\n<p><a></a></p>\n<h2 id=\"sign-in-permits-and-access-emails\">Sign-in permits and access emails</h2>\n<p>These are DB-managed admin settings with no env var, under Admin → Configuration, layered on\ntop of the mailer above. They govern the\n<a href=\"/docs/self-hosting/security-model#adds-are-auth-delegated-no-app-owned-credentials\">auth-delegated add-person flow</a>:\npersonal-team adds, canvas Add person, and admin sign-in permits.</p>\n<table>\n<thead>\n<tr>\n<th>Setting</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>email.invitesEnabled</code></td>\n<td><code>false</code></td>\n<td>Master switch. When off, grants still happen but no sign-in or access email is sent. Also needs a driver that can send (see above).</td>\n</tr>\n<tr>\n<td><code>email.notifyOnAddUser</code></td>\n<td><code>true</code></td>\n<td>Email a person when an admin permits their email to sign in.</td>\n</tr>\n<tr>\n<td><code>email.notifyOnCanvasAdd</code></td>\n<td><code>true</code></td>\n<td>Email an existing user when added to a canvas's people-and-teams list.</td>\n</tr>\n<tr>\n<td><code>email.notifyOnCanvasInvite</code></td>\n<td><code>true</code></td>\n<td>Email an existing user on an individual one-canvas access action.</td>\n</tr>\n<tr>\n<td><code>invites.allowMemberNewEmails</code></td>\n<td><code>false</code></td>\n<td>Let a non-admin member add a brand-new external email. Off = only admins can permit new emails; members can add people who can already sign in.</td>\n</tr>\n<tr>\n<td><code>invites.maxPerActorPerHour</code></td>\n<td><code>20</code></td>\n<td>Add-person actions per actor per hour (admins get a higher allowance).</td>\n</tr>\n<tr>\n<td><code>invites.pendingCap</code></td>\n<td><code>50</code></td>\n<td>Max unconsumed pending sign-ins one actor may hold.</td>\n</tr>\n<tr>\n<td><code>access.publicLinksEnabled</code></td>\n<td><code>true</code></td>\n<td>Instance-wide switch for the Public link rung. Off denies new public-link publishes and returns existing public-link canvases to <strong>Restricted</strong>.</td>\n</tr>\n</tbody></table>\n<p>Adding a brand-new person to a team or canvas sends them the sign-in invitation, gated only by\nthe master switch and the mailer; existing team members are not re-notified.</p>\n<p>Admin → Configuration also holds the runtime quotas for the KV and files primitives, with no\nenv var: <code>quota.kv.keys.shared</code> (default <code>10000</code> shared keys per canvas), <code>quota.kv.keys.user</code>\n(<code>1000</code> per-user keys), <code>quota.files.bytes.file</code> (<code>26214400</code>, 25 MB per file), and\n<code>quota.files.bytes.canvas</code> (<code>1073741824</code>, 1 GB per canvas).</p>\n<p>Email templates for each message are admin-editable (subject, HTML, text) under Admin →\nConfiguration → Email templates, with <code>{{variable}}</code> substitution from an allow-list (<code>name</code>,\n<code>recipientEmail</code>, <code>inviterName</code>, <code>canvasTitle</code>, <code>teamName</code>, <code>link</code>, <code>role</code>, <code>accessLabel</code>,\n<code>personEmail</code>, <code>reason</code>, plus the legacy <code>instanceName</code>, <code>orgName</code>, <code>orgContext</code>) and a reset to\nthe latest default. Values are HTML-escaped in the HTML body; an unknown variable renders empty.</p>\n<h2 id=\"logging-and-error-tracking\">Logging and error tracking</h2>\n<p>Structured logs go to stdout via pino: no app-side files, rotation, or shipping. <code>/healthz</code> is\nexcluded from request logging.</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>LOG_LEVEL</code></td>\n<td><code>info</code></td>\n<td><code>fatal</code> | <code>error</code> | <code>warn</code> | <code>info</code> | <code>debug</code> | <code>trace</code>.</td>\n</tr>\n<tr>\n<td><code>LOG_FORMAT</code></td>\n<td><code>json</code> when <code>NODE_ENV=production</code>, else <code>pretty</code></td>\n<td><code>json</code> | <code>pretty</code>. <code>pretty</code> runs pino-pretty in-process; use it for local dev only.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SENTRY_DSN</code></td>\n<td>(unset)</td>\n<td>Accepted and shown in Admin → Configuration, but the server ships no Sentry client today: setting it sends nothing. Error tracking is off.</td>\n</tr>\n</tbody></table>\n<h2 id=\"tooling-only-variables\">Tooling-only variables</h2>\n<p>A few <code>CANVAS_DROP_*</code> names in the repo are read by scripts and tests, not by the server, and\nare not part of the config schema:</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Read by</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_DEV_SEED</code></td>\n<td><code>pnpm dev</code></td>\n<td><code>0</code> skips the sample-canvas seed.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_DASHBOARD_PORT</code></td>\n<td>the dashboard's Vite dev server</td>\n<td>Dev port (default <code>5173</code>).</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_DASHBOARD_URL</code></td>\n<td><code>pnpm docs:screenshots</code>, <code>pnpm skins:shot</code></td>\n<td>Dashboard base for Playwright captures (default <code>http://localhost:5173</code>).</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_DB</code> (in tests)</td>\n<td><code>pnpm test:sqlite</code>, <code>pnpm test:pg</code></td>\n<td>When set, the suite runs only that dialect; unset runs both in-process.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_TEST_*</code></td>\n<td>the test runner and CI</td>\n<td>Run isolation (<code>_RUN_ID</code>, <code>_MAX_WORKERS</code>, <code>_REGISTRY_DIR</code>), the real Postgres and MinIO smoke tests (<code>_DATABASE_URL</code>, <code>_S3_*</code>), and the opt-in Chromium capture test (<code>_SCREENSHOTS=1</code>).</td>\n</tr>\n</tbody></table>\n<blockquote>\n<p>All examples use placeholder values. Never commit real secrets; set them in your deployment\nenvironment (systemd, container env, secrets manager).</p>\n</blockquote>\n",
    "headings": [
      {
        "id": "how-config-gets-loaded",
        "text": "How config gets loaded",
        "level": 2
      },
      {
        "id": "value-formats",
        "text": "Value formats",
        "level": 2
      },
      {
        "id": "boot-time-checks",
        "text": "Boot-time checks",
        "level": 2
      },
      {
        "id": "the-four-pluggable-interfaces",
        "text": "The four pluggable interfaces",
        "level": 2
      },
      {
        "id": "core",
        "text": "Core",
        "level": 2
      },
      {
        "id": "design-skins",
        "text": "Design skins",
        "level": 2
      },
      {
        "id": "auth",
        "text": "Auth",
        "level": 2
      },
      {
        "id": "dev-mode",
        "text": "dev mode",
        "level": 3
      },
      {
        "id": "oidc-mode",
        "text": "oidc mode",
        "level": 3
      },
      {
        "id": "proxy-mode",
        "text": "proxy mode",
        "level": 3
      },
      {
        "id": "tenancy-the-org-boundary-optional",
        "text": "Tenancy: the org boundary (optional)",
        "level": 2
      },
      {
        "id": "database",
        "text": "Database",
        "level": 2
      },
      {
        "id": "storage",
        "text": "Storage",
        "level": 2
      },
      {
        "id": "screenshots-optional",
        "text": "Screenshots (optional)",
        "level": 2
      },
      {
        "id": "rate-limiting",
        "text": "Rate limiting",
        "level": 2
      },
      {
        "id": "ai-optional",
        "text": "AI (optional)",
        "level": 2
      },
      {
        "id": "outbound-connections-optional",
        "text": "Outbound Connections (optional)",
        "level": 2
      },
      {
        "id": "authoring-optional",
        "text": "Authoring (optional)",
        "level": 2
      },
      {
        "id": "email-transport",
        "text": "Email transport",
        "level": 2
      },
      {
        "id": "sign-in-permits-and-access-emails",
        "text": "Sign-in permits and access emails",
        "level": 2
      },
      {
        "id": "logging-and-error-tracking",
        "text": "Logging and error tracking",
        "level": 2
      },
      {
        "id": "tooling-only-variables",
        "text": "Tooling-only variables",
        "level": 2
      }
    ],
    "text": "Configuration If you run a canvas-drop instance, this is the environment-variable reference: every variable the server reads, its default, and what refuses to boot when it is wrong. Every setting is an environment variable, validated once at startup. One module ( packages/shared/src/config/env.ts ) is the only reader of process.env ; everything else takes typed config. An invalid value or combination fails at boot with a message naming every variable to fix, never at request time. Nothing is reported anywhere: no telemetry, no phone-home. The defaults below are the schema defaults. .env.example mirrors them with comments; .env.production.example is the annotated production profile. How config gets loaded Local dev. pnpm dev reads the root .env file once ( node --env-file-if-exists=.env ). Copy the template and start: cp .env.example . env && pnpm dev # server on http://localhost:3000, dashboard on http://localhost:5173, signed in as dev@example.com The defaults give you a logged-in instance: path URLs, sqlite , local storage, dev auth. Production. The .env file is not read. Set variables in the process environment: a systemd EnvironmentFile , the container environment: block, or a secrets manager. The recommended profile (subdomain URLs, proxy auth verified against a JWKS, Postgres, S3) needs this set; everything else keeps its default: NODE_ENV=production CANVAS_DROP_URL_MODE=subdomain CANVAS_DROP_BASE_URL=https://canvases.example.com CANVAS_DROP_SESSION_SECRET=<output of: openssl rand -hex 32> CANVAS_DROP_AUTH_MODE=proxy CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL=https://idp.example.com/.well-known/jwks.json CANVAS_DROP_AUTH_PROXY_JWT_ISSUER=https://idp.example.com CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE=canvas-drop CANVAS_DROP_ALLOWED_EMAIL_DOMAINS=example.com CANVAS_DROP_ADMIN_EMAILS=admin@example.com CANVAS_DROP_DB=postgres CANVAS_DROP_DATABASE_URL=postgres://canvasdrop:<password>@db.internal:5432/canvasdrop CANVAS_DROP_STORAGE=s3 CANVAS_DROP_S3_BUCKET=canvas-drop CANVAS_DROP_S3_REGION=us-east-1 CANVAS_DROP_S3_ACCESS_KEY=<access key> CANVAS_DROP_S3_SECRET_KEY=<secret key> A misconfigured start prints every problem at once and exits with status 1: Invalid canvas-drop configuration: - CANVAS_DROP_SESSION_SECRET: a session secret of at least 32 characters is required outside dev mode (and always in production) - CANVAS_DROP_DATABASE_URL: CANVAS_DROP_DATABASE_URL is required when CANVAS_DROP_DB=postgres Fix the variables above (see .env.example) and restart. Docker image. The image presets NODE_ENV=production , CANVAS_DROP_PORT=3000 , CANVAS_DROP_SQLITE_PATH=/data/canvasdrop.db , CANVAS_DROP_STORAGE_PATH=/data/storage , and CANVAS_DROP_DASHBOARD_DIST=/app/apps/dashboard/dist , with /data as the volume. Everything else comes from the environment you pass in. See Deploy . Admin view. Admin → Configuration lists the main settings with their source: Environment, Default, or a DB override. Secrets render as set or unset plus the last four characters. A few settings can be edited there at runtime (a DB override layered over env); the tables below mark them runtime-editable . Everything else is env-only by design: a web panel must not be able to flip the auth mode, the database driver, or the session secret. A handful of env-only knobs are not shown in the panel at all ( CANVAS_DROP_MCP , CANVAS_DROP_DASHBOARD_DIST , the screenshot tuning values, the proxy header names, CANVAS_DROP_ORG_* , CANVAS_DROP_S3_FORCE_PATH_STYLE , CANVAS_DROP_DEV_USER_* , and the authoring share policy). Value formats Format Accepted values enum exactly one of the listed tokens, e.g. on or off CSV comma-separated; entries trimmed, empty entries dropped boolean 1 , true , on , yes or 0 , false , off , no (case-insensitive); anything else fails boot number any finite number integer ≥ 1 rate limits and screenshot tuning; 0 or a negative value fails boot (use the _ENABLED switch to turn a class off) integer ≥ 0 CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL URL an absolute URL with a scheme ( CANVAS_DROP_BASE_URL , JWKS, OIDC issuer, AI and Mailgun base URLs) An empty value counts as unset and takes the default. Boot-time checks These combinations are enforced at startup, in addition to each variable's format: CANVAS_DROP_AUTH_MODE=dev with NODE_ENV=production refuses to start. CANVAS_DROP_URL_MODE=subdomain needs a non-localhost CANVAS_DROP_BASE_URL ( localhost , 127.0.0.1 , ::1 , and *.localhost are rejected). CANVAS_DROP_SESSION_SECRET must be at least 32 characters whenever the auth mode is not dev , and always when NODE_ENV=production . CANVAS_DROP_DB=postgres requires CANVAS_DROP_DATABASE_URL . CANVAS_DROP_STORAGE=s3 requires CANVAS_DROP_S3_BUCKET , CANVAS_DROP_S3_REGION , CANVAS_DROP_S3_ACCESS_KEY , and CANVAS_DROP_S3_SECRET_KEY . Every CANVAS_DROP_TRUSTED_PROXY_IPS entry is validated, in every auth mode. proxy auth requires CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL or a non-empty CANVAS_DROP_TRUSTED_PROXY_IPS ; with a JWKS URL, CANVAS_DROP_AUTH_PROXY_JWT_ISSUER and CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE are required too. oidc auth requires CANVAS_DROP_OIDC_ISSUER , CANVAS_DROP_OIDC_CLIENT_ID , and CANVAS_DROP_OIDC_CLIENT_SECRET . Any auth mode other than dev requires at least one CANVAS_DROP_ALLOWED_EMAIL_DOMAINS entry. path URL mode with proxy or oidc auth requires CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true . Tenancy adds three more once CANVAS_DROP_ORG_NAME is set; see Tenancy . The four pluggable interfaces Each interface is selected by one switch variable. Swapping a driver is a config change, never a code change. Migrations run at boot on whichever database is selected, and a backup taken on one driver restores onto another (see Backups and maintenance ). Interface Switch Options (default first) Driver-specific vars Database CANVAS_DROP_DB sqlite / postgres sqlite: CANVAS_DROP_SQLITE_PATH ; postgres: CANVAS_DROP_DATABASE_URL Storage CANVAS_DROP_STORAGE local / s3 local: CANVAS_DROP_STORAGE_PATH ; s3: CANVAS_DROP_S3_* (any S3-compatible store via a custom endpoint) URL mode CANVAS_DROP_URL_MODE path / subdomain CANVAS_DROP_BASE_URL (non-localhost for subdomain); optional CANVAS_DROP_API_BASE_URL ; CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE for path mode with real auth Auth CANVAS_DROP_AUTH_MODE dev / proxy / oidc dev: CANVAS_DROP_DEV_USER_* ; proxy: JWT vars or trusted-header vars; oidc: CANVAS_DROP_OIDC_* ; both real modes: CANVAS_DROP_ALLOWED_EMAIL_DOMAINS , CANVAS_DROP_SESSION_SECRET Email transport ( CANVAS_DROP_EMAIL_DRIVER ) is a fifth, smaller driver seam; see Email transport . Core Variable Default Notes NODE_ENV development development | production | test . production forbids dev auth, makes the session secret mandatory, and switches the LOG_FORMAT default to json . CANVAS_DROP_URL_MODE path path serves canvases at {base}/c/{slug}/ ; subdomain serves {slug}.{baseHost} and gives each canvas its own origin. CANVAS_DROP_BASE_URL http://localhost:3000 Public origin of the instance. Must be non-localhost in subdomain mode. The app never reads X-Forwarded-Proto ; behind TLS this must be the https:// origin. CANVAS_DROP_API_BASE_URL (= CANVAS_DROP_BASE_URL ) Where the Deploy API ( /v1/canvases/* ) is reachable. Set it only when the API is fronted on its own host, e.g. canvases at {slug}.canvases.example.com and the API at https://api.canvases.example.com . MCP tools advertise endpoints built from this value, so agents never probe for the host. CANVAS_DROP_PORT 3000 Listen port. CANVAS_DROP_SESSION_SECRET (insecure dev fallback) Required, at least 32 characters, outside dev auth and always in production. HMAC key for password-gate cookies and screenshot capture tokens. Session cookies ( oidc , dev ) are random tokens stored hashed in the database, not derived from it. Generate with openssl rand -hex 32 . CANVAS_DROP_ADMIN_EMAILS (empty; in dev auth, the dev user) CSV, lowercased. Bootstrap admins: these accounts get the admin surface. CANVAS_DROP_REALTIME on on | off . Master switch for the realtime primitive. CANVAS_DROP_MCP on on | off . When off , the /mcp and OAuth routes are not mounted at all. CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE false boolean. Must be true to boot path mode with proxy or oidc auth: several users then share one origin, so browser isolation is reduced. See Path mode vs subdomain mode . CANVAS_DROP_DASHBOARD_DIST (resolved from the server module) Path to the built dashboard SPA. The Docker image sets /app/apps/dashboard/dist . CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL 300 integer ≥ 0, seconds. How long a shared cache (a CDN in front) may hold a public canvas's HTML, emitted as s-maxage ; the browser still revalidates every load. Only the Public link rung without a password is ever shared-cacheable; auth-gated canvases stay private . It is also how long a canvas can stay visible at the edge after access is restricted, and the dashboard warns owners with this figure. 0 disables shared caching. See Behind a CDN . CANVAS_DROP_DESIGN_SKIN editorial editorial | studio | workshop | canvas . Instance-wide visual language. Runtime-editable ; a DB override wins over this value. See Design skins . Admin → Configuration also carries core.instanceName , a DB-only display name (default: the host of CANVAS_DROP_BASE_URL ) for custom email templates that still use {{instanceName}} . The seeded templates name the recipient email, the inviter, and the specific canvas, team, or sign-in action instead of instance or org branding. Design skins A design skin is an instance-wide visual language: the dashboard, the editor, the signed-out landing page, the docs, and the legal pages all change together. It is a token-only layer over the brand tokens: a skin overrides the accent colour family, the display-type bundle (font, weight, tracking), and the corner-radius scale. No skin forks layout or structure. Error pages keep the default look. Skin Accent Display type Corners Feel editorial (default) deep teal Newsreader serif, weight 500 1× Calm publishing OS; the base look. studio terracotta Newsreader serif, weight 500 1× Warm editorial. workshop green Geist Mono, weight 500 0.62× (tighter) Developer / IDE. canvas violet Geist sans, weight 800 1.3× (rounder) Playful, bold. Accent lightness is kept conservative in every skin so the accent foreground clears WCAG AA on the fill in light and dark mode. Two ways to set it, with the same effect for everyone on the instance: Admin console. Admin → Configuration → Design skin. Changing the dropdown previews the skin live in your own session; Save writes the override to the database and applies it to all users at runtime, with no restart. Leaving the page without saving reverts your preview. CANVAS_DROP_DESIGN_SKIN . The boot default. A DB override set in the console always wins over this value; Clear in the console removes the override and falls back to it. The skin is global and admin-only. It is not a per-user preference; light or dark theme is the per-user setting. Auth CANVAS_DROP_AUTH_MODE selects how identity is established. Identity always comes from the server-side auth strategy, never from anything the client sends. In every mode the same checks run on every request: the email must pass the domain allowlist (or a sign-in permit), the user must not be blocked, and org membership is derived from the verified email domain. See Security model . Variable Default Notes CANVAS_DROP_AUTH_MODE dev dev | proxy | oidc . dev is rejected when NODE_ENV=production . CANVAS_DROP_ALLOWED_EMAIL_DOMAINS (empty; in dev auth, the dev user's domain) CSV, lowercased. Required (at least one) in proxy and oidc . Enforced on every request. Sign-in permits. Beyond the domain list, an admin can permit specific outside emails to sign in (a contractor, a test account) under Admin → People → Sign-in permits. It is an additive, DB-managed layer: the env domain list is unchanged, and an email passes if its domain is allowed or it is on this list. There is no app-owned password; adding an email sends a sign-in invitation (when access emails are on), and removing an entry revokes that email's access on its next sign-in. In proxy mode this list cannot widen the upstream IAP by itself, and no email is sent: admit a brand-new external email in the proxy first, then add it here. See Sign-in permits and access emails . dev mode Auto-logs-in a fixed local user with zero setup; no credential is examined. A session cookie still exists so /auth/logout works. Localhost only; production refuses to boot in this mode. Variable Default CANVAS_DROP_DEV_USER_EMAIL dev@example.com CANVAS_DROP_DEV_USER_NAME Dev User oidc mode The app owns login: Authorization Code with PKCE against your provider, then an HttpOnly session cookie ( __canvasdrop_session , 14-day rolling expiry, SameSite=Lax , Secure in production, Domain=.{baseHost} in subdomain mode, host-only in path mode). An unauthenticated request is redirected to /auth/login . Use this when no identity-aware proxy fronts the app. Variable Default Notes CANVAS_DROP_OIDC_ISSUER (unset) Required. Provider issuer URL (discovery runs against it). CANVAS_DROP_OIDC_CLIENT_ID (unset) Required. CANVAS_DROP_OIDC_CLIENT_SECRET (unset) Required. proxy mode Front the app with an identity-aware proxy that asserts identity on every request. The app issues no session cookie; an unauthenticated request gets 401 . Exactly one trust path is active, selected by whether CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL is set. They do not compose: when the JWKS URL is set, identity headers are never honored, even if they arrive (their arrival is logged as a downgrade probe). Boot fails if neither path is configured. Variable Default Notes CANVAS_DROP_AUTH_PROXY_JWT_HEADER Cf-Access-Jwt-Assertion Header carrying the proxy's signed JWT (JWKS path). The default is the header name Cloudflare Access sends; the bundled compose demo sets X-Forwarded-Access-Token for oauth2-proxy. CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL (unset) Enables the JWKS path: the JWT's signature, iss , aud , and expiry are verified against this key set on every request. The email claim is required. CANVAS_DROP_AUTH_PROXY_JWT_ISSUER (unset) Required when the JWKS URL is set. CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE (unset) Required when the JWKS URL is set. CANVAS_DROP_AUTH_PROXY_EMAIL_HEADER X-Auth-Request-Email Identity header (trusted-header path). The default is the header oauth2-proxy sends. CANVAS_DROP_AUTH_PROXY_NAME_HEADER X-Auth-Request-Preferred-Username Display-name header (trusted-header path). CANVAS_DROP_TRUSTED_PROXY_IPS (empty) CSV of IPv4 addresses or CIDRs ( /1 to /32 ). Enables the trusted-header path when no JWKS URL is set: identity headers are honored only when the TCP socket peer is in this list (never based on X-Forwarded-For ). Each entry is validated at boot in every mode: /0 , malformed IPv4, and IPv6 are rejected (use the JWKS path for IPv6 proxies). Set it in oidc mode too, to your proxy's egress, so login throttling and audit rows are keyed per client rather than per proxy. See Behind a CDN . CANVAS_DROP_CLIENT_IP_HEADER (unset) Header carrying the real client IP behind a CDN (for example True-Client-IP ; name whatever your CDN sends). Read only when the socket peer is a trusted proxy above, so it cannot be spoofed. When unset, the rightmost untrusted X-Forwarded-For hop is used. This IP keys rate limits and audit rows; it is never an auth input. The proxy must be the only way to reach the app, and it must overwrite the identity headers it forwards. See Auth at the edge . Tenancy: the org boundary (optional) Names an org so that guests (people who sign in on a non-org domain: a contractor, a personal address, an admin on another domain) cannot see canvases shared with the Whole org rung. Membership is derived server-side from the user's verified email domain; a member can home a canvas in the org, a guest only ever gets Personal. Off by default. With no CANVAS_DROP_ORG_NAME the feature is inert: Whole org keeps its legacy \"any signed-in user\" meaning and the home org is ignored everywhere. Setting the name turns it on, so you can deploy first and migrate later. Variable Default Notes CANVAS_DROP_ORG_NAME (unset) The org's display name, trimmed. Setting this turns tenancy on. Unset or blank = inert. CANVAS_DROP_ORG_DOMAINS (= CANVAS_DROP_ALLOWED_EMAIL_DOMAINS ) CSV of member domains, lowercased ASCII (list IDNs in punycode form). Defaults to the sign-in domain allowlist, the common single-org case. At boot the instance materializes the org and its domains idempotently. The configured domain set is authoritative: a domain you remove is pruned at the next boot, and its users drop to guest on their next request. Boot fails on a bad config: a domain mapped to two orgs, more than one org (multi-org is a later phase), or an org with no domains. Migrating an existing instance (homing current canvases, clamping guest-owned Whole org rows) is a one-time cutover with a read-only dry run: pnpm tenancy:plan , then pnpm tenancy:plan --apply . After narrowing CANVAS_DROP_ORG_DOMAINS , pnpm tenancy:reconcile --apply revokes the stale domain-sourced memberships. The runbook is docs/tenancy.md in the repo. Database Variable Default Notes CANVAS_DROP_DB sqlite sqlite | postgres . CANVAS_DROP_SQLITE_PATH ./data/canvasdrop.db SQLite file path (WAL mode). The Docker image sets /data/canvasdrop.db . CANVAS_DROP_DATABASE_URL (unset) Required when CANVAS_DROP_DB=postgres . Standard postgres:// connection string. Migrations run at boot on the selected database; there is no separate migrate command. Storage Variable Default Notes CANVAS_DROP_STORAGE local local | s3 . CANVAS_DROP_STORAGE_PATH ./data/storage Local storage root. The Docker image sets /data/storage . CANVAS_DROP_S3_ENDPOINT (unset; the provider's default) Custom endpoint for a self-hosted or non-AWS S3-compatible store. CANVAS_DROP_S3_BUCKET (unset) Required when CANVAS_DROP_STORAGE=s3 . The bucket must already exist. CANVAS_DROP_S3_REGION (unset) Required when s3 . CANVAS_DROP_S3_ACCESS_KEY (unset) Required when s3 . CANVAS_DROP_S3_SECRET_KEY (unset) Required when s3 . CANVAS_DROP_S3_FORCE_PATH_STYLE true boolean. Keep true for path-style stores such as MinIO. Screenshots (optional) Generates canvas preview thumbnails with headless Chromium. The pipeline is off by default and needs two things: Chromium in the image (build with --build-arg SCREENSHOTS=1 ) and CANVAS_DROP_SCREENSHOTS=on . The env var only sets availability; an admin still turns capture on under Admin → Configuration ( screenshots.enabled , runtime-editable , default off). Effective = available and enabled. Each tuning var is an integer ≥ 1. See Screenshots . Variable Default Notes CANVAS_DROP_SCREENSHOTS off on | off . Availability switch. CANVAS_DROP_SCREENSHOTS_CONCURRENCY 1 Concurrent render workers. CANVAS_DROP_SCREENSHOTS_TIMEOUT_MS 20000 Per-render timeout. CANVAS_DROP_SCREENSHOTS_RECYCLE_EVERY 50 Renders before a worker is recycled. CANVAS_DROP_SCREENSHOTS_LEASE_MS 120000 Job lease duration. CANVAS_DROP_SCREENSHOTS_MAX_ATTEMPTS 3 Attempts before a job is marked failed. CANVAS_DROP_SCREENSHOTS_FAILED_TTL_MS 86400000 How long a failed job is retained (24 h). CANVAS_DROP_SCREENSHOTS_TOKEN_TTL_MS 60000 Render-token lifetime. Rate limiting Per-minute request budgets, applied per class with in-process counters over a fixed 60-second window. Env-only: they are read on the hot path and are shown read-only in Admin → Configuration. Each limit is an integer ≥ 1; boot fails on 0 or a negative value, so use the master switch to disable limiting. Variable Default Notes CANVAS_DROP_RATELIMIT_ENABLED true boolean. Master switch. CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN 120 Canvas Runtime API. CANVAS_DROP_RATELIMIT_AI_PER_MIN 10 AI proxy calls. CANVAS_DROP_RATELIMIT_DEPLOY_PER_MIN 10 Deploy API, keyed per canvas, applied after key verification. CANVAS_DROP_RATELIMIT_MANAGEMENT_PER_MIN 120 Dashboard management routes. CANVAS_DROP_RATELIMIT_LOGIN_PER_MIN 10 Keyed on the resolved client IP. CANVAS_DROP_RATELIMIT_PASSWORD_GATE_PER_MIN 5 Password attempts on gated canvases. AI (optional) The AI primitive is off until a provider key is present, from CANVAS_DROP_AI_API_KEY or a runtime override. The key is server-side only and never reaches the browser. An empty or whitespace value counts as unset. Variable Default Notes CANVAS_DROP_AI_PROVIDER anthropic CANVAS_DROP_AI_API_KEY (unset) Runtime-editable (write-only in the admin view). Unset or blank disables AI. CANVAS_DROP_AI_BASE_URL (unset) Override the provider base URL. CANVAS_DROP_AI_MODELS claude-haiku-4-5,claude-sonnet-4-6,claude-opus-4-8 CSV allowlist of model ids canvases may call. Runtime-editable. CANVAS_DROP_AI_USER_DAILY_USD 5 Per-user daily spend cap. Runtime-editable. CANVAS_DROP_AI_CANVAS_MONTHLY_USD 50 Per-canvas monthly spend cap. Runtime-editable. Outbound Connections (optional) Connections stays inert until an admin creates a profile and grants it to a canvas. The root key is optional on a clean instance and for profiles with no protected headers. Once protected values exist, store the same key with the deployment secrets on every restart and restore. It must be base64 for exactly 32 decoded bytes ( openssl rand -base64 32 ). Missing or malformed key material fails protected-header create/use closed without preventing an otherwise unused instance from booting. All rows are env-only and shown read-only in Admin → Configuration. Limits are per server process; the supported first-release deployment is one app process. Variable Default Notes CANVAS_DROP_CONNECTIONS_ENCRYPTION_KEY unset Write-only external AES-256-GCM root key. Not stored in canvas-drop backups. CANVAS_DROP_CONNECTIONS_MAX_URL_BYTES 8192 Relative path plus query. CANVAS_DROP_CONNECTIONS_MAX_BODY_BYTES 262144 Request body (256 KiB). CANVAS_DROP_CONNECTIONS_MAX_RESPONSE_BYTES 2097152 Buffered upstream body (2 MiB). CANVAS_DROP_CONNECTIONS_TIMEOUT_MS 10000 Total DNS, redirects, request, and response deadline. CANVAS_DROP_CONNECTIONS_MAX_REDIRECTS 3 Exact-origin redirects; 0 disables following. CANVAS_DROP_CONNECTIONS_CANVAS_CONCURRENCY 5 In-flight calls per canvas. CANVAS_DROP_CONNECTIONS_INSTANCE_CONCURRENCY 50 In-flight calls in one app process. CANVAS_DROP_CONNECTIONS_ACTOR_PER_MIN 60 Per server-resolved actor + canvas + profile. CANVAS_DROP_CONNECTIONS_PROFILE_PER_MIN 600 Instance-wide bucket for one profile. CANVAS_DROP_CONNECTIONS_MAX_CALLER_HEADERS 32 Canvas-supplied header count. CANVAS_DROP_CONNECTIONS_MAX_CALLER_HEADER_BYTES 16384 Combined canvas-supplied header names and values. The Connections SDK reference explains the grant, network, header, and response boundaries. Authoring (optional) The authoring capability lets a signed-in viewer create a new canvas from a backend-enabled canvas's page, as themselves. It is higher-privilege than the other primitives, so it is off instance-wide by default and the per-canvas Backend toggle also defaults off; both must be on. The share policy (the last three rows) is env-only. Variable Default Notes CANVAS_DROP_AUTHORING off on | off . Instance master switch. Runtime-editable. CANVAS_DROP_AUTHORING_USER_DAILY_MAX 20 Max canvases a viewer may author per UTC day. Runtime-editable. CANVAS_DROP_AUTHORING_USER_TOTAL_MAX 200 All-time cap per viewer. Runtime-editable. CANVAS_DROP_AUTHORING_ALLOWED_RUNGS private,specific_people,whole_org,public_link CSV of access values a publish may request ( specific_people is a legacy alias of private ). public_link also covers the SDK's access: \"password\" . CANVAS_DROP_AUTHORING_MAX_EXPIRY_DAYS 0 Max share expiry in days; 0 = no cap. CANVAS_DROP_AUTHORING_REQUIRE_EXPIRY false boolean. Require an expiry for Whole org and Public link authoring publishes. Restricted values ( private , specific_people , team ) never require one. Email transport Sends the auth-delegated sign-in and access emails for account, canvas, and team access. It is a driver behind an interface, like the database and storage. Provider secrets are env-only, never DB-overridable, and never logged. proxy mode sends no email. The mailer is not built in that mode (the IAP owns sign-in), so every sign-in or access email is skipped; grants still happen. The variables below apply to dev and oidc . A driver that cannot send skips the email rather than failing the grant: smtp needs a host, mailgun needs an API key and a domain, noop never sends, log always \"sends\" (to the log). Variable Default Notes CANVAS_DROP_EMAIL_DRIVER log log (writes the message to the server log, nothing delivered; zero-setup dev) | smtp | mailgun | noop (discards). CANVAS_DROP_EMAIL_FROM no-reply@<CANVAS_DROP_MAILGUN_DOMAIN> when that is set, else no-reply@localhost Sender address. CANVAS_DROP_SMTP_HOST (unset) SMTP server host (driver smtp ). Not checked at boot; without it every email is skipped. CANVAS_DROP_SMTP_PORT 587 587 = STARTTLS, 465 = implicit TLS. CANVAS_DROP_SMTP_USER / CANVAS_DROP_SMTP_PASS (unset) Omit both for an IP-allowlisted relay; auth is sent only when both are present. CANVAS_DROP_SMTP_SECURE false boolean. true for implicit TLS (port 465). CANVAS_DROP_MAILGUN_API_KEY (unset) Mailgun HTTP API key (driver mailgun ). CANVAS_DROP_MAILGUN_DOMAIN (unset) e.g. mg.example.com . CANVAS_DROP_MAILGUN_BASE_URL https://api.mailgun.net Use https://api.eu.mailgun.net for EU accounts. Sign-in permits and access emails These are DB-managed admin settings with no env var, under Admin → Configuration, layered on top of the mailer above. They govern the auth-delegated add-person flow : personal-team adds, canvas Add person, and admin sign-in permits. Setting Default Notes email.invitesEnabled false Master switch. When off, grants still happen but no sign-in or access email is sent. Also needs a driver that can send (see above). email.notifyOnAddUser true Email a person when an admin permits their email to sign in. email.notifyOnCanvasAdd true Email an existing user when added to a canvas's people-and-teams list. email.notifyOnCanvasInvite true Email an existing user on an individual one-canvas access action. invites.allowMemberNewEmails false Let a non-admin member add a brand-new external email. Off = only admins can permit new emails; members can add people who can already sign in. invites.maxPerActorPerHour 20 Add-person actions per actor per hour (admins get a higher allowance). invites.pendingCap 50 Max unconsumed pending sign-ins one actor may hold. access.publicLinksEnabled true Instance-wide switch for the Public link rung. Off denies new public-link publishes and returns existing public-link canvases to Restricted . Adding a brand-new person to a team or canvas sends them the sign-in invitation, gated only by the master switch and the mailer; existing team members are not re-notified. Admin → Configuration also holds the runtime quotas for the KV and files primitives, with no env var: quota.kv.keys.shared (default 10000 shared keys per canvas), quota.kv.keys.user ( 1000 per-user keys), quota.files.bytes.file ( 26214400 , 25 MB per file), and quota.files.bytes.canvas ( 1073741824 , 1 GB per canvas). Email templates for each message are admin-editable (subject, HTML, text) under Admin → Configuration → Email templates, with {{variable}} substitution from an allow-list ( name , recipientEmail , inviterName , canvasTitle , teamName , link , role , accessLabel , personEmail , reason , plus the legacy instanceName , orgName , orgContext ) and a reset to the latest default. Values are HTML-escaped in the HTML body; an unknown variable renders empty. Logging and error tracking Structured logs go to stdout via pino: no app-side files, rotation, or shipping. /healthz is excluded from request logging. Variable Default Notes LOG_LEVEL info fatal | error | warn | info | debug | trace . LOG_FORMAT json when NODE_ENV=production , else pretty json | pretty . pretty runs pino-pretty in-process; use it for local dev only. CANVAS_DROP_SENTRY_DSN (unset) Accepted and shown in Admin → Configuration, but the server ships no Sentry client today: setting it sends nothing. Error tracking is off. Tooling-only variables A few CANVAS_DROP_* names in the repo are read by scripts and tests, not by the server, and are not part of the config schema: Variable Read by Purpose CANVAS_DROP_DEV_SEED pnpm dev 0 skips the sample-canvas seed. CANVAS_DROP_DASHBOARD_PORT the dashboard's Vite dev server Dev port (default 5173 ). CANVAS_DROP_DASHBOARD_URL pnpm docs:screenshots , pnpm skins:shot Dashboard base for Playwright captures (default http://localhost:5173 ). CANVAS_DROP_DB (in tests) pnpm test:sqlite , pnpm test:pg When set, the suite runs only that dialect; unset runs both in-process. CANVAS_DROP_TEST_* the test runner and CI Run isolation ( _RUN_ID , _MAX_WORKERS , _REGISTRY_DIR ), the real Postgres and MinIO smoke tests ( _DATABASE_URL , _S3_* ), and the opt-in Chromium capture test ( _SCREENSHOTS=1 ). All examples use placeholder values. Never commit real secrets; set them in your deployment environment (systemd, container env, secrets manager)."
  },
  {
    "path": "self-hosting/security-model",
    "section": "Self-hosting",
    "title": "Security model",
    "html": "<h1 id=\"security-model\">Security model</h1>\n<p>This page tells you, as the operator, where the trust boundary of a canvas-drop\ninstance is, which five guarantees hold inside it, and which config choices keep\nthem intact. Read it before you put an instance in front of colleagues.</p>\n<p>canvas-drop hosts arbitrary, often AI-generated, web artifacts for a trusted\norganization. Everyone who reaches a canvas has already passed your sign-in and the\nemail allowlist. It is not built to defend against the hostile internet. Inside that\nboundary it holds five hard invariants; beyond them it stays open and permissive,\nbecause the product is meant to be frictionless among colleagues.</p>\n<h2 id=\"the-two-settings-that-matter-most\">The two settings that matter most</h2>\n<table>\n<thead>\n<tr>\n<th>Env var</th>\n<th>Production choice</th>\n<th>Why it matters</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_AUTH_MODE</code></td>\n<td><code>proxy</code> (or <code>oidc</code> when nothing fronts the app)</td>\n<td>Decides how identity is established (invariant 1). Default <code>dev</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_URL_MODE</code></td>\n<td><code>subdomain</code></td>\n<td>Gives each canvas its own origin so the browser isolates canvases from each other (invariant 4).</td>\n</tr>\n</tbody></table>\n<p>A production baseline behind an identity-aware proxy that forwards a signed JWT:</p>\n<pre><code class=\"hljs language-bash\">CANVAS_DROP_AUTH_MODE=proxy\nCANVAS_DROP_URL_MODE=subdomain\nCANVAS_DROP_BASE_URL=https://canvases.example.com\nCANVAS_DROP_ALLOWED_EMAIL_DOMAINS=example.com\nCANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL=https://&lt;your-proxy&gt;/&lt;its-jwks-path&gt;\nCANVAS_DROP_AUTH_PROXY_JWT_ISSUER=https://&lt;your-proxy&gt;\nCANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE=&lt;audience-your-proxy-sets&gt;\nCANVAS_DROP_SESSION_SECRET=&lt;32 or more random characters&gt;</code></pre>\n<p>Without a proxy, let the app own login instead:</p>\n<pre><code class=\"hljs language-bash\">CANVAS_DROP_AUTH_MODE=oidc\nCANVAS_DROP_URL_MODE=subdomain\nCANVAS_DROP_BASE_URL=https://canvases.example.com\nCANVAS_DROP_ALLOWED_EMAIL_DOMAINS=example.com\nCANVAS_DROP_OIDC_ISSUER=https://&lt;your-openid-provider&gt;\nCANVAS_DROP_OIDC_CLIENT_ID=&lt;client <span class=\"hljs-built_in\">id</span>&gt;\nCANVAS_DROP_OIDC_CLIENT_SECRET=&lt;client secret&gt;\nCANVAS_DROP_SESSION_SECRET=&lt;32 or more random characters&gt;</code></pre>\n<p>Both are config swaps, never code changes. Config is validated at boot, and the\nprocess refuses to start on an unsafe combination:</p>\n<table>\n<thead>\n<tr>\n<th>Mode</th>\n<th>Refuses to start when</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>dev</code></td>\n<td><code>NODE_ENV=production</code></td>\n</tr>\n<tr>\n<td><code>proxy</code></td>\n<td>neither <code>CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL</code> nor <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code> is set; a JWKS URL is set without both <code>CANVAS_DROP_AUTH_PROXY_JWT_ISSUER</code> and <code>CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE</code>; a trusted-IP entry is malformed, IPv6, or <code>/0</code></td>\n</tr>\n<tr>\n<td><code>oidc</code></td>\n<td>any of <code>CANVAS_DROP_OIDC_ISSUER</code>, <code>CANVAS_DROP_OIDC_CLIENT_ID</code>, <code>CANVAS_DROP_OIDC_CLIENT_SECRET</code> is missing</td>\n</tr>\n<tr>\n<td><code>proxy</code> or <code>oidc</code></td>\n<td><code>CANVAS_DROP_ALLOWED_EMAIL_DOMAINS</code> is empty; <code>CANVAS_DROP_SESSION_SECRET</code> is shorter than 32 characters; <code>CANVAS_DROP_URL_MODE=path</code> without <code>CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true</code></td>\n</tr>\n</tbody></table>\n<p>See <a href=\"/docs/self-hosting/configuration\">Configuration</a> for every variable and\n<a href=\"/docs/self-hosting/deploy\">Deploy</a> for the proxy layout.</p>\n<h2 id=\"the-trust-boundary\">The trust boundary</h2>\n<p>A request becomes a <em>member</em> only after the auth gateway resolves an identity\nserver-side, checks the email allowlist (the <code>CANVAS_DROP_ALLOWED_EMAIL_DOMAINS</code>\ndomains, or an individual sign-in permit an admin added), maps it to a user, and\nrejects blocked users. The gateway runs on every request; nothing is cached between\nrequests. The only way past it without an identity is the public-link carve-out: an\nactive, unexpired <code>public_link</code> canvas whose owner may still publish publicly, while\nthe instance switch is on. It grants an anonymous principal for that one canvas and\nnothing else; a password on the canvas is still enforced by the gate.</p>\n<pre class=\"mermaid\">flowchart TD\n    Req([Incoming request]) --&gt; Carve{Active public_link canvas?}\n    Carve --&gt;|yes, no identity| Anon[Anonymous principal]\n    Carve --&gt;|otherwise| Gate{Auth gateway resolves identity}\n    Gate --&gt;|none| Unauth[\"oidc: 302 to login. proxy, dev: 401\"]\n    Gate --&gt;|identity| Allow{Email allowed?}\n    Allow --&gt;|no| Unauth\n    Allow --&gt;|yes| Blocked{User blocked?}\n    Blocked --&gt;|yes| Forbidden[403]\n    Blocked --&gt;|no| Member([Member principal + live org membership])\n    Member --&gt; Access{Canvas access decision, per request}\n    Anon --&gt; Access\n    Access --&gt;|owner or editor| Full[Canvas + primitives]\n    Access --&gt;|rung admits| PwGate{Password set?}\n    PwGate --&gt;|no| Full\n    PwGate --&gt;|yes| Gate2[Password gate] --&gt; Full\n    Access --&gt;|public_link, not owner or editor| Static[Static files only]\n    Access --&gt;|no route in| NotFound[404]</pre>\n<p>What each denial looks like, so you can read your logs and support tickets:</p>\n<table>\n<thead>\n<tr>\n<th>Situation</th>\n<th>Response</th>\n<th>Audit row</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>No identity</td>\n<td><code>oidc</code>: <code>302</code> to <code>/auth/login?returnTo=</code>; <code>proxy</code> and <code>dev</code>: <code>401 {\"error\":\"unauthorized\"}</code></td>\n<td><code>auth_denied</code> / <code>no_identity</code></td>\n</tr>\n<tr>\n<td>Email not on the domain list or the permit list</td>\n<td>same as no identity</td>\n<td><code>auth_denied</code> / <code>domain_not_allowed</code></td>\n</tr>\n<tr>\n<td>User blocked by an admin</td>\n<td><code>403 {\"error\":\"forbidden\"}</code></td>\n<td><code>auth_denied</code> / <code>blocked</code></td>\n</tr>\n<tr>\n<td>Signed in, but no route into the canvas</td>\n<td><code>404 {\"error\":\"not_found\"}</code>, never a \"forbidden\" that confirms the canvas exists</td>\n<td>none (the gateway already logged <code>auth_ok</code>)</td>\n</tr>\n<tr>\n<td>Canvas disabled by an admin</td>\n<td><code>403</code> disabled page on the canvas URL, shown to the owner, editors, and admins alike; the runtime API answers <code>403 {\"code\":\"DISABLED\"}</code>, management writes <code>409 {\"code\":\"DISABLED\"}</code></td>\n<td>none</td>\n</tr>\n<tr>\n<td>Canvas archived or deleted</td>\n<td><code>404</code>, opaque even to the owner</td>\n<td>none</td>\n</tr>\n</tbody></table>\n<p>If the allowlist lookup itself fails (a database error), the gateway denies. It fails\nclosed, never open.</p>\n<p>Pick the strategy with <code>CANVAS_DROP_AUTH_MODE</code>:</p>\n<ul>\n<li><code>dev</code>: auto-logs-in a fixed local user (<code>CANVAS_DROP_DEV_USER_EMAIL</code>, default\n<code>dev@example.com</code>) with no verification, and makes that user the bootstrap admin.\nLocalhost only. Refused at boot when <code>NODE_ENV=production</code>.</li>\n<li><code>proxy</code>: an identity-aware proxy in front of the app asserts identity. The app is\nsessionless; the proxy owns the session. The recommended production profile.</li>\n<li><code>oidc</code>: the app runs the OpenID Connect Authorization Code + PKCE flow itself and owns\nthe session cookie. The built-in path when you do not run a proxy.</li>\n</ul>\n<h2 id=\"the-five-hard-invariants\">The five hard invariants</h2>\n<p>These are the guarantees the platform upholds (<code>BUILD_BRIEF.md</code> §12.0). Each section\nafter this one names the mechanism behind one of them.</p>\n<ol>\n<li><strong>No impersonation.</strong> Identity (<code>me()</code>, write attribution, presence) comes from the\nserver-side auth context, never from anything the client sends.</li>\n<li><strong>No credential or canvas theft.</strong> No user can read another user's session, canvas\nAPI key, or canvas content. Session tokens and API keys are stored only as SHA-256\nhashes; an API key is shown once; a session token rides only in an HttpOnly cookie.</li>\n<li><strong>No unauthorized access.</strong> A canvas is reachable by its owner, by everyone on its\npeople-and-teams list (viewers and editors, people and teams, resolved live on every\nrequest and admitted at every General-access choice), and otherwise only as widely as\nthe owner chose: Restricted (nobody else), Whole org, or Public link. Admins get no special access to\ncanvases they do not own; cross-owner admin power is limited to the dedicated admin\nroutes (list, disable/enable/restore, reassign owner). Anyone without a route in gets an\nopaque <code>404</code>; an editor attempting an owner-only act gets an explicit <code>403 OWNER_ONLY</code>.</li>\n<li><strong>No cross-canvas reach in subdomain mode.</strong> One canvas, its code, its SDK calls, or\nits socket cannot read, write, or act on another canvas's data, files, AI quota, or\nrealtime channels. Path mode has reduced browser isolation and must be opted into.</li>\n<li><strong>Lifecycle is honored instantly.</strong> Revoke, expiry, disable, delete, slug regen, key\nregen, rung lowering, removal from a list, team, or org, ownership moves, and unpublish\ntake effect on the next request, and sharing changes revalidate live realtime\nsockets. Nothing grants access from a cache.</li>\n</ol>\n<h2 id=\"identity-is-always-server-side-invariant-1\">Identity is always server-side (invariant 1)</h2>\n<table>\n<thead>\n<tr>\n<th>Mode</th>\n<th>Identity comes from</th>\n<th>App session</th>\n<th>Unauthenticated request</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>dev</code></td>\n<td>the configured dev user</td>\n<td>cookie, used only by <code>/auth/logout</code></td>\n<td>never happens</td>\n</tr>\n<tr>\n<td><code>proxy</code>, JWT path</td>\n<td>the proxy's signed JWT: signature against the JWKS, <code>iss</code>, <code>aud</code>, <code>exp</code>, and a string <code>email</code> claim</td>\n<td>none</td>\n<td><code>401</code></td>\n</tr>\n<tr>\n<td><code>proxy</code>, trusted-header path</td>\n<td><code>X-Auth-Request-Email</code> (configurable), accepted only from a TCP peer in <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code></td>\n<td>none</td>\n<td><code>401</code></td>\n</tr>\n<tr>\n<td><code>oidc</code></td>\n<td>the app's own session cookie, minted by the OIDC callback</td>\n<td><code>__canvasdrop_session</code>, 14-day rolling expiry</td>\n<td><code>302</code> to <code>/auth/login</code></td>\n</tr>\n</tbody></table>\n<p>Each mode namespaces the stored identity (<code>dev:</code>, <code>proxy:</code>, <code>oidc:</code> prefixes on the\nprovider subject), so switching modes never merges accounts across trust sources.</p>\n<h3 id=\"proxy-mode-one-trust-path-never-two\"><code>proxy</code> mode: one trust path, never two</h3>\n<p>Exactly one trust path is active, selected by whether\n<code>CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL</code> is set. The two do not compose, so a client cannot\nomit the JWT to fall back to the weaker header path.</p>\n<ul>\n<li><strong>JWT / JWKS path (preferred, cryptographic).</strong> With a JWKS URL configured, identity\ncomes only from the token in <code>CANVAS_DROP_AUTH_PROXY_JWT_HEADER</code> (default\n<code>Cf-Access-Jwt-Assertion</code>), verified against the remote JWKS and the configured issuer\nand audience. The email header is never consulted in this mode. A request that carries\nan identity header but no valid JWT resolves to anonymous and is logged as a downgrade\nprobe. <code>CANVAS_DROP_AUTH_PROXY_JWT_ISSUER</code> and <code>CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE</code> are\nrequired when the JWKS URL is set. A JWKS fetch failure is logged as\n<code>proxy JWT verification failed (§12.5)</code>, so you can tell \"bad token\" from \"IdP down\".</li>\n<li><strong>Trusted-header path (only when no JWKS URL is set).</strong> The email header (default\n<code>X-Auth-Request-Email</code>; the optional name header defaults to\n<code>X-Auth-Request-Preferred-Username</code>) is honored only when the request's socket peer\nmatches an entry in <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code>. The check gates on the real TCP\npeer address, never on <code>X-Forwarded-For</code> or any other header. A header from any other\nsource is ignored and logged as <code>ignored identity header from untrusted source (§12.5)</code>.</li>\n</ul>\n<p>Boot guards for <code>proxy</code> mode: the app refuses to start without a JWKS URL or a non-empty\ntrusted-IP list, so an unguarded \"trust any header\" config cannot exist. Every\n<code>CANVAS_DROP_TRUSTED_PROXY_IPS</code> entry is validated: malformed IPv4 is rejected, <code>/0</code> is\nrejected (at boot and again at runtime), and IPv6 entries are rejected; use the JWT path\nfor IPv6 proxies. Two operational rules follow: the app must never be directly\nreachable, only through the proxy; and on the trusted-header path the proxy must\noverwrite (not append) the identity headers so a client cannot smuggle a second value.</p>\n<p>Two IPs, two purposes. The <strong>peer IP</strong> gates identity trust. The <strong>client IP</strong>, read from\n<code>CANVAS_DROP_CLIENT_IP_HEADER</code> or the rightmost untrusted <code>X-Forwarded-For</code> hop and only\nwhen the peer is a trusted proxy, keys login rate limits and audit rows. It is never an\nauth input. That is why <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code> is worth setting in <code>oidc</code> mode\ntoo (for example <code>127.0.0.1</code> for a local reverse proxy), even though header-asserted\nidentity fires only in <code>proxy</code> mode.</p>\n<h3 id=\"oidc-mode-the-app-owns-the-session\"><code>oidc</code> mode: the app owns the session</h3>\n<p>Login uses PKCE (S256), a random <code>state</code>, and <code>prompt=login</code>. The callback rejects a\nmissing or mismatched state, a failed code exchange, a missing <code>email</code> claim, and\n<code>email_verified=false</code>, and only then runs the same allowlist and blocked checks as every\nother request. Every rejection writes an <code>auth_denied</code> audit row with its reason\n(<code>missing_oidc_state</code>, <code>state_mismatch</code>, <code>token_exchange_failed</code>, <code>no_email_claim</code>,\n<code>email_not_verified</code>, <code>email_domain_not_allowed</code>, <code>blocked</code>).</p>\n<p>The session token is 256 bits of randomness; only its SHA-256 hash is stored, so the raw\ntoken never lands in the database. The cookie is HttpOnly always, Secure in production,\n<code>SameSite=Lax</code>, and scoped to <code>.{baseHost}</code> in subdomain mode. <code>/auth/logout</code> revokes the\nsession (audit <code>session_revoke</code>). <code>CANVAS_DROP_SESSION_SECRET</code> must be at least 32\ncharacters outside <code>dev</code> mode; it also signs the password-gate grants below.</p>\n<h3 id=\"agents\">Agents</h3>\n<p>Agents connect over MCP at <code>{base}/mcp</code> with OAuth tokens the instance issues itself\n(audited as <code>mcp_token_issue</code>, <code>mcp_token_revoke</code>, <code>mcp_authorize_ok</code>,\n<code>mcp_authorize_denied</code>). Every tool resolves the caller's user server-side and runs the\nsame owner-or-editor role gate as the dashboard. The keyed <a href=\"/docs/api/deploy-api\">Deploy API</a>\nhas no user at all: its writes are attributed to the canvas owner. See\n<a href=\"/docs/agents/mcp\">MCP server</a>.</p>\n<h2 id=\"access-is-decided-on-every-request-invariant-3\">Access is decided on every request (invariant 3)</h2>\n<p>Per-canvas roles and the access ladder are resolved from the server-side principal on\neach request, never cached on a session or an agent's token. Two roles manage a canvas:</p>\n<ul>\n<li>The <strong>owner</strong>: the single account on the canvas record. Only the owner can delete the\ncanvas, transfer ownership, or switch on AI for added people. An editor who tries gets\n<code>403 {\"code\":\"OWNER_ONLY\"}</code>.</li>\n<li>An <strong>editor</strong>: owner-equivalent for everything else, including settings, sharing, the\ndraft editor, publish, rollback, the deploy key, and adding or removing other editors.\nAn editor is a signed-in member holding a direct editor row or membership of an\neditor-role team. With an org boundary configured (below), the grant holds only while\nthey are currently a member of the canvas's home org; the check runs on every request.\nA guest or anonymous visitor never holds a role.</li>\n</ul>\n<p>A principal with no role sees the management surface as <code>404 {\"error\":\"not_found\"}</code>.\nThe people-and-teams list is consulted at every General-access choice; the choice itself\ndecides who else is admitted:</p>\n<table>\n<thead>\n<tr>\n<th>Who</th>\n<th>Admitted when</th>\n<th>Password and expiry</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>A person on the list</td>\n<td>always — an existing user by id, or a pending email once that exact email has signed in through your configured auth</td>\n<td>both apply</td>\n</tr>\n<tr>\n<td>A member of a team on the list</td>\n<td>always. A personal team admits by membership alone; an org team also requires the viewer to be a current member of that team's org, re-joined live on every request, so a stale team row cannot widen access</td>\n<td>both apply</td>\n</tr>\n<tr>\n<td>Anyone else at Restricted (<code>private</code>, or its legacy aliases <code>specific_people</code> / <code>team</code>)</td>\n<td>never</td>\n<td>—</td>\n</tr>\n<tr>\n<td>Whole org</td>\n<td>any signed-in member. When an org is named (below), members of the canvas's home org only</td>\n<td>both apply</td>\n</tr>\n<tr>\n<td>Public link</td>\n<td>anyone, static files only, while the instance-wide switch is on and the owner still holds the publish-public capability</td>\n<td>both apply; an anonymous visitor reaches the password prompt</td>\n</tr>\n</tbody></table>\n<p>The owner and editors are admitted at every choice, never see the password prompt, and\nare unaffected by expiry. Anyone with no route in gets <code>404</code>. A password is checked with argon2id; a successful attempt sets an HttpOnly grant\ncookie (<code>__canvasdrop_gate</code>), HMAC-signed with <code>CANVAS_DROP_SESSION_SECRET</code> and bound to\nthe canvas id and its password version, so changing the password invalidates every\noutstanding grant at once. In subdomain mode the grant is host-only; in path mode it is\nscoped to the canvas path. Attempts are limited to\n<code>CANVAS_DROP_RATELIMIT_PASSWORD_GATE_PER_MIN</code> (default 5) per user per canvas, or per\nclient IP for an anonymous public visitor; over the limit the gate answers <code>429</code> with\n<code>Retry-After</code>.</p>\n<p><strong>Ownership moves are instant and audited.</strong> The owner can hand a canvas to an existing\neditor who is an org member (<code>POST /api/canvases/{id}/transfer</code>, MCP <code>transfer_canvas</code>).\nThe previous owner stays on as an editor while their account is active and passes the\norg check; if the new owner may not publish publicly, a Public link rung is turned off;\nthe deploy key is unchanged. An admin can <strong>reassign</strong> a canvas whose owner has left\nfrom the admin routes, with a recorded reason. A reassign rotates the deploy key in the\nsame write (the plaintext is discarded; the new owner issues a fresh one) and never gives\nthe acting admin content access. Both write an audit row (<code>canvas_transfer</code>,\n<code>canvas_reassign_owner</code>) and revalidate the canvas's live realtime sockets.</p>\n<p><strong>Public links are doubly gated.</strong> Admin → Configuration → <code>access.publicLinksEnabled</code>\n(default on) is the instance switch; turning it off makes every public-link canvas\nRestricted. Each owner also needs the publish-public capability, granted and revoked per\nuser under Admin → People; revoking it sweeps that owner's public-link canvases back to\nRestricted. Both gates are also re-checked on every request, so a canvas whose owner lost\nthe grant is unreachable even before the sweep lands. A public-link visitor gets files\nonly: every runtime API call is refused with <code>403 {\"code\":\"STATIC_ONLY\"}</code>, including for\nsigned-in members who are not the owner or an editor. In <code>proxy</code> mode a public link\nworks only for requests your proxy lets reach the app.</p>\n<p><strong>Admins have no back door.</strong> For a canvas they do not own, an admin is an ordinary\nmember: a Restricted canvas returns <code>404</code>, a password prompts them, and they cannot open\nthe editor or change settings. Cross-owner admin power lives on the dedicated admin\nroutes: the all-canvases list, disable / enable / restore, reassigning the owner when\nsomeone leaves, and gallery featuring. It never extends to canvas content, the runtime\nAPI, or realtime. One recorded exception: the authoring API (<code>CANVAS_DROP_AUTHORING</code>,\ndefault <code>off</code>) still admits an admin to update or revoke an authored share it does not\nown.</p>\n<p>The retired guest magic-link flow is gone. Old <code>/guest/{token}</code> links return an\ninvalid-link page, never set a cookie, and never consume a token; the sharing paths\nbelow record pending access against your configured auth instead.</p>\n<h2 id=\"the-org-boundary-member-vs-guest\">The org boundary (member vs guest)</h2>\n<p>By default any signed-in user is treated as one org, so <code>whole_org</code> means \"anyone who\npassed sign-in\". Naming an org with <code>CANVAS_DROP_ORG_NAME</code> draws a member-vs-guest\nboundary:</p>\n<ul>\n<li>A signed-in user whose <strong>verified email domain</strong> is in <code>CANVAS_DROP_ORG_DOMAINS</code>\n(default: the allowed-email domains) is a <strong>member</strong>. Everyone else who can sign in,\nsuch as an allowlisted contractor or an admin on another domain, is a <strong>guest</strong>.\nMembership is independent of the sign-in permit list and of <code>CANVAS_DROP_ADMIN_EMAILS</code>;\nthose grant sign-in, not membership.</li>\n<li>Each canvas has a <strong>home org</strong>, set once at creation: a member picks Personal or the\norg; a guest only gets Personal. <code>whole_org</code> then means \"members of the canvas's home\norg\". A guest cannot see it, and a Personal <code>whole_org</code> canvas is an explicit deny to\neveryone but its owner and editors.</li>\n<li>Membership is derived server-side from the identity the gateway resolved. A client can\nnever assert which org it belongs to. Admin is orthogonal: it grants no membership and\nno content bypass.</li>\n</ul>\n<p>The boundary is inert until an org is named, so it is an opt-in tightening. Turning it on\nfor an existing instance is a one-time, dry-run-first cutover; see\n<a href=\"/docs/self-hosting/configuration#tenancy-the-org-boundary-optional\">Configuration → Tenancy</a>\nand the <code>docs/tenancy.md</code> runbook in the repo.</p>\n<h2 id=\"adds-are-auth-delegated-no-app-owned-credentials\">Adds are auth-delegated (no app-owned credentials)</h2>\n<p>When someone adds a person who has no account yet, whether to a canvas, to a personal\nteam, or under Admin → People → Sign-in permits, canvas-drop records <strong>pending access</strong>,\nnot a login. There is no app-owned magic-link account and no app-stored password. The\ngrant materializes the first time that email authenticates through your configured auth\n(<code>proxy</code>, <code>oidc</code>, or <code>dev</code>); the identity provider stays the only authority, so there is\nnothing to take over. The verified login email is the match key, and pending access\nnever grants anything on its own.</p>\n<p>Who may permit a <strong>brand-new email</strong> to sign in is gated:</p>\n<ul>\n<li>In <code>proxy</code> mode, an email that cannot already sign in (its domain is not allowed and\nit holds no permit) must be admitted at the proxy first. canvas-drop cannot widen an\nupstream IAP, so the add is refused with <code>403 {\"code\":\"AUTH_ADMISSION_REQUIRED\"}</code> until\nthen.</li>\n<li>An <strong>admin</strong> can add a permit under Sign-in permits.</li>\n<li>A <strong>member</strong> can only when the admin setting <code>invites.allowMemberNewEmails</code> is on (off\nby default), or when the email already authenticates. Otherwise the add is refused with\n<code>403 {\"code\":\"NOT_PERMITTED\"}</code>; a member cannot widen who may sign in to your instance.</li>\n</ul>\n<p>Add volume is bounded per actor with <code>invites.maxPerActorPerHour</code> (default 20) and\n<code>invites.pendingCap</code> (default 50). Both are DB-managed admin settings; see\n<a href=\"/docs/self-hosting/configuration#sign-in-permits-and-access-emails\">Sign-in permits and access emails</a>.</p>\n<h2 id=\"no-secrets-in-the-browser-invariant-2\">No secrets in the browser (invariant 2)</h2>\n<p>The AI provider key (<code>CANVAS_DROP_AI_API_KEY</code>), every canvas API key, and every Connection\nprotected header are server-side only. The browser SDK rides the viewer's session, so a\ncanvas calls the primitives (KV, files, AI, identity, realtime, and admin-granted\nConnections) with no secret in its code. The deploy engine lints every upload and warns\nwhen a file appears to contain a canvas API key.</p>\n<p>Deploy keys are <code>cd_</code> Bearer secrets: 32 random bytes, stored only as a SHA-256 hash,\nshown once at creation or regeneration. A key works only on its own canvas (<code>403</code> on any\nother), only while that canvas is active (<code>401</code> the moment it is archived or disabled),\nand dies the moment it is regenerated. Cloning a canvas mints a fresh key; it never\ncopies the source's. The key is per canvas, not per person: removing an editor does not\ninvalidate a key they copied, so regenerate it when someone leaves (the dashboard offers\nthis). See the <a href=\"/docs/api/deploy-api\">Deploy API</a>.</p>\n<h3 id=\"connections-bounded-outbound-authority\">Connections: bounded outbound authority</h3>\n<p>A Connection is an admin-created, reusable outbound profile, not a general proxy or a\nLambda runtime. It names one exact HTTPS DNS origin, a set of allowed HTTP methods, and\noptional protected headers such as <code>Authorization</code> or a controlled <code>User-Agent</code>. An admin\ngrants that profile to individual canvases. The canvas supplies only a root-relative path,\nquery, body, and non-protected request headers; the server adds protected headers last.\nAnonymous Public-link viewers cannot call it because the whole runtime API stays\nstatic-only for them.</p>\n<p>Protected header values are encrypted with AES-256-GCM under\n<code>CANVAS_DROP_CONNECTIONS_ENCRYPTION_KEY</code>, authenticated to the profile id, and are\nwrite-only in the admin API and UI. Logs, usage events, MCP results, and error bodies expose\nneither the values nor requested paths/query strings. Backups contain ciphertext but not\nthe root key; preserve that key separately as described in the operations runbook.</p>\n<p>Outbound requests resolve DNS on every hop and pin the selected public address into the\nsocket while retaining the hostname for TLS SNI and certificate validation. Literal IPs,\ncredentials in the URL, private/link-local/loopback/reserved IPv4 and IPv6, mixed public\nand private DNS answers, redirects to a different origin, and redirects to disallowed\nmethods are rejected. Request and response sizes, total duration, concurrency, and rate\nare bounded. As defense in depth, restrict the app container's network egress to the APIs\nyour profiles actually need; application-layer SSRF checks should not be the only barrier\nbetween a canvas and your internal network. See <a href=\"/docs/sdk/connections\">Connections</a>.</p>\n<h2 id=\"path-mode-vs-subdomain-mode-invariant-4\">Path mode vs subdomain mode (invariant 4)</h2>\n<p><code>CANVAS_DROP_URL_MODE</code> is the most consequential deployment choice.</p>\n<ul>\n<li><strong>Path mode</strong> (<code>{base}/c/{slug}/</code>): every canvas shares one origin with the others and\nwith the dashboard, so the browser does not isolate them. A malicious or compromised\ncanvas could make same-origin requests against other canvases' client-side state. Fine\nfor localhost and single-user hosting. Running <code>proxy</code> or <code>oidc</code> auth in path mode is\nmulti-user, and the app refuses to boot unless you set\n<code>CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true</code> to accept the tradeoff.</li>\n<li><strong>Subdomain mode</strong> (<code>{slug}.canvases.example.com</code>): each canvas is its own origin, so\nthe browser isolates them and invariant 4 holds in full. Needs a wildcard DNS record\nand wildcard TLS at the proxy, and a non-localhost <code>CANVAS_DROP_BASE_URL</code>.</li>\n</ul>\n<p>Server-side isolation holds in both modes: blobs, KV, files, AI usage, realtime channels,\nand Connection grants are keyed by canvas id, and management and admin writes require a same-origin\nrequest: <code>Sec-Fetch-Site: same-origin</code> (or <code>none</code>), else an <code>Origin</code> whose host matches\n<code>CANVAS_DROP_BASE_URL</code>; anything else gets <code>403 {\"error\":\"cross_origin_forbidden\"}</code>.\nSubdomain mode adds the browser's origin boundary on top. If you do not run an\nidentity-aware proxy, run subdomain mode with <code>oidc</code> so you keep that boundary without\nstanding up a proxy.</p>\n<h2 id=\"lifecycle-changes-land-on-the-next-request-invariant-5\">Lifecycle changes land on the next request (invariant 5)</h2>\n<p>Nothing about access is cached. Concretely:</p>\n<ul>\n<li>Roles and rungs are resolved per request, so removing an editor, lowering a rung,\nor removing someone from a list, team, or org denies them on their very next request.\nShare, people-list, ownership, password, and unpublish changes also revalidate that\ncanvas's live realtime sockets right away, and sockets that lost access are dropped.</li>\n<li>A heartbeat re-authorizes every live socket once a minute as a backstop, so a share\nexpiry passing, or an admin block or delete (which fire no mutation hook), drops the\nsocket within one tick.</li>\n<li>A blocked user is refused at the gateway on the next request, whatever session or\nagent token they hold.</li>\n<li>An archived, disabled, or deleted canvas rejects its own deploy key with <code>401</code>.</li>\n<li>Regenerating a slug invalidates the old URL and drops every live socket so clients\nreconnect under the new one. Regenerating a key invalidates the old key at once.</li>\n<li>Changing a canvas password invalidates every outstanding gate grant.</li>\n<li>Turning off public links, or revoking one owner's publish-public capability, makes\nthe affected canvases Restricted immediately.</li>\n</ul>\n<h2 id=\"reducing-canvas-xss-blast-radius\">Reducing canvas XSS blast radius</h2>\n<p>Subdomain mode contains the blast radius of a compromised canvas to that canvas's\norigin; Restricted-by-default access limits who can be exposed to it. Every response carries\n<code>X-Content-Type-Options: nosniff</code>, <code>Referrer-Policy: same-origin</code>, and\n<code>Cross-Origin-Opener-Policy: same-origin</code>. Canvas content adds a\n<code>Content-Security-Policy</code> of <code>frame-ancestors 'self' {base origin}</code> in subdomain mode\n(<code>frame-ancestors 'self'</code> in path mode), so the dashboard can preview a canvas and no\nother canvas can frame it. The dashboard itself runs under a strict CSP (<code>script-src 'self'</code>, <code>frame-ancestors 'none'</code>). Tell canvas authors to prefer <code>textContent</code> over\n<code>innerHTML</code>.</p>\n<h2 id=\"what-gets-audited\">What gets audited</h2>\n<p>The audit log records <code>auth_ok</code> and <code>auth_denied</code> (with the reason) on every gateway\ndecision, <code>session_create</code> and <code>session_revoke</code> in <code>oidc</code> mode, and every OIDC callback\nrejection. Canvas actions are attributed to the acting user or agent: <code>deploy</code>,\n<code>rollback</code>, <code>publish</code>, <code>canvas_unpublish</code>, <code>share_change</code>, <code>password_change</code>,\n<code>password_attempt</code>, <code>slug_regen</code>, <code>key_regen</code>, <code>canvas_transfer</code>, among others. Admin\nactions are audited by name: <code>canvas_disable</code>, <code>canvas_enable</code>, <code>canvas_restore</code>,\n<code>canvas_reassign_owner</code>, <code>canvas_feature</code>, <code>user_block</code>, <code>user_unblock</code>, <code>user_promote</code>,\n<code>user_demote</code>, <code>user_grant_public</code>, <code>user_revoke_public</code>, <code>allowed_email_add</code>,\n<code>allowed_email_remove</code>, <code>admin_settings_update</code>, and the Connection profile/grant actions.\nRows carry the client IP described\nabove. Login attempts are limited to <code>CANVAS_DROP_RATELIMIT_LOGIN_PER_MIN</code> (default 10)\nper client IP; see <a href=\"/docs/self-hosting/configuration#rate-limiting\">Rate limiting</a>.</p>\n<h2 id=\"no-telemetry\">No telemetry</h2>\n<p>canvas-drop does not phone home. There is no analytics, usage reporting, or third-party\nbeacon in the product. Nothing leaves your instance unless you configure an outbound\nintegration yourself, such as an OIDC provider, an AI provider, a mail transport, or an\nadmin-granted Connection.</p>\n",
    "headings": [
      {
        "id": "the-two-settings-that-matter-most",
        "text": "The two settings that matter most",
        "level": 2
      },
      {
        "id": "the-trust-boundary",
        "text": "The trust boundary",
        "level": 2
      },
      {
        "id": "the-five-hard-invariants",
        "text": "The five hard invariants",
        "level": 2
      },
      {
        "id": "identity-is-always-server-side-invariant-1",
        "text": "Identity is always server-side (invariant 1)",
        "level": 2
      },
      {
        "id": "proxy-mode-one-trust-path-never-two",
        "text": "proxy mode: one trust path, never two",
        "level": 3
      },
      {
        "id": "oidc-mode-the-app-owns-the-session",
        "text": "oidc mode: the app owns the session",
        "level": 3
      },
      {
        "id": "agents",
        "text": "Agents",
        "level": 3
      },
      {
        "id": "access-is-decided-on-every-request-invariant-3",
        "text": "Access is decided on every request (invariant 3)",
        "level": 2
      },
      {
        "id": "the-org-boundary-member-vs-guest",
        "text": "The org boundary (member vs guest)",
        "level": 2
      },
      {
        "id": "adds-are-auth-delegated-no-app-owned-credentials",
        "text": "Adds are auth-delegated (no app-owned credentials)",
        "level": 2
      },
      {
        "id": "no-secrets-in-the-browser-invariant-2",
        "text": "No secrets in the browser (invariant 2)",
        "level": 2
      },
      {
        "id": "connections-bounded-outbound-authority",
        "text": "Connections: bounded outbound authority",
        "level": 3
      },
      {
        "id": "path-mode-vs-subdomain-mode-invariant-4",
        "text": "Path mode vs subdomain mode (invariant 4)",
        "level": 2
      },
      {
        "id": "lifecycle-changes-land-on-the-next-request-invariant-5",
        "text": "Lifecycle changes land on the next request (invariant 5)",
        "level": 2
      },
      {
        "id": "reducing-canvas-xss-blast-radius",
        "text": "Reducing canvas XSS blast radius",
        "level": 2
      },
      {
        "id": "what-gets-audited",
        "text": "What gets audited",
        "level": 2
      },
      {
        "id": "no-telemetry",
        "text": "No telemetry",
        "level": 2
      }
    ],
    "text": "Security model This page tells you, as the operator, where the trust boundary of a canvas-drop instance is, which five guarantees hold inside it, and which config choices keep them intact. Read it before you put an instance in front of colleagues. canvas-drop hosts arbitrary, often AI-generated, web artifacts for a trusted organization. Everyone who reaches a canvas has already passed your sign-in and the email allowlist. It is not built to defend against the hostile internet. Inside that boundary it holds five hard invariants; beyond them it stays open and permissive, because the product is meant to be frictionless among colleagues. The two settings that matter most Env var Production choice Why it matters CANVAS_DROP_AUTH_MODE proxy (or oidc when nothing fronts the app) Decides how identity is established (invariant 1). Default dev . CANVAS_DROP_URL_MODE subdomain Gives each canvas its own origin so the browser isolates canvases from each other (invariant 4). A production baseline behind an identity-aware proxy that forwards a signed JWT: CANVAS_DROP_AUTH_MODE=proxy CANVAS_DROP_URL_MODE=subdomain CANVAS_DROP_BASE_URL=https://canvases.example.com CANVAS_DROP_ALLOWED_EMAIL_DOMAINS=example.com CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL=https://<your-proxy>/<its-jwks-path> CANVAS_DROP_AUTH_PROXY_JWT_ISSUER=https://<your-proxy> CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE=<audience-your-proxy-sets> CANVAS_DROP_SESSION_SECRET=<32 or more random characters> Without a proxy, let the app own login instead: CANVAS_DROP_AUTH_MODE=oidc CANVAS_DROP_URL_MODE=subdomain CANVAS_DROP_BASE_URL=https://canvases.example.com CANVAS_DROP_ALLOWED_EMAIL_DOMAINS=example.com CANVAS_DROP_OIDC_ISSUER=https://<your-openid-provider> CANVAS_DROP_OIDC_CLIENT_ID=<client id > CANVAS_DROP_OIDC_CLIENT_SECRET=<client secret> CANVAS_DROP_SESSION_SECRET=<32 or more random characters> Both are config swaps, never code changes. Config is validated at boot, and the process refuses to start on an unsafe combination: Mode Refuses to start when dev NODE_ENV=production proxy neither CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL nor CANVAS_DROP_TRUSTED_PROXY_IPS is set; a JWKS URL is set without both CANVAS_DROP_AUTH_PROXY_JWT_ISSUER and CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE ; a trusted-IP entry is malformed, IPv6, or /0 oidc any of CANVAS_DROP_OIDC_ISSUER , CANVAS_DROP_OIDC_CLIENT_ID , CANVAS_DROP_OIDC_CLIENT_SECRET is missing proxy or oidc CANVAS_DROP_ALLOWED_EMAIL_DOMAINS is empty; CANVAS_DROP_SESSION_SECRET is shorter than 32 characters; CANVAS_DROP_URL_MODE=path without CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true See Configuration for every variable and Deploy for the proxy layout. The trust boundary A request becomes a member only after the auth gateway resolves an identity server-side, checks the email allowlist (the CANVAS_DROP_ALLOWED_EMAIL_DOMAINS domains, or an individual sign-in permit an admin added), maps it to a user, and rejects blocked users. The gateway runs on every request; nothing is cached between requests. The only way past it without an identity is the public-link carve-out: an active, unexpired public_link canvas whose owner may still publish publicly, while the instance switch is on. It grants an anonymous principal for that one canvas and nothing else; a password on the canvas is still enforced by the gate. flowchart TD Req([Incoming request]) --> Carve{Active public_link canvas?} Carve -->|yes, no identity| Anon[Anonymous principal] Carve -->|otherwise| Gate{Auth gateway resolves identity} Gate -->|none| Unauth[\"oidc: 302 to login. proxy, dev: 401\"] Gate -->|identity| Allow{Email allowed?} Allow -->|no| Unauth Allow -->|yes| Blocked{User blocked?} Blocked -->|yes| Forbidden[403] Blocked -->|no| Member([Member principal + live org membership]) Member --> Access{Canvas access decision, per request} Anon --> Access Access -->|owner or editor| Full[Canvas + primitives] Access -->|rung admits| PwGate{Password set?} PwGate -->|no| Full PwGate -->|yes| Gate2[Password gate] --> Full Access -->|public_link, not owner or editor| Static[Static files only] Access -->|no route in| NotFound[404] What each denial looks like, so you can read your logs and support tickets: Situation Response Audit row No identity oidc : 302 to /auth/login?returnTo= ; proxy and dev : 401 {\"error\":\"unauthorized\"} auth_denied / no_identity Email not on the domain list or the permit list same as no identity auth_denied / domain_not_allowed User blocked by an admin 403 {\"error\":\"forbidden\"} auth_denied / blocked Signed in, but no route into the canvas 404 {\"error\":\"not_found\"} , never a \"forbidden\" that confirms the canvas exists none (the gateway already logged auth_ok ) Canvas disabled by an admin 403 disabled page on the canvas URL, shown to the owner, editors, and admins alike; the runtime API answers 403 {\"code\":\"DISABLED\"} , management writes 409 {\"code\":\"DISABLED\"} none Canvas archived or deleted 404 , opaque even to the owner none If the allowlist lookup itself fails (a database error), the gateway denies. It fails closed, never open. Pick the strategy with CANVAS_DROP_AUTH_MODE : dev : auto-logs-in a fixed local user ( CANVAS_DROP_DEV_USER_EMAIL , default dev@example.com ) with no verification, and makes that user the bootstrap admin. Localhost only. Refused at boot when NODE_ENV=production . proxy : an identity-aware proxy in front of the app asserts identity. The app is sessionless; the proxy owns the session. The recommended production profile. oidc : the app runs the OpenID Connect Authorization Code + PKCE flow itself and owns the session cookie. The built-in path when you do not run a proxy. The five hard invariants These are the guarantees the platform upholds ( BUILD_BRIEF.md §12.0). Each section after this one names the mechanism behind one of them. No impersonation. Identity ( me() , write attribution, presence) comes from the server-side auth context, never from anything the client sends. No credential or canvas theft. No user can read another user's session, canvas API key, or canvas content. Session tokens and API keys are stored only as SHA-256 hashes; an API key is shown once; a session token rides only in an HttpOnly cookie. No unauthorized access. A canvas is reachable by its owner, by everyone on its people-and-teams list (viewers and editors, people and teams, resolved live on every request and admitted at every General-access choice), and otherwise only as widely as the owner chose: Restricted (nobody else), Whole org, or Public link. Admins get no special access to canvases they do not own; cross-owner admin power is limited to the dedicated admin routes (list, disable/enable/restore, reassign owner). Anyone without a route in gets an opaque 404 ; an editor attempting an owner-only act gets an explicit 403 OWNER_ONLY . No cross-canvas reach in subdomain mode. One canvas, its code, its SDK calls, or its socket cannot read, write, or act on another canvas's data, files, AI quota, or realtime channels. Path mode has reduced browser isolation and must be opted into. Lifecycle is honored instantly. Revoke, expiry, disable, delete, slug regen, key regen, rung lowering, removal from a list, team, or org, ownership moves, and unpublish take effect on the next request, and sharing changes revalidate live realtime sockets. Nothing grants access from a cache. Identity is always server-side (invariant 1) Mode Identity comes from App session Unauthenticated request dev the configured dev user cookie, used only by /auth/logout never happens proxy , JWT path the proxy's signed JWT: signature against the JWKS, iss , aud , exp , and a string email claim none 401 proxy , trusted-header path X-Auth-Request-Email (configurable), accepted only from a TCP peer in CANVAS_DROP_TRUSTED_PROXY_IPS none 401 oidc the app's own session cookie, minted by the OIDC callback __canvasdrop_session , 14-day rolling expiry 302 to /auth/login Each mode namespaces the stored identity ( dev: , proxy: , oidc: prefixes on the provider subject), so switching modes never merges accounts across trust sources. proxy mode: one trust path, never two Exactly one trust path is active, selected by whether CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL is set. The two do not compose, so a client cannot omit the JWT to fall back to the weaker header path. JWT / JWKS path (preferred, cryptographic). With a JWKS URL configured, identity comes only from the token in CANVAS_DROP_AUTH_PROXY_JWT_HEADER (default Cf-Access-Jwt-Assertion ), verified against the remote JWKS and the configured issuer and audience. The email header is never consulted in this mode. A request that carries an identity header but no valid JWT resolves to anonymous and is logged as a downgrade probe. CANVAS_DROP_AUTH_PROXY_JWT_ISSUER and CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE are required when the JWKS URL is set. A JWKS fetch failure is logged as proxy JWT verification failed (§12.5) , so you can tell \"bad token\" from \"IdP down\". Trusted-header path (only when no JWKS URL is set). The email header (default X-Auth-Request-Email ; the optional name header defaults to X-Auth-Request-Preferred-Username ) is honored only when the request's socket peer matches an entry in CANVAS_DROP_TRUSTED_PROXY_IPS . The check gates on the real TCP peer address, never on X-Forwarded-For or any other header. A header from any other source is ignored and logged as ignored identity header from untrusted source (§12.5) . Boot guards for proxy mode: the app refuses to start without a JWKS URL or a non-empty trusted-IP list, so an unguarded \"trust any header\" config cannot exist. Every CANVAS_DROP_TRUSTED_PROXY_IPS entry is validated: malformed IPv4 is rejected, /0 is rejected (at boot and again at runtime), and IPv6 entries are rejected; use the JWT path for IPv6 proxies. Two operational rules follow: the app must never be directly reachable, only through the proxy; and on the trusted-header path the proxy must overwrite (not append) the identity headers so a client cannot smuggle a second value. Two IPs, two purposes. The peer IP gates identity trust. The client IP , read from CANVAS_DROP_CLIENT_IP_HEADER or the rightmost untrusted X-Forwarded-For hop and only when the peer is a trusted proxy, keys login rate limits and audit rows. It is never an auth input. That is why CANVAS_DROP_TRUSTED_PROXY_IPS is worth setting in oidc mode too (for example 127.0.0.1 for a local reverse proxy), even though header-asserted identity fires only in proxy mode. oidc mode: the app owns the session Login uses PKCE (S256), a random state , and prompt=login . The callback rejects a missing or mismatched state, a failed code exchange, a missing email claim, and email_verified=false , and only then runs the same allowlist and blocked checks as every other request. Every rejection writes an auth_denied audit row with its reason ( missing_oidc_state , state_mismatch , token_exchange_failed , no_email_claim , email_not_verified , email_domain_not_allowed , blocked ). The session token is 256 bits of randomness; only its SHA-256 hash is stored, so the raw token never lands in the database. The cookie is HttpOnly always, Secure in production, SameSite=Lax , and scoped to .{baseHost} in subdomain mode. /auth/logout revokes the session (audit session_revoke ). CANVAS_DROP_SESSION_SECRET must be at least 32 characters outside dev mode; it also signs the password-gate grants below. Agents Agents connect over MCP at {base}/mcp with OAuth tokens the instance issues itself (audited as mcp_token_issue , mcp_token_revoke , mcp_authorize_ok , mcp_authorize_denied ). Every tool resolves the caller's user server-side and runs the same owner-or-editor role gate as the dashboard. The keyed Deploy API has no user at all: its writes are attributed to the canvas owner. See MCP server . Access is decided on every request (invariant 3) Per-canvas roles and the access ladder are resolved from the server-side principal on each request, never cached on a session or an agent's token. Two roles manage a canvas: The owner : the single account on the canvas record. Only the owner can delete the canvas, transfer ownership, or switch on AI for added people. An editor who tries gets 403 {\"code\":\"OWNER_ONLY\"} . An editor : owner-equivalent for everything else, including settings, sharing, the draft editor, publish, rollback, the deploy key, and adding or removing other editors. An editor is a signed-in member holding a direct editor row or membership of an editor-role team. With an org boundary configured (below), the grant holds only while they are currently a member of the canvas's home org; the check runs on every request. A guest or anonymous visitor never holds a role. A principal with no role sees the management surface as 404 {\"error\":\"not_found\"} . The people-and-teams list is consulted at every General-access choice; the choice itself decides who else is admitted: Who Admitted when Password and expiry A person on the list always — an existing user by id, or a pending email once that exact email has signed in through your configured auth both apply A member of a team on the list always. A personal team admits by membership alone; an org team also requires the viewer to be a current member of that team's org, re-joined live on every request, so a stale team row cannot widen access both apply Anyone else at Restricted ( private , or its legacy aliases specific_people / team ) never — Whole org any signed-in member. When an org is named (below), members of the canvas's home org only both apply Public link anyone, static files only, while the instance-wide switch is on and the owner still holds the publish-public capability both apply; an anonymous visitor reaches the password prompt The owner and editors are admitted at every choice, never see the password prompt, and are unaffected by expiry. Anyone with no route in gets 404 . A password is checked with argon2id; a successful attempt sets an HttpOnly grant cookie ( __canvasdrop_gate ), HMAC-signed with CANVAS_DROP_SESSION_SECRET and bound to the canvas id and its password version, so changing the password invalidates every outstanding grant at once. In subdomain mode the grant is host-only; in path mode it is scoped to the canvas path. Attempts are limited to CANVAS_DROP_RATELIMIT_PASSWORD_GATE_PER_MIN (default 5) per user per canvas, or per client IP for an anonymous public visitor; over the limit the gate answers 429 with Retry-After . Ownership moves are instant and audited. The owner can hand a canvas to an existing editor who is an org member ( POST /api/canvases/{id}/transfer , MCP transfer_canvas ). The previous owner stays on as an editor while their account is active and passes the org check; if the new owner may not publish publicly, a Public link rung is turned off; the deploy key is unchanged. An admin can reassign a canvas whose owner has left from the admin routes, with a recorded reason. A reassign rotates the deploy key in the same write (the plaintext is discarded; the new owner issues a fresh one) and never gives the acting admin content access. Both write an audit row ( canvas_transfer , canvas_reassign_owner ) and revalidate the canvas's live realtime sockets. Public links are doubly gated. Admin → Configuration → access.publicLinksEnabled (default on) is the instance switch; turning it off makes every public-link canvas Restricted. Each owner also needs the publish-public capability, granted and revoked per user under Admin → People; revoking it sweeps that owner's public-link canvases back to Restricted. Both gates are also re-checked on every request, so a canvas whose owner lost the grant is unreachable even before the sweep lands. A public-link visitor gets files only: every runtime API call is refused with 403 {\"code\":\"STATIC_ONLY\"} , including for signed-in members who are not the owner or an editor. In proxy mode a public link works only for requests your proxy lets reach the app. Admins have no back door. For a canvas they do not own, an admin is an ordinary member: a Restricted canvas returns 404 , a password prompts them, and they cannot open the editor or change settings. Cross-owner admin power lives on the dedicated admin routes: the all-canvases list, disable / enable / restore, reassigning the owner when someone leaves, and gallery featuring. It never extends to canvas content, the runtime API, or realtime. One recorded exception: the authoring API ( CANVAS_DROP_AUTHORING , default off ) still admits an admin to update or revoke an authored share it does not own. The retired guest magic-link flow is gone. Old /guest/{token} links return an invalid-link page, never set a cookie, and never consume a token; the sharing paths below record pending access against your configured auth instead. The org boundary (member vs guest) By default any signed-in user is treated as one org, so whole_org means \"anyone who passed sign-in\". Naming an org with CANVAS_DROP_ORG_NAME draws a member-vs-guest boundary: A signed-in user whose verified email domain is in CANVAS_DROP_ORG_DOMAINS (default: the allowed-email domains) is a member . Everyone else who can sign in, such as an allowlisted contractor or an admin on another domain, is a guest . Membership is independent of the sign-in permit list and of CANVAS_DROP_ADMIN_EMAILS ; those grant sign-in, not membership. Each canvas has a home org , set once at creation: a member picks Personal or the org; a guest only gets Personal. whole_org then means \"members of the canvas's home org\". A guest cannot see it, and a Personal whole_org canvas is an explicit deny to everyone but its owner and editors. Membership is derived server-side from the identity the gateway resolved. A client can never assert which org it belongs to. Admin is orthogonal: it grants no membership and no content bypass. The boundary is inert until an org is named, so it is an opt-in tightening. Turning it on for an existing instance is a one-time, dry-run-first cutover; see Configuration → Tenancy and the docs/tenancy.md runbook in the repo. Adds are auth-delegated (no app-owned credentials) When someone adds a person who has no account yet, whether to a canvas, to a personal team, or under Admin → People → Sign-in permits, canvas-drop records pending access , not a login. There is no app-owned magic-link account and no app-stored password. The grant materializes the first time that email authenticates through your configured auth ( proxy , oidc , or dev ); the identity provider stays the only authority, so there is nothing to take over. The verified login email is the match key, and pending access never grants anything on its own. Who may permit a brand-new email to sign in is gated: In proxy mode, an email that cannot already sign in (its domain is not allowed and it holds no permit) must be admitted at the proxy first. canvas-drop cannot widen an upstream IAP, so the add is refused with 403 {\"code\":\"AUTH_ADMISSION_REQUIRED\"} until then. An admin can add a permit under Sign-in permits. A member can only when the admin setting invites.allowMemberNewEmails is on (off by default), or when the email already authenticates. Otherwise the add is refused with 403 {\"code\":\"NOT_PERMITTED\"} ; a member cannot widen who may sign in to your instance. Add volume is bounded per actor with invites.maxPerActorPerHour (default 20) and invites.pendingCap (default 50). Both are DB-managed admin settings; see Sign-in permits and access emails . No secrets in the browser (invariant 2) The AI provider key ( CANVAS_DROP_AI_API_KEY ), every canvas API key, and every Connection protected header are server-side only. The browser SDK rides the viewer's session, so a canvas calls the primitives (KV, files, AI, identity, realtime, and admin-granted Connections) with no secret in its code. The deploy engine lints every upload and warns when a file appears to contain a canvas API key. Deploy keys are cd_ Bearer secrets: 32 random bytes, stored only as a SHA-256 hash, shown once at creation or regeneration. A key works only on its own canvas ( 403 on any other), only while that canvas is active ( 401 the moment it is archived or disabled), and dies the moment it is regenerated. Cloning a canvas mints a fresh key; it never copies the source's. The key is per canvas, not per person: removing an editor does not invalidate a key they copied, so regenerate it when someone leaves (the dashboard offers this). See the Deploy API . Connections: bounded outbound authority A Connection is an admin-created, reusable outbound profile, not a general proxy or a Lambda runtime. It names one exact HTTPS DNS origin, a set of allowed HTTP methods, and optional protected headers such as Authorization or a controlled User-Agent . An admin grants that profile to individual canvases. The canvas supplies only a root-relative path, query, body, and non-protected request headers; the server adds protected headers last. Anonymous Public-link viewers cannot call it because the whole runtime API stays static-only for them. Protected header values are encrypted with AES-256-GCM under CANVAS_DROP_CONNECTIONS_ENCRYPTION_KEY , authenticated to the profile id, and are write-only in the admin API and UI. Logs, usage events, MCP results, and error bodies expose neither the values nor requested paths/query strings. Backups contain ciphertext but not the root key; preserve that key separately as described in the operations runbook. Outbound requests resolve DNS on every hop and pin the selected public address into the socket while retaining the hostname for TLS SNI and certificate validation. Literal IPs, credentials in the URL, private/link-local/loopback/reserved IPv4 and IPv6, mixed public and private DNS answers, redirects to a different origin, and redirects to disallowed methods are rejected. Request and response sizes, total duration, concurrency, and rate are bounded. As defense in depth, restrict the app container's network egress to the APIs your profiles actually need; application-layer SSRF checks should not be the only barrier between a canvas and your internal network. See Connections . Path mode vs subdomain mode (invariant 4) CANVAS_DROP_URL_MODE is the most consequential deployment choice. Path mode ( {base}/c/{slug}/ ): every canvas shares one origin with the others and with the dashboard, so the browser does not isolate them. A malicious or compromised canvas could make same-origin requests against other canvases' client-side state. Fine for localhost and single-user hosting. Running proxy or oidc auth in path mode is multi-user, and the app refuses to boot unless you set CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true to accept the tradeoff. Subdomain mode ( {slug}.canvases.example.com ): each canvas is its own origin, so the browser isolates them and invariant 4 holds in full. Needs a wildcard DNS record and wildcard TLS at the proxy, and a non-localhost CANVAS_DROP_BASE_URL . Server-side isolation holds in both modes: blobs, KV, files, AI usage, realtime channels, and Connection grants are keyed by canvas id, and management and admin writes require a same-origin request: Sec-Fetch-Site: same-origin (or none ), else an Origin whose host matches CANVAS_DROP_BASE_URL ; anything else gets 403 {\"error\":\"cross_origin_forbidden\"} . Subdomain mode adds the browser's origin boundary on top. If you do not run an identity-aware proxy, run subdomain mode with oidc so you keep that boundary without standing up a proxy. Lifecycle changes land on the next request (invariant 5) Nothing about access is cached. Concretely: Roles and rungs are resolved per request, so removing an editor, lowering a rung, or removing someone from a list, team, or org denies them on their very next request. Share, people-list, ownership, password, and unpublish changes also revalidate that canvas's live realtime sockets right away, and sockets that lost access are dropped. A heartbeat re-authorizes every live socket once a minute as a backstop, so a share expiry passing, or an admin block or delete (which fire no mutation hook), drops the socket within one tick. A blocked user is refused at the gateway on the next request, whatever session or agent token they hold. An archived, disabled, or deleted canvas rejects its own deploy key with 401 . Regenerating a slug invalidates the old URL and drops every live socket so clients reconnect under the new one. Regenerating a key invalidates the old key at once. Changing a canvas password invalidates every outstanding gate grant. Turning off public links, or revoking one owner's publish-public capability, makes the affected canvases Restricted immediately. Reducing canvas XSS blast radius Subdomain mode contains the blast radius of a compromised canvas to that canvas's origin; Restricted-by-default access limits who can be exposed to it. Every response carries X-Content-Type-Options: nosniff , Referrer-Policy: same-origin , and Cross-Origin-Opener-Policy: same-origin . Canvas content adds a Content-Security-Policy of frame-ancestors 'self' {base origin} in subdomain mode ( frame-ancestors 'self' in path mode), so the dashboard can preview a canvas and no other canvas can frame it. The dashboard itself runs under a strict CSP ( script-src 'self' , frame-ancestors 'none' ). Tell canvas authors to prefer textContent over innerHTML . What gets audited The audit log records auth_ok and auth_denied (with the reason) on every gateway decision, session_create and session_revoke in oidc mode, and every OIDC callback rejection. Canvas actions are attributed to the acting user or agent: deploy , rollback , publish , canvas_unpublish , share_change , password_change , password_attempt , slug_regen , key_regen , canvas_transfer , among others. Admin actions are audited by name: canvas_disable , canvas_enable , canvas_restore , canvas_reassign_owner , canvas_feature , user_block , user_unblock , user_promote , user_demote , user_grant_public , user_revoke_public , allowed_email_add , allowed_email_remove , admin_settings_update , and the Connection profile/grant actions. Rows carry the client IP described above. Login attempts are limited to CANVAS_DROP_RATELIMIT_LOGIN_PER_MIN (default 10) per client IP; see Rate limiting . No telemetry canvas-drop does not phone home. There is no analytics, usage reporting, or third-party beacon in the product. Nothing leaves your instance unless you configure an outbound integration yourself, such as an OIDC provider, an AI provider, a mail transport, or an admin-granted Connection."
  },
  {
    "path": "self-hosting/deploy",
    "section": "Self-hosting",
    "title": "Deploy",
    "html": "<h1 id=\"deploy\">Deploy</h1>\n<p>This page is for the operator putting canvas-drop in production. By the end you run one\nNode process behind a TLS-terminating reverse proxy that asserts who the user is, with\nPostgres and S3-compatible storage beside it, and you know how to check its health, back\nit up, and upgrade it. The full variable reference is on\n<a href=\"/docs/self-hosting/configuration\">Configuration</a>.</p>\n<p>The production recipe:</p>\n<pre><code class=\"hljs language-bash\">docker build -t canvas-drop .\n<span class=\"hljs-built_in\">cp</span> .env.production.example canvas-drop.env      <span class=\"hljs-comment\"># replace every CHANGE_ME / REPLACE_ME</span>\ndocker run -d --name canvas-drop --env-file canvas-drop.env \\\n  -p 127.0.0.1:3000:3000 -v canvas-drop-data:/data canvas-drop\ncurl -fsS http://127.0.0.1:3000/healthz\n<span class=\"hljs-comment\"># {\"status\":\"ok\",\"db\":\"ok\",\"version\":\"...\"}</span></code></pre>\n<p>Then point your identity-aware proxy at <code>127.0.0.1:3000</code> under the contract below. To see\nthe whole shape running first, with a bundled IdP and no external setup, use the compose\nstack:</p>\n<pre><code class=\"hljs language-bash\">docker compose up --build     <span class=\"hljs-comment\"># add -d to run in the background</span>\n<span class=\"hljs-comment\"># open http://localhost:8080 and sign in as demo@example.com / canvasdrop</span></code></pre>\n<p>The app is a single Hono server: dashboard, auth, management and platform APIs, canvas\ncontent, MCP, docs, and the realtime WebSocket all share one port and one log stream.\nIdentity only ever comes from the app's server-side auth context, so the proxy has one\nsecurity job: assert the user correctly. Everything else on this page is a config switch.</p>\n<h2 id=\"recommended-production-profile\">Recommended production profile</h2>\n<table>\n<thead>\n<tr>\n<th>Interface</th>\n<th>Setting</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>URL mode</td>\n<td><code>CANVAS_DROP_URL_MODE=subdomain</code></td>\n<td>Every canvas is its own browser origin (<code>{slug}.{base}</code>). Needs a non-localhost <code>CANVAS_DROP_BASE_URL</code>, wildcard DNS (<code>*.canvases.example.com</code>), and a wildcard certificate at the proxy.</td>\n</tr>\n<tr>\n<td>Auth</td>\n<td><code>CANVAS_DROP_AUTH_MODE=proxy</code> on the JWKS path</td>\n<td>An identity-aware proxy (IAP) in front forwards a signed JWT; the app verifies it.</td>\n</tr>\n<tr>\n<td>Database</td>\n<td><code>CANVAS_DROP_DB=postgres</code></td>\n<td><code>CANVAS_DROP_DATABASE_URL</code> is required.</td>\n</tr>\n<tr>\n<td>Storage</td>\n<td><code>CANVAS_DROP_STORAGE=s3</code></td>\n<td>Bucket, region, access key, and secret key are required; <code>CANVAS_DROP_S3_ENDPOINT</code> for MinIO or R2.</td>\n</tr>\n<tr>\n<td>TLS</td>\n<td>at the proxy</td>\n<td>One certificate covering <code>{base}</code> and <code>*.{base}</code>. The proxy-to-app hop is plain HTTP.</td>\n</tr>\n</tbody></table>\n<p><code>.env.production.example</code> in the repo is this profile, annotated. Any Docker host, VPS,\nPaaS, or Kubernetes cluster works; the design target is a single process on one modest\nbox. None of these choices is mandatory: SQLite plus local disk needs only a volume, and\neach interface (database, storage, URL mode, auth) is a config switch you can change\nlater without touching code (see \"Changing drivers later\").</p>\n<h2 id=\"shape\">Shape</h2>\n<pre><code class=\"hljs\">            ┌─────────────────────────┐\n  client ──▶│ <span class=\"hljs-keyword\">identity</span>-aware proxy/IAP │  terminates TLS, asserts <span class=\"hljs-keyword\">identity</span>\n            └────────────┬────────────┘\n                         ▼\n                 canvas-<span class=\"hljs-keyword\">drop</span> app  ── Postgres\n                                   └ S3-compatible <span class=\"hljs-keyword\">storage</span></code></pre>\n<p>The app must not be reachable directly; only the proxy talks to it. The proxy must\n<strong>overwrite</strong> (never append to) the identity headers it forwards.</p>\n<h2 id=\"what-the-reverse-proxy-must-do\">What the reverse proxy must do</h2>\n<p>Whichever proxy you run (an IAP, Caddy, nginx, a tunnel), the app needs these behaviors\nfrom it:</p>\n<ul>\n<li><strong>Preserve <code>Host</code>.</strong> In subdomain mode the app picks the canvas from the <code>Host</code> header\n(<code>{slug}.{baseHost}</code>); the base host itself routes by path. Caddy forwards <code>Host</code> by\ndefault; nginx needs <code>proxy_set_header Host $host</code>.</li>\n<li><strong>Pass WebSocket upgrades.</strong> The realtime primitive is a WebSocket on the same port as\neverything else.</li>\n<li><strong>Allow large request bodies on deploy routes.</strong> The app accepts up to 110 MB on\n<code>PUT /v1/canvases/{id}/deploy</code> and the dashboard deploy routes, and 26 MB per staged\nblob (<code>PUT /v1/canvases/{id}/uploads/{uploadId}/blobs/{hash}</code>). It answers <code>413</code> above\nthose limits itself; a proxy with a lower cap fails deploys first.</li>\n<li><strong>Forward <code>Authorization</code> untouched</strong> on <code>/v1/canvases/*</code> and <code>/mcp</code>, and route those\npaths around the IAP's login (see \"Agents and CI behind an IAP\" below).</li>\n<li><strong>Set <code>CANVAS_DROP_BASE_URL</code> to the public <code>https://</code> origin.</strong> The app builds every\nabsolute URL (canvas URLs, the OIDC redirect URI, MCP discovery metadata) from that\nvariable and never reads <code>X-Forwarded-Proto</code>, so the plain-HTTP hop between proxy and\napp is fine.</li>\n<li><strong>Give it 5 seconds to stop.</strong> On <code>SIGTERM</code> the app closes WebSockets, drops idle\nkeep-alive connections, force-closes after 5 s, flushes the audit log, and closes the\ndatabase. Set your stop grace period to at least that.</li>\n</ul>\n<h2 id=\"auth-at-the-edge\">Auth at the edge</h2>\n<p>In <code>proxy</code> mode the app is sessionless: the IAP owns the session, and the app re-derives\nidentity on every request from what the proxy forwards, so a proxy-side logout takes\neffect on the next request. Pick exactly one trust path. They do not compose.</p>\n<ul>\n<li><strong>JWT / JWKS (preferred, cryptographic).</strong> The proxy forwards a signed JWT (default\nheader <code>Cf-Access-Jwt-Assertion</code>; set <code>CANVAS_DROP_AUTH_PROXY_JWT_HEADER</code> to change it).\nSet <code>CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL</code>, <code>CANVAS_DROP_AUTH_PROXY_JWT_ISSUER</code>, and\n<code>CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE</code>; boot refuses a JWKS URL without both issuer and\naudience. The app checks the signature against the JWKS plus <code>iss</code>, <code>aud</code>, and expiry,\nand takes the identity from the token's <code>email</code> claim (a token without one resolves\nto anonymous). When a JWKS URL is set, plain identity headers are never honored: a\nstray <code>X-Auth-Request-Email</code> without a valid JWT resolves to anonymous and is logged.</li>\n<li><strong>Trusted header (only when no JWKS URL is set).</strong> The app trusts the forwarded email\nheader (default <code>X-Auth-Request-Email</code>; display name from\n<code>X-Auth-Request-Preferred-Username</code>) only when the request's TCP peer IP is listed in\n<code>CANVAS_DROP_TRUSTED_PROXY_IPS</code> (CSV of IPv4 addresses or CIDRs). The peer IP is the\nsocket address, never a forwarded header. Each entry is validated at boot: <code>/0</code>,\nmalformed entries, and IPv6 are rejected, so \"trust everything\" cannot be configured.\nA header from any other peer is ignored and logged.</li>\n</ul>\n<p>Proxy mode refuses to boot without a JWKS URL or a trusted-proxy IP list. In <code>proxy</code> and\n<code>oidc</code> modes you must also set <code>CANVAS_DROP_ALLOWED_EMAIL_DOMAINS</code> (one or more domains;\nCSV, lowercased), which is enforced on every request, and a <code>CANVAS_DROP_SESSION_SECRET</code>\nof at least 32 characters (also required whenever <code>NODE_ENV=production</code>, which the Docker\nimage sets). A request that yields no identity gets <code>401 { \"error\": \"unauthorized\" }</code>\nin <code>proxy</code> mode; the IAP is expected to have bounced it first. The\n<a href=\"/docs/self-hosting/security-model\">Security model</a> covers the invariants behind these\nrules.</p>\n<p>Running real auth (<code>proxy</code> or <code>oidc</code>) in <code>path</code> URL mode also requires\n<code>CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true</code>, because path mode puts every canvas on one\nbrowser origin. Set it only when you accept that tradeoff. The demo compose stack does,\nsince <code>subdomain</code> cannot boot on localhost. <code>subdomain</code> mode needs no opt-in.</p>\n<p>The third mode, <code>dev</code>, auto-signs-in a fixed local user with no verification and is\nrejected at boot when <code>NODE_ENV=production</code>.</p>\n<h3 id=\"agents-and-ci-behind-an-iap\">Agents and CI behind an IAP</h3>\n<p>Two surfaces authenticate themselves and never carry the IAP's browser session:</p>\n<ul>\n<li><strong>The Deploy API</strong>, <code>/v1/canvases/*</code>, authenticates with a per-canvas secret key\n(<code>Authorization: Bearer cd_...</code>).</li>\n<li><strong>The MCP endpoint</strong>, <code>/mcp</code>, authenticates with an OAuth 2.1 bearer token issued by the\napp itself. Clients discover the endpoints at <code>/.well-known/oauth-authorization-server</code>\nand <code>/.well-known/oauth-protected-resource</code>, register at <code>/register</code>, and exchange\ncodes at <code>/token</code> (<code>/revoke</code> revokes). Only the <code>/authorize</code> step is a browser\nround-trip, and it does go through the IAP.</li>\n</ul>\n<p>Configure the IAP to let <code>/v1/canvases/*</code>, <code>/mcp</code>, <code>/.well-known/*</code>, <code>/register</code>,\n<code>/token</code>, and <code>/revoke</code> through unauthenticated and to forward <code>Authorization</code> unchanged;\nthe app rejects bad or missing tokens with <code>401</code>. In subdomain mode a common layout is a\ndedicated API host: set <code>CANVAS_DROP_API_BASE_URL</code> (for example\n<code>https://api.canvases.example.com</code>) so the MCP tools advertise the right endpoints to\nagents; <code>api</code>, <code>v1</code>, <code>sdk</code>, <code>auth</code>, and <code>mcp</code> are reserved slugs, so no canvas can\ncollide with it. The demo Caddyfile strips <code>Authorization</code> on purpose, which is why the\nDeploy API is unreachable in the demo.</p>\n<p>Two related facts about anonymous traffic. <code>GET /healthz</code> is unauthenticated at the app;\nthe container health check calls it from inside, so expose it externally only if a\nmonitor needs it. And a Public link canvas is only public if the IAP lets anonymous\nrequests reach the app: the app serves that rung (static files only) to a request with\nno identity, and nothing else. Behind an IAP that demands sign-in on every host, public\nlinks land on the IAP's login page.</p>\n<h2 id=\"without-an-identity-aware-proxy-oidc\">Without an identity-aware proxy: <code>oidc</code></h2>\n<p>If you do not run an IAP, use the built-in <code>oidc</code> mode and point it at your OpenID\nprovider. Run it in <code>subdomain</code> mode so you keep per-canvas origin isolation without\nstanding up an IAP; a plain TLS-terminating proxy in front is enough.</p>\n<pre><code class=\"hljs\"><span class=\"hljs-attr\">CANVAS_DROP_URL_MODE</span>=subdomain\n<span class=\"hljs-attr\">CANVAS_DROP_BASE_URL</span>=https://canvases.example.com\n<span class=\"hljs-attr\">CANVAS_DROP_AUTH_MODE</span>=oidc\n<span class=\"hljs-attr\">CANVAS_DROP_OIDC_ISSUER</span>=https://accounts.example.com\n<span class=\"hljs-attr\">CANVAS_DROP_OIDC_CLIENT_ID</span>=...\n<span class=\"hljs-attr\">CANVAS_DROP_OIDC_CLIENT_SECRET</span>=...\n<span class=\"hljs-attr\">CANVAS_DROP_ALLOWED_EMAIL_DOMAINS</span>=example.com\n<span class=\"hljs-attr\">CANVAS_DROP_SESSION_SECRET</span>=...   <span class=\"hljs-comment\"># &gt;= 32 chars; openssl rand -hex 32</span></code></pre>\n<p>Register <code>{base}/auth/callback</code> as the redirect URI at your provider. The app runs\nAuthorization Code + PKCE (<code>S256</code>) with <code>scope=openid email profile</code>, requires an <code>email</code>\nclaim, rejects a token whose <code>email_verified</code> is <code>false</code>, and issues its own session\ncookie (<code>__canvasdrop_session</code>: <code>HttpOnly</code>, <code>Secure</code> in production, <code>SameSite=Lax</code>,\nscoped to <code>.{baseHost}</code> in subdomain mode and host-only in path mode, 14-day rolling\nexpiry). A request with no session is redirected to <code>/auth/login?returnTo=...</code>;\n<code>/auth/logout</code> revokes the local session only. Every rejected sign-in is audited as\n<code>auth_denied</code> with its reason.</p>\n<p><code>oidc</code> mode also honors <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code> (and <code>CANVAS_DROP_CLIENT_IP_HEADER</code>)\nso login throttling and audit rows key on the real client IP behind your proxy; in this\nmode those settings never assert identity. See <a href=\"/docs/self-hosting/cdn\">Behind a CDN</a>.</p>\n<h2 id=\"run-with-docker\">Run with Docker</h2>\n<p>There is no published image; build it from the repo. The <code>Dockerfile</code> is multi-stage on\n<code>node:24-slim</code>: a <code>builder</code> stage compiles the workspace (it needs <code>python3</code>, <code>make</code>, and\n<code>g++</code> for the SQLite driver), and a <code>runtime</code> stage carries no compilers and runs as a\ndedicated non-root <code>canvasdrop</code> user (uid/gid 1001).</p>\n<pre><code class=\"hljs language-bash\">docker build -t canvas-drop .</code></pre>\n<p>The image's operational contract:</p>\n<ul>\n<li><strong>Entry:</strong> <code>node --conditions=node-dist apps/server/dist/index.js</code>, with\n<code>NODE_ENV=production</code>, <code>CANVAS_DROP_PORT=3000</code> (<code>EXPOSE 3000</code>), and\n<code>CANVAS_DROP_DASHBOARD_DIST=/app/apps/dashboard/dist</code> preset. Because <code>NODE_ENV</code> is\n<code>production</code>, <code>dev</code> auth is refused; configure <code>proxy</code> or <code>oidc</code>.</li>\n<li><strong>State:</strong> <code>VOLUME /data</code>, pre-created and owned by the non-root user. In-image\ndefaults: <code>CANVAS_DROP_SQLITE_PATH=/data/canvasdrop.db</code> and\n<code>CANVAS_DROP_STORAGE_PATH=/data/storage</code>. Mount a volume there on the SQLite +\nlocal-storage profile; on Postgres + S3 the container holds no state.</li>\n<li><strong>Health:</strong> <code>HEALTHCHECK</code> fetches <code>http://127.0.0.1:3000/healthz</code> every 15 s (5 s\ntimeout, 5 retries) with a 60 s start period, which covers Postgres coming up and\nmigrations running on a cold start. Wire the same URL into your orchestrator's\nreadiness probe.</li>\n<li><strong>Config:</strong> pass <code>CANVAS_DROP_*</code> as container environment (<code>--env-file</code> or your\norchestrator's secrets). The image reads no <code>.env</code>.</li>\n<li><strong>Screenshots (optional):</strong> the default image has no browser. Build with\n<code>--build-arg SCREENSHOTS=1</code> to add Chromium (about 300 MB), then run with\n<code>CANVAS_DROP_SCREENSHOTS=on</code> and flip the admin toggle. See\n<a href=\"/docs/self-hosting/screenshots\">Screenshots</a>.</li>\n</ul>\n<h3 id=\"the-compose-stack\">The compose stack</h3>\n<p><code>docker-compose.yml</code> boots the whole production shape with no external setup. It is a\ndemo of the wiring, not a production deployment as-is.</p>\n<table>\n<thead>\n<tr>\n<th>Service</th>\n<th>Image</th>\n<th>Role</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>caddy</code></td>\n<td><code>caddy:2-alpine</code></td>\n<td>Edge proxy and the only service with a published port (<code>8080</code>). Routes <code>/dex/*</code> to Dex and everything else to oauth2-proxy, and strips client-supplied <code>X-Forwarded-Access-Token</code>, <code>X-Auth-Request-Email</code>, <code>X-Auth-Request-User</code>, <code>X-Auth-Request-Preferred-Username</code>, and <code>Authorization</code> headers on the way in. Plain HTTP for the demo; in production it would terminate TLS.</td>\n</tr>\n<tr>\n<td><code>oauth2-proxy</code></td>\n<td><code>quay.io/oauth2-proxy/oauth2-proxy:v7.6.0</code></td>\n<td>The identity-aware proxy. Signs users in against Dex and forwards the Dex-signed access token to the app in <code>X-Forwarded-Access-Token</code>.</td>\n</tr>\n<tr>\n<td><code>dex</code></td>\n<td><code>dexidp/dex:v2.41.1</code></td>\n<td>Bundled demo IdP with one static user, <code>demo@example.com</code> / <code>canvasdrop</code>.</td>\n</tr>\n<tr>\n<td><code>app</code></td>\n<td>built from the repo as <code>canvas-drop:dev</code></td>\n<td>canvas-drop in real <code>proxy</code> mode on the JWKS path: <code>CANVAS_DROP_AUTH_PROXY_JWT_HEADER=X-Forwarded-Access-Token</code>, <code>CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL=http://dex:5556/dex/keys</code>, issuer <code>http://localhost:8080/dex</code>, audience <code>canvas-drop-demo</code>. <code>path</code> URL mode with <code>CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true</code>, Postgres, local storage on the <code>app-data</code> volume, plus a <code>backups</code> volume. No published port; waits for the Postgres health check.</td>\n</tr>\n<tr>\n<td><code>postgres</code></td>\n<td><code>postgres:16-alpine</code></td>\n<td>The database, on the <code>pg-data</code> volume, with a <code>pg_isready</code> health check.</td>\n</tr>\n<tr>\n<td><code>minio</code></td>\n<td><code>minio/minio:RELEASE.2024-11-07T00-52-20Z</code></td>\n<td>Optional S3-compatible storage behind <code>--profile minio</code>. Starting it does not switch the app to S3; edit the <code>app</code> service's <code>environment:</code> block as shown on <a href=\"/docs/self-hosting/install\">Install</a>, and create the bucket yourself.</td>\n</tr>\n</tbody></table>\n<p>The app verifies a Dex-signed JWT against Dex's JWKS, the same cryptographic trust path\nyou run in production. The smoke test boots the stack and asserts the launch invariants:\nthe app reports healthy, the app has no host port, an unauthenticated request is\nredirected, a forged <code>X-Forwarded-Access-Token</code> / <code>X-Auth-Request-Email</code> pair is still\nredirected, a real Dex login resolves <code>demo@example.com</code> with <code>authMode: proxy</code>, and the\nsame user id survives <code>docker compose restart app postgres</code>.</p>\n<pre><code class=\"hljs language-bash\">./scripts/compose-smoke.sh              <span class=\"hljs-comment\"># leaves the stack running</span>\nKEEP_UP=0 ./scripts/compose-smoke.sh    <span class=\"hljs-comment\"># tears it down (docker compose down -v)</span></code></pre>\n<p>Every Dex and oauth2-proxy secret under <code>docker/</code> is a labeled demo placeholder, and the\nstack runs plain HTTP in path mode. Work the checklist below before anyone but you can\nreach it.</p>\n<p>The compose file also carries a commented-out <code>maintenance</code> service: a supercronic\nsidecar on the same image and volumes that runs <code>docker/maintenance.cron</code>, a nightly\n<code>backup</code> at 03:15 UTC (tarred, 14-day retention, the prune runs only after a good new\nbackup exists) and <code>purge 30</code> at 03:45 UTC on Sundays. Uncomment it, keep its\n<code>CANVAS_DROP_DB</code> / storage variables in step with the <code>app</code> service, and start it with\n<code>docker compose --profile maintenance up -d</code>.</p>\n<h3 id=\"graduating-to-a-real-idp\">Graduating to a real IdP</h3>\n<p>Moving off the bundled IdP is configuration, not code, but each step matters:</p>\n<ol>\n<li>Point oauth2-proxy (or your own IAP) at your real provider, and set\n<code>CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL</code>, <code>CANVAS_DROP_AUTH_PROXY_JWT_ISSUER</code>, and\n<code>CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE</code> to match the tokens it forwards.</li>\n<li>Confirm the forwarded JWT carries the verified email in its <code>email</code> claim; a token\nwithout one resolves to anonymous.</li>\n<li>Set <code>CANVAS_DROP_ALLOWED_EMAIL_DOMAINS</code> to your real domains and\n<code>CANVAS_DROP_ADMIN_EMAILS</code> to your bootstrap admins.</li>\n<li>Switch to <code>CANVAS_DROP_URL_MODE=subdomain</code> once wildcard DNS and a wildcard\ncertificate exist, and drop <code>CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE</code>.</li>\n<li>Rotate every demo secret, including <code>CANVAS_DROP_SESSION_SECRET</code>; set\n<code>cookie_secure=true</code> at oauth2-proxy once TLS terminates at the edge.</li>\n<li>If agents or CI will deploy, stop stripping <code>Authorization</code> for <code>/v1/canvases/*</code> and\n<code>/mcp</code> and exempt those paths from the IAP's login.</li>\n<li>Re-run the forged-token check against the new wiring.</li>\n</ol>\n<h2 id=\"running-the-bare-process\">Running the bare process</h2>\n<p>To run without Docker, build the workspace and start the compiled server. Node 24 or\nnewer and pnpm 11 (<code>corepack enable</code> picks up the pinned version) are required.</p>\n<pre><code class=\"hljs language-bash\">pnpm install --frozen-lockfile\npnpm build\nnode --conditions=node-dist apps/server/dist/index.js</code></pre>\n<p><code>pnpm build</code> compiles the shared package to <code>dist/</code> first; the <code>node-dist</code> export\ncondition makes <code>@canvas-drop/shared</code> resolve to that compiled JS, so production runs\nwithout <code>tsx</code>.</p>\n<p>Supply configuration through the process manager, not a <code>.env</code> file; only <code>pnpm dev</code>\nreads <code>.env</code>. A systemd unit with <code>EnvironmentFile=</code> and a TLS proxy in front (Caddy,\nnginx, a tunnel) is enough for a small instance on one box: the app binds a local port\n(default <code>3000</code>) and the proxy reverse-proxies to it under the contract above. If the\nport is already bound, the process prints a hint and exits <code>1</code> rather than retrying.</p>\n<h2 id=\"health-boot-and-upgrades\">Health, boot, and upgrades</h2>\n<p><code>GET /healthz</code> pings the database and answers <code>{\"status\":\"ok\",\"db\":\"ok\",\"version\":...}</code>\nwith <code>200</code>, or <code>{\"status\":\"degraded\",\"db\":\"error\",...}</code> with <code>503</code> when the database is\nunreachable. At boot the process validates its configuration first and exits <code>1</code> with a\nmessage naming every invalid variable, then connects to the database, runs pending\nmigrations, opens storage, and only then opens the port. A passing health check\ntherefore means the database is reachable and the schema is current.</p>\n<p>Migrations run at boot in every mode; there is no separate migrate command. To upgrade,\ntake a backup, replace the image or the built tree, and restart. Migrations are written\nto be additive, so an existing database is not rewritten in place; the backup is your\nrollback.</p>\n<p>Rate-limit counters (fixed 60 s windows, <code>CANVAS_DROP_RATELIMIT_*</code>) and realtime channels\nlive in process memory, which is why the profile above is one process per instance.</p>\n<h2 id=\"backups-and-maintenance\">Backups and maintenance</h2>\n<p>The server binary doubles as the ops tool. The same image, the same config, no extra\ntooling:</p>\n<pre><code class=\"hljs language-bash\">BIN=<span class=\"hljs-string\">\"node --conditions=node-dist apps/server/dist/index.js\"</span>\n<span class=\"hljs-variable\">$BIN</span> backup /backups/$(<span class=\"hljs-built_in\">date</span> -u +%Y%m%dT%H%M%SZ)   <span class=\"hljs-comment\"># whole instance: every table + every blob</span>\n<span class=\"hljs-variable\">$BIN</span> restore /backups/20260620T031500Z             <span class=\"hljs-comment\"># into an empty instance; --force to overwrite</span>\n<span class=\"hljs-variable\">$BIN</span> purge 30                                      <span class=\"hljs-comment\"># reclaim canvases deleted 30+ days ago; add dry-run to preview</span></code></pre>\n<p>A backup is a self-describing directory (<code>meta.json</code>, <code>db/&lt;table&gt;.ndjson</code>, <code>blobs/&lt;key&gt;</code>)\nwritten through the database and storage interfaces, so it is driver-agnostic: a backup\ntaken on SQLite + local disk restores into Postgres + S3 and vice versa. That makes\nbackup then restore the supported way to migrate between drivers. <code>backup</code> runs pending\nmigrations before it reads. <code>restore</code> refuses a non-empty database without <code>--force</code> and\nverifies row counts, blob count and bytes, and every blob's SHA-256 before writing\nanything.</p>\n<p>A backup is as sensitive as the database: it is a cleartext export of credential\nmaterial (password and API-key hashes, OAuth client secrets, session and MCP-token rows)\nand personal data (allowed emails, audit-log actor IPs). Keep it on a separate volume,\nrestrict it to the app user, and encrypt it before it leaves the host. Run a restore\ndrill into a throwaway instance periodically; a green restore is the only proof a backup\nis real.</p>\n<p><code>purge [days]</code> reclaims the storage and version rows of canvases soft-deleted longer ago\nthan <code>days</code>, then prunes <code>usage_events</code> and <code>ai_usage</code> rows older than 90 days. A\nsensible schedule is a nightly backup with 14-day local retention and a weekly\n<code>purge 30</code>, either from a host crontab or the compose <code>maintenance</code> sidecar. The full\nrunbook, including the crontab lines and the restore drill, is <code>docs/ops.md</code> in the repo.</p>\n<h2 id=\"logs\">Logs</h2>\n<p>Structured JSON to stdout via pino: no app-side files, rotation, or shipping. Tune with\n<code>LOG_LEVEL</code> (default <code>info</code>) and <code>LOG_FORMAT</code> (<code>json</code> when <code>NODE_ENV=production</code>,\notherwise <code>pretty</code>). Each request gets a correlation ID, taken from an inbound\n<code>X-Correlation-ID</code> or <code>X-Request-Id</code> header or generated, and echoed back as\n<code>X-Correlation-ID</code>. <code>/healthz</code> is excluded from request logging. Error tracking is off\nunless you set <code>CANVAS_DROP_SENTRY_DSN</code>. There is no telemetry or phone-home.</p>\n<h2 id=\"changing-drivers-later\">Changing drivers later</h2>\n<p>Start small and change env, never code:</p>\n<table>\n<thead>\n<tr>\n<th>Move</th>\n<th>Set</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Local disk to object storage</td>\n<td><code>CANVAS_DROP_STORAGE=s3</code> plus <code>CANVAS_DROP_S3_BUCKET</code>, <code>CANVAS_DROP_S3_REGION</code>, <code>CANVAS_DROP_S3_ACCESS_KEY</code>, <code>CANVAS_DROP_S3_SECRET_KEY</code>; <code>CANVAS_DROP_S3_ENDPOINT</code> for MinIO or R2 (<code>CANVAS_DROP_S3_FORCE_PATH_STYLE</code> defaults to <code>true</code>).</td>\n</tr>\n<tr>\n<td>SQLite to Postgres</td>\n<td><code>CANVAS_DROP_DB=postgres</code> plus <code>CANVAS_DROP_DATABASE_URL</code>.</td>\n</tr>\n<tr>\n<td>Built-in login to an IAP</td>\n<td>Put a JWT-issuing identity-aware proxy in front and set <code>CANVAS_DROP_AUTH_MODE=proxy</code> with the JWKS variables above.</td>\n</tr>\n<tr>\n<td>A CDN in front</td>\n<td>See <a href=\"/docs/self-hosting/cdn\">Behind a CDN</a>: trusted-proxy IPs, the client-IP header, a cache rule that bypasses on the session cookie, and <code>CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL</code>.</td>\n</tr>\n</tbody></table>\n<p>Switching a driver changes where new data goes; to carry existing data across, take a\nbackup on the old drivers and restore it on the new ones.</p>\n<p>See <a href=\"/docs/self-hosting/configuration\">Configuration</a> for the full env surface.</p>\n",
    "headings": [
      {
        "id": "recommended-production-profile",
        "text": "Recommended production profile",
        "level": 2
      },
      {
        "id": "shape",
        "text": "Shape",
        "level": 2
      },
      {
        "id": "what-the-reverse-proxy-must-do",
        "text": "What the reverse proxy must do",
        "level": 2
      },
      {
        "id": "auth-at-the-edge",
        "text": "Auth at the edge",
        "level": 2
      },
      {
        "id": "agents-and-ci-behind-an-iap",
        "text": "Agents and CI behind an IAP",
        "level": 3
      },
      {
        "id": "without-an-identity-aware-proxy-oidc",
        "text": "Without an identity-aware proxy: oidc",
        "level": 2
      },
      {
        "id": "run-with-docker",
        "text": "Run with Docker",
        "level": 2
      },
      {
        "id": "the-compose-stack",
        "text": "The compose stack",
        "level": 3
      },
      {
        "id": "graduating-to-a-real-idp",
        "text": "Graduating to a real IdP",
        "level": 3
      },
      {
        "id": "running-the-bare-process",
        "text": "Running the bare process",
        "level": 2
      },
      {
        "id": "health-boot-and-upgrades",
        "text": "Health, boot, and upgrades",
        "level": 2
      },
      {
        "id": "backups-and-maintenance",
        "text": "Backups and maintenance",
        "level": 2
      },
      {
        "id": "logs",
        "text": "Logs",
        "level": 2
      },
      {
        "id": "changing-drivers-later",
        "text": "Changing drivers later",
        "level": 2
      }
    ],
    "text": "Deploy This page is for the operator putting canvas-drop in production. By the end you run one Node process behind a TLS-terminating reverse proxy that asserts who the user is, with Postgres and S3-compatible storage beside it, and you know how to check its health, back it up, and upgrade it. The full variable reference is on Configuration . The production recipe: docker build -t canvas-drop . cp .env.production.example canvas-drop.env # replace every CHANGE_ME / REPLACE_ME docker run -d --name canvas-drop --env-file canvas-drop.env \\ -p 127.0.0.1:3000:3000 -v canvas-drop-data:/data canvas-drop curl -fsS http://127.0.0.1:3000/healthz # {\"status\":\"ok\",\"db\":\"ok\",\"version\":\"...\"} Then point your identity-aware proxy at 127.0.0.1:3000 under the contract below. To see the whole shape running first, with a bundled IdP and no external setup, use the compose stack: docker compose up --build # add -d to run in the background # open http://localhost:8080 and sign in as demo@example.com / canvasdrop The app is a single Hono server: dashboard, auth, management and platform APIs, canvas content, MCP, docs, and the realtime WebSocket all share one port and one log stream. Identity only ever comes from the app's server-side auth context, so the proxy has one security job: assert the user correctly. Everything else on this page is a config switch. Recommended production profile Interface Setting Notes URL mode CANVAS_DROP_URL_MODE=subdomain Every canvas is its own browser origin ( {slug}.{base} ). Needs a non-localhost CANVAS_DROP_BASE_URL , wildcard DNS ( *.canvases.example.com ), and a wildcard certificate at the proxy. Auth CANVAS_DROP_AUTH_MODE=proxy on the JWKS path An identity-aware proxy (IAP) in front forwards a signed JWT; the app verifies it. Database CANVAS_DROP_DB=postgres CANVAS_DROP_DATABASE_URL is required. Storage CANVAS_DROP_STORAGE=s3 Bucket, region, access key, and secret key are required; CANVAS_DROP_S3_ENDPOINT for MinIO or R2. TLS at the proxy One certificate covering {base} and *.{base} . The proxy-to-app hop is plain HTTP. .env.production.example in the repo is this profile, annotated. Any Docker host, VPS, PaaS, or Kubernetes cluster works; the design target is a single process on one modest box. None of these choices is mandatory: SQLite plus local disk needs only a volume, and each interface (database, storage, URL mode, auth) is a config switch you can change later without touching code (see \"Changing drivers later\"). Shape ┌─────────────────────────┐ client ──▶│ identity -aware proxy/IAP │ terminates TLS, asserts identity └────────────┬────────────┘ ▼ canvas- drop app ── Postgres └ S3-compatible storage The app must not be reachable directly; only the proxy talks to it. The proxy must overwrite (never append to) the identity headers it forwards. What the reverse proxy must do Whichever proxy you run (an IAP, Caddy, nginx, a tunnel), the app needs these behaviors from it: Preserve Host . In subdomain mode the app picks the canvas from the Host header ( {slug}.{baseHost} ); the base host itself routes by path. Caddy forwards Host by default; nginx needs proxy_set_header Host $host . Pass WebSocket upgrades. The realtime primitive is a WebSocket on the same port as everything else. Allow large request bodies on deploy routes. The app accepts up to 110 MB on PUT /v1/canvases/{id}/deploy and the dashboard deploy routes, and 26 MB per staged blob ( PUT /v1/canvases/{id}/uploads/{uploadId}/blobs/{hash} ). It answers 413 above those limits itself; a proxy with a lower cap fails deploys first. Forward Authorization untouched on /v1/canvases/* and /mcp , and route those paths around the IAP's login (see \"Agents and CI behind an IAP\" below). Set CANVAS_DROP_BASE_URL to the public https:// origin. The app builds every absolute URL (canvas URLs, the OIDC redirect URI, MCP discovery metadata) from that variable and never reads X-Forwarded-Proto , so the plain-HTTP hop between proxy and app is fine. Give it 5 seconds to stop. On SIGTERM the app closes WebSockets, drops idle keep-alive connections, force-closes after 5 s, flushes the audit log, and closes the database. Set your stop grace period to at least that. Auth at the edge In proxy mode the app is sessionless: the IAP owns the session, and the app re-derives identity on every request from what the proxy forwards, so a proxy-side logout takes effect on the next request. Pick exactly one trust path. They do not compose. JWT / JWKS (preferred, cryptographic). The proxy forwards a signed JWT (default header Cf-Access-Jwt-Assertion ; set CANVAS_DROP_AUTH_PROXY_JWT_HEADER to change it). Set CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL , CANVAS_DROP_AUTH_PROXY_JWT_ISSUER , and CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE ; boot refuses a JWKS URL without both issuer and audience. The app checks the signature against the JWKS plus iss , aud , and expiry, and takes the identity from the token's email claim (a token without one resolves to anonymous). When a JWKS URL is set, plain identity headers are never honored: a stray X-Auth-Request-Email without a valid JWT resolves to anonymous and is logged. Trusted header (only when no JWKS URL is set). The app trusts the forwarded email header (default X-Auth-Request-Email ; display name from X-Auth-Request-Preferred-Username ) only when the request's TCP peer IP is listed in CANVAS_DROP_TRUSTED_PROXY_IPS (CSV of IPv4 addresses or CIDRs). The peer IP is the socket address, never a forwarded header. Each entry is validated at boot: /0 , malformed entries, and IPv6 are rejected, so \"trust everything\" cannot be configured. A header from any other peer is ignored and logged. Proxy mode refuses to boot without a JWKS URL or a trusted-proxy IP list. In proxy and oidc modes you must also set CANVAS_DROP_ALLOWED_EMAIL_DOMAINS (one or more domains; CSV, lowercased), which is enforced on every request, and a CANVAS_DROP_SESSION_SECRET of at least 32 characters (also required whenever NODE_ENV=production , which the Docker image sets). A request that yields no identity gets 401 { \"error\": \"unauthorized\" } in proxy mode; the IAP is expected to have bounced it first. The Security model covers the invariants behind these rules. Running real auth ( proxy or oidc ) in path URL mode also requires CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true , because path mode puts every canvas on one browser origin. Set it only when you accept that tradeoff. The demo compose stack does, since subdomain cannot boot on localhost. subdomain mode needs no opt-in. The third mode, dev , auto-signs-in a fixed local user with no verification and is rejected at boot when NODE_ENV=production . Agents and CI behind an IAP Two surfaces authenticate themselves and never carry the IAP's browser session: The Deploy API , /v1/canvases/* , authenticates with a per-canvas secret key ( Authorization: Bearer cd_... ). The MCP endpoint , /mcp , authenticates with an OAuth 2.1 bearer token issued by the app itself. Clients discover the endpoints at /.well-known/oauth-authorization-server and /.well-known/oauth-protected-resource , register at /register , and exchange codes at /token ( /revoke revokes). Only the /authorize step is a browser round-trip, and it does go through the IAP. Configure the IAP to let /v1/canvases/* , /mcp , /.well-known/* , /register , /token , and /revoke through unauthenticated and to forward Authorization unchanged; the app rejects bad or missing tokens with 401 . In subdomain mode a common layout is a dedicated API host: set CANVAS_DROP_API_BASE_URL (for example https://api.canvases.example.com ) so the MCP tools advertise the right endpoints to agents; api , v1 , sdk , auth , and mcp are reserved slugs, so no canvas can collide with it. The demo Caddyfile strips Authorization on purpose, which is why the Deploy API is unreachable in the demo. Two related facts about anonymous traffic. GET /healthz is unauthenticated at the app; the container health check calls it from inside, so expose it externally only if a monitor needs it. And a Public link canvas is only public if the IAP lets anonymous requests reach the app: the app serves that rung (static files only) to a request with no identity, and nothing else. Behind an IAP that demands sign-in on every host, public links land on the IAP's login page. Without an identity-aware proxy: oidc If you do not run an IAP, use the built-in oidc mode and point it at your OpenID provider. Run it in subdomain mode so you keep per-canvas origin isolation without standing up an IAP; a plain TLS-terminating proxy in front is enough. CANVAS_DROP_URL_MODE =subdomain CANVAS_DROP_BASE_URL =https://canvases.example.com CANVAS_DROP_AUTH_MODE =oidc CANVAS_DROP_OIDC_ISSUER =https://accounts.example.com CANVAS_DROP_OIDC_CLIENT_ID =... CANVAS_DROP_OIDC_CLIENT_SECRET =... CANVAS_DROP_ALLOWED_EMAIL_DOMAINS =example.com CANVAS_DROP_SESSION_SECRET =... # >= 32 chars; openssl rand -hex 32 Register {base}/auth/callback as the redirect URI at your provider. The app runs Authorization Code + PKCE ( S256 ) with scope=openid email profile , requires an email claim, rejects a token whose email_verified is false , and issues its own session cookie ( __canvasdrop_session : HttpOnly , Secure in production, SameSite=Lax , scoped to .{baseHost} in subdomain mode and host-only in path mode, 14-day rolling expiry). A request with no session is redirected to /auth/login?returnTo=... ; /auth/logout revokes the local session only. Every rejected sign-in is audited as auth_denied with its reason. oidc mode also honors CANVAS_DROP_TRUSTED_PROXY_IPS (and CANVAS_DROP_CLIENT_IP_HEADER ) so login throttling and audit rows key on the real client IP behind your proxy; in this mode those settings never assert identity. See Behind a CDN . Run with Docker There is no published image; build it from the repo. The Dockerfile is multi-stage on node:24-slim : a builder stage compiles the workspace (it needs python3 , make , and g++ for the SQLite driver), and a runtime stage carries no compilers and runs as a dedicated non-root canvasdrop user (uid/gid 1001). docker build -t canvas-drop . The image's operational contract: Entry: node --conditions=node-dist apps/server/dist/index.js , with NODE_ENV=production , CANVAS_DROP_PORT=3000 ( EXPOSE 3000 ), and CANVAS_DROP_DASHBOARD_DIST=/app/apps/dashboard/dist preset. Because NODE_ENV is production , dev auth is refused; configure proxy or oidc . State: VOLUME /data , pre-created and owned by the non-root user. In-image defaults: CANVAS_DROP_SQLITE_PATH=/data/canvasdrop.db and CANVAS_DROP_STORAGE_PATH=/data/storage . Mount a volume there on the SQLite + local-storage profile; on Postgres + S3 the container holds no state. Health: HEALTHCHECK fetches http://127.0.0.1:3000/healthz every 15 s (5 s timeout, 5 retries) with a 60 s start period, which covers Postgres coming up and migrations running on a cold start. Wire the same URL into your orchestrator's readiness probe. Config: pass CANVAS_DROP_* as container environment ( --env-file or your orchestrator's secrets). The image reads no .env . Screenshots (optional): the default image has no browser. Build with --build-arg SCREENSHOTS=1 to add Chromium (about 300 MB), then run with CANVAS_DROP_SCREENSHOTS=on and flip the admin toggle. See Screenshots . The compose stack docker-compose.yml boots the whole production shape with no external setup. It is a demo of the wiring, not a production deployment as-is. Service Image Role caddy caddy:2-alpine Edge proxy and the only service with a published port ( 8080 ). Routes /dex/* to Dex and everything else to oauth2-proxy, and strips client-supplied X-Forwarded-Access-Token , X-Auth-Request-Email , X-Auth-Request-User , X-Auth-Request-Preferred-Username , and Authorization headers on the way in. Plain HTTP for the demo; in production it would terminate TLS. oauth2-proxy quay.io/oauth2-proxy/oauth2-proxy:v7.6.0 The identity-aware proxy. Signs users in against Dex and forwards the Dex-signed access token to the app in X-Forwarded-Access-Token . dex dexidp/dex:v2.41.1 Bundled demo IdP with one static user, demo@example.com / canvasdrop . app built from the repo as canvas-drop:dev canvas-drop in real proxy mode on the JWKS path: CANVAS_DROP_AUTH_PROXY_JWT_HEADER=X-Forwarded-Access-Token , CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL=http://dex:5556/dex/keys , issuer http://localhost:8080/dex , audience canvas-drop-demo . path URL mode with CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true , Postgres, local storage on the app-data volume, plus a backups volume. No published port; waits for the Postgres health check. postgres postgres:16-alpine The database, on the pg-data volume, with a pg_isready health check. minio minio/minio:RELEASE.2024-11-07T00-52-20Z Optional S3-compatible storage behind --profile minio . Starting it does not switch the app to S3; edit the app service's environment: block as shown on Install , and create the bucket yourself. The app verifies a Dex-signed JWT against Dex's JWKS, the same cryptographic trust path you run in production. The smoke test boots the stack and asserts the launch invariants: the app reports healthy, the app has no host port, an unauthenticated request is redirected, a forged X-Forwarded-Access-Token / X-Auth-Request-Email pair is still redirected, a real Dex login resolves demo@example.com with authMode: proxy , and the same user id survives docker compose restart app postgres . ./scripts/compose-smoke.sh # leaves the stack running KEEP_UP=0 ./scripts/compose-smoke.sh # tears it down (docker compose down -v) Every Dex and oauth2-proxy secret under docker/ is a labeled demo placeholder, and the stack runs plain HTTP in path mode. Work the checklist below before anyone but you can reach it. The compose file also carries a commented-out maintenance service: a supercronic sidecar on the same image and volumes that runs docker/maintenance.cron , a nightly backup at 03:15 UTC (tarred, 14-day retention, the prune runs only after a good new backup exists) and purge 30 at 03:45 UTC on Sundays. Uncomment it, keep its CANVAS_DROP_DB / storage variables in step with the app service, and start it with docker compose --profile maintenance up -d . Graduating to a real IdP Moving off the bundled IdP is configuration, not code, but each step matters: Point oauth2-proxy (or your own IAP) at your real provider, and set CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL , CANVAS_DROP_AUTH_PROXY_JWT_ISSUER , and CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE to match the tokens it forwards. Confirm the forwarded JWT carries the verified email in its email claim; a token without one resolves to anonymous. Set CANVAS_DROP_ALLOWED_EMAIL_DOMAINS to your real domains and CANVAS_DROP_ADMIN_EMAILS to your bootstrap admins. Switch to CANVAS_DROP_URL_MODE=subdomain once wildcard DNS and a wildcard certificate exist, and drop CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE . Rotate every demo secret, including CANVAS_DROP_SESSION_SECRET ; set cookie_secure=true at oauth2-proxy once TLS terminates at the edge. If agents or CI will deploy, stop stripping Authorization for /v1/canvases/* and /mcp and exempt those paths from the IAP's login. Re-run the forged-token check against the new wiring. Running the bare process To run without Docker, build the workspace and start the compiled server. Node 24 or newer and pnpm 11 ( corepack enable picks up the pinned version) are required. pnpm install --frozen-lockfile pnpm build node --conditions=node-dist apps/server/dist/index.js pnpm build compiles the shared package to dist/ first; the node-dist export condition makes @canvas-drop/shared resolve to that compiled JS, so production runs without tsx . Supply configuration through the process manager, not a .env file; only pnpm dev reads .env . A systemd unit with EnvironmentFile= and a TLS proxy in front (Caddy, nginx, a tunnel) is enough for a small instance on one box: the app binds a local port (default 3000 ) and the proxy reverse-proxies to it under the contract above. If the port is already bound, the process prints a hint and exits 1 rather than retrying. Health, boot, and upgrades GET /healthz pings the database and answers {\"status\":\"ok\",\"db\":\"ok\",\"version\":...} with 200 , or {\"status\":\"degraded\",\"db\":\"error\",...} with 503 when the database is unreachable. At boot the process validates its configuration first and exits 1 with a message naming every invalid variable, then connects to the database, runs pending migrations, opens storage, and only then opens the port. A passing health check therefore means the database is reachable and the schema is current. Migrations run at boot in every mode; there is no separate migrate command. To upgrade, take a backup, replace the image or the built tree, and restart. Migrations are written to be additive, so an existing database is not rewritten in place; the backup is your rollback. Rate-limit counters (fixed 60 s windows, CANVAS_DROP_RATELIMIT_* ) and realtime channels live in process memory, which is why the profile above is one process per instance. Backups and maintenance The server binary doubles as the ops tool. The same image, the same config, no extra tooling: BIN= \"node --conditions=node-dist apps/server/dist/index.js\" $BIN backup /backups/$( date -u +%Y%m%dT%H%M%SZ) # whole instance: every table + every blob $BIN restore /backups/20260620T031500Z # into an empty instance; --force to overwrite $BIN purge 30 # reclaim canvases deleted 30+ days ago; add dry-run to preview A backup is a self-describing directory ( meta.json , db/<table>.ndjson , blobs/<key> ) written through the database and storage interfaces, so it is driver-agnostic: a backup taken on SQLite + local disk restores into Postgres + S3 and vice versa. That makes backup then restore the supported way to migrate between drivers. backup runs pending migrations before it reads. restore refuses a non-empty database without --force and verifies row counts, blob count and bytes, and every blob's SHA-256 before writing anything. A backup is as sensitive as the database: it is a cleartext export of credential material (password and API-key hashes, OAuth client secrets, session and MCP-token rows) and personal data (allowed emails, audit-log actor IPs). Keep it on a separate volume, restrict it to the app user, and encrypt it before it leaves the host. Run a restore drill into a throwaway instance periodically; a green restore is the only proof a backup is real. purge [days] reclaims the storage and version rows of canvases soft-deleted longer ago than days , then prunes usage_events and ai_usage rows older than 90 days. A sensible schedule is a nightly backup with 14-day local retention and a weekly purge 30 , either from a host crontab or the compose maintenance sidecar. The full runbook, including the crontab lines and the restore drill, is docs/ops.md in the repo. Logs Structured JSON to stdout via pino: no app-side files, rotation, or shipping. Tune with LOG_LEVEL (default info ) and LOG_FORMAT ( json when NODE_ENV=production , otherwise pretty ). Each request gets a correlation ID, taken from an inbound X-Correlation-ID or X-Request-Id header or generated, and echoed back as X-Correlation-ID . /healthz is excluded from request logging. Error tracking is off unless you set CANVAS_DROP_SENTRY_DSN . There is no telemetry or phone-home. Changing drivers later Start small and change env, never code: Move Set Local disk to object storage CANVAS_DROP_STORAGE=s3 plus CANVAS_DROP_S3_BUCKET , CANVAS_DROP_S3_REGION , CANVAS_DROP_S3_ACCESS_KEY , CANVAS_DROP_S3_SECRET_KEY ; CANVAS_DROP_S3_ENDPOINT for MinIO or R2 ( CANVAS_DROP_S3_FORCE_PATH_STYLE defaults to true ). SQLite to Postgres CANVAS_DROP_DB=postgres plus CANVAS_DROP_DATABASE_URL . Built-in login to an IAP Put a JWT-issuing identity-aware proxy in front and set CANVAS_DROP_AUTH_MODE=proxy with the JWKS variables above. A CDN in front See Behind a CDN : trusted-proxy IPs, the client-IP header, a cache rule that bypasses on the session cookie, and CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL . Switching a driver changes where new data goes; to carry existing data across, take a backup on the old drivers and restore it on the new ones. See Configuration for the full env surface."
  },
  {
    "path": "self-hosting/cdn",
    "section": "Self-hosting",
    "title": "Behind a CDN",
    "html": "<h1 id=\"behind-a-cdn\">Behind a CDN</h1>\n<p>canvas-drop runs happily behind a CDN or shared reverse-proxy (Cloudflare, Fastly,\nFly, a plain nginx/Caddy edge). Identity is cookie/OIDC-based, so a CDN hop never\nweakens auth — but a <strong>shared cache</strong> in front of the app changes three things you must\nconfigure: which responses may be cached, who the \"client\" is, and how long a restricted\ncanvas can linger at the edge. This page covers all three.</p>\n<blockquote>\n<p><strong>TL;DR.</strong> Set <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code> to your CDN's egress, set\n<code>CANVAS_DROP_CLIENT_IP_HEADER</code> to your CDN's real-client header, review\n<code>CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL</code> (default 300s), and add a CDN cache rule that\n<strong>only caches HTML when there is no session cookie.</strong></p>\n</blockquote>\n<h2 id=\"1-what-is-safe-to-cache\">1. What is safe to cache</h2>\n<p>The app emits access-aware <code>Cache-Control</code> on every canvas response, so you don't have\nto reason about it per canvas — just honor the origin headers:</p>\n<table>\n<thead>\n<tr>\n<th>Response</th>\n<th><code>Cache-Control</code></th>\n<th>A shared CDN may…</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>HTML of a <strong>public</strong> canvas (<code>public_link</code>, no password)</td>\n<td><code>public, max-age=0, s-maxage=&lt;TTL&gt;</code></td>\n<td>cache at the edge for the TTL; the browser still revalidates</td>\n</tr>\n<tr>\n<td>HTML of any <strong>auth-gated</strong> canvas (Restricted / Whole org / password)</td>\n<td><code>private, no-cache</code></td>\n<td><strong>never</strong> store it</td>\n</tr>\n<tr>\n<td>Content-hashed asset (<code>app.a1b2c3d4.js</code>) of a <strong>public</strong> canvas</td>\n<td><code>public, max-age=1y, immutable</code></td>\n<td>cache forever</td>\n</tr>\n<tr>\n<td>Content-hashed asset of an <strong>auth-gated</strong> canvas</td>\n<td><code>private, max-age=1y, immutable</code></td>\n<td><strong>never</strong> store it (the browser still caches)</td>\n</tr>\n</tbody></table>\n<p>Only the <strong><code>public_link</code>, no-password</strong> rung is reachable by an anonymous request, so\nit's the only rung marked <code>public</code>. Everything else is <code>private</code> so a shared cache can't\nserve one viewer's bytes to another. See <a href=\"../authoring/sharing\">Sharing &amp; access</a> for\nthe rungs and the <a href=\"security-model\">Security model</a> for the isolation guarantee.</p>\n<h3 id=\"cloudflare-specifics\">Cloudflare specifics</h3>\n<ul>\n<li>Cloudflare (like most CDNs) <strong>does not cache HTML by default</strong> — it caches a static\nextension allow-list. To get the offload benefit on public canvas HTML, add a <strong>Cache\nRule</strong> that caches the document, and scope it to <strong>bypass cache when a session cookie\nis present</strong> (<em>Cache eligibility → Bypass cache on cookie</em>, matching your session\ncookie name). That cookie-bypass is belt-and-suspenders on top of the <code>private</code>\nheaders: a logged-in member is never served a cached body, and an authenticated\nresponse never populates the edge.</li>\n<li><strong>Don't enable features that rewrite ETags.</strong> Some optimization/compression settings\nturn a strong <code>ETag</code> into a weak <code>W/\"…\"</code>. The app's conditional-GET path is\nweak-ETag-tolerant, so a <code>304</code> still works either way — but it's one less surprise.</li>\n</ul>\n<h2 id=\"2-restore-the-real-client-ip\">2. Restore the real client IP</h2>\n<p>Behind a CDN the socket peer is the CDN's edge, not your user. Left unconfigured,\n<strong>every request buckets under the CDN's IP</strong> — login throttling becomes collateral (one\nuser can rate-limit everyone on the same edge) and audit logs record CDN IPs.</p>\n<p>Two settings fix it. Both take effect <strong>only</strong> when the peer is a trusted hop, so a\ndirect caller can't spoof them:</p>\n<pre><code class=\"hljs language-sh\"><span class=\"hljs-comment\"># Your CDN's egress ranges (+ any local reverse proxy). IPv4/CIDR only; /0 is rejected.</span>\nCANVAS_DROP_TRUSTED_PROXY_IPS=173.245.48.0/20,103.21.244.0/22,127.0.0.1\n<span class=\"hljs-comment\"># The header your CDN puts the real client IP in. Org-agnostic — name yours:</span>\n<span class=\"hljs-comment\">#   Cloudflare: CF-Connecting-IP (or True-Client-IP on Enterprise)</span>\n<span class=\"hljs-comment\">#   Fastly/Akamai: True-Client-IP    Fly.io: Fly-Client-IP</span>\nCANVAS_DROP_CLIENT_IP_HEADER=True-Client-IP</code></pre>\n<p>Without <code>CANVAS_DROP_CLIENT_IP_HEADER</code>, the app falls back to the rightmost-untrusted\n<code>X-Forwarded-For</code> hop, which also works if your CDN appends a correct XFF. Keep the trust\nlist current — CDN egress ranges change.</p>\n<p>This is rate-limit and audit <strong>accuracy</strong>, not auth: the identity-trust gate keys off the\nsocket peer, never a header, so a misconfigured CDN can mis-bucket a rate limit but can\nnever bypass authentication.</p>\n<h2 id=\"3-the-access-downgrade-staleness-window\">3. The access-downgrade staleness window</h2>\n<p><code>CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL</code> (default <strong>300s</strong>) is the <code>s-maxage</code> on public HTML.\nIt is also, by definition, <strong>how long a CDN can keep showing a canvas after its owner\nmakes it Restricted</strong> — the cached public copy lives until it expires. The dashboard warns\nowners about this in plain language whenever they restrict a public canvas (the warning\nquotes this exact TTL), and the same advisory is returned by the MCP <code>update_canvas</code> tool\nfor agents.</p>\n<p>Tune to taste:</p>\n<ul>\n<li><strong>Lower</strong> (e.g. 60s) → near-instant access changes, less origin offload.</li>\n<li><strong>Higher</strong> (e.g. 600s) → more offload for popular canvases, longer exposure window.</li>\n<li><strong>0</strong> → disables shared caching (HTML stays <code>no-cache</code>); the warning is suppressed\nbecause there's nothing to be stale.</li>\n</ul>\n<p>For a <em>hard</em> cut-over (you restricted a canvas that must come down <strong>now</strong>), purge that\nURL at your CDN — the app can't reach into the edge cache for you.</p>\n<p>One caveat the warning's TTL figure doesn't capture: it describes the <strong>HTML page</strong>.\nContent-hashed sub-assets (<code>app.a1b2c3d4.js</code>) cached at the edge <em>while the canvas was\npublic</em> were sent with <code>public, immutable</code> and a one-year max-age, so a CDN won't\nrevalidate them until then. Their URLs include the content hash and aren't discoverable\nwithout the (now auth-gated, uncached) HTML, so the practical exposure stays the HTML\nwindow above — but a full hard cut-over should purge the whole canvas path, not just its\nroot, at the CDN.</p>\n<h2 id=\"4-websockets-and-streaming\">4. WebSockets and streaming</h2>\n<ul>\n<li><strong>Realtime</strong> is a real WebSocket upgrade. CDNs proxy WebSockets but drop <strong>idle</strong>\nconnections (Cloudflare ~100s). The realtime hub heartbeats well under that, so quiet\nchannels stay alive — just don't front it with a CDN that buffers or forbids upgrades.</li>\n<li><strong>The AI proxy</strong> streams Server-Sent Events (<code>text/event-stream</code>). CDNs stream rather\nthan buffer this, but make sure no optimization feature buffers the response, or\nfirst-token latency regresses.</li>\n</ul>\n<h2 id=\"5-upload-size\">5. Upload size</h2>\n<p>CDNs cap proxied request bodies (Cloudflare Free/Pro: <strong>100 MB</strong>). Canvas deploys POST\nfile bytes; large uploads through a proxied hostname can <code>413</code> at the edge. If that bites,\nroute the deploy/upload host DNS-only (un-proxied) or raise your CDN plan.</p>\n<h2 id=\"checklist\">Checklist</h2>\n<ul>\n<li> <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code> = CDN egress (+ local proxy)</li>\n<li> <code>CANVAS_DROP_CLIENT_IP_HEADER</code> = your CDN's real-client header</li>\n<li> <code>CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL</code> reviewed (default 300s)</li>\n<li> CDN cache rule for HTML <strong>bypasses cache on the session cookie</strong></li>\n<li> CDN ETag-rewriting / aggressive optimization left off</li>\n<li> WebSocket idle timeout &gt; hub heartbeat; SSE not buffered</li>\n<li> Upload host un-proxied or within the body-size limit</li>\n</ul>\n",
    "headings": [
      {
        "id": "1-what-is-safe-to-cache",
        "text": "1. What is safe to cache",
        "level": 2
      },
      {
        "id": "cloudflare-specifics",
        "text": "Cloudflare specifics",
        "level": 3
      },
      {
        "id": "2-restore-the-real-client-ip",
        "text": "2. Restore the real client IP",
        "level": 2
      },
      {
        "id": "3-the-access-downgrade-staleness-window",
        "text": "3. The access-downgrade staleness window",
        "level": 2
      },
      {
        "id": "4-websockets-and-streaming",
        "text": "4. WebSockets and streaming",
        "level": 2
      },
      {
        "id": "5-upload-size",
        "text": "5. Upload size",
        "level": 2
      },
      {
        "id": "checklist",
        "text": "Checklist",
        "level": 2
      }
    ],
    "text": "Behind a CDN canvas-drop runs happily behind a CDN or shared reverse-proxy (Cloudflare, Fastly, Fly, a plain nginx/Caddy edge). Identity is cookie/OIDC-based, so a CDN hop never weakens auth — but a shared cache in front of the app changes three things you must configure: which responses may be cached, who the \"client\" is, and how long a restricted canvas can linger at the edge. This page covers all three. TL;DR. Set CANVAS_DROP_TRUSTED_PROXY_IPS to your CDN's egress, set CANVAS_DROP_CLIENT_IP_HEADER to your CDN's real-client header, review CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL (default 300s), and add a CDN cache rule that only caches HTML when there is no session cookie. 1. What is safe to cache The app emits access-aware Cache-Control on every canvas response, so you don't have to reason about it per canvas — just honor the origin headers: Response Cache-Control A shared CDN may… HTML of a public canvas ( public_link , no password) public, max-age=0, s-maxage=<TTL> cache at the edge for the TTL; the browser still revalidates HTML of any auth-gated canvas (Restricted / Whole org / password) private, no-cache never store it Content-hashed asset ( app.a1b2c3d4.js ) of a public canvas public, max-age=1y, immutable cache forever Content-hashed asset of an auth-gated canvas private, max-age=1y, immutable never store it (the browser still caches) Only the public_link , no-password rung is reachable by an anonymous request, so it's the only rung marked public . Everything else is private so a shared cache can't serve one viewer's bytes to another. See Sharing & access for the rungs and the Security model for the isolation guarantee. Cloudflare specifics Cloudflare (like most CDNs) does not cache HTML by default — it caches a static extension allow-list. To get the offload benefit on public canvas HTML, add a Cache Rule that caches the document, and scope it to bypass cache when a session cookie is present ( Cache eligibility → Bypass cache on cookie , matching your session cookie name). That cookie-bypass is belt-and-suspenders on top of the private headers: a logged-in member is never served a cached body, and an authenticated response never populates the edge. Don't enable features that rewrite ETags. Some optimization/compression settings turn a strong ETag into a weak W/\"…\" . The app's conditional-GET path is weak-ETag-tolerant, so a 304 still works either way — but it's one less surprise. 2. Restore the real client IP Behind a CDN the socket peer is the CDN's edge, not your user. Left unconfigured, every request buckets under the CDN's IP — login throttling becomes collateral (one user can rate-limit everyone on the same edge) and audit logs record CDN IPs. Two settings fix it. Both take effect only when the peer is a trusted hop, so a direct caller can't spoof them: # Your CDN's egress ranges (+ any local reverse proxy). IPv4/CIDR only; /0 is rejected. CANVAS_DROP_TRUSTED_PROXY_IPS=173.245.48.0/20,103.21.244.0/22,127.0.0.1 # The header your CDN puts the real client IP in. Org-agnostic — name yours: # Cloudflare: CF-Connecting-IP (or True-Client-IP on Enterprise) # Fastly/Akamai: True-Client-IP Fly.io: Fly-Client-IP CANVAS_DROP_CLIENT_IP_HEADER=True-Client-IP Without CANVAS_DROP_CLIENT_IP_HEADER , the app falls back to the rightmost-untrusted X-Forwarded-For hop, which also works if your CDN appends a correct XFF. Keep the trust list current — CDN egress ranges change. This is rate-limit and audit accuracy , not auth: the identity-trust gate keys off the socket peer, never a header, so a misconfigured CDN can mis-bucket a rate limit but can never bypass authentication. 3. The access-downgrade staleness window CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL (default 300s ) is the s-maxage on public HTML. It is also, by definition, how long a CDN can keep showing a canvas after its owner makes it Restricted — the cached public copy lives until it expires. The dashboard warns owners about this in plain language whenever they restrict a public canvas (the warning quotes this exact TTL), and the same advisory is returned by the MCP update_canvas tool for agents. Tune to taste: Lower (e.g. 60s) → near-instant access changes, less origin offload. Higher (e.g. 600s) → more offload for popular canvases, longer exposure window. 0 → disables shared caching (HTML stays no-cache ); the warning is suppressed because there's nothing to be stale. For a hard cut-over (you restricted a canvas that must come down now ), purge that URL at your CDN — the app can't reach into the edge cache for you. One caveat the warning's TTL figure doesn't capture: it describes the HTML page . Content-hashed sub-assets ( app.a1b2c3d4.js ) cached at the edge while the canvas was public were sent with public, immutable and a one-year max-age, so a CDN won't revalidate them until then. Their URLs include the content hash and aren't discoverable without the (now auth-gated, uncached) HTML, so the practical exposure stays the HTML window above — but a full hard cut-over should purge the whole canvas path, not just its root, at the CDN. 4. WebSockets and streaming Realtime is a real WebSocket upgrade. CDNs proxy WebSockets but drop idle connections (Cloudflare ~100s). The realtime hub heartbeats well under that, so quiet channels stay alive — just don't front it with a CDN that buffers or forbids upgrades. The AI proxy streams Server-Sent Events ( text/event-stream ). CDNs stream rather than buffer this, but make sure no optimization feature buffers the response, or first-token latency regresses. 5. Upload size CDNs cap proxied request bodies (Cloudflare Free/Pro: 100 MB ). Canvas deploys POST file bytes; large uploads through a proxied hostname can 413 at the edge. If that bites, route the deploy/upload host DNS-only (un-proxied) or raise your CDN plan. Checklist CANVAS_DROP_TRUSTED_PROXY_IPS = CDN egress (+ local proxy) CANVAS_DROP_CLIENT_IP_HEADER = your CDN's real-client header CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL reviewed (default 300s) CDN cache rule for HTML bypasses cache on the session cookie CDN ETag-rewriting / aggressive optimization left off WebSocket idle timeout > hub heartbeat; SSE not buffered Upload host un-proxied or within the body-size limit"
  },
  {
    "path": "self-hosting/screenshots",
    "section": "Self-hosting",
    "title": "Screenshots",
    "html": "<h1 id=\"screenshots-canvas-previews\">Screenshots (canvas previews)</h1>\n<p>canvas-drop can capture a <strong>preview screenshot of each published canvas</strong> and reuse it\nas the gallery cover and — for public canvases — the link-unfurl (Open Graph) image.\nIt's an optional, off-by-default extra: when it's off, the product behaves exactly as\nit always has (gallery shows generative-art covers, links unfurl with the branded\n<code>/og.png</code>).</p>\n<p><strong>Where previews appear today:</strong> the <strong>gallery</strong> cover and the <strong>public-link OG</strong> image.\nThe capture itself is stored once per canvas as a private, access-gated asset, so the\nsame image can back additional surfaces (e.g. dashboard thumbnails) without re-capturing\n— those are wired in as the product decides to surface them.</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<ul>\n<li><strong>Captured on publish.</strong> Publishing a canvas (editor publish or any deploy) schedules\na capture. Capture is asynchronous — it never slows down or blocks a publish.</li>\n<li><strong>One preview per canvas.</strong> A canvas has exactly one current preview, stored as WebP\nrenditions (<code>og</code>, <code>card</code>, <code>thumb</code>). On the default <code>auto</code> preview mode it is\n<strong>overwritten</strong> on each republish, so the image always reflects the latest published\nversion. The owner can override this per canvas (see <em>Per-canvas control</em> below).</li>\n<li><strong>One browser, a small queue.</strong> A single headless Chromium runs in the server process\nand is reused across captures (a fresh, isolated browser context per job, recycled\nperiodically). Jobs are drained from a small database queue one at a time by default\n(<code>CANVAS_DROP_SCREENSHOTS_CONCURRENCY</code>), so a burst of publishes just queues up.</li>\n<li><strong>Access-gated by default.</strong> Previews are stored privately and served through an\n<strong>access-gated</strong> route: a canvas's preview is only served to someone already allowed\nto see that canvas. Restricted and otherwise gated canvas previews are <strong>never</strong> exposed publicly — a\nper-canvas OG image is emitted only for <code>public_link</code> canvases.</li>\n<li><strong>Capture is sandboxed.</strong> While a canvas is being captured, its backend primitives\n(AI, realtime, network) are neutered — a capture makes no AI spend and no outbound\nnetwork calls. Dialogs are dismissed and a hard timeout bounds a slow/looping canvas.</li>\n</ul>\n<h2 id=\"enablement-two-org-wide-layers\">Enablement — two org-wide layers</h2>\n<p>Auto-capture only runs when <strong>both</strong> org-wide layers say yes (each canvas owner can then\nfurther opt out or override per canvas — see <em>Per-canvas control</em> below):</p>\n<ol>\n<li><strong>Environment availability</strong> — <code>CANVAS_DROP_SCREENSHOTS</code> must be <code>on</code>, <strong>and</strong> the\nruntime image must actually contain Chromium (see below). This is the operator's\n\"is this capability available at all\" switch. Default: <code>off</code>.</li>\n<li><strong>Admin runtime toggle</strong> — even when available, an admin must turn it <strong>on</strong> in the\ndashboard (Admin → Configuration → <em>Screenshots enabled</em>). Default: <strong>off</strong>.</li>\n</ol>\n<p>When either layer is off, <strong>no browser is launched</strong> and auto previews are not served —\nthe gallery and link unfurls behave exactly as they do today. (An owner-uploaded custom\ncover is independent of this gate: it's served even when auto-capture is off.)</p>\n<h2 id=\"per-canvas-control\">Per-canvas control</h2>\n<p>Each canvas owner controls its cover from <strong>Settings → Preview</strong> (and over MCP via\n<code>update_canvas</code> <code>previewMode</code> / <code>set_canvas_preview</code>):</p>\n<ul>\n<li><strong>Automatic</strong> (<code>auto</code>, the default) — screenshot on publish, as described above; subject\nto the two org-wide layers.</li>\n<li><strong>Off</strong> (<code>off</code>) — never screenshot this canvas; it shows the generative cover.</li>\n<li><strong>Custom</strong> (<code>custom</code>) — the owner uploads a cover image. It's encoded into the same\n<code>og</code>/<code>card</code>/<code>thumb</code> renditions, <strong>pinned so a publish never overwrites it</strong>, and served\neven when the org-wide auto-capture pipeline is off. Removing it reverts to <code>auto</code>.</li>\n</ul>\n<h2 id=\"turning-it-on\">Turning it on</h2>\n<h3 id=\"1-build-an-image-that-includes-chromium\">1. Build an image that includes Chromium</h3>\n<p>The default image does <strong>not</strong> include Chromium (it would add ~300 MB, and the feature\nships disabled). Build with the opt-in build arg:</p>\n<pre><code class=\"hljs language-sh\">docker build --build-arg SCREENSHOTS=1 -t canvas-drop:screenshots .</code></pre>\n<p>This installs Chromium and its system libraries into the image. The default build\n(<code>SCREENSHOTS=0</code>) skips this entirely and stays lean.</p>\n<h3 id=\"2-make-it-available-in-the-environment\">2. Make it available in the environment</h3>\n<p>Set the env flag on the running container/instance:</p>\n<pre><code class=\"hljs language-sh\">CANVAS_DROP_SCREENSHOTS=on</code></pre>\n<p>Optional tuning (all have sensible defaults):</p>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_CONCURRENCY</code></td>\n<td><code>1</code></td>\n<td>Captures processed at once (one browser, N contexts)</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_TIMEOUT_MS</code></td>\n<td><code>20000</code></td>\n<td>Hard per-capture wall-clock timeout</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_RECYCLE_EVERY</code></td>\n<td><code>50</code></td>\n<td>Relaunch the browser after this many jobs</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_LEASE_MS</code></td>\n<td><code>120000</code></td>\n<td>Job lease (a crashed worker's job is reclaimed after this)</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_MAX_ATTEMPTS</code></td>\n<td><code>3</code></td>\n<td>Retries before a capture is marked failed</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_FAILED_TTL_MS</code></td>\n<td><code>86400000</code></td>\n<td>Failed-job rows reclaimed after this</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SCREENSHOTS_TOKEN_TTL_MS</code></td>\n<td><code>60000</code></td>\n<td>Lifetime of the internal capture credential</td>\n</tr>\n</tbody></table>\n<h3 id=\"3-flip-the-admin-toggle\">3. Flip the admin toggle</h3>\n<p>Sign in as an admin → <strong>Admin → Configuration</strong> → turn <strong>Screenshots enabled</strong> on.\nNew publishes start getting previews immediately; existing canvases get one on their\nnext publish.</p>\n<h2 id=\"turning-it-off\">Turning it off</h2>\n<p>Flip the admin toggle off (or unset <code>CANVAS_DROP_SCREENSHOTS</code>). Capture stops and\npreviews are no longer served — covers revert to generative art and unfurls to the\nbranded card. Stored previews are harmless and are reclaimed when a canvas is deleted.</p>\n<h2 id=\"memory-note\">Memory note</h2>\n<p>Chromium is memory-heavy. On a small single-VPS deployment, keep concurrency at <code>1</code>\n(the default) and rely on the per-job timeout and periodic browser recycle to bound\nmemory. Scale concurrency up only if the host has headroom.</p>\n",
    "headings": [
      {
        "id": "how-it-works",
        "text": "How it works",
        "level": 2
      },
      {
        "id": "enablement-two-org-wide-layers",
        "text": "Enablement — two org-wide layers",
        "level": 2
      },
      {
        "id": "per-canvas-control",
        "text": "Per-canvas control",
        "level": 2
      },
      {
        "id": "turning-it-on",
        "text": "Turning it on",
        "level": 2
      },
      {
        "id": "1-build-an-image-that-includes-chromium",
        "text": "1. Build an image that includes Chromium",
        "level": 3
      },
      {
        "id": "2-make-it-available-in-the-environment",
        "text": "2. Make it available in the environment",
        "level": 3
      },
      {
        "id": "3-flip-the-admin-toggle",
        "text": "3. Flip the admin toggle",
        "level": 3
      },
      {
        "id": "turning-it-off",
        "text": "Turning it off",
        "level": 2
      },
      {
        "id": "memory-note",
        "text": "Memory note",
        "level": 2
      }
    ],
    "text": "Screenshots (canvas previews) canvas-drop can capture a preview screenshot of each published canvas and reuse it as the gallery cover and — for public canvases — the link-unfurl (Open Graph) image. It's an optional, off-by-default extra: when it's off, the product behaves exactly as it always has (gallery shows generative-art covers, links unfurl with the branded /og.png ). Where previews appear today: the gallery cover and the public-link OG image. The capture itself is stored once per canvas as a private, access-gated asset, so the same image can back additional surfaces (e.g. dashboard thumbnails) without re-capturing — those are wired in as the product decides to surface them. How it works Captured on publish. Publishing a canvas (editor publish or any deploy) schedules a capture. Capture is asynchronous — it never slows down or blocks a publish. One preview per canvas. A canvas has exactly one current preview, stored as WebP renditions ( og , card , thumb ). On the default auto preview mode it is overwritten on each republish, so the image always reflects the latest published version. The owner can override this per canvas (see Per-canvas control below). One browser, a small queue. A single headless Chromium runs in the server process and is reused across captures (a fresh, isolated browser context per job, recycled periodically). Jobs are drained from a small database queue one at a time by default ( CANVAS_DROP_SCREENSHOTS_CONCURRENCY ), so a burst of publishes just queues up. Access-gated by default. Previews are stored privately and served through an access-gated route: a canvas's preview is only served to someone already allowed to see that canvas. Restricted and otherwise gated canvas previews are never exposed publicly — a per-canvas OG image is emitted only for public_link canvases. Capture is sandboxed. While a canvas is being captured, its backend primitives (AI, realtime, network) are neutered — a capture makes no AI spend and no outbound network calls. Dialogs are dismissed and a hard timeout bounds a slow/looping canvas. Enablement — two org-wide layers Auto-capture only runs when both org-wide layers say yes (each canvas owner can then further opt out or override per canvas — see Per-canvas control below): Environment availability — CANVAS_DROP_SCREENSHOTS must be on , and the runtime image must actually contain Chromium (see below). This is the operator's \"is this capability available at all\" switch. Default: off . Admin runtime toggle — even when available, an admin must turn it on in the dashboard (Admin → Configuration → Screenshots enabled ). Default: off . When either layer is off, no browser is launched and auto previews are not served — the gallery and link unfurls behave exactly as they do today. (An owner-uploaded custom cover is independent of this gate: it's served even when auto-capture is off.) Per-canvas control Each canvas owner controls its cover from Settings → Preview (and over MCP via update_canvas previewMode / set_canvas_preview ): Automatic ( auto , the default) — screenshot on publish, as described above; subject to the two org-wide layers. Off ( off ) — never screenshot this canvas; it shows the generative cover. Custom ( custom ) — the owner uploads a cover image. It's encoded into the same og / card / thumb renditions, pinned so a publish never overwrites it , and served even when the org-wide auto-capture pipeline is off. Removing it reverts to auto . Turning it on 1. Build an image that includes Chromium The default image does not include Chromium (it would add ~300 MB, and the feature ships disabled). Build with the opt-in build arg: docker build --build-arg SCREENSHOTS=1 -t canvas-drop:screenshots . This installs Chromium and its system libraries into the image. The default build ( SCREENSHOTS=0 ) skips this entirely and stays lean. 2. Make it available in the environment Set the env flag on the running container/instance: CANVAS_DROP_SCREENSHOTS=on Optional tuning (all have sensible defaults): Variable Default What it does CANVAS_DROP_SCREENSHOTS_CONCURRENCY 1 Captures processed at once (one browser, N contexts) CANVAS_DROP_SCREENSHOTS_TIMEOUT_MS 20000 Hard per-capture wall-clock timeout CANVAS_DROP_SCREENSHOTS_RECYCLE_EVERY 50 Relaunch the browser after this many jobs CANVAS_DROP_SCREENSHOTS_LEASE_MS 120000 Job lease (a crashed worker's job is reclaimed after this) CANVAS_DROP_SCREENSHOTS_MAX_ATTEMPTS 3 Retries before a capture is marked failed CANVAS_DROP_SCREENSHOTS_FAILED_TTL_MS 86400000 Failed-job rows reclaimed after this CANVAS_DROP_SCREENSHOTS_TOKEN_TTL_MS 60000 Lifetime of the internal capture credential 3. Flip the admin toggle Sign in as an admin → Admin → Configuration → turn Screenshots enabled on. New publishes start getting previews immediately; existing canvases get one on their next publish. Turning it off Flip the admin toggle off (or unset CANVAS_DROP_SCREENSHOTS ). Capture stops and previews are no longer served — covers revert to generative art and unfurls to the branded card. Stored previews are harmless and are reclaimed when a canvas is deleted. Memory note Chromium is memory-heavy. On a small single-VPS deployment, keep concurrency at 1 (the default) and rely on the per-job timeout and periodic browser recycle to bound memory. Scale concurrency up only if the host has headroom."
  },
  {
    "path": "agents/mcp",
    "section": "For agents",
    "title": "MCP server",
    "html": "<h1 id=\"mcp-server\">MCP server</h1>\n<p>If you are an agent host that speaks MCP, this page gets you connected to a canvas-drop\ninstance once and then creating, deploying, sharing, and editing canvases as the\nsigned-in account, with no per-canvas key to paste. MCP is the identity-scoped companion\nto the keyed <a href=\"/docs/api/deploy-api\">Deploy API</a>: the Deploy API acts on one canvas with\nits secret key; MCP acts across every canvas you own or edit, as you.</p>\n<h2 id=\"connect\">Connect</h2>\n<p>Add the instance's endpoint to your MCP client:</p>\n<pre><code class=\"hljs\">{base}/mcp</code></pre>\n<p>The transport is Streamable HTTP and stateless: every request is authenticated on its\nown, and there is no session to keep alive. The server identifies itself as\n<code>canvas-drop</code>, version <code>1</code>.</p>\n<p>A first session, as tool calls:</p>\n<pre><code class=\"hljs\"><span class=\"hljs-keyword\">whoami</span>           {}\n                 <span class=\"hljs-operator\">-&gt;</span> { <span class=\"hljs-built_in\">id</span>, email, name, orgs, teams, isGuest }\n<span class=\"hljs-keyword\">create_canvas</span>    { <span class=\"hljs-string\">\"title\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"Retro board\"</span> }\n                 <span class=\"hljs-operator\">-&gt;</span> { <span class=\"hljs-built_in\">id</span>, slug, url, apiKey, deploy, <span class=\"hljs-operator\">...</span> }        apiKey is returned once\n<span class=\"hljs-keyword\">deploy_canvas</span>    { <span class=\"hljs-string\">\"id\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"&lt;id&gt;\"</span>, <span class=\"hljs-string\">\"files\"</span><span class=\"hljs-operator\">:</span> [{ <span class=\"hljs-string\">\"path\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"index.html\"</span>, <span class=\"hljs-string\">\"content\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"&lt;h1&gt;Hi&lt;/h1&gt;\"</span> }] }\n                 <span class=\"hljs-operator\">-&gt;</span> { url, version<span class=\"hljs-operator\">:</span> 1, fileCount<span class=\"hljs-operator\">:</span> 1, totalBytes, warnings<span class=\"hljs-operator\">:</span> [] }\n<span class=\"hljs-keyword\">get_canvas_file</span>  { <span class=\"hljs-string\">\"id\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"&lt;id&gt;\"</span>, <span class=\"hljs-string\">\"path\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"index.html\"</span> }\n                 <span class=\"hljs-operator\">-&gt;</span> { version, path, size, mime, hash, encoding<span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"utf8\"</span>, content }</code></pre>\n<p>The canvas is live after the third call. A new canvas starts Restricted (the <code>private</code> API value), so\nits URL serves content only to you until you widen access with <code>update_canvas</code>.</p>\n<h3 id=\"sign-in-oauth-2-1\">Sign-in (OAuth 2.1)</h3>\n<p>canvas-drop is its own OAuth 2.1 authorization server; it does not proxy your identity\nprovider. A compliant client needs nothing but the URL:</p>\n<ol>\n<li><strong>Discovery.</strong> <code>{base}/.well-known/oauth-authorization-server</code> and\n<code>{base}/.well-known/oauth-protected-resource</code> (RFC 8414 and RFC 9728). The issuer is\n<code>{base}</code>; the only scope is <code>canvas-drop</code>.</li>\n<li><strong>Registration.</strong> <code>POST {base}/register</code> (Dynamic Client Registration).</li>\n<li><strong>Authorization.</strong> <code>{base}/authorize</code> resolves identity server-side with the same auth\nstrategy the dashboard uses, then checks the email allowlist and the blocked flag. In\n<code>oidc</code> mode a signed-out browser goes through the normal login and back; in <code>proxy</code>\nand <code>dev</code> mode identity is already present, and a request without it is denied. PKCE\n<code>S256</code> is required.</li>\n<li><strong>Tokens.</strong> <code>POST {base}/token</code> exchanges the single-use code (60 s lifetime; client\nid and redirect URI must match) for an access token (<code>token_type: \"bearer\"</code>,\n<code>expires_in: 3600</code>) and a refresh token. Refresh tokens rotate on every use.\n<code>POST {base}/revoke</code> revokes.</li>\n</ol>\n<p>Every tool call carries <code>Authorization: Bearer &lt;access_token&gt;</code>. Tokens are stored\nhashed. On each call the server looks the token up and re-checks that the account is\nstill active, so blocking a user or removing their email domain from the allowlist ends\na live token on the next request. A missing or rejected token answers\n<code>401 { \"error\": \"unauthorized\" }</code> with a <code>WWW-Authenticate: Bearer</code> header whose\n<code>resource_metadata</code> points at <code>{base}/.well-known/oauth-protected-resource</code>. Nothing the\nclient asserts about identity reaches the tools; org membership is resolved server-side\non every request. Sign-in and token events are audited (<code>mcp_authorize_ok</code>,\n<code>mcp_authorize_denied</code>, <code>mcp_token_issue</code>, <code>mcp_token_revoke</code>).</p>\n<h2 id=\"how-every-tool-behaves\">How every tool behaves</h2>\n<ul>\n<li><strong>Results.</strong> Success returns the JSON result as text content. Failure returns\n<code>CODE: message</code> as text with <code>isError: true</code>. Codes are stable; messages are for\nhumans.</li>\n<li><strong>Ids, not slugs.</strong> A canvas tool's <code>id</code> is the canvas id. A team tool's <code>id</code> is the\nteam id.</li>\n<li><strong>Scope.</strong> Canvas tools see the canvases you own or edit. A canvas you hold no role on,\nincluding one you can only view, reads as <code>canvas not found</code>: no existence leak, no\ncross-account management. Admins get no extra reach on this surface.</li>\n<li><strong>Roles.</strong> An editor can do everything the owner can except <code>delete_canvas</code>,\n<code>transfer_canvas</code>, and the guest-AI fields of <code>update_canvas</code> (<code>guestAiEnabled</code>,\n<code>guestAiCap</code>). Those answer <code>OWNER_ONLY: Only the canvas owner can do this.</code> for an\neditor. Roles are resolved on every call; a demotion or removal applies to your next\nrequest.</li>\n<li><strong>Disabled canvases.</strong> When an admin has disabled a canvas, read tools keep working and\nevery mutation answers <code>DISABLED: This canvas has been disabled by an administrator.</code>\n(followed by <code>Reason: …</code> when one was given), the same contract as the management\nAPI's <code>409 { \"code\": \"DISABLED\" }</code>. Only an admin can re-enable it.</li>\n<li><strong>Archived canvases.</strong> Deploy, publish, and rollback refuse with <code>NOT_ACTIVE: …</code>; call\n<code>unarchive_canvas</code> first.</li>\n<li><strong>Limits.</strong> Calls are rate-limited per account from the canvas-API bucket\n(<code>CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN</code>, default 120 per minute); over the limit\nanswers <code>429 { \"error\": \"rate_limited\" }</code> with <code>Retry-After</code>. A request body over\n110 MiB answers <code>413 { \"error\": \"payload_too_large\" }</code>.</li>\n<li><strong>Audit.</strong> Every mutation writes the same audit event as its dashboard equivalent.</li>\n</ul>\n<p>Refusal codes you will meet across tools:</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>OWNER_ONLY</code></td>\n<td>An editor called an owner-only act, or tried to change the <code>owner</code> entry.</td>\n</tr>\n<tr>\n<td><code>DISABLED</code></td>\n<td>Admin takedown; the canvas is read-only.</td>\n</tr>\n<tr>\n<td><code>NOT_ACTIVE</code></td>\n<td>The canvas is archived.</td>\n</tr>\n<tr>\n<td><code>GUEST_VIEWER_ONLY</code></td>\n<td><code>editor</code> was requested for a guest (an email outside the org); guests are always viewers.</td>\n</tr>\n<tr>\n<td><code>PUBLIC_LINK_OWNER_GATED</code></td>\n<td>You are an editor and the owner's account cannot publish public links; the entitlement follows the owner, whoever acts. The owner hears <code>PUBLIC_NOT_ALLOWED</code> for the same state.</td>\n</tr>\n<tr>\n<td><code>DRAFT_CONFLICT</code></td>\n<td>A stale draft write; see the draft tools.</td>\n</tr>\n<tr>\n<td><code>INVALID_REQUEST</code></td>\n<td>Mutually exclusive inputs were both supplied, or neither was.</td>\n</tr>\n</tbody></table>\n<h2 id=\"tools\">Tools</h2>\n<p>47 tools: 13 open to any signed-in account, 32 at minimum role editor, 2 owner-only.\nOptional inputs are marked <code>?</code>. \"View\" is the canvas projection described under Return\nshapes below.</p>\n<h3 id=\"account-lists-create\">Account, lists, create</h3>\n<p>Available to any signed-in account.</p>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Input</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>whoami</code></td>\n<td>none</td>\n<td><code>{id, email, name, orgs: [{id, name}], teams: [{id, name, slug, orgId}], isGuest}</code>. <code>orgs</code> is empty when no org boundary is configured; <code>isGuest</code> is true only when an org boundary exists and you belong to no org.</td>\n</tr>\n<tr>\n<td><code>list_canvases</code></td>\n<td><code>role?</code> (<code>owned</code> | <code>edited</code>), <code>access?</code> (<code>restricted</code> | <code>whole_org</code> | <code>public_link</code>, or a legacy value), <code>scope?</code> (<code>active</code> default | <code>archived</code>), <code>shared?</code>, <code>protected?</code>, <code>listed?</code>, <code>template?</code>, <code>undeployed?</code> (booleans), <code>query?</code>, <code>tags?</code> (string[]), <code>sort?</code> (<code>updated</code> default, <code>created</code>, <code>title</code>, <code>popular</code>), <code>limit?</code> (clamped to 1-100, default 50), <code>offset?</code> (clamped to 0 or greater, default 0)</td>\n<td><code>{total, limit, offset, summary, canvases: [View + {owner: {id, name, email}, role: \"owner\" | \"editor\", recentViews}]}</code>. <code>summary</code> is the filter-independent Your-canvases inventory: <code>{active, archived, shared, protected, listed, templates, neverDeployed, owned, edited}</code>. The five boolean state filters match the dashboard chips; <code>shared</code> means access extends beyond the people-and-teams list. <code>query</code> is a forgiving text filter over title, description, tags, and slug (case-, accent-, and whitespace-insensitive; words are AND-ed). <code>tags</code> matches canvases carrying any of the given tags. <code>popular</code> ranks by views in the last 30 days (<code>recentViews</code>). For a multi-page sweep that mutates canvases, use the stable <code>created</code> or <code>title</code> sort.</td>\n</tr>\n<tr>\n<td><code>list_shared_canvases</code></td>\n<td><code>query?</code>, <code>sort?</code> (<code>updated</code> default, <code>title</code>, <code>owner</code>), <code>limit?</code> (1-100, default 50), <code>offset?</code> (default 0)</td>\n<td><code>{total, limit, offset, canvases: [{id, slug, url, title, description, tags, access: {kind: \"direct\" | \"team\" | \"whole_org\", label, teamIds?, teamNames?}, hasPassword, hasPreview, owner: {id, name, avatarUrl} | null, createdAt, updatedAt}]}</code>. Canvases you can open but do not manage: anything you are on the people-and-teams list of (directly or through a team, at any <code>access</code> value), plus the Whole-org shares their owner listed. Display-only; open the <code>url</code>.</td>\n</tr>\n<tr>\n<td><code>create_canvas</code></td>\n<td><code>title?</code>, <code>description?</code>, <code>backendEnabled?</code>, <code>slug?</code> (≤63), <code>orgId?</code> (string or null)</td>\n<td>View + <code>apiKey</code> (the deploy key, returned once) + <code>deploy</code> (ready-to-run endpoints with the real key embedded, see Return shapes). <code>orgId</code> from <code>whoami.orgs</code> homes the canvas in that org so it can be shared org-wide; omit it for a personal canvas. <code>ORG_FORBIDDEN</code>, <code>INVALID_SLUG</code>, <code>SLUG_TAKEN</code>.</td>\n</tr>\n<tr>\n<td><code>clone_canvas</code></td>\n<td><code>id</code> (the source)</td>\n<td>View of the new canvas: an unpublished, Restricted draft with an empty direct-access list, a fresh slug and key, and backend off. Eligible sources: any active canvas you own or edit, a gallery-listed templatable canvas (org-scoped), or a published, active, unexpired, password-free canvas granted directly to you or to a team you belong to, at any General-access value. General access alone does not grant cloning; pending invitations and retained legacy guests cannot clone. A viewer's new copy remains theirs if the source grant is later revoked. Anything else reads <code>canvas not found</code>.</td>\n</tr>\n</tbody></table>\n<h3 id=\"read-a-canvas\">Read a canvas</h3>\n<p>Minimum role: editor. These keep working on a disabled canvas.</p>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Input</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>get_canvas</code></td>\n<td><code>id</code></td>\n<td>View + <code>owner</code>, <code>role</code>, <code>teamIds</code> (the team grants on the people-and-teams list, at any <code>access</code> value; <code>[]</code> when none), <code>ownerOnlyActs: [\"delete\", \"transfer\", \"guest_ai\"]</code>, and <code>deploy</code> with a <code>$CANVAS_KEY</code> placeholder (the key is never re-issued).</td>\n</tr>\n<tr>\n<td><code>list_versions</code></td>\n<td><code>id</code></td>\n<td><code>{versions: [{number, source, status, createdBy, createdByName, createdByEmail, createdAt, fileCount, totalBytes, current, downloadUrl}]}</code>. <code>downloadUrl</code> is <code>{base}/mcp/canvases/{id}/versions/{n}/download</code>, a ZIP of that version.</td>\n</tr>\n<tr>\n<td><code>get_canvas_file</code></td>\n<td><code>id</code>, <code>path?</code></td>\n<td>Without <code>path</code>: <code>{version, fileCount, files: [{path, size, mime, hash}]}</code> for the live version. With <code>path</code>: <code>{version, path, size, mime, hash, encoding: \"utf8\" | \"base64\", content}</code>. A file over 256 KiB returns <code>truncated: true</code> and a <code>note</code> instead of <code>content</code>; compare the <code>hash</code>. Fails with <code>this canvas has no live version yet</code> or <code>no file at \"…\"</code>.</td>\n</tr>\n<tr>\n<td><code>get_canvas_usage</code></td>\n<td><code>id</code></td>\n<td><code>{totalViews, uniqueViewers, lastViewedAt, viewsByDay, kvOps, fileOps, fileCount, fileBytes, aiCalls, aiTokens, aiCostUsd, realtimeConnects}</code>.</td>\n</tr>\n<tr>\n<td><code>list_canvas_connections</code></td>\n<td><code>id</code></td>\n<td><code>{connections: [{key, label, origin, allowedMethods, protectedHeaderNames, enabled, available, unavailableReason}]}</code>. This is sanitized authority metadata only: protected header values are never returned. A Connection works only while its live admin grant and profile remain enabled and the canvas backend is on.</td>\n</tr>\n<tr>\n<td><code>list_access</code></td>\n<td><code>id</code></td>\n<td><code>{entries: [{id, kind, role, email, name, userId, teamId, teamOrgId, createdAt}]}</code>: the owner first, then people, pending sign-in grants, and teams. <code>kind</code> is <code>owner</code>, <code>member</code>, <code>guest</code>, <code>pending</code>, or <code>team</code>; <code>role</code> is <code>owner</code>, <code>viewer</code>, or <code>editor</code>. Entry ids are stable (<code>owner</code>, <code>member:&lt;id&gt;</code>, <code>guest:&lt;id&gt;</code>, <code>pending:&lt;id&gt;</code>, <code>team:&lt;teamId&gt;</code>); pass them to <code>set_access_role</code> and <code>revoke_access</code>. When you are the owner the result also carries <code>transferCandidates: [{id, name, email}]</code>, the set <code>transfer_canvas</code> accepts.</td>\n</tr>\n<tr>\n<td><code>search_people</code></td>\n<td><code>context</code> (<code>canvas</code> | <code>team</code>), <code>canvasId?</code>, <code>teamId?</code>, <code>q</code> (1-80 chars)</td>\n<td><code>{people: [{id, email, name}]}</code>: the dashboard's Add person suggestions, scoped to a canvas you own or edit (<code>canvasId</code>) or a team you can see (<code>teamId</code>). <code>INVALID_REQUEST: missing canvasId or teamId for context</code> when the matching id is absent; a canvas you do not manage or a team you cannot see reads <code>not found</code>. Does not expose the admin People directory.</td>\n</tr>\n</tbody></table>\n<h3 id=\"deploy\">Deploy</h3>\n<p>Minimum role: editor. <code>deploy_canvas</code>, <code>begin_deploy</code>, and <code>finalize_deploy</code> also\nrequire an active canvas (<code>NOT_ACTIVE</code>); <code>add_files</code> needs only the open handle. Every\ndeploy goes live at once as a new immutable version. There is no draft step on this\npath; for a draft, use the editor tools below.</p>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Input</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>deploy_canvas</code></td>\n<td><code>id</code>, and exactly one of <code>zipBase64</code> or <code>files: [{path, content, encoding?: \"utf8\" | \"base64\"}]</code></td>\n<td><code>{url, version, fileCount, totalBytes, warnings: []}</code>. <code>INVALID_REQUEST</code> for both or neither; <code>empty deploy</code> for a zero-byte ZIP. Ingest failures use the Deploy API codes: <code>EMPTY_DEPLOY</code>, <code>TOO_MANY_FILES</code>, <code>FILE_TOO_LARGE</code>, <code>CANVAS_TOO_LARGE</code>, <code>INVALID_ZIP</code>, <code>INVALID_PATH</code>, <code>INVALID_ENCODING</code>, <code>ZIP_SLIP_REJECTED</code>, <code>ZIP_BOMB_REJECTED</code>.</td>\n</tr>\n<tr>\n<td><code>begin_deploy</code></td>\n<td><code>id</code>, <code>manifest: [{path, hash, size}]</code> (<code>hash</code> is the sha256 hex of the bytes)</td>\n<td><code>{uploadId, missingHashes}</code>: the blobs the server does not already hold. The handle lives 15 minutes. <code>INVALID_MANIFEST</code>.</td>\n</tr>\n<tr>\n<td><code>add_files</code></td>\n<td><code>id</code>, <code>uploadId</code>, <code>files: [{path, content, encoding?}]</code></td>\n<td><code>{staged: &lt;count&gt;}</code>. Call repeatedly to chunk. <code>UPLOAD_HANDLE_INVALID</code>, <code>UPLOAD_EXPIRED</code>, <code>UPLOAD_ALREADY_FINALIZED</code>, <code>UPLOAD_UNEXPECTED_BLOB</code>, <code>BLOB_HASH_MISMATCH</code>, <code>INVALID_ENCODING</code>.</td>\n</tr>\n<tr>\n<td><code>finalize_deploy</code></td>\n<td><code>id</code>, <code>uploadId</code></td>\n<td>Same result as <code>deploy_canvas</code>. Single-use. <code>UPLOAD_MISSING_BLOB</code> (stage it and retry), <code>UPLOAD_IN_PROGRESS</code> (a 60 s lease), <code>UPLOAD_ALREADY_FINALIZED</code>.</td>\n</tr>\n</tbody></table>\n<p>Limits: 100 MiB per canvas, 25 MiB per file, 2000 files. Read \"Which deploy tool to\nuse\" below before sending bytes through a tool call.</p>\n<h3 id=\"versions-and-lifecycle\">Versions and lifecycle</h3>\n<p>Minimum role: editor unless marked owner-only.</p>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Input</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>rollback_canvas</code></td>\n<td><code>id</code>, <code>version</code> (integer)</td>\n<td>View + <code>version</code>. The target must be a ready version (<code>no ready version N</code> otherwise); <code>NOT_ACTIVE</code> on an archived canvas; <code>that version was just removed; retry</code> if it was pruned mid-swap.</td>\n</tr>\n<tr>\n<td><code>unpublish_canvas</code></td>\n<td><code>id</code></td>\n<td><code>{url, publicationState: \"draft\", currentVersionId: null}</code>. Takes the canvas offline, drops live sockets, and revokes legacy guest grants. <code>CANNOT_UNPUBLISH: this canvas isn't published</code>.</td>\n</tr>\n<tr>\n<td><code>delete_version</code></td>\n<td><code>id</code>, <code>version</code> (integer &gt; 0)</td>\n<td><code>{ok: true, version}</code>. The current version is protected (<code>CURRENT_VERSION</code>); also <code>VERSION_NOT_FOUND</code>, <code>VERSION_UNAVAILABLE</code>. Blobs shared with other versions or the draft are retained.</td>\n</tr>\n<tr>\n<td><code>archive_canvas</code></td>\n<td><code>id</code></td>\n<td>View with <code>status: \"archived\"</code>. Reversible; takes the URL offline. <code>NOT_ACTIVE: only an active canvas can be archived</code>.</td>\n</tr>\n<tr>\n<td><code>unarchive_canvas</code></td>\n<td><code>id</code></td>\n<td>View + <code>owner</code>, <code>role</code>, with <code>status: \"active\"</code>. <code>NOT_ARCHIVED: canvas is not archived</code>.</td>\n</tr>\n<tr>\n<td><code>delete_canvas</code> (owner-only)</td>\n<td><code>id</code></td>\n<td><code>{ok: true}</code>. Soft-delete: the URL stops resolving and the canvas is purged after the retention window. Refused with <code>DISABLED</code> on a disabled canvas; <code>OWNER_ONLY</code> for an editor. Not reversible over MCP.</td>\n</tr>\n<tr>\n<td><code>transfer_canvas</code> (owner-only)</td>\n<td><code>id</code>, <code>toUserId</code> (a user id, never an email)</td>\n<td><code>{ok: true, canvas: View, previousOwnerEditor, publicLinkReverted}</code>. Instant: the recipient, an existing editor (see <code>transferCandidates</code>), becomes owner and you stay on as an editor; the public-link entitlement now follows their account, and <code>publicLinkReverted</code> tells you if a public link had to be turned off. A team cannot receive a canvas. <code>NOT_ELIGIBLE</code>, <code>TARGET_NOT_FOUND</code>, <code>TARGET_BLOCKED</code>, <code>TARGET_NOT_MEMBER</code>, <code>ALREADY_OWNER</code>, <code>SELF</code>, <code>CONFLICT</code>.</td>\n</tr>\n</tbody></table>\n<h3 id=\"settings\">Settings</h3>\n<p>Minimum role: editor.</p>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Input</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>update_canvas</code></td>\n<td><code>id</code> plus any of: <code>title</code> (≤200), <code>description</code> (≤2000; null clears), <code>access</code> (<code>private</code> | <code>whole_org</code> | <code>public_link</code>; the legacy <code>specific_people</code> / <code>team</code> are accepted as aliases of <code>private</code>), <code>discoverability</code> (<code>link_only</code> | <code>listed</code>), <code>teamIds</code> (string[], ≤50), <code>password</code> (null clears), <code>sharedExpiresAt</code> (Unix ms; null clears), <code>spaFallback</code>, <code>previewMode</code> (<code>auto</code> | <code>off</code>), <code>galleryListed</code>, <code>galleryTemplatable</code>, <code>tags</code> (≤20, each ≤50 chars), and the owner-only <code>guestAiEnabled</code>, <code>guestAiCap</code></td>\n<td>View + <code>owner</code>, <code>role</code>, <code>teamIds</code>, and sometimes <code>warning</code> (an edge-cache staleness notice when restricting a formerly public canvas; surface it to the user). Omitted fields are unchanged. Audits <code>password_change</code> and <code>share_change</code>.</td>\n</tr>\n<tr>\n<td><code>set_capabilities</code></td>\n<td><code>id</code>, <code>backendEnabled?</code>, <code>kv?</code>, <code>files?</code>, <code>ai?</code>, <code>realtime?</code>, <code>authoring?</code> (all booleans)</td>\n<td>View + <code>owner</code>, <code>role</code>. <code>backendEnabled</code> is the master switch; the others take effect only when it is on. <code>authoring</code> also needs the instance switch on. Omitted fields are unchanged; switching a capability off drops sockets that lost access.</td>\n</tr>\n<tr>\n<td><code>set_canvas_slug</code></td>\n<td><code>id</code>, <code>slug?</code> (≤63; omit for a fresh random slug)</td>\n<td>View + <code>deploy</code> (<code>$CANVAS_KEY</code> placeholder). The old URL stops resolving immediately. <code>INVALID_SLUG</code>, <code>SLUG_TAKEN</code>.</td>\n</tr>\n<tr>\n<td><code>set_canvas_preview</code></td>\n<td><code>id</code>, <code>image?</code> (base64 PNG, JPEG, or WebP; the string ≤40 MiB, decoded ≤25 MiB)</td>\n<td>View + <code>owner</code>, <code>role</code>. With <code>image</code>, <code>previewMode</code> becomes <code>custom</code> and a publish never overwrites the cover; without it, a custom cover is cleared back to <code>auto</code> (an auto-captured screenshot is left alone). <code>INVALID_IMAGE</code>, <code>IMAGE_TOO_LARGE</code>. For the auto/off toggle use <code>update_canvas</code> <code>previewMode</code>.</td>\n</tr>\n<tr>\n<td><code>regenerate_deploy_key</code></td>\n<td><code>id</code></td>\n<td><code>{apiKey, deploy}</code>. Mints a new <code>cd_…</code> key (returned once, embedded in <code>deploy.curl</code>) and invalidates the old one. An editor may rotate it; the owner is emailed naming the actor.</td>\n</tr>\n</tbody></table>\n<p>Notes on <code>update_canvas</code>:</p>\n<ul>\n<li>The people-and-teams list (<code>grant_access</code> / <code>revoke_access</code>) applies at every <code>access</code>\nvalue; <code>access</code> only says who else may open the canvas. <code>whole_org</code> and <code>public_link</code>\nneed a published canvas: active with a live version (<code>SHARE_REQUIRES_PUBLISH</code>). <code>public_link</code> also needs the instance switch on\n(<code>PUBLIC_LINKS_DISABLED</code>) and an owner whose account may publish public links\n(<code>PUBLIC_NOT_ALLOWED</code> to the owner, <code>PUBLIC_LINK_OWNER_GATED</code> to an editor). When an\norg boundary is configured, <code>whole_org</code> needs a canvas homed in an org\n(<code>ORG_REQUIRED</code>); pass <code>orgId</code> at <code>create_canvas</code>.</li>\n<li>To share with a team, <code>grant_access</code> it with <code>teamId</code> (and a <code>role</code>). The legacy\n<code>teamIds</code> field replaces the viewer-team grants on the list (at least one team you\nbelong to; personal teams fit any canvas you own, org teams must match the canvas's org:\n<code>TEAM_REQUIRED</code>, <code>TEAM_FORBIDDEN</code>). Changing <code>access</code> never touches team grants.</li>\n<li><code>discoverability</code> controls only whether a Whole-org canvas appears in Shared for the\nwhole org; people and teams on the list always see it there. It never widens URL access.\nOff <code>whole_org</code> it is pinned to <code>link_only</code> on every write (a legacy <code>team</code> row that stored\n<code>listed</code> reads <code>listed</code> until its next settings write).</li>\n<li>Legacy shape: <code>teamIds: []</code> sent by a client that sees the canvas on the legacy <code>team</code>\nvalue, together with an <code>access</code> change to another value, is a no-op (the grants stay on\nthe list); <code>teamIds: []</code> on its own, or with any other <code>access</code> value, is refused\n(<code>TEAM_REQUIRED</code>). Setting <code>galleryListed: true</code> on a Whole-org canvas also\nsets <code>discoverability: \"listed\"</code>; passing <code>link_only</code> in the same call is refused\n(<code>DISCOVERY_CONFLICT</code>).</li>\n<li>Gallery listing needs a shared, published, password-free canvas on <code>public_link</code> or a\nlisted <code>whole_org</code> (<code>NOT_SHARED</code>, <code>NOT_PUBLISHED</code>, <code>PASSWORD_PROTECTED</code>,\n<code>NOT_GALLERY_ELIGIBLE</code>). <code>galleryTemplatable: true</code> needs the canvas listed first\n(<code>NOT_LISTED</code>).</li>\n<li>The people-and-teams list itself is managed with <code>grant_access</code>, <code>set_access_role</code>, and <code>revoke_access</code>.</li>\n</ul>\n<h3 id=\"sharing-and-people\">Sharing and people</h3>\n<p>Minimum role: editor.</p>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Input</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>grant_access</code></td>\n<td><code>id</code>, exactly one of <code>email</code> or <code>teamId</code>, <code>role?</code> (<code>viewer</code> default | <code>editor</code>)</td>\n<td>Person: <code>{ok: true, status: \"granted\" | \"pending\" | \"role_changed\" | \"already_added\" | \"already_pending\", role, emailDelivery?}</code>. An existing user is granted now; an admissible new email becomes a pending sign-in grant that carries the role; passing <code>role</code> for someone already listed updates it, and omitting it never changes an existing entry. Team: <code>{ok: true, status: \"granted\" | \"role_changed\" | \"already_added\", role, from}</code>. Only org members and teams can be editors (<code>GUEST_VIEWER_ONLY</code>). Every grant opens the canvas at once, whatever <code>access</code> (General access) says; an editor also manages it. Also <code>INVALID_REQUEST: pass exactly one of email or teamId</code>, <code>NOT_PERMITTED</code>, <code>AUTH_ADMISSION_REQUIRED</code>, <code>BLOCKED</code>, <code>RATE_LIMITED</code>, <code>TEAM_FORBIDDEN</code>, <code>EMAIL_NOT_CONFIGURED</code>.</td>\n</tr>\n<tr>\n<td><code>invite_to_canvas</code></td>\n<td><code>id</code>, <code>email</code>, <code>role?</code></td>\n<td>The same person result as <code>grant_access</code>, through the same Add person service, and it sends the access email. A brand-new external email is refused for a non-admin unless the instance allows it (<code>NOT_PERMITTED</code>); <code>RATE_LIMITED</code> past the cap.</td>\n</tr>\n<tr>\n<td><code>revoke_access</code></td>\n<td><code>id</code>, <code>entryId</code> (from <code>list_access</code>)</td>\n<td><code>{ok: true}</code>. Removes a person (another editor, or yourself), a pending grant, a legacy guest row, or a team grant; sockets the entry no longer permits are dropped. The <code>owner</code> entry refuses (<code>OWNER_ONLY</code>); an unknown id reads <code>access entry not found</code>.</td>\n</tr>\n<tr>\n<td><code>set_access_role</code></td>\n<td><code>id</code>, <code>entryId</code>, <code>role</code> (<code>viewer</code> | <code>editor</code>)</td>\n<td><code>{ok: true}</code>. A guest can only be a viewer (<code>GUEST_VIEWER_ONLY</code>); the <code>owner</code> entry refuses (<code>OWNER_ONLY</code>; use <code>transfer_canvas</code>). Demoting drops the person's live editor sockets.</td>\n</tr>\n</tbody></table>\n<h3 id=\"draft-editor-loop\">Draft editor loop</h3>\n<p>Minimum role: editor. These mirror the browser editor: a per-canvas draft that\n<code>publish_draft</code> snapshots into a live version. DraftView is\n<code>{files: [{path, size, mime, hash, updatedBy, updatedByName, updatedAt}], stale, baseVersionId, updatedAt, dirty}</code>,\nwhere <code>dirty</code> means the draft differs from live and <code>stale</code> means live moved on since\nthe draft was based.</p>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Input</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>get_draft</code></td>\n<td><code>id</code></td>\n<td>DraftView. Created from the live version on first open. Works on a disabled canvas.</td>\n</tr>\n<tr>\n<td><code>read_draft_file</code></td>\n<td><code>id</code>, <code>path</code></td>\n<td><code>{path, encoding, content, hash, updatedBy, updatedByName, updatedAt}</code>. <code>no draft file at \"…\"</code>.</td>\n</tr>\n<tr>\n<td><code>write_draft_file</code></td>\n<td><code>id</code>, <code>path</code>, <code>content</code>, <code>encoding?</code> (<code>utf8</code> default | <code>base64</code>), <code>create?</code>, <code>expectedHash?</code></td>\n<td>DraftView. <code>create: true</code> refuses to overwrite (<code>PATH_EXISTS</code>). <code>expectedHash</code> is the <code>hash</code> you loaded, or the literal <code>none</code> for a path you believe absent; a mismatch fails with <code>DRAFT_CONFLICT: … (path= currentHash= updatedBy= updatedByName= updatedAt=)</code>: re-read and retry with the current hash. Without <code>expectedHash</code> the write still fails with <code>DRAFT_CONFLICT</code> if a different user wrote that file last. Two editors in different files never conflict.</td>\n</tr>\n<tr>\n<td><code>delete_draft_file</code></td>\n<td><code>id</code>, <code>path</code>, <code>expectedHash?</code></td>\n<td>DraftView.</td>\n</tr>\n<tr>\n<td><code>rename_draft_file</code></td>\n<td><code>id</code>, <code>from</code>, <code>to</code>, <code>expectedHash?</code> (checked on <code>from</code>)</td>\n<td>DraftView.</td>\n</tr>\n<tr>\n<td><code>publish_draft</code></td>\n<td><code>id</code></td>\n<td><code>{version, versionId, fileCount, totalBytes}</code>. <code>NOT_ACTIVE: unarchive this canvas before publishing</code>, <code>EMPTY_DEPLOY</code>, <code>DISABLED</code>.</td>\n</tr>\n<tr>\n<td><code>restore_draft</code></td>\n<td><code>id</code>, <code>version</code> (integer &gt; 0)</td>\n<td>DraftView, reset to that version's files.</td>\n</tr>\n</tbody></table>\n<h3 id=\"teams\">Teams</h3>\n<p>Available to any signed-in account; self-serve only, with no admin reach. Here <code>id</code> is\na team id.</p>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Input</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>list_teams</code></td>\n<td>none</td>\n<td><code>{teams: [{id, orgId, name, slug, mine, canManage}]}</code>. <code>orgId</code> is null for a personal team. <code>mine</code>: you belong to it. <code>canManage</code>: you created it, so you can rename or delete it.</td>\n</tr>\n<tr>\n<td><code>create_team</code></td>\n<td><code>orgId?</code> (string or null), <code>name</code> (1-80 chars)</td>\n<td><code>{id, orgId, name, slug}</code>. Omit <code>orgId</code> for a personal team; pass one from <code>whoami.orgs</code> to attach the team to that org. You become its first member and its manager.</td>\n</tr>\n<tr>\n<td><code>rename_team</code></td>\n<td><code>id</code>, <code>name</code> (1-80 chars)</td>\n<td><code>{id, name}</code>. Creator only (<code>FORBIDDEN</code>).</td>\n</tr>\n<tr>\n<td><code>delete_team</code></td>\n<td><code>id</code></td>\n<td><code>{ok: true}</code>. Creator only (<code>FORBIDDEN</code>).</td>\n</tr>\n<tr>\n<td><code>add_team_member</code></td>\n<td><code>id</code>, <code>email</code></td>\n<td><code>{status: \"granted\" | \"pending\", emailDelivery?}</code>. Any member may add; an org team takes same-org members only; a brand-new external email on a personal team is refused for a non-admin unless the instance allows it.</td>\n</tr>\n<tr>\n<td><code>remove_team_member</code></td>\n<td><code>id</code>, <code>userId</code></td>\n<td><code>{ok: true}</code>. Pass your own id to leave.</td>\n</tr>\n<tr>\n<td><code>cancel_team_invite</code></td>\n<td><code>id</code>, <code>inviteId</code> (a <code>pending</code> row id from <code>list_team_members</code>)</td>\n<td><code>{ok: true}</code>. A stale id reads <code>TARGET_NOT_FOUND</code>.</td>\n</tr>\n<tr>\n<td><code>list_team_members</code></td>\n<td><code>id</code></td>\n<td><code>{members: [{userId, email, name}], pending: [{id, email, invitedAt}]}</code>.</td>\n</tr>\n</tbody></table>\n<p>Team error codes: <code>NOT_A_MEMBER</code>, <code>TEAM_NOT_FOUND</code>, <code>TEAM_NAME_TAKEN</code>, <code>FORBIDDEN</code>,\n<code>TARGET_NOT_FOUND</code>, <code>TARGET_NOT_MEMBER</code>, <code>TARGET_NOT_PERMITTED</code>, <code>TARGET_BLOCKED</code>,\n<code>AUTH_ADMISSION_REQUIRED</code>, <code>RATE_LIMITED</code>.</p>\n<h2 id=\"return-shapes\">Return shapes</h2>\n<p><strong>View</strong>, the canvas projection every canvas tool echoes:</p>\n<pre><code class=\"hljs\">{ id, slug, url, ownerId, title, description, status, publicationState,\n<span class=\"hljs-built_in\">  currentVersionId,</span> access, accessMode, discoverability, hasPassword, sharedExpiresAt,\n<span class=\"hljs-built_in\">  spaFallback,</span> backendEnabled, disabledReason, galleryListed, galleryTemplatable,\n<span class=\"hljs-built_in\">  tags,</span> guestAiEnabled, guestAiCap, previewMode, viewCount, lastViewedAt,\n<span class=\"hljs-built_in\">  hasPreview,</span> previewUrl? }</code></pre>\n<p><code>publicationState</code> is <code>draft</code>, <code>published</code>, <code>archived</code>, <code>disabled</code>, or <code>deleted</code>.\n<code>accessMode</code> is the audience — <code>restricted</code> (<code>private</code> or a legacy alias: only the\npeople-and-teams list, which applies at every value), <code>whole_org</code>, or <code>public_link</code> — so an\nagent never has to know the three spellings of \"restricted\". <code>previewUrl</code> is present when a\npreview exists. Identity-bearing tools add <code>owner: {id, name, email} | null</code> and\n<code>role: \"owner\" | \"editor\"</code>; <code>teamIds</code> lists the granted teams at any <code>access</code> value. The\npassword hash and the API key are never included. <code>list_canvases</code> also returns <code>limit</code>,\n<code>offset</code>, and the filter-independent <code>summary</code> counts shown on the dashboard; these describe\nthe whole owned-or-edited inventory even when the returned page is filtered.</p>\n<p><strong>deploy</strong>, returned by <code>create_canvas</code>, <code>get_canvas</code>, <code>set_canvas_slug</code>, and\n<code>regenerate_deploy_key</code>: the exact keyed <a href=\"/docs/api/deploy-api\">Deploy API</a> endpoints\nfor this canvas, so there is nothing to probe.</p>\n<pre><code class=\"hljs language-json\"><span class=\"hljs-punctuation\">{</span>\n  <span class=\"hljs-attr\">\"apiBase\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"https://canvases.example.com/v1/canvases/{id}\"</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"zipUpload\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"PUT https://canvases.example.com/v1/canvases/{id}/deploy\"</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"staged\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-punctuation\">{</span>\n    <span class=\"hljs-attr\">\"begin\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"POST https://canvases.example.com/v1/canvases/{id}/uploads\"</span><span class=\"hljs-punctuation\">,</span>\n    <span class=\"hljs-attr\">\"stageBlob\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"PUT https://canvases.example.com/v1/canvases/{id}/uploads/{uploadId}/blobs/{hash}\"</span><span class=\"hljs-punctuation\">,</span>\n    <span class=\"hljs-attr\">\"finalize\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"POST https://canvases.example.com/v1/canvases/{id}/uploads/{uploadId}/finalize\"</span>\n  <span class=\"hljs-punctuation\">}</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"readback\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"GET https://canvases.example.com/v1/canvases/{id}/files\"</span><span class=\"hljs-punctuation\">,</span>\n  <span class=\"hljs-attr\">\"curl\"</span><span class=\"hljs-punctuation\">:</span> <span class=\"hljs-string\">\"curl -X PUT \\\"https://canvases.example.com/v1/canvases/{id}/deploy\\\" -H \\\"Authorization: Bearer $CANVAS_KEY\\\" --data-binary @site.zip\"</span>\n<span class=\"hljs-punctuation\">}</span></code></pre>\n<p><code>create_canvas</code> and <code>regenerate_deploy_key</code> embed the real key in <code>curl</code> (returned\nonce); <code>get_canvas</code> and <code>set_canvas_slug</code> show the <code>$CANVAS_KEY</code> placeholder, so set it\nfrom your own copy. The host is <code>CANVAS_DROP_API_BASE_URL</code>, falling back to\n<code>CANVAS_DROP_BASE_URL</code>. In <code>subdomain</code> mode it differs from the canvas hosts, which is\nwhy you should use the advertised endpoints rather than guessing.</p>\n<h2 id=\"which-deploy-tool-to-use\">Which deploy tool to use</h2>\n<p><code>deploy_canvas</code> sends the whole payload in one call: use it for the first publish of a\nsmall canvas. Use the staged flow (<code>begin_deploy</code>, <code>add_files</code>, <code>finalize_deploy</code>) when\nthe canvas already has content or has many, large, or binary files. Fresh tiny canvas:\n<code>deploy_canvas</code>. Everything else: staged.</p>\n<p>The staged flow:</p>\n<ol>\n<li><code>begin_deploy</code> with the full manifest (<code>path</code>, <code>hash</code> as sha256 of the bytes, <code>size</code>).\nThe reply's <code>missingHashes</code> lists the blobs the server does not already hold. Storage\nis content-addressed, so an unchanged file is never re-sent; a re-deploy that changed\none file sends one file.</li>\n<li><code>add_files</code> with the contents for those hashes, in as many calls as you like.</li>\n<li><code>finalize_deploy</code> to publish. The handle is single-use and short-lived; a finalize\nthat is missing a blob fails cleanly and can be retried after staging it.</li>\n</ol>\n<p>Over MCP, <code>add_files</code> content still travels in the tool call. The saving comes from not\nresending unchanged files and from chunking.</p>\n<p><strong>Prefer <code>curl</code> and the keyed Deploy API for the file transfer whenever you can run\nshell commands.</strong> Every MCP deploy tool inlines file contents into the tool call, so they\npass through the model. If you lack command or network permission, request it rather\nthan inlining bytes. The same staged flow runs over plain HTTP: <code>POST {apiBase}/uploads</code>\nwith the manifest, <code>PUT {apiBase}/uploads/{uploadId}/blobs/{hash}</code> with each blob's raw\nbytes, then <code>POST {apiBase}/uploads/{uploadId}/finalize</code>. The bytes go from disk to the\nserver without entering the model context, with no tool-call size ceiling.\n<code>create_canvas</code> returns the per-canvas key and the exact URLs in its <code>deploy</code> block.\nReserve the MCP deploy tools for a small first publish when shell access is\nunavailable.</p>\n<h2 id=\"verify-a-deploy\">Verify a deploy</h2>\n<p>The live URL is access-controlled, so do not confirm a deploy by fetching it: an\nunauthenticated <code>GET</code> returns a login page, not your files. Verify through the server:</p>\n<ul>\n<li>The deploy or finalize result already returns <code>{url, version, fileCount, totalBytes}</code>.</li>\n<li><code>list_versions</code> shows the new version as <code>current</code>.</li>\n<li><code>get_canvas_file</code> reads back what is live: no <code>path</code> lists the live files (<code>path</code>,\n<code>size</code>, <code>mime</code>, <code>hash</code>); a <code>path</code> such as <code>index.html</code> returns that file's content\n(text as UTF-8, binary as base64; files over 256 KiB return their hash only, so compare\nit to what you deployed).</li>\n<li>Over curl, the same read-back is <code>GET {apiBase}/files</code>, with <code>?path=</code> for raw bytes and\nno size cap. <code>apiBase</code> comes from the <code>deploy</code> block.</li>\n</ul>\n<p>Each <code>list_versions</code> row carries a <code>downloadUrl</code> for a complete ZIP export of that\nimmutable version. Fetch it with the same OAuth access token in the\n<code>Authorization: Bearer …</code> header; the route accepts no query-string token and no\ndashboard cookie, and applies the same role gate and rate limit as the tools. It answers\n<code>application/zip</code> on success, <code>404 { \"error\": \"not_found\" }</code> for a canvas you hold no\nrole on, <code>400 { \"error\": \"invalid_version\" }</code> for a version that is not a positive\ninteger, <code>404 { \"code\": \"NOT_FOUND\" }</code> for a version that does not exist, and\n<code>503 { \"code\": \"VERSION_INCOMPLETE\" }</code> rather than a partial archive when a referenced\nblob is missing.</p>\n<h2 id=\"enabling-and-disabling\">Enabling and disabling</h2>\n<p>The MCP surface is on by default (<code>CANVAS_DROP_MCP=on</code>). <code>CANVAS_DROP_MCP=off</code> removes\nthe <code>/mcp</code> endpoint, the version download route, and the OAuth routes (<code>/authorize</code>,\n<code>/token</code>, <code>/register</code>, <code>/revoke</code>, <code>/.well-known/*</code>) entirely: they are not mounted, so\nthey answer like any unknown path. Behind a reverse proxy or identity-aware proxy, <code>/mcp</code>\nmust reach the app with its <code>Authorization</code> header intact and bypass the proxy's own\nlogin; MCP carries its own auth. See <a href=\"/docs/self-hosting/configuration\">Configuration</a>\nand <a href=\"/docs/self-hosting/deploy\">Deploy</a>.</p>\n<h2 id=\"which-path-should-an-agent-use\">Which path should an agent use?</h2>\n<ul>\n<li><strong>MCP</strong>: your host speaks MCP and you want a connect-once, multi-canvas,\nidentity-scoped surface with the draft editor, sharing, and teams.</li>\n<li><strong><a href=\"/docs/api/deploy-api\">Deploy API</a></strong> (HTTP with a per-canvas key): a keyed,\nsessionless agent or a CI step that holds one canvas's key. Also the right transport\nfor the bytes of any large deploy, even from an MCP session.</li>\n<li>The packaged <strong><a href=\"/docs/agents/skill\">Agent skill</a></strong> documents both for a coding agent,\nand <strong><a href=\"/llms.txt\"><code>/llms.txt</code></a></strong> is the single-file quick reference.</li>\n</ul>\n",
    "headings": [
      {
        "id": "connect",
        "text": "Connect",
        "level": 2
      },
      {
        "id": "sign-in-oauth-2-1",
        "text": "Sign-in (OAuth 2.1)",
        "level": 3
      },
      {
        "id": "how-every-tool-behaves",
        "text": "How every tool behaves",
        "level": 2
      },
      {
        "id": "tools",
        "text": "Tools",
        "level": 2
      },
      {
        "id": "account-lists-create",
        "text": "Account, lists, create",
        "level": 3
      },
      {
        "id": "read-a-canvas",
        "text": "Read a canvas",
        "level": 3
      },
      {
        "id": "deploy",
        "text": "Deploy",
        "level": 3
      },
      {
        "id": "versions-and-lifecycle",
        "text": "Versions and lifecycle",
        "level": 3
      },
      {
        "id": "settings",
        "text": "Settings",
        "level": 3
      },
      {
        "id": "sharing-and-people",
        "text": "Sharing and people",
        "level": 3
      },
      {
        "id": "draft-editor-loop",
        "text": "Draft editor loop",
        "level": 3
      },
      {
        "id": "teams",
        "text": "Teams",
        "level": 3
      },
      {
        "id": "return-shapes",
        "text": "Return shapes",
        "level": 2
      },
      {
        "id": "which-deploy-tool-to-use",
        "text": "Which deploy tool to use",
        "level": 2
      },
      {
        "id": "verify-a-deploy",
        "text": "Verify a deploy",
        "level": 2
      },
      {
        "id": "enabling-and-disabling",
        "text": "Enabling and disabling",
        "level": 2
      },
      {
        "id": "which-path-should-an-agent-use",
        "text": "Which path should an agent use?",
        "level": 2
      }
    ],
    "text": "MCP server If you are an agent host that speaks MCP, this page gets you connected to a canvas-drop instance once and then creating, deploying, sharing, and editing canvases as the signed-in account, with no per-canvas key to paste. MCP is the identity-scoped companion to the keyed Deploy API : the Deploy API acts on one canvas with its secret key; MCP acts across every canvas you own or edit, as you. Connect Add the instance's endpoint to your MCP client: {base}/mcp The transport is Streamable HTTP and stateless: every request is authenticated on its own, and there is no session to keep alive. The server identifies itself as canvas-drop , version 1 . A first session, as tool calls: whoami {} -> { id , email, name, orgs, teams, isGuest } create_canvas { \"title\" : \"Retro board\" } -> { id , slug, url, apiKey, deploy, ... } apiKey is returned once deploy_canvas { \"id\" : \"<id>\" , \"files\" : [{ \"path\" : \"index.html\" , \"content\" : \"<h1>Hi</h1>\" }] } -> { url, version : 1, fileCount : 1, totalBytes, warnings : [] } get_canvas_file { \"id\" : \"<id>\" , \"path\" : \"index.html\" } -> { version, path, size, mime, hash, encoding : \"utf8\" , content } The canvas is live after the third call. A new canvas starts Restricted (the private API value), so its URL serves content only to you until you widen access with update_canvas . Sign-in (OAuth 2.1) canvas-drop is its own OAuth 2.1 authorization server; it does not proxy your identity provider. A compliant client needs nothing but the URL: Discovery. {base}/.well-known/oauth-authorization-server and {base}/.well-known/oauth-protected-resource (RFC 8414 and RFC 9728). The issuer is {base} ; the only scope is canvas-drop . Registration. POST {base}/register (Dynamic Client Registration). Authorization. {base}/authorize resolves identity server-side with the same auth strategy the dashboard uses, then checks the email allowlist and the blocked flag. In oidc mode a signed-out browser goes through the normal login and back; in proxy and dev mode identity is already present, and a request without it is denied. PKCE S256 is required. Tokens. POST {base}/token exchanges the single-use code (60 s lifetime; client id and redirect URI must match) for an access token ( token_type: \"bearer\" , expires_in: 3600 ) and a refresh token. Refresh tokens rotate on every use. POST {base}/revoke revokes. Every tool call carries Authorization: Bearer <access_token> . Tokens are stored hashed. On each call the server looks the token up and re-checks that the account is still active, so blocking a user or removing their email domain from the allowlist ends a live token on the next request. A missing or rejected token answers 401 { \"error\": \"unauthorized\" } with a WWW-Authenticate: Bearer header whose resource_metadata points at {base}/.well-known/oauth-protected-resource . Nothing the client asserts about identity reaches the tools; org membership is resolved server-side on every request. Sign-in and token events are audited ( mcp_authorize_ok , mcp_authorize_denied , mcp_token_issue , mcp_token_revoke ). How every tool behaves Results. Success returns the JSON result as text content. Failure returns CODE: message as text with isError: true . Codes are stable; messages are for humans. Ids, not slugs. A canvas tool's id is the canvas id. A team tool's id is the team id. Scope. Canvas tools see the canvases you own or edit. A canvas you hold no role on, including one you can only view, reads as canvas not found : no existence leak, no cross-account management. Admins get no extra reach on this surface. Roles. An editor can do everything the owner can except delete_canvas , transfer_canvas , and the guest-AI fields of update_canvas ( guestAiEnabled , guestAiCap ). Those answer OWNER_ONLY: Only the canvas owner can do this. for an editor. Roles are resolved on every call; a demotion or removal applies to your next request. Disabled canvases. When an admin has disabled a canvas, read tools keep working and every mutation answers DISABLED: This canvas has been disabled by an administrator. (followed by Reason: … when one was given), the same contract as the management API's 409 { \"code\": \"DISABLED\" } . Only an admin can re-enable it. Archived canvases. Deploy, publish, and rollback refuse with NOT_ACTIVE: … ; call unarchive_canvas first. Limits. Calls are rate-limited per account from the canvas-API bucket ( CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN , default 120 per minute); over the limit answers 429 { \"error\": \"rate_limited\" } with Retry-After . A request body over 110 MiB answers 413 { \"error\": \"payload_too_large\" } . Audit. Every mutation writes the same audit event as its dashboard equivalent. Refusal codes you will meet across tools: Code Meaning OWNER_ONLY An editor called an owner-only act, or tried to change the owner entry. DISABLED Admin takedown; the canvas is read-only. NOT_ACTIVE The canvas is archived. GUEST_VIEWER_ONLY editor was requested for a guest (an email outside the org); guests are always viewers. PUBLIC_LINK_OWNER_GATED You are an editor and the owner's account cannot publish public links; the entitlement follows the owner, whoever acts. The owner hears PUBLIC_NOT_ALLOWED for the same state. DRAFT_CONFLICT A stale draft write; see the draft tools. INVALID_REQUEST Mutually exclusive inputs were both supplied, or neither was. Tools 47 tools: 13 open to any signed-in account, 32 at minimum role editor, 2 owner-only. Optional inputs are marked ? . \"View\" is the canvas projection described under Return shapes below. Account, lists, create Available to any signed-in account. Tool Input Result whoami none {id, email, name, orgs: [{id, name}], teams: [{id, name, slug, orgId}], isGuest} . orgs is empty when no org boundary is configured; isGuest is true only when an org boundary exists and you belong to no org. list_canvases role? ( owned | edited ), access? ( restricted | whole_org | public_link , or a legacy value), scope? ( active default | archived ), shared? , protected? , listed? , template? , undeployed? (booleans), query? , tags? (string[]), sort? ( updated default, created , title , popular ), limit? (clamped to 1-100, default 50), offset? (clamped to 0 or greater, default 0) {total, limit, offset, summary, canvases: [View + {owner: {id, name, email}, role: \"owner\" | \"editor\", recentViews}]} . summary is the filter-independent Your-canvases inventory: {active, archived, shared, protected, listed, templates, neverDeployed, owned, edited} . The five boolean state filters match the dashboard chips; shared means access extends beyond the people-and-teams list. query is a forgiving text filter over title, description, tags, and slug (case-, accent-, and whitespace-insensitive; words are AND-ed). tags matches canvases carrying any of the given tags. popular ranks by views in the last 30 days ( recentViews ). For a multi-page sweep that mutates canvases, use the stable created or title sort. list_shared_canvases query? , sort? ( updated default, title , owner ), limit? (1-100, default 50), offset? (default 0) {total, limit, offset, canvases: [{id, slug, url, title, description, tags, access: {kind: \"direct\" | \"team\" | \"whole_org\", label, teamIds?, teamNames?}, hasPassword, hasPreview, owner: {id, name, avatarUrl} | null, createdAt, updatedAt}]} . Canvases you can open but do not manage: anything you are on the people-and-teams list of (directly or through a team, at any access value), plus the Whole-org shares their owner listed. Display-only; open the url . create_canvas title? , description? , backendEnabled? , slug? (≤63), orgId? (string or null) View + apiKey (the deploy key, returned once) + deploy (ready-to-run endpoints with the real key embedded, see Return shapes). orgId from whoami.orgs homes the canvas in that org so it can be shared org-wide; omit it for a personal canvas. ORG_FORBIDDEN , INVALID_SLUG , SLUG_TAKEN . clone_canvas id (the source) View of the new canvas: an unpublished, Restricted draft with an empty direct-access list, a fresh slug and key, and backend off. Eligible sources: any active canvas you own or edit, a gallery-listed templatable canvas (org-scoped), or a published, active, unexpired, password-free canvas granted directly to you or to a team you belong to, at any General-access value. General access alone does not grant cloning; pending invitations and retained legacy guests cannot clone. A viewer's new copy remains theirs if the source grant is later revoked. Anything else reads canvas not found . Read a canvas Minimum role: editor. These keep working on a disabled canvas. Tool Input Result get_canvas id View + owner , role , teamIds (the team grants on the people-and-teams list, at any access value; [] when none), ownerOnlyActs: [\"delete\", \"transfer\", \"guest_ai\"] , and deploy with a $CANVAS_KEY placeholder (the key is never re-issued). list_versions id {versions: [{number, source, status, createdBy, createdByName, createdByEmail, createdAt, fileCount, totalBytes, current, downloadUrl}]} . downloadUrl is {base}/mcp/canvases/{id}/versions/{n}/download , a ZIP of that version. get_canvas_file id , path? Without path : {version, fileCount, files: [{path, size, mime, hash}]} for the live version. With path : {version, path, size, mime, hash, encoding: \"utf8\" | \"base64\", content} . A file over 256 KiB returns truncated: true and a note instead of content ; compare the hash . Fails with this canvas has no live version yet or no file at \"…\" . get_canvas_usage id {totalViews, uniqueViewers, lastViewedAt, viewsByDay, kvOps, fileOps, fileCount, fileBytes, aiCalls, aiTokens, aiCostUsd, realtimeConnects} . list_canvas_connections id {connections: [{key, label, origin, allowedMethods, protectedHeaderNames, enabled, available, unavailableReason}]} . This is sanitized authority metadata only: protected header values are never returned. A Connection works only while its live admin grant and profile remain enabled and the canvas backend is on. list_access id {entries: [{id, kind, role, email, name, userId, teamId, teamOrgId, createdAt}]} : the owner first, then people, pending sign-in grants, and teams. kind is owner , member , guest , pending , or team ; role is owner , viewer , or editor . Entry ids are stable ( owner , member:<id> , guest:<id> , pending:<id> , team:<teamId> ); pass them to set_access_role and revoke_access . When you are the owner the result also carries transferCandidates: [{id, name, email}] , the set transfer_canvas accepts. search_people context ( canvas | team ), canvasId? , teamId? , q (1-80 chars) {people: [{id, email, name}]} : the dashboard's Add person suggestions, scoped to a canvas you own or edit ( canvasId ) or a team you can see ( teamId ). INVALID_REQUEST: missing canvasId or teamId for context when the matching id is absent; a canvas you do not manage or a team you cannot see reads not found . Does not expose the admin People directory. Deploy Minimum role: editor. deploy_canvas , begin_deploy , and finalize_deploy also require an active canvas ( NOT_ACTIVE ); add_files needs only the open handle. Every deploy goes live at once as a new immutable version. There is no draft step on this path; for a draft, use the editor tools below. Tool Input Result deploy_canvas id , and exactly one of zipBase64 or files: [{path, content, encoding?: \"utf8\" | \"base64\"}] {url, version, fileCount, totalBytes, warnings: []} . INVALID_REQUEST for both or neither; empty deploy for a zero-byte ZIP. Ingest failures use the Deploy API codes: EMPTY_DEPLOY , TOO_MANY_FILES , FILE_TOO_LARGE , CANVAS_TOO_LARGE , INVALID_ZIP , INVALID_PATH , INVALID_ENCODING , ZIP_SLIP_REJECTED , ZIP_BOMB_REJECTED . begin_deploy id , manifest: [{path, hash, size}] ( hash is the sha256 hex of the bytes) {uploadId, missingHashes} : the blobs the server does not already hold. The handle lives 15 minutes. INVALID_MANIFEST . add_files id , uploadId , files: [{path, content, encoding?}] {staged: <count>} . Call repeatedly to chunk. UPLOAD_HANDLE_INVALID , UPLOAD_EXPIRED , UPLOAD_ALREADY_FINALIZED , UPLOAD_UNEXPECTED_BLOB , BLOB_HASH_MISMATCH , INVALID_ENCODING . finalize_deploy id , uploadId Same result as deploy_canvas . Single-use. UPLOAD_MISSING_BLOB (stage it and retry), UPLOAD_IN_PROGRESS (a 60 s lease), UPLOAD_ALREADY_FINALIZED . Limits: 100 MiB per canvas, 25 MiB per file, 2000 files. Read \"Which deploy tool to use\" below before sending bytes through a tool call. Versions and lifecycle Minimum role: editor unless marked owner-only. Tool Input Result rollback_canvas id , version (integer) View + version . The target must be a ready version ( no ready version N otherwise); NOT_ACTIVE on an archived canvas; that version was just removed; retry if it was pruned mid-swap. unpublish_canvas id {url, publicationState: \"draft\", currentVersionId: null} . Takes the canvas offline, drops live sockets, and revokes legacy guest grants. CANNOT_UNPUBLISH: this canvas isn't published . delete_version id , version (integer > 0) {ok: true, version} . The current version is protected ( CURRENT_VERSION ); also VERSION_NOT_FOUND , VERSION_UNAVAILABLE . Blobs shared with other versions or the draft are retained. archive_canvas id View with status: \"archived\" . Reversible; takes the URL offline. NOT_ACTIVE: only an active canvas can be archived . unarchive_canvas id View + owner , role , with status: \"active\" . NOT_ARCHIVED: canvas is not archived . delete_canvas (owner-only) id {ok: true} . Soft-delete: the URL stops resolving and the canvas is purged after the retention window. Refused with DISABLED on a disabled canvas; OWNER_ONLY for an editor. Not reversible over MCP. transfer_canvas (owner-only) id , toUserId (a user id, never an email) {ok: true, canvas: View, previousOwnerEditor, publicLinkReverted} . Instant: the recipient, an existing editor (see transferCandidates ), becomes owner and you stay on as an editor; the public-link entitlement now follows their account, and publicLinkReverted tells you if a public link had to be turned off. A team cannot receive a canvas. NOT_ELIGIBLE , TARGET_NOT_FOUND , TARGET_BLOCKED , TARGET_NOT_MEMBER , ALREADY_OWNER , SELF , CONFLICT . Settings Minimum role: editor. Tool Input Result update_canvas id plus any of: title (≤200), description (≤2000; null clears), access ( private | whole_org | public_link ; the legacy specific_people / team are accepted as aliases of private ), discoverability ( link_only | listed ), teamIds (string[], ≤50), password (null clears), sharedExpiresAt (Unix ms; null clears), spaFallback , previewMode ( auto | off ), galleryListed , galleryTemplatable , tags (≤20, each ≤50 chars), and the owner-only guestAiEnabled , guestAiCap View + owner , role , teamIds , and sometimes warning (an edge-cache staleness notice when restricting a formerly public canvas; surface it to the user). Omitted fields are unchanged. Audits password_change and share_change . set_capabilities id , backendEnabled? , kv? , files? , ai? , realtime? , authoring? (all booleans) View + owner , role . backendEnabled is the master switch; the others take effect only when it is on. authoring also needs the instance switch on. Omitted fields are unchanged; switching a capability off drops sockets that lost access. set_canvas_slug id , slug? (≤63; omit for a fresh random slug) View + deploy ( $CANVAS_KEY placeholder). The old URL stops resolving immediately. INVALID_SLUG , SLUG_TAKEN . set_canvas_preview id , image? (base64 PNG, JPEG, or WebP; the string ≤40 MiB, decoded ≤25 MiB) View + owner , role . With image , previewMode becomes custom and a publish never overwrites the cover; without it, a custom cover is cleared back to auto (an auto-captured screenshot is left alone). INVALID_IMAGE , IMAGE_TOO_LARGE . For the auto/off toggle use update_canvas previewMode . regenerate_deploy_key id {apiKey, deploy} . Mints a new cd_… key (returned once, embedded in deploy.curl ) and invalidates the old one. An editor may rotate it; the owner is emailed naming the actor. Notes on update_canvas : The people-and-teams list ( grant_access / revoke_access ) applies at every access value; access only says who else may open the canvas. whole_org and public_link need a published canvas: active with a live version ( SHARE_REQUIRES_PUBLISH ). public_link also needs the instance switch on ( PUBLIC_LINKS_DISABLED ) and an owner whose account may publish public links ( PUBLIC_NOT_ALLOWED to the owner, PUBLIC_LINK_OWNER_GATED to an editor). When an org boundary is configured, whole_org needs a canvas homed in an org ( ORG_REQUIRED ); pass orgId at create_canvas . To share with a team, grant_access it with teamId (and a role ). The legacy teamIds field replaces the viewer-team grants on the list (at least one team you belong to; personal teams fit any canvas you own, org teams must match the canvas's org: TEAM_REQUIRED , TEAM_FORBIDDEN ). Changing access never touches team grants. discoverability controls only whether a Whole-org canvas appears in Shared for the whole org; people and teams on the list always see it there. It never widens URL access. Off whole_org it is pinned to link_only on every write (a legacy team row that stored listed reads listed until its next settings write). Legacy shape: teamIds: [] sent by a client that sees the canvas on the legacy team value, together with an access change to another value, is a no-op (the grants stay on the list); teamIds: [] on its own, or with any other access value, is refused ( TEAM_REQUIRED ). Setting galleryListed: true on a Whole-org canvas also sets discoverability: \"listed\" ; passing link_only in the same call is refused ( DISCOVERY_CONFLICT ). Gallery listing needs a shared, published, password-free canvas on public_link or a listed whole_org ( NOT_SHARED , NOT_PUBLISHED , PASSWORD_PROTECTED , NOT_GALLERY_ELIGIBLE ). galleryTemplatable: true needs the canvas listed first ( NOT_LISTED ). The people-and-teams list itself is managed with grant_access , set_access_role , and revoke_access . Sharing and people Minimum role: editor. Tool Input Result grant_access id , exactly one of email or teamId , role? ( viewer default | editor ) Person: {ok: true, status: \"granted\" | \"pending\" | \"role_changed\" | \"already_added\" | \"already_pending\", role, emailDelivery?} . An existing user is granted now; an admissible new email becomes a pending sign-in grant that carries the role; passing role for someone already listed updates it, and omitting it never changes an existing entry. Team: {ok: true, status: \"granted\" | \"role_changed\" | \"already_added\", role, from} . Only org members and teams can be editors ( GUEST_VIEWER_ONLY ). Every grant opens the canvas at once, whatever access (General access) says; an editor also manages it. Also INVALID_REQUEST: pass exactly one of email or teamId , NOT_PERMITTED , AUTH_ADMISSION_REQUIRED , BLOCKED , RATE_LIMITED , TEAM_FORBIDDEN , EMAIL_NOT_CONFIGURED . invite_to_canvas id , email , role? The same person result as grant_access , through the same Add person service, and it sends the access email. A brand-new external email is refused for a non-admin unless the instance allows it ( NOT_PERMITTED ); RATE_LIMITED past the cap. revoke_access id , entryId (from list_access ) {ok: true} . Removes a person (another editor, or yourself), a pending grant, a legacy guest row, or a team grant; sockets the entry no longer permits are dropped. The owner entry refuses ( OWNER_ONLY ); an unknown id reads access entry not found . set_access_role id , entryId , role ( viewer | editor ) {ok: true} . A guest can only be a viewer ( GUEST_VIEWER_ONLY ); the owner entry refuses ( OWNER_ONLY ; use transfer_canvas ). Demoting drops the person's live editor sockets. Draft editor loop Minimum role: editor. These mirror the browser editor: a per-canvas draft that publish_draft snapshots into a live version. DraftView is {files: [{path, size, mime, hash, updatedBy, updatedByName, updatedAt}], stale, baseVersionId, updatedAt, dirty} , where dirty means the draft differs from live and stale means live moved on since the draft was based. Tool Input Result get_draft id DraftView. Created from the live version on first open. Works on a disabled canvas. read_draft_file id , path {path, encoding, content, hash, updatedBy, updatedByName, updatedAt} . no draft file at \"…\" . write_draft_file id , path , content , encoding? ( utf8 default | base64 ), create? , expectedHash? DraftView. create: true refuses to overwrite ( PATH_EXISTS ). expectedHash is the hash you loaded, or the literal none for a path you believe absent; a mismatch fails with DRAFT_CONFLICT: … (path= currentHash= updatedBy= updatedByName= updatedAt=) : re-read and retry with the current hash. Without expectedHash the write still fails with DRAFT_CONFLICT if a different user wrote that file last. Two editors in different files never conflict. delete_draft_file id , path , expectedHash? DraftView. rename_draft_file id , from , to , expectedHash? (checked on from ) DraftView. publish_draft id {version, versionId, fileCount, totalBytes} . NOT_ACTIVE: unarchive this canvas before publishing , EMPTY_DEPLOY , DISABLED . restore_draft id , version (integer > 0) DraftView, reset to that version's files. Teams Available to any signed-in account; self-serve only, with no admin reach. Here id is a team id. Tool Input Result list_teams none {teams: [{id, orgId, name, slug, mine, canManage}]} . orgId is null for a personal team. mine : you belong to it. canManage : you created it, so you can rename or delete it. create_team orgId? (string or null), name (1-80 chars) {id, orgId, name, slug} . Omit orgId for a personal team; pass one from whoami.orgs to attach the team to that org. You become its first member and its manager. rename_team id , name (1-80 chars) {id, name} . Creator only ( FORBIDDEN ). delete_team id {ok: true} . Creator only ( FORBIDDEN ). add_team_member id , email {status: \"granted\" | \"pending\", emailDelivery?} . Any member may add; an org team takes same-org members only; a brand-new external email on a personal team is refused for a non-admin unless the instance allows it. remove_team_member id , userId {ok: true} . Pass your own id to leave. cancel_team_invite id , inviteId (a pending row id from list_team_members ) {ok: true} . A stale id reads TARGET_NOT_FOUND . list_team_members id {members: [{userId, email, name}], pending: [{id, email, invitedAt}]} . Team error codes: NOT_A_MEMBER , TEAM_NOT_FOUND , TEAM_NAME_TAKEN , FORBIDDEN , TARGET_NOT_FOUND , TARGET_NOT_MEMBER , TARGET_NOT_PERMITTED , TARGET_BLOCKED , AUTH_ADMISSION_REQUIRED , RATE_LIMITED . Return shapes View , the canvas projection every canvas tool echoes: { id, slug, url, ownerId, title, description, status, publicationState, currentVersionId, access, accessMode, discoverability, hasPassword, sharedExpiresAt, spaFallback, backendEnabled, disabledReason, galleryListed, galleryTemplatable, tags, guestAiEnabled, guestAiCap, previewMode, viewCount, lastViewedAt, hasPreview, previewUrl? } publicationState is draft , published , archived , disabled , or deleted . accessMode is the audience — restricted ( private or a legacy alias: only the people-and-teams list, which applies at every value), whole_org , or public_link — so an agent never has to know the three spellings of \"restricted\". previewUrl is present when a preview exists. Identity-bearing tools add owner: {id, name, email} | null and role: \"owner\" | \"editor\" ; teamIds lists the granted teams at any access value. The password hash and the API key are never included. list_canvases also returns limit , offset , and the filter-independent summary counts shown on the dashboard; these describe the whole owned-or-edited inventory even when the returned page is filtered. deploy , returned by create_canvas , get_canvas , set_canvas_slug , and regenerate_deploy_key : the exact keyed Deploy API endpoints for this canvas, so there is nothing to probe. { \"apiBase\" : \"https://canvases.example.com/v1/canvases/{id}\" , \"zipUpload\" : \"PUT https://canvases.example.com/v1/canvases/{id}/deploy\" , \"staged\" : { \"begin\" : \"POST https://canvases.example.com/v1/canvases/{id}/uploads\" , \"stageBlob\" : \"PUT https://canvases.example.com/v1/canvases/{id}/uploads/{uploadId}/blobs/{hash}\" , \"finalize\" : \"POST https://canvases.example.com/v1/canvases/{id}/uploads/{uploadId}/finalize\" } , \"readback\" : \"GET https://canvases.example.com/v1/canvases/{id}/files\" , \"curl\" : \"curl -X PUT \\\"https://canvases.example.com/v1/canvases/{id}/deploy\\\" -H \\\"Authorization: Bearer $CANVAS_KEY\\\" --data-binary @site.zip\" } create_canvas and regenerate_deploy_key embed the real key in curl (returned once); get_canvas and set_canvas_slug show the $CANVAS_KEY placeholder, so set it from your own copy. The host is CANVAS_DROP_API_BASE_URL , falling back to CANVAS_DROP_BASE_URL . In subdomain mode it differs from the canvas hosts, which is why you should use the advertised endpoints rather than guessing. Which deploy tool to use deploy_canvas sends the whole payload in one call: use it for the first publish of a small canvas. Use the staged flow ( begin_deploy , add_files , finalize_deploy ) when the canvas already has content or has many, large, or binary files. Fresh tiny canvas: deploy_canvas . Everything else: staged. The staged flow: begin_deploy with the full manifest ( path , hash as sha256 of the bytes, size ). The reply's missingHashes lists the blobs the server does not already hold. Storage is content-addressed, so an unchanged file is never re-sent; a re-deploy that changed one file sends one file. add_files with the contents for those hashes, in as many calls as you like. finalize_deploy to publish. The handle is single-use and short-lived; a finalize that is missing a blob fails cleanly and can be retried after staging it. Over MCP, add_files content still travels in the tool call. The saving comes from not resending unchanged files and from chunking. Prefer curl and the keyed Deploy API for the file transfer whenever you can run shell commands. Every MCP deploy tool inlines file contents into the tool call, so they pass through the model. If you lack command or network permission, request it rather than inlining bytes. The same staged flow runs over plain HTTP: POST {apiBase}/uploads with the manifest, PUT {apiBase}/uploads/{uploadId}/blobs/{hash} with each blob's raw bytes, then POST {apiBase}/uploads/{uploadId}/finalize . The bytes go from disk to the server without entering the model context, with no tool-call size ceiling. create_canvas returns the per-canvas key and the exact URLs in its deploy block. Reserve the MCP deploy tools for a small first publish when shell access is unavailable. Verify a deploy The live URL is access-controlled, so do not confirm a deploy by fetching it: an unauthenticated GET returns a login page, not your files. Verify through the server: The deploy or finalize result already returns {url, version, fileCount, totalBytes} . list_versions shows the new version as current . get_canvas_file reads back what is live: no path lists the live files ( path , size , mime , hash ); a path such as index.html returns that file's content (text as UTF-8, binary as base64; files over 256 KiB return their hash only, so compare it to what you deployed). Over curl, the same read-back is GET {apiBase}/files , with ?path= for raw bytes and no size cap. apiBase comes from the deploy block. Each list_versions row carries a downloadUrl for a complete ZIP export of that immutable version. Fetch it with the same OAuth access token in the Authorization: Bearer … header; the route accepts no query-string token and no dashboard cookie, and applies the same role gate and rate limit as the tools. It answers application/zip on success, 404 { \"error\": \"not_found\" } for a canvas you hold no role on, 400 { \"error\": \"invalid_version\" } for a version that is not a positive integer, 404 { \"code\": \"NOT_FOUND\" } for a version that does not exist, and 503 { \"code\": \"VERSION_INCOMPLETE\" } rather than a partial archive when a referenced blob is missing. Enabling and disabling The MCP surface is on by default ( CANVAS_DROP_MCP=on ). CANVAS_DROP_MCP=off removes the /mcp endpoint, the version download route, and the OAuth routes ( /authorize , /token , /register , /revoke , /.well-known/* ) entirely: they are not mounted, so they answer like any unknown path. Behind a reverse proxy or identity-aware proxy, /mcp must reach the app with its Authorization header intact and bypass the proxy's own login; MCP carries its own auth. See Configuration and Deploy . Which path should an agent use? MCP : your host speaks MCP and you want a connect-once, multi-canvas, identity-scoped surface with the draft editor, sharing, and teams. Deploy API (HTTP with a per-canvas key): a keyed, sessionless agent or a CI step that holds one canvas's key. Also the right transport for the bytes of any large deploy, even from an MCP session. The packaged Agent skill documents both for a coding agent, and /llms.txt is the single-file quick reference."
  },
  {
    "path": "agents/llms",
    "section": "For agents",
    "title": "llms.txt",
    "html": "<h1 id=\"llms-txt\">llms.txt</h1>\n<p>If you are an agent putting a canvas on a canvas-drop instance, start here. The\ninstance serves this page and its companions (Overview, Quickstart, Capabilities,\nSDK overview, Deploy API, Runtime API, Error codes) as one plain-text file at\n<a href=\"/llms.txt\"><code>{base}/llms.txt</code></a>, meant to be dropped straight into context. It is\npublic: served on the instance's base host ahead of the sign-in gateway, so you\ncan read the contract before you hold any credential. <code>{base}</code> is the instance\norigin (a fresh local instance is <code>http://localhost:3000</code>).</p>\n<p>There are three ways in. Pick by what you hold.</p>\n<table>\n<thead>\n<tr>\n<th>You hold</th>\n<th>Use</th>\n<th>Reach</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>A per-canvas secret key (<code>cd_...</code>)</td>\n<td>Deploy API at <code>{base}/v1/canvases/{id}/...</code></td>\n<td>that one canvas: deploy, read back, roll back, unpublish</td>\n</tr>\n<tr>\n<td>An MCP-capable host</td>\n<td>MCP at <code>{base}/mcp</code> (OAuth 2.1, no key to paste)</td>\n<td>every canvas the signed-in account owns or edits; 47 tools</td>\n</tr>\n<tr>\n<td>Code running inside a canvas page</td>\n<td>Browser SDK at <code>{base}/sdk/v1.js</code>, global <code>canvasdrop</code></td>\n<td>the six fixed primitives for that canvas: KV, files, AI, identity, realtime, Connections</td>\n</tr>\n</tbody></table>\n<p>Two verbs recur below. <strong>Publish</strong> turns the editor draft into an immutable\nversion. <strong>Deploy</strong> (the Deploy API, <code>deploy_canvas</code>, the staged upload) publishes\ndirectly with no draft step. Both create a new version at the same URL; the last\n10 versions are kept.</p>\n<h2 id=\"deploy-with-a-key\">Deploy with a key</h2>\n<ol>\n<li>Get a canvas and its key. A person creates the canvas on the dashboard's\nCreate page with <strong>Use the API</strong>, or you call <code>create_canvas</code> over MCP. Either\nmints the canvas plus a one-time secret key, shown once.</li>\n<li>PUT a ZIP with <code>index.html</code> at its root:</li>\n</ol>\n<pre><code class=\"hljs language-bash\">curl -fsS -X PUT <span class=\"hljs-string\">\"{base}/v1/canvases/{id}/deploy\"</span> \\\n  -H <span class=\"hljs-string\">\"Authorization: Bearer <span class=\"hljs-variable\">$CANVAS_KEY</span>\"</span> \\\n  --data-binary @site.zip\n<span class=\"hljs-comment\"># 200 {\"url\":\"...\",\"version\":7,\"fileCount\":12,\"totalBytes\":348201,\"warnings\":[]}</span></code></pre>\n<ol>\n<li>Verify through the server, not the URL:</li>\n</ol>\n<pre><code class=\"hljs language-bash\">curl -fsS <span class=\"hljs-string\">\"{base}/v1/canvases/{id}/files\"</span> -H <span class=\"hljs-string\">\"Authorization: Bearer <span class=\"hljs-variable\">$CANVAS_KEY</span>\"</span>\n<span class=\"hljs-comment\"># 200 {\"version\":7,\"fileCount\":12,\"files\":[{\"path\":\"index.html\",\"size\":1204,\"mime\":\"text/html\",\"hash\":\"...\"}]}</span>\ncurl -fsS <span class=\"hljs-string\">\"{base}/v1/canvases/{id}/files?path=index.html\"</span> \\\n  -H <span class=\"hljs-string\">\"Authorization: Bearer <span class=\"hljs-variable\">$CANVAS_KEY</span>\"</span> | <span class=\"hljs-built_in\">sha256sum</span>\n<span class=\"hljs-comment\"># the live file's raw bytes; compare the hash with what you shipped</span></code></pre>\n<p>The deploy publishes a live version immediately. <code>{id}</code> is the canvas id, not the\nslug. The Deploy API host is <code>CANVAS_DROP_API_BASE_URL</code>, which defaults to the\ninstance base URL and can differ from the dashboard host; <code>create_canvas</code> and the\ndashboard both return the exact endpoints for the canvas, so never guess the host.</p>\n<p>The key is verified per canvas. A missing or unknown key, including a key for an\narchived, disabled, or deleted canvas, answers <code>401 {\"error\":\"unauthorized\"}</code>; a key\nfor a different canvas answers <code>403</code>. Validation failures answer <code>{\"code\", \"message\",\"path\"}</code>: <code>400</code> on the ZIP path with <code>EMPTY_DEPLOY</code>, <code>TOO_MANY_FILES</code>,\n<code>FILE_TOO_LARGE</code>, <code>CANVAS_TOO_LARGE</code>, <code>INVALID_ZIP</code>, <code>INVALID_PATH</code>,\n<code>ZIP_SLIP_REJECTED</code>, or <code>ZIP_BOMB_REJECTED</code>; a ZIP body over the canvas cap is\nrefused before it is read with <code>413 CANVAS_TOO_LARGE</code>. Deploys, staged\nbegin/finalize, and rollbacks are throttled at 10 per minute per canvas\n(<code>429 {\"error\":\"rate_limited\"}</code> with <code>Retry-After</code>).</p>\n<p>Companion routes, same Bearer key:</p>\n<table>\n<thead>\n<tr>\n<th>Route</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>GET /v1/canvases/{id}</code></td>\n<td><code>{id, slug, url, title, status, publicationState, accessMode, currentVersionId}</code> (<code>accessMode</code>: <code>restricted</code> | <code>whole_org</code> | <code>public_link</code> — who else can open it beyond the people-and-teams list)</td>\n</tr>\n<tr>\n<td><code>GET /v1/canvases/{id}/versions</code></td>\n<td><code>{versions: [{number, source, status, createdBy, createdAt, fileCount, totalBytes, current}]}</code></td>\n</tr>\n<tr>\n<td><code>GET /v1/canvases/{id}/files</code></td>\n<td>the live manifest as JSON; <code>?path=</code> returns that file's raw bytes; <code>404 NOT_PUBLISHED</code> before the first deploy</td>\n</tr>\n<tr>\n<td><code>POST /v1/canvases/{id}/rollback</code></td>\n<td>body <code>{\"version\": 6}</code>; makes that ready version current and returns <code>{url, version}</code>; <code>404</code> when no ready version has that number</td>\n</tr>\n<tr>\n<td><code>POST /v1/canvases/{id}/unpublish</code></td>\n<td>back to Draft: <code>{url, publicationState: \"draft\", currentVersionId: null}</code>; <code>409 CANNOT_UNPUBLISH</code> when not published</td>\n</tr>\n</tbody></table>\n<p>For large or repeat deploys, the staged flow sends only changed blobs:\n<code>POST /v1/canvases/{id}/uploads</code> with <code>{\"manifest\":[{\"path\",\"hash\",\"size\"}]}</code>\n(sha256 hex) returns <code>{uploadId, missingHashes}</code>; <code>PUT /v1/canvases/{id}/uploads/{uploadId}/blobs/{hash}</code> with the raw bytes of each\nmissing blob returns <code>204</code>; <code>POST /v1/canvases/{id}/uploads/{uploadId}/finalize</code>\nreturns the same <code>DeployResult</code>. A session lives 15 minutes and finalizes once.\nStaged errors use the same <code>{code, message}</code> shape at a mapped status: size caps\n<code>413</code>; an unknown or foreign <code>uploadId</code> <code>404 UPLOAD_HANDLE_INVALID</code>;\n<code>UPLOAD_ALREADY_FINALIZED</code> and <code>UPLOAD_IN_PROGRESS</code> <code>409</code>; <code>UPLOAD_EXPIRED</code>,\n<code>UPLOAD_MISSING_BLOB</code>, <code>BLOB_HASH_MISMATCH</code>, <code>INVALID_MANIFEST</code> <code>400</code>.</p>\n<p>Limits: 100 MB per canvas, 25 MB per file, 2 000 files. Full contract:\n<a href=\"/docs/api/deploy-api\">Deploy API</a>.</p>\n<h2 id=\"connect-over-mcp\">Connect over MCP</h2>\n<p>Add <code>{base}/mcp</code> to an MCP-capable host. First use runs OAuth 2.1 against\ncanvas-drop itself (RFC 8414/9728 discovery, Dynamic Client Registration, PKCE\n<code>S256</code>), sends you through the instance's normal org sign-in, and returns a 1 h\naccess token plus a rotating refresh token. Every call re-checks that the account\nis still active. Transport is Streamable HTTP, stateless; calls are limited to 120\nper minute per account and request bodies to 110 MiB. An instance with\n<code>CANVAS_DROP_MCP=off</code> has no <code>/mcp</code> endpoint at all.</p>\n<p>A first session, as tool calls:</p>\n<pre><code class=\"hljs\"><span class=\"hljs-keyword\">whoami</span>           {}                             <span class=\"hljs-operator\">-&gt;</span> { <span class=\"hljs-built_in\">id</span>, email, name, orgs, teams, isGuest }\n<span class=\"hljs-keyword\">create_canvas</span>    { <span class=\"hljs-string\">\"title\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"Retro board\"</span> }     <span class=\"hljs-operator\">-&gt;</span> { <span class=\"hljs-built_in\">id</span>, slug, url, apiKey, deploy, <span class=\"hljs-operator\">...</span> }   apiKey is returned once\n<span class=\"hljs-keyword\">deploy_canvas</span>    { <span class=\"hljs-string\">\"id\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"&lt;id&gt;\"</span>, <span class=\"hljs-string\">\"files\"</span><span class=\"hljs-operator\">:</span> [{ <span class=\"hljs-string\">\"path\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"index.html\"</span>, <span class=\"hljs-string\">\"content\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"&lt;h1&gt;Hi&lt;/h1&gt;\"</span> }] }\n                                                <span class=\"hljs-operator\">-&gt;</span> { url, version<span class=\"hljs-operator\">:</span> 1, fileCount<span class=\"hljs-operator\">:</span> 1, totalBytes, warnings<span class=\"hljs-operator\">:</span> [] }\n<span class=\"hljs-keyword\">get_canvas_file</span>  { <span class=\"hljs-string\">\"id\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"&lt;id&gt;\"</span>, <span class=\"hljs-string\">\"path\"</span><span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"index.html\"</span> }\n                                                <span class=\"hljs-operator\">-&gt;</span> { version, path, size, mime, hash, encoding<span class=\"hljs-operator\">:</span> <span class=\"hljs-string\">\"utf8\"</span>, content }</code></pre>\n<p><strong>Scope and roles.</strong> Tools act on the canvases the account owns or edits. A canvas\nyou hold no role on reads as <code>canvas not found</code>, for admins too. Each tool has a\nminimum role: <code>any</code> (identity, lists, create, teams), <code>editor</code> (everything on a\ncanvas), or <code>owner</code> (<code>delete_canvas</code>, <code>transfer_canvas</code>). An editor calling an\nowner-only tool, or setting <code>guestAiEnabled</code> / <code>guestAiCap</code> through <code>update_canvas</code>,\ngets <code>OWNER_ONLY: ...</code>; <code>get_canvas</code> echoes the list as <code>ownerOnlyActs: [\"delete\", \"transfer\", \"guest_ai\"]</code>. <code>id</code> parameters are canvas ids (team tools take team\nids), never slugs. Results are JSON in a text content block; failures are\n<code>isError: true</code> with the text <code>CODE: message</code>. An admin-disabled canvas stays\nreadable but every mutation fails <code>DISABLED: ...</code>; an archived canvas refuses\ndeploy and publish with <code>NOT_ACTIVE</code>.</p>\n<table>\n<thead>\n<tr>\n<th>Group</th>\n<th>Tools</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Identity, lists, create (<code>any</code>)</td>\n<td><code>whoami</code>, <code>list_canvases</code>, <code>list_shared_canvases</code>, <code>create_canvas</code>, <code>clone_canvas</code></td>\n</tr>\n<tr>\n<td>Read (<code>editor</code>)</td>\n<td><code>get_canvas</code>, <code>list_versions</code>, <code>get_canvas_file</code>, <code>get_canvas_usage</code>, <code>list_access</code>, <code>search_people</code></td>\n</tr>\n<tr>\n<td>Deploy (<code>editor</code>; publishes live immediately)</td>\n<td><code>deploy_canvas</code>, <code>begin_deploy</code>, <code>add_files</code>, <code>finalize_deploy</code></td>\n</tr>\n<tr>\n<td>Lifecycle (<code>editor</code> unless marked)</td>\n<td><code>rollback_canvas</code>, <code>unpublish_canvas</code>, <code>delete_version</code>, <code>archive_canvas</code>, <code>unarchive_canvas</code>, <code>delete_canvas</code> (owner), <code>transfer_canvas</code> (owner)</td>\n</tr>\n<tr>\n<td>Settings (<code>editor</code>)</td>\n<td><code>update_canvas</code>, <code>set_capabilities</code>, <code>set_canvas_slug</code>, <code>set_canvas_preview</code>, <code>regenerate_deploy_key</code>; <code>list_canvas_connections</code> reads the admin-granted profiles</td>\n</tr>\n<tr>\n<td>Sharing (<code>editor</code>)</td>\n<td><code>grant_access</code>, <code>invite_to_canvas</code>, <code>revoke_access</code>, <code>set_access_role</code></td>\n</tr>\n<tr>\n<td>Draft loop (<code>editor</code>)</td>\n<td><code>get_draft</code>, <code>read_draft_file</code>, <code>write_draft_file</code>, <code>delete_draft_file</code>, <code>rename_draft_file</code>, <code>publish_draft</code>, <code>restore_draft</code></td>\n</tr>\n<tr>\n<td>Teams (<code>any</code>)</td>\n<td><code>list_teams</code>, <code>create_team</code>, <code>rename_team</code>, <code>delete_team</code>, <code>add_team_member</code>, <code>remove_team_member</code>, <code>cancel_team_invite</code>, <code>list_team_members</code></td>\n</tr>\n</tbody></table>\n<p>Working notes:</p>\n<ul>\n<li>Typical flow: <code>create_canvas</code>, deploy, verify. <code>create_canvas</code> returns the canvas\nview, the one-time <code>apiKey</code>, and a <code>deploy</code> block (<code>apiBase</code>, <code>zipUpload</code>,\n<code>staged.begin</code> / <code>stageBlob</code> / <code>finalize</code>, <code>readback</code>, and a ready-to-run <code>curl</code>\nwith the key filled in). A new canvas is empty and Restricted (<code>private</code> in the API); its URL serves\ncontent only after a deploy.</li>\n<li>Prefer <code>curl</code> for bytes. <code>deploy_canvas</code> takes exactly one of <code>zipBase64</code> or\n<code>files: [{path, content, encoding?}]</code> (<code>utf8</code> default, or <code>base64</code>) and, like\n<code>add_files</code>, inlines file content into the model context. When you can run\nshell commands, use the <code>deploy</code> block's curl, whole ZIP or staged; keep\n<code>deploy_canvas</code> for a small first publish without a shell.</li>\n<li>Verify through the server, not the URL. The live URL is behind org sign-in: a\nsigned-out GET is redirected to login (<code>oidc</code>) or answered <code>401</code> (<code>proxy</code>, <code>dev</code>)\nunless the canvas is on the <code>public_link</code> rung. Check the returned\n<code>{version, fileCount}</code>, <code>list_versions</code>, the <code>readback</code> URL, or <code>get_canvas_file</code>\n(no <code>path</code> lists the manifest; a <code>path</code> returns the content as <code>utf8</code> or\n<code>base64</code>; over 256 KiB it returns <code>truncated: true</code> instead).</li>\n<li><code>list_canvases</code> returns owned and edited canvases, each with <code>role</code>\n(<code>\"owner\"</code> or <code>\"editor\"</code>) and <code>owner</code>; the <code>role</code> parameter (<code>owned</code> or <code>edited</code>)\nnarrows. <code>query</code> is a forgiving filter over title, description, tags, and slug\n(case, accent, and whitespace insensitive; multiple words AND). <code>tags</code> is\nany-match. <code>sort</code> is <code>updated</code> (default), <code>created</code>, <code>title</code>, or <code>popular</code> (views\nover the last 30 days). <code>limit</code> defaults to 50, max 100. <code>list_shared_canvases</code>\nlists canvases you can open but do not manage: a direct grant, a listed team\nshare, or a listed whole-org share.</li>\n<li>Draft loop: <code>write_draft_file</code>, <code>delete_draft_file</code>, and <code>rename_draft_file</code> take\n<code>expectedHash</code> (the file's current <code>hash</code> from <code>get_draft</code> / <code>read_draft_file</code>,\nor <code>\"none\"</code> for a path you expect absent); a mismatch fails <code>DRAFT_CONFLICT</code> with\nthe current hash and last writer. Without <code>expectedHash</code> the write still fails\n<code>DRAFT_CONFLICT</code> when a different user wrote the file last, so two editors never\noverwrite each other silently. <code>write_draft_file</code> with <code>create: true</code> refuses an\nexisting path (<code>PATH_EXISTS</code>). <code>publish_draft</code> snapshots the draft into a live\nversion and returns <code>{version, versionId, fileCount, totalBytes}</code>.</li>\n<li><code>update_canvas</code> fields: <code>title</code> (max 200), <code>description</code> (max 2000, or <code>null</code>),\n<code>tags</code> (max 20, each max 50 chars; one set serves list filtering and the gallery),\n<code>access</code>, <code>discoverability</code> (<code>link_only</code> by default; <code>listed</code> shows a Team or\nWhole-org canvas in Shared and makes a Whole-org canvas gallery-eligible),\n<code>teamIds</code>, <code>password</code> (or <code>null</code> to clear), <code>sharedExpiresAt</code> (unix ms, or <code>null</code>),\n<code>spaFallback</code>, <code>previewMode</code> (<code>auto</code> or <code>off</code>; <code>set_canvas_preview</code> with an image\nsets <code>custom</code>), <code>galleryListed</code>, <code>galleryTemplatable</code>, and the owner-only\n<code>guestAiEnabled</code> / <code>guestAiCap</code>. Refusals you will meet: <code>SHARE_REQUIRES_PUBLISH</code>\n(sharing needs a published canvas), <code>ORG_REQUIRED</code>, <code>PUBLIC_LINKS_DISABLED</code>\n(instance switch off), <code>PUBLIC_NOT_ALLOWED</code> (the owner may not publish publicly),\n<code>PUBLIC_LINK_OWNER_GATED</code> (an editor asked for <code>public_link</code> on such a canvas),\n<code>TEAM_REQUIRED</code> / <code>TEAM_FORBIDDEN</code>. <code>set_capabilities</code> takes <code>backendEnabled</code>,\n<code>kv</code>, <code>files</code>, <code>ai</code>, <code>realtime</code>, <code>authoring</code>.</li>\n<li>People: <code>list_access</code> entries are <code>viewer</code> or <code>editor</code>; the owner also gets\n<code>transferCandidates</code>. <code>grant_access</code> takes exactly one of <code>email</code> or <code>teamId</code> plus\n<code>role</code>; a new email is <code>pending</code> until its first verified sign-in through the\ninstance's identity provider; legacy guests are viewers only\n(<code>GUEST_VIEWER_ONLY</code>). <code>set_access_role</code> changes an entry's role.\n<code>transfer_canvas</code> takes a user id (never an email) of an existing editor\n(<code>NOT_ELIGIBLE</code> otherwise) and returns <code>{ok, canvas, previousOwnerEditor, publicLinkReverted}</code>: the previous owner keeps editor access while their account\nis active, and a <code>public_link</code> rung reverts when the new owner lacks the\nentitlement.</li>\n<li>Tenancy: <code>whoami</code> returns <code>orgs</code>, <code>teams</code>, and <code>isGuest</code> (true only when an org\nboundary is configured and you belong to none). <code>create_canvas.orgId</code>: omit to\ndefault to your only org, pass <code>null</code> for a personal canvas; an org you do not\nbelong to fails <code>ORG_FORBIDDEN</code>. Under an active org boundary the <code>whole_org</code>\nrung needs an org-homed canvas (<code>ORG_REQUIRED</code>).</li>\n<li>Versions: <code>list_versions</code> carries a <code>downloadUrl</code> per version\n(<code>{base}/mcp/canvases/{id}/versions/{n}/download</code>, a ZIP fetched with the same\nMCP access token as a Bearer). <code>delete_version</code> removes a non-current version\nonly (<code>CURRENT_VERSION</code> otherwise).</li>\n</ul>\n<p>Parameters and return shapes for every tool: <a href=\"/docs/agents/mcp\">MCP server</a>.</p>\n<h2 id=\"browser-sdk-inside-a-canvas\">Browser SDK inside a canvas</h2>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">src</span>=<span class=\"hljs-string\">\"/sdk/v1.js\"</span>&gt;</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">type</span>=<span class=\"hljs-string\">\"module\"</span>&gt;</span><span class=\"language-javascript\">\n  <span class=\"hljs-keyword\">const</span> me = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-title function_\">me</span>();                     <span class=\"hljs-comment\">// { id, email, name, avatarUrl, kind }</span>\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">set</span>(<span class=\"hljs-string\">\"last-viewer\"</span>, me.<span class=\"hljs-property\">name</span>);\n  <span class=\"hljs-keyword\">const</span> views = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">increment</span>(<span class=\"hljs-string\">\"views\"</span>); <span class=\"hljs-comment\">// 1 on the first call, then 2, 3, ...</span>\n</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span></code></pre>\n<p>One global, <code>window.canvasdrop</code>; there is no <code>cd</code> alias and no version property.\nZero config: the slug and API base are read from the page URL (<code>/c/{slug}/</code> in\npath mode, <code>{slug}.{host}</code> in subdomain mode), every call goes to\n<code>{apiBase}/v1/c/{slug}/...</code> with the session cookie, and no key ever reaches the\npage. The root-relative <code>src</code> resolves on the canvas's own origin in both modes.\nThe canvas must have Backend switched on (see Capabilities below); <code>/sdk/v1.js</code>\nsits behind the same sign-in as the canvas.</p>\n<ul>\n<li><code>me()</code> returns <code>{ id, email, name, avatarUrl, kind }</code>. <code>kind</code> is <code>\"member\"</code>;\n<code>\"guest\"</code> appears only for retained legacy guest sessions, since new Add person\ngrants materialize as signed-in users.</li>\n<li><code>kv</code> (shared) and <code>kv.user</code> (per viewer, keyed server-side) have the same five\nmethods: <code>get(key)</code> returns the value or <code>null</code>; <code>set(key, value)</code> stores any\nJSON except <code>null</code>; <code>delete(key)</code> is idempotent; <code>list({ prefix?, cursor?, limit? })</code>\nreturns <code>{ entries: [{ key, value }], nextCursor }</code> (<code>limit</code> 1 to 1000, default\n100); <code>increment(key, by = 1)</code> returns the new number and fails <code>NOT_NUMERIC</code> on\na non-numeric value. Key max 512 bytes, value max 64 KiB, 10 000 shared and\n1 000 per-user keys per canvas.</li>\n<li><code>files</code>: <code>upload(file)</code> returns <code>{ id, name, size, url }</code> with an absolute <code>url</code>;\n<code>list()</code> returns <code>[{ id, name, size, mime, createdAt }]</code>; <code>delete(id)</code>; <code>url(id)</code>\nis synchronous. 25 MiB per file, 1 GiB per canvas.</li>\n<li><code>ai</code>: <code>chat(messages, { model, system?, maxTokens? })</code> returns <code>{ text, usage: { inputTokens, outputTokens, cacheCreationInputTokens, cacheReadInputTokens }, cost }</code>; <code>stream(messages, options)</code> returns an <code>AsyncIterable&lt;string&gt;</code> of text\ndeltas with no usage. <code>model</code> is required and must be on the instance allowlist\n(<code>MODEL_NOT_ALLOWED</code>). Messages are <code>{ role: \"user\" | \"assistant\", content }</code>; the\nsystem prompt goes in <code>options.system</code>; <code>maxTokens</code> defaults to 1024, cap 8192.\nThe provider key stays server-side.</li>\n<li><code>realtime.channel(name)</code> returns a handle with <code>publish(event, data)</code> (fire and\nforget, buffered while reconnecting), <code>subscribe(handler)</code> where <code>handler</code>\nreceives <code>{ event, data, from: { id, name } }</code> and the call returns <code>void</code>,\n<code>unsubscribe()</code> (clears every handler on the channel), <code>presence()</code> resolving to\n<code>[{ id, name }]</code>, <code>onPresence</code>, <code>onJoin</code>, <code>onLeave</code>, and <code>close()</code>. One shared\nsocket per page with automatic reconnect; 30 connections per canvas, 16 KiB per\nmessage. There is no generic <code>.on(...)</code>.</li>\n<li><code>connections.fetch(profile, path, init?)</code> makes a bounded server-side request through\nan admin-granted profile. <code>path</code> must be root-relative; <code>init.method</code> must be one the\nadmin selected. The profile fixes one exact HTTPS DNS origin and adds write-only\nprotected headers last. Upstream 4xx/5xx remain normal <code>Response</code> objects; platform\npolicy failures throw. Public-link viewers remain static-only.</li>\n<li><code>canvases</code> (the <code>authoring</code> capability, off by default per canvas and per\ninstance): <code>publish</code>, <code>update</code>, <code>list</code>, <code>revoke</code> let a signed-in viewer create\nand manage a share canvas from a page.</li>\n</ul>\n<p>Full signatures and types: <a href=\"/docs/sdk/overview\">SDK overview</a>. Raw routes:\n<a href=\"/docs/api/runtime-api\">Runtime API</a>.</p>\n<h2 id=\"sharing-and-access\">Sharing and access</h2>\n<p>One access rung per canvas, set on the Share tab or with <code>update_canvas.access</code>.\nAccess is evaluated on every request, so a revoke, an expiry, or a role change\ntakes effect on the next request; a canvas you may not open reads as <code>404</code>.</p>\n<p>The people-and-teams list (<code>grant_access</code> / <code>revoke_access</code>) always applies: a viewer or\neditor row, or membership of a granted team, opens the canvas at every <code>access</code> value.\n<code>access</code> (General access) says who else can:</p>\n<table>\n<thead>\n<tr>\n<th><code>access</code></th>\n<th>Who else can open it</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>private</code></td>\n<td>nobody beyond the owner, the editors, and the list (<strong>Restricted</strong>). <code>specific_people</code> and <code>team</code> are legacy aliases of this value — accepted, stored, treated identically</td>\n</tr>\n<tr>\n<td><code>whole_org</code></td>\n<td>any signed-in org member; <code>discoverability: \"listed\"</code> shows it in Shared for them and makes it gallery-eligible</td>\n</tr>\n<tr>\n<td><code>public_link</code></td>\n<td>anyone with the link, while the instance switch is on and the owner may publish publicly (<code>canPublishPublic</code>); static only for everyone except the owner and editors: every primitive answers <code>403 STATIC_ONLY</code></td>\n</tr>\n</tbody></table>\n<p>An editor grant, direct or through an editor-role team, also manages the canvas. Editors\nskip the password gate and the share expiry. A password lock answers\n<code>403 PASSWORD_REQUIRED</code> on the runtime API until the viewer passes the gate; a\nshare expiry (<code>sharedExpiresAt</code>) turns other viewers away with\n<code>404 SHARE_EXPIRED</code> once it passes. Details: <a href=\"/docs/authoring/sharing\">Sharing &amp; access</a>.</p>\n<h2 id=\"capabilities\">Capabilities</h2>\n<p><code>backendEnabled</code> is off by default. With it on, <code>kv</code>, <code>files</code>, <code>ai</code>, and\n<code>realtime</code> default on and <code>authoring</code> defaults off; each toggles independently.\nEffective rule: <code>identity = backend</code>; <code>kv = backend &amp;&amp; capKv</code>; <code>files = backend &amp;&amp; capFiles</code>; <code>ai = backend &amp;&amp; capAi &amp;&amp; provider key configured</code>; <code>realtime = backend &amp;&amp; capRealtime &amp;&amp; CANVAS_DROP_REALTIME=on</code> (the default); <code>authoring = backend &amp;&amp; capAuthoring &amp;&amp; CANVAS_DROP_AUTHORING=on</code> (default <code>off</code>).</p>\n<p>An off feature answers <code>403 {\"code\":\"CAPABILITY_DISABLED\",\"capability\":\"kv\", \"backendEnabled\":false,\"reason\":\"backend_off\"|\"feature_off\"|\"operator_disabled\", \"hint\":\"...\"}</code>; the SDK throws <code>CapabilityDisabledError</code>. Toggle from the canvas's\nBackend tab, <code>PATCH /api/canvases/{id}/capabilities</code>, or <code>set_capabilities</code>.\nDetails: <a href=\"/docs/authoring/capabilities\">Capabilities</a>.</p>\n<h2 id=\"errors\">Errors</h2>\n<p>Every failure carries a stable string <code>code</code>; branch on it, not on message text.\nThe SDK throws <code>CanvasdropError</code> with <code>.code</code> and <code>.status</code>, plus six subclasses:\n<code>NotAuthenticatedError</code> (any 401), <code>NotFoundError</code> (any 404),\n<code>CapabilityDisabledError</code> (403 <code>CAPABILITY_DISABLED</code>, with <code>.hint</code>),\n<code>QuotaExceededError</code> (<code>QUOTA_EXCEEDED</code>, <code>GUEST_AI_CAP</code>, <code>KEY_LIMIT</code>, and every 413\nsize code), <code>PublishFailedError</code> (502, from <code>canvasdrop.canvases.publish</code>, with\nthe new canvas <code>.id</code>), and <code>UpdatePartialError</code> (502, from <code>canvasdrop.canvases.update</code>\nwhen the settings saved but the bundle deploy failed, with <code>.stage</code> and <code>.current</code>).\nEvery other code arrives as a plain <code>CanvasdropError</code>.\n<code>kv.get</code> returns <code>null</code> for a missing key instead of throwing.</p>\n<p>Codes you will meet most on the runtime API: <code>401 {\"error\":\"unauthorized\"}</code> (no\nsession; <code>oidc</code> redirects to login instead), <code>NOT_FOUND</code> / <code>ARCHIVED</code> /\n<code>OWNER_ONLY</code> / <code>SHARE_EXPIRED</code> 404, <code>DISABLED</code> 403, <code>PASSWORD_REQUIRED</code> 403,\n<code>STATIC_ONLY</code> 403, <code>CAPABILITY_DISABLED</code> 403, <code>CROSS_CANVAS_FORBIDDEN</code> /\n<code>CROSS_SITE_FORBIDDEN</code> 403, <code>MODEL_NOT_ALLOWED</code> 403, <code>INVALID_BODY</code> 400,\n<code>KEY_TOO_LARGE</code> / <code>VALUE_TOO_LARGE</code> / <code>FILE_TOO_LARGE</code> 413, <code>KEY_LIMIT</code> 409,\n<code>NOT_NUMERIC</code> 409, <code>QUOTA_EXCEEDED</code> 429 (409 on files), <code>CONNECTION_LIMIT</code> 429,\n<code>RATE_LIMITED</code> 429, <code>AI_STREAM_TRUNCATED</code> / <code>AI_UPSTREAM_ERROR</code> 502. Full table:\n<a href=\"/docs/api/errors\">Error codes</a>.</p>\n<h2 id=\"rate-limits\">Rate limits</h2>\n<p>Defaults; each is an env var the operator can change.</p>\n<table>\n<thead>\n<tr>\n<th>Surface</th>\n<th>Default</th>\n<th>Keyed by</th>\n<th>Env var</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Deploy API (<code>deploy</code>, <code>uploads</code> begin, <code>finalize</code>, <code>rollback</code>)</td>\n<td>10/min</td>\n<td>canvas</td>\n<td><code>CANVAS_DROP_RATELIMIT_DEPLOY_PER_MIN</code></td>\n</tr>\n<tr>\n<td>Runtime API <code>/v1/c/{slug}/...</code></td>\n<td>120/min</td>\n<td>user + canvas</td>\n<td><code>CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN</code></td>\n</tr>\n<tr>\n<td>Runtime AI <code>/v1/c/{slug}/ai/...</code></td>\n<td>10/min</td>\n<td>user</td>\n<td><code>CANVAS_DROP_RATELIMIT_AI_PER_MIN</code></td>\n</tr>\n<tr>\n<td>Runtime Connections</td>\n<td>60/min per actor/profile; 600/min per profile</td>\n<td>actor + canvas + profile; profile</td>\n<td><code>CANVAS_DROP_CONNECTIONS_ACTOR_PER_MIN</code>, <code>CANVAS_DROP_CONNECTIONS_PROFILE_PER_MIN</code></td>\n</tr>\n<tr>\n<td>MCP <code>/mcp</code></td>\n<td>120/min</td>\n<td>account</td>\n<td><code>CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN</code></td>\n</tr>\n</tbody></table>\n<p>For a packaged, installable version of this guidance, see the\n<a href=\"/docs/agents/skill\">Agent skill</a>.</p>\n",
    "headings": [
      {
        "id": "deploy-with-a-key",
        "text": "Deploy with a key",
        "level": 2
      },
      {
        "id": "connect-over-mcp",
        "text": "Connect over MCP",
        "level": 2
      },
      {
        "id": "browser-sdk-inside-a-canvas",
        "text": "Browser SDK inside a canvas",
        "level": 2
      },
      {
        "id": "sharing-and-access",
        "text": "Sharing and access",
        "level": 2
      },
      {
        "id": "capabilities",
        "text": "Capabilities",
        "level": 2
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      },
      {
        "id": "rate-limits",
        "text": "Rate limits",
        "level": 2
      }
    ],
    "text": "llms.txt If you are an agent putting a canvas on a canvas-drop instance, start here. The instance serves this page and its companions (Overview, Quickstart, Capabilities, SDK overview, Deploy API, Runtime API, Error codes) as one plain-text file at {base}/llms.txt , meant to be dropped straight into context. It is public: served on the instance's base host ahead of the sign-in gateway, so you can read the contract before you hold any credential. {base} is the instance origin (a fresh local instance is http://localhost:3000 ). There are three ways in. Pick by what you hold. You hold Use Reach A per-canvas secret key ( cd_... ) Deploy API at {base}/v1/canvases/{id}/... that one canvas: deploy, read back, roll back, unpublish An MCP-capable host MCP at {base}/mcp (OAuth 2.1, no key to paste) every canvas the signed-in account owns or edits; 47 tools Code running inside a canvas page Browser SDK at {base}/sdk/v1.js , global canvasdrop the six fixed primitives for that canvas: KV, files, AI, identity, realtime, Connections Two verbs recur below. Publish turns the editor draft into an immutable version. Deploy (the Deploy API, deploy_canvas , the staged upload) publishes directly with no draft step. Both create a new version at the same URL; the last 10 versions are kept. Deploy with a key Get a canvas and its key. A person creates the canvas on the dashboard's Create page with Use the API , or you call create_canvas over MCP. Either mints the canvas plus a one-time secret key, shown once. PUT a ZIP with index.html at its root: curl -fsS -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip # 200 {\"url\":\"...\",\"version\":7,\"fileCount\":12,\"totalBytes\":348201,\"warnings\":[]} Verify through the server, not the URL: curl -fsS \"{base}/v1/canvases/{id}/files\" -H \"Authorization: Bearer $CANVAS_KEY \" # 200 {\"version\":7,\"fileCount\":12,\"files\":[{\"path\":\"index.html\",\"size\":1204,\"mime\":\"text/html\",\"hash\":\"...\"}]} curl -fsS \"{base}/v1/canvases/{id}/files?path=index.html\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" | sha256sum # the live file's raw bytes; compare the hash with what you shipped The deploy publishes a live version immediately. {id} is the canvas id, not the slug. The Deploy API host is CANVAS_DROP_API_BASE_URL , which defaults to the instance base URL and can differ from the dashboard host; create_canvas and the dashboard both return the exact endpoints for the canvas, so never guess the host. The key is verified per canvas. A missing or unknown key, including a key for an archived, disabled, or deleted canvas, answers 401 {\"error\":\"unauthorized\"} ; a key for a different canvas answers 403 . Validation failures answer {\"code\", \"message\",\"path\"} : 400 on the ZIP path with EMPTY_DEPLOY , TOO_MANY_FILES , FILE_TOO_LARGE , CANVAS_TOO_LARGE , INVALID_ZIP , INVALID_PATH , ZIP_SLIP_REJECTED , or ZIP_BOMB_REJECTED ; a ZIP body over the canvas cap is refused before it is read with 413 CANVAS_TOO_LARGE . Deploys, staged begin/finalize, and rollbacks are throttled at 10 per minute per canvas ( 429 {\"error\":\"rate_limited\"} with Retry-After ). Companion routes, same Bearer key: Route Purpose GET /v1/canvases/{id} {id, slug, url, title, status, publicationState, accessMode, currentVersionId} ( accessMode : restricted | whole_org | public_link — who else can open it beyond the people-and-teams list) GET /v1/canvases/{id}/versions {versions: [{number, source, status, createdBy, createdAt, fileCount, totalBytes, current}]} GET /v1/canvases/{id}/files the live manifest as JSON; ?path= returns that file's raw bytes; 404 NOT_PUBLISHED before the first deploy POST /v1/canvases/{id}/rollback body {\"version\": 6} ; makes that ready version current and returns {url, version} ; 404 when no ready version has that number POST /v1/canvases/{id}/unpublish back to Draft: {url, publicationState: \"draft\", currentVersionId: null} ; 409 CANNOT_UNPUBLISH when not published For large or repeat deploys, the staged flow sends only changed blobs: POST /v1/canvases/{id}/uploads with {\"manifest\":[{\"path\",\"hash\",\"size\"}]} (sha256 hex) returns {uploadId, missingHashes} ; PUT /v1/canvases/{id}/uploads/{uploadId}/blobs/{hash} with the raw bytes of each missing blob returns 204 ; POST /v1/canvases/{id}/uploads/{uploadId}/finalize returns the same DeployResult . A session lives 15 minutes and finalizes once. Staged errors use the same {code, message} shape at a mapped status: size caps 413 ; an unknown or foreign uploadId 404 UPLOAD_HANDLE_INVALID ; UPLOAD_ALREADY_FINALIZED and UPLOAD_IN_PROGRESS 409 ; UPLOAD_EXPIRED , UPLOAD_MISSING_BLOB , BLOB_HASH_MISMATCH , INVALID_MANIFEST 400 . Limits: 100 MB per canvas, 25 MB per file, 2 000 files. Full contract: Deploy API . Connect over MCP Add {base}/mcp to an MCP-capable host. First use runs OAuth 2.1 against canvas-drop itself (RFC 8414/9728 discovery, Dynamic Client Registration, PKCE S256 ), sends you through the instance's normal org sign-in, and returns a 1 h access token plus a rotating refresh token. Every call re-checks that the account is still active. Transport is Streamable HTTP, stateless; calls are limited to 120 per minute per account and request bodies to 110 MiB. An instance with CANVAS_DROP_MCP=off has no /mcp endpoint at all. A first session, as tool calls: whoami {} -> { id , email, name, orgs, teams, isGuest } create_canvas { \"title\" : \"Retro board\" } -> { id , slug, url, apiKey, deploy, ... } apiKey is returned once deploy_canvas { \"id\" : \"<id>\" , \"files\" : [{ \"path\" : \"index.html\" , \"content\" : \"<h1>Hi</h1>\" }] } -> { url, version : 1, fileCount : 1, totalBytes, warnings : [] } get_canvas_file { \"id\" : \"<id>\" , \"path\" : \"index.html\" } -> { version, path, size, mime, hash, encoding : \"utf8\" , content } Scope and roles. Tools act on the canvases the account owns or edits. A canvas you hold no role on reads as canvas not found , for admins too. Each tool has a minimum role: any (identity, lists, create, teams), editor (everything on a canvas), or owner ( delete_canvas , transfer_canvas ). An editor calling an owner-only tool, or setting guestAiEnabled / guestAiCap through update_canvas , gets OWNER_ONLY: ... ; get_canvas echoes the list as ownerOnlyActs: [\"delete\", \"transfer\", \"guest_ai\"] . id parameters are canvas ids (team tools take team ids), never slugs. Results are JSON in a text content block; failures are isError: true with the text CODE: message . An admin-disabled canvas stays readable but every mutation fails DISABLED: ... ; an archived canvas refuses deploy and publish with NOT_ACTIVE . Group Tools Identity, lists, create ( any ) whoami , list_canvases , list_shared_canvases , create_canvas , clone_canvas Read ( editor ) get_canvas , list_versions , get_canvas_file , get_canvas_usage , list_access , search_people Deploy ( editor ; publishes live immediately) deploy_canvas , begin_deploy , add_files , finalize_deploy Lifecycle ( editor unless marked) rollback_canvas , unpublish_canvas , delete_version , archive_canvas , unarchive_canvas , delete_canvas (owner), transfer_canvas (owner) Settings ( editor ) update_canvas , set_capabilities , set_canvas_slug , set_canvas_preview , regenerate_deploy_key ; list_canvas_connections reads the admin-granted profiles Sharing ( editor ) grant_access , invite_to_canvas , revoke_access , set_access_role Draft loop ( editor ) get_draft , read_draft_file , write_draft_file , delete_draft_file , rename_draft_file , publish_draft , restore_draft Teams ( any ) list_teams , create_team , rename_team , delete_team , add_team_member , remove_team_member , cancel_team_invite , list_team_members Working notes: Typical flow: create_canvas , deploy, verify. create_canvas returns the canvas view, the one-time apiKey , and a deploy block ( apiBase , zipUpload , staged.begin / stageBlob / finalize , readback , and a ready-to-run curl with the key filled in). A new canvas is empty and Restricted ( private in the API); its URL serves content only after a deploy. Prefer curl for bytes. deploy_canvas takes exactly one of zipBase64 or files: [{path, content, encoding?}] ( utf8 default, or base64 ) and, like add_files , inlines file content into the model context. When you can run shell commands, use the deploy block's curl, whole ZIP or staged; keep deploy_canvas for a small first publish without a shell. Verify through the server, not the URL. The live URL is behind org sign-in: a signed-out GET is redirected to login ( oidc ) or answered 401 ( proxy , dev ) unless the canvas is on the public_link rung. Check the returned {version, fileCount} , list_versions , the readback URL, or get_canvas_file (no path lists the manifest; a path returns the content as utf8 or base64 ; over 256 KiB it returns truncated: true instead). list_canvases returns owned and edited canvases, each with role ( \"owner\" or \"editor\" ) and owner ; the role parameter ( owned or edited ) narrows. query is a forgiving filter over title, description, tags, and slug (case, accent, and whitespace insensitive; multiple words AND). tags is any-match. sort is updated (default), created , title , or popular (views over the last 30 days). limit defaults to 50, max 100. list_shared_canvases lists canvases you can open but do not manage: a direct grant, a listed team share, or a listed whole-org share. Draft loop: write_draft_file , delete_draft_file , and rename_draft_file take expectedHash (the file's current hash from get_draft / read_draft_file , or \"none\" for a path you expect absent); a mismatch fails DRAFT_CONFLICT with the current hash and last writer. Without expectedHash the write still fails DRAFT_CONFLICT when a different user wrote the file last, so two editors never overwrite each other silently. write_draft_file with create: true refuses an existing path ( PATH_EXISTS ). publish_draft snapshots the draft into a live version and returns {version, versionId, fileCount, totalBytes} . update_canvas fields: title (max 200), description (max 2000, or null ), tags (max 20, each max 50 chars; one set serves list filtering and the gallery), access , discoverability ( link_only by default; listed shows a Team or Whole-org canvas in Shared and makes a Whole-org canvas gallery-eligible), teamIds , password (or null to clear), sharedExpiresAt (unix ms, or null ), spaFallback , previewMode ( auto or off ; set_canvas_preview with an image sets custom ), galleryListed , galleryTemplatable , and the owner-only guestAiEnabled / guestAiCap . Refusals you will meet: SHARE_REQUIRES_PUBLISH (sharing needs a published canvas), ORG_REQUIRED , PUBLIC_LINKS_DISABLED (instance switch off), PUBLIC_NOT_ALLOWED (the owner may not publish publicly), PUBLIC_LINK_OWNER_GATED (an editor asked for public_link on such a canvas), TEAM_REQUIRED / TEAM_FORBIDDEN . set_capabilities takes backendEnabled , kv , files , ai , realtime , authoring . People: list_access entries are viewer or editor ; the owner also gets transferCandidates . grant_access takes exactly one of email or teamId plus role ; a new email is pending until its first verified sign-in through the instance's identity provider; legacy guests are viewers only ( GUEST_VIEWER_ONLY ). set_access_role changes an entry's role. transfer_canvas takes a user id (never an email) of an existing editor ( NOT_ELIGIBLE otherwise) and returns {ok, canvas, previousOwnerEditor, publicLinkReverted} : the previous owner keeps editor access while their account is active, and a public_link rung reverts when the new owner lacks the entitlement. Tenancy: whoami returns orgs , teams , and isGuest (true only when an org boundary is configured and you belong to none). create_canvas.orgId : omit to default to your only org, pass null for a personal canvas; an org you do not belong to fails ORG_FORBIDDEN . Under an active org boundary the whole_org rung needs an org-homed canvas ( ORG_REQUIRED ). Versions: list_versions carries a downloadUrl per version ( {base}/mcp/canvases/{id}/versions/{n}/download , a ZIP fetched with the same MCP access token as a Bearer). delete_version removes a non-current version only ( CURRENT_VERSION otherwise). Parameters and return shapes for every tool: MCP server . Browser SDK inside a canvas < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); // { id, email, name, avatarUrl, kind } await canvasdrop. kv . set ( \"last-viewer\" , me. name ); const views = await canvasdrop. kv . increment ( \"views\" ); // 1 on the first call, then 2, 3, ... </ script > One global, window.canvasdrop ; there is no cd alias and no version property. Zero config: the slug and API base are read from the page URL ( /c/{slug}/ in path mode, {slug}.{host} in subdomain mode), every call goes to {apiBase}/v1/c/{slug}/... with the session cookie, and no key ever reaches the page. The root-relative src resolves on the canvas's own origin in both modes. The canvas must have Backend switched on (see Capabilities below); /sdk/v1.js sits behind the same sign-in as the canvas. me() returns { id, email, name, avatarUrl, kind } . kind is \"member\" ; \"guest\" appears only for retained legacy guest sessions, since new Add person grants materialize as signed-in users. kv (shared) and kv.user (per viewer, keyed server-side) have the same five methods: get(key) returns the value or null ; set(key, value) stores any JSON except null ; delete(key) is idempotent; list({ prefix?, cursor?, limit? }) returns { entries: [{ key, value }], nextCursor } ( limit 1 to 1000, default 100); increment(key, by = 1) returns the new number and fails NOT_NUMERIC on a non-numeric value. Key max 512 bytes, value max 64 KiB, 10 000 shared and 1 000 per-user keys per canvas. files : upload(file) returns { id, name, size, url } with an absolute url ; list() returns [{ id, name, size, mime, createdAt }] ; delete(id) ; url(id) is synchronous. 25 MiB per file, 1 GiB per canvas. ai : chat(messages, { model, system?, maxTokens? }) returns { text, usage: { inputTokens, outputTokens, cacheCreationInputTokens, cacheReadInputTokens }, cost } ; stream(messages, options) returns an AsyncIterable<string> of text deltas with no usage. model is required and must be on the instance allowlist ( MODEL_NOT_ALLOWED ). Messages are { role: \"user\" | \"assistant\", content } ; the system prompt goes in options.system ; maxTokens defaults to 1024, cap 8192. The provider key stays server-side. realtime.channel(name) returns a handle with publish(event, data) (fire and forget, buffered while reconnecting), subscribe(handler) where handler receives { event, data, from: { id, name } } and the call returns void , unsubscribe() (clears every handler on the channel), presence() resolving to [{ id, name }] , onPresence , onJoin , onLeave , and close() . One shared socket per page with automatic reconnect; 30 connections per canvas, 16 KiB per message. There is no generic .on(...) . connections.fetch(profile, path, init?) makes a bounded server-side request through an admin-granted profile. path must be root-relative; init.method must be one the admin selected. The profile fixes one exact HTTPS DNS origin and adds write-only protected headers last. Upstream 4xx/5xx remain normal Response objects; platform policy failures throw. Public-link viewers remain static-only. canvases (the authoring capability, off by default per canvas and per instance): publish , update , list , revoke let a signed-in viewer create and manage a share canvas from a page. Full signatures and types: SDK overview . Raw routes: Runtime API . Sharing and access One access rung per canvas, set on the Share tab or with update_canvas.access . Access is evaluated on every request, so a revoke, an expiry, or a role change takes effect on the next request; a canvas you may not open reads as 404 . The people-and-teams list ( grant_access / revoke_access ) always applies: a viewer or editor row, or membership of a granted team, opens the canvas at every access value. access (General access) says who else can: access Who else can open it private nobody beyond the owner, the editors, and the list ( Restricted ). specific_people and team are legacy aliases of this value — accepted, stored, treated identically whole_org any signed-in org member; discoverability: \"listed\" shows it in Shared for them and makes it gallery-eligible public_link anyone with the link, while the instance switch is on and the owner may publish publicly ( canPublishPublic ); static only for everyone except the owner and editors: every primitive answers 403 STATIC_ONLY An editor grant, direct or through an editor-role team, also manages the canvas. Editors skip the password gate and the share expiry. A password lock answers 403 PASSWORD_REQUIRED on the runtime API until the viewer passes the gate; a share expiry ( sharedExpiresAt ) turns other viewers away with 404 SHARE_EXPIRED once it passes. Details: Sharing & access . Capabilities backendEnabled is off by default. With it on, kv , files , ai , and realtime default on and authoring defaults off; each toggles independently. Effective rule: identity = backend ; kv = backend && capKv ; files = backend && capFiles ; ai = backend && capAi && provider key configured ; realtime = backend && capRealtime && CANVAS_DROP_REALTIME=on (the default); authoring = backend && capAuthoring && CANVAS_DROP_AUTHORING=on (default off ). An off feature answers 403 {\"code\":\"CAPABILITY_DISABLED\",\"capability\":\"kv\", \"backendEnabled\":false,\"reason\":\"backend_off\"|\"feature_off\"|\"operator_disabled\", \"hint\":\"...\"} ; the SDK throws CapabilityDisabledError . Toggle from the canvas's Backend tab, PATCH /api/canvases/{id}/capabilities , or set_capabilities . Details: Capabilities . Errors Every failure carries a stable string code ; branch on it, not on message text. The SDK throws CanvasdropError with .code and .status , plus six subclasses: NotAuthenticatedError (any 401), NotFoundError (any 404), CapabilityDisabledError (403 CAPABILITY_DISABLED , with .hint ), QuotaExceededError ( QUOTA_EXCEEDED , GUEST_AI_CAP , KEY_LIMIT , and every 413 size code), PublishFailedError (502, from canvasdrop.canvases.publish , with the new canvas .id ), and UpdatePartialError (502, from canvasdrop.canvases.update when the settings saved but the bundle deploy failed, with .stage and .current ). Every other code arrives as a plain CanvasdropError . kv.get returns null for a missing key instead of throwing. Codes you will meet most on the runtime API: 401 {\"error\":\"unauthorized\"} (no session; oidc redirects to login instead), NOT_FOUND / ARCHIVED / OWNER_ONLY / SHARE_EXPIRED 404, DISABLED 403, PASSWORD_REQUIRED 403, STATIC_ONLY 403, CAPABILITY_DISABLED 403, CROSS_CANVAS_FORBIDDEN / CROSS_SITE_FORBIDDEN 403, MODEL_NOT_ALLOWED 403, INVALID_BODY 400, KEY_TOO_LARGE / VALUE_TOO_LARGE / FILE_TOO_LARGE 413, KEY_LIMIT 409, NOT_NUMERIC 409, QUOTA_EXCEEDED 429 (409 on files), CONNECTION_LIMIT 429, RATE_LIMITED 429, AI_STREAM_TRUNCATED / AI_UPSTREAM_ERROR 502. Full table: Error codes . Rate limits Defaults; each is an env var the operator can change. Surface Default Keyed by Env var Deploy API ( deploy , uploads begin, finalize , rollback ) 10/min canvas CANVAS_DROP_RATELIMIT_DEPLOY_PER_MIN Runtime API /v1/c/{slug}/... 120/min user + canvas CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN Runtime AI /v1/c/{slug}/ai/... 10/min user CANVAS_DROP_RATELIMIT_AI_PER_MIN Runtime Connections 60/min per actor/profile; 600/min per profile actor + canvas + profile; profile CANVAS_DROP_CONNECTIONS_ACTOR_PER_MIN , CANVAS_DROP_CONNECTIONS_PROFILE_PER_MIN MCP /mcp 120/min account CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN For a packaged, installable version of this guidance, see the Agent skill ."
  },
  {
    "path": "agents/skill",
    "section": "For agents",
    "title": "Agent skill",
    "html": "<h1 id=\"agent-skill\">Agent skill</h1>\n<p>Install this skill so your coding agent can create, deploy, verify, and extend\ncanvases on this instance first try. It is a standard skill package: a <code>SKILL.md</code>\nwhose <code>name</code> / <code>description</code> frontmatter carries the when-to-use trigger, so an\nagent host that discovers skills loads it on its own when a task matches. <code>{base}</code>\nbelow is the instance origin (a fresh local instance is <code>http://localhost:3000</code>).</p>\n<h2 id=\"install\">Install</h2>\n<p><code>GET {base}/skill.zip</code> is public. No session, cookie, or API key is required, so an\nagent fetches, unpacks, and checks it in three commands:</p>\n<pre><code class=\"hljs language-bash\">curl -fL <span class=\"hljs-string\">\"{base}/skill.zip\"</span> -o canvas-drop-skill.zip\nunzip -o canvas-drop-skill.zip -d ~/.claude/skills/\n<span class=\"hljs-built_in\">ls</span> ~/.claude/skills/canvas-drop\n<span class=\"hljs-comment\"># SKILL.md  examples</span></code></pre>\n<p>The archive unpacks to one folder:</p>\n<pre><code class=\"hljs\">canvas-<span class=\"hljs-keyword\">drop</span>/\n  SKILL.md          # frontmatter: <span class=\"hljs-type\">name</span>: canvas-<span class=\"hljs-keyword\">drop</span>, description: &lt;<span class=\"hljs-keyword\">when</span> <span class=\"hljs-keyword\">to</span> use&gt;\n  examples/poll.md  # a single-file poll <span class=\"hljs-keyword\">on</span> KV, <span class=\"hljs-keyword\">with</span> an optional realtime <span class=\"hljs-keyword\">add</span>-<span class=\"hljs-keyword\">on</span></code></pre>\n<p>The command above uses Claude Code's per-user skills directory; a <code>.claude/skills/</code>\nfolder inside a repo scopes the skill to that project. For any other host, drop the\n<code>canvas-drop/</code> folder wherever it discovers skills, or point the agent at the\nunpacked <code>SKILL.md</code> directly. The skill carries no instance-specific configuration:\nit refers to the instance as <code>{base}</code> and asks the user for the base URL when it\ndoes not know it.</p>\n<h2 id=\"the-endpoint\">The endpoint</h2>\n<table>\n<thead>\n<tr>\n<th>Fact</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Route</td>\n<td><code>GET {base}/skill.zip</code></td>\n</tr>\n<tr>\n<td>Auth</td>\n<td>None. Served ahead of the sign-in gateway, next to <code>/docs</code>, <code>/llms.txt</code>, and <code>/og.png</code>.</td>\n</tr>\n<tr>\n<td>Host</td>\n<td>The instance's base host only (<code>CANVAS_DROP_BASE_URL</code>). In <code>subdomain</code> URL mode, <code>/skill.zip</code> on a canvas host such as <code>{slug}.canvases.example.com</code> belongs to that canvas (its own file, or its 404). In <code>path</code> mode every host is the base host, so nothing collides.</td>\n</tr>\n<tr>\n<td>Response</td>\n<td><code>200</code> with <code>Content-Type: application/zip</code>, <code>Content-Disposition: attachment; filename=\"canvas-drop-skill.zip\"</code>, <code>Cache-Control: public, max-age=3600</code>.</td>\n</tr>\n<tr>\n<td>Contents</td>\n<td><code>canvas-drop/SKILL.md</code> plus every <code>canvas-drop/examples/*.md</code>. Nothing else can appear: the server zips an explicit allowlist (<code>SKILL.md</code> and the Markdown files in <code>examples/</code>), never a directory glob, so a stray file or secret is never served. A missing <code>examples/</code> folder still yields a valid zip with <code>SKILL.md</code> alone.</td>\n</tr>\n<tr>\n<td><code>404</code></td>\n<td>The server cannot read <code>skill/canvas-drop/SKILL.md</code> on disk. See <a href=\"#keeping-it-current\">Keeping it current</a>.</td>\n</tr>\n</tbody></table>\n<h2 id=\"what-the-skill-teaches\">What the skill teaches</h2>\n<p>Inside, the skill covers the same surface as this docs site, condensed for an agent\nthat is parsing rather than reading.</p>\n<table>\n<thead>\n<tr>\n<th>Section</th>\n<th>What the agent learns</th>\n<th>Full reference</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Rules that always hold</td>\n<td>Never put a secret in canvas files. Canvases are static files with no server-side build step. Backend is off by default: a primitive call throws <code>CapabilityDisabledError</code> (<code>code: \"CAPABILITY_DISABLED\"</code>, status 403) until an owner or editor turns it on (the Backend tab, or <code>set_capabilities</code> over MCP). A deploy is live at once and the last 10 versions are kept. Public link is static-only (<code>STATIC_ONLY</code>, 403).</td>\n<td><a href=\"/docs/authoring/capabilities\">Capabilities</a></td>\n</tr>\n<tr>\n<td>Pick a path</td>\n<td><code>curl</code> against the Deploy API for file transfer, so bytes stream from disk instead of through the model; MCP for everything else. The MCP deploy tools inline file bytes and suit a small first publish.</td>\n<td><a href=\"/docs/agents/llms\">llms.txt</a></td>\n</tr>\n<tr>\n<td>Connect over MCP</td>\n<td>Add <code>{base}/mcp</code>, sign in once through the instance's own login, then call identity-scoped tools with no key to paste: <code>whoami</code>, <code>list_canvases</code>, <code>create_canvas</code>, <code>deploy_canvas</code>, the staged <code>begin_deploy</code> / <code>add_files</code> / <code>finalize_deploy</code> upload, <code>get_canvas_file</code> to verify what went live, <code>rollback_canvas</code>, the draft-editor loop, sharing and people tools, teams, and <code>list_canvas_connections</code>. 47 tools in all, each acting only on canvases the account owns or edits; anything else reads as <code>canvas not found</code>, and an owner-only act (<code>delete_canvas</code>, <code>transfer_canvas</code>, the guest-AI fields of <code>update_canvas</code>) fails with <code>OWNER_ONLY</code> for an editor.</td>\n<td><a href=\"/docs/agents/mcp\">MCP server</a></td>\n</tr>\n<tr>\n<td>Deploy over HTTP</td>\n<td>A per-canvas <code>cd_...</code> key as <code>Authorization: Bearer</code>. <code>PUT {base}/v1/canvases/{id}/deploy</code> with a ZIP body publishes immediately. For large or repeat deploys, the staged upload (<code>POST .../uploads</code>, <code>PUT .../uploads/{uploadId}/blobs/{hash}</code>, <code>POST .../uploads/{uploadId}/finalize</code>) sends only the blobs the server does not hold. Read-back and recovery routes (<code>GET .../{id}</code>, <code>.../versions</code>, <code>.../files[?path=]</code>, <code>POST .../rollback</code>, <code>POST .../unpublish</code>) confirm a deploy and undo it. The agent takes the exact URLs from the <code>deploy</code> block that <code>create_canvas</code> and <code>get_canvas</code> return instead of guessing the API host.</td>\n<td><a href=\"/docs/api/deploy-api\">Deploy API</a></td>\n</tr>\n<tr>\n<td>Add backend capability</td>\n<td>One <code>&lt;script src=\"/sdk/v1.js\"&gt;&lt;/script&gt;</code> tag defines the global <code>window.canvasdrop</code> (there is no <code>cd</code> alias) and rides the signed-in session cookie, so the six fixed primitives (KV, files, AI, identity via <code>me()</code>, realtime, admin-granted Connections) work with no keys in canvas code. The skill lists the exact signatures that trip agents up, such as <code>ai.chat(messages, { model })</code>, <code>connections.fetch(profile, path, init?)</code>, and no generic <code>channel.on(event)</code>.</td>\n<td><a href=\"/docs/sdk/overview\">SDK overview</a></td>\n</tr>\n<tr>\n<td>Errors</td>\n<td>Branch on the stable <code>err.code</code> and <code>err.status</code>, never on the message. The skill carries the full code table and which codes arrive as typed subclasses.</td>\n<td><a href=\"/docs/api/errors\">Error codes</a></td>\n</tr>\n<tr>\n<td>Verify through the server</td>\n<td>The live URL is access-controlled, so an unauthenticated <code>GET</code> returns a login page, not the files. The agent confirms a deploy with the returned <code>{version, fileCount}</code>, with <code>list_versions</code> (<code>current: true</code>), or with <code>get_canvas_file</code>.</td>\n<td><a href=\"/docs/agents/mcp\">MCP server</a></td>\n</tr>\n</tbody></table>\n<h2 id=\"keeping-it-current\">Keeping it current</h2>\n<p>The server builds the zip in-process from its own <code>skill/canvas-drop/</code> files at\nstartup (memoized for the life of the process), so what you download always matches\nthe version the instance is running. After an instance upgrade, run the install\ncommand again; <code>unzip -o</code> overwrites the previous copy.</p>\n<p>A <code>404</code> from <code>{base}/skill.zip</code> means the running server cannot read\n<code>skill/canvas-drop/SKILL.md</code>. The Docker image ships the directory; a hand-rolled\ndeploy must copy <code>skill/</code> next to the server, the same way it copies\n<code>docs/site/assets</code>. That is an operator fix, not something the agent can work around.</p>\n<h2 id=\"lighter-alternative\">Lighter alternative</h2>\n<p>For a copy-into-context version with no install step, use\n<a href=\"/llms.txt\"><code>{base}/llms.txt</code></a>, the single-file agent quick reference. It is public\ntoo, served on the same base host. The <a href=\"/docs/agents/llms\">llms.txt page</a> explains\nwhat it contains.</p>\n",
    "headings": [
      {
        "id": "install",
        "text": "Install",
        "level": 2
      },
      {
        "id": "the-endpoint",
        "text": "The endpoint",
        "level": 2
      },
      {
        "id": "what-the-skill-teaches",
        "text": "What the skill teaches",
        "level": 2
      },
      {
        "id": "keeping-it-current",
        "text": "Keeping it current",
        "level": 2
      },
      {
        "id": "lighter-alternative",
        "text": "Lighter alternative",
        "level": 2
      }
    ],
    "text": "Agent skill Install this skill so your coding agent can create, deploy, verify, and extend canvases on this instance first try. It is a standard skill package: a SKILL.md whose name / description frontmatter carries the when-to-use trigger, so an agent host that discovers skills loads it on its own when a task matches. {base} below is the instance origin (a fresh local instance is http://localhost:3000 ). Install GET {base}/skill.zip is public. No session, cookie, or API key is required, so an agent fetches, unpacks, and checks it in three commands: curl -fL \"{base}/skill.zip\" -o canvas-drop-skill.zip unzip -o canvas-drop-skill.zip -d ~/.claude/skills/ ls ~/.claude/skills/canvas-drop # SKILL.md examples The archive unpacks to one folder: canvas- drop / SKILL.md # frontmatter: name : canvas- drop , description: < when to use> examples/poll.md # a single-file poll on KV, with an optional realtime add - on The command above uses Claude Code's per-user skills directory; a .claude/skills/ folder inside a repo scopes the skill to that project. For any other host, drop the canvas-drop/ folder wherever it discovers skills, or point the agent at the unpacked SKILL.md directly. The skill carries no instance-specific configuration: it refers to the instance as {base} and asks the user for the base URL when it does not know it. The endpoint Fact Value Route GET {base}/skill.zip Auth None. Served ahead of the sign-in gateway, next to /docs , /llms.txt , and /og.png . Host The instance's base host only ( CANVAS_DROP_BASE_URL ). In subdomain URL mode, /skill.zip on a canvas host such as {slug}.canvases.example.com belongs to that canvas (its own file, or its 404). In path mode every host is the base host, so nothing collides. Response 200 with Content-Type: application/zip , Content-Disposition: attachment; filename=\"canvas-drop-skill.zip\" , Cache-Control: public, max-age=3600 . Contents canvas-drop/SKILL.md plus every canvas-drop/examples/*.md . Nothing else can appear: the server zips an explicit allowlist ( SKILL.md and the Markdown files in examples/ ), never a directory glob, so a stray file or secret is never served. A missing examples/ folder still yields a valid zip with SKILL.md alone. 404 The server cannot read skill/canvas-drop/SKILL.md on disk. See Keeping it current . What the skill teaches Inside, the skill covers the same surface as this docs site, condensed for an agent that is parsing rather than reading. Section What the agent learns Full reference Rules that always hold Never put a secret in canvas files. Canvases are static files with no server-side build step. Backend is off by default: a primitive call throws CapabilityDisabledError ( code: \"CAPABILITY_DISABLED\" , status 403) until an owner or editor turns it on (the Backend tab, or set_capabilities over MCP). A deploy is live at once and the last 10 versions are kept. Public link is static-only ( STATIC_ONLY , 403). Capabilities Pick a path curl against the Deploy API for file transfer, so bytes stream from disk instead of through the model; MCP for everything else. The MCP deploy tools inline file bytes and suit a small first publish. llms.txt Connect over MCP Add {base}/mcp , sign in once through the instance's own login, then call identity-scoped tools with no key to paste: whoami , list_canvases , create_canvas , deploy_canvas , the staged begin_deploy / add_files / finalize_deploy upload, get_canvas_file to verify what went live, rollback_canvas , the draft-editor loop, sharing and people tools, teams, and list_canvas_connections . 47 tools in all, each acting only on canvases the account owns or edits; anything else reads as canvas not found , and an owner-only act ( delete_canvas , transfer_canvas , the guest-AI fields of update_canvas ) fails with OWNER_ONLY for an editor. MCP server Deploy over HTTP A per-canvas cd_... key as Authorization: Bearer . PUT {base}/v1/canvases/{id}/deploy with a ZIP body publishes immediately. For large or repeat deploys, the staged upload ( POST .../uploads , PUT .../uploads/{uploadId}/blobs/{hash} , POST .../uploads/{uploadId}/finalize ) sends only the blobs the server does not hold. Read-back and recovery routes ( GET .../{id} , .../versions , .../files[?path=] , POST .../rollback , POST .../unpublish ) confirm a deploy and undo it. The agent takes the exact URLs from the deploy block that create_canvas and get_canvas return instead of guessing the API host. Deploy API Add backend capability One <script src=\"/sdk/v1.js\"></script> tag defines the global window.canvasdrop (there is no cd alias) and rides the signed-in session cookie, so the six fixed primitives (KV, files, AI, identity via me() , realtime, admin-granted Connections) work with no keys in canvas code. The skill lists the exact signatures that trip agents up, such as ai.chat(messages, { model }) , connections.fetch(profile, path, init?) , and no generic channel.on(event) . SDK overview Errors Branch on the stable err.code and err.status , never on the message. The skill carries the full code table and which codes arrive as typed subclasses. Error codes Verify through the server The live URL is access-controlled, so an unauthenticated GET returns a login page, not the files. The agent confirms a deploy with the returned {version, fileCount} , with list_versions ( current: true ), or with get_canvas_file . MCP server Keeping it current The server builds the zip in-process from its own skill/canvas-drop/ files at startup (memoized for the life of the process), so what you download always matches the version the instance is running. After an instance upgrade, run the install command again; unzip -o overwrites the previous copy. A 404 from {base}/skill.zip means the running server cannot read skill/canvas-drop/SKILL.md . The Docker image ships the directory; a hand-rolled deploy must copy skill/ next to the server, the same way it copies docs/site/assets . That is an operator fix, not something the agent can work around. Lighter alternative For a copy-into-context version with no install step, use {base}/llms.txt , the single-file agent quick reference. It is public too, served on the same base host. The llms.txt page explains what it contains."
  }
];

export const DOC_NAV: NavSection[] = [
  {
    "section": "Getting started",
    "pages": [
      {
        "path": "",
        "title": "Overview"
      },
      {
        "path": "quickstart",
        "title": "Quickstart"
      }
    ]
  },
  {
    "section": "Authoring canvases",
    "pages": [
      {
        "path": "authoring/create-and-publish",
        "title": "Create & publish"
      },
      {
        "path": "authoring/sharing",
        "title": "Sharing & access"
      },
      {
        "path": "authoring/teams",
        "title": "Teams"
      },
      {
        "path": "authoring/capabilities",
        "title": "Capabilities"
      },
      {
        "path": "authoring/editor",
        "title": "The editor"
      }
    ]
  },
  {
    "section": "Browser SDK",
    "pages": [
      {
        "path": "sdk/overview",
        "title": "Overview"
      },
      {
        "path": "sdk/kv",
        "title": "Key–value storage"
      },
      {
        "path": "sdk/files",
        "title": "File storage"
      },
      {
        "path": "sdk/identity",
        "title": "Identity"
      },
      {
        "path": "sdk/ai",
        "title": "AI"
      },
      {
        "path": "sdk/realtime",
        "title": "Realtime"
      },
      {
        "path": "sdk/connections",
        "title": "Connections"
      },
      {
        "path": "sdk/authoring",
        "title": "Authoring"
      }
    ]
  },
  {
    "section": "API reference",
    "pages": [
      {
        "path": "api/deploy-api",
        "title": "Deploy API"
      },
      {
        "path": "api/runtime-api",
        "title": "Runtime API"
      },
      {
        "path": "api/errors",
        "title": "Error codes"
      }
    ]
  },
  {
    "section": "Self-hosting",
    "pages": [
      {
        "path": "self-hosting/install",
        "title": "Install"
      },
      {
        "path": "self-hosting/configuration",
        "title": "Configuration"
      },
      {
        "path": "self-hosting/security-model",
        "title": "Security model"
      },
      {
        "path": "self-hosting/deploy",
        "title": "Deploy"
      },
      {
        "path": "self-hosting/cdn",
        "title": "Behind a CDN"
      },
      {
        "path": "self-hosting/screenshots",
        "title": "Screenshots"
      }
    ]
  },
  {
    "section": "For agents",
    "pages": [
      {
        "path": "agents/mcp",
        "title": "MCP server"
      },
      {
        "path": "agents/llms",
        "title": "llms.txt"
      },
      {
        "path": "agents/skill",
        "title": "Agent skill"
      }
    ]
  }
];

export const SEARCH_INDEX: SearchEntry[] = [
  {
    "path": "",
    "title": "Overview",
    "headings": [
      "Try it",
      "What ships today",
      "Trust posture",
      "Status",
      "Where to go next"
    ],
    "text": "canvas-drop Deploy and share the small web tools your org builds, behind your sign-in. This page is for anyone deciding whether to run canvas-drop, or wondering what it does today. By the end you know what a canvas is, what ships, what is still open, and which page to read next for your job. canvas-drop is an open-source (MIT), self-hostable platform where signed-in members of an organization publish and share small web artifacts called canvases . A canvas is static files (HTML, CSS, JS, images). Drop them in and they are live at a URL your colleagues can open once they sign in. It is inspired by Shopify's Quick; canvas-drop is the open way to run the idea on your own infrastructure. The constraint is the product: a small, fixed set of primitives done well, not a general-purpose hosting platform. There is no server-side build step and no secret in the page. A canvas gains backend capability only through six fixed primitives (KV, files, AI, identity, realtime, and admin-granted Connections) served by a zero-config browser SDK, and that backend stays off until the owner or an editor turns it on. Try it A signed-in local instance with no external services ( path URLs, SQLite, local disk, dev auth). You need Node 24 or newer and pnpm: git clone https://github.com/markpasternak/canvas-drop.git cd canvas-drop pnpm install cp .env.example . env pnpm dev # dashboard http://localhost:5173 · server http://localhost:3000 Open http://localhost:5173 , click Create canvas , choose Paste HTML , and paste an index.html . It is created and published in one step and live at http://localhost:3000/c/{slug}/ . To give it a backend, switch on Enable backend in the canvas's Backend tab and add one tag to the page: < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); const views = await canvasdrop. kv . increment ( \"views\" ); </ script > The Quickstart walks the rest of the loop: deploy, edit, share, roll back. For the production shape in"
  },
  {
    "path": "quickstart",
    "title": "Quickstart",
    "headings": [
      "Run an instance locally",
      "Who you are signed in as",
      "Prefer Docker?",
      "Create a canvas",
      "Add content",
      "Give it a backend (optional)",
      "Publish and share",
      "Deploy as an agent",
      "Where next"
    ],
    "text": "Quickstart For anyone running canvas-drop for the first time: by the end of this page you have an instance on your machine, a canvas at a live URL, and the three ways to put content on it (the dashboard, the editor, and the keyed Deploy API). The default profile needs no external services: SQLite, local disk storage, path URL mode, and dev auth that signs you in automatically. Run an instance locally You need Node 24 or newer ( .nvmrc says 24 ) and pnpm; the repo pins pnpm@11.0.9 in package.json . git clone https://github.com/markpasternak/canvas-drop.git cd canvas-drop pnpm install cp .env.example . env pnpm dev URL What it serves http://localhost:5173 The dashboard (Vite dev server with HMR). It proxies /api , /auth , /v1 , /docs , /llms.txt , /skill.zip , /welcome , and /og.png to the server. http://localhost:3000 The Hono server: management API, Deploy API, MCP at /mcp , docs, and the canvases themselves at /c/{slug}/ . Canvas URLs always point here, not at :5173 . curl http://localhost:3000/healthz # 200 {\"status\":\"ok\",\"db\":\"ok\",\"version\":\"...\"} (503 \"degraded\" until the DB answers) What pnpm dev does: it loads .env once (Node's --env-file-if-exists , so variables you export in the shell still win), seeds an empty database with sample canvases, then runs the server ( tsx watch ), the dashboard ( vite ), and the browser SDK build (esbuild --watch ) in parallel. Only pnpm dev reads .env ; a production process takes its config from the environment (see Configuration ). In production the server serves the built dashboard itself on :3000 ; the Vite server is dev-only. The sample data is 100 canvases with no files across seven owners, 70 of them yours, so the dashboard, Shared , and the gallery are populated on first boot. The seed runs only when the database has no canvases. Command Effect pnpm dev:stop Stops the whole tree (Ctrl-C works too). pnpm dev:restart restarts it. CANVAS_DROP_DEV_SEED=0 pnpm dev Starts without the sample canvases. pnpm reset:data Deletes th"
  },
  {
    "path": "authoring/create-and-publish",
    "title": "Create & publish",
    "headings": [
      "Ways in",
      "Options at create time",
      "Files or folder",
      "Upload a ZIP",
      "Paste HTML",
      "New version on an existing canvas",
      "Editor",
      "What every publish enforces",
      "Custom slug",
      "Deploy API",
      "Versions and rollback"
    ],
    "text": "Create & publish You have HTML, a folder of static files, or a build directory, and you want a live URL that people in your org can open. By the end of this page you will have published a canvas, know what a publish does, and be able to control its URL. The shortest path, from a signed-in dashboard: Open /new and pick Paste HTML . Paste your markup, optionally give it a title, and click Create and publish . Save the key from the dialog (it is shown once), then Go to canvas . The Overview tab has the URL: {base}/c/{slug}/ in path mode, https://{slug}.canvases.example.com/ in subdomain mode. The same from a terminal, with the canvas id and key shown when you create a canvas: curl -fsS -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip # 200 {\"url\":\"…\",\"version\":1,\"fileCount\":12,\"totalBytes\":48213,\"warnings\":[]} Ways in Source Where What it does Paste HTML /new , or New version on a canvas Stores your markup as index.html and publishes it in one step Files or folder /new , or New version Uploads with relative paths preserved and publishes Upload ZIP /new , or New version Extracts the archive server-side and publishes Use the API /new Mints an empty canvas plus its key; the first PUT …/deploy publishes Editor The Editor tab Autosaves a draft ; you click Publish when ready Deploy API / MCP Scripts and agents Publishes directly to live with a key or over MCP flowchart TD subgraph Sources[\"Ways in\"] DnD[Files or folder] Zip[Upload ZIP] Paste[Paste HTML] API[Deploy API / MCP] Ed[Editor] end DnD --> Pub{Publish} Zip --> Pub Paste --> Pub API --> Pub Ed --> Draft[(Draft, autosaved)] Draft -->|keep editing| Draft Draft -->|Publish when ready| Pub Pub --> V[Immutable version N, content-addressed] V --> Live([Served at the canvas URL]) Live -.->|Make current| Prev[Re-point to an earlier version] Prev -.-> Live Every publish writes an immutable version : files are stored by content hash, and the live URL is a pointer at one"
  },
  {
    "path": "authoring/sharing",
    "title": "Sharing & access",
    "headings": [
      "Share with one colleague",
      "General access",
      "Slugs are not access control",
      "Access vs discovery",
      "Roles: viewers and editors",
      "Two editors, one file",
      "Transfer ownership",
      "Adding specific people",
      "Sharing with a team",
      "Protection: password & expiry",
      "Public links",
      "AI for added people",
      "Gallery, description, and tags",
      "Finding canvases",
      "When an admin disables a canvas",
      "Revoking",
      "Over MCP"
    ],
    "text": "Sharing & access You own or edit a canvas and want to decide who can open it, who edits it with you, and where it shows up. Every control on this page lives on the canvas's Share tab. Agents reach the same controls over MCP , through the same service and the same role checks. Every canvas starts Restricted : only you, the owner, can open it. Sharing is two controls plus optional layers, and they read the way Google Docs does: People and teams with direct access names who can open the canvas, each as a viewer or an editor . This list always applies , whatever the next control says. General access says who else can open it: Restricted (nobody beyond the list), Whole org , or Public link . Optionally add a password or a share expiry under Protection . Optionally opt into discovery: List for your org (the Shared page, Whole org only) or List in the gallery . Share with one colleague Publish first. Until the canvas has a live version, the Share tab shows one locked panel with a Publish button. Under People and teams with direct access , keep People selected, type their email, pick Viewer or Editor , and choose Add . That is all: they can open the canvas on their next request, with General access still on Restricted . Over MCP it is one call: grant_access { \"id\": \"<canvas id>\", \"email\": \"someone@example.com\", \"role\": \"viewer\" } Opening General access to Whole org or Public link on an unpublished or archived canvas is refused with SHARE_REQUIRES_PUBLISH (HTTP 409); the people-and-teams list can be filled in before publishing. Admins have no back door into your content. For a canvas they do not own, an admin is an ordinary member: a Restricted canvas returns 404, a password prompts them too, and they cannot open the editor or change settings. Cross-owner admin power is moderation and offboarding only: the all-canvases list, disable / re-enable / restore, and reassigning the owner when someone leaves. General access One choice per canvas, stored as access (default private )."
  },
  {
    "path": "authoring/teams",
    "title": "Teams",
    "headings": [
      "Personal vs org teams",
      "Creating a team and adding people",
      "The roster",
      "Sharing a canvas with a team",
      "Editor teams",
      "Over MCP (agents)"
    ],
    "text": "Teams A team is a named group you share canvases with — the slice between \"just me\" and \"everyone.\" Share a canvas with a team from its Share tab, and every member can open and use it (full backend, like a member). Manage teams on the Teams page in the dashboard, or over MCP . A team is one kind of entry under People and teams with direct access , next to individual people. Whatever is on that list can open the canvas at every General access choice: General access Who else can open it Restricted Nobody beyond the owner, the editors, and the people and teams on the list. Whole org Anyone signed in to your organization. Public link Anyone with the URL — static files only while the instance switch is on and the owner account has not been revoked. See Sharing & access for the full model and the password / expiry modifiers. Personal vs org teams The kind of team is a fixed choice at creation : Personal — friends & family. Any signed-in user can create one (no org required) and add people by email when policy allows it. Direct membership is the access boundary. Org-attached — a subset of your org. Only org members can create one, and members must belong to that org. Access is re-checked against your live org membership on every request, so someone removed from the org loses access at once — even if a stale membership row lingers. Team names are per-creator : you can't make two teams with the same name, but different people can each have a team named \"Design.\" Creating a team and adding people On the Teams page, pick Personal (or your org), name the team, and create it — you're its first member and its manager. Then add people by email: An existing user joins immediately. A brand-new person (no account yet) becomes Pending sign-in . They turn into a full member the first time they sign in through your instance's configured auth — there's no app-owned password and no magic-link account to manage. Until they sign in, they show as Pending on the roster and simply can't open a"
  },
  {
    "path": "authoring/capabilities",
    "title": "Capabilities",
    "headings": [
      "Turn on the backend",
      "The toggles",
      "When a feature is effective",
      "Limits",
      "When a feature is off",
      "Public links are static-only",
      "Authoring",
      "Clones start static",
      "Why off by default"
    ],
    "text": "Capabilities Give a canvas a backend, one feature at a time, and know exactly when a call from the page will work. This page is for a canvas's owner or editor. A canvas is static until you switch Enable backend on in its Backend tab. With the backend on, five features toggle independently: KV, files, AI, realtime, and authoring. Identity ( me() ) has no toggle; it is on whenever the backend is on. Outbound Connections also has no owner-controlled feature toggle: each reusable profile is granted to the canvas by an instance administrator and remains gated by the same Backend master switch. Turn on the backend Open the canvas and go to the Backend tab. Switch Enable backend on. It is off by default. You can also set it when you create the canvas: Enable backend (optional) on the create page, or \"backendEnabled\": true on POST /api/canvases and POST /api/canvases/paste . Check the feature toggles. Key-value storage , File storage , AI , and Realtime are on by default, so they go live as soon as the backend is on (where the instance supports them). Authoring starts off and stays off until you turn it on. Then call the features from the page through window.canvasdrop . No keys, no setup: < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); // on whenever the backend is on await canvasdrop. kv . set ( \"last-viewer\" , me. name ); // needs Key-value storage on </ script > Agents and scripts flip the same switches. The MCP tool set_capabilities and PATCH {base}/api/canvases/{id}/capabilities take the same body: any subset of backendEnabled , kv , files , ai , realtime , authoring as booleans. Omitted fields are unchanged. { \"backendEnabled\" : true , \"realtime\" : false } The response is the canvas view. Every canvas view (the management API and the MCP get_canvas tool included) carries two objects: capabilities (what is stored) and effective (what runs right now, after the instance switches below are applied). Owners and edit"
  },
  {
    "path": "authoring/editor",
    "title": "The editor",
    "headings": [
      "Draft and published version",
      "When a direct deploy lands",
      "Archived and disabled canvases",
      "Two people, one file",
      "Files",
      "The root page",
      "Preview",
      "Page text mode",
      "Versions",
      "The draft over HTTP"
    ],
    "text": "The editor Change a canvas's files in the browser, check the result, and publish when you are ready. Nothing viewers see changes until you publish. The editor is the Editor tab of any canvas you own or edit; there is no local setup and no build step. The loop: Open the canvas and pick Editor . On first open the draft is seeded from the current published version, or starts empty for a canvas that has never published. Edit files in the tree. Changes autosave to the draft 700 ms after you stop typing; ⌘S / Ctrl+S saves at once. Check the Preview pane, or Open full preview in a new tab. Press Publish (or ⌘↵ / Ctrl+Enter ). The draft becomes a new immutable version and the canvas URL serves it immediately. The toast names the version: Published version 7 . Editors get the same tab and the same controls as the owner (see Roles: viewers and editors ). Agents run the same loop over MCP with get_draft , read_draft_file , write_draft_file , rename_draft_file , delete_draft_file , publish_draft , and restore_draft (see MCP server ). Draft and published version Every canvas has exactly one draft : a working copy separate from the version viewers see. Saving changes the draft only; the canvas URL keeps serving the current version until you publish. Autosave runs 700 ms after you stop typing, with a final flush when you leave the editor or switch files. ⌘S / Ctrl+S in the code editor saves at once. The status line in the bar above the editor tells you where your edit is, in this order of precedence: Status Meaning Saving... A save is in flight. Save conflict Someone else saved this file first. See Two people, one file . Save failed The save errored. Your text stays in the editor; edit again or publish to retry (publishing saves first). Unsaved changes The edit is still in the autosave window. Unpublished changes The draft is saved and differs from the published version. Behind the published version The draft has no edits of its own, but a direct deploy moved the live version unde"
  },
  {
    "path": "sdk/overview",
    "title": "Overview",
    "headings": [
      "Add it to a canvas",
      "One global: canvasdrop",
      "How the SDK finds your canvas",
      "Turn the backend on first",
      "The surface",
      "Errors"
    ],
    "text": "Browser SDK You have a static canvas and want it to keep data, accept files, call a model, know who is looking, or sync between viewers. One <script> tag gives the page a global, canvasdrop , carrying the six fixed primitives (KV, files, AI, identity, realtime, and admin-granted outbound Connections) plus authoring. There is no build step and nothing to configure in the page: identity comes from the viewer's session, the canvas is identified from its own URL, and no key ever reaches the browser. Add it to a canvas One script tag, then call the global: < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); // { id, email, name, avatarUrl, kind } await canvasdrop. kv . set ( \"last-viewer\" , me. name ); const views = await canvasdrop. kv . increment ( \"views\" ); // 1 on the first call, then 2, 3, ... </ script > The SDK tag is a plain <script> , so it runs before your module; type=\"module\" gives you top-level await . The root-relative src resolves against the canvas's own origin, and the server serves the script there in both URL modes, behind the same sign-in as the canvas. A fresh local instance ( pnpm dev , dev auth) is already signed in, so the snippet runs as soon as the canvas has its backend on (see Turn the backend on first ). {base}/sdk/v1.js is a stable path for the life of v1, cached for an hour ( cache-control: public, max-age=3600 ), so deployed canvases pick up SDK fixes without a redeploy. On a local checkout, a 503 from that path means the bundle is not built yet: run pnpm build (or pnpm --filter @canvas-drop/sdk build ). One global: canvasdrop The script defines exactly one global, window.canvasdrop . There is no cd alias, no second name, and no version property on the object. The client has me() , kv , files , ai , realtime , connections , and canvases , and nothing else. The same client lives in the workspace package @canvas-drop/sdk (private, not published). Its module entry exports createClient , de"
  },
  {
    "path": "sdk/kv",
    "title": "Key–value storage",
    "headings": [
      "Two scopes, one interface",
      "Methods",
      "get",
      "set",
      "delete",
      "increment",
      "list",
      "Limits",
      "Errors",
      "HTTP calls behind each method"
    ],
    "text": "Key-value storage Store JSON that outlives a reload, from a static canvas, with no server code: counters, settings, form submissions, small documents. This page is the reference for canvasdrop.kv , the KV primitive on the canvasdrop global that <script src=\"/sdk/v1.js\"> defines in every canvas. By the end you can read, write, count, and page through keys in both scopes and handle every error KV returns. The canvas needs Enable backend on and the Key-value storage toggle on (it is pre-enabled) in its Backend tab; see Capabilities . < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > await canvasdrop. kv . set ( \"votes\" , 0 ); // any JSON value except null const n = await canvasdrop. kv . get ( \"votes\" ); // 0 (null if the key is absent) const total = await canvasdrop. kv . increment ( \"votes\" ); // 1, atomic +1 const stepped = await canvasdrop. kv . increment ( \"votes\" , 5 ); // 6, atomic +5 const page = await canvasdrop. kv . list ({ prefix : \"p:\" , limit : 100 }); // { entries, nextCursor } await canvasdrop. kv . delete ( \"votes\" ); // idempotent </ script > Two scopes, one interface Scope Namespace Who shares the keys HTTP base Shared canvasdrop.kv Every viewer of the canvas reads and writes the same values {base}/v1/c/{slug}/kv Per-viewer canvasdrop.kv.user Only the signed-in viewer; each person sees their own values {base}/v1/c/{slug}/kv/user Both namespaces have the same five methods ( KvNamespace ). The server derives the per-viewer scope from the viewer's session identity, never from anything your code sends, so one viewer cannot read or write another viewer's keys, and a canvas has no way to enumerate other viewers' per-viewer data. await canvasdrop. kv . user . set ( \"theme\" , \"dark\" ); const theme = await canvasdrop. kv . user . get ( \"theme\" ); // this viewer's value only Methods Signatures as declared in the SDK: Method Signature get get<T = unknown>(key: string): Promise<T | null> set set(key: string, value: unknown): Promise<void> dele"
  },
  {
    "path": "sdk/files",
    "title": "File storage",
    "headings": [
      "Methods",
      "upload",
      "list",
      "delete",
      "url",
      "Content URLs",
      "How content is served",
      "Limits",
      "Errors"
    ],
    "text": "File storage Let viewers upload files to a canvas and get back a URL you can put in an <img> , a link, or a fetch() . This page is the reference for canvasdrop.files , the files primitive on the canvasdrop global that <script src=\"/sdk/v1.js\"> defines in every canvas. By the end you can upload, list, serve, and delete files, and handle every error the primitive returns. The canvas needs Enable backend on and the File storage toggle on (it is pre-enabled) in its Backend tab; see Capabilities . There is nothing to configure in the page and no key to hold. < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > // <input type=\"file\" id=\"picker\"> and <img id=\"img\"> are on the page const f = await canvasdrop. files . upload (picker. files [ 0 ]); // { id, name, size, url } img. src = f. url ; // absolute content URL const all = await canvasdrop. files . list (); // FileMeta[] const href = canvasdrop. files . url (f. id ); // same URL as f.url, no request await canvasdrop. files . delete (f. id ); // the URL now returns 404 </ script > Files belong to the canvas, not to the viewer who uploaded them: every viewer who can open the canvas can list, read, and delete every file in it. There is no per-viewer scope for files (KV has kv.user ; files has no equivalent). The server records who uploaded each file for its audit trail, but the API does not expose or filter by uploader. Methods Signatures as declared in the SDK ( CanvasdropClient.files ), with the runtime API call each one makes: Method Signature HTTP call upload upload(file: File): Promise<{ id: string; name: string; size: number; url: string }> POST {base}/v1/c/{slug}/files list list(): Promise<FileMeta[]> GET {base}/v1/c/{slug}/files delete delete(id: string): Promise<void> DELETE {base}/v1/c/{slug}/files/{id} url url(id: string): string none (builds {base}/v1/c/{slug}/files/{id}/content ) interface FileMeta { id : string ; name : string ; size : number ; // bytes mime ?: string ; // always sent by the "
  },
  {
    "path": "sdk/identity",
    "title": "Identity",
    "headings": [
      "Signature",
      "Where the identity comes from",
      "Legacy guest sessions",
      "Errors",
      "Per-user data without plumbing"
    ],
    "text": "Identity Know who is looking at your canvas. This page is the reference for canvasdrop.me() , the identity primitive on the canvasdrop global that <script src=\"/sdk/v1.js\"> defines in every canvas. me() returns the signed-in viewer, resolved from the server-side session: the page never handles a token, and a viewer cannot claim to be someone else. By the end you can greet the viewer by name, key shared data per person, and handle every error me() returns. The canvas needs Enable backend on in its Backend tab. Identity has no toggle of its own: it is available whenever the backend is on (see Capabilities ). const me = await canvasdrop. me (); // { // id: \"0190a3f2-7c4e-7a1b-9d2f-3c5e6a7b8c9d\", // email: \"someone@example.com\", // name: \"Alex Rivera\", // avatarUrl: null, // kind: \"member\" // } document . querySelector ( \"#greeting\" ). textContent = `Hi, ${me.name} ` ; Call it once per page load and keep the result. The runtime API is rate-limited per viewer per canvas (120 requests a minute by default, one budget shared by me() , kv , and files ; ai has its own), so a me() on every render spends budget for nothing. Signature me (): Promise < Me > interface Me { id : string ; email : string ; name : string ; avatarUrl : string | null ; kind : \"member\" | \"guest\" ; } Field Type Notes id string Stable per-user id. Key per-user data on this, never on email . email string The viewer's email. name string Display name. avatarUrl string | null Avatar URL, or null when the identity provider gives none. kind \"member\" | \"guest\" \"member\" for a signed-in org user. \"guest\" is retained only for legacy guest sessions; see below. There is no \"anonymous\" : a signed-out visitor never reaches the runtime API. The projection is deliberately minimal. It carries no admin flag and no org membership, so canvas code cannot tell an admin from any other member. That information stays on the dashboard side. Where the identity comes from me() calls GET {base}/v1/c/{slug}/me with the viewer's session"
  },
  {
    "path": "sdk/ai",
    "title": "AI",
    "headings": [
      "Chat",
      "Stream",
      "Messages and options",
      "Models",
      "Prompt caching and cost",
      "Limits",
      "Errors"
    ],
    "text": "AI Call a language model from a static canvas without holding a provider key. This page is the reference for canvasdrop.ai , the AI primitive on the canvasdrop global that <script src=\"/sdk/v1.js\"> defines in every canvas. The page posts your messages to the server, the server runs the call with the instance's key and streams the reply back, and the key never reaches the browser. By the end you can await a reply, render it as it streams, read usage and cost, stay inside the model allowlist and spend caps, and handle every error AI returns. The canvas needs Enable backend on and the AI toggle on (it is pre-enabled) in its Backend tab; see Capabilities . The operator must also have configured a provider key for the instance ( CANVAS_DROP_AI_API_KEY , or the key an admin sets in Admin → Settings); without one the capability reads as off. < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const { text, usage, cost } = await canvasdrop. ai . chat ( [{ role : \"user\" , content : \"Summarise this poll result in one line.\" }], { model : \"claude-haiku-4-5\" }, ); // text: the model's reply (string) // usage: { inputTokens, outputTokens, cacheCreationInputTokens, cacheReadInputTokens } // cost: number (USD) </ script > Two entry points, both backed by POST /v1/c/{slug}/ai/chat : Method Returns Use when chat(messages, options) Promise<{ text, usage, cost }> You want the whole reply, plus usage and cost stream(messages, options) AsyncIterable<string> of text deltas You want to render the reply as it is generated messages must hold at least one message. options.model is required and must be on the instance's model allowlist . Calls go to Anthropic; there is no per-call provider choice. Chat chat(messages, options) collects every text delta and resolves once with { text, usage, cost } . It rejects if the model is not allowlisted, a quota or rate limit is hit, the provider fails, or the stream ends early; see Errors . cost is the call's price in USD, computed on the"
  },
  {
    "path": "sdk/realtime",
    "title": "Realtime",
    "headings": [
      "When realtime is on",
      "Channel methods",
      "What presence and messages mean",
      "How the connection works",
      "Limits and errors"
    ],
    "text": "Realtime Add live cursors, reactions, or a shared room to a canvas with canvasdrop.realtime : ephemeral pub/sub plus presence over named channels. By the end of this page you can publish to a channel, receive what others publish, read who is present, and handle the three ways a socket ends for good. Messages fan out to whoever is subscribed to the channel at that moment and are never stored; put anything that must survive a reload in KV . canvasdrop is already on window in any served canvas (see the SDK overview ) and connects to the right canvas from its URL. The from on every message and every entry in the presence list come from the viewer's server-side session, never from the page, so names are trustworthy. const room = canvasdrop. realtime . channel ( \"room-1\" ); // Receive what anyone on the channel publishes, including yourself. room. subscribe ( ( msg ) => { // msg = { event, data, from: { id, name } } console . log (msg. from . name , msg. event , msg. data ); }); // Broadcast to everyone subscribed right now. Fire-and-forget. room. publish ( \"cursor\" , { x : 120 , y : 48 }); // Who is in the channel right now. const users = await room. presence (); // [{ id, name }, ...] room. onJoin ( ( user ) => console . log (user. name , \"joined\" )); room. onLeave ( ( user ) => console . log (user. name , \"left\" )); room. unsubscribe (); // stop receiving messages; the handle stays usable room. close (); // done with this channel; the socket closes when none remain When realtime is on Realtime works when all three switches are on. A new canvas ships with the backend off, so the first step is usually turning that on. Switch Default Who sets it The canvas's backend (master switch) off The owner or an editor, in the canvas's Backend tab or with the set_capabilities MCP tool The canvas's realtime flag on Same place The instance switch CANVAS_DROP_REALTIME on The operator, in the server environment. The admin panel shows it read-only. When any of the three is off, the serve"
  },
  {
    "path": "sdk/connections",
    "title": "Connections",
    "headings": [
      "Fetch through a profile",
      "Responses and errors",
      "Fixed safety bounds",
      "Security boundary"
    ],
    "text": "Outbound Connections Use canvasdrop.connections.fetch when a canvas needs data from a third-party HTTP API that cannot be called directly from the browser—for example, an API that does not allow browser CORS or requires a fixed User-Agent or API key. Connections is a controlled request forwarder, not a Lambda runtime. Canvas Drop never runs canvas-supplied server code. An instance administrator creates a reusable profile for one exact HTTPS origin, chooses the allowed standard methods, stores any protected headers, and grants the profile to individual canvases. The protected values remain encrypted on the server. Fetch through a profile Suppose an administrator created the profile key stocks , approved https://api.example.com , allowed GET , fixed its User-Agent , and granted it to your canvas: const symbol = \"ACME\" ; const response = await canvasdrop. connections . fetch ( \"stocks\" , `/v2/quote?symbol= ${ encodeURIComponent (symbol)} ` , { headers : { accept : \"application/json\" } }, ); if (!response. ok ) { // This is the upstream's status and bounded response body, not a platform error. throw new Error ( `Stock provider returned ${response.status} ` ); } const quote = await response. json (); The signature is: connections. fetch ( profile : string , path : string , init ?: { method ?: \"GET\" | \"HEAD\" | \"POST\" | \"PUT\" | \"PATCH\" | \"DELETE\" ; headers ?: RequestInit [ \"headers\" ]; body ?: RequestInit [ \"body\" ]; signal ?: RequestInit [ \"signal\" ]; }, ): Promise < Response > path must start with one / . Absolute URLs and protocol-relative paths are rejected in the SDK before a request is sent. The path and query are appended to the profile's exact origin. Canvas code cannot change its scheme, hostname, or port. Only a method the administrator selected for that profile is accepted. Request bodies work for POST , PUT , PATCH , and DELETE ; GET and HEAD do not send one. headers accepts the normal browser HeadersInit shapes. Canvas cookies, authorization, forwarding, hop-b"
  },
  {
    "path": "sdk/authoring",
    "title": "Authoring",
    "headings": [
      "Methods",
      "AuthoredCanvas",
      "publish options",
      "update options",
      "list filter",
      "Update, revoke, and status",
      "Ownership, quotas, and authorization",
      "Errors"
    ],
    "text": "Authoring Let a signed-in viewer of your canvas create and manage canvases from the page — as themselves, with real ownership and no secret in the browser. A share created here is a managed artifact : you publish it once, update it in place (same URL) as its content changes, list your shares with their live status, and revoke one when it's done. The canvasdrop.canvases namespace wraps publish , update , list , and revoke . This is the authoring capability. It is off by default and higher-privilege than the other primitives: the owner turns it on in the Backend tab and the operator must enable it for the instance. Guests and public-link visitors can't use it — creation needs a signed-in org member. // Create a durable share once. const share = await canvasdrop. canvases . publish ({ title : \"Team roadmap — Q3 snapshot\" , access : \"public_link\" , // audience; defaults to Restricted (`private`) password : \"optional-extra-lock\" , // independent of the audience tags : [ \"roadmap\" ], metadata : { sourceApp : \"product-roadmap\" , sourceKind : \"roadmap-share\" , theme : \"light\" }, expiresAt : Date . now () + 7 * 864e5 , // optional; the operator may require/limit it bundle : zipBlob, // a static-site zip (Blob or ArrayBuffer) }); // share.url is the live canvas; it now appears in the viewer's own dashboard. // Later — update IN PLACE. The URL never changes; a new version is deployed. await canvasdrop. canvases . update (share. id , { bundle : newZipBlob, // omit to change only settings/metadata metadata : { ...share. metadata , itemCount : 42 , generatedAt : Date . now () }, }); // The management view — filter to this app's shares. const mine = await canvasdrop. canvases . list ({ sourceApp : \"product-roadmap\" }); // → [{ id, url, title, status, access, expiresAt, updatedAt, metadata, ... }] // Retire a share. Its URL goes dead, but it STAYS in list() as status \"revoked\". await canvasdrop. canvases . revoke (share. id ); Methods Method Returns publish(options) Promise<Authore"
  },
  {
    "path": "api/deploy-api",
    "title": "Deploy API",
    "headings": [
      "Deploy a version",
      "Staged upload (large or incremental)",
      "Get a canvas",
      "List versions",
      "Verify a deploy (read back the live version)",
      "Roll back",
      "Unpublish",
      "Errors"
    ],
    "text": "Deploy API Put a canvas live over HTTP from a script, CI, or an AI agent, with no dashboard session. You authenticate with the canvas's secret key as a Bearer token. A key operates only on its own canvas, and every response has a stable, machine-readable shape so a client can repair and retry without a human. This page covers the whole keyed loop: publish (one ZIP or a staged set of files), verify what shipped through the server, roll back, and unpublish. # Publish a ZIP. curl -fsS -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip # 200 {\"url\":\"<canvas URL>\",\"version\":7,\"fileCount\":12,\"totalBytes\":348201,\"warnings\":[]} # Read back the live manifest to confirm what is being served. curl -fsS \"{base}/v1/canvases/{id}/files\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" # 200 {\"version\":7,\"fileCount\":12,\"files\":[{\"path\":\"index.html\",\"size\":1280,\"mime\":\"text/html; charset=utf-8\",\"hash\":\"9f86d0...\"}, ...]} Base path: {base}/v1/canvases/{id} . {id} is the canvas id, not the slug. Auth: Authorization: Bearer cd_... , the canvas secret key. It is shown once: when the canvas is created ( POST /api/canvases answers 201 with an apiKey field; the MCP create_canvas tool returns it too) or regenerated ( POST /api/canvases/{id}/regenerate-key , MCP regenerate_deploy_key ). The server stores only its SHA-256 hash, and a clone never inherits the source canvas's key. This path takes no cookies and no CORS; it is separate from the session auth the Runtime API and browser SDK use. What is {base} ? The host serving the Deploy API: CANVAS_DROP_API_BASE_URL , which defaults to the instance base URL. Operators set it only when the API is routed on a different host than the canvases (for example canvases at {slug}.canvases.example.com and the API at api.canvases.example.com in subdomain mode), so do not assume it equals the dashboard host. You never have to guess: over MCP , create_canvas and regenerate_deploy_key (with the real key fill"
  },
  {
    "path": "api/runtime-api",
    "title": "Runtime API",
    "headings": [
      "Where the API lives",
      "Before a handler runs",
      "Identity",
      "Key-value",
      "Files",
      "AI",
      "Outbound Connections",
      "Realtime",
      "Authoring",
      "Adjacent endpoints",
      "Errors"
    ],
    "text": "Runtime API The runtime API is the HTTP surface a canvas calls from the browser: the six fixed primitives (KV, files, AI, identity, realtime, and admin-granted Connections) plus authoring, each under {base}/v1/c/{slug} . The browser SDK ( window.canvasdrop , served at {base}/sdk/v1.js ) builds every one of these requests, speaks the SSE and WebSocket wire formats, and maps errors to typed exceptions, so reach for it first. Use this page when you need the raw routes: reading the network tab, a non-JS client, or the exact shape a primitive returns. Requests are credentialed with the session cookie the browser already holds. Identity is resolved server-side from that session; the canvas never asserts who the viewer is. // Inside a canvas served in path mode at http://localhost:3000/c/quiet-otter-x7k2/ const api = \"/v1/c/quiet-otter-x7k2\" ; await fetch ( ` ${api} /kv/greeting` , { method : \"PUT\" , credentials : \"include\" , headers : { \"content-type\" : \"application/json\" }, body : JSON . stringify ( \"hi\" ), // the body is the value itself }); // 200 {\"ok\":true} const { value } = await ( await fetch ( ` ${api} /kv/greeting` , { credentials : \"include\" })). json (); // 200 {\"value\":\"hi\"} // The same two calls through the SDK: await canvasdrop. kv . set ( \"greeting\" , \"hi\" ); await canvasdrop. kv . get ( \"greeting\" ); // \"hi\" Where the API lives URL mode Canvas content Runtime API path {base}/c/{slug}/ Same origin: {base}/v1/c/{slug}/... subdomain https://{slug}.canvases.example.com/ The base host: https://canvases.example.com/v1/c/{slug}/... The route path is identical in both modes; only the origin changes. The SDK derives it from location : a pathname starting /c/{slug} means path mode and location.origin ; otherwise it strips the first hostname label and keeps the port. Every request goes out with credentials: \"include\" . CANVAS_DROP_API_BASE_URL shapes the Bearer-key Deploy API URLs ( /v1/canvases/... ), not this surface. Before a handler runs Every /v1/c/{slug}/* requ"
  },
  {
    "path": "api/errors",
    "title": "Error codes",
    "headings": [
      "The codes",
      "Typed SDK errors",
      "Server codes outside the enum",
      "Client-side codes",
      "AI stream errors",
      "Realtime close codes"
    ],
    "text": "Error codes If you call the Runtime API or the browser SDK from a canvas, or you are an agent handling its failures, this page is the contract. Branch on the error's code , never on its message text. Every refusal from /v1/c/{slug}/* is JSON with a stable code and an HTTP status, and the browser SDK throws typed errors that extend CanvasdropError , each with a readonly .code and .status (plus .hint when the server sent a remediation hint). The browser global is window.canvasdrop , loaded from {base}/sdk/v1.js ; there is no cd alias. The error classes, ERROR_CODES , and errorFromResponse are named exports of @canvas-drop/sdk for code that imports the package. try { await canvasdrop. kv . set ( \"prefs\" , { theme : \"dark\" }); } catch (err) { switch (err. code ) { case \"CAPABILITY_DISABLED\" : // KV is off for this canvas; the hint says how to turn it on showBanner (err. hint ); break ; case \"VALUE_TOO_LARGE\" : case \"KEY_LIMIT\" : showBanner ( \"Storage limit reached.\" ); break ; default : throw err; } } kv.get returns null for a missing key instead of throwing, so most reads need no try/catch . The codes Codes, statuses, and meanings below are the SDK's exported ERROR_CODES , in the same order (each entry is { status, summary } ; ErrorCode is its key type). Code Status Meaning NOT_AUTHENTICATED 401 The viewer is not signed in. PASSWORD_REQUIRED 403 The canvas is password-protected. CAPABILITY_DISABLED 403 Backend or the specific feature is off for this canvas. CROSS_CANVAS_FORBIDDEN 403 A request targeted another canvas's resources. MODEL_NOT_ALLOWED 403 The requested AI model is not in the allow-list. DISABLED 403 The canvas has been disabled by an administrator. STATIC_ONLY 403 The canvas is a public link ( public_link ): every backend primitive is refused for non-owners. GUEST_AI_DISABLED 403 AI is not enabled for a retained legacy guest-session viewer. GUEST_AI_CAP 429 The canvas reached its retained legacy guest-session AI spend cap. NOT_FOUND 404 The key, file, or c"
  },
  {
    "path": "self-hosting/install",
    "title": "Install",
    "headings": [
      "Docker Compose",
      "Verify the launch invariants",
      "Switch the demo to S3-compatible storage (MinIO)",
      "Add Chromium for canvas screenshots",
      "Run the image with your own proxy, database, and IdP",
      "Upgrade the compose stack",
      "Run from source",
      "What you choose at install time"
    ],
    "text": "Install You are standing up canvas-drop for the first time. By the end of this page you have an instance you can sign in to, and you know which database, storage, URL mode, and auth driver it is running. Two starting points, same repository: git clone https://github.com/markpasternak/canvas-drop.git cd canvas-drop # Evaluate or self-host: the production shape behind an identity-aware proxy docker compose up --build # open http://localhost:8080 and sign in as demo@example.com / canvasdrop # Develop canvas-drop itself: SQLite, local storage, automatic sign-in pnpm install && cp .env.example . env && pnpm dev # open http://localhost:5173 Path Needs What you get Docker Compose Docker with Compose v2 ( docker compose , not the legacy docker-compose ) An identity-aware proxy in front, canvas-drop in real proxy auth mode, Postgres, and a bundled demo identity provider on http://localhost:8080 . Evaluate here; graduating the same files to a real IdP is configuration. From source Node 24 or newer, pnpm 11 pnpm dev on http://localhost:5173 with SQLite, local storage, and dev auth that signs every request in. Develop canvas-drop itself, or try Bearer deploys and MCP with no proxy in the way. Both run the same code. Database, storage, URL mode, and auth are drivers behind one switch variable each; swapping one later is a config change, never a code change. The switches are listed in What you choose at install time . Docker Compose docker compose up --build docker compose ps app # STATUS ends in \"(healthy)\" when the stack is ready The first --build compiles the workspace inside the image. The server does not listen until Postgres is reachable and migrations have run, and GET /healthz returns 503 while the database is unreachable, so the container's health check allows a 60-second start period before it counts failures. Sign in at http://localhost:8080 as demo@example.com / canvasdrop . What comes up: Service Image Role caddy caddy:2-alpine Edge proxy on host port 8080 , the only"
  },
  {
    "path": "self-hosting/configuration",
    "title": "Configuration",
    "headings": [
      "How config gets loaded",
      "Value formats",
      "Boot-time checks",
      "The four pluggable interfaces",
      "Core",
      "Design skins",
      "Auth",
      "dev mode",
      "oidc mode",
      "proxy mode",
      "Tenancy: the org boundary (optional)",
      "Database",
      "Storage",
      "Screenshots (optional)",
      "Rate limiting",
      "AI (optional)",
      "Outbound Connections (optional)",
      "Authoring (optional)",
      "Email transport",
      "Sign-in permits and access emails",
      "Logging and error tracking",
      "Tooling-only variables"
    ],
    "text": "Configuration If you run a canvas-drop instance, this is the environment-variable reference: every variable the server reads, its default, and what refuses to boot when it is wrong. Every setting is an environment variable, validated once at startup. One module ( packages/shared/src/config/env.ts ) is the only reader of process.env ; everything else takes typed config. An invalid value or combination fails at boot with a message naming every variable to fix, never at request time. Nothing is reported anywhere: no telemetry, no phone-home. The defaults below are the schema defaults. .env.example mirrors them with comments; .env.production.example is the annotated production profile. How config gets loaded Local dev. pnpm dev reads the root .env file once ( node --env-file-if-exists=.env ). Copy the template and start: cp .env.example . env && pnpm dev # server on http://localhost:3000, dashboard on http://localhost:5173, signed in as dev@example.com The defaults give you a logged-in instance: path URLs, sqlite , local storage, dev auth. Production. The .env file is not read. Set variables in the process environment: a systemd EnvironmentFile , the container environment: block, or a secrets manager. The recommended profile (subdomain URLs, proxy auth verified against a JWKS, Postgres, S3) needs this set; everything else keeps its default: NODE_ENV=production CANVAS_DROP_URL_MODE=subdomain CANVAS_DROP_BASE_URL=https://canvases.example.com CANVAS_DROP_SESSION_SECRET=<output of: openssl rand -hex 32> CANVAS_DROP_AUTH_MODE=proxy CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL=https://idp.example.com/.well-known/jwks.json CANVAS_DROP_AUTH_PROXY_JWT_ISSUER=https://idp.example.com CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE=canvas-drop CANVAS_DROP_ALLOWED_EMAIL_DOMAINS=example.com CANVAS_DROP_ADMIN_EMAILS=admin@example.com CANVAS_DROP_DB=postgres CANVAS_DROP_DATABASE_URL=postgres://canvasdrop:<password>@db.internal:5432/canvasdrop CANVAS_DROP_STORAGE=s3 CANVAS_DROP_S3_BUCKET=canvas-drop CANVAS_"
  },
  {
    "path": "self-hosting/security-model",
    "title": "Security model",
    "headings": [
      "The two settings that matter most",
      "The trust boundary",
      "The five hard invariants",
      "Identity is always server-side (invariant 1)",
      "proxy mode: one trust path, never two",
      "oidc mode: the app owns the session",
      "Agents",
      "Access is decided on every request (invariant 3)",
      "The org boundary (member vs guest)",
      "Adds are auth-delegated (no app-owned credentials)",
      "No secrets in the browser (invariant 2)",
      "Connections: bounded outbound authority",
      "Path mode vs subdomain mode (invariant 4)",
      "Lifecycle changes land on the next request (invariant 5)",
      "Reducing canvas XSS blast radius",
      "What gets audited",
      "No telemetry"
    ],
    "text": "Security model This page tells you, as the operator, where the trust boundary of a canvas-drop instance is, which five guarantees hold inside it, and which config choices keep them intact. Read it before you put an instance in front of colleagues. canvas-drop hosts arbitrary, often AI-generated, web artifacts for a trusted organization. Everyone who reaches a canvas has already passed your sign-in and the email allowlist. It is not built to defend against the hostile internet. Inside that boundary it holds five hard invariants; beyond them it stays open and permissive, because the product is meant to be frictionless among colleagues. The two settings that matter most Env var Production choice Why it matters CANVAS_DROP_AUTH_MODE proxy (or oidc when nothing fronts the app) Decides how identity is established (invariant 1). Default dev . CANVAS_DROP_URL_MODE subdomain Gives each canvas its own origin so the browser isolates canvases from each other (invariant 4). A production baseline behind an identity-aware proxy that forwards a signed JWT: CANVAS_DROP_AUTH_MODE=proxy CANVAS_DROP_URL_MODE=subdomain CANVAS_DROP_BASE_URL=https://canvases.example.com CANVAS_DROP_ALLOWED_EMAIL_DOMAINS=example.com CANVAS_DROP_AUTH_PROXY_JWT_JWKS_URL=https://<your-proxy>/<its-jwks-path> CANVAS_DROP_AUTH_PROXY_JWT_ISSUER=https://<your-proxy> CANVAS_DROP_AUTH_PROXY_JWT_AUDIENCE=<audience-your-proxy-sets> CANVAS_DROP_SESSION_SECRET=<32 or more random characters> Without a proxy, let the app own login instead: CANVAS_DROP_AUTH_MODE=oidc CANVAS_DROP_URL_MODE=subdomain CANVAS_DROP_BASE_URL=https://canvases.example.com CANVAS_DROP_ALLOWED_EMAIL_DOMAINS=example.com CANVAS_DROP_OIDC_ISSUER=https://<your-openid-provider> CANVAS_DROP_OIDC_CLIENT_ID=<client id > CANVAS_DROP_OIDC_CLIENT_SECRET=<client secret> CANVAS_DROP_SESSION_SECRET=<32 or more random characters> Both are config swaps, never code changes. Config is validated at boot, and the process refuses to start on an unsafe combination: Mode R"
  },
  {
    "path": "self-hosting/deploy",
    "title": "Deploy",
    "headings": [
      "Recommended production profile",
      "Shape",
      "What the reverse proxy must do",
      "Auth at the edge",
      "Agents and CI behind an IAP",
      "Without an identity-aware proxy: oidc",
      "Run with Docker",
      "The compose stack",
      "Graduating to a real IdP",
      "Running the bare process",
      "Health, boot, and upgrades",
      "Backups and maintenance",
      "Logs",
      "Changing drivers later"
    ],
    "text": "Deploy This page is for the operator putting canvas-drop in production. By the end you run one Node process behind a TLS-terminating reverse proxy that asserts who the user is, with Postgres and S3-compatible storage beside it, and you know how to check its health, back it up, and upgrade it. The full variable reference is on Configuration . The production recipe: docker build -t canvas-drop . cp .env.production.example canvas-drop.env # replace every CHANGE_ME / REPLACE_ME docker run -d --name canvas-drop --env-file canvas-drop.env \\ -p 127.0.0.1:3000:3000 -v canvas-drop-data:/data canvas-drop curl -fsS http://127.0.0.1:3000/healthz # {\"status\":\"ok\",\"db\":\"ok\",\"version\":\"...\"} Then point your identity-aware proxy at 127.0.0.1:3000 under the contract below. To see the whole shape running first, with a bundled IdP and no external setup, use the compose stack: docker compose up --build # add -d to run in the background # open http://localhost:8080 and sign in as demo@example.com / canvasdrop The app is a single Hono server: dashboard, auth, management and platform APIs, canvas content, MCP, docs, and the realtime WebSocket all share one port and one log stream. Identity only ever comes from the app's server-side auth context, so the proxy has one security job: assert the user correctly. Everything else on this page is a config switch. Recommended production profile Interface Setting Notes URL mode CANVAS_DROP_URL_MODE=subdomain Every canvas is its own browser origin ( {slug}.{base} ). Needs a non-localhost CANVAS_DROP_BASE_URL , wildcard DNS ( *.canvases.example.com ), and a wildcard certificate at the proxy. Auth CANVAS_DROP_AUTH_MODE=proxy on the JWKS path An identity-aware proxy (IAP) in front forwards a signed JWT; the app verifies it. Database CANVAS_DROP_DB=postgres CANVAS_DROP_DATABASE_URL is required. Storage CANVAS_DROP_STORAGE=s3 Bucket, region, access key, and secret key are required; CANVAS_DROP_S3_ENDPOINT for MinIO or R2. TLS at the proxy One certificate "
  },
  {
    "path": "self-hosting/cdn",
    "title": "Behind a CDN",
    "headings": [
      "1. What is safe to cache",
      "Cloudflare specifics",
      "2. Restore the real client IP",
      "3. The access-downgrade staleness window",
      "4. WebSockets and streaming",
      "5. Upload size",
      "Checklist"
    ],
    "text": "Behind a CDN canvas-drop runs happily behind a CDN or shared reverse-proxy (Cloudflare, Fastly, Fly, a plain nginx/Caddy edge). Identity is cookie/OIDC-based, so a CDN hop never weakens auth — but a shared cache in front of the app changes three things you must configure: which responses may be cached, who the \"client\" is, and how long a restricted canvas can linger at the edge. This page covers all three. TL;DR. Set CANVAS_DROP_TRUSTED_PROXY_IPS to your CDN's egress, set CANVAS_DROP_CLIENT_IP_HEADER to your CDN's real-client header, review CANVAS_DROP_PUBLIC_EDGE_CACHE_TTL (default 300s), and add a CDN cache rule that only caches HTML when there is no session cookie. 1. What is safe to cache The app emits access-aware Cache-Control on every canvas response, so you don't have to reason about it per canvas — just honor the origin headers: Response Cache-Control A shared CDN may… HTML of a public canvas ( public_link , no password) public, max-age=0, s-maxage=<TTL> cache at the edge for the TTL; the browser still revalidates HTML of any auth-gated canvas (Restricted / Whole org / password) private, no-cache never store it Content-hashed asset ( app.a1b2c3d4.js ) of a public canvas public, max-age=1y, immutable cache forever Content-hashed asset of an auth-gated canvas private, max-age=1y, immutable never store it (the browser still caches) Only the public_link , no-password rung is reachable by an anonymous request, so it's the only rung marked public . Everything else is private so a shared cache can't serve one viewer's bytes to another. See Sharing & access for the rungs and the Security model for the isolation guarantee. Cloudflare specifics Cloudflare (like most CDNs) does not cache HTML by default — it caches a static extension allow-list. To get the offload benefit on public canvas HTML, add a Cache Rule that caches the document, and scope it to bypass cache when a session cookie is present ( Cache eligibility → Bypass cache on cookie , matching your session co"
  },
  {
    "path": "self-hosting/screenshots",
    "title": "Screenshots",
    "headings": [
      "How it works",
      "Enablement — two org-wide layers",
      "Per-canvas control",
      "Turning it on",
      "1. Build an image that includes Chromium",
      "2. Make it available in the environment",
      "3. Flip the admin toggle",
      "Turning it off",
      "Memory note"
    ],
    "text": "Screenshots (canvas previews) canvas-drop can capture a preview screenshot of each published canvas and reuse it as the gallery cover and — for public canvases — the link-unfurl (Open Graph) image. It's an optional, off-by-default extra: when it's off, the product behaves exactly as it always has (gallery shows generative-art covers, links unfurl with the branded /og.png ). Where previews appear today: the gallery cover and the public-link OG image. The capture itself is stored once per canvas as a private, access-gated asset, so the same image can back additional surfaces (e.g. dashboard thumbnails) without re-capturing — those are wired in as the product decides to surface them. How it works Captured on publish. Publishing a canvas (editor publish or any deploy) schedules a capture. Capture is asynchronous — it never slows down or blocks a publish. One preview per canvas. A canvas has exactly one current preview, stored as WebP renditions ( og , card , thumb ). On the default auto preview mode it is overwritten on each republish, so the image always reflects the latest published version. The owner can override this per canvas (see Per-canvas control below). One browser, a small queue. A single headless Chromium runs in the server process and is reused across captures (a fresh, isolated browser context per job, recycled periodically). Jobs are drained from a small database queue one at a time by default ( CANVAS_DROP_SCREENSHOTS_CONCURRENCY ), so a burst of publishes just queues up. Access-gated by default. Previews are stored privately and served through an access-gated route: a canvas's preview is only served to someone already allowed to see that canvas. Restricted and otherwise gated canvas previews are never exposed publicly — a per-canvas OG image is emitted only for public_link canvases. Capture is sandboxed. While a canvas is being captured, its backend primitives (AI, realtime, network) are neutered — a capture makes no AI spend and no outbound network cal"
  },
  {
    "path": "agents/mcp",
    "title": "MCP server",
    "headings": [
      "Connect",
      "Sign-in (OAuth 2.1)",
      "How every tool behaves",
      "Tools",
      "Account, lists, create",
      "Read a canvas",
      "Deploy",
      "Versions and lifecycle",
      "Settings",
      "Sharing and people",
      "Draft editor loop",
      "Teams",
      "Return shapes",
      "Which deploy tool to use",
      "Verify a deploy",
      "Enabling and disabling",
      "Which path should an agent use?"
    ],
    "text": "MCP server If you are an agent host that speaks MCP, this page gets you connected to a canvas-drop instance once and then creating, deploying, sharing, and editing canvases as the signed-in account, with no per-canvas key to paste. MCP is the identity-scoped companion to the keyed Deploy API : the Deploy API acts on one canvas with its secret key; MCP acts across every canvas you own or edit, as you. Connect Add the instance's endpoint to your MCP client: {base}/mcp The transport is Streamable HTTP and stateless: every request is authenticated on its own, and there is no session to keep alive. The server identifies itself as canvas-drop , version 1 . A first session, as tool calls: whoami {} -> { id , email, name, orgs, teams, isGuest } create_canvas { \"title\" : \"Retro board\" } -> { id , slug, url, apiKey, deploy, ... } apiKey is returned once deploy_canvas { \"id\" : \"<id>\" , \"files\" : [{ \"path\" : \"index.html\" , \"content\" : \"<h1>Hi</h1>\" }] } -> { url, version : 1, fileCount : 1, totalBytes, warnings : [] } get_canvas_file { \"id\" : \"<id>\" , \"path\" : \"index.html\" } -> { version, path, size, mime, hash, encoding : \"utf8\" , content } The canvas is live after the third call. A new canvas starts Restricted (the private API value), so its URL serves content only to you until you widen access with update_canvas . Sign-in (OAuth 2.1) canvas-drop is its own OAuth 2.1 authorization server; it does not proxy your identity provider. A compliant client needs nothing but the URL: Discovery. {base}/.well-known/oauth-authorization-server and {base}/.well-known/oauth-protected-resource (RFC 8414 and RFC 9728). The issuer is {base} ; the only scope is canvas-drop . Registration. POST {base}/register (Dynamic Client Registration). Authorization. {base}/authorize resolves identity server-side with the same auth strategy the dashboard uses, then checks the email allowlist and the blocked flag. In oidc mode a signed-out browser goes through the normal login and back; in proxy and dev mode"
  },
  {
    "path": "agents/llms",
    "title": "llms.txt",
    "headings": [
      "Deploy with a key",
      "Connect over MCP",
      "Browser SDK inside a canvas",
      "Sharing and access",
      "Capabilities",
      "Errors",
      "Rate limits"
    ],
    "text": "llms.txt If you are an agent putting a canvas on a canvas-drop instance, start here. The instance serves this page and its companions (Overview, Quickstart, Capabilities, SDK overview, Deploy API, Runtime API, Error codes) as one plain-text file at {base}/llms.txt , meant to be dropped straight into context. It is public: served on the instance's base host ahead of the sign-in gateway, so you can read the contract before you hold any credential. {base} is the instance origin (a fresh local instance is http://localhost:3000 ). There are three ways in. Pick by what you hold. You hold Use Reach A per-canvas secret key ( cd_... ) Deploy API at {base}/v1/canvases/{id}/... that one canvas: deploy, read back, roll back, unpublish An MCP-capable host MCP at {base}/mcp (OAuth 2.1, no key to paste) every canvas the signed-in account owns or edits; 47 tools Code running inside a canvas page Browser SDK at {base}/sdk/v1.js , global canvasdrop the six fixed primitives for that canvas: KV, files, AI, identity, realtime, Connections Two verbs recur below. Publish turns the editor draft into an immutable version. Deploy (the Deploy API, deploy_canvas , the staged upload) publishes directly with no draft step. Both create a new version at the same URL; the last 10 versions are kept. Deploy with a key Get a canvas and its key. A person creates the canvas on the dashboard's Create page with Use the API , or you call create_canvas over MCP. Either mints the canvas plus a one-time secret key, shown once. PUT a ZIP with index.html at its root: curl -fsS -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip # 200 {\"url\":\"...\",\"version\":7,\"fileCount\":12,\"totalBytes\":348201,\"warnings\":[]} Verify through the server, not the URL: curl -fsS \"{base}/v1/canvases/{id}/files\" -H \"Authorization: Bearer $CANVAS_KEY \" # 200 {\"version\":7,\"fileCount\":12,\"files\":[{\"path\":\"index.html\",\"size\":1204,\"mime\":\"text/html\",\"hash\":\"...\"}]} curl -fsS \"{base}/v1"
  },
  {
    "path": "agents/skill",
    "title": "Agent skill",
    "headings": [
      "Install",
      "The endpoint",
      "What the skill teaches",
      "Keeping it current",
      "Lighter alternative"
    ],
    "text": "Agent skill Install this skill so your coding agent can create, deploy, verify, and extend canvases on this instance first try. It is a standard skill package: a SKILL.md whose name / description frontmatter carries the when-to-use trigger, so an agent host that discovers skills loads it on its own when a task matches. {base} below is the instance origin (a fresh local instance is http://localhost:3000 ). Install GET {base}/skill.zip is public. No session, cookie, or API key is required, so an agent fetches, unpacks, and checks it in three commands: curl -fL \"{base}/skill.zip\" -o canvas-drop-skill.zip unzip -o canvas-drop-skill.zip -d ~/.claude/skills/ ls ~/.claude/skills/canvas-drop # SKILL.md examples The archive unpacks to one folder: canvas- drop / SKILL.md # frontmatter: name : canvas- drop , description: < when to use> examples/poll.md # a single-file poll on KV, with an optional realtime add - on The command above uses Claude Code's per-user skills directory; a .claude/skills/ folder inside a repo scopes the skill to that project. For any other host, drop the canvas-drop/ folder wherever it discovers skills, or point the agent at the unpacked SKILL.md directly. The skill carries no instance-specific configuration: it refers to the instance as {base} and asks the user for the base URL when it does not know it. The endpoint Fact Value Route GET {base}/skill.zip Auth None. Served ahead of the sign-in gateway, next to /docs , /llms.txt , and /og.png . Host The instance's base host only ( CANVAS_DROP_BASE_URL ). In subdomain URL mode, /skill.zip on a canvas host such as {slug}.canvases.example.com belongs to that canvas (its own file, or its 404). In path mode every host is the base host, so nothing collides. Response 200 with Content-Type: application/zip , Content-Disposition: attachment; filename=\"canvas-drop-skill.zip\" , Cache-Control: public, max-age=3600 . Contents canvas-drop/SKILL.md plus every canvas-drop/examples/*.md . Nothing else can appear: the serve"
  }
];

export const LLMS_TXT = "# canvas-drop — llms.txt\n\nAgent-readable reference for deploying and extending canvases. See {base}/docs for the full docs.\n\n## Overview\n\ncanvas-drop Deploy and share the small web tools your org builds, behind your sign-in. This page is for anyone deciding whether to run canvas-drop, or wondering what it does today. By the end you know what a canvas is, what ships, what is still open, and which page to read next for your job. canvas-drop is an open-source (MIT), self-hostable platform where signed-in members of an organization publish and share small web artifacts called canvases . A canvas is static files (HTML, CSS, JS, images). Drop them in and they are live at a URL your colleagues can open once they sign in. It is inspired by Shopify's Quick; canvas-drop is the open way to run the idea on your own infrastructure. The constraint is the product: a small, fixed set of primitives done well, not a general-purpose hosting platform. There is no server-side build step and no secret in the page. A canvas gains backend capability only through six fixed primitives (KV, files, AI, identity, realtime, and admin-granted Connections) served by a zero-config browser SDK, and that backend stays off until the owner or an editor turns it on. Try it A signed-in local instance with no external services ( path URLs, SQLite, local disk, dev auth). You need Node 24 or newer and pnpm: git clone https://github.com/markpasternak/canvas-drop.git cd canvas-drop pnpm install cp .env.example . env pnpm dev # dashboard http://localhost:5173 · server http://localhost:3000 Open http://localhost:5173 , click Create canvas , choose Paste HTML , and paste an index.html . It is created and published in one step and live at http://localhost:3000/c/{slug}/ . To give it a backend, switch on Enable backend in the canvas's Backend tab and add one tag to the page: < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); const views = await canvasdrop. kv . increment ( \"views\" ); </ script > The Quickstart walks the rest of the loop: deploy, edit, share, roll back. For the production shape instead, docker compose up --build boots Caddy, an identity-aware proxy (oauth2-proxy) signing in against a bundled demo identity provider (Dex), Postgres, and the app in real proxy auth mode at http://localhost:8080 (sign in as demo@example.com / canvasdrop ). See Self-hosting → Install . What ships today Area What you get Read more Publish Paste a single index.html , drag files or a folder, upload a .zip , or PUT a ZIP to the Deploy API with a per-canvas cd_... key. A staged upload sends only the files the server does not already have. Limits: 100 MB per canvas, 25 MB per file, 2 000 files. Create & publish , Deploy API Edit in the browser One mutable draft per canvas: file tree, code editor, autosave, inline preview, and on-page text editing when the draft is a single HTML file. Publish snapshots the draft into a version. The editor Versions Every publish or deploy is an immutable version; the last 10 are kept. Make current rolls back or forward, Restore copies a version into the draft, any version downloads as a ZIP, and non-current versions can be deleted. Versions Share Name people and teams as viewers or editors — that list always applies — then pick General access: Restricted, Whole org, or Public link. Add a password or an expiry. Access is checked on every request and never cached, so a revoke takes effect on the next one. Public link is static-only: anonymous visitors get no primitives. With an org boundary configured ( CANVAS_DROP_ORG_NAME ), Whole org means the canvas's home org. Sharing & access Roles Each person or team on the people list is a viewer or an editor . Editors edit, publish, roll back, change settings and sharing, and rotate the deploy key; deleting, transferring ownership, and the guest-AI switch stay with the owner ( OWNER_ONLY ). Only org members can be editors. A stale save is refused with DRAFT_CONFLICT , so two editors never overwrite each other silently. Ownership transfers to an editor in one step. Roles: viewers and editors Teams Any signed-in user can create a personal team; org members can create org-attached teams. Add people by email, then grant the team viewer or editor access to a canvas. Teams Backend Turn on the backend per canvas, then toggle KV, files, AI, and realtime independently; identity ( canvasdrop.me() ) is on whenever the backend is. A call to a disabled primitive fails with CAPABILITY_DISABLED . A further opt-in, authoring, lets a page create canvases as the signed-in viewer. Capabilities , Browser SDK Connections An admin can define a reusable exact-origin HTTPS profile with selected methods and write-only protected headers, then grant it to specific canvases. Canvas code gets a bounded fetch -like API; no arbitrary backend code runs. Connections Discover Shared lists the canvases already opened to you. The Gallery is opt-in: Public-link canvases and Whole-org canvases their owners list, scoped to your org. An owner can also allow a listed canvas to be used as a template, which others clone. Gallery, description, and tags Agents The MCP server at {base}/mcp (OAuth 2.1, 47 identity-scoped tools) covers everything the dashboard can do, with the same owner/editor/viewer checks. {base}/llms.txt and the agent skill ( {base}/skill.zip ) describe the whole surface for models. MCP server , llms.txt , Agent skill Admin Disable or restore any canvas, reassign owners, feature canvases in the gallery, block users, grant public-link publishing per account, and edit runtime settings (AI key and quotas, screenshots, design skin) without a restart. Configuration Operate SQLite or Postgres, local disk or S3-compatible storage, path or subdomain URLs, dev / proxy / oidc auth: each is a config swap, never a code change. Docker image and compose stack; pnpm backup , pnpm restore , pnpm purge . Install , Deploy , Security model Trust posture Identity comes from the server-side auth context, never from the client. In proxy mode only the trusted proxy may assert it. Secrets stay server-side. AI provider keys, deploy keys, and Connection protected headers never reach the browser. The backend is off by default; each primitive is a per-canvas opt-in. Every canvas starts Restricted. Only its owner or an editor widens access, and Public link is gated by an instance switch plus a per-account grant on the owner. Your infrastructure, your data. No telemetry, no phone-home. The security model states the threat model (a trusted org, not the hostile internet) and the tradeoffs, including path versus subdomain isolation. Status v1 is feature-complete. Milestones M1 through M9 (foundation, hosting and deploy, the dashboard, canvas management, the editor and version model, the five primitives and SDK, admin and hardening, the gallery, AI and realtime) are shipped, along with the post-v1 work in the table above: the access ladder, editor roles and ownership transfer, teams and auth-delegated invites, Shared discovery, the MCP server, staged uploads, custom slugs, clone-as-template, usage stats, optional screenshots and preview covers, design skins, the authoring capability, and admin-granted outbound Connections. Ops and packaging (M10) is the only open milestone. The Docker image and compose stack, vendor-neutral deploy docs, backup/restore tooling (with an automated round-trip test on both dialects), the security review, a secret scan in CI, and the starter examples have shipped. Still deferred: a single-VPS load test and a colleague pilot behind an identity-aware proxy. canvas-drop has not yet run under a real org's load; treat it as ready to self-host and evaluate, not as battle-tested. Not in v1: a CLI (the Deploy API, MCP server, and agent skill cover programmatic deploys), custom domains, multi-org tenancy, an audit-log viewer in the admin panel, KV change subscriptions, and realtime message history. Where to go next New here? Start with the Quickstart . Building a canvas with a backend? Read the SDK overview . Sharing with colleagues or adding editors? See Sharing & access . Running your own instance? See Self-hosting → Install , then Configuration . An AI agent? Read /llms.txt , then connect over the MCP server . Examples and URLs in these docs use {base} (your instance's base URL) and localhost placeholders. Substitute your own instance's address.\n\n## Quickstart\n\nQuickstart For anyone running canvas-drop for the first time: by the end of this page you have an instance on your machine, a canvas at a live URL, and the three ways to put content on it (the dashboard, the editor, and the keyed Deploy API). The default profile needs no external services: SQLite, local disk storage, path URL mode, and dev auth that signs you in automatically. Run an instance locally You need Node 24 or newer ( .nvmrc says 24 ) and pnpm; the repo pins pnpm@11.0.9 in package.json . git clone https://github.com/markpasternak/canvas-drop.git cd canvas-drop pnpm install cp .env.example . env pnpm dev URL What it serves http://localhost:5173 The dashboard (Vite dev server with HMR). It proxies /api , /auth , /v1 , /docs , /llms.txt , /skill.zip , /welcome , and /og.png to the server. http://localhost:3000 The Hono server: management API, Deploy API, MCP at /mcp , docs, and the canvases themselves at /c/{slug}/ . Canvas URLs always point here, not at :5173 . curl http://localhost:3000/healthz # 200 {\"status\":\"ok\",\"db\":\"ok\",\"version\":\"...\"} (503 \"degraded\" until the DB answers) What pnpm dev does: it loads .env once (Node's --env-file-if-exists , so variables you export in the shell still win), seeds an empty database with sample canvases, then runs the server ( tsx watch ), the dashboard ( vite ), and the browser SDK build (esbuild --watch ) in parallel. Only pnpm dev reads .env ; a production process takes its config from the environment (see Configuration ). In production the server serves the built dashboard itself on :3000 ; the Vite server is dev-only. The sample data is 100 canvases with no files across seven owners, 70 of them yours, so the dashboard, Shared , and the gallery are populated on first boot. The seed runs only when the database has no canvases. Command Effect pnpm dev:stop Stops the whole tree (Ctrl-C works too). pnpm dev:restart restarts it. CANVAS_DROP_DEV_SEED=0 pnpm dev Starts without the sample canvases. pnpm reset:data Deletes the local SQLite file and the local storage directory. pnpm dev:fresh resets, reseeds, and starts. CANVAS_DROP_PORT=3001 pnpm dev Moves the server if :3000 is taken; the dashboard proxy follows. CANVAS_DROP_DASHBOARD_PORT moves the dashboard. Data lives at ./data/canvasdrop.db (SQLite) and ./data/storage (uploaded files). Both paths are the defaults of CANVAS_DROP_SQLITE_PATH and CANVAS_DROP_STORAGE_PATH . Who you are signed in as In dev auth mode every request is dev@example.com ( Dev User ), and that account is an admin: CANVAS_DROP_ADMIN_EMAILS defaults to the dev user in this mode. Override the identity with CANVAS_DROP_DEV_USER_EMAIL and CANVAS_DROP_DEV_USER_NAME in .env . dev mode refuses to boot when NODE_ENV=production ; a real instance uses proxy or oidc auth, and can swap in Postgres ( CANVAS_DROP_DB=postgres ) and S3-compatible storage ( CANVAS_DROP_STORAGE=s3 ). Each of those is a config change, not a code change: see Configuration and Install . Prefer Docker? docker compose up --build starts a demo stack at http://localhost:8080 : the app in real proxy auth mode behind oauth2-proxy and a bundled Dex identity provider, plus Postgres. Sign in as demo@example.com / canvasdrop . Only Caddy publishes a port; the app itself is unreachable from the host. Details and the production shape are in Install . Create a canvas Click Create canvas in the dashboard (or open /new ). Pick a source: Source What happens Paste HTML A single index.html , created and published in one step. Files or folder Drag files or a folder; relative paths are kept from the canvas root. Upload ZIP Extracted server-side; dotfiles are stripped, paths that escape the root are rejected. Use the API Mints an empty canvas plus a per-canvas key (shown once) and a ready-to-run curl command. The canvas stays unpublished until your first deploy. On the same screen you set a title, an optional custom slug (checked live for availability), the audience the canvas opens to once it publishes ( Restricted by default), and Enable backend (optional) . The first three sources publish immediately. Every canvas gets a slug and a URL. The default slug is random ( quiet-otter-x7k2 ); a custom slug is lowercase letters, digits, and hyphens, 1 to 63 characters, no leading or trailing hyphen, with a reserved-word list ( api , admin , docs , mcp , and similar). Change it later under Settings → URL & routing → Change slug ; leave the field empty for a fresh random one. The old URL stops working immediately. URL mode Canvas URL Locally path (default) {base}/c/{slug}/ http://localhost:3000/c/quiet-otter-x7k2/ subdomain {slug}.{baseHost} not available on localhost ; for example quiet-otter-x7k2.canvases.example.com Add content A canvas is static files, served as-is. No build step runs on the server. The page at the canvas URL is the index.html at the root (a canvas whose only HTML file sits elsewhere serves that file instead): <!doctype html > < html > < body > < h1 > Hello from my canvas </ h1 > </ body > </ html > Limits: 100 MB per canvas, 25 MB per file, 2 000 files. Each publish creates an immutable version; the last 10 are kept, and identical files are stored once. Give it a backend (optional) The browser SDK gives a canvas six fixed primitives (KV, files, AI, identity, realtime, and admin-granted Connections) with no keys in the page. Identity comes from the signed-in session, and the SDK works out which canvas it is running in from the page URL: < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); const views = await canvasdrop. kv . increment ( \"views\" ); // 1, then 2, ... </ script > The backend is off by default. Turn on Enable backend in the canvas's Backend tab, then the primitives you need: Key-value storage, File storage, AI, Realtime, and Authoring (a canvas that creates canvases; off by default per canvas and per instance, CANVAS_DROP_AUTHORING defaults to off ). Identity ( me() ) is on whenever the backend is on. A primitive is live only when the backend is on, its own toggle is on, and the operator has it available: AI needs CANVAS_DROP_AI_API_KEY set on the server, realtime needs CANVAS_DROP_REALTIME=on (the default). A call to a primitive that is not live throws CapabilityDisabledError . See the SDK overview and Capabilities . Connections are granted separately by an administrator. They let a canvas call one pre-approved HTTPS origin with controlled methods and protected headers—for example, a stock-data API that requires a server-side credential or fixed User-Agent . See Connections . Publish and share Two words mean different things here. Publish turns the editor's draft into a new immutable version. Deploy (folder, ZIP, paste, or the Deploy API) publishes a version directly, with no draft step; the New version button on every detail tab does this for an existing canvas. The Editor tab works on a mutable draft with autosave. Publish (⌘↵ or Ctrl+Enter) snapshots it into a new version and points the canvas URL at it. Two people editing the same file cannot silently overwrite each other; a stale save shows a conflict banner instead. The Versions tab lists every kept version: Make current re-points the live URL at it (in either direction), Restore copies one back into the draft, Download ZIP exports it. The Sharing and permissions page sets who can open the canvas, in two parts. People and teams with direct access names people (by email) and teams, each as a viewer or an editor ; that list always applies, and a brand-new email becomes live only after that person's first verified sign-in. General access says who else can open the URL (opening it to the org or the public needs a published version): General access Who else can open the URL Restricted (default) Nobody beyond you, your editors, and the people and teams on the list. A non-owner admin is treated like any member and gets a 404. Whole org Any signed-in member with the link. Public link Anyone with the link, static files only: the primitives are refused for anonymous visitors. Needs the instance's public-links switch (on by default) and the owner account's public-link permission (granted by default, revocable by an admin). On top of any choice you can add a password gate or a share expiry. People and teams on the list see the canvas in Shared at once; a Whole-org share is link-only until you turn on List for your org . The gallery is narrower: it lists only Public-link canvases and Whole-org canvases whose owners turn on List in the gallery . Revoking access takes effect on the next request. Details in Sharing & access . Deploy as an agent Scripts and agents deploy over HTTP with the per-canvas key and no dashboard session. The key is shown once at creation; an owner or editor can mint a new one under Settings → Regenerate key . curl -fsS -X PUT \"http://localhost:3000/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip # 200 {\"url\":\"http://localhost:3000/c/quiet-otter-x7k2/\",\"version\":1,\"fileCount\":3,\"totalBytes\":4096,\"warnings\":[]} The key operates only on its own canvas and never belongs in canvas files (a deploy that embeds one returns a warning). Deploys are limited to 10 per minute per canvas. The same surface offers GET /v1/canvases/{id}/files to read the live version back, POST …/rollback , POST …/unpublish , and a staged upload (begin, send only the blobs the server is missing, finalize) for large or frequently redeployed canvases. See the Deploy API . Agents can also connect once to the instance's MCP server at {base}/mcp (on by default; OAuth 2.1 through your own sign-in) for the full owner and editor surface, install the agent skill , or read the whole contract at /llms.txt . Where next Authors: Create & publish , The editor , Teams . Operators: Install , Configuration , Security model , Deploy .\n\n## llms.txt\n\nllms.txt If you are an agent putting a canvas on a canvas-drop instance, start here. The instance serves this page and its companions (Overview, Quickstart, Capabilities, SDK overview, Deploy API, Runtime API, Error codes) as one plain-text file at {base}/llms.txt , meant to be dropped straight into context. It is public: served on the instance's base host ahead of the sign-in gateway, so you can read the contract before you hold any credential. {base} is the instance origin (a fresh local instance is http://localhost:3000 ). There are three ways in. Pick by what you hold. You hold Use Reach A per-canvas secret key ( cd_... ) Deploy API at {base}/v1/canvases/{id}/... that one canvas: deploy, read back, roll back, unpublish An MCP-capable host MCP at {base}/mcp (OAuth 2.1, no key to paste) every canvas the signed-in account owns or edits; 47 tools Code running inside a canvas page Browser SDK at {base}/sdk/v1.js , global canvasdrop the six fixed primitives for that canvas: KV, files, AI, identity, realtime, Connections Two verbs recur below. Publish turns the editor draft into an immutable version. Deploy (the Deploy API, deploy_canvas , the staged upload) publishes directly with no draft step. Both create a new version at the same URL; the last 10 versions are kept. Deploy with a key Get a canvas and its key. A person creates the canvas on the dashboard's Create page with Use the API , or you call create_canvas over MCP. Either mints the canvas plus a one-time secret key, shown once. PUT a ZIP with index.html at its root: curl -fsS -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip # 200 {\"url\":\"...\",\"version\":7,\"fileCount\":12,\"totalBytes\":348201,\"warnings\":[]} Verify through the server, not the URL: curl -fsS \"{base}/v1/canvases/{id}/files\" -H \"Authorization: Bearer $CANVAS_KEY \" # 200 {\"version\":7,\"fileCount\":12,\"files\":[{\"path\":\"index.html\",\"size\":1204,\"mime\":\"text/html\",\"hash\":\"...\"}]} curl -fsS \"{base}/v1/canvases/{id}/files?path=index.html\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" | sha256sum # the live file's raw bytes; compare the hash with what you shipped The deploy publishes a live version immediately. {id} is the canvas id, not the slug. The Deploy API host is CANVAS_DROP_API_BASE_URL , which defaults to the instance base URL and can differ from the dashboard host; create_canvas and the dashboard both return the exact endpoints for the canvas, so never guess the host. The key is verified per canvas. A missing or unknown key, including a key for an archived, disabled, or deleted canvas, answers 401 {\"error\":\"unauthorized\"} ; a key for a different canvas answers 403 . Validation failures answer {\"code\", \"message\",\"path\"} : 400 on the ZIP path with EMPTY_DEPLOY , TOO_MANY_FILES , FILE_TOO_LARGE , CANVAS_TOO_LARGE , INVALID_ZIP , INVALID_PATH , ZIP_SLIP_REJECTED , or ZIP_BOMB_REJECTED ; a ZIP body over the canvas cap is refused before it is read with 413 CANVAS_TOO_LARGE . Deploys, staged begin/finalize, and rollbacks are throttled at 10 per minute per canvas ( 429 {\"error\":\"rate_limited\"} with Retry-After ). Companion routes, same Bearer key: Route Purpose GET /v1/canvases/{id} {id, slug, url, title, status, publicationState, accessMode, currentVersionId} ( accessMode : restricted | whole_org | public_link — who else can open it beyond the people-and-teams list) GET /v1/canvases/{id}/versions {versions: [{number, source, status, createdBy, createdAt, fileCount, totalBytes, current}]} GET /v1/canvases/{id}/files the live manifest as JSON; ?path= returns that file's raw bytes; 404 NOT_PUBLISHED before the first deploy POST /v1/canvases/{id}/rollback body {\"version\": 6} ; makes that ready version current and returns {url, version} ; 404 when no ready version has that number POST /v1/canvases/{id}/unpublish back to Draft: {url, publicationState: \"draft\", currentVersionId: null} ; 409 CANNOT_UNPUBLISH when not published For large or repeat deploys, the staged flow sends only changed blobs: POST /v1/canvases/{id}/uploads with {\"manifest\":[{\"path\",\"hash\",\"size\"}]} (sha256 hex) returns {uploadId, missingHashes} ; PUT /v1/canvases/{id}/uploads/{uploadId}/blobs/{hash} with the raw bytes of each missing blob returns 204 ; POST /v1/canvases/{id}/uploads/{uploadId}/finalize returns the same DeployResult . A session lives 15 minutes and finalizes once. Staged errors use the same {code, message} shape at a mapped status: size caps 413 ; an unknown or foreign uploadId 404 UPLOAD_HANDLE_INVALID ; UPLOAD_ALREADY_FINALIZED and UPLOAD_IN_PROGRESS 409 ; UPLOAD_EXPIRED , UPLOAD_MISSING_BLOB , BLOB_HASH_MISMATCH , INVALID_MANIFEST 400 . Limits: 100 MB per canvas, 25 MB per file, 2 000 files. Full contract: Deploy API . Connect over MCP Add {base}/mcp to an MCP-capable host. First use runs OAuth 2.1 against canvas-drop itself (RFC 8414/9728 discovery, Dynamic Client Registration, PKCE S256 ), sends you through the instance's normal org sign-in, and returns a 1 h access token plus a rotating refresh token. Every call re-checks that the account is still active. Transport is Streamable HTTP, stateless; calls are limited to 120 per minute per account and request bodies to 110 MiB. An instance with CANVAS_DROP_MCP=off has no /mcp endpoint at all. A first session, as tool calls: whoami {} -> { id , email, name, orgs, teams, isGuest } create_canvas { \"title\" : \"Retro board\" } -> { id , slug, url, apiKey, deploy, ... } apiKey is returned once deploy_canvas { \"id\" : \"<id>\" , \"files\" : [{ \"path\" : \"index.html\" , \"content\" : \"<h1>Hi</h1>\" }] } -> { url, version : 1, fileCount : 1, totalBytes, warnings : [] } get_canvas_file { \"id\" : \"<id>\" , \"path\" : \"index.html\" } -> { version, path, size, mime, hash, encoding : \"utf8\" , content } Scope and roles. Tools act on the canvases the account owns or edits. A canvas you hold no role on reads as canvas not found , for admins too. Each tool has a minimum role: any (identity, lists, create, teams), editor (everything on a canvas), or owner ( delete_canvas , transfer_canvas ). An editor calling an owner-only tool, or setting guestAiEnabled / guestAiCap through update_canvas , gets OWNER_ONLY: ... ; get_canvas echoes the list as ownerOnlyActs: [\"delete\", \"transfer\", \"guest_ai\"] . id parameters are canvas ids (team tools take team ids), never slugs. Results are JSON in a text content block; failures are isError: true with the text CODE: message . An admin-disabled canvas stays readable but every mutation fails DISABLED: ... ; an archived canvas refuses deploy and publish with NOT_ACTIVE . Group Tools Identity, lists, create ( any ) whoami , list_canvases , list_shared_canvases , create_canvas , clone_canvas Read ( editor ) get_canvas , list_versions , get_canvas_file , get_canvas_usage , list_access , search_people Deploy ( editor ; publishes live immediately) deploy_canvas , begin_deploy , add_files , finalize_deploy Lifecycle ( editor unless marked) rollback_canvas , unpublish_canvas , delete_version , archive_canvas , unarchive_canvas , delete_canvas (owner), transfer_canvas (owner) Settings ( editor ) update_canvas , set_capabilities , set_canvas_slug , set_canvas_preview , regenerate_deploy_key ; list_canvas_connections reads the admin-granted profiles Sharing ( editor ) grant_access , invite_to_canvas , revoke_access , set_access_role Draft loop ( editor ) get_draft , read_draft_file , write_draft_file , delete_draft_file , rename_draft_file , publish_draft , restore_draft Teams ( any ) list_teams , create_team , rename_team , delete_team , add_team_member , remove_team_member , cancel_team_invite , list_team_members Working notes: Typical flow: create_canvas , deploy, verify. create_canvas returns the canvas view, the one-time apiKey , and a deploy block ( apiBase , zipUpload , staged.begin / stageBlob / finalize , readback , and a ready-to-run curl with the key filled in). A new canvas is empty and Restricted ( private in the API); its URL serves content only after a deploy. Prefer curl for bytes. deploy_canvas takes exactly one of zipBase64 or files: [{path, content, encoding?}] ( utf8 default, or base64 ) and, like add_files , inlines file content into the model context. When you can run shell commands, use the deploy block's curl, whole ZIP or staged; keep deploy_canvas for a small first publish without a shell. Verify through the server, not the URL. The live URL is behind org sign-in: a signed-out GET is redirected to login ( oidc ) or answered 401 ( proxy , dev ) unless the canvas is on the public_link rung. Check the returned {version, fileCount} , list_versions , the readback URL, or get_canvas_file (no path lists the manifest; a path returns the content as utf8 or base64 ; over 256 KiB it returns truncated: true instead). list_canvases returns owned and edited canvases, each with role ( \"owner\" or \"editor\" ) and owner ; the role parameter ( owned or edited ) narrows. query is a forgiving filter over title, description, tags, and slug (case, accent, and whitespace insensitive; multiple words AND). tags is any-match. sort is updated (default), created , title , or popular (views over the last 30 days). limit defaults to 50, max 100. list_shared_canvases lists canvases you can open but do not manage: a direct grant, a listed team share, or a listed whole-org share. Draft loop: write_draft_file , delete_draft_file , and rename_draft_file take expectedHash (the file's current hash from get_draft / read_draft_file , or \"none\" for a path you expect absent); a mismatch fails DRAFT_CONFLICT with the current hash and last writer. Without expectedHash the write still fails DRAFT_CONFLICT when a different user wrote the file last, so two editors never overwrite each other silently. write_draft_file with create: true refuses an existing path ( PATH_EXISTS ). publish_draft snapshots the draft into a live version and returns {version, versionId, fileCount, totalBytes} . update_canvas fields: title (max 200), description (max 2000, or null ), tags (max 20, each max 50 chars; one set serves list filtering and the gallery), access , discoverability ( link_only by default; listed shows a Team or Whole-org canvas in Shared and makes a Whole-org canvas gallery-eligible), teamIds , password (or null to clear), sharedExpiresAt (unix ms, or null ), spaFallback , previewMode ( auto or off ; set_canvas_preview with an image sets custom ), galleryListed , galleryTemplatable , and the owner-only guestAiEnabled / guestAiCap . Refusals you will meet: SHARE_REQUIRES_PUBLISH (sharing needs a published canvas), ORG_REQUIRED , PUBLIC_LINKS_DISABLED (instance switch off), PUBLIC_NOT_ALLOWED (the owner may not publish publicly), PUBLIC_LINK_OWNER_GATED (an editor asked for public_link on such a canvas), TEAM_REQUIRED / TEAM_FORBIDDEN . set_capabilities takes backendEnabled , kv , files , ai , realtime , authoring . People: list_access entries are viewer or editor ; the owner also gets transferCandidates . grant_access takes exactly one of email or teamId plus role ; a new email is pending until its first verified sign-in through the instance's identity provider; legacy guests are viewers only ( GUEST_VIEWER_ONLY ). set_access_role changes an entry's role. transfer_canvas takes a user id (never an email) of an existing editor ( NOT_ELIGIBLE otherwise) and returns {ok, canvas, previousOwnerEditor, publicLinkReverted} : the previous owner keeps editor access while their account is active, and a public_link rung reverts when the new owner lacks the entitlement. Tenancy: whoami returns orgs , teams , and isGuest (true only when an org boundary is configured and you belong to none). create_canvas.orgId : omit to default to your only org, pass null for a personal canvas; an org you do not belong to fails ORG_FORBIDDEN . Under an active org boundary the whole_org rung needs an org-homed canvas ( ORG_REQUIRED ). Versions: list_versions carries a downloadUrl per version ( {base}/mcp/canvases/{id}/versions/{n}/download , a ZIP fetched with the same MCP access token as a Bearer). delete_version removes a non-current version only ( CURRENT_VERSION otherwise). Parameters and return shapes for every tool: MCP server . Browser SDK inside a canvas < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); // { id, email, name, avatarUrl, kind } await canvasdrop. kv . set ( \"last-viewer\" , me. name ); const views = await canvasdrop. kv . increment ( \"views\" ); // 1 on the first call, then 2, 3, ... </ script > One global, window.canvasdrop ; there is no cd alias and no version property. Zero config: the slug and API base are read from the page URL ( /c/{slug}/ in path mode, {slug}.{host} in subdomain mode), every call goes to {apiBase}/v1/c/{slug}/... with the session cookie, and no key ever reaches the page. The root-relative src resolves on the canvas's own origin in both modes. The canvas must have Backend switched on (see Capabilities below); /sdk/v1.js sits behind the same sign-in as the canvas. me() returns { id, email, name, avatarUrl, kind } . kind is \"member\" ; \"guest\" appears only for retained legacy guest sessions, since new Add person grants materialize as signed-in users. kv (shared) and kv.user (per viewer, keyed server-side) have the same five methods: get(key) returns the value or null ; set(key, value) stores any JSON except null ; delete(key) is idempotent; list({ prefix?, cursor?, limit? }) returns { entries: [{ key, value }], nextCursor } ( limit 1 to 1000, default 100); increment(key, by = 1) returns the new number and fails NOT_NUMERIC on a non-numeric value. Key max 512 bytes, value max 64 KiB, 10 000 shared and 1 000 per-user keys per canvas. files : upload(file) returns { id, name, size, url } with an absolute url ; list() returns [{ id, name, size, mime, createdAt }] ; delete(id) ; url(id) is synchronous. 25 MiB per file, 1 GiB per canvas. ai : chat(messages, { model, system?, maxTokens? }) returns { text, usage: { inputTokens, outputTokens, cacheCreationInputTokens, cacheReadInputTokens }, cost } ; stream(messages, options) returns an AsyncIterable<string> of text deltas with no usage. model is required and must be on the instance allowlist ( MODEL_NOT_ALLOWED ). Messages are { role: \"user\" | \"assistant\", content } ; the system prompt goes in options.system ; maxTokens defaults to 1024, cap 8192. The provider key stays server-side. realtime.channel(name) returns a handle with publish(event, data) (fire and forget, buffered while reconnecting), subscribe(handler) where handler receives { event, data, from: { id, name } } and the call returns void , unsubscribe() (clears every handler on the channel), presence() resolving to [{ id, name }] , onPresence , onJoin , onLeave , and close() . One shared socket per page with automatic reconnect; 30 connections per canvas, 16 KiB per message. There is no generic .on(...) . connections.fetch(profile, path, init?) makes a bounded server-side request through an admin-granted profile. path must be root-relative; init.method must be one the admin selected. The profile fixes one exact HTTPS DNS origin and adds write-only protected headers last. Upstream 4xx/5xx remain normal Response objects; platform policy failures throw. Public-link viewers remain static-only. canvases (the authoring capability, off by default per canvas and per instance): publish , update , list , revoke let a signed-in viewer create and manage a share canvas from a page. Full signatures and types: SDK overview . Raw routes: Runtime API . Sharing and access One access rung per canvas, set on the Share tab or with update_canvas.access . Access is evaluated on every request, so a revoke, an expiry, or a role change takes effect on the next request; a canvas you may not open reads as 404 . The people-and-teams list ( grant_access / revoke_access ) always applies: a viewer or editor row, or membership of a granted team, opens the canvas at every access value. access (General access) says who else can: access Who else can open it private nobody beyond the owner, the editors, and the list ( Restricted ). specific_people and team are legacy aliases of this value — accepted, stored, treated identically whole_org any signed-in org member; discoverability: \"listed\" shows it in Shared for them and makes it gallery-eligible public_link anyone with the link, while the instance switch is on and the owner may publish publicly ( canPublishPublic ); static only for everyone except the owner and editors: every primitive answers 403 STATIC_ONLY An editor grant, direct or through an editor-role team, also manages the canvas. Editors skip the password gate and the share expiry. A password lock answers 403 PASSWORD_REQUIRED on the runtime API until the viewer passes the gate; a share expiry ( sharedExpiresAt ) turns other viewers away with 404 SHARE_EXPIRED once it passes. Details: Sharing & access . Capabilities backendEnabled is off by default. With it on, kv , files , ai , and realtime default on and authoring defaults off; each toggles independently. Effective rule: identity = backend ; kv = backend && capKv ; files = backend && capFiles ; ai = backend && capAi && provider key configured ; realtime = backend && capRealtime && CANVAS_DROP_REALTIME=on (the default); authoring = backend && capAuthoring && CANVAS_DROP_AUTHORING=on (default off ). An off feature answers 403 {\"code\":\"CAPABILITY_DISABLED\",\"capability\":\"kv\", \"backendEnabled\":false,\"reason\":\"backend_off\"|\"feature_off\"|\"operator_disabled\", \"hint\":\"...\"} ; the SDK throws CapabilityDisabledError . Toggle from the canvas's Backend tab, PATCH /api/canvases/{id}/capabilities , or set_capabilities . Details: Capabilities . Errors Every failure carries a stable string code ; branch on it, not on message text. The SDK throws CanvasdropError with .code and .status , plus six subclasses: NotAuthenticatedError (any 401), NotFoundError (any 404), CapabilityDisabledError (403 CAPABILITY_DISABLED , with .hint ), QuotaExceededError ( QUOTA_EXCEEDED , GUEST_AI_CAP , KEY_LIMIT , and every 413 size code), PublishFailedError (502, from canvasdrop.canvases.publish , with the new canvas .id ), and UpdatePartialError (502, from canvasdrop.canvases.update when the settings saved but the bundle deploy failed, with .stage and .current ). Every other code arrives as a plain CanvasdropError . kv.get returns null for a missing key instead of throwing. Codes you will meet most on the runtime API: 401 {\"error\":\"unauthorized\"} (no session; oidc redirects to login instead), NOT_FOUND / ARCHIVED / OWNER_ONLY / SHARE_EXPIRED 404, DISABLED 403, PASSWORD_REQUIRED 403, STATIC_ONLY 403, CAPABILITY_DISABLED 403, CROSS_CANVAS_FORBIDDEN / CROSS_SITE_FORBIDDEN 403, MODEL_NOT_ALLOWED 403, INVALID_BODY 400, KEY_TOO_LARGE / VALUE_TOO_LARGE / FILE_TOO_LARGE 413, KEY_LIMIT 409, NOT_NUMERIC 409, QUOTA_EXCEEDED 429 (409 on files), CONNECTION_LIMIT 429, RATE_LIMITED 429, AI_STREAM_TRUNCATED / AI_UPSTREAM_ERROR 502. Full table: Error codes . Rate limits Defaults; each is an env var the operator can change. Surface Default Keyed by Env var Deploy API ( deploy , uploads begin, finalize , rollback ) 10/min canvas CANVAS_DROP_RATELIMIT_DEPLOY_PER_MIN Runtime API /v1/c/{slug}/... 120/min user + canvas CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN Runtime AI /v1/c/{slug}/ai/... 10/min user CANVAS_DROP_RATELIMIT_AI_PER_MIN Runtime Connections 60/min per actor/profile; 600/min per profile actor + canvas + profile; profile CANVAS_DROP_CONNECTIONS_ACTOR_PER_MIN , CANVAS_DROP_CONNECTIONS_PROFILE_PER_MIN MCP /mcp 120/min account CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN For a packaged, installable version of this guidance, see the Agent skill .\n\n## Capabilities\n\nCapabilities Give a canvas a backend, one feature at a time, and know exactly when a call from the page will work. This page is for a canvas's owner or editor. A canvas is static until you switch Enable backend on in its Backend tab. With the backend on, five features toggle independently: KV, files, AI, realtime, and authoring. Identity ( me() ) has no toggle; it is on whenever the backend is on. Outbound Connections also has no owner-controlled feature toggle: each reusable profile is granted to the canvas by an instance administrator and remains gated by the same Backend master switch. Turn on the backend Open the canvas and go to the Backend tab. Switch Enable backend on. It is off by default. You can also set it when you create the canvas: Enable backend (optional) on the create page, or \"backendEnabled\": true on POST /api/canvases and POST /api/canvases/paste . Check the feature toggles. Key-value storage , File storage , AI , and Realtime are on by default, so they go live as soon as the backend is on (where the instance supports them). Authoring starts off and stays off until you turn it on. Then call the features from the page through window.canvasdrop . No keys, no setup: < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); // on whenever the backend is on await canvasdrop. kv . set ( \"last-viewer\" , me. name ); // needs Key-value storage on </ script > Agents and scripts flip the same switches. The MCP tool set_capabilities and PATCH {base}/api/canvases/{id}/capabilities take the same body: any subset of backendEnabled , kv , files , ai , realtime , authoring as booleans. Omitted fields are unchanged. { \"backendEnabled\" : true , \"realtime\" : false } The response is the canvas view. Every canvas view (the management API and the MCP get_canvas tool included) carries two objects: capabilities (what is stored) and effective (what runs right now, after the instance switches below are applied). Owners and editors can change capabilities; viewers cannot. Every change is audited ( capabilities_update , with the list of changed fields) and applies on the next request. Turning the backend or realtime off also drops the canvas's live realtime sockets. A canvas an admin has disabled refuses the change with 409 DISABLED . The toggles Backend tab row Key Stored default What it gives the canvas SDK Enable backend backendEnabled off The master switch; nothing below runs without it Identity (no toggle) identity follows the backend The signed-in viewer: id, email, name, avatar me() Key-value storage kv on Shared and per-viewer JSON storage, atomic increment kv File storage files on Upload, list, delete, and serve files files AI ai on Server-side model calls; no provider key in the page ai Realtime realtime on Ephemeral pub/sub and presence over WebSockets realtime Connections no feature flag no grants Bounded requests to exact HTTPS origins an admin attached to this canvas connections Authoring authoring off A signed-in viewer creates and manages canvases from the page, as themselves canvases The feature toggles are disabled in the UI while the backend is off. Their stored values are kept, so switching the backend back on restores the same set. The Identity row reads Always on when the backend is on and Off when it is not. When a feature is effective A feature runs only when every gate in its row is open: the backend, its own toggle, and (for AI, realtime, and authoring) an instance switch the operator controls. The server applies this rule on each request; the Backend tab shows the outcome, and effective in the API is the same answer. Feature Backend on Its toggle on Instance switch Identity ( me() ) yes none none KV yes yes none Files yes yes none AI yes yes An AI provider key is configured: CANVAS_DROP_AI_API_KEY , or the Provider API key an admin sets in Admin → Settings Realtime yes yes CANVAS_DROP_REALTIME=on (the default) Connections yes an enabled profile is attached CANVAS_DROP_CONNECTIONS_ENCRYPTION_KEY is available when protected headers are configured Authoring yes yes CANVAS_DROP_AUTHORING=on (default off ), or Authoring enabled set by an admin in Admin → Settings KV and files have no instance switch: your two toggles are the whole story. When your toggle is on but the instance switch is off, the toggle stays on and the row is labelled Disabled by your administrator for this instance. The AI key and the authoring switch are read per request, so an admin's change applies immediately. Realtime follows CANVAS_DROP_REALTIME as set when the server started; the admin panel shows it but cannot change it. Limits Each feature has fixed ceilings. Exceeding one returns the error named in the last column, not CAPABILITY_DISABLED ; see error codes . Feature Limit Admin-adjustable Error KV 64 KB per value, 512 bytes per key; 10 000 shared keys and 1 000 per-viewer keys per canvas the key counts VALUE_TOO_LARGE , KEY_TOO_LARGE , KEY_LIMIT Files 25 MB per file, 1 GB per canvas both FILE_TOO_LARGE , QUOTA_EXCEEDED AI Models on the allowlist ( CANVAS_DROP_AI_MODELS ); spend caps of CANVAS_DROP_AI_USER_DAILY_USD (default 5 ) per viewer per day and CANVAS_DROP_AI_CANVAS_MONTHLY_USD (default 50 ) per canvas per month allowlist and both caps MODEL_NOT_ALLOWED , QUOTA_EXCEEDED Realtime 30 concurrent connections per canvas, 16 KB per message no CONNECTION_LIMIT (socket close 4429 ) Connections 8 KiB URL; 32/16 KiB caller headers; 256 KiB request; 2 MiB response; 10 s; 3 redirects; 60/min actor+canvas+profile; 600/min profile; 5 concurrent/canvas; 50/process env-only REQUEST_TOO_LARGE , RESPONSE_TOO_LARGE , UPSTREAM_TIMEOUT , CONNECTION_RATE_LIMIT , CONNECTION_LIMIT When a feature is off A call to a feature that is off fails with a 403 whose body names the gate that failed: { \"code\" : \"CAPABILITY_DISABLED\" , \"capability\" : \"kv\" , \"backendEnabled\" : false , \"reason\" : \"backend_off\" , \"hint\" : \"This canvas's backend is off (the master switch, off by default). Turn it on in the dashboard Backend tab, the set_capabilities MCP tool, or PATCH /api/canvases/:id/capabilities {\\\"backendEnabled\\\": true}.\" } reason is backend_off , feature_off , or operator_disabled ; hint says what to turn on. The SDK throws a CapabilityDisabledError ( err.code === \"CAPABILITY_DISABLED\" , err.status === 403 ) and exposes the server hint as err.hint , which is also the error message: try { await canvasdrop. kv . set ( \"count\" , 1 ); } catch (err) { if (err. code === \"CAPABILITY_DISABLED\" ) console . log (err. hint ); else throw err; } Realtime reports the same condition over the socket: a connection opened while realtime is off receives one { \"type\": \"error\", \"code\": \"CAPABILITY_DISABLED\", \"capability\": \"realtime\" } frame and is closed with code 4403 . A socket that is already open when realtime is turned off is closed with 4403 too. The SDK turns both into the same CapabilityDisabledError and does not reconnect. See error codes for the full list. Public links are static-only On the Public link rung ( public_link ) the server serves the canvas's files to anyone with the URL and refuses every primitive with 403 STATIC_ONLY for everyone except the canvas's owner and editors. Signed-in org members are refused too. The Backend tab shows a warning when a public-link canvas has its backend on. If the canvas needs a backend for its audience, share it on a more restricted rung; see Sharing & access . Authoring Authoring lets a signed-in org member viewing your canvas create a new canvas from the page, as themselves, through canvasdrop.canvases.publish(...) . The new canvas is created under the viewer's own account and appears in their dashboard; they can update , list , and revoke it later. Legacy guest sessions and public-link visitors cannot use it. Because it mints canvases, authoring is the one feature whose stored flag starts off, and its instance switch ( CANVAS_DROP_AUTHORING ) is off by default as well. The operator also sets the policy a publish is checked against: Policy Env var Default Canvases one viewer may publish per day CANVAS_DROP_AUTHORING_USER_DAILY_MAX 20 Canvases one viewer may publish in total CANVAS_DROP_AUTHORING_USER_TOTAL_MAX 200 Access rungs a publish may request CANVAS_DROP_AUTHORING_ALLOWED_RUNGS private,specific_people,whole_org,public_link Longest allowed share expiry, in days CANVAS_DROP_AUTHORING_MAX_EXPIRY_DAYS 0 (no cap) Whether a share expiry is required CANVAS_DROP_AUTHORING_REQUIRE_EXPIRY false An admin can change the two quotas at runtime in Admin → Settings; the rung and expiry policy is env-only ( specific_people is a legacy alias of private ). Requiring an expiry applies only to Whole org and Public link publishes. Restricted publishes ( private , specific_people , or team ) do not require one. See the authoring SDK reference . Clones start static Duplicating a canvas, or using a gallery template, creates a new canvas with the backend off and the feature flags at their defaults (KV, files, AI, and realtime on; authoring off). KV data, files, and usage are not copied. The new owner turns the backend on when they need it. Why off by default Capabilities are enforced by the server, per request, from the signed-in session: the canvas can ask, the server decides. Canvas files never carry a secret, and a static canvas has no backend surface at all. Turning a capability on is normally a per-canvas choice its owner or an editor makes. Connections is the deliberate exception: only an admin defines profiles and attaches or revokes their grants, while owners and editors inspect the non-secret authority in the Backend tab.\n\n## Overview\n\nBrowser SDK You have a static canvas and want it to keep data, accept files, call a model, know who is looking, or sync between viewers. One <script> tag gives the page a global, canvasdrop , carrying the six fixed primitives (KV, files, AI, identity, realtime, and admin-granted outbound Connections) plus authoring. There is no build step and nothing to configure in the page: identity comes from the viewer's session, the canvas is identified from its own URL, and no key ever reaches the browser. Add it to a canvas One script tag, then call the global: < script src = \"/sdk/v1.js\" > </ script > < script type = \"module\" > const me = await canvasdrop. me (); // { id, email, name, avatarUrl, kind } await canvasdrop. kv . set ( \"last-viewer\" , me. name ); const views = await canvasdrop. kv . increment ( \"views\" ); // 1 on the first call, then 2, 3, ... </ script > The SDK tag is a plain <script> , so it runs before your module; type=\"module\" gives you top-level await . The root-relative src resolves against the canvas's own origin, and the server serves the script there in both URL modes, behind the same sign-in as the canvas. A fresh local instance ( pnpm dev , dev auth) is already signed in, so the snippet runs as soon as the canvas has its backend on (see Turn the backend on first ). {base}/sdk/v1.js is a stable path for the life of v1, cached for an hour ( cache-control: public, max-age=3600 ), so deployed canvases pick up SDK fixes without a redeploy. On a local checkout, a 503 from that path means the bundle is not built yet: run pnpm build (or pnpm --filter @canvas-drop/sdk build ). One global: canvasdrop The script defines exactly one global, window.canvasdrop . There is no cd alias, no second name, and no version property on the object. The client has me() , kv , files , ai , realtime , connections , and canvases , and nothing else. The same client lives in the workspace package @canvas-drop/sdk (private, not published). Its module entry exports createClient , detectContext , SDK_VERSION ( \"1\" ), ERROR_CODES , errorFromResponse , the error classes, and the types. The served script is exactly createClient({ context: detectContext(window.location) }) assigned to window.canvasdrop . How the SDK finds your canvas detectContext(window.location) reads the slug and the API base from the page URL. You never pass either yourself, and there is no override. URL mode Page URL Slug API base Path https://canvases.example.com/c/quiet-otter-x7k2/ the segment after /c/ the page's own origin Subdomain https://quiet-otter-x7k2.canvases.example.com/ the first hostname label the protocol plus the remaining labels (port kept, if any) Path mode is checked first ( /c/{slug} ); any other URL is treated as a subdomain. Every HTTP request goes to {apiBase}/v1/c/{slug}/… with credentials: \"include\" , so the viewer's session cookie rides along; realtime opens one WebSocket per page at {apiBase}/v1/c/{slug}/realtime ( ws or wss to match the page). On a local path-mode instance the page is http://localhost:3000/c/quiet-otter-x7k2/ and the API base is http://localhost:3000 . Turn the backend on first A canvas is static until its owner or an editor switches Enable backend on in the canvas's Backend tab. That master switch is off by default and gates every primitive, including me() . With it on, each feature has its own toggle, and three of them also depend on an operator setting for the instance: Feature Toggle in the Backend tab Toggle default Operator setting Identity, me() none; on whenever the backend is on on none KV Key-value storage on none Files File storage on none AI AI on an AI provider key ( CANVAS_DROP_AI_API_KEY , or the admin override) Realtime Realtime on CANVAS_DROP_REALTIME (default on ) Connections no canvas-owned toggle; each admin grant is the switch no grants an enabled admin profile attached to this canvas Authoring Authoring off CANVAS_DROP_AUTHORING (default off ), or the admin override in Admin → Configuration A call to anything that is off throws CapabilityDisabledError (code CAPABILITY_DISABLED , status 403). Its .hint (also its .message ) names the gate that failed, the master switch, the feature toggle, or the operator setting, and how to open it. Two share states close the whole API no matter how the switches are set; both skip the owner and editors: Public link : every viewer who is not the owner or an editor, signed in or not, gets STATIC_ONLY (403) from every primitive; the owner and editors keep the full API. Password : on a password-protected canvas the API answers PASSWORD_REQUIRED (403) until the viewer has passed the password gate on the canvas page itself; the owner and editors are never prompted. The Capabilities page covers the tab. The surface Namespace What it does Reference canvasdrop.me() The signed-in viewer: { id, email, name, avatarUrl, kind } . kind is \"member\" , or \"guest\" for a retained legacy guest session. Identity canvasdrop.kv get , set , delete , list , increment . The same five on canvasdrop.kv.user store per viewer. get resolves null for a missing key. KV canvasdrop.files upload(file) , list() , delete(id) , and the synchronous url(id) . Files canvasdrop.ai chat(messages, { model }) resolves { text, usage, cost } ; stream(messages, { model }) yields text chunks. model is required; system and maxTokens are optional. AI canvasdrop.realtime channel(name) returns a channel with publish , subscribe , unsubscribe , presence , onPresence , onJoin , onLeave , close , over one shared WebSocket that reconnects on its own. Realtime canvasdrop.connections fetch(profile, relativePath, init?) returns a native upstream Response through one exact origin an admin granted to the canvas. Protected headers stay server-side. Connections canvasdrop.canvases publish , update , list , revoke : a signed-in viewer creates and manages canvases from the page, as themselves (the authoring capability). Authoring Errors Every failure throws an error extending CanvasdropError : a string .code , a numeric .status , and, when the server sent one, a .hint with the fix. Six subclasses cover the cases you most often branch on: Class .code .status NotAuthenticatedError NOT_AUTHENTICATED 401 NotFoundError NOT_FOUND 404 CapabilityDisabledError CAPABILITY_DISABLED 403 QuotaExceededError QUOTA_EXCEEDED , GUEST_AI_CAP , KEY_LIMIT , CONNECTION_LIMIT , or a size code ( KEY_TOO_LARGE , VALUE_TOO_LARGE , FILE_TOO_LARGE ) 429, 409, or 413 (the wire status) PublishFailedError PUBLISH_FAILED , with the new canvas's .id 502 UpdatePartialError UPDATE_PARTIAL , with .stage and the saved .current record 502 Everything else ( PASSWORD_REQUIRED , STATIC_ONLY , DISABLED , MODEL_NOT_ALLOWED , NOT_NUMERIC , INVALID_BODY , and the rest) arrives as the base CanvasdropError ; branch on .code . The full list is on the error codes page. try { await canvasdrop. kv . increment ( \"votes\" ); } catch (err) { if (err. code === \"CAPABILITY_DISABLED\" ) { // ask the owner to enable KV in the Backend tab; err.hint says what is off } else { throw err; } } Three cases do not throw the way the rest do: kv.get on a missing key resolves null rather than throwing NotFoundError . Realtime reports sign-in, capability, and connection-limit failures by closing the socket; the next publish or presence() call on any channel throws that error. A presence() call still in flight when the connection drops rejects with DISCONNECTED (status 0); retry it once the socket is back. A channel you have close() d throws CHANNEL_CLOSED (status 0). With the module import, the error classes are exported for instanceof checks; with the global script, branch on .code or .name .\n\n## Deploy API\n\nDeploy API Put a canvas live over HTTP from a script, CI, or an AI agent, with no dashboard session. You authenticate with the canvas's secret key as a Bearer token. A key operates only on its own canvas, and every response has a stable, machine-readable shape so a client can repair and retry without a human. This page covers the whole keyed loop: publish (one ZIP or a staged set of files), verify what shipped through the server, roll back, and unpublish. # Publish a ZIP. curl -fsS -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip # 200 {\"url\":\"<canvas URL>\",\"version\":7,\"fileCount\":12,\"totalBytes\":348201,\"warnings\":[]} # Read back the live manifest to confirm what is being served. curl -fsS \"{base}/v1/canvases/{id}/files\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" # 200 {\"version\":7,\"fileCount\":12,\"files\":[{\"path\":\"index.html\",\"size\":1280,\"mime\":\"text/html; charset=utf-8\",\"hash\":\"9f86d0...\"}, ...]} Base path: {base}/v1/canvases/{id} . {id} is the canvas id, not the slug. Auth: Authorization: Bearer cd_... , the canvas secret key. It is shown once: when the canvas is created ( POST /api/canvases answers 201 with an apiKey field; the MCP create_canvas tool returns it too) or regenerated ( POST /api/canvases/{id}/regenerate-key , MCP regenerate_deploy_key ). The server stores only its SHA-256 hash, and a clone never inherits the source canvas's key. This path takes no cookies and no CORS; it is separate from the session auth the Runtime API and browser SDK use. What is {base} ? The host serving the Deploy API: CANVAS_DROP_API_BASE_URL , which defaults to the instance base URL. Operators set it only when the API is routed on a different host than the canvases (for example canvases at {slug}.canvases.example.com and the API at api.canvases.example.com in subdomain mode), so do not assume it equals the dashboard host. You never have to guess: over MCP , create_canvas and regenerate_deploy_key (with the real key filled in) and get_canvas (with $CANVAS_KEY ) return a deploy block holding the canvas's exact apiBase , the ZIP, staged-upload and read-back endpoints, and a copy-paste curl command. Method Path Purpose PUT /v1/canvases/{id}/deploy Publish a live version from a ZIP body POST /v1/canvases/{id}/uploads Open a staged upload from a manifest PUT /v1/canvases/{id}/uploads/{uploadId}/blobs/{hash} Stage one file's bytes POST /v1/canvases/{id}/uploads/{uploadId}/finalize Publish from the staged upload GET /v1/canvases/{id} Canvas metadata and publication state GET /v1/canvases/{id}/versions List versions, newest first GET /v1/canvases/{id}/files Read back the live version (verify a deploy) POST /v1/canvases/{id}/rollback Make a prior ready version current POST /v1/canvases/{id}/unpublish Take the canvas back to Draft Deploy means live. A deploy creates a new version and points the canvas at it in one step; there is no draft to publish afterwards. If the canvas has a browser draft, the draft is synced to the new version, or flagged stale when it holds unpublished edits; a deploy never overwrites those edits. Deploys carry static files only: the key cannot switch on capabilities. An owner or editor enables Backend and the primitives the canvas needs (KV, files, AI, realtime) on the canvas's Backend tab or with the set_capabilities MCP tool. Attribution. There is no user on this path, so every write (deploy, rollback, unpublish) is audited as the canvas owner , and a version's createdBy is the owner's id, whoever holds the key. Deploy a version PUT {base}/v1/canvases/{ id }/deploy Authorization: Bearer cd_... Content-Type: application/zip <ZIP bytes> The body is read as a ZIP regardless of Content-Type (a tar archive fails with INVALID_ZIP ). Put index.html at the archive root. Dotfiles and dot-directories ( .git/ , .env ) and directory entries are dropped. The ZIP reader rejects an entry name with .. segments or a leading / ( ZIP_SLIP_REJECTED ), and an entry that declares, or inflates to, more than 25 MB is rejected before it can exhaust memory ( ZIP_BOMB_REJECTED ). The version's source is \"api\" . Success, 200 ( DeployResult , the same shape on every deploy path): { \"url\" : \"<canvas URL>\" , \"version\" : 7 , \"fileCount\" : 12 , \"totalBytes\" : 348201 , \"warnings\" : [ \"legacy.php will be served as text/plain\" ] } url : the canvas URL ( {scheme}//{slug}.{host}/ in subdomain mode, {base}/c/{slug}/ in path mode). version : the new version number; it only ever increases for a canvas. fileCount / totalBytes : files and bytes in the version, after stripping. warnings[] : non-fatal notices. Three exist: <path> will be served as text/plain (the extension is unknown, or is a server-side script type such as .php , so the type is downgraded); <path> may contain a canvas API key (a text file matches the cd_... key shape; keys are server-side only, so remove it and redeploy); and a no- index.html notice, because the canvas root will 404 (a deploy whose only HTML file has another name is served at the root and is not flagged). Warnings never fail the deploy. Limits: 100 MB per canvas, 25 MB per file, 2000 files, enforced while the archive streams. The request body is also capped before buffering at 110 MB (canvas cap plus 10 MB of archive headroom); an over-cap body returns 413 { \"code\": \"CANVAS_TOO_LARGE\" } . An empty body returns 400 { \"code\": \"EMPTY_DEPLOY\" } . Every other validation failure on this route is a 400 with the error shape . A failed deploy never touches the live version. Retention: the 10 most recent ready versions are kept per canvas; older ones are pruned and can no longer be rolled back to. Rate limit: when rate limiting is on ( CANVAS_DROP_RATELIMIT_ENABLED , default true ), the deploy class throttles PUT .../deploy , POST .../uploads (begin), POST .../uploads/{uploadId}/finalize , and POST .../rollback per canvas, keyed after the key is verified. The default is 10 per minute ( CANVAS_DROP_RATELIMIT_DEPLOY_PER_MIN ). Over the limit returns 429 { \"error\": \"rate_limited\" } with a Retry-After header. Blob staging, the GET routes, and unpublish are not throttled. Staged upload (large or incremental) PUT .../deploy sends the whole archive in one request. For large canvases, or when you redeploy often and want to send only what changed, use the three-step staged flow. Bytes go straight to the server (never base64'd through an agent's context), and blobs are content-addressed, so a file the canvas already stores is not uploaded again. 1. Begin. Send the full manifest: each file's canvas-relative path , the hash (lowercase sha256 hex of its bytes, 64 characters), and its size in bytes ( 0 is allowed). POST {base}/v1/canvases/{ id }/uploads Authorization: Bearer cd_... Content-Type: application/json { \"manifest\" : [ { \"path\" : \"index.html\" , \"hash\" : \"<sha256>\" , \"size\" : 1234 } ] } Returns the handle and the subset of hashes the canvas does not already store: { \"uploadId\" : \"up_...\" , \"missingHashes\" : [ \"<sha256>\" , \"...\" ] } Dotfile and directory entries are skipped; a leading ./ or / on a path is stripped. A body that is not JSON, a manifest that is not an array, an empty manifest, a manifest with no deployable entry left after skipping, a malformed hash or size , or two entries that share a hash but declare different sizes returns 400 INVALID_MANIFEST . A path with a .. segment returns 400 ZIP_SLIP_REJECTED . The declared sizes are checked against the caps up front: 413 FILE_TOO_LARGE , 413 TOO_MANY_FILES , or 413 CANVAS_TOO_LARGE . 2. Stage each missing blob. Raw bytes; the path is irrelevant here because blobs are addressed by {hash} : PUT {base}/v1/canvases/{ id }/uploads/{uploadId}/blobs/{ hash } Authorization: Bearer cd_... <raw file bytes> 204 on success. A blob is capped at 25 MB ( 413 FILE_TOO_LARGE ), the bytes must hash to {hash} (else 400 BLOB_HASH_MISMATCH ), the hash must appear in the begin manifest (else 400 UPLOAD_UNEXPECTED_BLOB ), and the byte count must match the size declared for that hash (else 400 BLOB_HASH_MISMATCH ). A handle that is unknown, was minted for a different canvas, or was begun by a different actor returns 404 UPLOAD_HANDLE_INVALID (one code, no existence leak). Staging a blob whose text matches the cd_... key shape is accepted; the API-key lint is logged on the server rather than returned on this channel. 3. Finalize to publish a version from the staged manifest: POST {base}/v1/canvases/{ id }/uploads/{uploadId}/finalize Authorization: Bearer cd_... Returns the same DeployResult as PUT .../deploy ; the version's source is \"upload\" . Finalize re-checks that the actor who began the upload is still active and still an owner or editor of the canvas (else 404 UPLOAD_HANDLE_INVALID ). The handle is single-use and lives 15 minutes from begin (then 400 UPLOAD_EXPIRED on stage or finalize). A finalize before every blob is staged returns 400 UPLOAD_MISSING_BLOB ; stage the rest and call finalize again. The handle is consumed when finalize commits a version, after which any further stage or finalize returns 409 UPLOAD_ALREADY_FINALIZED and a fresh begin is required. Two concurrent finalizes of one handle: the second gets 409 UPLOAD_IN_PROGRESS while the first holds a 60-second lease. The warnings on this path cover the text/plain downgrade and the missing index.html . Get a canvas GET {base}/v1/canvases/{ id } Authorization: Bearer cd_... Returns { id, slug, url, title, status, publicationState, accessMode, currentVersionId } . A key resolves only an active canvas (an archived, disabled, or deleted canvas's key fails auth with 401 ), so status is always \"active\" here and publicationState is \"published\" when a live version exists, otherwise \"draft\" . To confirm a canvas is live, check publicationState === \"published\" ; you do not need to interpret currentVersionId yourself. accessMode is the audience — restricted (only the people-and-teams list), whole_org , or public_link — the same derived value the management API and MCP return. List versions GET {base}/v1/canvases/{ id }/versions Authorization: Bearer cd_... Returns { versions: [...] } , newest first. Each entry is { number, source, status, createdBy, createdAt, fileCount, totalBytes, current } . current marks the live version. source records how the version was made: \"api\" for PUT .../deploy , \"upload\" for the staged flow, \"editor\" for a publish from the browser editor, and \"zip\" , \"folder\" , or \"paste\" for dashboard deploys. status is \"pending\" while a version is being written and \"ready\" once committed; only a ready version can be a rollback target. Verify a deploy (read back the live version) The canvas URL is access-controlled, so a keyed client cannot fetch it to check what shipped: an unauthenticated request gets the login page. Read the live version through the key instead. GET {base}/v1/canvases/{ id }/files Authorization: Bearer cd_... With no query, returns the live version's manifest, sorted by path: { \"version\" : 7 , \"fileCount\" : 3 , \"files\" : [ { \"path\" : \"index.html\" , \"size\" : 1280 , \"mime\" : \"text/html; charset=utf-8\" , \"hash\" : \"9f86d0...\" } ] } Add ?path= (the path exactly as it appears in the listing) to get one file's raw bytes : the body is the file itself, with its Content-Type , an ETag equal to the quoted sha256 of the content, and Cache-Control: no-store . Pipe it to a checksum to confirm the bytes match what you deployed: curl -fsS \"{base}/v1/canvases/{id}/files?path=index.html\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" | sha256sum 404 NOT_PUBLISHED when the canvas has no live version; 404 NOT_FOUND when the path is not in the live manifest. There is no size cap on this read: you stream the body. (The MCP get_canvas_file tool is the identity-scoped equivalent; it inlines content up to 256 KiB and returns hash-only metadata above that.) Roll back POST {base}/v1/canvases/{ id }/rollback Authorization: Bearer cd_... Content-Type: application/json { \"version\" : 5 } Makes a prior ready version current and returns 200 { \"url\": \"<canvas URL>\", \"version\": 5 } . version must be a positive integer; a missing or invalid body returns 400 { \"error\": \"invalid_body\" } . A number with no ready version behind it (never existed, or pruned out of the 10-version window) returns 404 { \"code\": \"INVALID_PATH\" } . If the target is pruned between selection and the pointer swap you get 409 { \"code\": \"VERSION_UNAVAILABLE\" } : list versions again and retry. Rollback moves only the live pointer; it does not change sharing settings. It shares the deploy-class rate limit. Unpublish POST {base}/v1/canvases/{ id }/unpublish Authorization: Bearer cd_... Takes the canvas back to Draft . The live version pointer is cleared, so the canvas URL stops serving, and any live realtime sockets are dropped. Publication settings are reset at the same time: access returns to Restricted , List for people with access is turned off, any share expiry is cleared, and the canvas is unlisted from the gallery. The draft and the version history are kept. Publish again with a new deploy, or by rolling back to a kept version; either way the canvas comes back Restricted, so re-share it from the dashboard or with the update_canvas MCP tool. Success, 200 : { \"url\": \"<canvas URL>\", \"publicationState\": \"draft\", \"currentVersionId\": null } . Unpublishing a canvas that is not currently published returns 409 { \"code\": \"CANNOT_UNPUBLISH\" } . Errors Branch on code (or error ), never on message text. Two body shapes appear on these routes. Auth, body validation, and throttling use { \"error\": \"...\" } : Status Body Cause 401 { \"error\": \"unauthorized\" } Missing or unknown Bearer key, or a key whose canvas is archived, disabled, or deleted 403 { \"error\": \"unauthorized\" } The key belongs to a different canvas than {id} 400 { \"error\": \"invalid_body\" } Rollback body without a positive-integer version 429 { \"error\": \"rate_limited\" } Over the deploy-class limit for this canvas; honor Retry-After Deploy and lifecycle failures use a stable code plus a human message, and name the offending file when there is one: { \"code\" : \"<code>\" , \"message\" : \"...\" , \"path\" : \"<offending path, optional>\" } On PUT .../deploy every validation code is a 400 , except CANVAS_TOO_LARGE , which is a 413 when the body is rejected before buffering: Code Cause EMPTY_DEPLOY Empty body, or an archive with no deployable files once dotfiles and directory entries are stripped INVALID_ZIP The body is not a readable ZIP (a tar archive lands here), or an entry name contains a backslash ZIP_SLIP_REJECTED An entry name with .. segments or a leading / ZIP_BOMB_REJECTED An entry declares, or inflates to, more than 25 MB (the per-file cap; inside a ZIP it never surfaces as FILE_TOO_LARGE ) TOO_MANY_FILES More than 2000 files CANVAS_TOO_LARGE More than 100 MB in total ( 413 when caught at the body cap) The staged-upload routes map the same shape to richer statuses: Code Status Cause INVALID_MANIFEST 400 Body not JSON; manifest not an array, or empty, or with no deployable entry left after dotfiles are skipped; a hash that is not 64 lowercase hex characters; a size that is not a non-negative number; or one hash declared with two sizes ZIP_SLIP_REJECTED 400 A manifest path with a .. segment INVALID_PATH 400 A manifest path still absolute after one leading / or ./ is stripped ( //a.js ); make paths relative FILE_TOO_LARGE , TOO_MANY_FILES , CANVAS_TOO_LARGE 413 A cap exceeded: by declared size at begin, by actual bytes at stage, or by the manifest sum at finalize UPLOAD_HANDLE_INVALID 404 Unknown handle, a different canvas, a different actor, or an actor no longer active or no longer owner/editor at finalize UPLOAD_EXPIRED 400 More than 15 minutes since begin UPLOAD_UNEXPECTED_BLOB 400 The staged hash is not in the begin manifest BLOB_HASH_MISMATCH 400 The bytes do not hash to {hash} , or their size differs from the declared size UPLOAD_MISSING_BLOB 400 Finalize before every manifest hash is staged; stage the rest and retry UPLOAD_IN_PROGRESS 409 Another finalize holds the lease; retry shortly UPLOAD_ALREADY_FINALIZED 409 The handle was consumed; begin again The remaining routes use the same shape: Code Status Route and cause INVALID_PATH 404 Rollback: no ready version with that number VERSION_UNAVAILABLE 409 Rollback: the target was pruned during the swap; refresh and retry CANNOT_UNPUBLISH 409 Unpublish: the canvas is not currently published NOT_PUBLISHED 404 Read-back: no live version NOT_FOUND 404 Read-back: ?path= is not in the live manifest The MCP deploy tools ( deploy_canvas , begin_deploy , add_files , finalize_deploy , rollback_canvas , unpublish_canvas ) wrap the same service layer and surface the same codes; INVALID_ENCODING belongs to that channel only (a files[] entry with an encoding other than utf8 or base64 ) and never occurs on these HTTP routes. Codes returned to canvases at runtime are a separate set; see Error codes .\n\n## Runtime API\n\nRuntime API The runtime API is the HTTP surface a canvas calls from the browser: the six fixed primitives (KV, files, AI, identity, realtime, and admin-granted Connections) plus authoring, each under {base}/v1/c/{slug} . The browser SDK ( window.canvasdrop , served at {base}/sdk/v1.js ) builds every one of these requests, speaks the SSE and WebSocket wire formats, and maps errors to typed exceptions, so reach for it first. Use this page when you need the raw routes: reading the network tab, a non-JS client, or the exact shape a primitive returns. Requests are credentialed with the session cookie the browser already holds. Identity is resolved server-side from that session; the canvas never asserts who the viewer is. // Inside a canvas served in path mode at http://localhost:3000/c/quiet-otter-x7k2/ const api = \"/v1/c/quiet-otter-x7k2\" ; await fetch ( ` ${api} /kv/greeting` , { method : \"PUT\" , credentials : \"include\" , headers : { \"content-type\" : \"application/json\" }, body : JSON . stringify ( \"hi\" ), // the body is the value itself }); // 200 {\"ok\":true} const { value } = await ( await fetch ( ` ${api} /kv/greeting` , { credentials : \"include\" })). json (); // 200 {\"value\":\"hi\"} // The same two calls through the SDK: await canvasdrop. kv . set ( \"greeting\" , \"hi\" ); await canvasdrop. kv . get ( \"greeting\" ); // \"hi\" Where the API lives URL mode Canvas content Runtime API path {base}/c/{slug}/ Same origin: {base}/v1/c/{slug}/... subdomain https://{slug}.canvases.example.com/ The base host: https://canvases.example.com/v1/c/{slug}/... The route path is identical in both modes; only the origin changes. The SDK derives it from location : a pathname starting /c/{slug} means path mode and location.origin ; otherwise it strips the first hostname label and keeps the port. Every request goes out with credentials: \"include\" . CANVAS_DROP_API_BASE_URL shapes the Bearer-key Deploy API URLs ( /v1/canvases/... ), not this surface. Before a handler runs Every /v1/c/{slug}/* request passes the same pipeline, in this order. Any route on this page can return these. 1. Auth gateway. No session: 401 {\"error\":\"unauthorized\"} in proxy and dev mode, or a 302 to /auth/login in oidc mode. One carve-out: an anonymous visitor to an active, unexpired Public link canvas (public links on for the instance and for the owner) is let through as an anonymous principal, then refused with STATIC_ONLY in step 3. 2. Rate limit. 429 {\"code\":\"RATE_LIMITED\"} with Retry-After , X-RateLimit-Limit , X-RateLimit-Remaining , X-RateLimit-Reset . Two classes: Class Paths Keyed Default Env ai /v1/c/{slug}/ai* per user 10/min CANVAS_DROP_RATELIMIT_AI_PER_MIN canvas everything else under /v1/c/{slug} per user per canvas 120/min CANVAS_DROP_RATELIMIT_CANVAS_API_PER_MIN CANVAS_DROP_RATELIMIT_ENABLED=false skips this step. 3. Resolve and authorize. The canvas is looked up by slug and the viewer is checked against its sharing rung, then the password gate, then the static-only rule. The owner and effective editors pass the rung and the password gate. A non-owner admin has no bypass here: they face the rung and the gate like any other member. Code HTTP When NOT_FOUND 404 Unknown slug, or the canvas is deleted. ARCHIVED 404 The canvas is archived. DISABLED 403 An admin disabled the canvas. The owner is not exempt. NOT_INVITED 404 A guest principal scoped to a different canvas. OWNER_ONLY 404 The viewer has no route in: not on the people-and-teams list (directly or through a team) and not admitted by General access — Restricted, Whole org as a non-member (or, under tenancy, a member of another org), or Public link while public links are off for the instance or for the owner. SHARE_EXPIRED 404 The share's expiry has passed (for anyone but the owner and editors). PASSWORD_REQUIRED 403 A shared canvas with a password set and no valid __canvasdrop_gate cookie. The gate that sets the cookie lives on the canvas page, not under /v1/c/ ; this API only checks it. Guests skip the gate. STATIC_ONLY 403 Public link canvas and the viewer is not the owner or an editor. Applies to signed-in members as well as anonymous visitors: the whole runtime API is closed. Body: {\"code\":\"STATIC_ONLY\",\"message\":\"This canvas is public and static-only.\"} . A guest principal reaches the handlers as a synthetic user whose id is guest:<inviteId> , never as an admin and never able to publish public links. 4. Cross-canvas isolation. Mode Rule Response subdomain Origin present and not exactly https://{slug}.{baseHost} 403 CROSS_CANVAS_FORBIDDEN path Sec-Fetch-Site present and not same-origin or none 403 CROSS_SITE_FORBIDDEN path Referer path not /c/{slug} or beneath it 403 CROSS_CANVAS_FORBIDDEN In subdomain mode a request with no Origin passes (a programmatic caller), and a matching Origin gets credentialed CORS headers: Access-Control-Allow-Origin: <origin> , Access-Control-Allow-Credentials: true , Vary: Origin . Those headers are applied before the step 3 refusals, so STATIC_ONLY and PASSWORD_REQUIRED reach the SDK as readable JSON rather than a CORS failure. In path mode the canvas is same-origin and no CORS headers are sent. Preflight OPTIONS /v1/c/{slug}/* is answered before the auth gateway: always 204 , Access-Control-Allow-Methods: GET,HEAD,POST,PUT,PATCH,DELETE,OPTIONS , and either Access-Control-Allow-Headers: Content-Type or the canvas origin's requested header names, plus the credentialed headers above in subdomain mode when Origin matches. It never returns 401 . 5. Capability gate. Each primitive checks its capability and returns 403 when it is off: { \"code\" : \"CAPABILITY_DISABLED\" , \"capability\" : \"kv\" , \"backendEnabled\" : true , \"reason\" : \"feature_off\" , \"hint\" : \"...\" } reason is backend_off , feature_off , or operator_disabled ; hint is a short remediation string. Capability Effective when identity the canvas backend is on kv , files backend on, plus the capability's own switch ai backend on, switch on, and the instance has a provider key realtime backend on, switch on, and CANVAS_DROP_REALTIME=on (the default) connections backend on, plus a live enabled profile and per-canvas admin grant; the external encryption key must be available when the profile has protected headers authoring backend on, switch on, and CANVAS_DROP_AUTHORING=on (default off ) Identity Capability: identity (on whenever the canvas backend is on). GET {base}/v1/c/{slug}/me → 200 { id , email, name, avatarUrl, kind } avatarUrl may be null . kind is \"member\" for a signed-in org member; \"guest\" is retained for legacy guest sessions. The response deliberately omits isAdmin ; that lives on the dashboard's /api/me , covered under Adjacent endpoints below. An anonymous visitor never reaches this handler: a Public link canvas answers STATIC_ONLY first. Key-value Capability: kv . Two scopes with the same five routes: Shared at /kv : one namespace for every viewer of the canvas. Per-viewer at /kv/user : scoped to the caller's server-resolved user id. The client never names the scope, and user is never read as a key. GET {base}/v1/c/{slug}/kv?prefix=&cursor=& limit = list → 200 { entries: [{ key, value }], nextCursor } GET {base}/v1/c/{slug}/kv/{key} read → 200 { value } 404 NOT_FOUND if absent PUT {base}/v1/c/{slug}/kv/{key} write → 200 { ok: true } body = the JSON value DELETE {base}/v1/c/{slug}/kv/{key} delete → 200 { ok: true } idempotent, never 404 POST {base}/v1/c/{slug}/kv/{key}/increment atomic add → 200 { value } body { by?: number } Replace /kv with /kv/user for the per-viewer scope. {key} is one path segment; URL-encode it. list : limit is clamped to 1..1000 (default 100, non-numeric ignored); entries come back in key order; nextCursor is the last key on the page and null on the last page. read : a stored JSON null reads as { \"value\": null } . PUT : the body is the value itself ( content-type: application/json ), any JSON except null . Unparseable JSON and null are 400 INVALID_BODY (delete the key instead of storing null ). increment : by defaults to 1 and must be a finite number ( 400 INVALID_BODY ); a missing or malformed body counts as {} . A missing key starts at 0 . An existing non-numeric value (a stored null included) is 409 NOT_NUMERIC . Limits: key ≤ 512 bytes ( 413 KEY_TOO_LARGE ), serialized value ≤ 64 KiB ( 413 VALUE_TOO_LARGE ), 10 000 shared keys and 1 000 per-viewer keys per canvas ( 409 KEY_LIMIT ; admin-tunable quota keys kv.keys.shared and kv.keys.user ). The key cap applies to new keys; updating an existing key always succeeds. Checks run in the order key size, body, value size, key count. Files Capability: files . POST {base}/v1/c/{slug}/files upload → 201 { id , name, size, url } GET {base}/v1/c/{slug}/files list → 200 { files: [{ id , name, size, mime, createdAt }] } GET {base}/v1/c/{slug}/files/{ id }/content download → 200 raw bytes 404 NOT_FOUND DELETE {base}/v1/c/{slug}/files/{ id } delete → 200 { ok: true } 404 NOT_FOUND Upload is multipart/form-data with the file in a field named file ; the name defaults to upload and the type to application/octet-stream when the part carries none. The returned url is root-relative ( /v1/c/{slug}/files/{id}/content ); the SDK rewrites it to an absolute URL on the API base, which matters in subdomain mode. Content is served with Content-Type set to the stored mime, X-Content-Type-Options: nosniff , and a sanitized filename ( filename plus RFC 5987 filename* ). Only image/png , image/jpeg , image/gif , image/webp , and image/avif render inline; everything else, SVG and HTML included, is sent as attachment . Limits: 25 MiB per file ( 413 FILE_TOO_LARGE , from either the transport body cap or the per-file quota), 1 GiB per canvas ( 409 QUOTA_EXCEEDED ); both admin-tunable ( files.bytes.file , files.bytes.canvas ). A missing or non-multipart body is 400 INVALID_BODY . DELETE and content answer 404 NOT_FOUND for a file that belongs to another canvas. AI Capability: ai . Effective only when the canvas has ai on and the instance has a provider key configured; the key stays server-side and never appears in a response. This route uses the stricter ai rate-limit class (default 10/min per user). POST {base}/v1/c/{slug}/ai/chat chat completion → 200 SSE stream Request body (JSON, ≤ 256 KiB): { \"model\" : \"<provider model id>\" , \"messages\" : [ { \"role\" : \"user\" , \"content\" : \"Hello\" } ] , \"system\" : \"optional system prompt\" , \"maxTokens\" : 1024 } model is required and must be on the instance allowlist. messages needs at least one entry, each with role user or assistant ; the system prompt goes in system , not in a message. maxTokens is a positive integer, defaults to 1024, and is capped at 8192. The response is text/event-stream . Each event is one JSON data: line: data : { \"type\" : \"delta\" , \"text\" : \"Hel\" } data : { \"type\" : \"delta\" , \"text\" : \"lo\" } data : { \"type\" : \"done\" , \"usage\" :{ \"inputTokens\" : 12 , \"outputTokens\" : 2 , \"cacheCreationInputTokens\" : 0 , \"cacheReadInputTokens\" : 0 }, \"cost\" : 0 . 000123 } cost is in USD. The cache fields report prompt-cache writes and reads when the provider supplies them, 0 otherwise. With the Anthropic provider the server sets ephemeral cache breakpoints on the system prompt and on the last message before the newest user turn, so a stable conversation prefix is cached across calls. Errors, by when they happen: Before the stream (JSON body, normal status), in this order: 413 BODY_TOO_LARGE , 400 INVALID_BODY , 403 MODEL_NOT_ALLOWED (not allowlisted, or allowlisted but unpriced), 403 GUEST_AI_DISABLED (legacy guest session on a canvas with guest AI off), 429 GUEST_AI_CAP ( scope: \"guest\" ), 429 QUOTA_EXCEEDED ( scope: \"user_daily\" or \"canvas_monthly\" , the USD spend caps), 403 CAPABILITY_DISABLED (no effective provider key after the gate). During the stream (HTTP is already 200 ): a final {\"type\":\"error\",\"code\":\"AI_UPSTREAM_ERROR\",\"message\":\"the AI provider returned an error\"} event. The SDK raises AI_STREAM_TRUNCATED (502) itself when the stream ends with neither done nor error . Usage and spend are recorded exactly once per request: on success, on upstream error, and when the client aborts mid-stream. Outbound Connections Capability: connections . A grant, not a canvas-owned feature flag, is the switch. An instance administrator defines one exact HTTPS origin, the allowed standard methods, and optional protected headers, then attaches that profile to individual canvases. Backend must also be on. GET| HEAD | POST | PUT | PATCH | DELETE {base}/v1/c/{slug}/connections/{profile}/{relative-path}?query {relative-path} stays under the approved origin. An empty suffix means / . Only methods selected for the profile are accepted. GET and HEAD have no request body; the other supported methods accept at most 256 KiB. Canvas caller headers are limited to 32 and 16 KiB total. Cookies, authorization, forwarding, host, compression, and hop-by-hop headers are stripped; fixed protected headers are applied last. The upstream response status and bounded body are returned unchanged, including 4xx and 5xx , with only safe response headers. Canvas Drop adds X-Canvas-Drop-Connection-Response: upstream , Cache-Control: no-store , X-Content-Type-Options: nosniff , and Content-Security-Policy: sandbox . Upstream cookies, redirects, CORS policy, server identification, and hop-by-hop headers are removed. Non-identity content encoding is refused. Platform failures are JSON and carry one of the stable connection codes on the Error codes page. Default bounds are an 8 KiB relative URL, 2 MiB response, 10-second total DNS/request/body deadline, three exact-origin redirects, 60 requests/minute per actor+canvas+profile, 600/minute per profile, five concurrent requests per canvas, and 50 per server process. Security is enforced before every socket: IP literals and non-HTTPS origins are invalid; every A/AAAA answer must be public; mixed public/private answers reject; the validated address is pinned to the socket while TLS verifies the approved hostname; every redirect retains the exact origin and re-runs DNS validation. See Outbound Connections for the SDK contract and the remaining upstream-reflection trust boundary. Realtime Capability: realtime . Effective only with CANVAS_DROP_REALTIME=on (the default) plus the canvas switch. The route exists only where the server wires a WebSocket adaptor (the Node server does). GET {base} /v1/ c /{slug}/ realtime WebSocket upgrade (ws: // or wss: // on the API base) The full pipeline above runs before the upgrade: a failed login, authorization, password gate, or isolation check refuses the 101 with the usual HTTP error. After the socket is open, the server closes it with one of these codes: Close code When 4403 realtime is off. The server first sends {\"type\":\"error\",\"code\":\"CAPABILITY_DISABLED\",\"capability\":\"realtime\"} , then closes. Also used when realtime is switched off mid-session. 4429 The canvas already has 30 open connections ( \"connection limit\" ). 4401 Access was lost on revalidation: canvas gone, access removed, the canvas became a Public link (static-only), a password gate was set, or the user was deactivated. 1001 Server shutdown. Limits: 30 connections per canvas, 100 publishes per minute per connection, 16 KiB per frame. The SDK treats 4401 , 4403 , and 4429 as terminal and does not reconnect. Frames are JSON text, ≤ 16 KiB each, all scoped to the canvas from the handshake. Client to server: Frame Purpose {\"type\":\"subscribe\",\"channel\"} Join a channel; the server answers subscribed and a presence snapshot, and tells other subscribers join . {\"type\":\"unsubscribe\",\"channel\"} Leave it; others get leave once the user's last connection is gone. {\"type\":\"publish\",\"channel\",\"event\",\"data\"} Fan a message out to the channel's subscribers. A missing event is sent as \"\" . {\"type\":\"presence\",\"channel\"} Ask who is on the channel. Server to client: Frame Meaning {\"type\":\"subscribed\",\"channel\"} Subscription confirmed. {\"type\":\"message\",\"channel\",\"event\",\"data\",\"from\":{id,name}} A published message. from is resolved server-side; a client cannot spoof it. {\"type\":\"presence\",\"channel\",\"users\":[{id,name}]} Current members. {\"type\":\"join\"|\"leave\",\"channel\",\"user\":{id,name}} Membership change. {\"type\":\"error\",\"code\",\"message\"} MESSAGE_TOO_LARGE (> 16 KiB), INVALID_FRAME (not JSON), CHANNEL_NAME_TOO_LARGE (> 128 bytes), CHANNEL_LIMIT (> 64 channels on one connection), RATE_LIMITED (> 100 publishes per minute on one connection), UNKNOWN_FRAME . The SDK's canvasdrop.realtime.channel(name) handles framing, reconnection, and presence; see Realtime . Authoring Capability: authoring . Off by default; needs the canvas switch and the instance switch CANVAS_DROP_AUTHORING=on . Lets a signed-in org member create and manage further canvases (managed shares) from inside a canvas. A legacy guest principal gets 401 NOT_AUTHENTICATED ; an anonymous visitor never gets this far ( STATIC_ONLY ). POST {base}/v1/c/{slug}/authoring publish a new canvas → 200 AuthoredCanvas PUT {base}/v1/c/{slug}/authoring/{ id } update in place → 200 AuthoredCanvas GET {base}/v1/c/{slug}/authoring list your shares → 200 { canvases: [AuthoredCanvas] } DELETE {base}/v1/c/{slug}/authoring/{ id } revoke → 204 POST and PUT are multipart/form-data with two parts: metadata , a JSON string, and bundle , the static-site zip (required on POST , optional on PUT , never empty). The password is an optional lock independent of the audience; access: \"password\" is the compatibility shorthand for a Public link plus a password. metadata field POST PUT Notes title required, 1-200 chars optional slug optional not accepted Custom slug. Invalid: 400 INVALID_BODY with reason: \"invalid_slug\" ; in use: 409 SLUG_TAKEN . tags optional optional Up to 20, each 1-64 chars. access optional, default private optional private (Restricted: the people-and-teams list only), whole_org , public_link , or password (a Public link with a password); specific_people is accepted as a legacy alias of private . Must be in CANVAS_DROP_AUTHORING_ALLOWED_RUNGS (default private,specific_people,whole_org,public_link ; public_link covers password ), else 400 INVALID_BODY . password optional, 1-200 chars optional; null clears Required when access is password . expiresAt optional, epoch ms optional; null clears Must be in the future and within CANVAS_DROP_AUTHORING_MAX_EXPIRY_DAYS (default 0 , no cap); required when CANVAS_DROP_AUTHORING_REQUIRE_EXPIRY is on (default off). metadata optional object optional Free-form, ≤ 16 KiB. expectedUpdatedAt not accepted optional, epoch ms Compare-and-swap token: the updatedAt you last read. A stale value is refused with 409 SHARE_CONFLICT and the current record. POST creates the canvas under your account, deploys the bundle, and applies the share settings in one call. It counts against the authoring quota ( 429 QUOTA_EXCEEDED with scope: \"user_daily\" or \"user_total\" ; defaults 20 per day and 200 total, set by CANVAS_DROP_AUTHORING_USER_DAILY_MAX and CANVAS_DROP_AUTHORING_USER_TOTAL_MAX ). A deploy or share-config failure after the row exists still counts. PUT updates an existing share in place: a new immutable version at the same URL when bundle is included, settings and metadata only when it is omitted. You must be the owner or an editor of {id} (an instance admin acting on a known id also passes); anything else reads as 404 NOT_FOUND . It does not consume quota. Gates are checked against the share's resulting state, so clearing a password or dropping an expiry on a Public link faces the same checks a fresh publish would. Settings are saved before the bundle deploys: if the deploy then fails, the response is 502 UPDATE_PARTIAL with stage and the saved current record, so nothing you changed is lost. GET returns the shares you authored and still manage as owner or editor, including draft, revoked, expired, archived, and admin-disabled rows. Deleted canvases are omitted. Filters: ?sourceApp=&sourceKind=&tags=a,b (every listed tag must match). The response is Cache-Control: private, no-store . DELETE revokes: the URL stops serving and revokedAt is set, but the record stays listed with status: \"revoked\" . A later PUT with a bundle publishes it again; a settings-only PUT on a revoked share is 409 SHARE_REVOKED . AuthoredCanvas : { id, url, title, tags, access, accessMode, publicationStatus, hasPassword, status, createdAt, updatedAt, expiresAt, galleryListed, galleryTemplatable, discoverability, revokedAt, createdBy, viewerRole, audienceSummary, version, bundleUpdatedAt, sourceApp, sourceKind, metadata } accessMode is the audience: restricted ( access is private or a legacy alias — only the people and teams on the list, who are admitted at every value), whole_org , or public_link . publicationStatus is the lifecycle, independent of the audience, first match wins: disabled , archived , unpublished ( revokedAt set), draft (no version), expired ( expiresAt passed), else published ; deleted canvases never appear. The older status ( revoked › expired › private › live ) is deprecated and frozen: its private means the persisted value is literally private , not that nobody can open the share. version is the id of the current published version ( null when none) and advances on every deploy; bundleUpdatedAt is the row's last write (deploy or settings), the same value as updatedAt , so watch version to detect a bundle change. sourceApp and sourceKind are read from the free-form metadata when they are strings. viewerRole ( owner , editor , or admin ) says why you may manage the record; audienceSummary ( { count, names } ) is a safe summary of the people and teams on its list. Code HTTP When NOT_AUTHENTICATED 401 Legacy guest caller. INVALID_BODY 400 Not multipart, metadata missing or not JSON, bundle missing ( POST ) or empty, schema failure, rung not allowed, missing password for password access, expiry missing, past, or over the maximum, metadata over 16 KiB. reason may be org_forbidden or invalid_slug . INVALID_BODY 413 Bundle over 50 MiB ( message: \"bundle too large\" ). ORG_REQUIRED 409 access: whole_org requested while the caller (or, on PUT , the canvas) has no home org under tenancy. PUBLIC_LINKS_DISABLED 403 A Public link requested while the instance has public links off. PUBLIC_NOT_ALLOWED 403 A Public link requested by an owner whose account may not publish public links. PUBLIC_LINK_OWNER_GATED 403 PUT by an editor requesting a Public link on a canvas whose owner may not publish them. QUOTA_EXCEEDED 429 POST only; scope is user_daily or user_total . SLUG_TAKEN 409 POST with a custom slug already in use. NOT_FOUND 404 PUT or DELETE on a canvas you hold no role on, or that is missing, deleted, or (for DELETE ) not active. DISABLED 409 PUT or DELETE on an admin-disabled canvas. Note the status: 409 here, 403 from the shared pipeline. SHARE_REVOKED 409 Settings-only PUT on a revoked share. SHARE_CONFLICT 409 PUT with a stale expectedUpdatedAt ; the body carries the current record. PUBLISH_FAILED 502 Create, deploy, or share config failed. id is included once the record exists so you can retry or revoke. UPDATE_PARTIAL 502 PUT saved the settings but the bundle deploy failed; stage and current are included. The SDK wraps these four routes as canvasdrop.canvases . Adjacent endpoints Not under /v1/c/{slug} , but part of the browser-facing surface: GET { base}/sdk/v1.js the browser SDK bundle GET { base}/api/me dashboard identity and instance config /sdk/v1.js sits behind the auth gateway, is served as application/javascript; charset=utf-8 with cache-control: public, max-age=3600 , is not rate-limited, and answers 503 (plain text) when the bundle has not been built ( pnpm build ). /api/me is the dashboard's identity call: { id, email, name, avatarUrl, isAdmin, orgs: [{ id, name }], isGuest, canPublishPublic, authMode, urlMode, baseUrl, designSkin } . authMode is proxy , oidc , or dev ; urlMode is path or subdomain . It is not capability-gated and uses the management rate-limit class ( 429 {\"error\":\"rate_limited\"} ). Canvas code should call /v1/c/{slug}/me instead. Errors Every code is stable; branch on it, never on message text. The full list, with the SDK's exception classes, is on Error codes .\n\n## Error codes\n\nError codes If you call the Runtime API or the browser SDK from a canvas, or you are an agent handling its failures, this page is the contract. Branch on the error's code , never on its message text. Every refusal from /v1/c/{slug}/* is JSON with a stable code and an HTTP status, and the browser SDK throws typed errors that extend CanvasdropError , each with a readonly .code and .status (plus .hint when the server sent a remediation hint). The browser global is window.canvasdrop , loaded from {base}/sdk/v1.js ; there is no cd alias. The error classes, ERROR_CODES , and errorFromResponse are named exports of @canvas-drop/sdk for code that imports the package. try { await canvasdrop. kv . set ( \"prefs\" , { theme : \"dark\" }); } catch (err) { switch (err. code ) { case \"CAPABILITY_DISABLED\" : // KV is off for this canvas; the hint says how to turn it on showBanner (err. hint ); break ; case \"VALUE_TOO_LARGE\" : case \"KEY_LIMIT\" : showBanner ( \"Storage limit reached.\" ); break ; default : throw err; } } kv.get returns null for a missing key instead of throwing, so most reads need no try/catch . The codes Codes, statuses, and meanings below are the SDK's exported ERROR_CODES , in the same order (each entry is { status, summary } ; ErrorCode is its key type). Code Status Meaning NOT_AUTHENTICATED 401 The viewer is not signed in. PASSWORD_REQUIRED 403 The canvas is password-protected. CAPABILITY_DISABLED 403 Backend or the specific feature is off for this canvas. CROSS_CANVAS_FORBIDDEN 403 A request targeted another canvas's resources. MODEL_NOT_ALLOWED 403 The requested AI model is not in the allow-list. DISABLED 403 The canvas has been disabled by an administrator. STATIC_ONLY 403 The canvas is a public link ( public_link ): every backend primitive is refused for non-owners. GUEST_AI_DISABLED 403 AI is not enabled for a retained legacy guest-session viewer. GUEST_AI_CAP 429 The canvas reached its retained legacy guest-session AI spend cap. NOT_FOUND 404 The key, file, or canvas does not exist. INVALID_BODY 400 The request body failed validation. KEY_TOO_LARGE 413 The KV key exceeds the size limit. VALUE_TOO_LARGE 413 The KV value exceeds the size limit. FILE_TOO_LARGE 413 An uploaded file exceeds the per-file size limit. KEY_LIMIT 409 The canvas hit its key-count limit. NOT_NUMERIC 409 increment was called on a non-numeric value. QUOTA_EXCEEDED 429 A spend or rate quota was exceeded. CONNECTION_LIMIT 429 A realtime or outbound connection concurrency limit was reached. CONNECTION_RATE_LIMIT 429 The outbound connection rate limit was reached. CONNECTION_NOT_GRANTED 404 The outbound connection is not granted to this canvas. CONNECTION_DISABLED 503 The outbound connection profile is disabled. CONNECTION_KEY_UNAVAILABLE 503 The protected-header encryption key is unavailable. METHOD_NOT_ALLOWED 405 The outbound method is unsupported or not allowed by the profile. DESTINATION_BLOCKED 403 The outbound origin, DNS answer, redirect, or encoding crossed the approved boundary. UPSTREAM_TIMEOUT 504 The total outbound deadline expired. UPSTREAM_UNAVAILABLE 502 DNS or the approved upstream failed. RESPONSE_TOO_LARGE 502 The upstream response exceeded its limit. REQUEST_TOO_LARGE 413 The connection URL, headers, or request body exceeded its limit. AI_STREAM_TRUNCATED 502 An AI stream ended before completion. AI_UPSTREAM_ERROR 502 The AI provider returned an error. PUBLISH_FAILED 502 canvasdrop.canvases.publish created the canvas but its deploy or share-config failed; the new canvas's id is returned so the caller can retry or revoke. SHARE_REVOKED 409 canvasdrop.canvases.update was called on an unpublished share without a bundle; include a bundle to publish it again. SHARE_CONFLICT 409 canvasdrop.canvases.update sent a stale expectedUpdatedAt : the share changed after it was read. The current state is returned; refresh before retrying. UPDATE_PARTIAL 502 canvasdrop.canvases.update saved the share settings, but a later stage (the bundle deploy) failed; stage names it and current carries the saved state. REQUEST_FAILED 0 A request failed without a more specific code. Five rows need a note: NOT_AUTHENTICATED is what the SDK throws for any 401. The auth gateway's own 401 (in dev and proxy modes) has the shape { \"error\": \"unauthorized\" } with no code ; the SDK normalizes it. In oidc mode the gateway does not return 401 at all: a signed-out request is redirected (302) to the login page. The authoring routes return 401 { \"code\": \"NOT_AUTHENTICATED\" } to a retained legacy guest session, since only org members can author. STATIC_ONLY applies to every caller except the canvas's owner and its editors. A signed-in member gets it on a Public link canvas too, not only an anonymous visitor. QUOTA_EXCEEDED is 429 from AI (the body adds scope : user_daily or canvas_monthly ) and from authoring ( scope : user_daily or user_total ), but 409 when a file upload would exceed the per-canvas byte quota. err.code is the same in all three cases; err.status and scope tell them apart. INVALID_BODY is 400 everywhere except one case: an authoring bundle over 50 MiB returns 413 { \"code\": \"INVALID_BODY\", \"message\": \"bundle too large\" } , and the SDK's any-413 rule puts it on QuotaExceededError (see below). REQUEST_FAILED is the SDK's fallback when a response carries no code . The 0 is nominal; err.status holds the real HTTP status. CAPABILITY_DISABLED is self-repairing. Its 403 body carries extra fields beyond code , so a caller (or an agent) can fix it without guessing: capability (which one), backendEnabled (the master switch, false on a new canvas), reason ( backend_off , feature_off , or operator_disabled ), and a human-readable hint . The SDK puts the hint on the thrown CapabilityDisabledError as both its message and .hint . For backend_off and feature_off the fix is the owner's or an editor's: the canvas's Backend tab, the set_capabilities MCP tool, or PATCH /api/canvases/{id}/capabilities {\"backendEnabled\": true, \"kv\": true} . operator_disabled (AI, realtime, and authoring only) is a deployment-level setting: no AI provider key, CANVAS_DROP_REALTIME=off , or CANVAS_DROP_AUTHORING=off (the default). See Capabilities . DISABLED has two surfaces. On the Runtime API (viewers, the browser SDK) a canvas an admin has taken down returns DISABLED with status 403 , the row above. On the owner management API ( /api/canvases/{id}/... ), the authoring routes' PUT / DELETE , and over MCP, the same takedown makes the canvas read-only to its owner and editors: reads succeed, but every mutation (settings, sharing, tags, capabilities, slug, preview, deploy, publish, rollback, archive, unpublish, draft edits) is refused with HTTP 409 { \"code\": \"DISABLED\", \"message\": \"This canvas has been disabled by an administrator.\" } , with Reason: <text> appended when the admin set one. The MCP tool result is DISABLED: <that message> . The same management surfaces refuse an editor's owner-only act (delete, transfer, the guest-AI opt-in) with 403 { \"code\": \"OWNER_ONLY\", \"message\": \"Only the canvas owner can do this.\" } ; do not confuse it with the Runtime API's OWNER_ONLY , which is a 404 (next section). Typed SDK errors The SDK exports the base class and six subclasses. Any code without a dedicated subclass is thrown as the base CanvasdropError with .code set from the response. Class .code .status Extra NotAuthenticatedError NOT_AUTHENTICATED 401 CapabilityDisabledError CAPABILITY_DISABLED 403 message and .hint from the server's hint ; no .capability property NotFoundError NOT_FOUND 404 Every 404 except CONNECTION_NOT_GRANTED . QuotaExceededError QUOTA_EXCEEDED by default; see below 429 by default; see below message is always quota exceeded PublishFailedError PUBLISH_FAILED 502 .id : the created canvas's id, when the failure happened after creation UpdatePartialError UPDATE_PARTIAL 502 .stage : the stage that failed after the settings were saved; .current : the saved share state, when the server returned it CanvasdropError (base) any any .hint when the server sent one The SDK picks the class from the HTTP response in this order ( errorFromResponse ): Status 401 → NotAuthenticatedError (any body code is ignored). Status 403 with code: \"CAPABILITY_DISABLED\" → CapabilityDisabledError . Status 404, except code: \"CONNECTION_NOT_GRANTED\" , → NotFoundError (the other body codes are ignored; see the next section). code: \"PUBLISH_FAILED\" → PublishFailedError with .id from the body. code: \"UPDATE_PARTIAL\" → UpdatePartialError with .stage and .current from the body. code of QUOTA_EXCEEDED , GUEST_AI_CAP , KEY_LIMIT , CONNECTION_LIMIT , or CONNECTION_RATE_LIMIT , or any 413 → QuotaExceededError , keeping the body's code and the response's status. Anything else—including CONNECTION_NOT_GRANTED at 404—→ CanvasdropError with the body's code (or REQUEST_FAILED ), the response status, and the body's hint or message as the message. So QuotaExceededError is the one limit-shaped class and it is reused: expect .code values of QUOTA_EXCEEDED , GUEST_AI_CAP , KEY_LIMIT (409), KEY_TOO_LARGE , VALUE_TOO_LARGE , FILE_TOO_LARGE , BODY_TOO_LARGE , INVALID_BODY (413), CONNECTION_LIMIT (429, realtime or outbound admission), and CONNECTION_RATE_LIMIT (429). NOT_NUMERIC is a 409 but not a limit, so it stays on the base class. Never assume a QuotaExceededError is literally QUOTA_EXCEEDED ; read err.code . Classes such as PasswordRequiredError , ModelNotAllowedError , or CrossCanvasForbiddenError do not exist. PASSWORD_REQUIRED , STATIC_ONLY , DISABLED , MODEL_NOT_ALLOWED , CROSS_CANVAS_FORBIDDEN , GUEST_AI_DISABLED , INVALID_BODY (at 400), NOT_NUMERIC , SHARE_REVOKED , SHARE_CONFLICT , and AI_UPSTREAM_ERROR all arrive on the base CanvasdropError . Branching on err.code is the only reliable check. Server codes outside the enum The Runtime API can return a few codes that are not in ERROR_CODES . They are stable; the table shows how each reaches SDK callers. Code Status When In the SDK RATE_LIMITED 429 A request bucket is spent. Primitives: per user per canvas, default 120/min. .../ai/* : per user across all canvases, default 10/min. Headers: Retry-After , X-RateLimit-Limit , X-RateLimit-Remaining , X-RateLimit-Reset . CanvasdropError , code: \"RATE_LIMITED\" ARCHIVED , NOT_INVITED , OWNER_ONLY , SHARE_EXPIRED 404 The canvas resolved but access was denied (archived; guest scoped to another canvas; not on the sharing rung, including a non-owner admin; share expired). NotFoundError , code: \"NOT_FOUND\" ; the body's code is not preserved CROSS_SITE_FORBIDDEN 403 Path mode only: Sec-Fetch-Site is present and not same-origin or none . CanvasdropError BODY_TOO_LARGE 413 POST .../ai/chat body over 256 KiB. QuotaExceededError , code: \"BODY_TOO_LARGE\" (every 413 maps there) ORG_REQUIRED , SLUG_TAKEN 409 Authoring publish/update. CanvasdropError PUBLIC_LINKS_DISABLED , PUBLIC_NOT_ALLOWED , PUBLIC_LINK_OWNER_GATED 403 Authoring publish/update asked for a Public link the instance, the actor, or the actor's role (editor) cannot grant. CanvasdropError The full per-route list, including which routes emit INVALID_BODY with a reason field, is on the Runtime API page. Client-side codes The SDK also throws a handful of codes that never come from the server. They are base CanvasdropError s and are not in ERROR_CODES , so err.code is typed string , not ErrorCode . Code Status When NO_STREAM the HTTP status The AI response had no body. MALFORMED_FRAME 502 An SSE data: line was not valid JSON (proxy teardown, partial flush). AI_ERROR 502 An in-stream error frame arrived without a string code . DISCONNECTED 0 The realtime socket dropped while a presence() call was in flight; the SDK is reconnecting, retry the call. CHANNEL_CLOSED 0 publish() or presence() was called on a channel after close() ; pending presence() calls also reject with it when close() runs. AI stream errors ai.chat and ai.stream consume a server-sent event stream. Failures surface in two ways: Before the stream starts , as an HTTP error mapped per the tables above, in the server's check order: BODY_TOO_LARGE (413), INVALID_BODY (400), MODEL_NOT_ALLOWED (403), GUEST_AI_DISABLED (403), GUEST_AI_CAP (429), QUOTA_EXCEEDED (429), or CAPABILITY_DISABLED (403). RATE_LIMITED (429) fires before any of them. Mid-stream , as an error frame. CAPABILITY_DISABLED becomes CapabilityDisabledError ; QUOTA_EXCEEDED and GUEST_AI_CAP become QuotaExceededError with status 429; any other code becomes a base CanvasdropError with that code and status 502. The server's mid-stream provider failure is AI_UPSTREAM_ERROR ; a frame with no code defaults to AI_ERROR . If the stream ends without a terminal done or error frame, both methods throw AI_STREAM_TRUNCATED (502). ai.stream yields text only; usage and cost are available from ai.chat . Realtime close codes A terminal WebSocket close maps to a typed error and stops reconnecting. Any pending presence() call rejects with it, and later publish() or presence() calls on the same client throw it. Close code Error 4403 CapabilityDisabledError ( realtime is off) 4401 NotAuthenticatedError (access lost, canvas gone, canvas turned Public link, password gate newly set, or user inactive) 4429 QuotaExceededError ( code: \"CONNECTION_LIMIT\" , status 429; 30 connections per canvas) An in-band { \"type\": \"error\", \"code\": \"CAPABILITY_DISABLED\" } frame is terminal in the same way. The hub's other error frames ( MESSAGE_TOO_LARGE , INVALID_FRAME , CHANNEL_NAME_TOO_LARGE , CHANNEL_LIMIT , RATE_LIMITED , UNKNOWN_FRAME ) are dropped by the SDK; there is no hook to observe them, so a rejected publish is silent. Stay inside the limits on the Runtime API page. Any other close is transient: the SDK reconnects with exponential backoff (500 ms doubling to a 10 s cap), re-subscribes every channel, and flushes up to 256 buffered frames. An in-flight presence() call rejects with DISCONNECTED (status 0) so the caller can retry.\n";
