// AUTO-GENERATED by scripts/build-docs.mjs from docs/site/. Do not edit by hand.
// Run `pnpm docs:build` to regenerate. CI asserts this file has no uncommitted drift.

export interface DocHeading { id: string; text: string; level: number; }
export interface DocPage { path: string; section: string; title: string; html: string; headings: DocHeading[]; text: string; }
export interface NavSection { section: string; pages: { path: string; title: string }[]; }
export interface SearchEntry { path: string; title: string; headings: string[]; text: string; }

export const DOC_PAGES: DocPage[] = [
  {
    "path": "",
    "section": "Getting started",
    "title": "Overview",
    "html": "<h1 id=\"canvas-drop\">canvas-drop</h1>\n<p>canvas-drop is an open-source, self-hostable platform where members of an\norganization deploy and share small web artifacts — <strong>canvases</strong>. A canvas is\njust static files (HTML, CSS, JS, images). Drop them in and they're live at a\nURL your colleagues can open.</p>\n<p>The constraint is the product: a small set of primitives, done well, instead of\na general-purpose hosting platform. Canvases run with no build step and no\nsecrets in the page, and they gain backend capability only through five\nprimitives exposed by a zero-config browser SDK.</p>\n<h2 id=\"what-you-can-build\">What you can build</h2>\n<p>Prototypes, dashboards, demos, microsites, small games, internal tools — anything\nthat communicates better as a working artifact than as a screenshot or a slide.</p>\n<h2 id=\"how-it-fits-together\">How it fits together</h2>\n<ul>\n<li><strong>Deploy</strong> a canvas by dragging a folder/ZIP into the dashboard, pasting HTML,\nusing the in-browser editor, or calling the <a href=\"/docs/api/deploy-api\">deploy API</a>\n(agents can ship without a human in the loop).</li>\n<li><strong>Add backend capability</strong> with the <a href=\"/docs/sdk/overview\">browser SDK</a>: key–value\nstorage, file storage, the signed-in viewer's identity, AI, and realtime — each\ngated by a capability the canvas owner turns on.</li>\n<li><strong>Share</strong> the URL. Access follows your organization's sign-in.</li>\n</ul>\n<h2 id=\"where-to-go-next\">Where to go next</h2>\n<ul>\n<li>New here? Start with the <a href=\"/docs/quickstart\">Quickstart</a>.</li>\n<li>Building a canvas with a backend? Read the <a href=\"/docs/sdk/overview\">SDK overview</a>.</li>\n<li>Running your own instance? See <a href=\"/docs/self-hosting/install\">Self-hosting → Install</a>.</li>\n<li>An AI agent? Read <a href=\"/llms.txt\"><code>/llms.txt</code></a> and the <a href=\"/docs/agents/skill\">agent skill</a>.</li>\n</ul>\n<blockquote>\n<p>Examples and URLs in these docs use <code>{base}</code> (your instance's base URL) and\n<code>localhost</code> placeholders. Substitute your own instance's address.</p>\n</blockquote>\n",
    "headings": [
      {
        "id": "what-you-can-build",
        "text": "What you can build",
        "level": 2
      },
      {
        "id": "how-it-fits-together",
        "text": "How it fits together",
        "level": 2
      },
      {
        "id": "where-to-go-next",
        "text": "Where to go next",
        "level": 2
      }
    ],
    "text": "canvas-drop canvas-drop is an open-source, self-hostable platform where members of an organization deploy and share small web artifacts — canvases . A canvas is just static files (HTML, CSS, JS, images). Drop them in and they're live at a URL your colleagues can open. The constraint is the product: a small set of primitives, done well, instead of a general-purpose hosting platform. Canvases run with no build step and no secrets in the page, and they gain backend capability only through five primitives exposed by a zero-config browser SDK. What you can build Prototypes, dashboards, demos, microsites, small games, internal tools — anything that communicates better as a working artifact than as a screenshot or a slide. How it fits together Deploy a canvas by dragging a folder/ZIP into the dashboard, pasting HTML, using the in-browser editor, or calling the deploy API (agents can ship without a human in the loop). Add backend capability with the browser SDK : key–value storage, file storage, the signed-in viewer's identity, AI, and realtime — each gated by a capability the canvas owner turns on. Share the URL. Access follows your organization's sign-in. Where to go next New here? Start with the Quickstart . Building a canvas with a backend? Read the SDK overview . Running your own instance? See Self-hosting → Install . An AI agent? Read /llms.txt and the agent skill . Examples and URLs in these docs use {base} (your instance's base URL) and localhost placeholders. Substitute your own instance's address."
  },
  {
    "path": "quickstart",
    "section": "Getting started",
    "title": "Quickstart",
    "html": "<h1 id=\"quickstart\">Quickstart</h1>\n<p>Deploy your first canvas in a few minutes.</p>\n<h2 id=\"1-create-a-canvas\">1. Create a canvas</h2>\n<p>From the dashboard, click <strong>New</strong>. You can:</p>\n<ul>\n<li><strong>Drag a folder or ZIP</strong> containing an <code>index.html</code>.</li>\n<li><strong>Paste HTML</strong> for a quick single-page canvas.</li>\n<li><strong>Start from the editor</strong> and write files in the browser.</li>\n</ul>\n<p>Each canvas gets a slug and a URL. In path mode that's <code>{base}/c/{slug}/</code>; in\nsubdomain mode it's <code>{slug}.{base}</code>.</p>\n<h2 id=\"2-add-some-content\">2. Add some content</h2>\n<p>A canvas is static files. The simplest possible canvas is one <code>index.html</code>:</p>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-meta\">&lt;!doctype <span class=\"hljs-keyword\">html</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">html</span>&gt;</span>\n  <span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">body</span>&gt;</span>\n    <span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">h1</span>&gt;</span>Hello from my canvas<span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">h1</span>&gt;</span>\n  <span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">body</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">html</span>&gt;</span></code></pre>\n<p>No build step runs on the server — what you deploy is what's served.</p>\n<h2 id=\"3-give-it-a-backend-optional\">3. Give it a backend (optional)</h2>\n<p>Add the SDK and you get storage, identity, and more with no keys in the page:</p>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">src</span>=<span class=\"hljs-string\">\"/sdk/v1.js\"</span>&gt;</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span>\n<span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span>&gt;</span><span class=\"language-javascript\">\n  <span class=\"hljs-keyword\">const</span> me = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-title function_\">me</span>();\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">increment</span>(<span class=\"hljs-string\">\"views\"</span>);\n</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span></code></pre>\n<p>The owner enables <strong>Backend</strong> (and the specific feature) in the canvas's\n<strong>Capabilities</strong> tab first. See the <a href=\"/docs/sdk/overview\">SDK overview</a>.</p>\n<h2 id=\"4-publish-amp-share\">4. Publish &amp; share</h2>\n<p>Publish a version and share the canvas URL. Who can open it follows your\norganization's sign-in — canvases are private to your org by default.</p>\n<h2 id=\"deploying-as-an-agent\">Deploying as an agent</h2>\n<p>Agents deploy over HTTP with a per-canvas API key — no dashboard session needed.\nSee the <a href=\"/docs/api/deploy-api\">Deploy API</a> and <a href=\"/llms.txt\"><code>/llms.txt</code></a>.</p>\n",
    "headings": [
      {
        "id": "1-create-a-canvas",
        "text": "1. Create a canvas",
        "level": 2
      },
      {
        "id": "2-add-some-content",
        "text": "2. Add some content",
        "level": 2
      },
      {
        "id": "3-give-it-a-backend-optional",
        "text": "3. Give it a backend (optional)",
        "level": 2
      },
      {
        "id": "4-publish-amp-share",
        "text": "4. Publish & share",
        "level": 2
      },
      {
        "id": "deploying-as-an-agent",
        "text": "Deploying as an agent",
        "level": 2
      }
    ],
    "text": "Quickstart Deploy your first canvas in a few minutes. 1. Create a canvas From the dashboard, click New . You can: Drag a folder or ZIP containing an index.html . Paste HTML for a quick single-page canvas. Start from the editor and write files in the browser. Each canvas gets a slug and a URL. In path mode that's {base}/c/{slug}/ ; in subdomain mode it's {slug}.{base} . 2. Add some content A canvas is static files. The simplest possible canvas is one index.html : <!doctype html > < html > < body > < h1 > Hello from my canvas </ h1 > </ body > </ html > No build step runs on the server — what you deploy is what's served. 3. Give it a backend (optional) Add the SDK and you get storage, identity, and more with no keys in the page: < script src = \"/sdk/v1.js\" > </ script > < script > const me = await canvasdrop. me (); await canvasdrop. kv . increment ( \"views\" ); </ script > The owner enables Backend (and the specific feature) in the canvas's Capabilities tab first. See the SDK overview . 4. Publish & share Publish a version and share the canvas URL. Who can open it follows your organization's sign-in — canvases are private to your org by default. Deploying as an agent Agents deploy over HTTP with a per-canvas API key — no dashboard session needed. See the Deploy API and /llms.txt ."
  },
  {
    "path": "authoring/create-and-deploy",
    "section": "Authoring canvases",
    "title": "Create & deploy",
    "html": "<h1 id=\"create-amp-deploy\">Create &amp; deploy</h1>\n<p>There are four ways to get a canvas live. All produce the same thing: a versioned\nset of static files served at the canvas URL.</p>\n<h2 id=\"drag-and-drop-a-folder-or-zip\">Drag-and-drop a folder or ZIP</h2>\n<p>The fastest path for an existing project. Drop a folder (or a <code>.zip</code>) that\ncontains an <code>index.html</code> at its root. The files are uploaded, a version is\ncreated, and the canvas goes live.</p>\n<h2 id=\"paste-html\">Paste HTML</h2>\n<p>For a one-file canvas, paste HTML directly. canvas-drop wraps it into an\n<code>index.html</code> and deploys it.</p>\n<h2 id=\"in-browser-editor\">In-browser editor</h2>\n<p>Create files and edit them with syntax highlighting in the browser. Your work is\nsaved as a <strong>draft</strong>; you choose when to <strong>publish</strong> a version. See\n<a href=\"/docs/authoring/editor\">The editor</a>.</p>\n<h2 id=\"deploy-api\">Deploy API</h2>\n<p>Ship from CI or an agent with a per-canvas API key over HTTP — no human and no\ndashboard session required:</p>\n<pre><code class=\"hljs language-bash\">curl -X PUT <span class=\"hljs-string\">\"{base}/v1/canvases/{id}/deploy\"</span> \\\n  -H <span class=\"hljs-string\">\"Authorization: Bearer <span class=\"hljs-variable\">$CANVAS_KEY</span>\"</span> \\\n  --data-binary @site.zip</code></pre>\n<p>See the <a href=\"/docs/api/deploy-api\">Deploy API reference</a>.</p>\n<h2 id=\"versions-and-rollback\">Versions and rollback</h2>\n<p>Every deploy creates a new version. The canvas always serves its currently\npublished version, and you can roll back to a previous one from the dashboard or\nthe API. Versions are content-addressed, so re-deploying identical files is cheap.</p>\n",
    "headings": [
      {
        "id": "drag-and-drop-a-folder-or-zip",
        "text": "Drag-and-drop a folder or ZIP",
        "level": 2
      },
      {
        "id": "paste-html",
        "text": "Paste HTML",
        "level": 2
      },
      {
        "id": "in-browser-editor",
        "text": "In-browser editor",
        "level": 2
      },
      {
        "id": "deploy-api",
        "text": "Deploy API",
        "level": 2
      },
      {
        "id": "versions-and-rollback",
        "text": "Versions and rollback",
        "level": 2
      }
    ],
    "text": "Create & deploy There are four ways to get a canvas live. All produce the same thing: a versioned set of static files served at the canvas URL. Drag-and-drop a folder or ZIP The fastest path for an existing project. Drop a folder (or a .zip ) that contains an index.html at its root. The files are uploaded, a version is created, and the canvas goes live. Paste HTML For a one-file canvas, paste HTML directly. canvas-drop wraps it into an index.html and deploys it. In-browser editor Create files and edit them with syntax highlighting in the browser. Your work is saved as a draft ; you choose when to publish a version. See The editor . Deploy API Ship from CI or an agent with a per-canvas API key over HTTP — no human and no dashboard session required: curl -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip See the Deploy API reference . Versions and rollback Every deploy creates a new version. The canvas always serves its currently published version, and you can roll back to a previous one from the dashboard or the API. Versions are content-addressed, so re-deploying identical files is cheap."
  },
  {
    "path": "authoring/capabilities",
    "section": "Authoring canvases",
    "title": "Capabilities",
    "html": "<h1 id=\"capabilities\">Capabilities</h1>\n<p>A canvas is static by default. To give it backend behaviour, the owner turns on\ncapabilities in the canvas's <strong>Capabilities</strong> tab. Capabilities are off until\nexplicitly enabled, and a method whose capability is off throws a\n<code>CapabilityDisabledError</code>.</p>\n<h2 id=\"the-switch-hierarchy\">The switch hierarchy</h2>\n<p>A feature is only <strong>effective</strong> when three things are all true:</p>\n<ol>\n<li><strong>Backend</strong> is on for the canvas (the master switch).</li>\n<li>The <strong>specific feature</strong> (KV, files, identity, AI, realtime) is on.</li>\n<li>The <strong>operator</strong> has not globally disabled that feature for the instance.</li>\n</ol>\n<p>Identity is effective whenever Backend is on.</p>\n<h2 id=\"the-five-primitives\">The five primitives</h2>\n<table>\n<thead>\n<tr>\n<th>Capability</th>\n<th>What it gives the canvas</th>\n<th>SDK</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Key–value</td>\n<td>Shared and per-viewer JSON storage, atomic increment</td>\n<td><a href=\"/docs/sdk/kv\">kv</a></td>\n</tr>\n<tr>\n<td>Files</td>\n<td>Upload, list, serve files</td>\n<td><a href=\"/docs/sdk/files\">files</a></td>\n</tr>\n<tr>\n<td>Identity</td>\n<td>The signed-in viewer's id, email, name</td>\n<td><a href=\"/docs/sdk/identity\">identity</a></td>\n</tr>\n<tr>\n<td>AI</td>\n<td>Server-side model calls, no provider key in the page</td>\n<td><a href=\"/docs/sdk/ai\">ai</a></td>\n</tr>\n<tr>\n<td>Realtime</td>\n<td>Ephemeral pub/sub + presence</td>\n<td><a href=\"/docs/sdk/realtime\">realtime</a></td>\n</tr>\n</tbody></table>\n<h2 id=\"why-off-by-default\">Why off by default</h2>\n<p>No secrets ever live in canvas files. Capabilities are server-enforced per\nrequest from the signed-in session — the canvas can ask, but the server decides.\nTurning a capability on is a deliberate choice the owner makes per canvas.</p>\n",
    "headings": [
      {
        "id": "the-switch-hierarchy",
        "text": "The switch hierarchy",
        "level": 2
      },
      {
        "id": "the-five-primitives",
        "text": "The five primitives",
        "level": 2
      },
      {
        "id": "why-off-by-default",
        "text": "Why off by default",
        "level": 2
      }
    ],
    "text": "Capabilities A canvas is static by default. To give it backend behaviour, the owner turns on capabilities in the canvas's Capabilities tab. Capabilities are off until explicitly enabled, and a method whose capability is off throws a CapabilityDisabledError . The switch hierarchy A feature is only effective when three things are all true: Backend is on for the canvas (the master switch). The specific feature (KV, files, identity, AI, realtime) is on. The operator has not globally disabled that feature for the instance. Identity is effective whenever Backend is on. The five primitives Capability What it gives the canvas SDK Key–value Shared and per-viewer JSON storage, atomic increment kv Files Upload, list, serve files files Identity The signed-in viewer's id, email, name identity AI Server-side model calls, no provider key in the page ai Realtime Ephemeral pub/sub + presence realtime Why off by default No secrets ever live in canvas files. Capabilities are server-enforced per request from the signed-in session — the canvas can ask, but the server decides. Turning a capability on is a deliberate choice the owner makes per canvas."
  },
  {
    "path": "authoring/editor",
    "section": "Authoring canvases",
    "title": "The editor",
    "html": "<h1 id=\"the-editor\">The editor</h1>\n<p>The in-browser editor lets you create and edit a canvas's files with syntax\nhighlighting — no local setup, no build step.</p>\n<h2 id=\"drafts-and-publishing\">Drafts and publishing</h2>\n<p>Editing works on a <strong>draft</strong>: a working copy separate from the live version. Your\nchanges autosave to the draft as you go. The live canvas keeps serving its\npublished version until you explicitly <strong>publish</strong>.</p>\n<p>This gives you a safe place to iterate:</p>\n<ul>\n<li>Edit freely without affecting what viewers see.</li>\n<li>Preview the draft.</li>\n<li>Publish when ready — that creates a new version and points the canvas at it.</li>\n</ul>\n<h2 id=\"files\">Files</h2>\n<p>Add, rename, and delete files in the file tree. <code>index.html</code> at the root is the\ncanvas's entry point. Binary assets (images, fonts) are supported alongside text\nfiles.</p>\n<h2 id=\"versions\">Versions</h2>\n<p>Each publish is a version. From the <strong>Versions</strong> view you can see history and roll\nback. Storage is content-addressed, so unchanged files across versions are stored\nonce.</p>\n<p>See also <a href=\"/docs/authoring/create-and-deploy\">Create &amp; deploy</a> for the non-editor\npaths (drag-drop, paste, API).</p>\n",
    "headings": [
      {
        "id": "drafts-and-publishing",
        "text": "Drafts and publishing",
        "level": 2
      },
      {
        "id": "files",
        "text": "Files",
        "level": 2
      },
      {
        "id": "versions",
        "text": "Versions",
        "level": 2
      }
    ],
    "text": "The editor The in-browser editor lets you create and edit a canvas's files with syntax highlighting — no local setup, no build step. Drafts and publishing Editing works on a draft : a working copy separate from the live version. Your changes autosave to the draft as you go. The live canvas keeps serving its published version until you explicitly publish . This gives you a safe place to iterate: Edit freely without affecting what viewers see. Preview the draft. Publish when ready — that creates a new version and points the canvas at it. Files Add, rename, and delete files in the file tree. index.html at the root is the canvas's entry point. Binary assets (images, fonts) are supported alongside text files. Versions Each publish is a version. From the Versions view you can see history and roll back. Storage is content-addressed, so unchanged files across versions are stored once. See also Create & deploy for the non-editor paths (drag-drop, paste, API)."
  },
  {
    "path": "sdk/overview",
    "section": "Browser SDK",
    "title": "Overview",
    "html": "<h1 id=\"browser-sdk\">Browser SDK</h1>\n<p>The browser SDK gives a canvas backend capability — key–value storage, file\nstorage, the signed-in viewer's identity, AI, and realtime — with <strong>no build step\nand no secrets in the canvas</strong>. Identity comes from the signed-in session; the\ncanvas is identified by its own URL.</p>\n<h2 id=\"add-it-to-a-canvas\">Add it to a canvas</h2>\n<pre><code class=\"hljs language-html\"><span class=\"hljs-tag\">&lt;<span class=\"hljs-name\">script</span> <span class=\"hljs-attr\">src</span>=<span class=\"hljs-string\">\"/sdk/v1.js\"</span>&gt;</span><span class=\"hljs-tag\">&lt;/<span class=\"hljs-name\">script</span>&gt;</span></code></pre>\n<p>That defines the global <code>window.canvasdrop</code>. The SDK auto-detects the canvas slug\nand the API base from the page's location (path mode <code>/c/{slug}/…</code> or subdomain\n<code>{slug}.{base}</code>) and sends every request with the session cookie.</p>\n<blockquote>\n<p><code>/sdk/v1.js</code> is served to signed-in canvas pages — it rides the same session as\nthe canvas. The stable <code>/sdk/v1.js</code> path is additive and backward-compatible\nwithin v1, so pointing a <code>&lt;script&gt;</code> at it means you receive fixes (including\nsecurity patches) without redeploying. A breaking change would ship under a new\npath (<code>/sdk/v2.js</code>).</p>\n</blockquote>\n<h2 id=\"enable-the-capability-first\">Enable the capability first</h2>\n<p>The canvas owner must turn on <strong>Backend</strong> (and the specific feature) in the\ncanvas's <strong>Capabilities</strong> tab. A method whose capability is off throws a\n<code>CapabilityDisabledError</code>. See <a href=\"/docs/authoring/capabilities\">Capabilities</a>.</p>\n<h2 id=\"the-surface\">The surface</h2>\n<ul>\n<li><a href=\"/docs/sdk/identity\"><code>canvasdrop.me()</code></a> — the signed-in viewer.</li>\n<li><a href=\"/docs/sdk/kv\"><code>canvasdrop.kv</code></a> — shared and per-viewer key–value storage.</li>\n<li><a href=\"/docs/sdk/files\"><code>canvasdrop.files</code></a> — file upload, list, serve.</li>\n<li><a href=\"/docs/sdk/ai\"><code>canvasdrop.ai</code></a> — server-side model calls.</li>\n<li><a href=\"/docs/sdk/realtime\"><code>canvasdrop.realtime</code></a> — pub/sub + presence.</li>\n</ul>\n<h2 id=\"errors\">Errors</h2>\n<p>Every failure throws a typed error extending <code>CanvasdropError</code>, each carrying a\nstable <code>.code</code> and <code>.status</code>. See the <a href=\"/docs/api/errors\">error code reference</a>.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">try</span> {\n  <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">increment</span>(<span class=\"hljs-string\">\"votes\"</span>);\n} <span class=\"hljs-keyword\">catch</span> (err) {\n  <span class=\"hljs-keyword\">if</span> (err.<span class=\"hljs-property\">code</span> === <span class=\"hljs-string\">\"CAPABILITY_DISABLED\"</span>) {\n    <span class=\"hljs-comment\">// ask the owner to enable KV in the Capabilities tab</span>\n  } <span class=\"hljs-keyword\">else</span> {\n    <span class=\"hljs-keyword\">throw</span> err;\n  }\n}</code></pre>\n<p>If you import <code>@canvas-drop/sdk</code> as a module instead of the global script, the\nerror classes are exported for <code>instanceof</code> checks.</p>\n",
    "headings": [
      {
        "id": "add-it-to-a-canvas",
        "text": "Add it to a canvas",
        "level": 2
      },
      {
        "id": "enable-the-capability-first",
        "text": "Enable the capability first",
        "level": 2
      },
      {
        "id": "the-surface",
        "text": "The surface",
        "level": 2
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      }
    ],
    "text": "Browser SDK The browser SDK gives a canvas backend capability — key–value storage, file storage, the signed-in viewer's identity, AI, and realtime — with no build step and no secrets in the canvas . Identity comes from the signed-in session; the canvas is identified by its own URL. Add it to a canvas < script src = \"/sdk/v1.js\" > </ script > That defines the global window.canvasdrop . The SDK auto-detects the canvas slug and the API base from the page's location (path mode /c/{slug}/… or subdomain {slug}.{base} ) and sends every request with the session cookie. /sdk/v1.js is served to signed-in canvas pages — it rides the same session as the canvas. The stable /sdk/v1.js path is additive and backward-compatible within v1, so pointing a <script> at it means you receive fixes (including security patches) without redeploying. A breaking change would ship under a new path ( /sdk/v2.js ). Enable the capability first The canvas owner must turn on Backend (and the specific feature) in the canvas's Capabilities tab. A method whose capability is off throws a CapabilityDisabledError . See Capabilities . The surface canvasdrop.me() — the signed-in viewer. canvasdrop.kv — shared and per-viewer key–value storage. canvasdrop.files — file upload, list, serve. canvasdrop.ai — server-side model calls. canvasdrop.realtime — pub/sub + presence. Errors Every failure throws a typed error extending CanvasdropError , each carrying a stable .code and .status . See the error code reference . try { await canvasdrop. kv . increment ( \"votes\" ); } catch (err) { if (err. code === \"CAPABILITY_DISABLED\" ) { // ask the owner to enable KV in the Capabilities tab } else { throw err; } } If you import @canvas-drop/sdk as a module instead of the global script, the error classes are exported for instanceof checks."
  },
  {
    "path": "sdk/kv",
    "section": "Browser SDK",
    "title": "Key–value storage",
    "html": "<h1 id=\"key-value-storage\">Key–value storage</h1>\n<p><code>canvasdrop.kv</code> is JSON key–value storage scoped to the canvas. Two namespaces:\n<strong>shared</strong> (canvas-global) and <strong>user</strong> (auto-scoped to the signed-in viewer).</p>\n<h2 id=\"shared\">Shared</h2>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">set</span>(<span class=\"hljs-string\">\"votes\"</span>, <span class=\"hljs-number\">0</span>);\n<span class=\"hljs-keyword\">const</span> n = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">get</span>(<span class=\"hljs-string\">\"votes\"</span>);            <span class=\"hljs-comment\">// value, or null if absent</span>\n<span class=\"hljs-keyword\">const</span> total = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">increment</span>(<span class=\"hljs-string\">\"votes\"</span>);  <span class=\"hljs-comment\">// atomic +1</span>\n<span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">delete</span>(<span class=\"hljs-string\">\"votes\"</span>);\n<span class=\"hljs-keyword\">const</span> { entries, nextCursor } = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-title function_\">list</span>({ <span class=\"hljs-attr\">prefix</span>: <span class=\"hljs-string\">\"p:\"</span>, <span class=\"hljs-attr\">limit</span>: <span class=\"hljs-number\">100</span> });</code></pre>\n<p><code>increment</code> is atomic — safe for concurrent polls, counters, and votes.</p>\n<h2 id=\"per-viewer\">Per-viewer</h2>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-property\">user</span>.<span class=\"hljs-title function_\">set</span>(<span class=\"hljs-string\">\"pref\"</span>, <span class=\"hljs-string\">\"dark\"</span>);\n<span class=\"hljs-keyword\">const</span> pref = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">kv</span>.<span class=\"hljs-property\">user</span>.<span class=\"hljs-title function_\">get</span>(<span class=\"hljs-string\">\"pref\"</span>);</code></pre>\n<p>The user namespace is keyed by the signed-in viewer, so each person reads and\nwrites their own values without the canvas managing identity.</p>\n<h2 id=\"limits\">Limits</h2>\n<ul>\n<li>Values up to <strong>64 KB</strong> (JSON).</li>\n<li>Keys up to <strong>512 bytes</strong>.</li>\n<li><strong>10,000</strong> keys per canvas, <strong>1,000</strong> per user namespace.</li>\n</ul>\n<p>Exceeding a limit throws a <code>QuotaExceededError</code>. A disabled capability throws\n<code>CapabilityDisabledError</code>. See <a href=\"/docs/api/errors\">error codes</a>.</p>\n<p>The underlying HTTP endpoints are documented in the\n<a href=\"/docs/api/runtime-api\">Runtime API</a>.</p>\n",
    "headings": [
      {
        "id": "shared",
        "text": "Shared",
        "level": 2
      },
      {
        "id": "per-viewer",
        "text": "Per-viewer",
        "level": 2
      },
      {
        "id": "limits",
        "text": "Limits",
        "level": 2
      }
    ],
    "text": "Key–value storage canvasdrop.kv is JSON key–value storage scoped to the canvas. Two namespaces: shared (canvas-global) and user (auto-scoped to the signed-in viewer). Shared await canvasdrop. kv . set ( \"votes\" , 0 ); const n = await canvasdrop. kv . get ( \"votes\" ); // value, or null if absent const total = await canvasdrop. kv . increment ( \"votes\" ); // atomic +1 await canvasdrop. kv . delete ( \"votes\" ); const { entries, nextCursor } = await canvasdrop. kv . list ({ prefix : \"p:\" , limit : 100 }); increment is atomic — safe for concurrent polls, counters, and votes. Per-viewer await canvasdrop. kv . user . set ( \"pref\" , \"dark\" ); const pref = await canvasdrop. kv . user . get ( \"pref\" ); The user namespace is keyed by the signed-in viewer, so each person reads and writes their own values without the canvas managing identity. Limits Values up to 64 KB (JSON). Keys up to 512 bytes . 10,000 keys per canvas, 1,000 per user namespace. Exceeding a limit throws a QuotaExceededError . A disabled capability throws CapabilityDisabledError . See error codes . The underlying HTTP endpoints are documented in the Runtime API ."
  },
  {
    "path": "sdk/files",
    "section": "Browser SDK",
    "title": "File storage",
    "html": "<h1 id=\"file-storage\">File storage</h1>\n<p><code>canvasdrop.files</code> stores files for the canvas and serves them from a same-origin\nURL you can drop into <code>&lt;img&gt;</code> or <code>&lt;a&gt;</code>.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">const</span> f = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">files</span>.<span class=\"hljs-title function_\">upload</span>(input.<span class=\"hljs-property\">files</span>[<span class=\"hljs-number\">0</span>]); <span class=\"hljs-comment\">// { id, name, size, url }</span>\n<span class=\"hljs-keyword\">const</span> all = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">files</span>.<span class=\"hljs-title function_\">list</span>();\n<span class=\"hljs-keyword\">const</span> href = canvasdrop.<span class=\"hljs-property\">files</span>.<span class=\"hljs-title function_\">url</span>(f.<span class=\"hljs-property\">id</span>);                 <span class=\"hljs-comment\">// same-origin content URL</span>\n<span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">files</span>.<span class=\"hljs-title function_\">delete</span>(f.<span class=\"hljs-property\">id</span>);</code></pre>\n<h2 id=\"limits-and-safety\">Limits and safety</h2>\n<ul>\n<li><strong>25 MB</strong> per file, <strong>1 GB</strong> per canvas.</li>\n<li>Downloads are served as attachments; uploaded HTML/SVG is never rendered inline,\nso an uploaded file can't run as another viewer.</li>\n</ul>\n<p>Exceeding a limit throws a <code>QuotaExceededError</code>; a disabled capability throws\n<code>CapabilityDisabledError</code>. See <a href=\"/docs/api/errors\">error codes</a>.</p>\n<p>The underlying HTTP endpoints are documented in the\n<a href=\"/docs/api/runtime-api\">Runtime API</a>.</p>\n",
    "headings": [
      {
        "id": "limits-and-safety",
        "text": "Limits and safety",
        "level": 2
      }
    ],
    "text": "File storage canvasdrop.files stores files for the canvas and serves them from a same-origin URL you can drop into <img> or <a> . const f = await canvasdrop. files . upload (input. files [ 0 ]); // { id, name, size, url } const all = await canvasdrop. files . list (); const href = canvasdrop. files . url (f. id ); // same-origin content URL await canvasdrop. files . delete (f. id ); Limits and safety 25 MB per file, 1 GB per canvas. Downloads are served as attachments; uploaded HTML/SVG is never rendered inline, so an uploaded file can't run as another viewer. Exceeding a limit throws a QuotaExceededError ; a disabled capability throws CapabilityDisabledError . See error codes . The underlying HTTP endpoints are documented in the Runtime API ."
  },
  {
    "path": "sdk/identity",
    "section": "Browser SDK",
    "title": "Identity",
    "html": "<h1 id=\"identity\">Identity</h1>\n<p><code>canvasdrop.me()</code> returns the signed-in viewer. Identity comes from the\nserver-side session — the canvas never handles credentials.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">const</span> me = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-title function_\">me</span>(); <span class=\"hljs-comment\">// { id, email, name, avatarUrl }</span></code></pre>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>id</code></td>\n<td>Stable per-user id. Use this as a key, not the email.</td>\n</tr>\n<tr>\n<td><code>email</code></td>\n<td>The viewer's email.</td>\n</tr>\n<tr>\n<td><code>name</code></td>\n<td>Display name (may be empty for some providers).</td>\n</tr>\n<tr>\n<td><code>avatarUrl</code></td>\n<td>Avatar URL, or <code>null</code> when the provider gives none.</td>\n</tr>\n</tbody></table>\n<p>Identity is effective whenever <strong>Backend</strong> is on for the canvas. If the viewer is\nnot signed in, <code>me()</code> throws <code>NotAuthenticatedError</code> — though in normal use a\nviewer reaching a canvas has already been authenticated by the platform.</p>\n<p>Use <code>me().id</code> to scope per-user data — or just use the\n<a href=\"/docs/sdk/kv\">user KV namespace</a>, which scopes automatically.</p>\n",
    "headings": [],
    "text": "Identity canvasdrop.me() returns the signed-in viewer. Identity comes from the server-side session — the canvas never handles credentials. const me = await canvasdrop. me (); // { id, email, name, avatarUrl } Field Notes id Stable per-user id. Use this as a key, not the email. email The viewer's email. name Display name (may be empty for some providers). avatarUrl Avatar URL, or null when the provider gives none. Identity is effective whenever Backend is on for the canvas. If the viewer is not signed in, me() throws NotAuthenticatedError — though in normal use a viewer reaching a canvas has already been authenticated by the platform. Use me().id to scope per-user data — or just use the user KV namespace , which scopes automatically."
  },
  {
    "path": "sdk/ai",
    "section": "Browser SDK",
    "title": "AI",
    "html": "<h1 id=\"ai\">AI</h1>\n<p><code>canvasdrop.ai</code> runs model calls <strong>server-side</strong> so the provider key never lives\nin the canvas. The owner enables the AI capability, and the operator configures\nthe provider and per-user/per-canvas spend limits.</p>\n<h2 id=\"complete\">Complete</h2>\n<p>Accumulate a full response:</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">const</span> { text, usage } = <span class=\"hljs-keyword\">await</span> canvasdrop.<span class=\"hljs-property\">ai</span>.<span class=\"hljs-title function_\">complete</span>(\n  [{ <span class=\"hljs-attr\">role</span>: <span class=\"hljs-string\">\"user\"</span>, <span class=\"hljs-attr\">content</span>: <span class=\"hljs-string\">\"Summarise this poll result in one line.\"</span> }],\n  { <span class=\"hljs-attr\">model</span>: <span class=\"hljs-string\">\"&lt;configured-model&gt;\"</span> },\n);</code></pre>\n<h2 id=\"stream\">Stream</h2>\n<p>Iterate tokens as they arrive:</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">for</span> <span class=\"hljs-title function_\">await</span> (<span class=\"hljs-keyword\">const</span> chunk <span class=\"hljs-keyword\">of</span> canvasdrop.<span class=\"hljs-property\">ai</span>.<span class=\"hljs-title function_\">stream</span>(messages, { model })) {\n  output.<span class=\"hljs-property\">textContent</span> += chunk;\n}</code></pre>\n<h2 id=\"limits-and-errors\">Limits and errors</h2>\n<ul>\n<li>Spend is metered per user and per canvas; exceeding a budget throws\n<code>QuotaExceededError</code> (<code>code: \"QUOTA_EXCEEDED\"</code>).</li>\n<li>If a stream ends before completion you get <code>AI_STREAM_TRUNCATED</code>.</li>\n<li>A disabled capability throws <code>CapabilityDisabledError</code>.</li>\n</ul>\n<p>See <a href=\"/docs/api/errors\">error codes</a> and the\n<a href=\"/docs/api/runtime-api\">Runtime API</a> for the underlying endpoint.</p>\n",
    "headings": [
      {
        "id": "complete",
        "text": "Complete",
        "level": 2
      },
      {
        "id": "stream",
        "text": "Stream",
        "level": 2
      },
      {
        "id": "limits-and-errors",
        "text": "Limits and errors",
        "level": 2
      }
    ],
    "text": "AI canvasdrop.ai runs model calls server-side so the provider key never lives in the canvas. The owner enables the AI capability, and the operator configures the provider and per-user/per-canvas spend limits. Complete Accumulate a full response: const { text, usage } = await canvasdrop. ai . complete ( [{ role : \"user\" , content : \"Summarise this poll result in one line.\" }], { model : \"<configured-model>\" }, ); Stream Iterate tokens as they arrive: for await ( const chunk of canvasdrop. ai . stream (messages, { model })) { output. textContent += chunk; } Limits and errors Spend is metered per user and per canvas; exceeding a budget throws QuotaExceededError ( code: \"QUOTA_EXCEEDED\" ). If a stream ends before completion you get AI_STREAM_TRUNCATED . A disabled capability throws CapabilityDisabledError . See error codes and the Runtime API for the underlying endpoint."
  },
  {
    "path": "sdk/realtime",
    "section": "Browser SDK",
    "title": "Realtime",
    "html": "<h1 id=\"realtime\">Realtime</h1>\n<p><code>canvasdrop.realtime</code> is ephemeral pub/sub with presence — for live cursors,\nreactions, and collaborative demos. Messages are not persisted; they fan out to\nwhoever is connected to a channel right now.</p>\n<pre><code class=\"hljs language-js\"><span class=\"hljs-keyword\">const</span> channel = canvasdrop.<span class=\"hljs-property\">realtime</span>.<span class=\"hljs-title function_\">channel</span>(<span class=\"hljs-string\">\"room-1\"</span>);\n\nchannel.<span class=\"hljs-title function_\">subscribe</span>(<span class=\"hljs-function\">(<span class=\"hljs-params\">msg</span>) =&gt;</span> {\n  <span class=\"hljs-variable language_\">console</span>.<span class=\"hljs-title function_\">log</span>(msg.<span class=\"hljs-property\">event</span>, msg.<span class=\"hljs-property\">data</span>);\n});\n\nchannel.<span class=\"hljs-title function_\">publish</span>(<span class=\"hljs-string\">\"reaction\"</span>, { <span class=\"hljs-attr\">emoji</span>: <span class=\"hljs-string\">\"🎉\"</span> });\n\n<span class=\"hljs-keyword\">const</span> who = <span class=\"hljs-keyword\">await</span> channel.<span class=\"hljs-title function_\">presence</span>(); <span class=\"hljs-comment\">// RealtimeUser[] currently in the channel</span>\n\nchannel.<span class=\"hljs-title function_\">unsubscribe</span>();\nchannel.<span class=\"hljs-title function_\">close</span>(); <span class=\"hljs-comment\">// closes the shared socket when no channels remain</span></code></pre>\n<h2 id=\"notes\">Notes</h2>\n<ul>\n<li>Channels share one WebSocket per canvas; <code>close()</code> releases it when the last\nchannel goes away.</li>\n<li>Realtime is bounded: during an outage a high-frequency publisher's buffer is\ncapped rather than growing without limit.</li>\n<li>A disabled capability surfaces as <code>CapabilityDisabledError</code>.</li>\n</ul>\n<p>See <a href=\"/docs/api/errors\">error codes</a> and the\n<a href=\"/docs/api/runtime-api\">Runtime API</a>.</p>\n",
    "headings": [
      {
        "id": "notes",
        "text": "Notes",
        "level": 2
      }
    ],
    "text": "Realtime canvasdrop.realtime is ephemeral pub/sub with presence — for live cursors, reactions, and collaborative demos. Messages are not persisted; they fan out to whoever is connected to a channel right now. const channel = canvasdrop. realtime . channel ( \"room-1\" ); channel. subscribe ( ( msg ) => { console . log (msg. event , msg. data ); }); channel. publish ( \"reaction\" , { emoji : \"🎉\" }); const who = await channel. presence (); // RealtimeUser[] currently in the channel channel. unsubscribe (); channel. close (); // closes the shared socket when no channels remain Notes Channels share one WebSocket per canvas; close() releases it when the last channel goes away. Realtime is bounded: during an outage a high-frequency publisher's buffer is capped rather than growing without limit. A disabled capability surfaces as CapabilityDisabledError . See error codes and the Runtime API ."
  },
  {
    "path": "api/deploy-api",
    "section": "API reference",
    "title": "Deploy API",
    "html": "<h1 id=\"deploy-api\">Deploy API</h1>\n<p>The deploy API ships canvases over HTTP — from CI, a script, or an AI agent — with\n<strong>no dashboard session</strong>. It authenticates with a per-canvas <strong>API key</strong> (a Bearer\ntoken), and a key operates only on its own canvas.</p>\n<blockquote>\n<p><strong>Auth:</strong> <code>Authorization: Bearer &lt;canvas-key&gt;</code>. This is distinct from the\nsession-cookie auth the <a href=\"/docs/api/runtime-api\">Runtime API</a> and the browser SDK\nuse. The deploy API is mounted ahead of the session gateway so agents need no\norg login.</p>\n</blockquote>\n<p>Base path: <code>{base}/v1/canvases</code>.</p>\n<h2 id=\"deploy-a-version\">Deploy a version</h2>\n<pre><code class=\"hljs\">PUT {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}/deploy\nAuthorization: Bearer &lt;canvas-key&gt;\nContent-Type: application/zip\n&lt;zip body&gt;</code></pre>\n<p>Uploads a ZIP (with <code>index.html</code> at its root), creates a new version, and points\nthe canvas at it. Returns the new version metadata. Oversized bodies are rejected.</p>\n<pre><code class=\"hljs language-bash\">curl -X PUT <span class=\"hljs-string\">\"{base}/v1/canvases/{id}/deploy\"</span> \\\n  -H <span class=\"hljs-string\">\"Authorization: Bearer <span class=\"hljs-variable\">$CANVAS_KEY</span>\"</span> \\\n  --data-binary @site.zip</code></pre>\n<h2 id=\"get-a-canvas\">Get a canvas</h2>\n<pre><code class=\"hljs\">GET {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}\nAuthorization: Bearer &lt;canvas-key&gt;</code></pre>\n<p>Returns the canvas's current state (slug, current version, status).</p>\n<h2 id=\"list-versions\">List versions</h2>\n<pre><code class=\"hljs\">GET {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}/versions\nAuthorization: Bearer &lt;canvas-key&gt;</code></pre>\n<p>Returns the version history, newest first.</p>\n<h2 id=\"roll-back\">Roll back</h2>\n<pre><code class=\"hljs\">POST {base}/v1/canvases/{<span class=\"hljs-built_in\">id</span>}/rollback\nAuthorization: Bearer &lt;canvas-key&gt;</code></pre>\n<p>Points the canvas back at a previous version.</p>\n<h2 id=\"errors\">Errors</h2>\n<p>Failures return a stable machine-readable <code>code</code> (see <a href=\"/docs/api/errors\">Error codes</a>).\nA key used against a canvas it does not own gets <code>NOT_FOUND</code> (no existence leak).</p>\n",
    "headings": [
      {
        "id": "deploy-a-version",
        "text": "Deploy a version",
        "level": 2
      },
      {
        "id": "get-a-canvas",
        "text": "Get a canvas",
        "level": 2
      },
      {
        "id": "list-versions",
        "text": "List versions",
        "level": 2
      },
      {
        "id": "roll-back",
        "text": "Roll back",
        "level": 2
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      }
    ],
    "text": "Deploy API The deploy API ships canvases over HTTP — from CI, a script, or an AI agent — with no dashboard session . It authenticates with a per-canvas API key (a Bearer token), and a key operates only on its own canvas. Auth: Authorization: Bearer <canvas-key> . This is distinct from the session-cookie auth the Runtime API and the browser SDK use. The deploy API is mounted ahead of the session gateway so agents need no org login. Base path: {base}/v1/canvases . Deploy a version PUT {base}/v1/canvases/{ id }/deploy Authorization: Bearer <canvas-key> Content-Type: application/zip <zip body> Uploads a ZIP (with index.html at its root), creates a new version, and points the canvas at it. Returns the new version metadata. Oversized bodies are rejected. curl -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip Get a canvas GET {base}/v1/canvases/{ id } Authorization: Bearer <canvas-key> Returns the canvas's current state (slug, current version, status). List versions GET {base}/v1/canvases/{ id }/versions Authorization: Bearer <canvas-key> Returns the version history, newest first. Roll back POST {base}/v1/canvases/{ id }/rollback Authorization: Bearer <canvas-key> Points the canvas back at a previous version. Errors Failures return a stable machine-readable code (see Error codes ). A key used against a canvas it does not own gets NOT_FOUND (no existence leak)."
  },
  {
    "path": "api/runtime-api",
    "section": "API reference",
    "title": "Runtime API",
    "html": "<h1 id=\"runtime-api\">Runtime API</h1>\n<p>The runtime API is what the <a href=\"/docs/sdk/overview\">browser SDK</a> calls from inside a\ncanvas. You normally use the SDK rather than these endpoints directly, but they're\ndocumented here for completeness.</p>\n<blockquote>\n<p><strong>Auth:</strong> the <strong>session cookie</strong>, sent automatically by the browser. Requests are\ncredentialed and identity is resolved server-side — the canvas never asserts who\nthe viewer is. Each endpoint is gated by its capability; a disabled capability\nreturns <code>CAPABILITY_DISABLED</code>. (This differs from the Bearer-key\n<a href=\"/docs/api/deploy-api\">Deploy API</a>.)</p>\n<p>The SDK script itself, <code>{base}/sdk/v1.js</code>, is served to signed-in canvas pages —\nit requires a session, unlike the public deploy API.</p>\n</blockquote>\n<p>Base path: <code>{base}/v1/c/{slug}</code>.</p>\n<h2 id=\"identity\">Identity</h2>\n<pre><code class=\"hljs\">GET {base}/v1/c/{slug}/me        → { <span class=\"hljs-built_in\">id</span>, email, name, avatarUrl }</code></pre>\n<h2 id=\"key-value\">Key–value</h2>\n<p>Shared namespace under <code>/kv</code>, per-viewer namespace under <code>/kv/user</code> (the server\nforces the caller's id as scope — never client-supplied).</p>\n<pre><code class=\"hljs\">GET    {base}/v1/c/{slug}/kv?prefix=&amp;<span class=\"hljs-built_in\">limit</span>=     list\nGET    {base}/v1/c/{slug}/kv/{key}              <span class=\"hljs-built_in\">read</span> (null <span class=\"hljs-keyword\">if</span> absent)\nPUT    {base}/v1/c/{slug}/kv/{key}              write\nDELETE {base}/v1/c/{slug}/kv/{key}              delete</code></pre>\n<p><code>increment</code> is exposed as an atomic write on a numeric key. The same paths exist\nunder <code>/kv/user/...</code> for the per-viewer namespace.</p>\n<h2 id=\"files\">Files</h2>\n<pre><code class=\"hljs\">POST   {base}/v1/c/{slug}/files                 upload (multipart form, field <span class=\"hljs-string\">\"file\"</span>)\nGET    {base}/v1/c/{slug}/files                 list\nGET    {base}/v1/c/{slug}/files/{<span class=\"hljs-built_in\">id</span>}/content    download (served as attachment)\nDELETE {base}/v1/c/{slug}/files/{<span class=\"hljs-built_in\">id</span>}            delete</code></pre>\n<h2 id=\"ai\">AI</h2>\n<pre><code class=\"hljs\">POST   {base}/v1/c/{slug}/ai                    chat completion (SSE stream)</code></pre>\n<p>The provider key is server-side only; spend is metered per user and per canvas.</p>\n<h2 id=\"realtime\">Realtime</h2>\n<pre><code class=\"hljs\">WS     {base}/v1/c/{slug}/realtime              ephemeral pub/sub + presence</code></pre>\n<h2 id=\"errors\">Errors</h2>\n<p>All endpoints return stable <code>code</code> values — see <a href=\"/docs/api/errors\">Error codes</a>.</p>\n",
    "headings": [
      {
        "id": "identity",
        "text": "Identity",
        "level": 2
      },
      {
        "id": "key-value",
        "text": "Key–value",
        "level": 2
      },
      {
        "id": "files",
        "text": "Files",
        "level": 2
      },
      {
        "id": "ai",
        "text": "AI",
        "level": 2
      },
      {
        "id": "realtime",
        "text": "Realtime",
        "level": 2
      },
      {
        "id": "errors",
        "text": "Errors",
        "level": 2
      }
    ],
    "text": "Runtime API The runtime API is what the browser SDK calls from inside a canvas. You normally use the SDK rather than these endpoints directly, but they're documented here for completeness. Auth: the session cookie , sent automatically by the browser. Requests are credentialed and identity is resolved server-side — the canvas never asserts who the viewer is. Each endpoint is gated by its capability; a disabled capability returns CAPABILITY_DISABLED . (This differs from the Bearer-key Deploy API .) The SDK script itself, {base}/sdk/v1.js , is served to signed-in canvas pages — it requires a session, unlike the public deploy API. Base path: {base}/v1/c/{slug} . Identity GET {base}/v1/c/{slug}/me → { id , email, name, avatarUrl } Key–value Shared namespace under /kv , per-viewer namespace under /kv/user (the server forces the caller's id as scope — never client-supplied). GET {base}/v1/c/{slug}/kv?prefix=& limit = list GET {base}/v1/c/{slug}/kv/{key} read (null if absent) PUT {base}/v1/c/{slug}/kv/{key} write DELETE {base}/v1/c/{slug}/kv/{key} delete increment is exposed as an atomic write on a numeric key. The same paths exist under /kv/user/... for the per-viewer namespace. Files POST {base}/v1/c/{slug}/files upload (multipart form, field \"file\" ) GET {base}/v1/c/{slug}/files list GET {base}/v1/c/{slug}/files/{ id }/content download (served as attachment) DELETE {base}/v1/c/{slug}/files/{ id } delete AI POST {base}/v1/c/{slug}/ai chat completion (SSE stream) The provider key is server-side only; spend is metered per user and per canvas. Realtime WS {base}/v1/c/{slug}/realtime ephemeral pub/sub + presence Errors All endpoints return stable code values — see Error codes ."
  },
  {
    "path": "api/errors",
    "section": "API reference",
    "title": "Error codes",
    "html": "<h1 id=\"error-codes\">Error codes</h1>\n<p>Every failure from the runtime and deploy APIs carries a stable, machine-readable\n<code>code</code> and an HTTP <code>status</code>. The browser SDK maps these to typed errors extending\n<code>CanvasdropError</code> (each with a <code>.code</code> and <code>.status</code>). Branch on <code>err.code</code> rather\nthan on message text.</p>\n<p>This table is the canonical set; it is kept in lockstep with the SDK's exported\n<code>ERROR_CODES</code>.</p>\n<table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Status</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NOT_AUTHENTICATED</code></td>\n<td>401</td>\n<td>The viewer is not signed in.</td>\n</tr>\n<tr>\n<td><code>PASSWORD_REQUIRED</code></td>\n<td>401</td>\n<td>The canvas is password-protected.</td>\n</tr>\n<tr>\n<td><code>CAPABILITY_DISABLED</code></td>\n<td>403</td>\n<td>Backend or the specific feature is off for this canvas.</td>\n</tr>\n<tr>\n<td><code>CROSS_CANVAS_FORBIDDEN</code></td>\n<td>403</td>\n<td>A request targeted another canvas's resources.</td>\n</tr>\n<tr>\n<td><code>MODEL_NOT_ALLOWED</code></td>\n<td>403</td>\n<td>The requested AI model is not in the allow-list.</td>\n</tr>\n<tr>\n<td><code>DISABLED</code></td>\n<td>403</td>\n<td>The canvas has been disabled by an administrator.</td>\n</tr>\n<tr>\n<td><code>NOT_FOUND</code></td>\n<td>404</td>\n<td>The key, file, or canvas does not exist.</td>\n</tr>\n<tr>\n<td><code>INVALID_BODY</code></td>\n<td>400</td>\n<td>The request body failed validation.</td>\n</tr>\n<tr>\n<td><code>KEY_TOO_LARGE</code></td>\n<td>413</td>\n<td>The KV key exceeds the size limit.</td>\n</tr>\n<tr>\n<td><code>VALUE_TOO_LARGE</code></td>\n<td>413</td>\n<td>The KV value or file exceeds the size limit.</td>\n</tr>\n<tr>\n<td><code>KEY_LIMIT</code></td>\n<td>409</td>\n<td>The canvas hit its key-count limit.</td>\n</tr>\n<tr>\n<td><code>NOT_NUMERIC</code></td>\n<td>409</td>\n<td><code>increment</code> was called on a non-numeric value.</td>\n</tr>\n<tr>\n<td><code>QUOTA_EXCEEDED</code></td>\n<td>429</td>\n<td>A spend or rate quota was exceeded.</td>\n</tr>\n<tr>\n<td><code>CONNECTION_LIMIT</code></td>\n<td>429</td>\n<td>Too many concurrent realtime connections.</td>\n</tr>\n<tr>\n<td><code>AI_STREAM_TRUNCATED</code></td>\n<td>502</td>\n<td>An AI stream ended before completion.</td>\n</tr>\n<tr>\n<td><code>AI_UPSTREAM_ERROR</code></td>\n<td>502</td>\n<td>The AI provider returned an error.</td>\n</tr>\n<tr>\n<td><code>REQUEST_FAILED</code></td>\n<td>—</td>\n<td>A request failed without a more specific code.</td>\n</tr>\n</tbody></table>\n<h2 id=\"typed-sdk-errors\">Typed SDK errors</h2>\n<table>\n<thead>\n<tr>\n<th>Class</th>\n<th>Codes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NotAuthenticatedError</code></td>\n<td><code>NOT_AUTHENTICATED</code></td>\n</tr>\n<tr>\n<td><code>CapabilityDisabledError</code></td>\n<td><code>CAPABILITY_DISABLED</code></td>\n</tr>\n<tr>\n<td><code>NotFoundError</code></td>\n<td><code>NOT_FOUND</code></td>\n</tr>\n<tr>\n<td><code>QuotaExceededError</code></td>\n<td><code>QUOTA_EXCEEDED</code>, <code>KEY_LIMIT</code>, <code>VALUE_TOO_LARGE</code>, <code>CONNECTION_LIMIT</code></td>\n</tr>\n<tr>\n<td><code>CanvasdropError</code></td>\n<td>any other code (base class)</td>\n</tr>\n</tbody></table>\n",
    "headings": [
      {
        "id": "typed-sdk-errors",
        "text": "Typed SDK errors",
        "level": 2
      }
    ],
    "text": "Error codes Every failure from the runtime and deploy APIs carries a stable, machine-readable code and an HTTP status . The browser SDK maps these to typed errors extending CanvasdropError (each with a .code and .status ). Branch on err.code rather than on message text. This table is the canonical set; it is kept in lockstep with the SDK's exported ERROR_CODES . Code Status Meaning NOT_AUTHENTICATED 401 The viewer is not signed in. PASSWORD_REQUIRED 401 The canvas is password-protected. CAPABILITY_DISABLED 403 Backend or the specific feature is off for this canvas. CROSS_CANVAS_FORBIDDEN 403 A request targeted another canvas's resources. MODEL_NOT_ALLOWED 403 The requested AI model is not in the allow-list. DISABLED 403 The canvas has been disabled by an administrator. NOT_FOUND 404 The key, file, or canvas does not exist. INVALID_BODY 400 The request body failed validation. KEY_TOO_LARGE 413 The KV key exceeds the size limit. VALUE_TOO_LARGE 413 The KV value or file exceeds the size limit. KEY_LIMIT 409 The canvas hit its key-count limit. NOT_NUMERIC 409 increment was called on a non-numeric value. QUOTA_EXCEEDED 429 A spend or rate quota was exceeded. CONNECTION_LIMIT 429 Too many concurrent realtime connections. AI_STREAM_TRUNCATED 502 An AI stream ended before completion. AI_UPSTREAM_ERROR 502 The AI provider returned an error. REQUEST_FAILED — A request failed without a more specific code. Typed SDK errors Class Codes NotAuthenticatedError NOT_AUTHENTICATED CapabilityDisabledError CAPABILITY_DISABLED NotFoundError NOT_FOUND QuotaExceededError QUOTA_EXCEEDED , KEY_LIMIT , VALUE_TOO_LARGE , CONNECTION_LIMIT CanvasdropError any other code (base class)"
  },
  {
    "path": "self-hosting/install",
    "section": "Self-hosting",
    "title": "Install",
    "html": "<h1 id=\"install\">Install</h1>\n<p>canvas-drop is 12-factor and self-hostable. The simplest way to try it is locally\nwith the bundled dev profile.</p>\n<h2 id=\"local-dev-profile\">Local (dev profile)</h2>\n<pre><code class=\"hljs language-bash\">git <span class=\"hljs-built_in\">clone</span> &lt;your-fork-or-the-repo&gt;\n<span class=\"hljs-built_in\">cd</span> canvas-drop\npnpm install\n<span class=\"hljs-built_in\">cp</span> .env.example .<span class=\"hljs-built_in\">env</span>\npnpm dev</code></pre>\n<p>That boots a logged-in instance on <code>localhost</code> using the dev profile:\npath URLs, SQLite, local file storage, and <code>dev</code> auth (auto-login as a dev user).\nOpen the dashboard and deploy your first canvas — see the\n<a href=\"/docs/quickstart\">Quickstart</a>.</p>\n<h2 id=\"what-you-choose-at-install-time\">What you choose at install time</h2>\n<p>Everything swappable is behind an interface, selected by config — never a code\nchange:</p>\n<ul>\n<li><strong>Database</strong> — SQLite (default) or Postgres.</li>\n<li><strong>Storage</strong> — local filesystem (default) or S3-compatible.</li>\n<li><strong>URL mode</strong> — path (<code>{base}/c/{slug}</code>) or subdomain (<code>{slug}.{base}</code>).</li>\n<li><strong>Auth</strong> — <code>dev</code> (local), <code>proxy</code> (an identity-aware proxy in front), or <code>oidc</code>\n(built-in OpenID Connect login).</li>\n</ul>\n<p>See <a href=\"/docs/self-hosting/configuration\">Configuration</a> for the env vars, the\n<a href=\"/docs/self-hosting/security-model\">Security model</a> for the trade-offs, and\n<a href=\"/docs/self-hosting/deploy\">Deploy</a> for a production setup.</p>\n",
    "headings": [
      {
        "id": "local-dev-profile",
        "text": "Local (dev profile)",
        "level": 2
      },
      {
        "id": "what-you-choose-at-install-time",
        "text": "What you choose at install time",
        "level": 2
      }
    ],
    "text": "Install canvas-drop is 12-factor and self-hostable. The simplest way to try it is locally with the bundled dev profile. Local (dev profile) git clone <your-fork-or-the-repo> cd canvas-drop pnpm install cp .env.example . env pnpm dev That boots a logged-in instance on localhost using the dev profile: path URLs, SQLite, local file storage, and dev auth (auto-login as a dev user). Open the dashboard and deploy your first canvas — see the Quickstart . What you choose at install time Everything swappable is behind an interface, selected by config — never a code change: Database — SQLite (default) or Postgres. Storage — local filesystem (default) or S3-compatible. URL mode — path ( {base}/c/{slug} ) or subdomain ( {slug}.{base} ). Auth — dev (local), proxy (an identity-aware proxy in front), or oidc (built-in OpenID Connect login). See Configuration for the env vars, the Security model for the trade-offs, and Deploy for a production setup."
  },
  {
    "path": "self-hosting/configuration",
    "section": "Self-hosting",
    "title": "Configuration",
    "html": "<h1 id=\"configuration\">Configuration</h1>\n<p>canvas-drop is configured entirely by environment variables, validated at boot. A\nsingle config module is the only reader of the environment; everything else takes\ntyped config. Misconfiguration fails loudly at startup rather than at request time.</p>\n<p>This is a tour of the load-bearing settings — see <code>.env.example</code> in the repo for\nthe full, commented list.</p>\n<h2 id=\"core\">Core</h2>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_BASE_URL</code></td>\n<td>The instance's public base URL.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_PORT</code></td>\n<td>Port to listen on.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_URL_MODE</code></td>\n<td><code>path</code> or <code>subdomain</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_SESSION_SECRET</code></td>\n<td>Secret for session signing (set a strong value in prod).</td>\n</tr>\n</tbody></table>\n<h2 id=\"auth\">Auth</h2>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_AUTH_MODE</code></td>\n<td><code>dev</code>, <code>proxy</code>, or <code>oidc</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_ALLOWED_EMAIL_DOMAINS</code></td>\n<td>Restrict sign-in to these domains.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_ADMIN_EMAILS</code></td>\n<td>Who gets the admin surface.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_TRUSTED_PROXY_IPS</code></td>\n<td>In <code>proxy</code> mode, the only peers allowed to assert identity.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_OIDC_ISSUER</code> / <code>_CLIENT_ID</code> / <code>_CLIENT_SECRET</code></td>\n<td><code>oidc</code> provider settings.</td>\n</tr>\n</tbody></table>\n<h2 id=\"database-amp-storage\">Database &amp; storage</h2>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_DB</code></td>\n<td><code>sqlite</code> or <code>postgres</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_DATABASE_URL</code></td>\n<td>Postgres connection string.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_STORAGE</code></td>\n<td><code>local</code> or <code>s3</code>.</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_S3_*</code></td>\n<td>S3 endpoint, bucket, region, credentials.</td>\n</tr>\n</tbody></table>\n<h2 id=\"ai-optional\">AI (optional)</h2>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CANVAS_DROP_AI_API_KEY</code></td>\n<td>Provider key (server-side only; unset disables AI).</td>\n</tr>\n<tr>\n<td><code>CANVAS_DROP_AI_USER_DAILY_USD</code> / <code>_CANVAS_MONTHLY_USD</code></td>\n<td>Spend caps.</td>\n</tr>\n</tbody></table>\n<blockquote>\n<p>All examples use placeholder values. Never commit real secrets — set them in\nyour deployment environment (systemd, container env, secrets manager).</p>\n</blockquote>\n",
    "headings": [
      {
        "id": "core",
        "text": "Core",
        "level": 2
      },
      {
        "id": "auth",
        "text": "Auth",
        "level": 2
      },
      {
        "id": "database-amp-storage",
        "text": "Database & storage",
        "level": 2
      },
      {
        "id": "ai-optional",
        "text": "AI (optional)",
        "level": 2
      }
    ],
    "text": "Configuration canvas-drop is configured entirely by environment variables, validated at boot. A single config module is the only reader of the environment; everything else takes typed config. Misconfiguration fails loudly at startup rather than at request time. This is a tour of the load-bearing settings — see .env.example in the repo for the full, commented list. Core Variable Purpose CANVAS_DROP_BASE_URL The instance's public base URL. CANVAS_DROP_PORT Port to listen on. CANVAS_DROP_URL_MODE path or subdomain . CANVAS_DROP_SESSION_SECRET Secret for session signing (set a strong value in prod). Auth Variable Purpose CANVAS_DROP_AUTH_MODE dev , proxy , or oidc . CANVAS_DROP_ALLOWED_EMAIL_DOMAINS Restrict sign-in to these domains. CANVAS_DROP_ADMIN_EMAILS Who gets the admin surface. CANVAS_DROP_TRUSTED_PROXY_IPS In proxy mode, the only peers allowed to assert identity. CANVAS_DROP_OIDC_ISSUER / _CLIENT_ID / _CLIENT_SECRET oidc provider settings. Database & storage Variable Purpose CANVAS_DROP_DB sqlite or postgres . CANVAS_DROP_DATABASE_URL Postgres connection string. CANVAS_DROP_STORAGE local or s3 . CANVAS_DROP_S3_* S3 endpoint, bucket, region, credentials. AI (optional) Variable Purpose CANVAS_DROP_AI_API_KEY Provider key (server-side only; unset disables AI). CANVAS_DROP_AI_USER_DAILY_USD / _CANVAS_MONTHLY_USD Spend caps. All examples use placeholder values. Never commit real secrets — set them in your deployment environment (systemd, container env, secrets manager)."
  },
  {
    "path": "self-hosting/security-model",
    "section": "Self-hosting",
    "title": "Security model",
    "html": "<h1 id=\"security-model\">Security model</h1>\n<p>canvas-drop hosts arbitrary, sometimes AI-generated, web artifacts for a trusted\norganization. The model is honest about what each mode guarantees.</p>\n<h2 id=\"identity-is-always-server-side\">Identity is always server-side</h2>\n<p>Identity comes from the server-side auth context, never from the client. In\n<code>proxy</code> mode only the trusted proxy may assert identity — verified by JWT, or by\ntrusting headers solely from <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code>. A canvas can <em>ask</em>\nfor a capability; the server decides per request.</p>\n<h2 id=\"no-secrets-in-the-browser\">No secrets in the browser</h2>\n<p>AI provider keys and canvas API keys are server-side only. Canvas files never\ncontain a key. The browser SDK rides the session cookie.</p>\n<h2 id=\"path-mode-vs-subdomain-mode\">Path mode vs subdomain mode</h2>\n<p>This is the most important deployment choice:</p>\n<ul>\n<li><strong>Path mode</strong> (<code>{base}/c/{slug}</code>) — all canvases share one origin with each\nother and with the dashboard. A malicious or XSS'd canvas could issue\nsame-origin requests toward other canvases' client-side state. <strong>Fine for\nlocalhost and trusted single-user hosting.</strong> Multi-user path mode must be opted\ninto explicitly (<code>CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true</code>) and shows an\nadmin warning.</li>\n<li><strong>Subdomain mode</strong> (<code>{slug}.{base}</code>) — each canvas is its own origin, so the\nbrowser isolates them. <strong>This is the blessed production profile</strong>, fronted by an\nidentity-aware proxy with wildcard TLS.</li>\n</ul>\n<h2 id=\"fronting-with-an-identity-aware-proxy\">Fronting with an identity-aware proxy</h2>\n<p>The blessed production setup is subdomain mode + a proxy that handles auth (JWT)\nand TLS. If you don't run such a proxy, the built-in <code>oidc</code> mode is the documented\nfallback so you're never forced to stand one up just to try the product.</p>\n<h2 id=\"reducing-canvas-xss-blast-radius\">Reducing canvas XSS blast radius</h2>\n<p>Subdomain mode contains the blast radius of a compromised canvas; private-by-\ndefault limits exposure. Canvas authors should prefer <code>textContent</code> over\n<code>innerHTML</code>.</p>\n",
    "headings": [
      {
        "id": "identity-is-always-server-side",
        "text": "Identity is always server-side",
        "level": 2
      },
      {
        "id": "no-secrets-in-the-browser",
        "text": "No secrets in the browser",
        "level": 2
      },
      {
        "id": "path-mode-vs-subdomain-mode",
        "text": "Path mode vs subdomain mode",
        "level": 2
      },
      {
        "id": "fronting-with-an-identity-aware-proxy",
        "text": "Fronting with an identity-aware proxy",
        "level": 2
      },
      {
        "id": "reducing-canvas-xss-blast-radius",
        "text": "Reducing canvas XSS blast radius",
        "level": 2
      }
    ],
    "text": "Security model canvas-drop hosts arbitrary, sometimes AI-generated, web artifacts for a trusted organization. The model is honest about what each mode guarantees. Identity is always server-side Identity comes from the server-side auth context, never from the client. In proxy mode only the trusted proxy may assert identity — verified by JWT, or by trusting headers solely from CANVAS_DROP_TRUSTED_PROXY_IPS . A canvas can ask for a capability; the server decides per request. No secrets in the browser AI provider keys and canvas API keys are server-side only. Canvas files never contain a key. The browser SDK rides the session cookie. Path mode vs subdomain mode This is the most important deployment choice: Path mode ( {base}/c/{slug} ) — all canvases share one origin with each other and with the dashboard. A malicious or XSS'd canvas could issue same-origin requests toward other canvases' client-side state. Fine for localhost and trusted single-user hosting. Multi-user path mode must be opted into explicitly ( CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true ) and shows an admin warning. Subdomain mode ( {slug}.{base} ) — each canvas is its own origin, so the browser isolates them. This is the blessed production profile , fronted by an identity-aware proxy with wildcard TLS. Fronting with an identity-aware proxy The blessed production setup is subdomain mode + a proxy that handles auth (JWT) and TLS. If you don't run such a proxy, the built-in oidc mode is the documented fallback so you're never forced to stand one up just to try the product. Reducing canvas XSS blast radius Subdomain mode contains the blast radius of a compromised canvas; private-by- default limits exposure. Canvas authors should prefer textContent over innerHTML ."
  },
  {
    "path": "self-hosting/deploy",
    "section": "Self-hosting",
    "title": "Deploy",
    "html": "<h1 id=\"deploy\">Deploy</h1>\n<p>A production canvas-drop runs as a single app image, fronted by an identity-aware\nreverse proxy that terminates TLS and asserts identity.</p>\n<h2 id=\"blessed-production-profile\">Blessed production profile</h2>\n<ul>\n<li><strong>URL mode:</strong> subdomain (<code>{slug}.{base}</code>) — origin isolation per canvas.</li>\n<li><strong>Auth:</strong> <code>proxy</code> with JWT, behind an identity-aware proxy / IAP.</li>\n<li><strong>Database:</strong> Postgres.</li>\n<li><strong>Storage:</strong> S3-compatible.</li>\n<li><strong>TLS:</strong> wildcard cert at the proxy (covers <code>*.{base}</code>).</li>\n</ul>\n<p>Any Docker host works — a small VPS, a PaaS, or Kubernetes. Cost on a single modest\nVPS is intended to stay low.</p>\n<h2 id=\"shape\">Shape</h2>\n<pre><code class=\"hljs\">            ┌─────────────────────────┐\n  client ──▶│ <span class=\"hljs-keyword\">identity</span>-aware proxy/IAP │  TLS, asserts <span class=\"hljs-keyword\">identity</span> (JWT <span class=\"hljs-keyword\">header</span>)\n            └────────────┬────────────┘\n                         ▼\n                 canvas-<span class=\"hljs-keyword\">drop</span> app  ── Postgres\n                                   └ S3-compatible <span class=\"hljs-keyword\">storage</span></code></pre>\n<p>The proxy's egress IPs go in <code>CANVAS_DROP_TRUSTED_PROXY_IPS</code> so only it may assert\nidentity. See the <a href=\"/docs/self-hosting/security-model\">Security model</a>.</p>\n<h2 id=\"backups\">Backups</h2>\n<p>Nightly <code>pg_dump</code> (or a SQLite snapshot) plus object-store versioning.</p>\n<h2 id=\"without-a-proxy\">Without a proxy</h2>\n<p>If you don't run an identity-aware proxy, use the built-in <code>oidc</code> auth mode — point\nit at your OpenID provider via <code>CANVAS_DROP_OIDC_*</code>. You still get real auth without\nstanding up extra infrastructure.</p>\n<p>See <a href=\"/docs/self-hosting/configuration\">Configuration</a> for the full env surface.</p>\n",
    "headings": [
      {
        "id": "blessed-production-profile",
        "text": "Blessed production profile",
        "level": 2
      },
      {
        "id": "shape",
        "text": "Shape",
        "level": 2
      },
      {
        "id": "backups",
        "text": "Backups",
        "level": 2
      },
      {
        "id": "without-a-proxy",
        "text": "Without a proxy",
        "level": 2
      }
    ],
    "text": "Deploy A production canvas-drop runs as a single app image, fronted by an identity-aware reverse proxy that terminates TLS and asserts identity. Blessed production profile URL mode: subdomain ( {slug}.{base} ) — origin isolation per canvas. Auth: proxy with JWT, behind an identity-aware proxy / IAP. Database: Postgres. Storage: S3-compatible. TLS: wildcard cert at the proxy (covers *.{base} ). Any Docker host works — a small VPS, a PaaS, or Kubernetes. Cost on a single modest VPS is intended to stay low. Shape ┌─────────────────────────┐ client ──▶│ identity -aware proxy/IAP │ TLS, asserts identity (JWT header ) └────────────┬────────────┘ ▼ canvas- drop app ── Postgres └ S3-compatible storage The proxy's egress IPs go in CANVAS_DROP_TRUSTED_PROXY_IPS so only it may assert identity. See the Security model . Backups Nightly pg_dump (or a SQLite snapshot) plus object-store versioning. Without a proxy If you don't run an identity-aware proxy, use the built-in oidc auth mode — point it at your OpenID provider via CANVAS_DROP_OIDC_* . You still get real auth without standing up extra infrastructure. See Configuration for the full env surface."
  },
  {
    "path": "agents/llms",
    "section": "For agents",
    "title": "llms.txt",
    "html": "<h1 id=\"llms-txt\">llms.txt</h1>\n<p>canvas-drop serves a single agent-optimized reference at\n<a href=\"/llms.txt\"><code>{base}/llms.txt</code></a> — plain text, no markup chrome, designed to be\ndropped into an LLM's context.</p>\n<p>It is <strong>public</strong> (readable without a session) so an agent can learn the API before\nit has credentials, and it carries the essentials:</p>\n<ul>\n<li>What a canvas is and how to deploy one.</li>\n<li>The deploy API (Bearer-key, agent-usable from day one).</li>\n<li>The browser SDK surface (<code>canvasdrop.kv</code>, <code>files</code>, <code>me</code>, <code>ai</code>, <code>realtime</code>).</li>\n<li>The capability model and the stable error codes.</li>\n</ul>\n<h2 id=\"for-agents\">For agents</h2>\n<ol>\n<li>Read <a href=\"/llms.txt\"><code>/llms.txt</code></a> for the contract.</li>\n<li>Obtain a per-canvas API key (from the canvas owner / dashboard).</li>\n<li>Deploy with <code>PUT {base}/v1/canvases/{id}/deploy</code> — see the\n<a href=\"/docs/api/deploy-api\">Deploy API</a>.</li>\n<li>Use the <a href=\"/docs/sdk/overview\">SDK</a> inside the canvas for backend capability.</li>\n</ol>\n<p>For a packaged, installable version of this guidance, see the\n<a href=\"/docs/agents/skill\">Agent skill</a>.</p>\n",
    "headings": [
      {
        "id": "for-agents",
        "text": "For agents",
        "level": 2
      }
    ],
    "text": "llms.txt canvas-drop serves a single agent-optimized reference at {base}/llms.txt — plain text, no markup chrome, designed to be dropped into an LLM's context. It is public (readable without a session) so an agent can learn the API before it has credentials, and it carries the essentials: What a canvas is and how to deploy one. The deploy API (Bearer-key, agent-usable from day one). The browser SDK surface ( canvasdrop.kv , files , me , ai , realtime ). The capability model and the stable error codes. For agents Read /llms.txt for the contract. Obtain a per-canvas API key (from the canvas owner / dashboard). Deploy with PUT {base}/v1/canvases/{id}/deploy — see the Deploy API . Use the SDK inside the canvas for backend capability. For a packaged, installable version of this guidance, see the Agent skill ."
  },
  {
    "path": "agents/skill",
    "section": "For agents",
    "title": "Agent skill",
    "html": "<h1 id=\"agent-skill\">Agent skill</h1>\n<p>canvas-drop ships an installable agent skill that teaches a coding agent to deploy\nand extend canvases against your instance. It follows the Claude-skill /\n<code>AGENTS.md</code> conventions.</p>\n<h2 id=\"get-it\">Get it</h2>\n<p>Download the packaged skill:</p>\n<pre><code class=\"hljs\"><span class=\"hljs-built_in\">GET</span> {base}/skill.zip</code></pre>\n<p>It contains a <code>SKILL.md</code> (when-to-use + the deploy/SDK workflow) and runnable\nexamples. The archive is public — no session required — so an agent can fetch it\ndirectly.</p>\n<h2 id=\"what-it-covers\">What it covers</h2>\n<ul>\n<li>Obtaining and using a per-canvas API key.</li>\n<li>Deploying with the <a href=\"/docs/api/deploy-api\">Deploy API</a>.</li>\n<li>Using the <a href=\"/docs/sdk/overview\">browser SDK</a> primitives inside a canvas.</li>\n<li>The zero-secrets rule: never put a provider or canvas key in canvas files.</li>\n</ul>\n<h2 id=\"install\">Install</h2>\n<p>Unzip into your agent's skills directory (or point your agent at the unpacked\n<code>SKILL.md</code>). The skill is self-contained and references this instance's API by its\nbase URL.</p>\n<p>For the lighter-weight, copy-into-context version, use\n<a href=\"/llms.txt\"><code>/llms.txt</code></a>.</p>\n",
    "headings": [
      {
        "id": "get-it",
        "text": "Get it",
        "level": 2
      },
      {
        "id": "what-it-covers",
        "text": "What it covers",
        "level": 2
      },
      {
        "id": "install",
        "text": "Install",
        "level": 2
      }
    ],
    "text": "Agent skill canvas-drop ships an installable agent skill that teaches a coding agent to deploy and extend canvases against your instance. It follows the Claude-skill / AGENTS.md conventions. Get it Download the packaged skill: GET {base}/skill.zip It contains a SKILL.md (when-to-use + the deploy/SDK workflow) and runnable examples. The archive is public — no session required — so an agent can fetch it directly. What it covers Obtaining and using a per-canvas API key. Deploying with the Deploy API . Using the browser SDK primitives inside a canvas. The zero-secrets rule: never put a provider or canvas key in canvas files. Install Unzip into your agent's skills directory (or point your agent at the unpacked SKILL.md ). The skill is self-contained and references this instance's API by its base URL. For the lighter-weight, copy-into-context version, use /llms.txt ."
  }
];

export const DOC_NAV: NavSection[] = [
  {
    "section": "Getting started",
    "pages": [
      {
        "path": "",
        "title": "Overview"
      },
      {
        "path": "quickstart",
        "title": "Quickstart"
      }
    ]
  },
  {
    "section": "Authoring canvases",
    "pages": [
      {
        "path": "authoring/create-and-deploy",
        "title": "Create & deploy"
      },
      {
        "path": "authoring/capabilities",
        "title": "Capabilities"
      },
      {
        "path": "authoring/editor",
        "title": "The editor"
      }
    ]
  },
  {
    "section": "Browser SDK",
    "pages": [
      {
        "path": "sdk/overview",
        "title": "Overview"
      },
      {
        "path": "sdk/kv",
        "title": "Key–value storage"
      },
      {
        "path": "sdk/files",
        "title": "File storage"
      },
      {
        "path": "sdk/identity",
        "title": "Identity"
      },
      {
        "path": "sdk/ai",
        "title": "AI"
      },
      {
        "path": "sdk/realtime",
        "title": "Realtime"
      }
    ]
  },
  {
    "section": "API reference",
    "pages": [
      {
        "path": "api/deploy-api",
        "title": "Deploy API"
      },
      {
        "path": "api/runtime-api",
        "title": "Runtime API"
      },
      {
        "path": "api/errors",
        "title": "Error codes"
      }
    ]
  },
  {
    "section": "Self-hosting",
    "pages": [
      {
        "path": "self-hosting/install",
        "title": "Install"
      },
      {
        "path": "self-hosting/configuration",
        "title": "Configuration"
      },
      {
        "path": "self-hosting/security-model",
        "title": "Security model"
      },
      {
        "path": "self-hosting/deploy",
        "title": "Deploy"
      }
    ]
  },
  {
    "section": "For agents",
    "pages": [
      {
        "path": "agents/llms",
        "title": "llms.txt"
      },
      {
        "path": "agents/skill",
        "title": "Agent skill"
      }
    ]
  }
];

export const SEARCH_INDEX: SearchEntry[] = [
  {
    "path": "",
    "title": "Overview",
    "headings": [
      "What you can build",
      "How it fits together",
      "Where to go next"
    ],
    "text": "canvas-drop canvas-drop is an open-source, self-hostable platform where members of an organization deploy and share small web artifacts — canvases . A canvas is just static files (HTML, CSS, JS, images). Drop them in and they're live at a URL your colleagues can open. The constraint is the product: a small set of primitives, done well, instead of a general-purpose hosting platform. Canvases run with no build step and no secrets in the page, and they gain backend capability only through five primitives exposed by a zero-config browser SDK. What you can build Prototypes, dashboards, demos, microsites, small games, internal tools — anything that communicates better as a working artifact than as a screenshot or a slide. How it fits together Deploy a canvas by dragging a folder/ZIP into the dashboard, pasting HTML, using the in-browser editor, or calling the deploy API (agents can ship without a human in the loop). Add backend capability with the browser SDK : key–value storage, file storage, the signed-in viewer's identity, AI, and realtime — each gated by a capability the canvas owner turns on. Share the URL. Access follows your organization's sign-in. Where to go next New here? Start with the Quickstart . Building a canvas with a backend? Read the SDK overview . Running your own instance? See Self-hosting → Install . An AI agent? Read /llms.txt and the agent skill . Examples and URLs in these docs use {base} (your instance's base URL) and localhost placeholders. Substitute your own instance's address."
  },
  {
    "path": "quickstart",
    "title": "Quickstart",
    "headings": [
      "1. Create a canvas",
      "2. Add some content",
      "3. Give it a backend (optional)",
      "4. Publish & share",
      "Deploying as an agent"
    ],
    "text": "Quickstart Deploy your first canvas in a few minutes. 1. Create a canvas From the dashboard, click New . You can: Drag a folder or ZIP containing an index.html . Paste HTML for a quick single-page canvas. Start from the editor and write files in the browser. Each canvas gets a slug and a URL. In path mode that's {base}/c/{slug}/ ; in subdomain mode it's {slug}.{base} . 2. Add some content A canvas is static files. The simplest possible canvas is one index.html : <!doctype html > < html > < body > < h1 > Hello from my canvas </ h1 > </ body > </ html > No build step runs on the server — what you deploy is what's served. 3. Give it a backend (optional) Add the SDK and you get storage, identity, and more with no keys in the page: < script src = \"/sdk/v1.js\" > </ script > < script > const me = await canvasdrop. me (); await canvasdrop. kv . increment ( \"views\" ); </ script > The owner enables Backend (and the specific feature) in the canvas's Capabilities tab first. See the SDK overview . 4. Publish & share Publish a version and share the canvas URL. Who can open it follows your organization's sign-in — canvases are private to your org by default. Deploying as an agent Agents deploy over HTTP with a per-canvas API key — no dashboard session needed. See the Deploy API and /llms.txt ."
  },
  {
    "path": "authoring/create-and-deploy",
    "title": "Create & deploy",
    "headings": [
      "Drag-and-drop a folder or ZIP",
      "Paste HTML",
      "In-browser editor",
      "Deploy API",
      "Versions and rollback"
    ],
    "text": "Create & deploy There are four ways to get a canvas live. All produce the same thing: a versioned set of static files served at the canvas URL. Drag-and-drop a folder or ZIP The fastest path for an existing project. Drop a folder (or a .zip ) that contains an index.html at its root. The files are uploaded, a version is created, and the canvas goes live. Paste HTML For a one-file canvas, paste HTML directly. canvas-drop wraps it into an index.html and deploys it. In-browser editor Create files and edit them with syntax highlighting in the browser. Your work is saved as a draft ; you choose when to publish a version. See The editor . Deploy API Ship from CI or an agent with a per-canvas API key over HTTP — no human and no dashboard session required: curl -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip See the Deploy API reference . Versions and rollback Every deploy creates a new version. The canvas always serves its currently published version, and you can roll back to a previous one from the dashboard or the API. Versions are content-addressed, so re-deploying identical files is cheap."
  },
  {
    "path": "authoring/capabilities",
    "title": "Capabilities",
    "headings": [
      "The switch hierarchy",
      "The five primitives",
      "Why off by default"
    ],
    "text": "Capabilities A canvas is static by default. To give it backend behaviour, the owner turns on capabilities in the canvas's Capabilities tab. Capabilities are off until explicitly enabled, and a method whose capability is off throws a CapabilityDisabledError . The switch hierarchy A feature is only effective when three things are all true: Backend is on for the canvas (the master switch). The specific feature (KV, files, identity, AI, realtime) is on. The operator has not globally disabled that feature for the instance. Identity is effective whenever Backend is on. The five primitives Capability What it gives the canvas SDK Key–value Shared and per-viewer JSON storage, atomic increment kv Files Upload, list, serve files files Identity The signed-in viewer's id, email, name identity AI Server-side model calls, no provider key in the page ai Realtime Ephemeral pub/sub + presence realtime Why off by default No secrets ever live in canvas files. Capabilities are server-enforced per request from the signed-in session — the canvas can ask, but the server decides. Turning a capability on is a deliberate choice the owner makes per canvas."
  },
  {
    "path": "authoring/editor",
    "title": "The editor",
    "headings": [
      "Drafts and publishing",
      "Files",
      "Versions"
    ],
    "text": "The editor The in-browser editor lets you create and edit a canvas's files with syntax highlighting — no local setup, no build step. Drafts and publishing Editing works on a draft : a working copy separate from the live version. Your changes autosave to the draft as you go. The live canvas keeps serving its published version until you explicitly publish . This gives you a safe place to iterate: Edit freely without affecting what viewers see. Preview the draft. Publish when ready — that creates a new version and points the canvas at it. Files Add, rename, and delete files in the file tree. index.html at the root is the canvas's entry point. Binary assets (images, fonts) are supported alongside text files. Versions Each publish is a version. From the Versions view you can see history and roll back. Storage is content-addressed, so unchanged files across versions are stored once. See also Create & deploy for the non-editor paths (drag-drop, paste, API)."
  },
  {
    "path": "sdk/overview",
    "title": "Overview",
    "headings": [
      "Add it to a canvas",
      "Enable the capability first",
      "The surface",
      "Errors"
    ],
    "text": "Browser SDK The browser SDK gives a canvas backend capability — key–value storage, file storage, the signed-in viewer's identity, AI, and realtime — with no build step and no secrets in the canvas . Identity comes from the signed-in session; the canvas is identified by its own URL. Add it to a canvas < script src = \"/sdk/v1.js\" > </ script > That defines the global window.canvasdrop . The SDK auto-detects the canvas slug and the API base from the page's location (path mode /c/{slug}/… or subdomain {slug}.{base} ) and sends every request with the session cookie. /sdk/v1.js is served to signed-in canvas pages — it rides the same session as the canvas. The stable /sdk/v1.js path is additive and backward-compatible within v1, so pointing a <script> at it means you receive fixes (including security patches) without redeploying. A breaking change would ship under a new path ( /sdk/v2.js ). Enable the capability first The canvas owner must turn on Backend (and the specific feature) in the canvas's Capabilities tab. A method whose capability is off throws a CapabilityDisabledError . See Capabilities . The surface canvasdrop.me() — the signed-in viewer. canvasdrop.kv — shared and per-viewer key–value storage. canvasdrop.files — file upload, list, serve. canvasdrop.ai — server-side model calls. canvasdrop.realtime — pub/sub + presence. Errors Every failure throws a typed error extending CanvasdropError , each carrying a stable .code and .status . See the error code reference . try { await canvasdrop. kv . increment ( \"votes\" ); } catch (err) { if (err. code === \"CAPABILITY_DISABLED\" ) { // ask the owner to enable KV in the Capabilities tab } else { throw err; } } If you import @canvas-drop/sdk as a module instead of the global script, the error classes are exported for instanceof checks."
  },
  {
    "path": "sdk/kv",
    "title": "Key–value storage",
    "headings": [
      "Shared",
      "Per-viewer",
      "Limits"
    ],
    "text": "Key–value storage canvasdrop.kv is JSON key–value storage scoped to the canvas. Two namespaces: shared (canvas-global) and user (auto-scoped to the signed-in viewer). Shared await canvasdrop. kv . set ( \"votes\" , 0 ); const n = await canvasdrop. kv . get ( \"votes\" ); // value, or null if absent const total = await canvasdrop. kv . increment ( \"votes\" ); // atomic +1 await canvasdrop. kv . delete ( \"votes\" ); const { entries, nextCursor } = await canvasdrop. kv . list ({ prefix : \"p:\" , limit : 100 }); increment is atomic — safe for concurrent polls, counters, and votes. Per-viewer await canvasdrop. kv . user . set ( \"pref\" , \"dark\" ); const pref = await canvasdrop. kv . user . get ( \"pref\" ); The user namespace is keyed by the signed-in viewer, so each person reads and writes their own values without the canvas managing identity. Limits Values up to 64 KB (JSON). Keys up to 512 bytes . 10,000 keys per canvas, 1,000 per user namespace. Exceeding a limit throws a QuotaExceededError . A disabled capability throws CapabilityDisabledError . See error codes . The underlying HTTP endpoints are documented in the Runtime API ."
  },
  {
    "path": "sdk/files",
    "title": "File storage",
    "headings": [
      "Limits and safety"
    ],
    "text": "File storage canvasdrop.files stores files for the canvas and serves them from a same-origin URL you can drop into <img> or <a> . const f = await canvasdrop. files . upload (input. files [ 0 ]); // { id, name, size, url } const all = await canvasdrop. files . list (); const href = canvasdrop. files . url (f. id ); // same-origin content URL await canvasdrop. files . delete (f. id ); Limits and safety 25 MB per file, 1 GB per canvas. Downloads are served as attachments; uploaded HTML/SVG is never rendered inline, so an uploaded file can't run as another viewer. Exceeding a limit throws a QuotaExceededError ; a disabled capability throws CapabilityDisabledError . See error codes . The underlying HTTP endpoints are documented in the Runtime API ."
  },
  {
    "path": "sdk/identity",
    "title": "Identity",
    "headings": [],
    "text": "Identity canvasdrop.me() returns the signed-in viewer. Identity comes from the server-side session — the canvas never handles credentials. const me = await canvasdrop. me (); // { id, email, name, avatarUrl } Field Notes id Stable per-user id. Use this as a key, not the email. email The viewer's email. name Display name (may be empty for some providers). avatarUrl Avatar URL, or null when the provider gives none. Identity is effective whenever Backend is on for the canvas. If the viewer is not signed in, me() throws NotAuthenticatedError — though in normal use a viewer reaching a canvas has already been authenticated by the platform. Use me().id to scope per-user data — or just use the user KV namespace , which scopes automatically."
  },
  {
    "path": "sdk/ai",
    "title": "AI",
    "headings": [
      "Complete",
      "Stream",
      "Limits and errors"
    ],
    "text": "AI canvasdrop.ai runs model calls server-side so the provider key never lives in the canvas. The owner enables the AI capability, and the operator configures the provider and per-user/per-canvas spend limits. Complete Accumulate a full response: const { text, usage } = await canvasdrop. ai . complete ( [{ role : \"user\" , content : \"Summarise this poll result in one line.\" }], { model : \"<configured-model>\" }, ); Stream Iterate tokens as they arrive: for await ( const chunk of canvasdrop. ai . stream (messages, { model })) { output. textContent += chunk; } Limits and errors Spend is metered per user and per canvas; exceeding a budget throws QuotaExceededError ( code: \"QUOTA_EXCEEDED\" ). If a stream ends before completion you get AI_STREAM_TRUNCATED . A disabled capability throws CapabilityDisabledError . See error codes and the Runtime API for the underlying endpoint."
  },
  {
    "path": "sdk/realtime",
    "title": "Realtime",
    "headings": [
      "Notes"
    ],
    "text": "Realtime canvasdrop.realtime is ephemeral pub/sub with presence — for live cursors, reactions, and collaborative demos. Messages are not persisted; they fan out to whoever is connected to a channel right now. const channel = canvasdrop. realtime . channel ( \"room-1\" ); channel. subscribe ( ( msg ) => { console . log (msg. event , msg. data ); }); channel. publish ( \"reaction\" , { emoji : \"🎉\" }); const who = await channel. presence (); // RealtimeUser[] currently in the channel channel. unsubscribe (); channel. close (); // closes the shared socket when no channels remain Notes Channels share one WebSocket per canvas; close() releases it when the last channel goes away. Realtime is bounded: during an outage a high-frequency publisher's buffer is capped rather than growing without limit. A disabled capability surfaces as CapabilityDisabledError . See error codes and the Runtime API ."
  },
  {
    "path": "api/deploy-api",
    "title": "Deploy API",
    "headings": [
      "Deploy a version",
      "Get a canvas",
      "List versions",
      "Roll back",
      "Errors"
    ],
    "text": "Deploy API The deploy API ships canvases over HTTP — from CI, a script, or an AI agent — with no dashboard session . It authenticates with a per-canvas API key (a Bearer token), and a key operates only on its own canvas. Auth: Authorization: Bearer <canvas-key> . This is distinct from the session-cookie auth the Runtime API and the browser SDK use. The deploy API is mounted ahead of the session gateway so agents need no org login. Base path: {base}/v1/canvases . Deploy a version PUT {base}/v1/canvases/{ id }/deploy Authorization: Bearer <canvas-key> Content-Type: application/zip <zip body> Uploads a ZIP (with index.html at its root), creates a new version, and points the canvas at it. Returns the new version metadata. Oversized bodies are rejected. curl -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip Get a canvas GET {base}/v1/canvases/{ id } Authorization: Bearer <canvas-key> Returns the canvas's current state (slug, current version, status). List versions GET {base}/v1/canvases/{ id }/versions Authorization: Bearer <canvas-key> Returns the version history, newest first. Roll back POST {base}/v1/canvases/{ id }/rollback Authorization: Bearer <canvas-key> Points the canvas back at a previous version. Errors Failures return a stable machine-readable code (see Error codes ). A key used against a canvas it does not own gets NOT_FOUND (no existence leak)."
  },
  {
    "path": "api/runtime-api",
    "title": "Runtime API",
    "headings": [
      "Identity",
      "Key–value",
      "Files",
      "AI",
      "Realtime",
      "Errors"
    ],
    "text": "Runtime API The runtime API is what the browser SDK calls from inside a canvas. You normally use the SDK rather than these endpoints directly, but they're documented here for completeness. Auth: the session cookie , sent automatically by the browser. Requests are credentialed and identity is resolved server-side — the canvas never asserts who the viewer is. Each endpoint is gated by its capability; a disabled capability returns CAPABILITY_DISABLED . (This differs from the Bearer-key Deploy API .) The SDK script itself, {base}/sdk/v1.js , is served to signed-in canvas pages — it requires a session, unlike the public deploy API. Base path: {base}/v1/c/{slug} . Identity GET {base}/v1/c/{slug}/me → { id , email, name, avatarUrl } Key–value Shared namespace under /kv , per-viewer namespace under /kv/user (the server forces the caller's id as scope — never client-supplied). GET {base}/v1/c/{slug}/kv?prefix=& limit = list GET {base}/v1/c/{slug}/kv/{key} read (null if absent) PUT {base}/v1/c/{slug}/kv/{key} write DELETE {base}/v1/c/{slug}/kv/{key} delete increment is exposed as an atomic write on a numeric key. The same paths exist under /kv/user/... for the per-viewer namespace. Files POST {base}/v1/c/{slug}/files upload (multipart form, field \"file\" ) GET {base}/v1/c/{slug}/files list GET {base}/v1/c/{slug}/files/{ id }/content download (served as attachment) DELETE {base}/v1/c/{slug}/files/{ id } delete AI POST {base}/v1/c/{slug}/ai chat completion (SSE stream) The provider key is server-side only; spend is metered per user and per canvas. Realtime WS {base}/v1/c/{slug}/realtime ephemeral pub/sub + presence Errors All endpoints return stable code values — see Error codes ."
  },
  {
    "path": "api/errors",
    "title": "Error codes",
    "headings": [
      "Typed SDK errors"
    ],
    "text": "Error codes Every failure from the runtime and deploy APIs carries a stable, machine-readable code and an HTTP status . The browser SDK maps these to typed errors extending CanvasdropError (each with a .code and .status ). Branch on err.code rather than on message text. This table is the canonical set; it is kept in lockstep with the SDK's exported ERROR_CODES . Code Status Meaning NOT_AUTHENTICATED 401 The viewer is not signed in. PASSWORD_REQUIRED 401 The canvas is password-protected. CAPABILITY_DISABLED 403 Backend or the specific feature is off for this canvas. CROSS_CANVAS_FORBIDDEN 403 A request targeted another canvas's resources. MODEL_NOT_ALLOWED 403 The requested AI model is not in the allow-list. DISABLED 403 The canvas has been disabled by an administrator. NOT_FOUND 404 The key, file, or canvas does not exist. INVALID_BODY 400 The request body failed validation. KEY_TOO_LARGE 413 The KV key exceeds the size limit. VALUE_TOO_LARGE 413 The KV value or file exceeds the size limit. KEY_LIMIT 409 The canvas hit its key-count limit. NOT_NUMERIC 409 increment was called on a non-numeric value. QUOTA_EXCEEDED 429 A spend or rate quota was exceeded. CONNECTION_LIMIT 429 Too many concurrent realtime connections. AI_STREAM_TRUNCATED 502 An AI stream ended before completion. AI_UPSTREAM_ERROR 502 The AI provider returned an error. REQUEST_FAILED — A request failed without a more specific code. Typed SDK errors Class Codes NotAuthenticatedError NOT_AUTHENTICATED CapabilityDisabledError CAPABILITY_DISABLED NotFoundError NOT_FOUND QuotaExceededError QUOTA_EXCEEDED , KEY_LIMIT , VALUE_TOO_LARGE , CONNECTION_LIMIT CanvasdropError any other code (base class)"
  },
  {
    "path": "self-hosting/install",
    "title": "Install",
    "headings": [
      "Local (dev profile)",
      "What you choose at install time"
    ],
    "text": "Install canvas-drop is 12-factor and self-hostable. The simplest way to try it is locally with the bundled dev profile. Local (dev profile) git clone <your-fork-or-the-repo> cd canvas-drop pnpm install cp .env.example . env pnpm dev That boots a logged-in instance on localhost using the dev profile: path URLs, SQLite, local file storage, and dev auth (auto-login as a dev user). Open the dashboard and deploy your first canvas — see the Quickstart . What you choose at install time Everything swappable is behind an interface, selected by config — never a code change: Database — SQLite (default) or Postgres. Storage — local filesystem (default) or S3-compatible. URL mode — path ( {base}/c/{slug} ) or subdomain ( {slug}.{base} ). Auth — dev (local), proxy (an identity-aware proxy in front), or oidc (built-in OpenID Connect login). See Configuration for the env vars, the Security model for the trade-offs, and Deploy for a production setup."
  },
  {
    "path": "self-hosting/configuration",
    "title": "Configuration",
    "headings": [
      "Core",
      "Auth",
      "Database & storage",
      "AI (optional)"
    ],
    "text": "Configuration canvas-drop is configured entirely by environment variables, validated at boot. A single config module is the only reader of the environment; everything else takes typed config. Misconfiguration fails loudly at startup rather than at request time. This is a tour of the load-bearing settings — see .env.example in the repo for the full, commented list. Core Variable Purpose CANVAS_DROP_BASE_URL The instance's public base URL. CANVAS_DROP_PORT Port to listen on. CANVAS_DROP_URL_MODE path or subdomain . CANVAS_DROP_SESSION_SECRET Secret for session signing (set a strong value in prod). Auth Variable Purpose CANVAS_DROP_AUTH_MODE dev , proxy , or oidc . CANVAS_DROP_ALLOWED_EMAIL_DOMAINS Restrict sign-in to these domains. CANVAS_DROP_ADMIN_EMAILS Who gets the admin surface. CANVAS_DROP_TRUSTED_PROXY_IPS In proxy mode, the only peers allowed to assert identity. CANVAS_DROP_OIDC_ISSUER / _CLIENT_ID / _CLIENT_SECRET oidc provider settings. Database & storage Variable Purpose CANVAS_DROP_DB sqlite or postgres . CANVAS_DROP_DATABASE_URL Postgres connection string. CANVAS_DROP_STORAGE local or s3 . CANVAS_DROP_S3_* S3 endpoint, bucket, region, credentials. AI (optional) Variable Purpose CANVAS_DROP_AI_API_KEY Provider key (server-side only; unset disables AI). CANVAS_DROP_AI_USER_DAILY_USD / _CANVAS_MONTHLY_USD Spend caps. All examples use placeholder values. Never commit real secrets — set them in your deployment environment (systemd, container env, secrets manager)."
  },
  {
    "path": "self-hosting/security-model",
    "title": "Security model",
    "headings": [
      "Identity is always server-side",
      "No secrets in the browser",
      "Path mode vs subdomain mode",
      "Fronting with an identity-aware proxy",
      "Reducing canvas XSS blast radius"
    ],
    "text": "Security model canvas-drop hosts arbitrary, sometimes AI-generated, web artifacts for a trusted organization. The model is honest about what each mode guarantees. Identity is always server-side Identity comes from the server-side auth context, never from the client. In proxy mode only the trusted proxy may assert identity — verified by JWT, or by trusting headers solely from CANVAS_DROP_TRUSTED_PROXY_IPS . A canvas can ask for a capability; the server decides per request. No secrets in the browser AI provider keys and canvas API keys are server-side only. Canvas files never contain a key. The browser SDK rides the session cookie. Path mode vs subdomain mode This is the most important deployment choice: Path mode ( {base}/c/{slug} ) — all canvases share one origin with each other and with the dashboard. A malicious or XSS'd canvas could issue same-origin requests toward other canvases' client-side state. Fine for localhost and trusted single-user hosting. Multi-user path mode must be opted into explicitly ( CANVAS_DROP_ALLOW_MULTI_USER_PATH_MODE=true ) and shows an admin warning. Subdomain mode ( {slug}.{base} ) — each canvas is its own origin, so the browser isolates them. This is the blessed production profile , fronted by an identity-aware proxy with wildcard TLS. Fronting with an identity-aware proxy The blessed production setup is subdomain mode + a proxy that handles auth (JWT) and TLS. If you don't run such a proxy, the built-in oidc mode is the documented fallback so you're never forced to stand one up just to try the product. Reducing canvas XSS blast radius Subdomain mode contains the blast radius of a compromised canvas; private-by- default limits exposure. Canvas authors should prefer textContent over innerHTML ."
  },
  {
    "path": "self-hosting/deploy",
    "title": "Deploy",
    "headings": [
      "Blessed production profile",
      "Shape",
      "Backups",
      "Without a proxy"
    ],
    "text": "Deploy A production canvas-drop runs as a single app image, fronted by an identity-aware reverse proxy that terminates TLS and asserts identity. Blessed production profile URL mode: subdomain ( {slug}.{base} ) — origin isolation per canvas. Auth: proxy with JWT, behind an identity-aware proxy / IAP. Database: Postgres. Storage: S3-compatible. TLS: wildcard cert at the proxy (covers *.{base} ). Any Docker host works — a small VPS, a PaaS, or Kubernetes. Cost on a single modest VPS is intended to stay low. Shape ┌─────────────────────────┐ client ──▶│ identity -aware proxy/IAP │ TLS, asserts identity (JWT header ) └────────────┬────────────┘ ▼ canvas- drop app ── Postgres └ S3-compatible storage The proxy's egress IPs go in CANVAS_DROP_TRUSTED_PROXY_IPS so only it may assert identity. See the Security model . Backups Nightly pg_dump (or a SQLite snapshot) plus object-store versioning. Without a proxy If you don't run an identity-aware proxy, use the built-in oidc auth mode — point it at your OpenID provider via CANVAS_DROP_OIDC_* . You still get real auth without standing up extra infrastructure. See Configuration for the full env surface."
  },
  {
    "path": "agents/llms",
    "title": "llms.txt",
    "headings": [
      "For agents"
    ],
    "text": "llms.txt canvas-drop serves a single agent-optimized reference at {base}/llms.txt — plain text, no markup chrome, designed to be dropped into an LLM's context. It is public (readable without a session) so an agent can learn the API before it has credentials, and it carries the essentials: What a canvas is and how to deploy one. The deploy API (Bearer-key, agent-usable from day one). The browser SDK surface ( canvasdrop.kv , files , me , ai , realtime ). The capability model and the stable error codes. For agents Read /llms.txt for the contract. Obtain a per-canvas API key (from the canvas owner / dashboard). Deploy with PUT {base}/v1/canvases/{id}/deploy — see the Deploy API . Use the SDK inside the canvas for backend capability. For a packaged, installable version of this guidance, see the Agent skill ."
  },
  {
    "path": "agents/skill",
    "title": "Agent skill",
    "headings": [
      "Get it",
      "What it covers",
      "Install"
    ],
    "text": "Agent skill canvas-drop ships an installable agent skill that teaches a coding agent to deploy and extend canvases against your instance. It follows the Claude-skill / AGENTS.md conventions. Get it Download the packaged skill: GET {base}/skill.zip It contains a SKILL.md (when-to-use + the deploy/SDK workflow) and runnable examples. The archive is public — no session required — so an agent can fetch it directly. What it covers Obtaining and using a per-canvas API key. Deploying with the Deploy API . Using the browser SDK primitives inside a canvas. The zero-secrets rule: never put a provider or canvas key in canvas files. Install Unzip into your agent's skills directory (or point your agent at the unpacked SKILL.md ). The skill is self-contained and references this instance's API by its base URL. For the lighter-weight, copy-into-context version, use /llms.txt ."
  }
];

export const LLMS_TXT = "# canvas-drop — llms.txt\n\nAgent-readable reference for deploying and extending canvases. See {base}/docs for the full docs.\n\n## Overview\n\ncanvas-drop canvas-drop is an open-source, self-hostable platform where members of an organization deploy and share small web artifacts — canvases . A canvas is just static files (HTML, CSS, JS, images). Drop them in and they're live at a URL your colleagues can open. The constraint is the product: a small set of primitives, done well, instead of a general-purpose hosting platform. Canvases run with no build step and no secrets in the page, and they gain backend capability only through five primitives exposed by a zero-config browser SDK. What you can build Prototypes, dashboards, demos, microsites, small games, internal tools — anything that communicates better as a working artifact than as a screenshot or a slide. How it fits together Deploy a canvas by dragging a folder/ZIP into the dashboard, pasting HTML, using the in-browser editor, or calling the deploy API (agents can ship without a human in the loop). Add backend capability with the browser SDK : key–value storage, file storage, the signed-in viewer's identity, AI, and realtime — each gated by a capability the canvas owner turns on. Share the URL. Access follows your organization's sign-in. Where to go next New here? Start with the Quickstart . Building a canvas with a backend? Read the SDK overview . Running your own instance? See Self-hosting → Install . An AI agent? Read /llms.txt and the agent skill . Examples and URLs in these docs use {base} (your instance's base URL) and localhost placeholders. Substitute your own instance's address.\n\n## Quickstart\n\nQuickstart Deploy your first canvas in a few minutes. 1. Create a canvas From the dashboard, click New . You can: Drag a folder or ZIP containing an index.html . Paste HTML for a quick single-page canvas. Start from the editor and write files in the browser. Each canvas gets a slug and a URL. In path mode that's {base}/c/{slug}/ ; in subdomain mode it's {slug}.{base} . 2. Add some content A canvas is static files. The simplest possible canvas is one index.html : <!doctype html > < html > < body > < h1 > Hello from my canvas </ h1 > </ body > </ html > No build step runs on the server — what you deploy is what's served. 3. Give it a backend (optional) Add the SDK and you get storage, identity, and more with no keys in the page: < script src = \"/sdk/v1.js\" > </ script > < script > const me = await canvasdrop. me (); await canvasdrop. kv . increment ( \"views\" ); </ script > The owner enables Backend (and the specific feature) in the canvas's Capabilities tab first. See the SDK overview . 4. Publish & share Publish a version and share the canvas URL. Who can open it follows your organization's sign-in — canvases are private to your org by default. Deploying as an agent Agents deploy over HTTP with a per-canvas API key — no dashboard session needed. See the Deploy API and /llms.txt .\n\n## Capabilities\n\nCapabilities A canvas is static by default. To give it backend behaviour, the owner turns on capabilities in the canvas's Capabilities tab. Capabilities are off until explicitly enabled, and a method whose capability is off throws a CapabilityDisabledError . The switch hierarchy A feature is only effective when three things are all true: Backend is on for the canvas (the master switch). The specific feature (KV, files, identity, AI, realtime) is on. The operator has not globally disabled that feature for the instance. Identity is effective whenever Backend is on. The five primitives Capability What it gives the canvas SDK Key–value Shared and per-viewer JSON storage, atomic increment kv Files Upload, list, serve files files Identity The signed-in viewer's id, email, name identity AI Server-side model calls, no provider key in the page ai Realtime Ephemeral pub/sub + presence realtime Why off by default No secrets ever live in canvas files. Capabilities are server-enforced per request from the signed-in session — the canvas can ask, but the server decides. Turning a capability on is a deliberate choice the owner makes per canvas.\n\n## Overview\n\nBrowser SDK The browser SDK gives a canvas backend capability — key–value storage, file storage, the signed-in viewer's identity, AI, and realtime — with no build step and no secrets in the canvas . Identity comes from the signed-in session; the canvas is identified by its own URL. Add it to a canvas < script src = \"/sdk/v1.js\" > </ script > That defines the global window.canvasdrop . The SDK auto-detects the canvas slug and the API base from the page's location (path mode /c/{slug}/… or subdomain {slug}.{base} ) and sends every request with the session cookie. /sdk/v1.js is served to signed-in canvas pages — it rides the same session as the canvas. The stable /sdk/v1.js path is additive and backward-compatible within v1, so pointing a <script> at it means you receive fixes (including security patches) without redeploying. A breaking change would ship under a new path ( /sdk/v2.js ). Enable the capability first The canvas owner must turn on Backend (and the specific feature) in the canvas's Capabilities tab. A method whose capability is off throws a CapabilityDisabledError . See Capabilities . The surface canvasdrop.me() — the signed-in viewer. canvasdrop.kv — shared and per-viewer key–value storage. canvasdrop.files — file upload, list, serve. canvasdrop.ai — server-side model calls. canvasdrop.realtime — pub/sub + presence. Errors Every failure throws a typed error extending CanvasdropError , each carrying a stable .code and .status . See the error code reference . try { await canvasdrop. kv . increment ( \"votes\" ); } catch (err) { if (err. code === \"CAPABILITY_DISABLED\" ) { // ask the owner to enable KV in the Capabilities tab } else { throw err; } } If you import @canvas-drop/sdk as a module instead of the global script, the error classes are exported for instanceof checks.\n\n## Deploy API\n\nDeploy API The deploy API ships canvases over HTTP — from CI, a script, or an AI agent — with no dashboard session . It authenticates with a per-canvas API key (a Bearer token), and a key operates only on its own canvas. Auth: Authorization: Bearer <canvas-key> . This is distinct from the session-cookie auth the Runtime API and the browser SDK use. The deploy API is mounted ahead of the session gateway so agents need no org login. Base path: {base}/v1/canvases . Deploy a version PUT {base}/v1/canvases/{ id }/deploy Authorization: Bearer <canvas-key> Content-Type: application/zip <zip body> Uploads a ZIP (with index.html at its root), creates a new version, and points the canvas at it. Returns the new version metadata. Oversized bodies are rejected. curl -X PUT \"{base}/v1/canvases/{id}/deploy\" \\ -H \"Authorization: Bearer $CANVAS_KEY \" \\ --data-binary @site.zip Get a canvas GET {base}/v1/canvases/{ id } Authorization: Bearer <canvas-key> Returns the canvas's current state (slug, current version, status). List versions GET {base}/v1/canvases/{ id }/versions Authorization: Bearer <canvas-key> Returns the version history, newest first. Roll back POST {base}/v1/canvases/{ id }/rollback Authorization: Bearer <canvas-key> Points the canvas back at a previous version. Errors Failures return a stable machine-readable code (see Error codes ). A key used against a canvas it does not own gets NOT_FOUND (no existence leak).\n\n## Runtime API\n\nRuntime API The runtime API is what the browser SDK calls from inside a canvas. You normally use the SDK rather than these endpoints directly, but they're documented here for completeness. Auth: the session cookie , sent automatically by the browser. Requests are credentialed and identity is resolved server-side — the canvas never asserts who the viewer is. Each endpoint is gated by its capability; a disabled capability returns CAPABILITY_DISABLED . (This differs from the Bearer-key Deploy API .) The SDK script itself, {base}/sdk/v1.js , is served to signed-in canvas pages — it requires a session, unlike the public deploy API. Base path: {base}/v1/c/{slug} . Identity GET {base}/v1/c/{slug}/me → { id , email, name, avatarUrl } Key–value Shared namespace under /kv , per-viewer namespace under /kv/user (the server forces the caller's id as scope — never client-supplied). GET {base}/v1/c/{slug}/kv?prefix=& limit = list GET {base}/v1/c/{slug}/kv/{key} read (null if absent) PUT {base}/v1/c/{slug}/kv/{key} write DELETE {base}/v1/c/{slug}/kv/{key} delete increment is exposed as an atomic write on a numeric key. The same paths exist under /kv/user/... for the per-viewer namespace. Files POST {base}/v1/c/{slug}/files upload (multipart form, field \"file\" ) GET {base}/v1/c/{slug}/files list GET {base}/v1/c/{slug}/files/{ id }/content download (served as attachment) DELETE {base}/v1/c/{slug}/files/{ id } delete AI POST {base}/v1/c/{slug}/ai chat completion (SSE stream) The provider key is server-side only; spend is metered per user and per canvas. Realtime WS {base}/v1/c/{slug}/realtime ephemeral pub/sub + presence Errors All endpoints return stable code values — see Error codes .\n\n## Error codes\n\nError codes Every failure from the runtime and deploy APIs carries a stable, machine-readable code and an HTTP status . The browser SDK maps these to typed errors extending CanvasdropError (each with a .code and .status ). Branch on err.code rather than on message text. This table is the canonical set; it is kept in lockstep with the SDK's exported ERROR_CODES . Code Status Meaning NOT_AUTHENTICATED 401 The viewer is not signed in. PASSWORD_REQUIRED 401 The canvas is password-protected. CAPABILITY_DISABLED 403 Backend or the specific feature is off for this canvas. CROSS_CANVAS_FORBIDDEN 403 A request targeted another canvas's resources. MODEL_NOT_ALLOWED 403 The requested AI model is not in the allow-list. DISABLED 403 The canvas has been disabled by an administrator. NOT_FOUND 404 The key, file, or canvas does not exist. INVALID_BODY 400 The request body failed validation. KEY_TOO_LARGE 413 The KV key exceeds the size limit. VALUE_TOO_LARGE 413 The KV value or file exceeds the size limit. KEY_LIMIT 409 The canvas hit its key-count limit. NOT_NUMERIC 409 increment was called on a non-numeric value. QUOTA_EXCEEDED 429 A spend or rate quota was exceeded. CONNECTION_LIMIT 429 Too many concurrent realtime connections. AI_STREAM_TRUNCATED 502 An AI stream ended before completion. AI_UPSTREAM_ERROR 502 The AI provider returned an error. REQUEST_FAILED — A request failed without a more specific code. Typed SDK errors Class Codes NotAuthenticatedError NOT_AUTHENTICATED CapabilityDisabledError CAPABILITY_DISABLED NotFoundError NOT_FOUND QuotaExceededError QUOTA_EXCEEDED , KEY_LIMIT , VALUE_TOO_LARGE , CONNECTION_LIMIT CanvasdropError any other code (base class)\n";
